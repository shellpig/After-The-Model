extends Control
class_name FloatingToast

func show_toast(text: String) -> void:
	# Keep backwards compatibility stub
	show(text)

func show(text: String, anchor_node: Control = null) -> void:
	# Stub method matching the spec signature
	pass
