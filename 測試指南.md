# 測試指南

> 測試流程與手動驗收步驟清單。
> **撰寫時機**：各子階段的測試項目於**該階段規格定稿時即寫入**（與規格同期，acceptance-test 先行），供實作時據以建測試與自我驗收——不延到驗證時才補。
> **角色歸屬**：測試項目於設計定稿時寫入；之後由 verifier 維護與執行。實作者照清單建測試、不得放寬清單，不改本檔（只列建議）。

## 本檔範圍與邊界

- **規格書「驗收」= 意圖層**：什麼狀態必須為真（what must be true），不寫操作步驟。
- **本檔 = 操作層**：開哪個場景、按哪顆鍵、看什麼畫面（click-by-click）。
- 同一條斷言不抄兩份；本檔的每個清單項用 link 指回規格書對應「### 驗收」段。
- 規格定稿時，測試項目可能引用尚未定的 content（如 prompt 文字）；以「顯示其 prompt」等中性描述先寫，實作時填實際字樣。

## Godot 測試環境

外部工具路徑見 `AGENTS.md > 專案外部工具路徑`。

### Headless 驗證命令

> 已知限制：sandbox 內直接跑會因無法開 `user://logs/godot*.log` 而 crash（signal 11）。請用 escalated 權限執行，見 `AGENTS.md > Godot Headless 驗證`。

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . res://tests/manual/test_runner.tscn
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . -s res://tests/manual/verify_game_state.gd
```

現有測試檔：

```text
tests/manual/test_runner.tscn
tests/manual/test_runner.gd
tests/manual/verify_game_state.gd
```

## 手動驗收清單

### Phase 1（已收尾）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 2 — 公寓解謎

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 3 — 公寓觸控化（手機可玩）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 4 — 跨場景架構化（Main Host / GameUI / SceneRouter）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 5 — NPC + 對話系統（真系統）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 6 — SaveSystem（手動多槽存檔）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 7 — QuestManager + `alley_backrooms_3f` vertical slice

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 8 — 便利商店 vertical slice + 買賣系統

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 9 — 拾遺系統（殘響蒐集 + 收藏家）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 10 — 氛圍與演出（Atmosphere & Presentation pass）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

### Phase 11+ — 主線《雨還沒停》v2.3（順敘版）
> 驗收意圖見 `遊戲規格書.md > Phase 11+`；實作契約見 `開發設計方針.md > Phase 11+`；敘事事實來源見 `subdocs/主線/雨還沒停v2.3.md`。
> **順敘執行、不跳號**：逐 Phase 開工才把該 Phase 清單展開到完整操作步驟；以下為規劃層 checklist 骨架。
> 通則：每個 Phase 末子階段做存讀檔 round-trip；每個 Phase 都跑「Phase 1~10 + 已完成主線 Phase 不退化」回歸。戰鬥 / 跳躍 / 氛圍 / 結局演出「對不對」＝GUI / 真機目視，headless 僅護欄。

#### Phase 11 Trust/Trace + 清洗縫（headless）— as-built（test_runner Phase 11 區）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 12 跳躍 / 平台（headless / GUI / 觸控）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 13 戰鬥（headless / GUI / 觸控）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 14 Act 1 鉤子（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 15 Act 2 場景骨架（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 16 三張臉 NPC（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 17 Act 2A 碎片 + 殘響（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 18 Act 2B 戰鬥 + 回執（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 19 Act 2C 阿達罪責（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 20 Act 2D 七號可選 + 和平線 D（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 21 Act 3 夜總會 + 林霏核心殘響 + 主題歌（headless；原 Phase 22 已併入）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 22 — 已併入 Phase 21（編號保留不再使用）

#### Phase 23 Act 3 夜總會保全四解小解謎（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 24 七號小爆點三分支（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 25 Act 4 備份區 + 戰鬥③（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase 26 Act 4 演出 + 真相碎片 + 結局觸發點（headless / GUI）
> **狀態：✅ 已完成**（headless PASS，commits `2bc4da7` / `003db3b` / `d31f4a0`）。
> 契約見 `開發設計方針.md > Act 4 演出 + 真相碎片 + 結局觸發點（Phase 26，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 26`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §4.3 / §3 / §7 / §8 / §7.2。**前提硬規則**：不碰 Trace；不寫結局分支 / 不開結局選單（路由＝27-B）；④不得早於③；私心版不設 affinity 門檻；Branch B 小岑具體、其餘不具名、不點名伍姐／七號／林霏。**canonical 旗標**：`wan_act4_pull_seen` / `ada_final_words_seen` / `mem_frag_chose_deletion` / `stood_before_own_backup`（皆走 story_flags，零新 schema）。

- [ ] 26-A 晚拉扯：`datacenter_entrance` 門禁大門前走近自動觸發（§4.3 安全版＋私心版一次給足、無 affinity 門檻）；結束 set `wan_act4_pull_seen`；重入 / 旗標已設不重播；之後晚可 E retalk 短版；拉扯不擋門禁（看完即可過門）。
- [ ] 26-B 阿達④：缺 `read_old_work_order` 到場**不登場**（順序護欄④不早於③）；齊全首次到場自動觸發一拍短對話 → set `ada_final_words_seen` → 淡出永久消失；旗標已設重進場景不生成。
- [ ] 26-C 真相碎片：core 備份主機前必經自動收取 → messagebox（`MSG_MEM_FRAG_CHOSE_DELETION`）＋ set `mem_frag_chose_deletion`；重入不重複收取。
- [ ] 26-D 觸發點武裝：碎片**前**「自己的備份」examine＝25-A 中性佔位、**不** set 旗標；碎片**後**＝重量級文字（`MSG_DATACENTER_OWN_BACKUP_TRUTH`，名字露出一次）＋ set `stood_before_own_backup`（冪等、可重看、**不開選單**）。（註：「冪等、可重看」已被 27-B 取代——武裝後重看改開三選對話，見 Phase 27-B 清單。）
- [ ] 26-D 檔案索引終端：`seven_stopped_partial` false → 中性 flavor（`MSG_DATACENTER_FILE_INDEX_NEUTRAL`）；true → 變體（`MSG_DATACENTER_FILE_INDEX_REMARKED`）＝小岑聲紋檔標紅「待清理」＋未具名黑戶檔案重標記；兩版正確分派；文字不含伍姐／七號／林霏名。
- [ ] 26-E 存讀檔：四旗標 round-trip；26-A/B 一次性跨存讀不重播；`stood_before_own_backup` 存讀後保持（27-B 路由前置）。
- [ ] 26-E 回歸：Phase 1~25 不退化（尤其 25 travel / 門禁 gate、戰鬥③ 抵達門、24 追逐 / 攤牌、聚落往返）。
- [ ] 外部素材：**阿達世界 sprite（idle）＋對話立繪**（g2d 生成，26-B 動工前補；離場 fade 不需 walk）；其餘（晚 sprite / 立繪、Act 4 BGM）皆既有。
- [ ] GUI / 純觸控：同趟連吃阿達→晚兩拍節奏可接受；阿達淡出自然；碎片收取演出；備份 / 終端兩互動物文字各版正確；對話選項 / BtnE 純觸控可推進。

#### Phase 27 地下廣播站 + 結局路由 + 清洗閘（headless / GUI）
> **狀態：✅ 27-A~D 全部已完成**（27-A commit `7941519`；27-B/27-C/27-D 本次實作，headless 本機待跑）。
> 契約見 `開發設計方針.md > 地下廣播站 + 結局路由 + 清洗閘（Phase 27，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 27`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8 / §6 / §4.1。**前提硬規則**：三選無條件全開（不看 Trust / Trace，v2.3 §12 問題 3 拍板不硬鎖）；選定即鎖、確認拍前皆可退開；清洗＝單一 bool、不可逐項檢視；本 Phase 不碰 Trace / 不做 A/B/C 判定（29）/ 不做結局演出（28）。**canonical 旗標**：`ending_route_reclaim` / `ending_route_protect` / `ending_route_expose`（互斥）/ `expose_upload_cleaned` / `expose_upload_done` / `ending_save_hint_seen`（存檔提示去重，2026-07-03 拍板回補）（皆走 story_flags，零新 schema）。

- [x] 27-A `broadcast_station` 載入 + entry `from_backup_core` 落點正確；進場一次性 MessageBox（`MSG_BROADCAST_ARRIVAL`；讀檔回場不重播，6-B 慣例）；flavor examine 派發；**無回資料中心出口**；`can_save_here == true`。（headless PASS，commit `7941519`）
- [ ] 27-B 分派鏈四層：碎片前中性佔位 → TRUTH＋`stood_before_own_backup`（26-D 不變）→ 重看開三選 → 任一 route 旗標後 DECIDED 中性 examine 不可重選。（程式 + test_runner 已寫，本機 headless 待跑）
- [ ] 27-B 三選：頂層 / 確認層退開皆不寫 route 旗標、可重開；三路確認拍各自寫入互斥旗標（三旗標僅一真）；R / P 短收尾停原地；Expose 單向轉場 `broadcast_station`。（程式 + test_runner 已寫，本機 headless 待跑）
- [ ] 27-B 存檔提示：首次開三選先出提示拍（文案拍板版）＋推進 set `ending_save_hint_seen`；退開可當場存檔（`can_save_here == true`）；旗標已設重開直入重錨定不重播；跨存讀不重播。（程式 + test_runner 已寫，本機 headless 待跑；跨存讀 round-trip 屬 27-D 範圍尚未寫）
- [ ] 27-C 清洗閘五拍：`echo_count` 兩檔掃描變體、`seven_stopped_partial` 警告變體正確分派；清洗 / 原樣 → `expose_upload_cleaned` 二態正確＋`expose_upload_done`；**退開不留半套狀態**（旗標只在最終確認寫入）；上傳後終端 idle 變體不重開樹。（程式 + test_runner 已寫，本機 headless 待跑）
- [ ] 27-D 存讀檔：6 旗標＋`broadcast_station` 場景 round-trip（互斥性、清洗二態、存檔提示皆持久化、讀檔不重播進場 MessageBox、上傳終端 idle gate 持久化）；三選前留檔 → 讀檔可分別走三路（三結局路由皆可達、每次重讀提示皆不重播）；Phase 1~26 不退化（尤其 26 四旗標、25 travel / 門禁、戰鬥③、24 追逐 / 攤牌、聚落往返）＋ 27 廣播站新掛點。（程式 + test_runner 已寫，本機 headless 待跑）
- [x] 外部素材：已全數到貨（2026-07-02）——map `assets/generated/maps/broadcast_station/broadcast_station-20260702-234241.png` ＋ 廣播站 BGM `assets/bgm/The Yellow Light Tape.mp3`（user 委製）；無剩餘 blocker。
- [ ] GUI / 純觸控：三選對話與清洗閘對話 D-pad / BtnE 可推進；確認前小節重量拍節奏；轉場與進場 MessageBox 演出；新 BGM 切換自然。

#### Phase 28 Reclaim + Protect 結局演出（headless / GUI）
> 契約見 `開發設計方針.md > Reclaim + Protect 結局演出（Phase 28，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 28`；敘事事實來源 v2.3 §8（＋2026-07-03 加註）。**前提硬規則**：序列全自動＋全程禁存；Protect 台詞固定不染色；復駛＝背景事件不改聚落安全；七號不實體登場；不做共用收尾（30-A）；Trace 只讀不寫。**canonical 旗標**：`ending_reclaim_played` / `ending_protect_played`（story_flags，零新 schema）。

- [ ] 28-A Reclaim 序列：`ending_route_reclaim` → 三站鏈（backup_core 灌回多頁 → `apartment_entrance:epilogue_wan` 晚訣別＋CG 拍（`wan/cg_walk_into_rain`）永久消失 → `apartment:epilogue_home` 痕跡拍＋`ending_reclaim_played`）。
- [ ] 28-A 染色：`trace` 高低兩檔 → 壓垮拍 `MSG_EPILOGUE_RECLAIM_CRUSH_HIGH/_LOW` 二選正確；`affinity_wan >= 2` 兩檔 → 訣別長版 / 短冷版路由正確。
- [ ] 28-B Protect 序列：`ending_route_protect` → 刪除演出（一至兩頁，與 Reclaim 頁數不對稱）→ 28-C 中間站 → 晚固定三句、**不 fade 不消失** → `ending_protect_played`。
- [ ] 28-C 分岔：`cen_voiceprint_exposed` false → `subway_station:epilogue_cen`（廣播復駛拍＋小岑過閘 CG 拍（`cen/cg_gate_pass`）＋台詞「車要來了欸」）；true → `underground_settlement:epilogue_settlement`（`empty_tent`＋伍姐沉默搖頭拍）。
- [ ] 28-D 存讀檔：2 旗標 round-trip；序列各站 `can_save_here == false`；三選前留檔 → 讀檔分走 R / P（含 28-C 兩變體共 3 條路）；孤兒檔救援（route 已設未 played → 讀檔強制回 backup_core 開序列）；Phase 1~27 不退化。
- [ ] GUI / 純觸控：序列站間轉場節奏；灌回多頁 / 刪除短頁對比；晚 CG 拍後消失 / 留原地兩版；小岑 CG 拍顯示與關閉銜接（觸控可推進）；伍姐搖頭；`AmbientSubway` 遠轟切入自然；序列中暫停選單 Save 項正確禁用。

#### Phase 29 Edited Expose 三判定（headless / GUI）
> 契約見 `開發設計方針.md > Edited Expose 三判定（Phase 29，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 29`；敘事事實來源 v2.3 §8。**前提硬規則**：連續式無自由行動間隙；判定一次鎖死、30-A 不重算；C 收虛無不收懲罰；三條統一靜止停輸入不恢復；Trace 只讀不寫（經濟補正＝echo_db data）。**canonical 旗標**：`ending_expose_a/b/c_played`（互斥，story_flags，零新 schema）。

- [ ] 29-A 經濟補正：echo_db 全殘響 `trace_on_collect == 1`（補 4 條）；採集各 +1、重複採集 / 載入不重加；舊「其餘殘響無 `trace_on_collect`」護欄已更新。
- [ ] 29-A 框架＋A：上傳前留檔（未清洗 + trace < 3）→ 最終確認後自動接序列（無自由行動間隙）→ 上傳拍 → 警報拍 → CG 拍（`cg_settlement_burning`）→ 敘述終拍 → `ending_expose_a_played`；鎖點後 `can_save_here == false` 全程不恢復。
- [ ] 29-B：已清洗 + trace < 3 → 雜訊收尾（無 CG、頁數少）→ `ending_expose_b_played`。
- [ ] 29-C：trace >= 3（**未清洗 / 已清洗兩組合皆**）→ 99% 攔截 → 抹除收黑／白噪 → `ending_expose_c_played`（C 優先於 A/B 雙驗）。
- [ ] 29-D 存讀檔：三 played 互斥 round-trip、played 已設讀檔不重開序列；孤兒檔救援（`expose_upload_done` 已設、三 played 皆 false → 讀檔強制回 `broadcast_station` 直開判定、重演同判）；Phase 24 兩分支回歸（trace >= 3 無警告 → `seven_stopped_partial`、有警告 → `seven_stopped_full`；Branch B 首次真可達）；Phase 1~27 不退化。
- [ ] GUI / 純觸控：判定序列演出節奏（觸控可推進）；A CG 拍顯示與關閉銜接；C 收黑／白噪效果（虛無不懲罰、無 Game Over 畫面）；三終態輸入鎖定；序列中暫停選單 Save 項正確禁用。

#### Phase 30 三結局收尾 + 全主線回歸（headless / GUI）
> 契約見 `開發設計方針.md > 三結局共用收尾 + 全主線回歸（Phase 30，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 30`；敘事事實來源 v2.3 §8 收尾段。**前提硬規則**：讀 5 互斥 played 旗標不重算；染色封頂＝played＋`seven_peace_branch_d`；終句共用一字不改；復駛帶白不做條件；credits 後回標題、存檔槽不動；歌照播與殘響賣否無關。**零新 story_flags**；新 meta：`user://meta.cfg`（結局達成 5 bool，不入存檔槽）。

- [ ] 30-A 接手：五接手點（公寓 R / P ×2、廣播站 A / B / C ×3）終拍完成後**自動**淡黑接收尾（無按鍵）；雨聲 bed＋《雨還沒停》起（該殘響**已賣**的存檔亦照播）；場景 BGM fade out。
- [ ] 30-A 三拍：拍 1 共用遠景＋復駛句固定帶；拍 2 橙特寫 5 變體＋和平線帶白（**10 組合各選中正確 key**，`EXPOSE_A × PEACE` 專句非模板）；拍 3 終景 5 變體＋終句共用 `MSG_ENDING_FINAL_LINE`。
- [ ] 30-A 收尾後：credits 上捲（雨聲墊底、歌收、按住可加速）→ 回標題；meta 結局記錄拍 3 完成即寫（中離 credits 仍計）、重啟保留、save dict 無 endings key、`reset_for_new_game` 不清；M1 進度頁「結局 N / 5」＋未達成 `????` 佔位。
- [ ] 30-B 脊椎：`_run_mainline_spine_phase30` 從 `wake_bed` 按真實玩家路徑（**不灌旗標**）五終點全跑（R / P / EA / EB / EC，中段各自湊分岔：回執、清洗、trace）皆 PASS；跨 Phase 接縫（旗標時序 / 轉場鏈 / 任務依賴）無斷點。
- [ ] 30-C 存讀檔：5 終點「最後可存點留檔 → 讀回 → 走完結局＋30-A」round-trip；鎖點後禁存維持（played 已設檔不可產生）；28 / 29 孤兒檔救援重驗（救援後直通新收尾）；Phase 1~29 不退化。
- [ ] 30-C i18n：新 key 三語 CSV lint（覆蓋 / key 唯一 / 佔位一致 / 禁字）照 M2-E 護欄；GUI 抽一條結局換三語各走一遍（文字頁 / credits / 進度頁不破版）。
- [ ] GUI / 純觸控：5 結局各走查一輪——三拍節奏、CG 顯示與銜接、文字頁觸控 tap 可推進、credits 捲動、回標題、進度頁顯示；終句五圖五讀成立（人工判斷）。
- [ ] 外部素材：情境圖 CG ×11（`cg_ending_street_wide` ×1 / `cg_ending_orange_*` ×5 / `cg_ending_final_*` ×5；g2d 生成、Art Bible 必讀、30-A 動工前補、非 user blocker）。

#### Phase 31 P-00 隱藏（⬜ 暫緩，headless）
- [ ] `abandoned_archive` 載入。
- [ ] P-00 三選（Reset / 留[Trace 大升] / 還·公開[可能暴露活人]）+ trace 連動。

#### Phase 11+ 回歸（每 Phase 必跑）
- [ ] `test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~10 + 已完成主線 Phase 不退化）。
- [ ] 既有殘響 / 對話 / 任務 / 買賣 / 存讀檔行為不退化。

---

#### Phase M1 進度頁（headless / GUI）

>（已完成，全文歸檔於 `subdocs/歸檔/測試指南_歸檔.md`，同標題可 grep。）

#### Phase M2 多國語系 i18n（headless / GUI）
> 契約見 `開發設計方針.md > Phase M2`；驗收意圖見 `遊戲規格書.md > Phase M2`。**Meta / 非敘事 QoL，玩家行為純加法**。前提：三語 `zh_TW`(預設+fallback)/`zh_CN`/`en`、機制＝原生 `tr()`+CSV、簡中與英文 LLM 獨立翻譯、相容 iOS/Android。

##### headless（M2-A 基礎）
- [ ] `LocaleManager.set_locale("zh_CN")` 後 `TranslationServer.get_locale()` == `zh_CN`；非法值退 `zh_TW`。
- [ ] `settings.cfg` round-trip：`set_locale("en")` → 重讀 `_load_saved_locale()` 回 `en`；無檔時回 `""`。
- [ ] `detect_default_locale()` 映射：`zh_TW`/`zh_HK`/`zh_Hant`→`zh_TW`、`zh_CN`/`zh_Hans_CN`/`zh_SG`→`zh_CN`、`en_US`/其餘→`en`。
- [ ] fallback：在 `en` 下查一個只有 `zh_TW` 欄的 key → 回繁中字串（非空、非 key）。
- [ ] 漏譯：查一個三語全缺的 key → `tr(key)` 回 key 本身、不崩潰。
- [ ] 字型：三個 locale 的 `FONT_FOR` 路徑皆 `load()` 成功（`NotoSansSC` 已入專案）。
- [ ] `TranslationServer.get_fallback_locale()` == `zh_TW`。

##### headless（M2-B~E 覆蓋 + 一致性 lint）
- [ ] **覆蓋 lint**：逐 CSV 每列 `zh_TW`/`zh_CN`/`en` 三欄皆非空（漏譯 = FAIL，列出 key）。
- [ ] **key 唯一**：跨所有 CSV 無重複 key。
- [ ] **佔位一致**：同一 key 三語的 `%d`/`%s` 佔位數量一致（語序可不同、個數需同）。
- [ ] **禁字**：三語全文掃描**無「林霏」**；其餘各 Phase 既有禁字規則於三語維持。
- [ ] 抽樣：`tr("MSG_DOOR_LOCKED")` 在三語各回該語言字串；`tr("ITEM_FINGERLESS_GLOVES_NAME")` 同。
- [ ] 對話邏輯不退化：9 棵樹 goto/effect/condition 仍以既有 id/flag 運作（換 locale 不影響分支判定）。
- [ ] Phase 1~19 / M1 headless 不退化（顯示改 key、邏輯不動）。

##### GUI 走查（每語言各跑一輪）
- [ ] 標題畫面 →「設定」→ 語言三選一可切；暫停選單 →「設定」→ 同（遊戲中 `UIMode==NONE` 才可開）。
- [ ] 切語言**即時生效**：當前頁 + 之後開的 UI / 對話 / 筆記 / 物品 / 提示全變新語言。
- [ ] 重啟遊戲沿用上次語言；首次啟動（刪 `settings.cfg`）依裝置語言自動選。
- [ ] 三語各無豆腐框；繁中繁體字形、簡中簡體字形（字型確實切換）。
- [ ] 英文較長字串不破版（按鈕 / 提示 / 對話框不溢出或截斷）。
- [ ] 含數值字串（credits、頁碼）三語佔位 + 語序正確。
- [ ] 全程純觸控可進設定頁、可選語言、可返回。
- [ ] 跑一輪主線片段（公寓→便利商店→聚落）確認對話 / 筆記 / 殘響 / 商店三語可玩、分支不變。

##### 真機（M2-E，iOS / Android）
- [ ] 裝置語言偵測：iOS/Android 系統語言為繁/簡/英時首啟自動選對應語言。
- [ ] 真機切語言即時生效、字型正確、無豆腐、記憶體無異常（只駐一個 CJK 字型）。
- [ ] 純觸控於真機可完成語言切換流程。
