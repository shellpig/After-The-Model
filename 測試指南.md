# 測試指南

> 測試流程與手動驗收步驟清單。
> **撰寫時機**：各子階段的測試項目於**該階段規格定稿時即寫入**（與規格同期，acceptance-test 先行），供實作時據以建測試與自我驗收——不延到驗證時才補。
> **角色歸屬**：測試項目於設計定稿時寫入；之後由 verifier 維護與執行。實作者照清單建測試、不得放寬清單，不改本檔（只列建議）。

## 本檔範圍與邊界

- **規格書「驗收」= 意圖層**：什麼狀態必須為真（what must be true），不寫操作步驟。
- **本檔 = 操作層**：開哪個場景、按哪顆鍵、看什麼畫面（click-by-click）。
- 同一條斷言不抄兩份；本檔的每個清單項用 link 指回規格書對應「### 驗收」段。
- 規格定稿時，測試項目可能引用尚未定的 content（如 prompt 文字）；以「顯示其 prompt」等中性描述先寫，實作時填實際字樣。

## Godot 測試環境

外部工具路徑見 `AGENTS.md > 專案外部工具路徑`。

### Headless 驗證命令

> 已知限制：sandbox 內直接跑會因無法開 `user://logs/godot*.log` 而 crash（signal 11）。請用 escalated 權限執行，見 `AGENTS.md > Godot Headless 驗證`。

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . res://tests/manual/test_runner.tscn
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . -s res://tests/manual/verify_game_state.gd
```

現有測試檔：

```text
tests/manual/test_runner.tscn
tests/manual/test_runner.gd
tests/manual/verify_game_state.gd
```

## 手動驗收清單

### Phase 1（已收尾）
- Phase 1 各系統之驗收意圖見 `遊戲規格書.md` 對應「### 驗收」段。
- 手動操作清單：_本檔自 Phase 2 起新寫，Phase 1 不 backfill；需要時依規格書驗收段現場操作。_

### Phase 2 — 公寓解謎
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 2`；流程見 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`。
> 各子階段測試項目於該階段規格定稿時寫入。

#### 2-A 線索 examine（桌上電腦 / 左牆錄音機）

對應規格：`遊戲規格書.md > Phase 2 > 2-A 驗收意圖`。前置：新遊戲進入公寓場景。

桌上電腦（善後員 / `work_ai_cleanup_role`，分類：工作）：
- [ ] 新遊戲剛進公寓、尚未 examine 桌上電腦前，筆記 UI 不應已有 `work_ai_cleanup_role`（Phase 1 demo preload 已移除或改由 examine 覆寫）。
- [ ] 走近桌上電腦 → 顯示其 prompt。
- [ ] 按 E → MessageBox 顯示現場敘述句，且訊息附「已記入筆記」提示。
- [ ] 關閉訊息後開筆記 UI（`J`）→「工作」分類出現 `work_ai_cleanup_role`，標題 / 內容正確。
- [ ] 再走近按 E → 重播同一現場敘述句；回 `J` 確認該筆記仍只有 1 筆（未重覆新增）。

左牆錄音機（拾遺者 / `identity_gleaner`，分類：身份）：
- [ ] 走近左牆錄音機 → 顯示其 prompt。
- [ ] 按 E → MessageBox 現場敘述句 +「已記入筆記」；`J`「身份」分類出現 `identity_gleaner`，內容正確。
- [ ] 重複 examine → 重播訊息、不重覆加筆記。

通則：
- [ ] 開局未做任何前置即可 examine 兩物（無 `required_knowledge` 阻擋）。
- [ ] examine 跳 MessageBox 期間遵守 UI Mode（玩家不能移動、Prompt 隱藏等，依既有規格）。
- [ ] 兩物的現場敘述句與筆記正文為不同文字（敘述句較短、筆記較完整）。

> 待 content 定稿後填實際 prompt 文字與「已記入筆記」字樣。

#### 2-B 解碼手套 + 魔術方塊轉換

對應規格：`遊戲規格書.md > Phase 2 > 2-B 驗收意圖`。前置：新遊戲進入公寓，背包有 `fingerless_gloves`，櫥櫃預載 `worn_rubiks_cube`。

手套線索筆記：
- [ ] 首次裝備手套（E）→ 得 `clue_gloves_decoder` 筆記（分類：線索）+「已記入筆記」提示。
- [ ] 卸下再裝備 / 之後再 R 查看 → 不重覆新增該筆記。
- [ ] （另一存檔）改先 R 查看手套 → 同樣首次即得 `clue_gloves_decoder`。

未裝備手套（不轉換）：
- [ ] 未戴手套，把 `worn_rubiks_cube` 從櫥櫃拿進背包 → 仍是普通魔術方塊，無轉換、無筆記。
- [ ] 未戴手套，R 查看 `worn_rubiks_cube` → 普通玩具描述，無轉換。

裝備手套 + 拿上手觸發：
- [ ] 戴手套後，把 `worn_rubiks_cube` 從櫥櫃移入背包 → 跳 MessageBox 敘事；該格變成 `decoder_cube`（名稱「在公寓裡找到的解碼方塊」）；得「解碼方塊」筆記。
- [ ] headless / debug 驗證：上一步轉換使用該次 `item_moved` payload 的 `target_instance_id` 定位，不依賴背包排序後猜格；背包內不會同時多出 `worn_rubiks_cube` 與 `decoder_cube` 兩個實例。
- [ ] 轉換後該物再搬動（背包↔櫥櫃）→ 不再跳訊息、不重覆加筆記。

裝備手套 + R 查看 fallback：
- [ ]（另一存檔）先未戴手套把 `worn_rubiks_cube` 拿進背包 → 不轉換；之後戴手套，**不自動回掃**（仍是 `worn_rubiks_cube`）。
- [ ] 接上：戴手套狀態 R 查看該 `worn_rubiks_cube` → 跳 MessageBox；modal 刷新顯示 `decoder_cube`；得「解碼方塊」筆記。

通則：
- [ ] 解碼 MessageBox 期間遵守 UI Mode。
- [ ] `worn_rubiks_cube` / `decoder_cube` 皆 `max_stack=1`。
- [ ] `clue_gloves_decoder` 去重使用 note 存在判斷（`has_note` 或等價），不是 `has_knowledge`；線索分類不應被當成大門 gate knowledge。

> 待 content 定稿後填實際 item id、description、MessageBox 敘事與「已記入筆記」字樣。

#### 2-C 容器白名單 / deposit_locked（headless）

對應規格：`遊戲規格書.md > Phase 2 > 2-C 驗收意圖` + `External Container API > 容器選項擴充`。可用 `tests/manual/verify_game_state.gd` 風格的腳本驗證（純資料層，無需 UI）。

向後相容：
- [ ] `configure_container("cabinet_storage", 30)`（省略新參數）→ 任意 item 可放入、可取出，與 Phase 1-D 一致。

白名單（accepted_item）：
- [ ] `configure_container("test_slot", 1, ["decoder_cube"])` 後，`move_one_item_to("test_slot", <decoder_cube instance>)` → `true`，物入槽.
- [ ] 同容器放入非清單物（如 `<canned_food instance>`）→ `false`，來源與容器內容皆不變。
- [ ] 白名單給多個 id（如 `["decoder_cube","worn_rubiks_cube"]`）→ 兩者皆可放入。
- [ ] `get_container_config("test_slot")` 回傳 `accepted_item = ["decoder_cube"]`、`deposit_locked = false`，且外部修改回傳值不會改 GameState 內部設定。

鎖定（deposit_locked）：
- [ ] `configure_container("test_locked", 1, ["decoder_cube"], true)` 並放入 decoder_cube 後，`move_one_item_to("player_inventory", <該 instance>)`（從鎖定容器取出）→ `false`，容器內容不變。
- [ ] 對 `deposit_locked` 容器**放入**（作為目標）仍正常 → `true`。
- [ ] `get_container_config("test_locked")` 回傳 `deposit_locked = true`，供 UI 區分取出被擋與容量不足。

通則：
- [ ] `move_one_item_to` 全程只回傳 bool，不回原因碼。
- [ ] 成功搬運時 emit `item_moved(move)`，payload 含 `source_container_id`、`target_container_id`、`source_instance_id`、`target_instance_id`、`item_id`。
- [ ] 失敗（任一 false）時來源與目標內容皆無變動。

> headless 執行限制見本檔「Godot 測試環境」段（escalated 權限）。

#### 2-D 投影時鐘 + 聲納 reveal 隱藏插槽

對應規格：`遊戲規格書.md > Phase 2 > 2-D 驗收意圖`；流程見 `subdocs/地點/主角公寓.md`。前置：新遊戲進入公寓，冰箱（`fridge_storage`）預載 `nutrition_bar_synth_blueberry`。

線索麵包屑（營養棒，雙路徑同效）：
- [ ] 開局投影時鐘無 prompt / 不可互動（線索未取得）。
- [ ] 開冰箱 → R 查看 `nutrition_bar_synth_blueberry` → 跳 MessageBox 空包裝藏字敘述；關閉後該道具消失（消耗）、得線索。
- [ ]（另一存檔）把營養棒移入背包 → 單獨背包 footer 顯示 `E: 使用` → 按 E → 同上 MessageBox + 消耗 + 得線索。
- [ ] 營養棒 icon 使用 `res://assets/generated/sprites/items/synthetic_blueberry_nutrition_bar/icon.png`。
- [ ] `J` 線索分類出現 `clue_projection_clock`，標題 / 內容正確。
- [ ] 取得線索後投影時鐘變可互動（顯示 `E: 啟動`）。

背包 footer E 段（Phase 1-E 連帶修正）：
- [ ] 選中可裝備物（如 `fingerless_gloves`）→ footer 顯示 `E: 裝備`；裝備後顯示 `E: 卸下`。
- [ ] 選中 consumable（如 `canned_food`）→ footer 顯示 `E: 使用`（非「裝備/卸下」）。
- [ ] 選中無動作物（如 `old_work_badge`）→ footer **不顯示 E 段**（仍有 R / T / Esc·I）。
- [ ] `canned_food` 按 E: 使用 → FloatingToast「現在用不上。」，不消耗、背包數量不變。
- [ ] 雙欄左欄（背包欄）選物 → E 仍為「移動」，不出現「使用」。

聲納啟動 + reveal：
- [ ] 互動投影時鐘 → 進入聲納模式，10 秒倒數開始，出現視覺強度條。
- [ ] 水平靠近隱藏插槽方向 → `res://assets/sound/sonar_ping.wav` 短音播放頻率 + 視覺條上升；遠離 → 下降。
- [ ] 停留最強區帶寬內（目標 X 座標 `±36px`，小幅移動仍算）→ 鎖定環累積 4 秒至滿 → 播放 `res://assets/sound/hidden_slot_reveal.wav` 並 reveal 隱藏插槽。
- [ ] 鎖定中離開最強區帶寬 → 鎖定環重置。

限時與凍結：
- [ ] 啟動後放任 10 秒倒數歸零（不去鎖定）→ 聲納結束、無 reveal；再互動投影時鐘可重新啟動。
- [ ] 聲納進行中按 `I` / `J` 開大 UI → 聲納、倒數、鎖定進度凍結；關閉 UI → 由凍結點接續（未歸零）。

reveal 後：
- [ ] reveal 後隱藏插槽常駐，可如一般雙欄容器開啟。
- [ ] 投影時鐘不再可互動（互動移除）；聲納無法再啟動。

通則：
- [ ] 聲納 / MessageBox 期間遵守 UI Mode（玩家移動、Prompt 隱藏依既有規格）。
- [ ] 新存檔重跑可重現上述全鏈。

> 短音播放頻率曲線、鎖定環視覺、文字字樣待 content 定稿後對齊。

#### 2-E 插槽放入 → 語音 → 開門 全鏈

對應規格：`遊戲規格書.md > Phase 2 > 2-E 驗收意圖` + `UIMode > overlay modal 的 caller 還原`。前置：完成 2-A~2-D，背包有 `decoder_cube`，`apartment_slot` 已 reveal。

放入觸發：
- [ ] 開插槽雙欄 → E 移動 `decoder_cube` 入插槽 → 電磁聲響 + 語音 MessageBox **疊在雙欄上**（雙欄仍可見、底層凍結）。
- [ ] 語音期間按方向 / E / R → 不操作底層雙欄（overlay 捕捉輸入）。
- [ ] 關閉語音 → 焦點回雙欄原格；`J` 身份分類出現 `identity_door_unlock_method`「我鎖上的門」，內容正確。

一次性與鎖定：
- [ ] 方塊在插槽內 → 嘗試 E 移回背包 → `false`，取不出（deposit_locked）。
- [ ] 關閉再重開插槽雙欄 → 不重播語音、不重覆 add_knowledge。

開門：
- [ ] 取得 `identity_door_unlock_method` 後靠近大門 → 顯示 `door_opened`（MVP 不轉場）。
- [ ] 未取得知識前靠近大門 → 仍顯示 `door_locked`（gate 未過）。

overlay 重構回歸：
- [ ] examine(2-A) / 解碼(2-B) 的 MessageBox 行為不變（從世界 / 背包開、關閉回原狀態）。
- [ ] 1-B~1-E 大型 UI 互切（`I` / `J` / 容器）、CONFIRM 丟棄確認、focus 還原全部不破。
- [ ] ItemDetailModal 開啟中觸發 MessageBox（如 R 查看手套後方塊、或 R 查看營養棒）：MessageBox 疊在 detail modal 上、detail modal 凍結不吃鍵盤；關閉後 capture 還原 + modal 刷新（變身物刷新顯示 / 消耗物連 modal 一起關回 grid）。

全鏈（B0→B9）：
- [ ] 新遊戲一路走通：醒來 → 試門鎖 → examine 兩線索 → 戴手套 → 解碼方塊 → 冰箱營養棒得線索 → 投影時鐘聲納 reveal 插槽 → 放入方塊 → 語音 → 開門。

> 電磁聲響素材待 content 定稿後對齊。

#### 2-F 筆記修正 + 公寓 BGM

對應規格：`遊戲規格書.md > Phase 2 > 2-F 驗收意圖` + `筆記 / 知識系統` + `音訊（BGM）`。前置：新遊戲進入公寓場景。

筆記內容：
- [ ] 開筆記（`J`）→ `身份` 分類出現「雨還沒停」，內容為氛圍獨白。
- [ ] 「雨還沒停」全文**不含**任何測試/捲動字樣（Page Down）與謎題暗示（集齊線索、密碼鎖、手套清理工作）。
- [ ] `線索` 分類**不再**出現舊的「測試長筆記捲動」。
- [ ] 「雨還沒停」全文在全文欄一頁內可完整讀完，無需捲動。

筆記操作：
- [ ] `A` / `D` 切分頁，與 Left / Right 效果相同。
- [ ] `W` / `S` 上下選筆記，與 Up / Down 效果相同；列表邊界不繞回。
- [ ] footer 顯示 `A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`。
- [ ] Page Up / Down 按下無反應（不報錯、不捲動）。

公寓 BGM：
- [ ] 進公寓場景即聽到背景音樂循環播放，無爆音、循環不中斷。
- [ ] 音量為背景襯底（`-12 dB`），不蓋過聲納短音等解謎音效。
- [ ] （回歸）暫時移除 / 改名 BGM 檔 → 遊戲仍正常啟動、不噴錯（靜音）。

#### 2-G 開場獨白序列

對應規格：`遊戲規格書.md > Phase 2 > 2-G 驗收意圖`；文案見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`；契約見 `開發設計方針.md > 2-G`。前置：新遊戲進入公寓場景。

進場狀態：
- [ ] 新遊戲一進公寓即自動播開場：玩家為趴睡姿（`prone`）、無法移動、互動 prompt 不顯示、MessageBox 顯示第 1 頁。
- [ ] 開場期間按 `I` / `J` / `E` / 移動鍵皆無效（世界互動與大型 UI 全擋）；只有換頁鍵與隱藏 T 有作用。

逐頁播放 / 不可跳過：
- [ ] 每頁文字逐字顯示（約 6 字/秒）；文字未跑完前按 WASDE / 方向鍵 / Esc 皆**無法換頁、不快進、不跳過**。
- [ ] 每頁標題（感官甦醒 / 空白的記憶 / 自囚與尋回）顯示在該頁正文第一行，並隨打字機一起逐字出現。
- [ ] 某頁文字完整顯示後，淡入換頁提示：第 1~2 頁「▼ 繼續」、第 3 頁「▼ 開始」；文字未跑完不顯示提示。
- [ ] 文字跑完後按 WASDE / 方向鍵 / Esc 任一 → 進下一頁。
- [ ] 共 3 頁，依序 P1→P2→P3，每頁文案與 subdoc 全文一致。

起身動畫（第 3 頁）：
- [ ] 第 3 頁一出現即播放 `get_up`（約 1 秒、10 frame、不循環），播完停最後一格並切 `idle`。
- [ ] 起身動畫播放期間第 3 頁文字同時繼續跑（文字不等動畫、動畫不等文字）。
- [ ] 起身（~1 秒）早於第 3 頁文字結束；動畫結束後角色維持站立 `idle`、文字續跑。

結束 / 解鎖：
- [ ] 第 3 頁文字跑完、按 WASDE / 方向鍵 / Esc → MessageBox 關閉、解除輸入封鎖、玩家可自由移動。
- [ ] 開場結束後角色為站立 `idle`（非 prone）；可正常走動、開背包 / 筆記、與互動物互動（接 B1 試門等）。

隱藏測試捷徑（dev skip）：
- [ ] 任一頁 1 秒內連按 3 下 T → 立即補完當頁文字並跳下一頁。
- [ ] 第 3 頁 1 秒內連按 3 下 T → 直接關閉開場、解鎖操作；角色 snap 為站立 `idle`（即使 get_up 未播完）。
- [ ] 連按間隔超過 1 秒（非連續）→ 計數歸零、不觸發跳頁。
- [ ] 換頁提示中不出現任何 T 捷徑字樣（隱藏性）。

每次進場：
- [ ] 重開新遊戲 / 重進公寓場景 → 開場每次都播（MVP 無「只播一次」flag）。

通則：
- [ ] 開場期間遵守輸入封鎖，玩家不能移動、Prompt 隱藏。
- [ ] 文案以 subdoc 全文為準，逐頁標點一致。

### Phase 3 — 公寓觸控化（手機可玩）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 3`；實作契約見 `開發設計方針.md > Phase 3`。
> 3-A~3-D 驗收環境：Windows 桌面版 + 滑鼠模擬觸控（開 `emulate_touch_from_mouse` 後，滑鼠點按 = 觸控）。3-E 為 iPhone 真機。
> 「純觸控」= 全程只用滑鼠點螢幕按鈕，**不碰鍵盤**。

#### 3-A 顯示 / 橫向 / 觸控模擬設定
- [ ] 桌面版啟動為橫向；視窗縮放時畫面以 `canvas_items` + `expand` 自適應，不變形。
- [ ] （回歸）純鍵盤可完整玩通 B0–B9，無任何退步。
- [ ] （回歸）`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。
- [ ] 桌面版下用滑鼠點場景互動物，能觸發（滑鼠被當觸控事件）。

#### 3-B 世界模式觸控
- [ ] （顯示規則）PC 桌面版啟動 → 左上出現「[ 觸控:關 ]」開關，手把預設隱藏，鍵盤照玩；點開關切到「[ 觸控:開 ]」→ 虛擬手把出現（左下方向鍵、右下 E、右上 背包 / 筆記）。再點即關。
- [ ] （顯示規則）開場獨白播放中 / 非世界模式時，左上開關按鈕不顯示。
- [ ] （備註）已棄用舊 `is_touchscreen_available` 判定（Windows 觸控筆電誤判）；改 `OS.get_name()` 判 PC，手機平台預設直接顯示手把、免開關。
- [ ] **純觸控**：長按左下 ← / → → 角色持續向左 / 右走；輕點 → 走一步即停。
- [ ] **純觸控**：走到大門 / 桌上電腦 / 左牆錄音機，點右下 E → 觸發 examine / 互動（同鍵盤 E）。
- [ ] **純觸控**：點右上「背包」→ 開背包；點右上「筆記」→ 開筆記。

#### 3-C 面板模式觸控
- [ ] 背包內**純觸控**：方向鍵移焦點到目標格；R 查看（ItemDetailModal）、T 丟棄（ConfirmDialog）、E 裝備 / 卸下或移動。
- [ ] 筆記內**純觸控**：← / → 翻分頁、↑ / ↓ 選筆記。
- [ ] 雙欄容器**純觸控**：方向鍵跨欄移焦點、E 搬 1 個物品。
- [ ] 面板開啟時右上「背包 / 筆記」換成「X 返回」；點 X → 關面板（= `ui_cancel`），回到世界模式手把。
- [ ] ConfirmDialog / ItemDetailModal / MessageBox 等 overlay 皆可用手把操作（E 確認 / R 或 X 取消）。
- [ ] **里程碑（純觸控全程）**：從新遊戲 2-G 開場（連點 T 三下可跳頁）→ B1 試門 → B2 examine → B3 戴手套 → B4 解碼方塊 → B5~B7 終端 / 聲納 → B8 放入方塊 → B9 開門，全程不碰鍵盤可通關。

#### 3-D Safe area + iPhone 比例排版
- [ ] 將桌面視窗拉成 iPhone 橫向比例（約 19.5:9）→ 按鈕不重疊、不遮 prompt / MessageBox / 背包格。
- [ ] 每顆按鈕點擊區 ≥ 44×44 px（目視 / 量測夠大好點）。
- [ ] 按鈕內縮於 safe area（桌面上以模擬 inset 確認；真實瀏海待 3-E）。

#### 3-E 真機導出 + iPhone 校正（需 Mac）
- [ ] Mac + Xcode 免費簽名（Personal Team）導出進自己 iPhone，app 可安裝啟動（**未付 Apple Developer Program**）。
- [ ] iPhone 上**純觸控**完成 B0–B9。
- [ ] 真實瀏海 / home indicator 下，按鈕不被遮、不誤觸系統手勢區。
- [ ] 按鈕大小順手、長按走路無明顯延遲、整體效能可接受。
- [ ] （預期，非缺陷）憑證 7 天到期後 app 打不開，需 Xcode 重簽——確認重簽流程可行。

### Phase 4 — 跨場景架構化（Main Host / GameUI / SceneRouter）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 4`；實作契約見 `開發設計方針.md > Phase 4`。
> Phase 4 只驗證架構與薄 stub；不要求完整 NPC 對話、完整任務、真存檔、完整 MusicManager、地鐵完整移動網路或街區正式美術。

#### 4-A0 檔案結構整理
- [ ] root 下不再有 `apartment_room.tscn` / `apartment_room.gd` / `player.gd` / `interactable_area.gd`；各自就位於 `scenes/levels/apartment/` / `scenes/actors/player/` / `scripts/components/`。
- [ ] 4-A0 未新增 `player.tscn`；`apartment_room.tscn` 仍保留內嵌 `Player` node，只把 script path 改到 `scenes/actors/player/player.gd`。
- [ ] 每個搬移的 `.gd` 旁有對應 `.gd.uid` sidecar，且 uid 值與搬移前相同。
- [ ] `apartment_room.tscn` 三個 `ext_resource` 的 `path` 指向新位置，`uid` 欄位未變。
- [ ] 搜尋非文件 source / scene / test 檔無殘留 `res://apartment_room.tscn` / `res://apartment_room.gd` / `res://player.gd` / `res://interactable_area.gd` 舊路徑引用（排除 `*.md` 與 `.godot/` 快取；文件中的遷移說明不算殘留引用）。
- [ ] `tests/manual/test_runner.gd` 的 `load()` 路徑已更新為新位置。
- [ ] `project.godot` 在本子階段未被修改（main_scene 仍為原 uid，搬檔不影響）。
- [ ] Godot editor 開啟 `apartment_room.tscn` 無 missing resource 紅字；player 動畫、互動區掛載正常。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` PASS。
- [ ] 本次搬移為獨立 commit，diff 只含檔案移動與路徑字串更新，無遊戲邏輯改動。

#### 4-A Main Host / SceneRouter / SceneRegistry
- [ ] `project.godot` 的 `run/main_scene` 指向 `main.tscn`。
- [ ] 啟動遊戲後，`main.tscn` 將公寓場景載入到 `WorldRoot`，玩家看到的初始畫面仍是公寓。
- [ ] 新遊戲進入後，2-G 開場獨白照常播放；玩家、背景、互動物位置無明顯偏移。
- [ ] 純鍵盤路徑：開場結束後可左右移動、靠近互動物、按 E 互動。
- [ ] SceneRegistry 至少有 `apartment` 與 `street_stub` 條目，且條目含 `path` / `default_entry_point_id` / `entry_points` / `music_id`；`scene_id` 可由 registry key 表示。
- [ ] `apartment.default_entry_point_id == "wake_bed"`，且 `apartment.entry_points` 含 `wake_bed` / `from_street`；`street_stub.entry_points` 含 `from_apartment`。
- [ ] 搜尋程式碼確認轉場新路徑使用 `scene_id + entry_point_id`；新增 Phase 4 邏輯不得直接呼叫 `get_tree().change_scene_to_file()` 作為門轉場主路徑。
- [ ] `transition_to("apartment", "wake_bed")` 或等價 debug 呼叫能以新遊戲入口重載公寓並播放開場，且結束後 `UIMode` 回到 `NONE`。
- [ ] `transition_to("apartment", "from_street")` 或等價 debug 呼叫可被 SceneRouter 接受並記錄目標 entry point；完整「站門邊 idle、不播開場」行為待 4-E 驗收。

#### 4-B GameUI 抽離
- [ ] `main.tscn` 下存在常駐 `GameUI`（CanvasLayer），與 `WorldRoot` 為 sibling。
- [ ] `apartment_room.tscn` 不再直接包含 `InventoryPanel` / `NotebookPanel` / `DualPaneContainer` / `MessageBox` / `ConfirmDialog` / `ItemDetailModal` / `PromptPanel` / `UIOverlay` / `WorldHUDLabel` / `FloatingToast`。
- [ ] 按 `I` 開背包：顯示原本 5x3 inventory、Credits、footer；WASD / 方向鍵 focus 可動。
- [ ] 按 `J` 開筆記：三分類 tab、列表、全文欄正常；A/D 切分類，W/S 選筆記。
- [ ] 開櫥櫃 / 冰箱：DualPane 顯示左背包右容器；E 移動 1 個；R / T / Esc 行為不退化。
- [ ] MessageBox 可從世界互動、物品使用、插槽放入等路徑顯示；打字機與關閉鍵不退化。
- [ ] FloatingToast 由 `GameUI` 生成 / 掛載；背包、雙欄與丟棄流程的 toast 顯示位置與層級不退化。
- [ ] UI 面板（背包 / 筆記 / MessageBox / DualPane / Prompt）搬進 `GameUI` 後，anchor / layout_mode / 螢幕位置與 Phase 3 一致；在 iPhone 橫向比例（~19.5:9）下不重疊、不遮擋、不被 safe area 切到。
- [ ] TouchControls 的 safe area 內縮與按鈕尺寸 / 位置不因 UI 搬進 GameUI 而改變（autoload 與 layer 100 不動）。
- [ ] 切換或重載關卡時，GameUI 不因關卡釋放而消失或重建造成閃爍。

#### 4-C Level interaction contract + Prompt
- [ ] 走近任一公寓互動物，Prompt 由 `GameUI` 顯示；離開範圍後 Prompt 隱藏。
- [ ] 多個互動物範圍重疊時，Prompt 仍顯示離玩家最近的互動物文字。
- [ ] 開啟 Inventory / Notebook / DualPane / MessageBox / ConfirmDialog 時，Prompt 隱藏。
- [ ] 關閉 UI 回到世界模式後，若玩家仍站在互動物範圍內，Prompt 恢復。
- [ ] Level 只 emit interaction / current-interactable 資料；不得直接查或修改 `GameUI` 內部 node path。
- [ ] Message-only 互動物可透過 contract 顯示 MessageBox。
- [ ] Container 互動物可透過 contract 開啟 DualPane。
- [ ] Transition 互動物可透過 contract 請求 SceneRouter。

#### 4-D TouchControls 改接 GameUI contract
- [ ] PC 桌面啟動後，觸控 toggle 行為與 Phase 3 相同：預設關閉，開啟後顯示虛擬手把。
- [ ] 世界模式：只有目前有互動物時，右下 E 顯示；離開互動範圍 E 隱藏。
- [ ] 背包模式：E / R / T 顯示規則依 focused item 可用動作變化，與 Phase 3 相同。
- [ ] 雙欄模式：E / R / T 顯示且可操作 focused item；X 返回關閉面板。
- [ ] MessageBox / ConfirmDialog / ItemDetailModal overlay 期間，TouchControls 不誤操作底層 UI。
- [ ] 開場獨白播放期間，PC 觸控 toggle 不顯示；開場結束且回到世界模式後才恢復顯示。
- [ ] 搜尋 `touch_controls.gd` 確認不再讀目前 level 的 `current_interactable`、`_opening_monologue_active` 或 `UI/InventoryPanel/...` / `UI/DualPaneContainer` 固定路徑。
- [ ] 純觸控路徑仍可從 B0 玩到 B9。

#### 4-E 公寓完整遷移 + B0-B9 回歸
- [ ] B0：進公寓自動開場；prone / get_up / idle 流程不退化。
- [ ] B1：未取得知識前試門，顯示 locked message。
- [ ] B2：桌上電腦 / 錄音機 examine，MessageBox 顯示，筆記寫入不重覆。
- [ ] B3：背包裝備手套，得到手套線索筆記。
- [ ] B4：櫥櫃拿 / R 查看魔術方塊，戴手套時轉成 `decoder_cube`。
- [ ] B5：冰箱營養棒 R 查看或 E 使用，跳 MessageBox、消耗物品、取得投影時鐘線索。
- [ ] B6：投影時鐘可啟動聲納。
- [ ] B7：聲納最強區停留足夠時間後 reveal 隱藏插槽。
- [ ] B8：插槽 DualPane 中放入 `decoder_cube`，MessageBox 疊在雙欄上；關閉後焦點回雙欄，取得 `identity_door_unlock_method`。
- [ ] B9：取得知識後大門 gate 通過，觸發 Phase 4 的轉場請求。
- [ ] 開場多頁流程仍支援頁尾提示、不可跳過規則、T 三連跳測試捷徑、prone/get_up/idle 動畫；GameUI 抽離後不退化。
- [ ] 以 `apartment:from_street` 回公寓時，不播放開場、不回床上、不進 prone；玩家站在門邊 idle。
- [ ] ConfirmDialog 丟棄確認、ItemDetailModal、MessageBox overlay caller restore 皆不破。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS（若測試需更新 node path，更新後再跑）。

#### 4-F 第二場景 stub + 真轉場
- [ ] `street_stub` 場景可由 SceneRouter 載入，至少有 placeholder 背景、玩家 spawn point、回公寓互動物、1-2 個 message-only examine 互動物。
- [ ] 公寓 B9 開門後，從公寓轉到 `street_stub:from_apartment`。
- [ ] 抵達 `street_stub` 後，GameUI 仍可開背包 / 筆記；GameState 的背包、筆記、裝備狀態保留。
- [ ] `street_stub` 的 examine 互動可顯示 Prompt 與 MessageBox。
- [ ] `street_stub` 的回公寓互動可轉回 `apartment:from_street`，且不重播公寓開場。
- [ ] 轉場前若有開啟 UI，轉場後 UI 被關閉且 `UIMode == NONE`。
- [ ] 轉場後舊場景的 prompt / message / container 不殘留。
- [ ] 轉到 `street_stub` 後，公寓門事件不再播放或保留門後 / 街道 BGM；`street_stub` 可靜音或自行播放 stub BGM。

#### 4-G Future-system contracts pass（已取消）
> ❌ 本子階段已取消，以下勾項作廢、不再執行；改由 Phase 5 做真系統。保留僅為歷史脈絡。
- [ ] 新增或模擬一個帶 `dialogue_id` 的 NPC-like interactable；Phase 4 可顯示 placeholder MessageBox，但資料欄位必須通過 contract 傳遞。
- [ ] 新增或模擬一個帶 `quest_event_id` 的互動；Phase 4 可 noop / log，但不得為了測試而把任務狀態硬塞進 `GameState.knowledge`。
- [ ] SceneRegistry 的每個 scene 條目都有 `music_id`；即使尚未實作 MusicManager，也不得阻礙未來接入。
- [ ] SceneRouter 可讀出目前 `current_scene_id`；未來 SaveSystem 可用此 id 還原場景，而非依賴 raw file path。
- [ ] 每個 level 有穩定 entry point 命名；至少驗證 `apartment:wake_bed`、`apartment:from_street`、`street_stub:from_apartment`。
- [ ] 搜尋程式碼確認沒有新增「只有公寓一個場景」的硬編碼假設；公寓專屬邏輯留在公寓，通用 UI / 轉場 / 互動 contract 不依賴公寓檔名。

### Phase 5 — NPC + 對話系統（真系統）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 5`；實作契約見 `開發設計方針.md > Phase 5`；NPC「晚」內容見 `subdocs/人/晚.md`。
> 4-G 已取消，本 Phase 直接做真對話 / 故事旗標；任務與存檔仍只預留資料路徑。

#### 5-A 故事旗標 store + 對話資料 + DialogueRunner（headless）
- [ ] `GameState` 有 `set_flag` / `get_flag` / `has_flag` / `add_int` 與 `flag_changed` signal；`story_flags` 只存 bool / int。
- [ ] 故事旗標與 `knowledge` / `notes` 分離：設旗標不影響 knowledge，反之亦然。
- [ ] `data/dialogue/wan.gd` 的對話樹涵蓋 `subdocs/人/晚.md` 的 start（條件路由）/ first_meet / retalk / watch / who / kin / intel_gate / intel / intel_locked / end_warm / end_cold / flee 節點。
- [ ] `DialogueRunner.start()`（入口 `start` 條件路由，初始 `met_wan` 未設）後，`current()` 回傳 `first_meet` 的 speaker / text / 三個選項。
- [ ] 選 `who` → 設 `knows_wan_name == true`，並前進到 `watch`。
- [ ] 選 `kin` → `affinity_wan` +1、`met_wan == true`、前進 `end_warm`。
- [ ] `current().choices` 已過濾 `condition` 不成立的選項。
- [ ] 各選項 `index` 為原始 `choices` 序號；隱藏選項後，以該 `index`（非可見清單位置）呼叫 `choose()` 仍走正確分支。
- [ ] 重講路由：`met_wan == true` 時 `start` → `retalk`；未設時 → `first_meet`。
- [ ] 條件路由：`affinity_wan < 2` 時 `intel_gate` → `intel_locked`；`affinity_wan >= 2` 時 → `intel`。
- [ ] 進入 `intel` 觸發 `add_gleaner_lead`，且僅 log / noop，未寫入 `GameState.knowledge` 或任何任務狀態。
- [ ] terminal 節點 `current().is_terminal == true`；再 `advance()` 發出 `finished`。
- [ ] 5-A 不建立任何 UI 節點即可跑完上述；納入 `test_runner.gd`，headless PASS。

#### 5-B 對話 UI + UIMode.DIALOGUE + 接線（GUI / 鍵盤）
- [ ] `UIMode.Mode` 含 `DIALOGUE`；進入對話時主角無法移動，結束後可移動。
- [ ] `interactable_area` 有 `dialogue_id` export 欄位。
- [ ] 走近 NPC 顯示 Prompt「交談」；按 `E` 開啟 DialoguePanel。
- [ ] Level 經 `interaction_requested({type:"dialogue", dialogue_id})` → Main → `GameUI.start_dialogue()`，Level 未直接碰 GameUI node path。
- [ ] DialoguePanel 顯示立繪槽 / 名字 / 內文 / 選項列；立繪 texture 為 null 時不報錯。
- [ ] 鍵盤 上 / 下 移選項焦點，焦點有高亮；`E` 確認選取。
- [ ] 無選項節點按 `E` 推進；terminal 節點按 `E` 關閉對話。
- [ ] 對話分支與 effect 在 UI 流程正確反映（選 `kin` 後 `affinity_wan` 實際 +1）。
- [ ] 對話結束後 DialoguePanel 隱藏、`UIMode == NONE`、Prompt 依目前互動物恢復、無殘留。
- [ ] 對話期間開背包 / 筆記等其他 UI 不會與對話疊加錯亂（進對話前其他面板已關）。

#### 5-C TouchControls 對話路由（純觸控）
- [ ] `UIMode == DIALOGUE` 時，觸控只顯示對話所需 D-pad + 確認鍵；世界移動 / 其他面板觸控鍵隱藏。
- [ ] D-pad 上 / 下移選項焦點；右下確認鍵＝選取 / 推進，效果同鍵盤 `E`。
- [ ] 對話期間觸控不誤觸底層世界互動或其他 UI。
- [ ] 搜尋 `touch_controls.gd` 確認對話路由經 GameUI 公開查詢（`has_active_dialogue` / `dialogue_move_focus` / `dialogue_confirm`），未讀固定 node path。
- [ ] 純觸控可從第一句走到對話結束，含選分支。

#### 5-D 晚落地 + placeholder 美術 + 手動驗收
- [ ] `apartment_entrance` 暗巷口有 NPC「晚」（interactable + sprite 子節點 placeholder），`interaction_id="talk_wan"`、`dialogue_id="wan"`。
- [ ] 從公寓 B9 開門轉到街道後，走近「晚」可對話。
- [ ] 第一次對話經 `start` 路由到 `first_meet`；三個分支（watch / who / flee 與其下游）皆可正常走到結尾。
- [ ] 設 `met_wan` 後再對話，經 `start` 路由走 `retalk` 短版。
- [ ] 好感度足夠（`affinity_wan >= 2`）時「有新消息嗎？」可達 `intel`；不足走 `intel_locked` 婉拒。
- [ ] 場景 dispatch 以 `dialogue_id` 欄位泛化判斷，未為「晚」新增寫死的 `interaction_id` 字串分支（mailboxes / alley_view / back_to_apartment 仍走原路徑）。
- [ ] 跨場景：對話後回公寓再回街道，背包 / 筆記 / `story_flags`（含 `affinity_wan` / `met_wan`）保留，不重置、不重播任何開場。
- [ ] 立繪 / 角色圖未提供時，panel 與場景以 placeholder 呈現不崩潰；提供後可替換。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。

### Phase 6 — SaveSystem（手動多槽存檔）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 6`；實作契約見 `開發設計方針.md > Phase 6`；存檔架構見 `技術概念.md > 存檔策略`。
> Phase 6 只做存檔；任務狀態仍不得寫進 `knowledge`。

#### 6-A SaveSystem Autoload + 序列化（headless）
- [ ] `SaveSystem` 已註冊為 Autoload；`GameState` 有 `to_save_dict()` / `load_save_dict()`。
- [ ] `to_save_dict()` 涵蓋全可變狀態：credits / inventory / equipment / knowledge / notes / external_containers / external_container_configs / apartment_* 四旗標 / story_flags / `_last_instance_id`。
- [ ] `to_save_dict()` 不含 code-owned 常數（STORY_NOTES / STORY_MESSAGES / ITEMS_DB / EQUIPMENT_LIMITS）。
- [ ] Round-trip：改動狀態（加物品 / 設旗標 / `affinity_wan` += 1 / 開容器）→ `capture` → `write_slot` → 清狀態 → `read_slot` → `apply`，全欄位還原一致。
- [ ] `_last_instance_id` 還原後，`generate_instance_id()` 接續舊號、不與既有 `instance_id` 撞號。
- [ ] `inventory` / `notes` 等 typed array 還原後型別正確、內容深拷貝不共用參照。
- [ ] 序列化用 `var_to_str` / `str_to_var`（搜尋確認未用 JSON / ResourceSaver）。
- [ ] `SaveSystem.validate(payload)` 只驗 shape / version（payload 非空、有 meta/data、`meta.version` 不符回 false、必要欄位缺漏回 false），不查 scene_id、不改任何狀態。
- [ ] scene_id 存在性由 Main / SceneRouter 驗（`current_scene_id ∈ SCENES`），非 SaveSystem 職責；搜尋確認 SaveSystem 未參照 main.gd 的 `SCENES`。
- [ ] `GameState.reset_for_new_game()` 後：credits=300、inventory 空槽、equipment 清空、knowledge / notes / containers / story_flags 清空、apartment_* 旗標 false、`_last_instance_id`=0。
- [ ] `SaveSystem.can_save_here` 預留存在且預設 true。
- [ ] 納入 `test_runner.gd`，headless PASS。

#### 6-B 讀檔回場路徑（headless / GUI）
- [ ] `transition_to` 帶 restore payload 時，進場景後玩家座標 = 存檔座標，且不播 entry point 副作用。
- [ ] New Game 進 `wake_bed` 播開場獨白；Load 還原同一場景時不播開場、不跑 wake 動畫。
- [ ] player 有 `get_save_x` / `set_save_x`（+ facing）存取點；還原後 facing 正確。
- [ ] 在公寓解謎中途（如已 sonar_revealed、已拿 decoder_cube）存檔再讀，謎題進度與容器內容一致。

#### 6-C 多槽模型（headless）
- [ ] 存到 slot N → `user://save_0N.sav` 產生；各槽獨立互不覆蓋。
- [ ] `list_slots()` 回傳 10 項；空槽 `empty==true`、有效槽帶 `meta`（version/timestamp/scene_name/credits）、損毀槽 `corrupt==true`。
- [ ] 覆蓋已佔用槽後，該槽 meta / data 更新為新存檔。
- [ ] 手動把某槽檔案改壞 / 改版本號 → `read_slot` 回 `{}`、`list_slots` 標 corrupt，不 crash。

#### 6-D 最小標題畫面（GUI）
- [ ] 啟動遊戲先進標題畫面，只有 New Game / Load Game 兩入口。
- [ ] New Game → 進公寓 `wake_bed`、播開場。
- [ ] Load Game → 列 10 槽；選有效槽 → 還原到該存檔場景與站位；空 / 損毀槽不可載入。
- [ ] 標題畫面與遊戲世界互不污染（無殘留世界節點 / 無提前載入）。
- [ ] 標題畫面不出現 TouchControls 的 D-pad / 功能鍵 / PC 觸控 toggle（PC 與觸控平台皆是）。
- [ ] 流程乾淨：Load 有效槽 → 回標題 → New Game，新局狀態乾淨（credits=300、背包空、`story_flags` 空），無上一局殘留。

#### 6-E 暫停選單 + 槽列表 + gating（GUI / 觸控）
- [ ] 自由探索（`UIMode == NONE`）按暫停鍵 / 鈕 → 開暫停選單（Resume / Save / Load / 回標題）。
- [ ] 面板（背包 / 筆記 / 容器）/ 對話 / 開場獨白期間，暫停選單無法開啟。
- [ ] Save：選空槽直接寫入；選已佔用槽 → `ConfirmDialog` 覆蓋確認後才寫。
- [ ] Load：選有效槽 → 還原；損毀 / 空槽不可選。
- [ ] Save / Load 共用同一槽列表元件（搜尋確認非兩份重複 UI）。
- [ ] 回標題若進度未存 → `ConfirmDialog` 二次確認後才離開。
- [ ] 觸控：暫停鈕可開選單；選單 / 槽列表可純觸控操作；搜尋確認經公開查詢路由、未讀固定 node path。

#### 6-F 邊界與回歸
- [ ] 版本不符 / 損毀槽載入被擋並提示，不 crash。
- [ ] 還原 scene_id 不在 `SCENES` → Main 於 apply 前擋下：不轉場、跳訊息、不黑屏，且 **GameState 未被套用**（credits / inventory 維持載入前）。
- [ ] 還原 player_x 出界 → clamp 回場景邊界，玩家不卡牆外。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。
- [ ] GUI 走查：存檔 → 離開 → Load → 回到同一站位與世界狀態（背包 / 筆記 / 旗標一致）。

### Phase 7 — QuestManager + `alley_backrooms_3f` vertical slice
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 7`；實作契約見 `開發設計方針.md > Phase 7`。
> Phase 7 不做大型 Quest UI；任務顯示暫用筆記 UI「工作」分類；`apartment_fire_escape` 外牆場景不可存檔。

#### 7-A QuestManager + quest state save（headless）
- [ ] `QuestManager` 已註冊為 Autoload；`GameState` 有 `quest_states` 與任務查詢 / 寫入 API。
- [ ] `QuestManager.start("alley_backrooms_3f")` 建立 active state，step 為初始步驟，且新增 / 更新「工作」筆記 `quest_alley_backrooms_3f`。
- [ ] `advance()` 只能走合法 step；非法 step 回 false 且不改 state / note。
- [ ] `complete()` / `fail()` 更新 status；completed 任務再次 `start()` 不重置進度。
- [ ] `alley_backrooms_3f` 不發 credits / item reward；完成任務不呼叫空殼 reward 發放。
- [ ] Quest data 的 active 工作筆記用 step 查表，completed / failed 工作筆記用 status 查表；不存在 `completed` 作為 step，也沒有未被設定的死 step（例如 `reported_to_wan`）。
- [ ] 任務狀態不寫入 `GameState.knowledge`；搜尋確認 `quest_states` 與 `knowledge` 分離。
- [ ] 任務內部旗標（例如是否拿過箱子、是否取出 B）只存在 `quest_states[quest_id].flags`，不寫入 `story_flags`。
- [ ] `GameState.to_save_dict()` / `load_save_dict()` 包含 `quest_states`；存檔 round-trip 後 quest status / step / flags 一致。
- [ ] `reset_for_new_game()` 清空 `quest_states`，工作筆記也回到新局空狀態。
- [ ] 任務每次 `start` / `advance` / `complete` / `fail` 後，同一筆「工作」筆記 body/status 依進度更新，不新增多筆重複 note。

#### 7-B 晚的對話接任務（headless / GUI）
- [ ] `DialogueRunner` 支援 `start_quest` effect，會呼叫 `QuestManager.start()`。
- [ ] `DialogueRunner` 支援 Phase 7 condition：`quest_status`、`quest_step`、`has_item`；舊 `{flag, op, value}` story flag 條件仍相容。
- [ ] `condition` 為 Array 時採 AND；任一條件不成立則選項隱藏 / goto 不命中。
- [ ] 首次見晚時不會直接給任務；任務啟動沿用既有 `affinity_wan >= 2` 的 `intel` 節點，不新增第二套信任旗標。
- [ ] 任務啟動後，筆記 UI 的「工作」分類出現「暗巷三樓的舊物」。
- [ ] 重複對話不重開任務、不重置 step、不觸發任何物質 reward。
- [ ] 任務 completed 後再與晚對話，走完成後短句或不再顯示交任務分支。

#### 7-C 街道後巷偵查事件（GUI）
- [ ] 未接 `alley_backrooms_3f` 時，`apartment_entrance` 的後巷互動文字與既有 `alley_view` 一致。
- [ ] 接任務後第一次互動後巷，顯示危險暗巷描述；文字暗示巷內可能有人打劫 / 監視器照不到，但不引入戰鬥系統。
- [ ] 接任務後互動後巷不轉場、不進後巷。
- [ ] 後巷偵查後，Quest step 推進到 `checked_alley` 或等價步驟。
- [ ] 工作筆記更新，內容明確寫出：後巷不能硬走、主角想起自己住在四樓、窗外連通平台可能可繞到三樓。
- [ ] 再次互動後巷不重複推進、不重複新增筆記。

#### 7-D 公寓窗戶條件式入口（GUI）
- [ ] 未接任務時，公寓窗戶不可進入外牆場景。
- [ ] 接任務但尚未偵查後巷時，公寓窗戶仍不可進入外牆場景。
- [ ] 完成後巷偵查 / 工作筆記更新後，窗戶顯示可互動 prompt。
- [ ] 從窗戶互動後轉場到 `apartment_fire_escape:from_window`。
- [ ] 從外牆回公寓時使用 `apartment:from_fire_escape`，玩家落在窗邊，不落在門邊；不播放 wake_bed 開場獨白。

#### 7-E `apartment_fire_escape` 場景骨架（GUI / inspection）
- [ ] SceneRegistry 包含 `apartment_fire_escape`，唯一 entry point 為 `from_window`。
- [ ] `apartment_fire_escape` 檔案放在 `scenes/levels/apartment_fire_escape/` 資料夾下（`.tscn/.gd` 同資料夾），不散放於 levels 根目錄。
- [ ] 場景相機可看見垂直多層動線：4F 平台、梯子攀爬、3F 天橋與箱子互動區皆不被固定 y 相機裁掉。
- [ ] 玩家從右棟四樓窗戶位置進場。
- [ ] 場景含右棟四樓平台、右棟梯子、右棟三樓平台、中央空隙 gap、3F 天橋 / 簡易連通橋、左棟三樓平台、左側箱子、亮燈住戶窗。
- [ ] 3F walk-line 在 gap 處只透過天橋 collision / walk segment 連接；玩家不能直接走空中或穿過空地。
- [ ] 玩家可從右側四樓平台走到梯子，下到右棟三樓，走過天橋到左棟三樓箱子。
- [ ] 右側四樓窗戶 `WindowReturn` 互動後回 `apartment:from_fire_escape`。
- [ ] 場景內沒有從街道後巷直接進入的入口。

#### 7-F 梯子攀爬模式 + 外牆禁存（GUI / 觸控）
- [ ] 玩家靠近梯子可進入 climb mode；進入時 x snap 到梯子中心。
- [ ] climb mode 時可上下移動，y clamp 在梯子上下端。
- [ ] climb mode 時不套用一般 `walk_line_y` 鎖定；離開梯子後才回到對應平台 walk line。
- [ ] 玩家可從四樓爬到三樓，也可從三樓原路爬回四樓。
- [ ] 玩家可在三樓經天橋跨過 gap 到左棟平台，再原路回右棟梯子。
- [ ] climb mode 中按 E 只離開梯子 / snap 到平台，該次 E 不會同時觸發 `WindowReturn`、`QuestBox` 或其它互動物；離梯後需再按一次 E 才觸發平台互動。
- [ ] climb mode 不提供跳躍 / 跌落 / 斜向 platforming。
- [ ] 外牆場景中開暫停選單時，Save 入口不可用或提示「此處不能存檔」；不得寫入 save slot。
- [ ] `apartment_fire_escape.gd._exit_tree()` 恢復 `SaveSystem.can_save_here = true`；回到公寓或街道後 Save 入口恢復可用。
- [ ] 觸控操作下也可進入梯子、上下爬、離開梯子；不誤觸背包 / 筆記按鈕。

#### 7-G 三樓箱子搜索 + A 物品取得（GUI）
- [ ] 未達任務條件時，箱子不可取得 A，或只顯示非任務描述。
- [ ] 達條件後搜索箱子，文字提到亮燈窗戶 / 有人住 / 主角盡量不發出聲音。
- [ ] 搜索後取得 A「早期 AI 助理啟用盒」。
- [ ] 取得 A 時跳異常提示，暗示盒底有卡扣聲 / 第二層重量，但不明說按 R 教學。
- [ ] 取得 A 後 quest step 推進到 `found_activation_box` 或等價步驟，工作筆記更新。
- [ ] 背包滿時，搜索提示背包放不下，不推進任務、不設 quest flag、不更新工作筆記、不吞掉 A；箱子仍可互動，公寓窗戶仍可進外牆，整理空間後可再拿。

#### 7-H R 查看 A 得 B（GUI / headless item logic）
- [ ] A 的 item description 顯示其為保存良好的早期 AI 助理啟用盒，有收藏價值。
- [ ] A 的 metadata `usable == false`，不出現或不執行 E 使用路徑；取得 B 只走 R 查看。
- [ ] 第一次 R 查看 A，顯示打開夾層描述並取得 B「舊式 AI 授權模組」。
- [ ] R 查看取得 B 後，A 仍留在背包，item_id 不改、不消耗。
- [ ] B 的 item description 說明其像舊系統 / AI 終端 / 維修門可用的授權鑰，但 Phase 7 不實作用途。
- [ ] 再次 R 查看 A 不重複取得 B。
- [ ] 背包滿時 R 查看 A 提示放不下，不設 quest flag、不吞 B；A 仍可再次 R 查看，整理空間後可取得 B。

#### 7-I 回報晚 + 任務完成（GUI）
- [ ] 未接任務、任務未到 `found_activation_box`、或背包沒有 A 時，晚的交任務分支不顯示。
- [ ] 任務 active、step 為 `found_activation_box` 且背包持有 A 時，晚的對話出現交任務分支。
- [ ] 交任務只需要 A；B 不被檢查、不交出。
- [ ] 交出 A 後，A 從背包移除，Quest status 變 completed。
- [ ] 若資料異常導致交任務節點被觸發但 `remove_item("early_ai_assistant_activation_box", 1)` 回 false，Quest status 保持 active，不更新 completed 工作筆記。
- [ ] 若玩家已先 R 查看 A，B 保留在背包。
- [ ] 若玩家未查看 A 直接交出，B 不補發；任務仍完成。
- [ ] 工作筆記更新為 completed，並不再顯示 active 目標。
- [ ] 任務完成後再次找晚，不重複收 A、不觸發任何物質 reward。

#### 7-J 回歸與存讀檔驗證（headless / GUI）
- [ ] 在接任務後存檔 → 讀檔，晚不重發任務，工作筆記仍在「工作」分類。
- [ ] 在後巷偵查後存檔 → 讀檔，公寓窗戶仍可互動。
- [ ] 在取得 A 但未查看 A 時存檔 → 讀檔，A 仍可 R 查看取得 B。
- [ ] 在已取得 B 但未交任務時存檔 → 讀檔，A/B 都保留，且不重複給 B。
- [ ] 任務完成後存檔 → 讀檔，A 已移除、B 視是否取得而保留、工作筆記 completed。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。

#### 7-K 任務結局分支（headless / GUI）
> 驗收意圖見 `遊戲規格書.md > Phase 7 > 7-K`；實作契約見 `開發設計方針.md > 7-K`。前提：晚不知道盒子有夾層；是否交出 B 由玩家選擇。

**Dialogue effect / 分流**
- [ ] `DialogueRunner._apply_effect()` 支援 `add_credits`，會呼叫 `GameState.add_credits()`；舊 op 行為不變。
- [ ] 葉節點 effect 順序為 `remove_item` 在前；若 `remove_item` 回 false，後續 `add_credits` / `add_int` / `set_flag` / `complete_quest` 不執行（runner break），任務不誤完成、不誤發錢。
- [ ] 回報時未持有 B：直接走平淡交還，不出現二選一。
- [ ] 回報時持有 B：晚先收下盒子道謝（台詞不提夾層 / 不暗示她知道有模組），再出現「只交啟用盒 / 連模組一起交」二選一。

**結局 A（盲目的清理者）— 未持有 B**
- [ ] 移除 A、credits +500、任務 completed。
- [ ] 結局訊息走「結局 1」。
- [ ] 完成工作筆記為 A 版（暗示盒子掂起來偏重、自己多心，未察覺錯過夾層）。
- [ ] `gave_wan_old_module` 維持假；之後找晚走原本 `retalk`（非親近版）。

**結局 B（拾遺者的直覺）— 持有 B、只交啟用盒**
- [ ] 移除 A、B 保留在背包、credits +500、任務 completed。
- [ ] 結局訊息走「結局 2」。
- [ ] 完成工作筆記為 B 版（寫出「刻意把像鑰匙的模組留在自己身上」），與 A 版明顯不同、不只差幾個字。
- [ ] `gave_wan_old_module` 維持假；之後找晚走原本 `retalk`。

**結局 C（托付）— 持有 B、連模組一起交**
- [ ] 移除 A 與 B、credits +1000、`affinity_wan += 2`、set `gave_wan_old_module`、任務 completed。
- [ ] 晚出現特別感謝 / 另眼相看的台詞（驚訝於玩家連這個都掏出來）。
- [ ] 結局訊息走新增的「結局 3」。
- [ ] 完成工作筆記為 C 版（寫出「把可能很重要的鑰匙交給同樣對抗遺忘的人」）。
- [ ] 之後找晚，開場白換成一眼可辨的親近版（`retalk_close`），非原 retalk 開場；語氣是熟朋友 + 小默契，不回顧啟用盒劇情。

**結局訊息三向**
- [ ] `_check_and_trigger_endings()` 依 `gave_wan_old_module` → `found_old_ai_authorization_module` → 皆無，分別觸發結局 3 / 2 / 1，且只觸發一次（`alley_backrooms_ended` 守衛）。
- [ ] 結局 1 / 2 訊息中的互相提示文字已更新，含「交出模組 → 結局 3」，提示與實際分支一致。

**存讀檔回歸**
- [ ] 三種結局各自完成後存檔 → 讀檔：credits、`affinity_wan`、`gave_wan_old_module`、B 是否在背包、completed 工作筆記版本皆一致。
- [ ] 讀檔後結局訊息不重播（`alley_backrooms_ended` 已存）。
- [ ] 結局 C 讀檔後找晚仍走 `retalk_close` 親近版。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。

### Phase 8 — 便利商店 vertical slice + 買賣系統
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 8`；實作契約見 `開發設計方針.md > Phase 8`；場景敘事見 `subdocs/地點/便利商店.md`。
> Phase 8 不做動態定價 / 時間系統 / 拾遺副業經濟；後門鎖死；店內可存檔。
> 驗收狀態（2026-06-13）：人工對話與買賣系統 GUI / 純觸控走查已完成；下方清單保留作回歸驗收腳本。

#### 8-A 場景骨架 + 轉場（headless / GUI）
- [ ] SceneRegistry 含 `convenience_store`，唯一 entry point `from_street`。
- [ ] `convenience_store` 檔案放在 `scenes/levels/convenience_store/`（`.tscn/.gd` 同資料夾）。
- [ ] 街道 `store_front` 互動由 examine 改為門 → 轉場 `convenience_store:from_street`，玩家落在自動門內側，不播開場。
- [ ] 店內互動自動門 / 出口 → 回 `apartment_entrance:from_store`，玩家落在店門口附近。
- [ ] 店內可手動存檔（暫停選單 Save 入口可用）；離開店後仍可存。
- [ ] headless 載入 `convenience_store` PASS；街道↔店內雙向轉場 + 存讀檔 round-trip PASS。

#### 8-B 前導對話 + 發現錯誤旗標（headless / GUI）
- [ ] 街道外面販賣機互動文字改為前導 babble（取代既有 `vending_machine`），互動後設 `talked_outside_vendor`。
- [ ] 店內機器人未接任務時對話走前導 babble，對話後設 `talked_store_robot`。
- [ ] 僅互動其中一台時，`discovered_vendor_error` 不設立。
- [ ] 兩台都互動過後，`discovered_vendor_error` 設立，且筆記 UI「線索」分類新增 `clue_vendor_error_lead`；重複互動不重複副作用、不重複新增筆記。
- [ ] 機器人前導 babble 透露「它不認為自己是販賣機」的異常感。

#### 8-C 公寓電腦兩段 gate + 接案（headless / GUI）
- [ ] 從未互動電腦時，第一次互動顯示舊內容（`work_ai_cleanup_role`），並設 `used_room_computer_once`。
- [ ] 即使 `discovered_vendor_error` 已成立，電腦的「第一次」互動仍顯示舊內容、不派工。
- [ ] 已互動過電腦 + `discovered_vendor_error` + 任務未接 → 互動電腦派工，`QuestManager.start("repair_vendor_bot")`。
- [ ] 未發現錯誤時，第二次起互動電腦不派工（顯示舊內容 / 無新訊息）。
- [ ] 派工後工作筆記「便利商店的故障機器人」出現於「工作」分類。
- [ ] 任務已 active 時再互動電腦不重開任務。

#### 8-D 蒐證 + 診斷對話樹（headless / GUI）
- [ ] 接案前，5 個線索物件不可 examine 或只給模糊 prompt；基礎 flavor 物件（貨架 / 熟食機 / 冷藏櫃）接案前可看但只給與維修無關的淺說。
- [ ] 接案後，5 個線索皆可 examine，各寫一筆 `clue_*`（category 線索）筆記；重複 examine 不重複新增。
- [ ] 機器人診斷對話含誤導選項；正確選項需對應 `clue_*` 筆記存在才成立 / 命中真相分支。
- [ ] 蒐證齊全（5 筆）+ 全程選對 → 設 `understood_robot_truth`（quest flag）。
- [ ] 任一線索缺漏或中途選到誤導 → 落 partial，只設 `mainframe_revealed`，不設 `understood_robot_truth`，對話結尾引導去主機直接重置。
- [ ] 對話可反覆：補查線索後重談可由 partial 升到 truth；旗標只設不回退。
- [ ] 診斷對話過程揭露店員「將被辭退 → 自暴自棄 → 拒絕工作」與機器人讀爆自己的因果。

#### 8-E 店籍主機三段 + 兩種結局（headless / GUI）
- [ ] `mainframe_revealed` 前，店籍主機不可互動 / 不出 prompt。
- [ ] 段1（揭示但診斷不足）：互動只說明「這是店籍主機」，不提供重置。
- [ ] 段2（診斷完成、未全對）：互動提供「直接重置」。
- [ ] 段3（`understood_robot_truth`）：互動提供「直接重置 / 先錄殘響再重置」二選一。
- [ ] 直接重置：設 `store_robot_resolution = "reset"`、`vendor_bot_repaired`、`complete("repair_vendor_bot")`。
- [ ] 先錄殘響再重置：取得 `clerk_echo_recording` + 筆記後，設 `store_robot_resolution = "gleaned"`、`vendor_bot_repaired`、complete。
- [ ] 先錄殘響時背包滿：整筆中止，不設旗標、不重置、不吞殘響，可整理後重試。
- [ ] 修復完成訊息暗示外面那台販賣機因連線也恢復。
- [ ] 完成工作筆記依 `store_robot_resolution` 分 reset / gleaned 兩版，文字明顯不同。
- [ ] 重置後店籍主機不再提供重置；機器人招呼改 repaired 版（reset 純 AI / gleaned 殘留人味）。
- [ ] 殘響 `clerk_echo_recording` 不可賣、不可丟（`sellable=false`、`discardable=false`）。

#### 8-F 買賣系統核心 + ShopPanel（headless / GUI / 觸控）
- [ ] `ITEMS_DB` 可賣物有 `value`；`is_sellable` 對 key_item / 殘響回 false、對一般有值物回 true。
- [ ] `get_sell_value` = `floor(value * SELL_RATIO)`。
- [ ] 折算賣出價為 0 的物品（value 過低）`is_sellable` 回 false，右欄灰掉不可賣。
- [ ] 修好前與機器人互動只走對話，不開 ShopPanel。
- [ ] 修好後與機器人互動先播 repaired 招呼（依結局分流），招呼結尾自動開 ShopPanel（`UIMode.SHOP`），玩家移動凍結。
- [ ] ShopPanel 左欄顯示店內貨架名稱 / 買價 / 庫存；右欄顯示背包，可賣物顯示售出價、不可賣灰掉；頂部顯示 credits。
- [ ] 左欄 E 買入：credits 足 + 庫存 > 0 + 背包有空間才成功，扣 credits、扣庫存、入背包；任一不足給對應 toast 且狀態不變。
- [ ] 右欄 E 賣出：可賣且持有才成功，移除物品 + 加 credits；不回補店庫存。
- [ ] 賣出以**焦點格 instance_id** 定位：同一 item 多 stack / 多 instance 時，右欄 E 只賣目前 focus 的那一格，不會用 `remove_item(item_id)` 掃到其他格、不誤動非焦點格。
- [ ] 已裝備物不可賣：右欄已裝備格灰掉、E 賣出被擋（toast 提示先卸下）且狀態不變；手動卸下後可賣。
- [ ] 庫存買到 0 後該品項不可再買。
- [ ] `ui_cancel` 關閉 ShopPanel 回 `UIMode.NONE`，無殘留。
- [ ] 觸控操作下可開店、切欄、買賣、關閉，不誤觸背包 / 筆記。
- [ ] `shop_states` 納入存讀檔：買掉部分庫存後存檔 → 讀檔，庫存與 credits 一致。
- [ ] `reset_for_new_game()` 後 shop_states 清空（重新 lazy-init）。

#### 8-G 迷你飲料商店 + 多商店（headless / GUI）
- [ ] 修好前街道外面販賣機只給前導訊息，不開店。
- [ ] 修好後互動外面販賣機**直接**開 ShopPanel（`street_vending` 飲料 catalog，無對話）。
- [ ] `convenience_store` 與 `street_vending` 庫存各自獨立；在一店買賣不影響另一店庫存。
- [ ] 兩店庫存各自存讀檔 round-trip 一致。

#### 8-H 回歸與存讀檔驗證（headless / GUI）
- [ ] 接案前存檔 → 讀檔：電腦 gate、發現錯誤旗標狀態一致。
- [ ] 接案後 / 蒐證中存檔 → 讀檔：任務 active、已查線索筆記、`understood_robot_truth` / `mainframe_revealed` 一致。
- [ ] 修好後（reset / gleaned 各一）存檔 → 讀檔：`vendor_bot_repaired`、`store_robot_resolution`、殘響是否在背包、完成工作筆記版本、兩店是否可買賣一致。
- [ ] 修好後讀檔，機器人招呼仍走對應 repaired 版。
- [ ] 純鍵盤走查：進店 → 兩台前導 → 電腦派工 → 蒐證 → 診斷全對 → 店籍主機錄殘響重置 → 開店買賣 → 街道飲料商店買賣，全鏈可通。
- [ ] 純觸控走查：同上全鏈可通，無誤觸。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。

### Phase 9 — 拾遺系統（殘響蒐集 + 收藏家）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 9`；實作契約見 `開發設計方針.md > Phase 9`；人物見 `subdocs/人/鹿其琛.md`；場景見 `subdocs/地點/收藏家的店.md`。
> Phase 9 不做「還」流程 / TravelPanel / 時間系統；鑑定鏈不走 QuestManager。

#### 9-A 殘響資料模型 + 筆記投影（headless / GUI）
- [ ] `collect_echo_segment` 首次採集回 true 並 emit `echo_changed`；同一片重複寫入回 false 無副作用。
- [ ] `is_echo_known` / `is_echo_complete`：1 片即 known；全部段齊才 complete。
- [ ] `record_full_echo` 一次寫滿全部段；重複呼叫 idempotent。
- [ ] `echo_progress` 存讀檔 round-trip 一致；`reset_for_new_game()` 後清空。
- [ ] 舊存檔（無 `echo_progress` 欄位）讀檔不報錯，預設空。
- [ ] gleaned 結局新流程：店籍主機錄殘響當下，殘響筆記即出現完整「店員的殘響」。
- [ ] gleaned 舊存檔讀檔：backfill 補上完整店員殘響；reset 結局存檔不補。
- [ ] 筆記「殘響」分頁：未有任何片時列表為空；取得 1 片後該條出現，標題 +（n/N）。
- [ ] 詳情頁未採集段顯示 `????`；採集後即時補進同條（面板開著時也更新）。

#### 9-B 時鐘取模組（headless / GUI）
- [ ] 公寓解謎完成前（無 `identity_door_unlock_method`），時鐘維持原行為，無取模組分支。
- [ ] 解謎完成後互動時鐘：取得 `old_probe_module` + 設 `probe_module_taken` + 線索筆記 `clue_probe_module_lead`。
- [ ] 背包滿時：訊息提示、不給物、不設 flag、不寫筆記；清空格後重試成功。
- [ ] 已取得後再互動時鐘：恢復原行為，不重複給。
- [ ] `old_probe_module` 不可丟、不可賣；`fingerless_gloves` 不可丟。

#### 9-C 暫時通道 + 收藏家的店 + 鑑定（headless / GUI）
- [ ] 街道最右端「離開街區」互動 → 目的地選單（收藏家的店 / 取消）；取消無副作用。
- [ ] 選收藏家的店 → 轉場 `collector_shop:from_street`，落門口、不播開場；店內出口 → 回 `apartment_entrance:from_collector_shop` 落街道右端。
- [ ] 店內可手動存檔。
- [ ] 首見鹿其琛：`first_meet` 對話 + 設 `met_lu`；再談走 `hub`。
- [ ] 持有模組且未裝時，hub 出現鑑定分支；無模組 / 已裝時不出現。
- [ ] 鑑定完成：`old_probe_module` 移除、手套升級為 `gleaner_gloves`（說明更新）、設 `gleaner_gloves_installed`。
- [ ] 手套**裝備中**鑑定：升級後仍在裝備位、裝備狀態不變；手套在背包時：原格位置不變。
- [ ] 鑑定後對話提及「還」這個選項（口頭，無流程）。

#### 9-D 感知採集（headless / GUI）
- [ ] 未裝備拾遺手套（含僅有舊解碼手套）：殘響點無微光、無雜訊、不可互動。
- [ ] 裝備拾遺手套：殘響點微光顯形；接近時雜訊漸強（越近越清楚）。
- [ ] 進入點區域後**靜止 ≥ 1 秒**才出互動提示；期間移動則重置計時、不出提示。
- [ ] E 採集：寫入進度 + 採集 SFX + toast（標題 + n/N）；該點消失。
- [ ] 已採集點在重進場景 / 存讀檔後保持消失。
- [ ] 採集中途卸下手套：所有未採集點即時隱去。
- [ ] 外牆（逃生梯）場景可正常採集，禁存規則不受影響。

#### 9-E 媒體層（GUI / 觸控）
- [ ] 未集滿：殘響詳情頁無媒體控制。
- [ ] 集滿且僅有圖：footer `E: 看照片`；僅有音：`E: 播放錄音`；兩者皆有：`E: 看照片 / R: 播放錄音`。
- [ ] 純文字殘響集滿後也無媒體控制。
- [ ] 照片 overlay 開啟 / 關閉回筆記、無殘留；`ui_cancel` 可關。
- [ ] 錄音播放中再按 → 停止；關閉筆記 / 切分頁 / 切條目 → 停止。
- [ ] 觸控：媒體 E / R 按鈕顯隱與鍵盤一致、可操作。

#### 9-F 內容鋪設（GUI）
- [ ] 4 條殘響全部可集滿；7 採集點分佈：公寓 ×1、街道 ×3、逃生梯 ×1、便利商店 ×1、收藏家的店 ×1（`echo_clerk` 無場上採集點，走 8-E 回寫）。
- [ ] `echo_room401_tenant` 集滿可看舊照片；`echo_song_rain_doesnt_stop` / `echo_clerk` 集滿可播音訊。
- [ ] 雜訊 SFX、採集 SFX、店內 BGM 接上。

#### 9-G 收購（headless / GUI）
- [ ] 無任何已集滿殘響時：收購入口不成立（或被「殘缺的故事不值錢」打發，依實作擇一）。
- [ ] 已集滿未售出的殘響才出現在收購清單；`echo_lu_family` 永不出現。
- [ ] 選定殘響 → 顯示該條專屬評語 → 賣 / 留二選一；留 → 返回不變。
- [ ] 賣：credits 增加 `sell_price`、該條標記已售出。
- [ ] 已售出：筆記轉錄保留、加註「已售出」、媒體控制永久消失；收購清單不再列出。
- [ ] 對未集滿殘響呼叫 `sell_echo`（headless）：回 false 無副作用。

#### 9-H 回歸與存讀檔驗證（headless / GUI / 觸控）
- [ ] 取模組前 / 鑑定前 / 採集中 / 集滿未賣 / 賣出後五節點存讀檔：旗標、手套狀態、echo 進度、credits 一致。
- [ ] 賣出後讀檔：媒體控制仍失效、收購清單仍不列。
- [ ] 純鍵盤走查：時鐘取模組 → 通道進店 → 鑑定升級 → 四場景採集集滿一條 → 看照片 / 播錄音 → 回店賣出，全鏈可通。
- [ ] 純觸控走查：同上全鏈可通，無誤觸。
- [ ] Headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~8 不退化）。

### Phase 10 — 氛圍與演出（Atmosphere & Presentation pass）
> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 10`；實作契約見 `開發設計方針.md > Phase 10`。
> 本階段以 GUI / 真機目視為主，headless 僅作回歸護欄。只在街道 `apartment_entrance` 驗。
> 注意：氛圍「好不好看 / 好不好聽」是主觀目視項——勾選時記錄當下判斷與截圖，不是只看「有沒有出現」。

#### 10-A 街道音景（headless / GUI）
- [ ] `Ambient` bus 存在；雨 bed 與地鐵 player 都 route 到 `Ambient`。
- [ ] 進入街道：雨聲 loop 持續、音量偏低（是底噪不是主角，不蓋過腳步 / 對話）。
- [ ] 地鐵遠轟偶爾出現、間隔不固定（連聽數分鐘，無明顯固定週期感）；單響短、低。
- [ ] 採集殘響並播放照片附音 / 錄音：雨 + 地鐵 + BGM 一同壓低；媒體結束後淡回原音量。
- [ ] EchoPoint 接近漸強雜訊**不**被 duck（仍隨距離漸強，行為同 Phase 9）。
- [ ] 街道只有單一 BGM 來源（無雙軌疊音；ducking 時無殘留未壓的 BGM）。
- [ ] headless：街道載入無 runtime error；環境音節點 + `Ambient` bus 存在；`main.duck_ambient` 介面存在、重複呼叫 idempotent。

#### 10-B 不分層視覺基底（GUI / 手機）
- [ ] 街道整體罩上冷藍夜色（`CanvasModulate`）；UI（提示 / 背包 / 筆記 / MessageBox）**不**被一起染色 / 變暗。
- [ ] 雨有兩層縱深（遠稀慢 / 近粗快）、密度與不透明度偏低、雨絲帶霓虹染色非純白。
- [ ] 4080 寬場景任何位置都有雨（走到最左 / 最右皆有，不會某段沒雨）。
- [ ] 畫面底部有落地水花，雨「落地」成立。
- [ ] vignette 暗角輕微聚焦；不過重、不糊掉互動物。
- [ ] 相機 idle 有極輕微微擺；移動時不暈、跟隨手感與 Phase 9 一致。
- [ ] 無 CRT / 掃描線 / 色差（確認沒誤加）。
- [ ] 既有互動 / 解謎 / 對話 / 採集在加氛圍後行為不退化。
- [ ] 手機 / 低效能端：雨粒子 + 夜色開啟後仍流暢（無明顯掉幀）；必要時記錄粒子 amount 上限。
- [ ] headless：街道載入無 error；`CanvasModulate` / 雨粒子 / vignette 節點存在。

#### 10-C-1 看板廣告螢幕 + 路燈柔光池（免分層，可先行 — GUI / 手機）
- [ ] 中央遠景看板上有廣告在「播放」，看起來像**發光螢幕**（掃描線 / flicker），不是貼了張靜態貼紙。
- [ ] 廣告對齊看板矩形、不溢出邊框；透視大致吻合（不歪、不飄）。
- [ ] 廣告風格符合 Art Bible（限色 / 線條 / riso）；內容貼世界觀（AI 改變世界後的在地廣告）。
- [ ] 單張時靠 shader（掃描線 / flicker）就有動感；多張時輪播切換自然（每張停幾秒、轉場不突兀）。
- [ ] 看板不被雨 / vignette 糊掉；繪製順序正確（雨蓋過看板、看板在背景之上）。
- [ ]（選配）路燈柔光池：`Light2D` ≤ 3、軟 / 大 / 弱、貼在既有燈源位置；不過曝、手機流暢。
- [ ] headless：`BillboardScreen`（Polygon2D）存在、`billboard_screen.gdshader` 載入無錯。

#### 10-C-2 分層進階（條件式 — 僅 10-B 通過且決定投入才驗）
- [ ] 街道背景已分層（背景 / 中景 / 前景 + 發光層），風格符合 Art Bible（限色 / 線條 / riso）。
- [ ] 招牌閃爍（壞霓虹 stutter）/ 窗光緩慢呼吸；看起來是「故意的」而非 bug。
- [ ] 視差：移動時遠層慢於近層、有縱深；不露邊。
- [ ] 濕地面反射霓虹：雨夜質感成立（這是 signature 賣點，重點目視）。
- [ ] `Light2D` ≤ 3、皆軟 / 大 / 弱、有來源；手機端仍流暢。
- [ ] kill-switch 確認：若效果不達標，可退回 10-A/10-B 而不破壞前兩段。

#### 10-D 晚 idle break（撇一眼暗巷 — GUI）
- [ ] 晚平時定在 idle；每隔幾秒撇一眼右側暗巷再回正；節奏自然、看起來是「故意的」非 bug。
- [ ] 新動畫幀接得上現有 idle：臉 / 線條 / 配色 / 比例不漂移；符合 Art Bible。
- [ ] 撇頭方向朝暗巷（畫面右側），呼應「他在留意那條巷子 / 留意你」。
- [ ] `talk_wan` 對話互動仍正常觸發、不被 idle break 卡住。
- [ ] 可獨立移除：刪 `IdleBreak` 節點後晚回到靜態 idle，場景其餘不受影響。
- [ ] headless：`NpcWan` 之 `AnimatedSprite2D` 有 `idle` / `idle_glance` 動畫、`IdleBreak` 節點存在。

#### Phase 10 回歸（headless）
- [ ] `test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~9 不退化）。
- [ ] EchoPoint 採集 / ducking / 存讀檔行為不退化。

### Phase 11+ — 主線《雨還沒停》v2.3（順敘版）
> 驗收意圖見 `遊戲規格書.md > Phase 11+`；實作契約見 `開發設計方針.md > Phase 11+`；敘事事實來源見 `subdocs/主線/雨還沒停v2.3.md`。
> **順敘執行、不跳號**：逐 Phase 開工才把該 Phase 清單展開到完整操作步驟；以下為規劃層 checklist 骨架。
> 通則：每個 Phase 末子階段做存讀檔 round-trip；每個 Phase 都跑「Phase 1~10 + 已完成主線 Phase 不退化」回歸。戰鬥 / 跳躍 / 氛圍 / 結局演出「對不對」＝GUI / 真機目視，headless 僅護欄。

#### Phase 11 Trust/Trace + 清洗縫（headless）— as-built（test_runner Phase 11 區）
- [ ] `trace` 預設 0；`add_trace` 累加（正負皆可、`add_trace(0)` no-op）；`get_trace` 正確。
- [ ] `to_save_dict` 含 `trace`；`load_save_dict` 還原；**缺 `trace` 鍵時 →0**（向後相容）；`reset_for_new_game` 歸零。
- [ ] `get_trust("wan")` == `affinity_wan`；未知 target → 0。
- [ ] **賣**殘響（`sell_echo`）→ trace↓（集中在 `sell_echo`，非對話 effect）。
- [ ] DialogueRunner `add_trace` effect op 可改 trace（留 / 還未來用）。
- [ ]（留 / 還的對話觸發尚未存在，於 Phase 18/20/鹿線落地時補測。）
- [ ] trace / trust 不對玩家顯式露出（無數值 UI）。
- [ ] 清洗**不**在本階段出現（併 Phase 27）。

#### Phase 12 跳躍 / 平台（headless / GUI / 觸控）
> 驗收意圖 + 架構決策見 `遊戲規格書.md > Phase 12`；實作契約見 `開發設計方針.md > Phase 12`。
> 採 walk-line 之上腳本化拋物弧 + 抓邊緣，不引入全域重力；不動 `climb_mode`。手感為主觀 GUI 項。

##### 12-A 跳躍狀態（headless / GUI）
- [ ] headless：`InputMap.has_action("jump")` 為真。
- [ ] headless：player 有 `is_jumping()` + 跳躍參數（`jump_height` / `jump_duration` / 抓取窗 / `air_speed_scale`）。
- [ ] headless：起跳 → 落回後 `get_save_x()` 不變；`snap_to_walk_line()` 仍回 walk line；`climb_mode` 行為不退化。
- [ ] GUI：地面短跳可起跳；空中可用左右微調落點；落回原 walk line。
- [ ] GUI：**無二段跳**（空中再按 jump 無效）、**無蹬牆**。
- [ ] GUI：室內 / 無平台場景按 jump 只是原地小跳、無副作用（走動 / 互動 / 採集不退化）。
- [ ] GUI：`jump` 動畫起跳時播放、落回 walk/idle。

##### 12-B 平台 / ledge / gap + 原型床（headless / GUI）
- [ ] headless：`LedgeArea2D`（`ledge_area.gd`）節點 + `contains_x` / `get_target_y` API 存在；`jump_proto` 載入無 runtime error。
- [ ] GUI：原型床可完成序列——跨過 gap 落到對側、跳上較高 ledge（抓邊緣吸附）、抵達 ledge 上的採集點並可採集。
- [ ] GUI：抓邊緣容錯寬鬆（不需像素級精準）；沒抓到時落回較低 walk line、可重試、不卡死。

##### 12-C 觸控（headless / 純觸控 GUI）
- [ ] headless：TouchControls `BtnJump` 節點存在且綁定 `jump` action。
- [ ] 純觸控：可跳、可抓邊；Jump 鈕只在世界模式顯示，面板 / 對話 / 暫停時隱藏。
- [ ] 純觸控：Jump 鈕不誤觸其他鍵；DPad 左右 + Jump 可同時操作（空中微調）。

##### Phase 12 回歸（headless）
- [ ] `test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~11 不退化）。
- [ ] 既有走動 / 攀爬（`climb_mode`）/ 存讀檔座標 / 入口落點行為不變。

#### Phase 13 戰鬥（headless / GUI / 觸控）
> 驗收意圖 + 架構決策見 `遊戲規格書.md > Phase 13`；實作契約見 `開發設計方針.md > Phase 13`。
> 棍子只 stun、kill＝繞後格式化（限機器）、人類不格式化、輸=岔線無 Game Over；複用 12 跳躍 + E 長按。手感為主觀 GUI 項。

##### 13-A 棍子 stun + 敵人 AI（headless / GUI）
- [ ] headless：`InputMap.has_action("attack")` 為真。
- [ ] headless：`enemy_base` / walker 狀態機（patrol / stunned 自修復 / recover）+ `is_stunned()` + stun 計時存在；**無 HP 欄位**。
- [ ] GUI：揮棍命中敵人 → 倒地自修復（stun）；棍不壞、無升級、無血條；敵人不會被「打死」。
- [ ] GUI：敵人只在右側區域巡邏；受擊倒地自修復，起身後恢復巡邏。**本階段不驗察覺 / 追擊**。

##### 13-B 繞後格式化（限機器，headless / GUI）
- [ ] headless：`machine_enemy.can_format()` 只在敵人 `is_stunned()` 且玩家在**背後** X 距離 `160px` 內為 true；正面 / 非 stun 狀態 false。
- [ ] headless：`can_format` 時按住 `interact_primary` `2.0s` → defeated 旗標；鬆手 / 離區 / walker 起身 / 進 UI 或對話 → 進度歸零、不 defeated、可重試。
- [ ] headless：defeated 後 walker 留在場上，播放停機 / prone frame，不消失、不爆炸。
- [ ] GUI：輕跳或走位繞後（用 Phase 12 跳躍）→ 按住 E 2 秒 → 格式化重置成功；正面或 walker 未倒地時按 E 打不開格式化。

##### 13-C 人類敵人分支（headless / GUI）
- [ ] headless：`human_enemy.can_format()` 恆 false（無格式化路徑）。
- [ ] headless：攻擊人類骨架不會 set defeated；可接 dialogue / escape flag。
- [ ] GUI：本階段可不放正式人類敵人圖；正式人類解法（對話 / 環境阻隔 / 潛行撐過）留到 Phase 23 / 25 作者化。

##### 13-D 輸 = 岔故事線（headless / GUI）
- [ ] headless：`combat_proto` 中 walker 起身後碰到 / 抓到玩家一次 → `combat_proto_failed = true`，玩家送回同場景左側安全點（約 `x = 250`），顯示替代訊息；**無 Game Over / 無死亡重來 / 不強制讀檔**。
- [ ] GUI：戰鬥失敗後可從安全點續玩（不回到戰鬥前讀檔）；walker 仍只在右側區域巡邏，不連續觸發失敗訊息。

##### 13-E 原型測試床（headless / GUI）
- [ ] headless：`combat_proto` 載入無 runtime error；敵人 / 棍 / 格式化組件節點存在。
- [ ] headless：Phase 13 戰鬥責任位於 `combat_proto`；`jump_proto` 不再是長期戰鬥驗收場。
- [ ] GUI：原型可打通——倒地自修復 stun → 繞後 → 長按 E 2 秒格式化 → defeated 留場停機。

##### 13-F 觸控（headless / 純觸控 GUI）
- [ ] headless：TouchControls `BtnAttack` 存在且綁 `attack`；格式化長按沿用 `BtnE`。
- [ ] 純觸控：可揮擊 / 繞後 / 格式化長按；戰鬥鈕只在世界模式顯示，面板 / 對話 / 暫停隱藏；不誤觸其他鍵。

##### Phase 13 回歸（headless）
- [ ] `test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~12 不退化）。
- [ ] 無敵人場景按 `attack` 無副作用；走動 / 跳躍 / 互動 / 對話 / 存讀檔行為不變。

#### Phase 14 Act 1 鉤子（headless / GUI）
> 旗標 / 組件契約 + final 台詞見 `開發設計方針.md > Act 1 主線鉤子（Phase 14，實作契約）`。擺位定案：街道 `apartment_entrance` x≈1500（door x=730 ↔ store x≈2618 之間，必撞）。

##### headless
- [ ] `memory_fragment_area.gd` 載入無錯；`body_entered` 觸發路徑設 `mem_frag_linfei_1`、`show_message(STORY_MESSAGES["mem_frag_linfei_1"])`、已觸發旗標去重不重播。
- [ ] 鹿「從線上面下來的」鉤（gate `mem_frag_linfei_1` AND `not lu_hinted_topside`，effect `lu_hinted_topside`）：未撞林霏前不出、撞後出、重講不重設。
- [ ] 晚瞥見失神鉤（gate `mem_frag_linfei_1` AND `not wan_noticed_daze`，effect `wan_noticed_daze`）：條件正確、就位、不破壞 5-D / 7-B（met_wan / retalk / retalk_close / intel）。
- [ ] 阿達①店控誤認分支（gate `mem_frag_linfei_1` AND `not ada_misrecognized`，effect `ada_misrecognized` + `talked_store_robot`）：未修好店控首談即可觸發，播完回既有 babble；不破壞 Phase 8 既有 babble / diagnose / repaired / quest / 買賣路由。
- [ ] 四旗標 round-trip（save → load 保留、缺鍵→預設、reset 歸零）。
- [ ] Phase 1~13 headless 不退化（`talk_wan` / 鹿鑑定 / 店控 repaired / 買賣 / 存讀檔）。

##### GUI 走查
- [ ] 出公寓往右走向便利商店，**必經 x≈1500 撞到林霏碎片**，「一震」短訊只播一次；折返再過不重播。
- [ ] 撞林霏後：與鹿 / 晚 / 店控機器人對話，三條鉤在條件成立後出現、旗標就位；未撞前三鉤皆不出。
- [ ] 店控誤認在 babble 前置（未修好）狀態首談觸發；首談後可繼續既有 babble，`talked_store_robot` 仍成立。
- [ ] 存讀檔後一震不重播、三鉤不重觸發、旗標不重設。

#### Phase 15 Act 2 場景骨架（headless / GUI）
> 契約見 `開發設計方針.md > Act 2 場景骨架（Phase 15，實作契約）`；場景設計見 `subdocs/地點/地鐵站.md` / `subdocs/地點/地下道聚落.md`。**本 Phase 不暫代**：真美術 / 真 BGM / final flavor。
- [ ] `subway_station` + `underground_settlement` 載入無 error；SceneRegistry 註冊 + entry points 齊全。
- [ ] 全鏈雙向轉場：街道→地鐵→聚落→地鐵→街道，每段落點正確；round-trip 無孤兒落點。
- [ ] 街道 travel 選單「地鐵站」目的地：Act 1 解鎖 gate 未成立不出、成立後出；`travel` → `subway_station:from_street`。
- [ ] `reached_settlement` 首抵設旗標、跨存檔保留、缺鍵→false、reset 歸零。
- [ ] 兩場景 `can_save_here == true`；存讀檔 round-trip。
- [ ] flavor examine **final 文案**就位（非佔位 / 非「待填」）。
- [ ] 真掛點登記齊全（NPC 站位 / EchoPoint / 戰鬥入口在 subdocs 開放時機表），場景中無假 NPC / 假物件 stand-in。
- [ ] GUI：地鐵站 / 聚落 BGM 為**專屬曲**（非借既有 6 軌）；美術 final（非 placeholder 幾何）。
- [ ] Phase 1~14 headless 不退化（既有 travel / 轉場 / 存讀檔）。

#### Phase 16 三張臉 NPC（headless / GUI）
> 契約見 `開發設計方針.md > 三張臉 NPC 落地（Phase 16，實作契約）`；人設 / 對話定稿草案 / 旗標見 `subdocs/人/三張臉.md`；NPC 站位見 `subdocs/地點/地下道聚落.md`。**三前提硬規則**：主角失憶不知「林霏」名字；對話不准出現「林霏」；七號不明說妹妹。

##### headless
- [ ] `dialogue_db.gd` 註冊 `cen` / `wu` / `seven` 三樹；`get_tree_for` 三者皆非空、載入無 error。
- [ ] 三樹 `start` 條件路由：`met_*==false` 走 `first_meet`、`met_*==true` 走 `retalk`。
- [ ] 小岑：自我介紹節點設 `met_cen` + `affinity_cen +1`；**被抓包冷收尾分支**可達（純對話、無偷竊系統）且設 `met_cen`、不洗白。
- [ ] 伍姐：維修人節點設 `met_wu` + `affinity_wu +1`；**建立者節點（C 版）**設 `met_wu` + `knows_settlement_had_maker` + `affinity_wu +1`，內容含「她只是比我們晚一點壞掉」且**不具名**；追問 `ask_maker_more` gate＝`knows_settlement_had_maker` 條件正確、仍不具名。
- [ ] 七號：鋪墊鉤設 `met_seven` + `seven_hinted_name_topside`，含「我有一個名字還掛在上面」、**全程不提妹妹**；追問 `ask_name` gate＝`seven_hinted_name_topside` 條件正確。
- [ ] **三樹 text 全文不含「林霏」字串**（敘事不破功斷言）。
- [ ] 六旗標 round-trip（`met_cen`/`met_wu`/`met_seven` + `knows_settlement_had_maker` + `seven_hinted_name_topside` + `affinity_*` int）：save→load 保留、缺鍵→預設、reset 歸零。
- [ ] 跨場景旗標保留（離開聚落再回不重置；首見→重講路由正確）。
- [ ] Phase 1~15 headless 不退化（既有 `wan` / `store_robot` / `lu_qichen` / travel 對話、聚落兩室轉場 / `reached_settlement` / 存讀檔）。

##### GUI 走查
- [ ] 進聚落：左室帳篷群與小岑、右室與伍姐（淨水站前）/ 七號（深隧道口牆邊）對話可玩；NPC 站位對齊背景。
- [ ] 首見走 `first_meet`、再談走 `retalk`；暖向選項加好感。
- [ ] 小岑「（手放規矩點）」被抓包冷收尾可達；伍姐建立者節點 / 追問、七號鋪墊鉤 / 追問條件正確。
- [ ] 純觸控：D-pad 選項 + 確認推進三人對話（沿用 Phase 5-C，不需新接線）。
- [ ] 立繪 / 角色圖到位後風格合規（Art Bible Ch3；三人不可大面積飽和橙＝主角專屬）；未到位前 placeholder 不卡對話邏輯驗收。

#### Phase 17 Act 2A 碎片 + 殘響（headless / GUI）
> 契約見 `開發設計方針.md > Act 2A 碎片 + 殘響鋪設（Phase 17，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 17`；真掛點見 `subdocs/地點/地鐵站.md`（通勤螢幕碎片錨）/ `subdocs/地點/地下道聚落.md`（右室殘響點）。**前提硬規則**：碎片不揭主角身份；聚落殘響不得是林霏 / 阿達、segment 不含「林霏」。

##### headless
- [ ] 17-A：`MemoryFragmentArea` 在 `subway_station_platform`，`fragment_flag`/`message_id="mem_frag_commute_topside"`；模擬進區設旗標、`STORY_MESSAGES["mem_frag_commute_topside"]` 非空且為失神基調（不揭身份 / 不點名）。
- [ ] 17-A：一次性——旗標已 true 不重觸；round-trip（save→load 保留、缺鍵→false、reset 歸零）。
- [ ] 17-B：`EchoDB.has_echo("echo_settlement_erased")` 真；`get_segment_count` 與 `segments` 一致；`collect_echo_segment` 寫 `echo_progress`、重複採集無副作用。
- [ ] 17-B：`echo_progress` round-trip（collected / sold 保留、缺鍵→預設、reset 清空）；採集後點以 `echo_progress` 判定永久消失（跨存讀檔不復生）。
- [ ] 17-B：媒體層可選——echo 缺 `image_path`/`audio_path` 不報錯（採集 / 筆記不依賴媒體）。
- [ ] **斷言**：`echo_settlement_erased` 任一 segment text 不含「林霏」。
- [ ] Phase 1~16 headless 不退化（碎片①、既有 4 條殘響採集 / 賣留、聚落兩室轉場 / 三 NPC 對話 / `reached_settlement` / 存讀檔）。

##### GUI 走查
- [ ] 回月台經停運通勤螢幕：碎片訊息跳一次（失神基調），再經不重跳。
- [ ] 進右室、裝拾遺手套：殘響點微光 + 漸強雜訊；靜止 ≥1.0s 出提示、E 採集、toast「殘響已記錄」；未裝手套完全無感、無提示。
- [ ] 殘響點不與七號 / 伍姐互動區、P18 戰鬥入口重疊；採集後點消失。
- [ ] 集滿後筆記「殘響」分頁有此條；媒體到位後 E/R 媒體鍵出現（未到位不出、不報錯）。

#### Phase 18 Act 2B 戰鬥 + 回執（headless / GUI）
> 契約見 `開發設計方針.md > Act 2B 戰鬥遭遇 + 回執取得（Phase 18，實作契約）` 及「戰鬥場景互動契約（Phase 18，前瞻契約）」；驗收意圖見 `遊戲規格書.md > Phase 18`；敘事 §4.2.1 / §4.2.2。**前提硬規則**：回執表面＝雜物物流單、不揭七號 / 妹妹 / 林霏；無 Game Over；清潔機格式化＝停機不死。

##### headless
- [ ] 18-A：`tunnel_combat` 載入無錯；walker_01 / Player / 失物物流箱 / 出口節點存在；Player `combat_mode==true`。
- [ ] 18-A：模擬 walker `defeated()` → `tunnel_machine_defeated` 旗標為 true；`deep_tunnel` 互動改為轉場（非 message）。
- [ ] 18-B：失物物流箱 gate `tunnel_machine_defeated`（未 defeated 不給回執）；取得後 `has_item("childcare_supply_receipt")` 真、一次性不重複加。
- [ ] 18-B：背包滿時 `add_item` 回 false、回執未掉件、未重複；`description` 不含「七號 / 妹妹 / 林霏」。
- [ ] 18-C：晚賣回執分支 gate `has_item`；賣出 effect → `has_item` 假 + `trace` 降 + `peace_line_locked` 真；未賣則 `peace_line_locked` 未設、回執保持。
- [ ] 18-D：`tunnel_machine_defeated` / 回執持有 / `peace_line_locked` round-trip（save→load 保留、缺鍵→預設、reset 歸零）。
- [ ] Phase 1~17 headless 不退化（Phase 13 戰鬥組件、Phase 9 殘響、聚落轉場 / NPC / 存讀檔；非戰鬥場景按 attack 無副作用）。

##### GUI 走查
- [ ] 右室深隧道口 E：由封閉提示改為進入戰鬥區；進場戰鬥姿勢（`combat_walk`）、攻擊 / 繞後格式化可玩。
- [ ] 打倒清潔機（自修復窗 2.5 / 4.0 / 8.0 漸寬、格式化按住 3 秒）→ 停機留場；戰敗送安全點 + 替代訊息、無死亡畫面。
- [ ] 清潔機停機後失物物流箱可搜 → 取得回執；背包滿時提示且不掉件。
- [ ] 回街道找晚：賣回執分支出現（持有才出）；賣出後晚台詞 §4.2.2、回執消失；不賣則保留。
- [ ] 純觸控：BtnAttack 攻擊、BtnE 繞後格式化 / 搜箱 / 賣出對話皆可推進。

#### Phase 19 Act 2C 阿達罪責（headless / GUI）
> 契約見 `開發設計方針.md > Act 2C 阿達罪責（Phase 19，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 19`；敘事 §3（四段式 ②③，③鎖 2C）。**前提硬規則**：阿達本人不登場（④留 Phase 26）、罪責中檔不洗白不加碼、三拍文字不含「林霏」、順序鎖死（殘響→工單→碎片）。

##### headless
- [ ] 19-A：`EchoDB.has_echo("echo_ada_reset")`；`get_segment_count` 與 `segments` 一致；`collect_echo_segment` 寫 `echo_progress`。
- [ ] 19-A：採集完成觸發 `trace_on_collect`（trace +1）**一次**；重複採集 / save→load 載入不重加；其餘殘響（無此欄位）trace 行為不變。
- [ ] 19-A：可賣——`sell_echo("echo_ada_reset")` → credits 入帳 + `sold` 真 + trace −1（與採集 +1 方向相反）。
- [ ] 19-B：**未採殘響** examine 工單 → 中性提示、`read_old_work_order` 未設。
- [ ] 19-B：**已採殘響** examine 工單 → `read_old_work_order` 真 + `clue_old_work_order` 入筆記（category 身份）；一次性。且按 `R` 查看 `old_work_badge` 時，描述動態追加為 Revealed 版本。
- [ ] 19-B：翻看工單時工作證數量依然為 1，不重複發放工作證。
- [ ] 19-C：碎片 `require_flag="read_old_work_order"` 未達時進區**不觸發**；達成後一次性設 `mem_frag_erased_ada` + 顯示 `STORY_MESSAGES` 文字；二次進區不重觸。
- [ ] 19-C：`MemoryFragmentArea` 既有碎片（`mem_frag_linfei_1` / `mem_frag_commute_topside`，`require_flag` 預設 ""）行為不退化。
- [ ] 19-D：`echo_progress`（collected/sold）/ `read_old_work_order` / `old_work_badge` 持有 / `mem_frag_erased_ada` / `trace` round-trip（save→load 保留、缺鍵→預設、reset 歸零）。
- [ ] 19-D：斷言三拍文字不含「林霏」（`echo_ada_reset` segments / `clue_old_work_order.body` / `mem_frag_erased_ada`）。
- [ ] Phase 1~18 headless 不退化（Phase 9 殘響採集 / 賣留、Phase 14/17 碎片、聚落左室轉場 / 小岑對話 / 存讀檔）。

##### GUI 走查
- [ ] 聚落左室：戴拾遺手套 → 阿達殘響點微光 + 雜訊；靜止採集 → toast；採集後該點消失、trace 隱性 +1（無 UI，靠後續行為 / debug 確認）。
- [ ] 採殘響前 examine 工單：只給看不懂的中性提示；採後 examine：揭露兇手＝你、背包內已有的「磨損的工作證」描述動態變更（多出暗示善後員身份的段落）、筆記身份分頁多一條。
- [ ] 揭露工單後往出口走：觸發碎片「你親手抹過阿達」MessageBox；未揭露前經過不觸發。
- [ ] 賣阿達殘響給鹿：成交、評語、媒體永久失效（沿用 Phase 9 收購）。
- [ ] 純觸控：BtnE 採殘響 / examine 工單 / 推進碎片 MessageBox 皆可。

#### Phase 20 Act 2D 七號可選 + 和平線 D（headless / GUI）
> 契約見 `開發設計方針.md > Act 2D 七號可選 + 和平線 Branch D（Phase 20，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 20`；敘事 §4.2.1 / §4.2.3。**前提硬規則**：稀有恩典不加路標；妹妹永不登場；七號不洗白；賣回執 `peace_line_locked` 永久鎖死 D。

##### headless
- [ ] 20-A：`data/dialogue/seven.gd` 有 Phase 20 交還入口；gate 同時要求 `has_item("childcare_supply_receipt")`、`peace_line_locked != true`、`seven_peace_branch_d != true`。
- [ ] 20-A：缺回執時不出成功入口；已賣回執（`peace_line_locked=true`）時不可達；已 D（`seven_peace_branch_d=true`）時不重複顯示交還入口。
- [ ] 20-A：錯誤語氣 / 逼問 / 嘲諷路線只冷收尾，不移除 `childcare_supply_receipt`、不設 `seven_peace_branch_d`、不改 `affinity_seven` / trace。
- [ ] 20-B：成功交還路線 effect 順序正確：`remove_item("childcare_supply_receipt")` 成功後才 `set_flag("seven_peace_branch_d", true)`、`add_int("affinity_seven", +N)`、Trace 不增或微降。
- [ ] 20-B：若 `remove_item("childcare_supply_receipt")` 失敗，後續 D flag / trust / trace effect 不套用。
- [ ] 20-B：成功後 `seven_peace_branch_d` / `affinity_seven` / trace round-trip（save→load 保留、缺鍵→預設、reset 歸零）；回執已移除但 M1 特殊道具進度不退勾。
- [ ] 20-B：七號台詞不把妹妹寫成登場人物、不給姓名、不保證幸福；不把七號寫成已被感化的朋友。
- [ ] 20-C：未走 D 時 `seven_peace_branch_d` 保持 false；Phase 24 可用 `not seven_peace_branch_d` 初始化 `seven_betrayal_pending`；Phase 20 不因一次錯誤對話過早設 pending。
- [ ] 20-C：Phase 18 賣回執回歸仍 PASS：`peace_line_locked=true` + 回執消失時，七號和平成功入口不可達。
- [ ] Phase 1~19 headless 不退化（Phase 16 七號首見 / 重講 / `ask_name` gate、Phase 18 回執取得與賣出、Phase 11 trust/trace、存讀檔、M1 進度頁）。

##### GUI 走查
- [ ] 右室七號：持回執、未賣、未 D 時，重講對話中可自然看到「把那張回執拿給他看」類入口；UI / 筆記 / 任務不明示路標。
- [ ] 選逼問 / 威脅 / 嘲諷：七號冷收尾；回執仍在背包；可重談但不給提示。
- [ ] 選交還：七號先不信 → 認出代碼 → 「她還在 / 那我不能回去」節拍成立；情緒冷，不寫成溫馨重逢。
- [ ] 成功後回執從背包移除；七號後續重講不再顯示交還入口，給短版冷招呼。
- [ ] 賣過回執後找七號：不出交還入口；不補救。
- [ ] 純觸控：BtnE 進七號對話、方向鍵選項、確認、MessageBox 推進皆可。

#### Phase 21 Act 3 夜總會 + 林霏核心殘響 + 主題歌（headless；原 Phase 22 已併入）
- [ ] 21-A `nightclub` 載入 + 雙向 round-trip。
- [ ] 21-B `echo_linfei` 登記 + 採集（手套 + 靜止 ≥1s + E）；`trace_on_collect` 採集 Trace↑。
- [ ] 21-B 分段媒體門檻：收集達半→歌 slot 解鎖、全滿→照片解鎖；其他殘響「集滿才出」不退化。
- [ ] 21-C 碎片「你看到的是黑戶藏身方式」`mem_frag_hideout`；gate＝採過林霏殘響，未達前不可設。
- [ ] 21-D 女聲主題歌資產載入 + 掛半收集媒體 slot（GUI：採半播歌）。
- [ ] 21-E 《雨還沒停》環境母題播放點存在（複用既有 `echo_song_rain_doesnt_stop.mp3`，非新素材）。
- [ ] 21-F 林霏殘響可賣（集滿才收、`sell_echo`→Trace↓ + credits）+ set `sold_linfei_echo`。
- [ ] 21-F 存讀檔：`echo_progress`（含 sold）/ 半全媒體狀態 / `mem_frag_hideout` / `sold_linfei_echo` round-trip；Phase 1~20 不退化。

#### Phase 22 — 已併入 Phase 21（編號保留不再使用）

#### Phase 23 Act 3 夜總會保全四解小解謎（headless / GUI）
> 契約見 `開發設計方針.md > Act 3 夜總會保全四解小解謎（Phase 23，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 23`；場景設計 `subdocs/地點/夜總會.md > 保全解謎`。**前提硬規則**：保全人類不可打死 / 格式化；四解殊途同歸 `passed_nightclub_security`；大門口閘門無 Trace 無代價；工牌不點名七號 / 林霏 / 妹妹；四解皆不動 Trace；引開 / 潛行 transient 不存檔。

##### headless
- [ ] 23-A：未通過時門面廳 `back_door` 不轉場（gate `passed_nightclub_security` false）；set true 後可反覆轉場進 `nightclub_back`。
- [ ] 23-A：保全 NPC（`Bodyguard` Area2D，`dialogue_id="nightclub_bodyguard"`）存在並擋 `back_door`；大門口 `biometric_gate` examine 不設旗標、不動 Trace、不扣 credits。
- [ ] 23-A：工牌 examine → `add_item("nightclub_staff_pass")` 成功後 set `found_staff_pass`；**背包滿**時 add_item 回 false、不設 flag、不吞道具、可重試。
- [ ] 23-A：`nightclub_staff_pass` 入 ITEMS_DB（`category=key_item`、不可賣、不可丟）；與 `old_work_badge` 為不同 id / 不同顯示名。
- [ ] 23-B：`data/dialogue/nightclub_bodyguard.gd` 註冊進 DialogueDB；`start` 依 `passed_nightclub_security` 路由（已通過＝中性招呼、無解法選項）。
- [ ] 23-B 賄賂：credits=499 無成交分支 / 走回絕；credits=500 成交後 `get_credits()` 減 500 + `passed_nightclub_security` true；回絕路線不扣款、不設旗標、可重試。
- [ ] 23-B 假裝身份：持 `found_staff_pass` 可過並設旗標；無 `found_staff_pass` 被識破、不設旗標、可重試。
- [ ] 23-B：DialogueRunner 新增 `credits` condition type（讀 `get_credits()`，`>=` 比較生效）。
- [ ] 23-C 引開：互動 `bar_bot` → 保全淡出離崗、`_bodyguard_off_post=true`（場景 transient）。
- [ ] 23-C 潛行：離崗空窗期 E `back_door` → set `passed_nightclub_security` + 轉場；**未引開**（`_bodyguard_off_post=false`）時 `back_door` 仍被擋（潛行不可單獨）。
- [ ] 23-C：`_bodyguard_off_post` 不入 `to_save_dict`；重進門面廳（未通過時）保全歸位、騷動狀態不持久。
- [ ] 23-D：`passed_nightclub_security` / `found_staff_pass` / `nightclub_staff_pass` 持有 round-trip（save→load 保留、缺鍵→false、reset 歸零）；M1 特殊道具進度因取得工牌打勾、不退勾。
- [ ] 23-D：四解皆不呼叫 `add_trace`（Trace 不變）。
- [ ] Phase 1~21 headless 不退化（尤其 Phase 21 `echo_linfei` 採集 / 半全媒體門檻 / 後場殘響房可達、`mem_frag_hideout` gate）。

##### GUI 走查
- [ ] 大門口：examine `biometric_gate` 被拒訊息成立、導向側門進門面廳；無卡關。
- [ ] 賄賂：credits 足→成交選項可見、扣 500、後場門開；credits 不足→只冷回絕、可重試。
- [ ] 假裝身份：先門面廳 examine 取得工牌 → 對保全謊稱身份可過；未取得工牌→被識破。
- [ ] 引開→潛行：碰 `bar_bot` → 保全淡出離崗演出成立 → 空窗期 E `back_door` 溜過；不碰 bar_bot 直接 E `back_door`→被擋。
- [ ] 通過後 `back_door` 反覆進出後場不重跑解謎；保全對話改中性招呼。
- [ ] 純觸控：BtnE 進保全對話 / examine 工牌 / 碰 bar_bot / 溜後場皆可；方向鍵選項、確認、MessageBox 推進皆可。

#### Phase 24 七號小爆點三分支（headless / GUI）
> 契約見 `開發設計方針.md > Act 3→4 七號小爆點（Phase 24，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 24`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §7.2。**前提硬規則**：無 C 分支 / 不做大災難線；D 窗口只在 Phase 20；七號人類不可殺 / 格式化；小岑代價純敘事（無 minigame）；分支確定性可 headless。**判定公式**：`seven_stopped_partial = (get_trace() >= TRACE_BETRAYAL_THRESHOLD) and not (get_trust("wu") >= 2 or get_trust("cen") >= 2)`，否則 `seven_stopped_full`；`TRACE_BETRAYAL_THRESHOLD` 第一版 = 3。

- [ ] 24-A：`data/quests/seven_betrayal.gd` 註冊 `quest_db.gd`；`QuestManager.start` 起 quest。
- [ ] 24-A 爆發 gate：`passed_nightclub_security` false → 不爆；`seven_peace_branch_d` true → 不爆（走 D）；非 D 首進聚落右圖 → set `seven_betrayal_triggered` + `seven_betrayal_pending`；重進不重複爆發（flag-based，與探索順序無關）。
- [ ] 24-B 分支判定：`trace=2` → A；`trace=3` 且有警告（`affinity_wu` 或 `affinity_cen >= 2`）→ A；`trace=3` 無警告 → B；`seven_stopped_full` / `seven_stopped_partial` 互斥；邊界值正確分流。
- [ ] 24-B：`wu.gd` / `cen.gd` 各有 `affinity >= 2` gate 的提前警告分支；`got_advance_warning` 公式直接讀 affinity（不依賴是否看過分支）。
- [ ] 24-C 深隧道口路由：`deep_tunnel_opened` false → 直通 `tunnel_combat`；true → `travel_deep_tunnel` 兩去處選單（`tunnel_combat` / `tunnel_chase` 皆可達）。
- [ ] 24-C：七號逃走 set `deep_tunnel_opened`；兩室 `tunnel_chase`（房間1）/ `tunnel_chase_right`（房間2）載入；房間1正解高門 → `tunnel_chase_right:from_left`；房間2左緣 → `tunnel_chase:from_right`（backtrack）；攤牌出口 → `underground_settlement_right:from_deep_tunnel`；全鏈 round-trip；兩室 `can_save_here=false`。
- [ ] 24-C 隨機真出口：`tunnel_chase_true_exit` 於 `reset_for_new_game()` 設為 0/1/2（`randi()%3`）；save→load 固定不重骰；模擬重進房間2 / 死路折返不重骰。
- [ ] 24-C 倒數（transient）：`_tunnel_chase_time_left` init=180；房間1→2 轉場沿用不重置；走錯一個出口扣 20 秒（同一口只扣一次，已試入 `_tunnel_chase_tried`）；4 個錯口最多扣 80；扣不成負；倒數 / 已試 / 接通% 皆不入 save dict。
- [ ] 24-C 死路：未試過 → 長訊息 + 扣時；已試過 → 短訊息「已知死路」+ 不扣時；折返原地可重試。房間1左緣 → `MSG_TUNNEL_CHASE_MUST_PURSUE` 擋回、不轉場回聚落。
- [ ] 24-C 梯子攀爬：沿用 Phase 7-F 防火梯 climb 機制（梯子互動口 + `player.enter_climb_mode` + 場景 climb 迴圈，同 `apartment_fire_escape.gd`，無新組件）；房間1正解高門、房間2上層出口需爬梯抵達；到頂/底 auto-exit；climb mode 僅追逐兩室使用。
- [ ] 24-C 攤牌觸發：① 走到房間2真出口 ② 倒數歸零——兩路皆播七號攤牌情境圖 overlay（鎖操作）+ 呼叫結算；倒數歸零不改變分支（純張力）。
- [ ] 24-C 攔截結算（攤牌時）：依 24-B 公式 set 分支旗標 + `add_int affinity_seven`（A −2 / B −1）+ B 時 `affinity_wu −1`；`seven_betrayal_pending` 清為 false；`QuestManager.complete`；Trace 不動（不呼叫 `add_trace`）。攤牌 A/B 兩段文字依結算旗標分流。
- [ ] 24-D **Branch B**：`seven_stopped_partial` → set `cen_voiceprint_exposed`；聚落左圖小岑 NPC 條件式移除 + `empty_tent`「帳篷空了 / 踩扁零件」可 examine；A/D → 小岑在場、`empty_tent` 不可 examine。
- [ ] 24-E 存讀檔：`seven_betrayal_triggered` / `seven_stopped_full` / `seven_stopped_partial` / `cen_voiceprint_exposed` / `deep_tunnel_opened` / `tunnel_chase_true_exit` / `affinity_seven` round-trip（缺鍵→false/0、reset 歸零；`tunnel_chase_true_exit` reset 重骰且為 0/1/2）。
- [ ] Phase 1~23 不退化（尤其 Phase 21 `echo_linfei` 採集 / 後場殘響房、Phase 23 四解、`tunnel_combat` 入口仍可達）。
- [ ] 確認**無** C 分支 / **無**大災難線。
- [ ] **外部素材：已全數到貨（2026-06-28）**：`tunnel-chase-left.png` / `tunnel-chase-right.png`（maps/tunnel_chase/）+ 七號攤牌情境圖（sprites/seven_tunnel_showdown/situation/）。
- [ ] GUI / 純觸控：三分支可玩；追逐關限時選門（倒數 + 通話接通 % 顯示、走錯扣時、已知死路短訊息、房間1左緣鎖、房間2出口需攀爬抵達、真出口/歸零皆觸發攤牌情境圖）；兩去處選單；Branch B 帳篷空了演出；BtnE / 對話選項純觸控可推進。

#### Phase 25 Act 4 備份區 + 戰鬥③（headless / GUI）
> 契約見 `開發設計方針.md > Act 4 備份區 + 戰鬥③（Phase 25，實作契約）`；驗收意圖見 `遊戲規格書.md > Phase 25`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §7（Act 4）。**前提硬規則**：人類保全不可打死 / 格式化；無 Game Over / 無失敗岔線；本 Phase 不碰結局 / 不碰 Phase 26 演出 / 不動 Trace；無新存讀檔欄位。**門禁 gate**：`has_item("old_work_badge") AND get_flag("read_old_work_order")`。**travel gate**：`passed_nightclub_security AND (seven_peace_branch_d OR seven_stopped_full OR seven_stopped_partial)`。

- [ ] 25-A 三場景：`datacenter_entrance`（anchor-05）/ `datacenter_backup` / `datacenter_backup_core` SceneRegistry 註冊 + 載入；entrance↔backup↔core + entrance↔nightclub 全鏈雙向 round-trip。
- [ ] 25-A travel gate：`nightclub_entrance` 出「AI 資料中心」目的地；未通 Act 3（`passed_nightclub_security` false）不可達；D（`seven_peace_branch_d`）/ A（`seven_stopped_full`）/ B（`seven_stopped_partial`）三終端旗標各自可達；零新旗標、不改 Phase 24。
- [ ] 25-A 善後員門禁：缺 `read_old_work_order` → 中性「權限不足」訊息（`MSG_DATACENTER_ACCESS_DENIED`）、不轉場；`has_item("old_work_badge")` + `read_old_work_order` → 放行轉 `datacenter_backup:from_entrance`。確認 `old_work_badge` 不可丟（`discardable:false`）/ 不可賣（key_item）→ 無 soft-lock；19-B 自動成 Act 4 前置。
- [ ] 25-A 晚 flavor（不參與 gate）：travel 到資料中心時晚提醒「再去看一眼那張舊卡」；台詞晚出 / 沒出 / 略過皆不卡開門。
- [ ] 25-A 核心掛點：`datacenter_backup_core` 的「自己的備份」examine 給中性佔位（`MSG_DATACENTER_OWN_BACKUP_PLACEHOLDER`），**不觸發結局**（26-D 才接）。
- [ ] 25-B 戰鬥③ 敵人：機器哨兵（`machine_enemy`）`can_format` 背後 stun true、`format_reset` 按住 E → `defeated()` 可清；人類保全（`human_enemy`）`can_format` 恆 false、`apply_stun` no-op（**不可殺 / 不可格式化**）+ 最小追擊（朝玩家 x 移動）。
- [ ] 25-B 勝利＝跑到門：抵達 `datacenter_backup` 右端門 x → 轉場 `datacenter_backup_core:from_backup`；碰撞＝knockback / stagger，**無失敗態 / 無 Game Over / 無 combat_loss 岔線**；非戰鬥場景按 attack 無副作用。
- [ ] 25-B 禁存：戰鬥廊道 `can_save_here=false`；進核心恢復 `true`。
- [ ] 25-C：Phase 1~24 不退化（尤其 Phase 24 追逐 / 攤牌、`tunnel_combat` 入口、聚落往返、Phase 21 `echo_linfei` 仍可達）。
- [ ] **外部素材（blocker，你方提供）**：`datacenter_backup` / `datacenter_backup_core` map 圖、機器哨兵 + 人類保全 sprite、Act 4 專屬 BGM；`datacenter_entrance` 用既有 anchor-05。
- [ ] GUI / 純觸控：三場景可玩；門禁齊全放行 / 缺件擋；戰鬥③ 機器可清 / 人類追不可殺 / 跑到門過關；BtnE / BtnAttack / 對話選項純觸控可推進。

#### Phase 26 Act 4 演出 + 真相碎片（headless / GUI）
- [ ] 晚拉扯（含私心台詞）+ 旗標。
- [ ] 阿達④本人短暫登場一次。
- [ ] 碎片「你自己選擇被刪、為保護那個網」。
- [ ] 站在自己備份前＝結局觸發點；Branch B 時檔案重標記呈現。

#### Phase 27 廣播站 + 結局路由 + 清洗閘（headless）
- [ ] 地下廣播站場景載入（若 blocker 1 拍板成立）。
- [ ] Act 4 備份前 → 三結局路由分派正確；硬鎖規則僅 Expose 硬分。
- [ ] 上傳前清洗閘：`cleaned` 寫入上傳檔；不可逆損細節。

#### Phase 28 Reclaim + Protect（headless / GUI）
- [ ] Reclaim 分派（偏留 / Trust 低 / Trace 高；晚失望消失）。
- [ ] Protect 分派（晚「你又變回來了」、不甜）。
- [ ] 小岑條件式回收：未走 Branch B → 安全通過不認得你；走 B → 永遠缺席 + 伍姐沉默搖頭。
- [ ] 存讀檔。

#### Phase 29 Edited Expose 三判定（headless）
- [ ] A：未清洗 + **trace 未臨界**上傳 → 聚落焚毀（trace 臨界一律先判 C）。
- [ ] B：已清洗 + trace 未臨界 → 匿名雜訊、保住所有人。
- [ ] C：trace 過臨界 → 99% 被攔、當場被抹除（C 優先於 A/B）。
- [ ] 三判定 × 存讀檔。

#### Phase 30 三結局收尾 + 全主線回歸（headless / GUI）
- [ ] 共用收尾（雨 + 《雨還沒停》+ 一點橙）就位。
- [ ] 全主線脊椎 Act 1→結局一輪 headless PASS。
- [ ] 三結局 × 存讀檔矩陣；GUI 走查。

#### Phase 31 P-00 隱藏（⬜ 暫緩，headless）
- [ ] `abandoned_archive` 載入。
- [ ] P-00 三選（Reset / 留[Trace 大升] / 還·公開[可能暴露活人]）+ trace 連動。

#### Phase 11+ 回歸（每 Phase 必跑）
- [ ] `test_runner.tscn` / `verify_game_state.gd` 仍 PASS（Phase 1~10 + 已完成主線 Phase 不退化）。
- [ ] 既有殘響 / 對話 / 任務 / 買賣 / 存讀檔行為不退化。

---

#### Phase M1 進度頁（headless / GUI）
> 契約見 `開發設計方針.md > Phase M1`；驗收意圖見 `遊戲規格書.md > Phase M1`。**Meta / 非敘事 QoL，純加法**。前提：未解鎖顯示 `???`、判定＝曾達成（非當前持有）、總數 31。

##### headless
- [ ] `GameState.get_progress_summary()` 五分類 total 正確（場景10 / NPC6 / 任務3 / 殘響5 / 特殊7）、`overall_total==31`。
- [ ] 場景：`mark_scene_visited("apartment")` 後 scenes.done +1、冪等（重複呼叫不重複計）；`visited_scenes` round-trip（save→load 保留、缺鍵→空、reset 清空）。
- [ ] NPC：`mark_npc_talked("wan")` 計入；白名單外（`travel_street_east` / `store_registry_host`）呼叫不計入；round-trip。
- [ ] 任務：set `left_apartment_once` → 任務 done +1；`quest_states["repair_vendor_bot"].status=="completed"` → done +1；total 固定 3。
- [ ] 殘響：採滿 `echo_clerk` → echoes.done +1；鹿家記事採到現有段數即視為完成（done 計入）；沿用 `echo_progress` round-trip。
- [ ] 特殊道具：`add_item("childcare_supply_receipt")` → `collected_special_items` 含該 id；**賣出 / `remove_item` 後仍打勾**（曾獲得不退勾）；`change_item_id(worn_rubiks_cube→decoder_cube)` 與手套升級 `→gleaner_gloves` 皆標記；round-trip。
- [ ] `old_work_badge` 不在 `PROGRESS_SPECIAL_ITEMS`（即使 add 也不計入特殊道具）。
- [ ] 整體 %：`overall_pct == round(overall_done*100/31)`；全空 0%、全滿 100%。
- [ ] 缺鍵相容：載入無三集合的舊檔 → summary 不報錯、視為空集合 / false。
- [ ] Phase 1~18 headless 不退化（純加法，既有旗標 / 任務 / 殘響 / 買賣 / 存讀檔不變）。

##### GUI 走查
- [ ] 筆記分類列最右出現「進度」，鍵盤 / 觸控可切入。
- [ ] 左列固定 6 項、第一項為「總進度」；總進度顯示五分類 done/total + 整體 %。
- [ ] 選分類 → 右詳情逐項清單 + 計數；已解鎖顯示真名 + ✓、未解鎖顯示 `???`。
- [ ] 走過一個新場景 / 跟一個新 NPC 對話 / 取得一個特殊道具後，重開進度頁計數即時更新。
- [ ] 賣掉回執後，特殊道具「兒少照護補給回執」仍為已解鎖（不退勾）。
- [ ] 純觸控可切入進度頁、可在 6 項間移焦、可翻清單。

#### Phase M2 多國語系 i18n（headless / GUI）
> 契約見 `開發設計方針.md > Phase M2`；驗收意圖見 `遊戲規格書.md > Phase M2`。**Meta / 非敘事 QoL，玩家行為純加法**。前提：三語 `zh_TW`(預設+fallback)/`zh_CN`/`en`、機制＝原生 `tr()`+CSV、簡中與英文 LLM 獨立翻譯、相容 iOS/Android。

##### headless（M2-A 基礎）
- [ ] `LocaleManager.set_locale("zh_CN")` 後 `TranslationServer.get_locale()` == `zh_CN`；非法值退 `zh_TW`。
- [ ] `settings.cfg` round-trip：`set_locale("en")` → 重讀 `_load_saved_locale()` 回 `en`；無檔時回 `""`。
- [ ] `detect_default_locale()` 映射：`zh_TW`/`zh_HK`/`zh_Hant`→`zh_TW`、`zh_CN`/`zh_Hans_CN`/`zh_SG`→`zh_CN`、`en_US`/其餘→`en`。
- [ ] fallback：在 `en` 下查一個只有 `zh_TW` 欄的 key → 回繁中字串（非空、非 key）。
- [ ] 漏譯：查一個三語全缺的 key → `tr(key)` 回 key 本身、不崩潰。
- [ ] 字型：三個 locale 的 `FONT_FOR` 路徑皆 `load()` 成功（`NotoSansSC` 已入專案）。
- [ ] `TranslationServer.get_fallback_locale()` == `zh_TW`。

##### headless（M2-B~E 覆蓋 + 一致性 lint）
- [ ] **覆蓋 lint**：逐 CSV 每列 `zh_TW`/`zh_CN`/`en` 三欄皆非空（漏譯 = FAIL，列出 key）。
- [ ] **key 唯一**：跨所有 CSV 無重複 key。
- [ ] **佔位一致**：同一 key 三語的 `%d`/`%s` 佔位數量一致（語序可不同、個數需同）。
- [ ] **禁字**：三語全文掃描**無「林霏」**；其餘各 Phase 既有禁字規則於三語維持。
- [ ] 抽樣：`tr("MSG_DOOR_LOCKED")` 在三語各回該語言字串；`tr("ITEM_FINGERLESS_GLOVES_NAME")` 同。
- [ ] 對話邏輯不退化：9 棵樹 goto/effect/condition 仍以既有 id/flag 運作（換 locale 不影響分支判定）。
- [ ] Phase 1~19 / M1 headless 不退化（顯示改 key、邏輯不動）。

##### GUI 走查（每語言各跑一輪）
- [ ] 標題畫面 →「設定」→ 語言三選一可切；暫停選單 →「設定」→ 同（遊戲中 `UIMode==NONE` 才可開）。
- [ ] 切語言**即時生效**：當前頁 + 之後開的 UI / 對話 / 筆記 / 物品 / 提示全變新語言。
- [ ] 重啟遊戲沿用上次語言；首次啟動（刪 `settings.cfg`）依裝置語言自動選。
- [ ] 三語各無豆腐框；繁中繁體字形、簡中簡體字形（字型確實切換）。
- [ ] 英文較長字串不破版（按鈕 / 提示 / 對話框不溢出或截斷）。
- [ ] 含數值字串（credits、頁碼）三語佔位 + 語序正確。
- [ ] 全程純觸控可進設定頁、可選語言、可返回。
- [ ] 跑一輪主線片段（公寓→便利商店→聚落）確認對話 / 筆記 / 殘響 / 商店三語可玩、分支不變。

##### 真機（M2-E，iOS / Android）
- [ ] 裝置語言偵測：iOS/Android 系統語言為繁/簡/英時首啟自動選對應語言。
- [ ] 真機切語言即時生效、字型正確、無豆腐、記憶體無異常（只駐一個 CJK 字型）。
- [ ] 純觸控於真機可完成語言切換流程。
