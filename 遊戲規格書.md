# After-The-Model 遊戲規格書

> 本文件記錄全遊戲通用系統的規格與驗收條件。場景專屬規格放在 `subdocs/地點/`，由主規格書 link 過去。
>
> 每個系統段落含「驗收」清單：可逐條 check 的行為條件，用於手動驗收與已知問題追蹤。

## 目前狀態

- 引擎：Godot 4.6.3 / GDScript；純 2D 側捲 + light platforming。
- 專案現況（當前 phase、可玩場景、已完成系統）以 `PROJECT_BRIEF.md` 頂部「當前進度」與 `> Phase 進度` 為單一事實來源，本檔不重複維護。
- 本檔前半為全遊戲通用系統規格（活文件，隨系統演進更新）；後半 `## Phase 規劃` 為各 phase 驗收意圖。已完成 phase 的段落歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，原位置留同標題 stub。

## 輸入設計

正式遊戲行為走 Godot InputMap，不在程式中直接監聽實體按鍵。例外：UI focus 元件可在已啟用 UI mode 時補實體 WASD fallback，作為方向鍵導覽鏡像；不得觸發世界互動或資料變更。

| Action | 鍵盤 | 用途 |
|---|---|---|
| `move_left` | A / Left | 世界移動；UI 開啟時改為焦點移動 |
| `move_right` | D / Right | 世界移動；UI 開啟時改為焦點移動 |
| `interact_primary` | E | 世界互動；UI 主要動作 |
| `interact_secondary` | R | UI 查看 / 取消等次要動作 |
| `interact_tertiary` | T | UI 丟棄等第三動作 |
| `open_inventory` | I | 開 / 關背包，已完成 Phase 1-B |
| `open_notebook` | J | 開 / 關筆記，已完成 Phase 1-C |
| `ui_cancel` | Esc | 關閉目前 UI，背包 / 訊息 / 容器已接入 Phase 1-B |

### 驗收

- 所有會改變遊戲狀態的玩家輸入均經 `InputMap`，程式不得直接用 `KEY_E` / `KEY_R` / `KEY_T` 等實體鍵碼觸發互動或資料變更。
- 任一 action 重新綁定後，所有相依行為（移動、互動、UI 焦點）均跟隨更新。

## UI Mode

單一 UI 模式狀態，避免角色移動與 UI 操作互相衝突。

```text
Autoload name: UIMode
File: res://scripts/autoload/ui_mode.gd
Scope: 跨場景共用
```

模式列舉：

```text
NONE
INVENTORY
CONTAINER
NOTEBOOK
MESSAGE   # overlay modal：疊在 caller UI 上，記住並還原 caller mode（見下）
CONFIRM   # Phase 1-E 啟用，由 ConfirmDialog 使用；同為 overlay modal
```

規則：

- `NONE`：玩家可移動，E 與附近物件互動。
- 任何大型 UI 開啟時，玩家不能移動。
- UI 開啟時，WASD / 方向鍵改為移動 UI 焦點。
- `Esc` 關閉目前 UI，狀態回到 `NONE`。
- 同時只能有一個大型 UI（INVENTORY / CONTAINER / NOTEBOOK）開啟；要切到另一個必須先關閉當前 UI。（`MESSAGE` / `CONFIRM` 為 overlay modal，可疊在大型 UI 之上，不受此限——見下「overlay modal 的 caller 還原」。）
- 背包 / 筆記 / 容器 UI 開啟時，**世界仍可見**（半透明 overlay 蓋在世界之上，不全黑遮蓋），方便邊看世界邊操作 UI。

最小 API：

```gdscript
signal mode_changed(new_mode: int)

enum Mode { NONE, INVENTORY, CONTAINER, NOTEBOOK, MESSAGE, CONFIRM }

func get_mode() -> int
func set_mode(mode: int) -> void
func is_world_input_blocked() -> bool   # 當前 mode != NONE 時為 true
```

### 大型 UI 之間互切

`I` / `J` 等大型 UI 鍵在當前 mode != NONE 時的行為：

- 當前 mode 與按鍵對應的 UI **相同** → 關閉，回到 `NONE`。
- 當前 mode 與按鍵對應的 UI **不同** → **先關閉當前 UI、再開新的**（一鍵切換，不要求玩家先 Esc）。

例：
- INVENTORY 中按 `J` → 背包關、筆記開、mode = NOTEBOOK。
- NOTEBOOK 中按 `I` → 筆記關、背包開、mode = INVENTORY。
- CONTAINER 中按 `I` → 容器關、背包開、mode = INVENTORY（雙欄容器同規則）。
- `MESSAGE` / `CONFIRM` overlay 期間，大型 UI 鍵（`I` / `J` 等）不反應（overlay 為暫時 modal，見下「overlay modal 的 caller 還原」）。

實作上 `UIMode` 保持純狀態管理，只更新 mode 並 emit `mode_changed(new_mode)`。場景 controller（目前為 `apartment_room.gd`）負責監聽 signal，關閉舊 UI、開啟新 UI、同步 overlay / prompt / focus。

### overlay modal 的 caller 還原（MESSAGE / CONFIRM）

`MESSAGE`（MessageBox）與 `CONFIRM`（ConfirmDialog）**不是**大型 UI，而是**疊在當前畫面上的 overlay modal**；行為與大型 UI 互切規則不同：

- 進入時 UIMode 記住 caller mode（如 `NONE` / `INVENTORY` / `CONTAINER`），**不關閉 caller UI**（世界 / 背包 / 雙欄維持顯示但停止接鍵）。
- overlay 顯示期間捕捉鍵盤、暫停世界；caller UI 凍結但可見。
- 離開時還原 caller mode，焦點回 caller UI 原位置（如雙欄回原格）。
- overlay 期間 `I` / `J` 等大型 UI 鍵不反應；`MESSAGE` 只接推進 / `Esc`，`CONFIRM` 只接 `E` / `Esc`。
- 這讓「雙欄內放入方塊 → 直接疊 MessageBox 語音 → 關閉後回雙欄」成立（2-E）；**取代** Phase 1-D「雙欄不可有 MessageBox」舊限制——改為「雙欄上可疊 overlay MessageBox，底層雙欄凍結」。FloatingToast 仍用於不需 modal 的即時提示（如「放不下了。」）。

```gdscript
# UIMode 內部（MESSAGE 與 CONFIRM 共用 caller-restore）
var _caller_mode := Mode.NONE

func enter_overlay(overlay_mode: int) -> void:  # overlay_mode ∈ {MESSAGE, CONFIRM}
    _caller_mode = current_mode
    current_mode = overlay_mode
    mode_changed.emit(overlay_mode)

func exit_overlay() -> void:
    current_mode = _caller_mode
    _caller_mode = Mode.NONE
    mode_changed.emit(current_mode)
```

> MVP 只支援**單層 overlay**（不疊 MESSAGE on CONFIRM）；多層 overlay 待實際需要再做。

### 驗收

- 任何 UI 開啟時，主角不能左右移動、不會觸發世界互動。
- 大型 UI 關閉後恢復 `NONE`，主角可移動且 Prompt 重新出現；overlay modal（MESSAGE / CONFIRM）關閉後則**還原 caller mode**（caller=NONE 才回世界、可移動）。
- 同時只能有一個大型 UI 開啟。
- 背包 / 筆記 / 容器 UI 開啟時，背景仍透出世界畫面（透明度由半透明 overlay 控制）。
- `mode_changed` signal 在每次切換時 emit。
- 大型 UI 互切時舊 UI 確實隱藏，無雙重 panel 疊加。
- 進 / 離 `MESSAGE` / `CONFIRM` overlay 時 caller UI 不關閉、焦點正確還原（caller=NONE / INVENTORY / CONTAINER 各情況皆驗）。

## 共用 UI 元件

跨 UI 重複使用的元件，集中規範以避免漂移。實作上應抽成獨立 scene / script。

### BagGrid

玩家背包格子的視覺與資料 component。

- 5x3 = 15 slot，sizing / skin / focus 視覺依「背包系統」段。
- 資料源：`GameState.get_inventory()`，listens `inventory_changed` signal。
- 同一個 `BagGrid` 同時用於：
  - 1-B 背包獨立 UI（INVENTORY mode）
  - 1-D 雙欄左欄（CONTAINER mode）
- 接受外部設定當前 focused index 與「是否捕捉鍵盤輸入」flag（決定誰主導鍵盤）。
- 視覺絕對一致；改背包視覺只能改這一處。

### FloatingToast

短暫提示，不切 UI Mode。

```text
位置        = 視觸發 panel 上方 8px，水平置中於該 panel
字          = 18px 米白 (0.94, 0.92, 0.84, 1.0)
背景        = 半透明黑 (0, 0, 0, 0.7) + 8px padding
顯示時間    = 1.5 秒
淡出時間    = 0.5 秒
重複觸發    = 取代既有 toast、計時器重置；不疊加
```

API：

```gdscript
FloatingToast.show(text: String, anchor_node: Control = null)
```

用途清單：
- 1-D「放不下了。」
- 1-E「無法丟棄這個物品」
- 1-E「這類裝備已經滿了，先卸下身上的再裝備新的。」
- Phase 2+ 解謎條件未滿足等臨時提示

### PanelFooterHint

panel 底部一行操作鍵提示。

```text
字          = 14px 暗灰 (0.55, 0.55, 0.55, 1.0)
位置        = panel 底部 padding 內，左對齊
格式        = "<KEY>: <action>    <KEY>: <action>    ..."
```

API：

```gdscript
PanelFooterHint.set_hints(panel: Control, hints: Array)  # [["E", "移動"], ["Esc", "關閉"]]
```

各 UI 對應 hint（依 Phase 進度）：

| UI | Phase 1-B | Phase 1-C | Phase 1-D | Phase 1-E |
|---|---|---|---|---|
| 背包（單獨） | `Esc / I: 關閉` | — | — | `E: 依物品（裝備/卸下·使用·無）    R: 查看    T: 丟棄    Esc / I: 關閉` |
| 筆記 | — | `Esc / J: 關閉` | — | — |
| 雙欄（兩欄各一，同字串） | — | — | `E: 移動    Esc: 關閉` | `E: 移動    R: 查看    T: 丟棄    Esc: 關閉` |
| ItemDetailModal | — | — | — | `R / Esc: 關閉` |
| ConfirmDialog | — | — | — | `E: 確定    Esc: 取消` |

> 背包 footer 的 **E 段依選中物品變動**（2-D 連帶修正 Phase 1-E，**取代**「consumable 按 E 沉默」舊規）：
> - 可裝備物 → `E: 裝備`（未裝備）/ `E: 卸下`（已裝備）。
> - `category == consumable` 或可反覆使用物 → `E: 使用`。
> - 無對應動作物（如 `old_work_badge`）→ **不顯示 E 段**。
> - `R: 查看` / `T: 丟棄` / `Esc·I: 關閉` 不受影響。`E: 使用` 僅在單獨背包（`I`）出現；雙欄左欄 E 仍為「移動」。
>
> `E: 使用` dispatch：帶 `consume_grants_note` 的物 → MessageBox + `add_knowledge` + 移除該 instance（消耗）；無定義效果的 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」、不消耗；可反覆使用物執行效果、不消耗（預見類型，暫無實例）。Phase 3+ 加血量系統後再補飢餓類效果。
>
> **筆記 footer（2-F，待實作）**：上表 Phase 1-C 欄的 `Esc / J: 關閉` 為早期占位；2-F 起筆記 footer 主打 WASD 導航——`A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`。方向鍵仍可用但不列於 footer。

### ItemDetailModal

R 鍵查看物品 description 用的 modal。**不切 UIMode**（INVENTORY / CONTAINER 不變），開啟時 capture 鍵盤，關閉後焦點回原 grid 格。

**與 MessageBox 疊層**：ItemDetailModal 開啟中觸發 MESSAGE overlay（如 R 查看 `consume_grants_note` 物 → 跳訊息）時，MessageBox 疊在 detail modal 上方；此時 detail modal 凍結、不吃鍵盤輸入（capture 暫交給 MessageBox）。MessageBox 關閉後 detail modal 刷新顯示（消耗物會連 modal 一起關並回 grid；變身物如 `decoder_cube` 則刷新當前 instance），並還原鍵盤 capture。

```text
panel 寬     = 320
panel 高     = 400
位置         = 蓋在背包 panel 或雙欄左欄上方中央
skin         = inventory
```

內部排版（垂直）：

```text
[ icon 128x128，置中 padding 16 ]
[ name 22px 米白，置中 ]
[ description 16px，換行可讀，左右 padding 16 ]
[ flex spacer ]
[ category tag 14px 暗灰，置中於 panel 底 padding 內 ]
```

`category tag` 規則：

| `category` | tag 文字 |
|---|---|
| `key_item` | `劇情物品（不可丟棄）` |
| `equipment` | `裝備（{equipment_slot 中文}）`，例 `裝備（手持）` |
| `consumable` | `消耗品` |
| `misc` | `物品` |

`equipment_slot` 中文映射：`clothing → 衣服`、`hand → 手持`、`accessory → 其他`。

API：

```gdscript
ItemDetailModal.show(instance_id: String, anchor_node: Control = null) -> void
ItemDetailModal.close() -> void
```

操作：

- `R` 或 `Esc` → 關閉並回到 grid focus。
- 其他鍵：modal 開啟期間全部 block（不讓背包 / 雙欄處理）。

### ConfirmDialog

通用確認 dialog。1-E 用於丟棄確認。切 `UIMode.CONFIRM`，開啟期間 capture 所有鍵盤。

```text
panel 寬     = 360
panel 高     = 160
位置         = viewport 置中
skin         = inventory
```

內部排版：

```text
[ message 18px 米白，置中 padding 16 ]
[ flex spacer ]
[ footer hint: "E: 確定    Esc: 取消" ]
```

API：

```gdscript
ConfirmDialog.show(message: String, on_confirm: Callable, on_cancel: Callable = Callable()) -> void
```

操作：

- `E` → 呼叫 `on_confirm` → 關閉 dialog → UIMode 回先前 mode（INVENTORY / CONTAINER）。
- `Esc` → 呼叫 `on_cancel`（如有）→ 關閉 dialog → UIMode 回先前 mode。
- 其他鍵：block。

> ConfirmDialog 切 `CONFIRM` mode；關閉時必須回到 dialog 開啟**之前**的 mode（INVENTORY 或 CONTAINER），而非預設 NONE。UIMode 內部需記住 caller mode。

## 互動系統

每個可互動物件使用 `Area2D`，掛 `interactable_area.gd`。

基本欄位：

```gdscript
interaction_id: String
prompt_text: String
message_id: String
```

目前做法：

- `Area2D` 只負責偵測玩家進出。
- 場景 controller 負責：
  - 選目前互動物。
  - 顯示 prompt。
  - 按 E 後執行對應行為。
- 多個互動區重疊時，選離玩家最近的 `CollisionShape2D`。

### 驗收

- 玩家站在互動區內時，Prompt 顯示對應 `prompt_text`。
- 玩家離開互動區後 Prompt 立刻消失。
- 同時站在多個互動區內時，只顯示最近一個的 Prompt，按 E 也只觸發該物件。

## Prompt

互動提示使用共用 `PromptPanel`。

目前已完成：

- 有 UI 底圖。
- 文字由互動物或目前 UI 狀態決定。
- 位置跟隨主角，水平大致置中在主角上方。

### 驗收

- 主角移動時 Prompt 跟著移動，不會落後或抖動。
- Prompt 不擋住主角頭頂的關鍵互動範圍。
- 任何 UI 開啟時 Prompt 隱藏，UI 關閉後恢復。

## MessageBox

共用訊息框用於短文本敘事與無法執行某動作時的回饋。

目前已完成：

- 訊息框依完整文字自動調整大小。
- 文字逐字出現，速度為每秒 3 字。
- 含標點符號一起逐步顯示。

後續規則：

- `MESSAGE` mode 中，`E` 或 `Esc` 關閉訊息。

### 驗收

- 訊息框尺寸依完整文字長度自動調整，不出現截字或大量留白。
- 文字以每秒 3 字逐字顯示，含標點。
- `MESSAGE` mode 中主角不能移動，按 E 或 Esc 收掉訊息後**還原 caller mode**（overlay 機制：caller=NONE → 回世界、caller=CONTAINER → 回雙欄原格、caller=INVENTORY → 回背包原格）。詳見「overlay modal 的 caller 還原」。

## 容器系統

容器使用資料驅動。

目前 prototype 使用單一 `ContainerPanel`：

```gdscript
CONTAINERS = {
  "cabinet_storage": {
    "title": "櫥櫃",
    "cols": 5,
    "rows": 3,
    "skin": "cabinet",
    "panel_position": Vector2(...)
  },
  "fridge_storage": {
    "title": "冰箱",
    "cols": 5,
    "rows": 2,
    "skin": "fridge",
    "panel_position": Vector2(...)
  }
}
```

已完成：

- 不同容器可設定 title、cols、rows、skin、panel_position。
- 容器 panel 會依格數自動調整大小。
- `cabinet` skin：暗木 / 暗褐。
- `fridge` skin：米白 / 淡藍 / 冷灰。

規格目標：

- 背包：5x3。
- 櫥櫃：5x6，作為玩家倉庫。
- 冰箱：5x2。
- 目前程式中的櫥櫃仍是 5x3，待調整到 5x6。

### 資料層接 GameState（Phase 1-D）

雙欄打開時容器 panel **必須**讀 `GameState.get_container(container_id)`，不再用空 Button grid。

- 場景在 `_ready()` 呼叫 `GameState.configure_container("cabinet_storage", 30)` / `configure_container("fridge_storage", 10)` 初始化。
- 預載資料寫法見 `subdocs/地點/主角公寓.md > 預載資料`。
- panel 顯示時：foreach slot → 若有對應 inventory entry 顯示 icon + 數量；空格顯示 slot skin 底。
- listen `GameState.container_changed(container_id)` signal 即時刷新。
- 移動物品請呼叫 `GameState.move_one_item_to(...)`，禁止 panel 自行操作 GameState 內部資料。

### 驗收

- 不同容器顯示的 title、格數、skin 完全由 `CONTAINERS` dict 驅動，程式不得 hardcode。
- 容器 panel 寬高隨 cols/rows 自動調整。
- 櫥櫃顯示 5x6，冰箱顯示 5x2，背包顯示 5x3。
- 容器 UI 開啟 / 關閉時遵守 UI Mode 規則（主角不能移動、Prompt 隱藏）。
- 容器 panel 內容由 `GameState.get_container()` 提供；空格顯示 skin 底色，有物品顯示 icon + 數量。
- 容器內容變動時透過 `container_changed` signal 即時刷新，無需重開 panel。

## 背包系統

已完成 Phase 1-B 基礎 UI 與人工驗收。物品操作（E 使用 / 裝備、R 查看、T 丟棄）已於 Phase 1-E 完成。

背包是玩家自身容器：

- `I` 開 / 關。
- 單獨打開時只顯示背包，位置置中於 viewport。
- 格數：5x3。
- 顯示 `Credits`，不佔格。
- 全鍵盤 / 手把友善，不依賴滑鼠。
- 開啟時走半透明 overlay，世界仍可見（依 UI Mode 規則）。

背包操作：

```text
方向鍵 / WASD = 選格
E = 使用 / 裝備 / 卸下（Phase 1-E）
R = 查看（Phase 1-E）
T = 丟棄（Phase 1-E）
Esc / I = 關閉
```

Phase 1-B 期間，背包內按 E / R / T 必須被 UI 吃掉但不產生任何效果、log 或提示。

### Panel 尺寸與排版

沿用容器系統常數：

```text
SLOT_SIZE       = 64 x 64
SLOT_GAP        = 4
PANEL_PADDING   = (16, 16)
TITLE_HEIGHT    = 24
```

5x3 計算：

```text
內容寬 = 5 * 64 + 4 * 4 = 336
內容高 = 3 * 64 + 2 * 4 = 200
panel 寬 = 336 + 32 = 368
panel 高 = 200 + 32 + 24 = 256
```

於 1280×800 viewport 置中：左上座標 ≈ `(456, 272)`。

### 標題列（含 Credits）

```text
[ 背包                                    Credits: 0 ]
```

- 左對齊「背包」字樣（18px，inventory skin 標題字色）。
- 右對齊「Credits: <值>」（18px，同色），值來自 `GameState.get_credits()`，聽 `credits_changed` signal 即時更新。
- Credits 內聯於標題列，**不另佔行**。

### Inventory Skin（區別於 cabinet / fridge）

```text
skin id        = "inventory"
panel 底       = (0.12, 0.14, 0.18, 0.88)   # 深藍灰
panel 邊框     = (0.78, 0.42, 0.20, 1.0)    # 主角橙紅
slot 底        = (0.08, 0.10, 0.12, 0.80)
slot 邊框      = (0.30, 0.32, 0.36, 1.0)
標題字色       = (0.94, 0.92, 0.84, 1.0)   # 米白
```

實作建議在 `_apply_container_panel_style` 加 `inventory` 分支（或重構為 dict 對應）。

### 格內顯示

- **Icon**（必有，path 由 `ITEMS_DB[item_id].icon_path` 提供）。
- **數量**：`quantity > 1` 時顯示於右下角，14px，白底深字（或加字陰影確保可讀）。
- **已裝備標記**：右上角 12×12 像素 `E` 字符號，白底深字。1-B 階段預載 item 未裝備，此標記預留位置；1-E 實裝裝備系統後啟用。
- **缺 icon**：顯示紅底白色 `?` placeholder（48×48 居中），方便 dev 期發現未補 icon。

### Focus 視覺（鍵盤導航）

選中格用 2px 亮橙紅外框（同 panel 邊框色 `(0.78, 0.42, 0.20, 1.0)`），蓋在 slot 邊框上，視覺優先順序高於既有 hover。

```text
未選中 / 滑鼠 hover    = 走既有 slot 邊框
鍵盤 focus              = 2px 橙紅外框（dedicated focus_style）
```

Focus 邊界**不繞回**：上 / 下 / 左 / 右抵邊後不移動。

### 半透明 Overlay

開啟背包 / 筆記 / 容器 UI 時，世界上覆蓋一層半透明黑色：

```text
overlay color = (0, 0, 0, 0.3)
```

overlay 在 panel 之下、世界之上。背包 / 筆記 / 容器三者共用同一份 overlay 規格。

### 驗收

- [x] `I` 開啟時 panel 置中於 viewport，尺寸 368×256，左上 ≈ (456, 272)。
- [x] 標題列左「背包」、右「Credits: <值>」；Credits 數字顯示來自 `GameState.get_credits()`。
- [x] 每格顯示對應 icon；可堆疊品於右下角顯示數量；已裝備品於右上角顯示 `E` 標記（1-B 預留位置）。
- [x] 缺 icon 時顯示紅底 `?` placeholder。
- [x] 全鍵盤可完成目前 Phase 1-B 操作，不依賴滑鼠；focus 可見、邊界不繞回。
- [x] 開啟期間主角不可移動，Prompt 隱藏；`Esc` 或 `I` 關閉。
- [x] 開啟期間世界仍透出（黑色 alpha 0.3 overlay）。
- [x] E / R / T 在背包內無反應（無錯誤、無訊息），行為留 Phase 1-E。

## 容器雙欄操作

已完成 Phase 1-D 基礎 UI 與人工驗收。

打開櫥櫃 / 冰箱等外部容器時，UI 改為雙欄：

```text
左：玩家背包
右：外部容器
```

操作：

```text
方向鍵 / WASD = 選格 / 跨欄（抵欄邊自動跳對欄）
E             = 移動 1 個到對方
R             = 查看（Phase 1-E 啟用）
T             = 丟棄（Phase 1-E 啟用）
Esc           = 關閉
```

> ⚠️ 雙欄打開後 **E 鍵不再用於關閉容器**（移除原本單欄 prototype 的「再按 E 關閉」行為）；E 一律是「移動」。`Esc` 為唯一關閉鍵。

### Panel 架構

- 左欄背包必須與 1-B 背包 UI **同一個 `BagGrid` component**（同視覺、同資料源）。
- 右欄為 `ExternalGrid` 顯示當前外部容器（cabinet_storage 或 fridge_storage）。
- 兩欄各自獨立 panel，並排呈現；左右欄 panel 套 inventory skin。

### 雙欄整體 Layout

```text
左欄 panel        = 368 × 256        # 同 1-B 背包
右欄 panel(cabinet)= 368 × ~388      # 5x6 + title
右欄 panel(fridge) = 368 × ~152      # 5x2 + title
panel 間距        = 16
```

**對齊規則**：兩 panel **垂直置中對齊**（panel 中心同高），不對齊頂部。

於 1280×800 viewport 整組置中：

```text
總寬 = 368 + 16 + 368 = 752
整組左上 x ≈ (1280 - 752) / 2 = 264
左欄左上 = (264, viewport 垂直置中對應 y)
右欄左上 = (264 + 368 + 16, viewport 垂直置中對應 y)
```

### 跨欄 input 規則

無 Tab 鍵（手把不便）。靠方向鍵 / WASD 抵欄邊自動跳對欄：

- 左欄最右行按 → → 跳右欄
- 右欄最左行按 ← → 跳左欄
- 跨欄時**保持當前 row index**；對方 row 不足時落對方最後一 row
  - 例：背包 focus 在 row 2（最下），→ 跳 cabinet row 2 或 fridge row 1（fridge 只有 row 0-1）
- 上 / 下抵欄邊不繞回、不跨欄

### 自動整理時機

每次 `add_item` / `remove_item` / `move_one_item_to` 後 GameState **立刻**自動整理：同 id 合併、未滿 stack 補位、空格集中於尾、stack 排序。

> 這修正本規格之前版本「關閉時整理」的舊敘述。整理永遠在 data layer 即時發生，UI 只讀。

### Focus 行為（移動後）

物品移走後，focus **保持原 row/col index**，不跟著物件、不跳到對欄。

理由：玩家連續從容器搬到背包時，focus 不動可以連按 E 快速搬完。

該 index 整理後可能變成空格，玩家以方向鍵繼續導航即可。

### 預設 focus / 重置策略

- 雙欄打開時預設 focus 在**右欄第一格 (row 0, col 0)**。
- 每次重新打開都重置；不記憶上次 focus 位置。

### 「放不下了。」訊息 — FloatingToast

一般失敗 / 操作提示（如放不下）在雙欄不使用 MessageBox，改用 **FloatingToast** 非模態元件（共用，見「共用 UI 元件」段）。敘事性訊息則可用 `MESSAGE` overlay 疊在雙欄上（caller=CONTAINER，關閉後還原雙欄；見「overlay modal 的 caller 還原」）—— overlay 不再與 CONTAINER mode 衝突。

文字：`放不下了。`

### Panel Footer Hint

兩欄 panel 底部各自顯示 footer hint，列出當下可用操作鍵。

```text
Phase 1-D 階段：  "E: 移動    Esc: 關閉"
Phase 1-E 完成：  "E: 移動    R: 查看    T: 丟棄    Esc: 關閉"
```

字 14px，暗灰 `(0.55, 0.55, 0.55, 1.0)`，左對齊距 panel 底邊 padding 內。

詳見「共用 UI 元件 > PanelFooterHint」段。

### 世界 Prompt

雙欄 mode 期間世界 Prompt 完全隱藏（依 UI Mode 規則）。打開容器後 prompt 不顯示「E: 關閉櫥櫃」這類提示，由 Esc + footer 取代。

### 規則彙整

- 可堆疊物品按 E 一次只移動 1 個。
- 目標側若已有同 id 未滿 stack，先補該 stack；補不下時開新 stack；位置由 GameState 整理規則決定。
- 兩側皆無空格、且無未滿 stack 可補時，顯示 FloatingToast「放不下了。」，原物品不消失。
- 開啟時走半透明 overlay alpha 0.3（依 UI Mode 規則）。
- 預設 focus 落在右側容器，移動後 focus 保持 index。
- 雙欄期間 E 永遠是「移動」，關閉只能 Esc。
- T 在雙欄中對任一欄 focused 物品皆可丟棄（背包或容器都生效）；行為走 Phase 1-E ConfirmDialog → discard 流程。
- 劇情重要物品（`discardable=false`）按 T 顯示 FloatingToast「無法丟棄 {item_name}」，不消失。

### 驗收

- 對外部容器互動後 UI 改為雙欄：左背包（同 1-B 視覺）、右容器。
- 兩 panel 垂直置中對齊，間距 16，整組於 viewport 水平置中。
- 雙欄開啟時預設 focus 在右欄第一格 (0, 0)。
- 方向鍵抵欄邊自動跨欄，保持 row index，row 不足時落對方最後一 row。
- E 一次只移動 1 個可堆疊物，目標側有同 id 先補 stack 再開新 stack。
- 目標兩側都放不下時顯示 FloatingToast「放不下了。」，1.5 秒 + 0.5 秒淡出，原物品不消失。
- 移動後 focus 保持原 row/col index。
- 關閉前 / 操作期間背包與容器內物品都已自動整理（合併、排序、空格集中於尾）— 任一時刻檢查皆然。
- 雙欄期間世界 Prompt 隱藏；兩欄 panel 底部 footer 顯示操作鍵提示。
- 雙欄期間 E 不會關閉 panel；`Esc` 為唯一關閉鍵。
- 關閉後再次打開：focus 重置回右欄 (0, 0)，不記憶上次位置。
- 開啟期間世界仍透出（黑色 alpha 0.3 overlay）。
- 櫥櫃顯示 5x6 = 30 slot；冰箱顯示 5x2 = 10 slot。

## GameState

已完成（Phase 1-A）。目前已實作 MVP in-memory 狀態、背包、裝備、知識 / 筆記、外部容器基本 API 與手動驗證腳本。

Phase 1-D 仍需補 `seed_container()`，用於場景預載外部容器內容；正式物品資料系統也仍待後續拆出。

GameState 是 MVP 階段的全域玩家狀態 Autoload。第一版只做記憶體狀態，不做存檔 / 讀檔。

```text
Autoload name: GameState
File: res://scripts/autoload/game_state.gd
Scope: MVP in-memory only
```

### 職責

GameState 管理：

- Credits。
- 玩家背包。
- 裝備。
- 已取得知識。
- 筆記條目。
- 外部容器的 MVP runtime state，例如櫥櫃與冰箱。

GameState 不負責：

- UI 繪製。
- 場景切換。
- 逐字訊息動畫。
- 物件互動 Area 偵測。
- UI 文字、對話字串格式化、提示訊息、log 輸出。

### 初始狀態

新遊戲啟動時：

```gdscript
credits = 0
inventory_slots = 15 # 5x3
inventory = []
equipment = {
  "clothing": [],
  "hand": [],
  "accessory": []
}
knowledge = {}
notes = []
external_containers = {}
```

MVP 中 `notes` 可預載少量起始線索，也可由場景互動逐步加入；若有預載，必須在場景規格中列出。

### 場景預載

新遊戲進入第一個場景時，由該場景的 `_ready()` 呼叫 GameState API 預載測試 / 劇本所需資料：

- 背包預載：`GameState.add_item(item_id, count)`
- 外部容器初始化 + 預載：`GameState.configure_container(id, slots)` 後再以內部 helper 直接寫入該容器內容（或由場景 API 提供，避免重複建立 instance_id 邏輯）。
- 筆記預載：`GameState.add_knowledge(note)`。

預載清單由場景規格負責（例如 `subdocs/地點/主角公寓.md > 預載資料`），GameState 規格只規範 API 與不變性。

### MVP Item Metadata Stub

第一版 `ITEMS_DB` 只作為 MVP temporary stub，用來支撐 stack / equipment / discardable 行為驗證，不是正式物品資料架構。後續正式物品資料應拆成獨立資料系統。

MVP stub 至少包含：

```gdscript
{
  "old_work_badge": {
    "category": "key_item",
    "stackable": false,
    "max_stack": 1,
    "discardable": false,
    "usable": false,
    "equipment_slot": ""
  },
  "fingerless_gloves": {
    "category": "equipment",
    "stackable": false,
    "max_stack": 1,
    "discardable": true,
    "usable": true,
    "equipment_slot": "hand"
  },
  "canned_food": {
    "category": "consumable",
    "stackable": true,
    "max_stack": 5,
    "discardable": true,
    "usable": true,
    "equipment_slot": ""
  },
  "faded_jacket": {
    "category": "equipment",
    "stackable": false,
    "max_stack": 1,
    "discardable": true,
    "usable": true,
    "equipment_slot": "clothing"
  }
}
```

### Knowledge / Notes API

```gdscript
func has_knowledge(id: String) -> bool
func has_note(id: String) -> bool
func add_knowledge(note: Dictionary) -> void
func get_notes(category: String) -> Array[Dictionary]
func get_all_notes() -> Array[Dictionary]
```

`has_knowledge(id)` 與 `has_note(id)` 分工：

- `has_knowledge(id)` 只判斷可作為劇情 gate 的穩定知識；目前由 `category == "身份"` 的 note 寫入，例如大門的 `identity_door_unlock_method`。
- `has_note(id)` 判斷任一分類筆記是否已存在，供 UI / examine / 一次性提示去重使用。
- 不得用 `has_knowledge()` 判斷 `工作` / `線索` 類 note 是否已記過；這類去重一律用 `has_note()` 或 `add_knowledge()` 的 idempotent 行為。

`add_knowledge(note)` 規則：

- `note.id` 必填。
- `note.category` 必填，合法值為 `身份` / `工作` / `線索`。
- `note.title` 必填。
- `note.body` 必填。
- `note.status` 若未提供，預設為 `active`。
- 同一 `id` 重複新增時不新增第二筆，可更新既有 note 內容。
- 若 `note.category == "身份"`，同時設定 `knowledge[note.id] = true`。
- 若其他系統需要將線索轉成穩定知識，應新增一筆 `身份` note，而不是直接改線索文字。
- `get_notes()` 與 `get_all_notes()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。

筆記 note schema：

```gdscript
{
  "id": "identity_door_unlock_method",
  "category": "身份",
  "title": "門鎖的開法",
  "body": "你想起來了。門鎖需要先按住門把下方的舊感應片, 再用右手手套靠近讀取器。",
  "status": "active"
}
```

### Credits API

```gdscript
func get_credits() -> int
func add_credits(amount: int) -> void
func set_credits(value: int) -> void
```

規則：

- Credits 型別為 `int`。
- Credits 不佔背包格。
- `set_credits()` 不允許低於 0；若輸入負值，clamp 到 0。

### Inventory API

```gdscript
func get_inventory() -> Array[Dictionary]
func add_item(item_id: String, count: int = 1) -> bool
func remove_item(item_id: String, count: int = 1) -> bool
```

MVP 背包容量固定 15 格。背包資料以 slot array 表示，空格為 `{}`。

Slot schema：

```gdscript
{
  "instance_id": "item_0001",
  "item_id": "old_work_badge",
  "quantity": 1
}
```

規則：

- 可堆疊物品以 `item_id` 合併。
- 移動堆疊物品時，按一次 E 只移動 1 個。
- 先補目標側未滿 stack，再開新 stack。
- 沒空格回傳 `false`，UI 顯示「放不下了。」。
- 每次新增、移除、移動後自動整理，空格集中在後面。
- `add_item(item_id, count <= 0)` 與 `remove_item(item_id, count <= 0)` 立即回傳 `false`，不改狀態、不 emit signal。
- `get_inventory()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。

### Equipment API

```gdscript
func equip(instance_id: String) -> bool
func unequip(equipment_type: String, slot_index: int) -> bool
func unequip_by_instance(instance_id: String) -> bool     # Phase 1-E helper
func is_equipped(instance_id: String) -> bool             # Phase 1-E helper
func get_equipment() -> Dictionary
```

裝備槽：

```text
clothing: 1
hand: 2
accessory: 2
```

規則：

- 裝備槽滿時 `equip()` 回傳 `false`，UI 顯示：
  ```text
  這類裝備已經滿了，先卸下身上的再裝備新的。
  ```
- 裝備不會離開背包，只是被 equipment 參照。
- 已裝備物品在背包排序中放最前面；卸下後自動整理重排。
- `get_equipment()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。
- `unequip_by_instance(instance_id)`：當 UI 焦點在背包格時直接用 instance_id 卸下對應裝備，不需先查 slot_index。內部仍走 `unequip` 同樣的整理邏輯。
- `is_equipped(instance_id) -> bool`：給 `BagGrid` 用來判斷是否要畫右上角 `E` 標記。

成功時 emit `equipment_changed`，並 emit `inventory_changed`（因為背包排序變動）。

### External Container API

MVP 中外部容器由 GameState 管理 in-memory runtime state。容器 id 規則：

```text
player_inventory = 保留 id，代表玩家背包
cabinet_storage = 櫥櫃容器，目前 5x6 = 30 slots
fridge_storage = 冰箱容器，目前 5x2 = 10 slots
```

外部容器 id 使用 lowercase underscore，並盡量與 interactable / scene id 一致。空 slot 為 `{}`。

```gdscript
func configure_container(container_id: String, slot_count: int, accepted_item: Array[String] = [], deposit_locked: bool = false) -> void  # accepted_item / deposit_locked 為 Phase 2-C 加入
func get_container(container_id: String) -> Array[Dictionary]
func get_container_config(container_id: String) -> Dictionary
func move_one_item_to(target_container_id: String, instance_id: String) -> bool

# Phase 1-D 加入；用於場景預載外部容器內容
func seed_container(container_id: String, item_id: String, count: int) -> bool
```

`configure_container()` 規則：

- 若 `container_id` 尚未初始化，建立指定數量的空 slot。
- 若容器已存在，不覆蓋既有內容。
- `accepted_item` / `deposit_locked` 旗標於首次 configure 時設定（沿用「已存在不覆蓋」）。既有呼叫（櫥櫃 30 / 冰箱 10）省略兩參數＝空白名單 + 未鎖，行為與現狀完全一致。

`get_container()` 規則：

- 回傳指定容器的 deep copy。
- 未設定的容器回傳空陣列 `[]`。

`get_container_config()` 規則（Phase 2-C 新增）：

- 回傳指定容器的只讀設定 deep copy，例如：
  ```gdscript
  {
    "slot_count": 1,
    "accepted_item": ["decoder_cube"],
    "deposit_locked": true
  }
  ```
- 未設定的容器回傳預設設定：`slot_count = 0`、`accepted_item = []`、`deposit_locked = false`。
- UI 若需要顯示不同失敗訊息，必須讀此 API 或未來的 preflight API，不得直接讀 / 修改 GameState 內部容器結構。

`move_one_item_to()` 規則：

- 動態搜尋 `instance_id` 來源，來源可能是玩家背包或任一外部容器。
- MVP 只支援 `player_inventory -> external container` 與 `external container -> player_inventory`。
- 不支援 `container -> container`；若來源是外部容器且目標不是 `player_inventory`，立即回傳 `false`。
- 堆疊物品一次只移動 1 個。
- 先檢查目標是否可容納：有同 `item_id` 且未滿 stack，或有空 slot。
- 若目標無法容納，立即回傳 `false`，來源與目標都不可變動。
- 成功時先扣來源 1 個，再加入目標；來源數量歸零時清空該 slot。
- 新開 slot 時產生新的 `instance_id`。
- 成功後來源與目標都自動排序。
- 成功後 emit `item_moved(move)`，供需要定位移動結果的系統使用；不要從排序後的背包 / 容器列表猜測新 instance。
- 只回傳 `true` / `false`，不回傳 UI message、不 log UI message。UI 層收到 `false` 後自行決定顯示文字，例如「放不下了。」。

`item_moved(move)` payload：

```gdscript
{
  "source_container_id": "cabinet_storage",
  "target_container_id": "player_inventory",
  "source_instance_id": "inst_12",
  "target_instance_id": "inst_34",  # merge 時為目標既有 stack instance；新 slot 時為新 instance
  "item_id": "worn_rubiks_cube",
  "quantity": 1
}
```

需要後續操作「剛移動的那個物品」（例如 Phase 2-B 拿上手解碼）的 controller 必須使用 `item_moved` payload 的 `target_instance_id` 定位；不得依賴原本傳入的 `source_instance_id`，因為新 slot 會產生新 instance。

`seed_container()` 規則（Phase 1-D 新增）：

- 用途：場景預載，把指定數量的某 item 放入外部容器。
- 必須先 `configure_container()` 否則回傳 `false`。
- 行為等同對該容器多次呼叫 `add_item` 的容器版本：補同 id 未滿 stack → 開新 slot；自動產生 `instance_id`；操作後自動整理。
- 容量不足時：能放多少放多少，未能放完仍回傳 `false`；放完則回傳 `true`。
- 成功時 emit `container_changed(container_id)`。
- 僅限場景 `_ready()` 或 dev tool 呼叫；遊戲流程內物品搬運必須走 `move_one_item_to`。

容器選項擴充（Phase 2-C，規格定稿，**待實作**）：

供「插槽 / 鎖定容器」這類解謎容器使用；櫥櫃 / 冰箱 / 背包省略參數即不受影響。插槽沿用一般雙欄容器 UI（不特化單格，格數可變）。

- `accepted_item`（`Array[String]` 白名單）：空＝接受所有；非空＝僅清單內 item_id 可放入。`move_one_item_to` 目標白名單非空、且來源 item 不在清單 → 回傳 `false`。
- `deposit_locked`（`bool`）：放入該容器的物品不可再移出。`move_one_item_to` 來源若是 `deposit_locked` 容器 → 立即回傳 `false`（不可作為搬運來源）；**放入不受影響**。
- 兩者皆為可組合 config 旗標，不對單一容器寫死 if。
- **false 原因辨識**：`move_one_item_to` 仍只回傳 bool（沿用既有規則）。UI 在移動前自行檢查目標白名單 / 來源 `deposit_locked`，挑對應訊息（`放不下了。` / `放不進去` / 取出被擋字樣）；`move_one_item_to` 為權威 guard，雙重保險。
- 屬「裝備條件互動 / 鑰匙開鎖容器」家族的早期個案；目前只實作 Phase 2 公寓插槽用得到的部分，不提前展開其他變體（如鑰匙被消耗）。

### Discard API（Phase 1-E）

```gdscript
func discard_item(instance_id: String) -> bool
```

通用丟棄入口，由 T 鍵流程呼叫。

規則：

- 動態搜尋 `instance_id` 來源（玩家背包或任一外部容器）。
- 若該 `instance_id` 對應 item 的 `discardable=false` → 立即回傳 `false`，狀態不變。
- 若該 item 已裝備 → 立即回傳 `false`，狀態不變（必須先卸下；UI 端 1-E 把已裝備品的 T 接到「先卸下」流程，或直接擋住）。
- 否則：將 `instance_id` 對應的 1 個（若為 stackable）或整 slot（非 stackable）從來源 slot 移除；數量歸零時清空 slot；操作後來源自動整理。
- 成功時 emit `inventory_changed`（背包來源）或 `container_changed(container_id)`（容器來源）；若該 item 為已裝備品經卸下後再丟棄，額外 emit `equipment_changed`。
- 只回傳 `true` / `false`，不回傳 UI message。UI 層收到 `true` 顯示「已丟棄 {item_name}」toast；收到 `false` 顯示「無法丟棄 {item_name}」toast。

> ⚠️ MVP 階段「stackable 物品按 T 一次丟 1 個」：與 `move_one_item_to` 對稱；不提供整 stack 一次丟。

### Signals

```gdscript
signal inventory_changed
signal container_changed(container_id: String)
signal credits_changed(new_value: int)
signal knowledge_added(id: String)
signal notes_changed
signal equipment_changed
signal item_moved(move: Dictionary)
```

Transfer signal 規則：

- `player_inventory -> external container`：emit `inventory_changed`，並 emit `container_changed(target_container_id)`。
- `external container -> player_inventory`：emit `inventory_changed`，並 emit `container_changed(source_container_id)`。
- 移動成功時額外 emit `item_moved(move)`；emit 順序以資料已更新後為準。
- `count <= 0` 或 atomic 檢查失敗時不 emit。

### 驗收

- `GameState` 以 `GameState.has_knowledge("identity_door_unlock_method")` 可被任何場景直接呼叫。
- `has_note("clue_gloves_decoder")` 可判斷線索筆記是否存在；`has_knowledge("clue_gloves_decoder")` 不應作為線索筆記去重依據。
- 新增同一筆 note 兩次，不會在筆記 UI 中出現重複條目。
- `add_credits(-999)` 或 `set_credits(-1)` 不會讓 credits 小於 0。
- 背包容量超過 15 格時 `add_item()` 回傳 `false`。
- `add_item(item_id, 0)`、`add_item(item_id, -1)`、`remove_item(item_id, 0)`、`remove_item(item_id, -1)` 回傳 `false`，且不改狀態。
- 裝備槽滿時 `equip()` 回傳 `false`，且不替換既有裝備。
- `get_inventory()`、`get_container()`、`get_equipment()` 回傳值被外部修改時，不會改到 GameState 內部資料。
- `configure_container("fridge_storage", 10)` 後可取得 10 格空容器。
- `move_one_item_to("fridge_storage", instance_id)` 可將背包物品移入冰箱；`move_one_item_to("player_inventory", instance_id)` 可將冰箱物品移回背包。
- 嘗試 `container -> container` 回傳 `false`，且不改狀態。
- 目標容器滿格時移動回傳 `false`，來源數量不可減少。
- 背包與容器移動成功時 emit 正確的 `inventory_changed` 與 `container_changed(container_id)`。
- 背包與容器移動成功時 `item_moved(move)` payload 可精準定位 target instance，供 2-B 解碼轉換使用。
- MVP 不寫入磁碟；重開遊戲狀態可重置。

## 物品資料

待實作（GameState 中已有 MVP stub `ITEMS_DB`，正式版稍後拆出獨立資料系統）。

欄位：

```gdscript
{
  "id": "old_work_badge",
  "name": "磨損的工作證",
  "description": "...",
  "category": "key_item",
  "stackable": false,
  "max_stack": 1,
  "discardable": false,
  "usable": false,
  "equipment_slot": "",
  "icon_path": "res://assets/generated/sprites/items/old_work_badge/icon.png"
}
```

欄位規則：

- `icon_path` 必填，指向 64x64 透明 PNG。
- `name` 顯示於查看 R Modal 與 FloatingToast 訊息。
- `description` 顯示於查看 R Modal 描述區。
- `category` 列舉：`key_item` / `equipment` / `consumable` / `misc`。決定 R Modal 底部 tag 文字與 E 鍵行為。
- `equipment_slot` 空字串代表非裝備品；非空時必須為 `clothing` / `hand` / `accessory` 三者之一。
- `stackable` true 時 stack 規則由 `max_stack` 控制；false 時每個 instance 各占一格。
- `discardable` false 時 T 鍵被 `discard_item` API 攔下；UI 端顯示 FloatingToast「無法丟棄 {name}」。
- `consume_grants_note`（選填）代表 E 使用或 R 查看會消耗該 instance 並寫入指定筆記；無此欄位的 consumable 使用時顯示「現在用不上。」且不消耗。
- `usable` / 可反覆使用旗標保留給未來非消耗型使用效果；目前無實例，不提前實作變體。

`Credits` 不放入物品格，而是玩家狀態數字。

### Phase 1 所需 Item Icon 清單

MVP 階段預載至少這 4 個物品（對應 `ITEMS_DB` stub），均需 64x64 透明 PNG，風格遵守 Art Bible（HD 2D + riso 顆粒、限色）：

| item_id | name | category | icon path |
|---|---|---|---|
| `old_work_badge` | 磨損的工作證 | key_item | `res://assets/generated/sprites/items/old_work_badge/icon.png` |
| `fingerless_gloves` | 無指工作手套 | equipment (hand) | `res://assets/generated/sprites/items/fingerless_gloves/icon.png` |
| `canned_food` | 合成罐頭 | consumable | `res://assets/generated/sprites/items/canned_food/icon.png` |
| `faded_jacket` | 隱士防風夾克 | equipment (clothing) | `res://assets/generated/sprites/items/faded_jacket/icon.png` |

生成由 `agent-sprite-forge` 走 `$generate2dsprite`（非場景類），輸出依 AGENTS.md 規則放在 `assets/generated/sprites/items/<item_id>/`。

Phase 2 解謎可能新增更多物品 icon，到時再追加清單。

### 驗收

- 所有物品行為（堆疊、丟棄、裝備）皆由資料欄位決定，程式不得針對特定 `id` 寫死規則。
- `Credits` 不出現在背包格中，但顯示於背包 UI。
- 每個 `ITEMS_DB` 條目都有 `icon_path`，且該檔案存在；缺檔時背包格顯示明顯 placeholder（如紅底問號）方便發現。

## 裝備系統

已完成 Phase 1-E 實作與人工驗收。

裝備槽：

```text
衣服 (clothing)：1
手持 (hand)    ：2
其他 (accessory)：2
```

僅 `category=equipment` 且 `equipment_slot != ""` 的物品可裝備。

E 鍵行為（**只在 INVENTORY mode、focused 在背包格時生效**；CONTAINER mode 中 E 永遠是「移動」）：

| Focused item | E 行為 |
|---|---|
| 未裝備 equipment | 裝備到對應 slot；槽滿時顯示 FloatingToast「這類裝備已經滿了，先卸下身上的再裝備新的。」 |
| 已裝備 equipment | 卸下 |
| `category=consumable` 或可反覆使用物 | 使用 dispatch：帶 `consume_grants_note` 者顯示 MessageBox + 寫入筆記 + 消耗；無效果 consumable 顯示 FloatingToast「現在用不上。」且不消耗；可反覆使用物執行效果且不消耗（預見類型，暫無實例） |
| 無對應動作物（如 `key_item` / `misc`） | 無 E 動作；footer 不顯示 E 段，若收到 E 輸入則不改變狀態 |

排序與視覺：

- 已裝備品在背包排序中固定置頂；同 `item_id` 多裝備時內部順序不強規範。
- 卸下後自動整理重排（依 GameState 自動整理規則），focus 保持 row/col index。
- 已裝備品在背包格右上角顯示 `E` 標記（1-B 已預留位置，1-E 啟用渲染）。

### 驗收

- 衣服槽 1、手持槽 2、其他槽 2，超過時不可裝備。
- 已裝備品在背包排序中固定置頂；卸下後自動重排，focus index 不變。
- 對未裝備品按 E 是「裝備」，對已裝備品按 E 是「卸下」，互不混淆。
- 槽滿時顯示 FloatingToast「這類裝備已經滿了，先卸下身上的再裝備新的。」，原裝備不替換。
- 已裝備品右上角顯示 `E` 標記；卸下後標記消失。
- INVENTORY 中對帶 `consume_grants_note` 的 consumable 按 E 使用時，顯示訊息、寫入筆記並移除該 instance；對無效果 consumable 按 E 顯示 FloatingToast「現在用不上。」且不消耗。
- INVENTORY 中對無 E 動作的 `key_item` / `misc` 不顯示 footer E 段，按 E 不改變狀態。
- CONTAINER 雙欄中 E 永遠是「移動」，不會觸發裝備行為。

## 筆記 / 知識系統

已完成 Phase 1-C 基礎 UI 與人工驗收。

筆記分類：

```text
身份
工作
線索
```

定義：

- `身份`：穩定、已確認的自我資訊，例如這裡是自己的公寓、自己的職業、門鎖開法。
- `工作`：委託、任務、零工、工作紀錄。
- `線索`：暫時、不完整、尚未理解的資訊，可轉化為身份或工作筆記。

操作：

```text
J = 開 / 關筆記
Esc = 關閉
A / D（或 Left / Right）= 切分類
W / S（或 Up / Down）= 選筆記
選到筆記後自動顯示全文
```

> 2-F 修正（**待實作**）：筆記導航比照 `BagGrid` 同時接 WASD 與方向鍵——A / D 切分類、W / S 選筆記，方向鍵保留為鏡像。footer 主打 WASD（見「共用 UI 元件 > PanelFooterHint」2-F 註）。同時筆記內容皆控制在全文欄一頁內，Page Up / Down 捲動移除。

UI 規則：

- 開啟時走半透明 overlay，世界仍可見（依 UI Mode 規則）。
- 三分類顯示為頂部 tab；當前 tab 高亮。
- 同一面板左側顯示該分類筆記列表，右側顯示選中筆記全文（無需另開 Modal）。
- 玩家進入筆記 UI 時預設 focus 在第一個非空分類的第一筆筆記。

### Panel 尺寸與排版

```text
panel 寬 = 880
panel 高 = 560
位置     = viewport 置中（1280x800 viewport 下左上 ≈ (200, 120)）

內部分區：
  TAB_BAR_HEIGHT     = 36         # 頂部三分類 tab
  LIST_PANE_WIDTH    = 240        # 左側列表欄
  PADDING            = 16
  全文欄寬           = panel 寬 - LIST_PANE_WIDTH - PADDING * 3 = ~600
  內容區高           = panel 高 - TAB_BAR_HEIGHT - PADDING * 2 = ~492
```

### Skin

沿用 1-B 已定的 `inventory` skin（深藍灰底 + 主角橙紅邊框 + 米白標題字），與背包同一視覺家族。

### Tab Bar

```text
[ 身份 ]  [ 工作 ]  [ 線索 ]
```

- 每個 tab 固定寬 120px、高 = TAB_BAR_HEIGHT (36px)。
- 文字 18px。
- **未選中** tab：文字暗灰 `(0.55, 0.55, 0.55, 1.0)`，無底線。
- **選中** tab：文字米白 + 底部 2px 橙紅線（同 panel 邊框色 `(0.78, 0.42, 0.20, 1.0)`）。
- 鍵盤 A / D（或 Left / Right）切換 tab；切換後 focus 自動跳到該分類第一筆。

### 列表欄

- 每行高 32px、font 16px。
- 顯示**只有 title**（不顯示 body 預覽）。
- 240px 寬可塞 ~14 筆可見；超出時列表自動垂直捲動跟隨 focus（Godot ScrollContainer + 自動 ensure_control_visible）。
- 行間 hover / focus 視覺：
  - 預設 title 字色依 status 決定（見下）。
  - 鍵盤 focus 行：2px 橙紅外框 + 行底色微亮。
- 空分類：列表顯示「（尚無筆記）」placeholder，右側全文欄空白。

### `status` 視覺

筆記 `status` 欄位 MVP 渲染：

- `active`：title 字色米白 `(0.94, 0.92, 0.84, 1.0)`。
- 其他值（`superseded` / `inactive` / 未來新增）：title 字色灰 `(0.55, 0.55, 0.55, 1.0)`。

1-C 階段所有預載筆記都是 `active`，此規則為 Phase 2 / 3 預留。

### 全文欄

- 用 `RichTextLabel` 顯示 `body`，`scroll_active = true`。
- 字 16px，行高建議 1.5。
- 2-F 起：MVP 筆記內容皆控制在全文欄一頁內，不提供捲動；Page Up / Down 已停用（程式吞掉不作用）。
- 切到不同筆記時全文重新填入。

### 操作 focus 流向

```text
進入筆記 UI    → focus 在「第一個非空分類」的第一筆筆記
A / D          → 切 tab，focus 跳到該分類第一筆（Left / Right 同義）
W / S          → 在列表內上下選筆記；超出列表邊界不繞回（Up / Down 同義）
                 列表捲動跟隨 focus（自動 ensure_control_visible）
切到不同筆記   → 全文欄即時更新
J / Esc        → 關閉筆記
```

全文欄本身**不參與 Tab focus 鏈**。2-F 起筆記皆一頁內，不需捲動全文，Page Up / Down 已停用。

資料範例：

```gdscript
{
  "i#### 2-G 驗收意圖

開場獨白序列（新遊戲進公寓即播；建立「失憶甦醒」基調，銜接 B0 醒來）——

- **進場狀態**：場景載入即進入開場——玩家鎖在 `prone`（趴睡）姿態、世界輸入全擋、互動 prompt 隱藏、MessageBox 接管畫面顯示第 1 頁。
- **共 3 頁**，逐頁逐字播放（沿用既有 MessageBox 6 字/秒打字機）；**文字播放中不可跳過**，整頁顯示完成後才接受換頁。
- **換頁鍵**：WASDE / 方向鍵 / Esc，任一即進下一頁；最後一頁同鍵關閉 MessageBox。
- **換頁提示**：每頁文字跑完後淡入提示——前二頁「▼ 繼續」、第三頁「▼ 開始」；文字未跑完不顯示、不接受換頁。
- **分頁標題**（感官甦醒 / 空白的記憶 / 自囚與尋回）置於該頁正文第一行，隨打字機一起逐字顯示，不另設標題樣式。
- **起身動畫**：第 3 頁一出現即播放 `get_up`（10 frame、約 1 秒、不循環），播完停最後一格切 `idle`；此頁文字同時以 6 字/秒續跑，文字與動畫互不等待（文字與動畫互不影響）。
- **結束**：第 3 頁文字跑完、玩家按鍵關閉 MessageBox → 解除輸入封鎖，玩家以站立 `idle` 開始自由操作。開場結束時角色一律停在站立 `idle`，不論動畫播放進度（被跳過則直接 snap 站姿）。
- **每次進場都播**（MVP 無存檔機制；日後加入存檔再 gate「只播一次」）。
- **隱藏測試捷徑**：開場期間 **1 秒內連按 3 下 T** = 立即補完當頁文字並跳下一頁；於第 3 頁則直接關閉開場、解鎖操作。此捷徑不顯示於提示，純測試用途。
- 沿用既有系統：MessageBox（2-E overlay 視覺 / 打字機）、`UIMode.is_world_input_blocked()` 輸入封鎖路徑、玩家 `AnimatedSprite2D`；無新 GameState API。開場接管邏輯（多頁 / 不可跳過 / 動畫鉤子 / T 捷徑）實作見 `開發設計方針.md > 2-G`。
- 開場文案全文見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`。��垂直捲動，切到其他筆記時捲動位置重置回頂。
- INVENTORY 中按 `J` 直接切到筆記（背包關、筆記開），無雙重 panel 疊加。

## 知識解鎖

門與部分劇情進展使用知識 id 判斷，不直接讀筆記文字。

範例：

```gdscript
GameState.has_knowledge("identity_door_unlock_method")
```

目前規格：

- 大門離開公寓的條件是知識，不是物品。
- 玩家需要從房間探索中取得足夠線索，最後形成穩定知識。

### 驗收

- 任何劇情 gate（如大門）只透過 `GameState.has_knowledge(id)` 判斷，不依賴 inventory item id。
- 未取得對應知識時 gate 行為穩定（顯示鎖定訊息、不放行）。

## 音訊（BGM）

MVP 音訊極簡。解謎短音（`sonar_ping.wav` / `hidden_slot_reveal.wav` / 電磁聲響）由觸發處各自播放，無集中匯流排。背景音樂於 2-F 加入。

### 公寓 BGM（2-F，待實作）

- 進公寓場景自動循環播放單一背景音樂。
- 檔案：`res://assets/bgm/Echoes of a Cozy Night.mp3`（Godot 4.6 原生支援 `AudioStreamMP3`；匯入設定開 `Loop`）。
- 音量寫死 `-12 dB`；設定選單調音量留 Phase 3+。
- 檔案不存在時靜音、不報錯。
- 實作位置：`apartment_room.tscn` 直接掛一個 `AudioStreamPlayer` 節點（`autoplay = on`、stream loop、`volume_db = -12`），不為單場景抽 autoload。Phase 3+ 有第二場景、需跨場景續播時再抽 `MusicManager`。

#### 驗收

- 進公寓場景即聽到 BGM 循環播放，無明顯爆音；循環不中斷停止。
- 音量為背景襯底音量（`-12 dB`），不蓋過解謎短音。
- 移除 BGM 檔案後遊戲仍正常啟動、不噴錯（靜音）。

## Phase 規劃

### Phase 1 — 系統基本功能

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 2 — 公寓解謎

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 3 — 公寓觸控化（手機可玩）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 4 — 跨場景架構化（Main Host / GameUI / SceneRouter）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 5 — NPC + 對話系統（真系統）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 6 — SaveSystem（手動多槽存檔）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 7 — QuestManager + 第一個任務 vertical slice

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 8 — 便利商店 vertical slice + 買賣系統

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 9 — 拾遺系統（殘響蒐集 + 收藏家）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 10 — 氛圍與演出（Atmosphere & Presentation pass）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

### Phase 11+ — 主線《雨還沒停》v2.3 實作規劃（順敘版）

> **敘事 / 世界邏輯 / 台詞 / 因果事實來源：`subdocs/主線/雨還沒停v2.3.md`（拍板整合稿）。** 本段只定義「系統規格 + 子階段 + 驗收意圖」，不重述劇情；角色動機、台詞、結局判定語意一律以該檔為準。
> 實作契約見 `開發設計方針.md > Phase 11+`；操作 / 驗收清單見 `測試指南.md > Phase 11+`。
> **執行原則：嚴格順敘、不跳號。** Phase 編號順序＝遊戲時序（Act 1→2→3→過場→4→結局），每個 Phase 疊在前一個已完成且可測的 Phase 上。v2.3 §11「先做可通關脊椎再回填」的 build order **本專案不採用**——那需要跳做；順敘可確保永遠不會 callback 到尚未實作的內容。
> 規模：5 幕 8–10h，3 個新場景（地下道/聚落、夜總會、AI 資料中心備份區）+ 地下廣播站 + 戰鬥 + 跳躍 + Trust/Trace 雙軸 + 三結局。**Phase 11–31 共 21 個 Phase、約 90 子階段。**

#### 開工前 blocker（v2.3 §12 待拍板，影響排程）

1. **地下廣播站是否升正式場景** → 決定 Phase 27 是否成立。（✅ 2026-07-02 拍板：**升正式場景** `broadcast_station`，27-A 成立；見 Phase 27。）
2. **新女聲主題歌資產**（歌名 / 編曲 / VO）→ Phase 21 核心；雖排在中段，**素材需提早委製**，否則 21 卡料（夜總會 + 林霏核心殘響已併入 21）。
3. **Reclaim / Protect 確認只染色、不硬鎖** → 決定 Phase 27/28 結局路由設計。（✅ 2026-07-02 拍板：**不硬鎖**，三選無條件全開；唯一硬分支＝Expose 內部 A/B/C；見 Phase 27。）

#### 跨 Phase 系統語意（新地基，先讀）

**Trust / Trace 雙軸 + 清洗（v2.3 §6）**

- **Trace（系統追蹤風險）**：第一版獨立 `int` 軸（GameState），隱性、不顯數值。`留`（保存殘響原始資料）↑；`賣`（原始資料離手）↓；`還`（給回執 / 替鹿還母親殘響）不增或微降。Trace 過高在 Expose 結局硬鎖 C（見三結局）。
- **Trust（他人信任）**：第一版**不新建獨立軸**，以既有 `affinity_*`（如 `affinity_wan`）近似；統一以 adapter 介面查詢，留日後抽獨立軸的縫。
- **清洗（cleaning）**：對可保存的記憶資料（殘響 / 回執）執行「刪定位 / 模糊聲紋 / 移除系統標記、只留情感內容」。清洗後再還 → Trust↑、Trace 較低。**代價：不可逆損失部分真相細節，玩家無法得知刪掉了多少。** 清洗第一次硬兌現在 Expose 結局（Phase 27）；若要在「替鹿還母親殘響」先讓玩家碰到，於該處加輕量 hook。
- **落地順序（v2.3 拍板）**：先「Trace 單軸 + 清洗」，Trust 暫接 `affinity`。

**戰鬥 + 跳躍（v2.3 §5，先做 Act 2B 原型）**

- **戰鬥限機器**：棍子只 stun；核心解＝輕跳繞後 + 按住 E 強制格式化重置。**人類敵人不格式化**（嚇退 / 交易 / 環境阻隔 / 逃）。**輸 = 岔到另一條故事線，無 Game Over。** 撿來的棍子不會壞、無升級、無技能樹。
- **跳躍＝探索 / 移動動詞**：短跳 + 抓邊緣，**無二段跳 / 蹬牆**；戰鬥中只用於繞後。
- 先做 Act 2B 一場原型（隧道清潔機）可玩再擴張；長線約 2–3 場。

**三結局硬鎖規則（v2.3 §8）**

- 共用觸發點：Act 4 備份區，站在自己的備份前。
- **Reclaim / Protect 傾向只染色、不硬鎖。** **Expose 例外**——用 Trace + 上傳檔案清洗狀態硬分 A/B/C：A 未清洗→聚落焚毀；B 已清洗→匿名雜訊但保住所有人；C Trace 過臨界→上傳 99% 被攔、主角當場被抹除（收向「你以為在選、系統毫不在意」的虛無，非懲罰玩家）。

---

#### Phase 11 — Trust/Trace 雙軸 + 清洗（地基，無新場景）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 12 — 跳躍 / 平台移動（地基）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 13 — 戰鬥（機器格式化重置 + 人類分支 + 輸=岔線）（地基）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 14 — Act 1 主線鉤子（複用平民區，輕量）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 15 — Act 2 場景骨架（地鐵站 + 地下道聚落，2 真場景）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 16 — 三張臉 NPC 落地（小岑 / 伍姐 / 七號）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 17 — Act 2A 記憶碎片 + 殘響鋪設

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 18 — Act 2B 戰鬥遭遇 + 回執取得（接 Phase 13）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 19 — Act 2C 阿達罪責（四段式 ②③，鎖 2C）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 20 — Act 2D 七號可選 + 和平線 Branch D

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 21 — Act 3 夜總會 + 林霏核心殘響 + 主題歌整合（原 Phase 22 已併入）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 23 — Act 3 夜總會保全四解小解謎（戰鬥② 智取版）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 24 — Act 3→4 七號小爆點（三分支 quest）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 25 — Act 4 備份區場景 + 戰鬥③

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

#### Phase 26 — Act 4 演出 + 真相碎片 + 結局觸發點

> **狀態：✅ 已完成**（headless PASS，commits `2bc4da7` / `003db3b` / `d31f4a0`）。以下驗收意圖存檔供回查。
> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §4.3（晚拉扯兩句拍板台詞）+ §3（四段式揭露④）+ §7 五幕表（Act 4 真相碎片）+ §8（三結局共用觸發點）+ §7.2（Branch B）。**核心：在 Phase 25 已建好的三場景上掛 Act 4 全部演出——阿達④、晚拉扯、真相碎片、結局觸發點武裝。零新系統、純複用**（對話 5/16、`MemoryFragmentArea` 14、examine 條件變體 19、自動觸發 Area2D）。三結局路由本體＝27-B；**本 Phase 不寫任何結局分支、不開結局選單、不碰 Trace、零新存讀檔 schema。**（規格 2026-07-02 討論定案；v2.3 §12 開放問題 5「Branch B 哪些檔案被重標記」同日拍板，見 26-D。）

Act 4 節拍動線（拍板）：

| 場景 | 節拍（依序） | 觸發 |
|---|---|---|
| `datacenter_entrance` | ① 26-B 阿達④短暫登場（靠 travel 落點）→ ② 26-A 晚拉扯（門禁大門前） | 皆走近自動觸發、一次性；帶工單來的玩家常同趟連吃兩拍（中間隔 ~300px + DeliveryBot flavor，屬設計內） |
| `datacenter_backup` | 戰鬥③（Phase 25 已完成，**不動**） | — |
| `datacenter_backup_core` | ③ 26-C 真相碎片（備份主機前必經路徑）→ ④ 26-D 檔案索引終端 + 自己的備份（觸發點武裝） | 碎片走近自動收取；其餘 examine |

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 26-A | 晚拉扯演出：晚實體到場 `datacenter_entrance` 門禁大門前，走近自動觸發一次性對話——v2.3 §4.3 兩句拍板台詞（活人安全版＋私心版）**一次給足、不設 affinity 門檻**（私心＝晚核心人設非好感獎勵，§12-3 只染色不硬鎖），結尾 set `wan_act4_pull_seen`；之後晚留原地可 E 重談 retalk 短版（5-D 條件路由慣例）。25-A 的 travel flavor「再看一眼舊卡」＝她跟來了的鋪墊；拉扯**不參與門禁 gate**（看完即可過門，25-A 契約不動） | headless：走近觸發＋旗標寫入；一次性不重播；retalk 路由正確；不擋門禁 |
| 26-B | 阿達④本人短暫登場一次（§3 四段式收尾，拍板台詞「你忘了啊？也好。至少有人可以忘」）：`datacenter_entrance` 靠 travel 落點，**首次到場且 `get_flag("read_old_work_order")`** 才自動觸發一拍短對話（無選項），結尾 set `ada_final_words_seen` → 說完淡出（modulate fade）**永久消失**（旗標已設時重進場景不生成）。順序護欄：**④不得早於③**（19-D 已鎖②→③順序，驗③即隱含②；缺工單本趟不登場、下趟補） | headless：缺 `read_old_work_order` 不登場；齊全首次觸發＋旗標；一次性；旗標已設重進不生成 |
| 26-C | 核心真相碎片「你自己選擇被刪、為保護那個網」：`datacenter_backup_core` 備份主機**前必經路徑**掛 `MemoryFragmentArea`（14 同款，走近自動收取），set `mem_frag_chose_deletion`；文案回收 `mem_frag_commute_topside`（往上＝去服務富人）與 `mem_frag_hideout`（黑戶藏身方式）兩碎片意象（純文字回連、不作 gate）。空間順序保證碎片先於觸發點 | headless：進 core 必經自動收取＋旗標；重入不重複 |
| 26-D | 結局觸發點武裝 + Branch B 檔案重標記（**v2.3 §12 開放問題 5 拍板**）：（1）「自己的備份」examine（25-A 既有互動物改分派）——碎片前維持中性佔位、不 set 旗標；`mem_frag_chose_deletion` 後換重量級文字（**名字露出一次**，§4.1 名字不對稱）＋ set `stood_before_own_backup`（冪等、可重看；**已被 27-B 取代**：武裝後重看改開三選對話），**不開任何選單**（4-G 教訓）。**forward 契約：27-B 三結局路由掛同一互動物 + 此旗標**。（2）新增「檔案索引終端」examine（core）——全分支中性 flavor；`seven_stopped_partial` 時變體＝**小岑聲紋檔具體標紅「待清理」＋一批未具名黑戶檔案被重標記**（背叛要具體 §4.2.4；不點名伍姐／七號／林霏）。Branch B 變體**不寫進**自己備份的 examine：「小岑被連累」與「你自己的真相」兩個情緒重點分屬兩互動物，避免互鈍化（§3 同理） | headless：碎片前 examine 中性、不 set 旗標；碎片後重量級＋旗標；終端兩版文字依 `seven_stopped_partial` 正確分派 |
| 26-E | 回歸 + 存讀檔 + GUI：四旗標 round-trip；26-A/B 一次性跨存讀不重播；順序護欄；Phase 1~25 不退化 | headless PASS + GUI / 純觸控走查 |

依賴：25（三場景 + 核心佔位 + BGM 已接）、24（`seven_stopped_partial`）、19-B（`read_old_work_order`）、5/16（對話系統 + NPC 掛法）、14（`MemoryFragmentArea`）、19（examine 條件變體 pattern）。

canonical 旗標（皆 bool / `story_flags`，**零新存讀檔 schema**）：`wan_act4_pull_seen` / `ada_final_words_seen` / `mem_frag_chose_deletion` / `stood_before_own_backup`（**27-B 路由掛點**）。只讀不寫：`read_old_work_order`、`seven_stopped_partial`。

系統擴充：**無**（對話 / MemoryFragmentArea / examine 條件變體 / 自動觸發 Area 全複用）。

外部素材：**唯一缺口＝阿達**——世界 sprite（idle 即可，離場用 fade 不需 walk）＋對話立繪；g2d 可生、非 user blocker，26-B 動工前補。晚 sprite / 對話立繪、Act 4 BGM（25 已接線）皆既有。

前提硬規則：不碰 Trace；不寫結局分支 / 不開結局選單（路由＝27-B）；④不得早於③；私心版不設 affinity 門檻；Branch B 呈現小岑具體、其餘不具名，不點名伍姐／七號／林霏；§4.3 / §3 拍板句直接用，補白文案實作時定稿。

#### Phase 27 — 地下廣播站 + 結局路由 + 清洗閘兌現

> **狀態：✅ 27-A~D 全部已完成**（27-A commit `7941519`；27-B/27-C/27-D 本次實作，headless 本機待跑）。
> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8（三結局共用觸發點 + Expose 清洗）+ §6（清洗定義）+ §4.1（名字不對稱）。**核心：兌現 26-D forward 契約——把武裝好的觸發點接上三結局路由（三選對話），新建 Expose 出口場景 `broadcast_station`，清洗第一次硬兌現（上傳終端五拍）。**（規格 2026-07-02 討論定案；同日拍板 v2.3 §12 開放問題 1「地下廣播站升正式場景」與 3「Reclaim / Protect 只染色不硬鎖」。）
> **路由規則（拍板）**：三選（Reclaim / Protect / Expose）**無條件全開、不看 Trust / Trace**（隱性數值鎖選項既不公平也傳達不了主題；「系統毫不在意」由 29-C 專門承載，前提正是選擇自由）；選單可退開、**選定即鎖**（point of no return＝各選項確認拍按下「就這麼做」）；唯一硬分支＝Expose 內部 A/B/C（**判定與 Trace 臨界值全留 Phase 29，本 Phase 不碰 Trace**）。

三選對話節拍（27-B，掛「自己的備份」同一互動物；`stood_before_own_backup` 已設後重看＝開選單——第一次你讀懂了自己，再伸手才是決定）：

1. **存檔提示（一次性；2026-07-03 P28 規格討論拍板回補）**：首次開選單前先出提示拍，覆蓋三條路（Expose 單向轉場同樣吃到）；推進 set `ending_save_hint_seen`，之後重開選單不再出。文案已拍板：「你站在自己的備份前面。也許，你也該為現在的自己留一份。（接下來的選擇無法撤回——建議先存檔。）」（`datacenter_backup_core` `can_save_here == true`，玩家退開即可當場存檔）。
2. **重錨定**：一兩行短拍，不重複 26-D 真相全文；名字不再露出（§4.1，26-D 已露一次）。
3. **三個動詞**：選項不用抽象標籤，用動作——「**灌回去**」（Reclaim）/「**刪掉它**」（Protect）/「**拷貝走**」（Expose）＋「退開」。
4. **確認前小節**（每條各一拍，只給重量、不劇透結局）→「就這麼做」＝鎖點寫旗標 /「回去再想」退回。
5. **收尾**：Reclaim / Protect 寫旗標＋一拍短收尾停原地（結局演出＝28 接手；之後備份 examine＝「已決定」中性變體、不可重選）；Expose 寫旗標＋**單向轉場** `broadcast_station`。

清洗閘五拍（27-C，掛廣播站上傳終端）：

1. **插入與掃描**：終端列出檔案內容（你的過去、遺忘服務證據、殘響）；依已蒐殘響段數兩檔文字變體（數字化呈現、不點名）。
2. **中性警告**：檔案含定位元數據與聲紋，廣播出去可沿線找到活人；`seven_stopped_partial` 時加一句 Branch B 變體（你已見過一次下場）。
3. **手藝反諷＋首選**：清洗＝刪定位 / 模糊聲紋 / 移除系統標記——善後員做過一萬次的動作，這次做不做由你；「動手清洗」/「原樣保留」/「退開」。
4. **代價當場兌現**：清洗→刪除演出（「已移除 N 項可追蹤資料」但不告知是哪些；「有些東西沒了，你永遠不會知道是什麼」）；原樣→靜默一拍（「檔案完整。包括能找到他們的每一條路。」）。
5. **最終確認＝鎖點**：「開始上傳？」是→寫 `expose_upload_cleaned`＋`expose_upload_done`＋中性收尾（上傳開始）停（A/B/C 判定演出＝29 接手）；退開→清洗選擇**不保留**、下次重走整段（旗標只在最終確認寫入，天然無「已清洗未上傳」中間態）。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 27-A | `broadcast_station` 正式場景（**v2.3 §12 開放問題 1 拍板升正式，2026-07-02**）：單室、唯一 entry `from_backup_core`、進場一次性 MessageBox 交代空間跳躍（資料中心→地下的路程不玩、文案跳過）、2–3 個 flavor examine（暗示有人維護、**不放 NPC**——結局前最後一室留給玩家一個人）、**無回資料中心出口**（單向）、`can_save_here = true`（2026-07-02 user 拍板可存檔）、新 BGM（user 委製，已到貨 2026-07-02）、map（已到貨 2026-07-02） | headless：載入 + 轉場 + 場景內存讀 |
| 27-B | 三結局路由（26-D forward 契約兌現）：三選對話樹節拍見上（含首開選單前一次性存檔提示拍，set `ending_save_hint_seen`）；選定寫互斥路由旗標；R / P 停原地＋備份改「已決定」變體不可重選；Expose 單向轉場 | headless：三路由分派正確；退開不寫旗標；鎖後不可重選；存檔提示一次性（旗標已設重開直入重錨定） |
| 27-C | 上傳前清洗閘（**清洗第一次硬兌現**，11-D 移入）：五拍見上；清洗＝單一 bool 整包決定、**無逐項清洗 UI**（「你永遠無法知道自己刪掉了多少」要求玩家不可逐項檢視） | headless：清洗二態寫入正確；退開不留半套狀態；變體分派正確 |
| 27-D | 回歸 + 存讀檔：6 旗標 round-trip（含 `ending_save_hint_seen`）；廣播站內存讀 round-trip；三選前留檔可分別走三路；Phase 1~26 不退化 | headless PASS + GUI / 純觸控走查 |

依賴：26（`stood_before_own_backup` 觸發點武裝）、5/16（對話系統）、24（`travel` effect op / `seven_stopped_partial`）、4-E（entry point 演出）、9（`echo_progress` 計數）、11（清洗概念自 11-D 移入；**trace 本 Phase 不讀不寫**）。

canonical 旗標（皆 bool / `story_flags`，**零新存讀檔 schema**）：`ending_route_reclaim` / `ending_route_protect` / `ending_route_expose`（**互斥**，27-B 確認拍寫入；下游 28-A / 28-B / 29）＋ `expose_upload_cleaned` / `expose_upload_done`（27-C 鎖點同時寫入；29-A/B 分界 / 29 判定觸發掛點）＋ `ending_save_hint_seen`（27-B 存檔提示拍寫入；僅一次性去重、無下游讀者；2026-07-03 拍板回補）。只讀不寫：`stood_before_own_backup`、`seven_stopped_partial`、`echo_progress`（計數染色）。

系統擴充：**唯一＝DialogueRunner 補 `echo_count` condition**（已蒐殘響段數門檻，供清洗閘第 1 拍兩檔變體；Phase 23 補 `credits` condition 同例）；其餘全複用（對話 5/16、`travel` effect 24、entry point 演出 4-E、examine 條件變體 19）。

外部素材：**已全數到貨（2026-07-02）**——map（`assets/generated/maps/broadcast_station/broadcast_station-20260702-234241.png`）＋ 廣播站 BGM 新軌（`assets/bgm/The Yellow Light Tape.mp3`，user 委製）；無剩餘 blocker。

前提硬規則：三選無條件全開（不看 Trust / Trace）；選定即鎖、確認拍前皆可退開；名字不再露出（§4.1）；清洗＝單一 bool、玩家不可逐項檢視；本 Phase 不碰 Trace / 不做 A/B/C 判定（29）/ 不做結局演出（28）；台詞補白實作時定稿。

#### Phase 28 — Reclaim + Protect 結局演出

> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8（Reclaim / Protect 兩結局＋拍板台詞）；小岑台詞舞台化改句與「上行線復駛」背景事件＝2026-07-03 本 Phase 規格討論拍板（v2.3 §8 已加註）。**核心：兌現 27-B 的 R / P 演出掛點——確認拍後接腳本化單向序列（`travel` 串既有場景＋epilogue entry point 演出），Reclaim 三站、Protect 三站（中間站依 Branch B 分岔），終站公寓寫 `ending_reclaim_played` / `ending_protect_played`（= 30-A 共用收尾觸發掛點）。零新場景、零系統擴充。**（規格 2026-07-03 討論定案。）
> **序列規則（拍板）**：全程腳本化、站間自動 `travel`、玩家不自由走動（尾聲防護成本歸零，結局不該還能逛便利商店）；序列全程 `can_save_here = false`（鎖點前已有 27-B 存檔提示；中斷＝從選擇前的檔重來）；染色只落 Reclaim（`trace` / `affinity_wan` 各兩檔、共四版文字）、Protect 全程 v2.3 拍板台詞固定不染——**Reclaim 重、Protect 靜，不對稱本身是表達**。

Reclaim 序列（28-A，三站）：

1. **原地灌回演出**（`datacenter_backup_core`）：2-G 開場獨白的鏡像——begin_message 多頁不可跳過「想起全部」，回收五枚歷程碎片（撞林霏 14 / 往上通勤 17 / 抹過阿達 19 / 黑戶藏身 22 / 自選被刪 26）＋壓垮拍；壓垮拍依 `trace` 兩檔變體（高＝帶回的真相還連著線、危險寫得具體；低＝壓垮純粹是重量本身；門檻常數**獨立於 29-C 判定臨界**、開工校）。
2. **晚訣別**（travel → `apartment_entrance`）：晚在老位置，走近自動觸發一次性對話（26-A pattern）；依 `affinity_wan` 兩檔（`>=2` 慣例門檻）：高＝長訣別版（「最痛」落點）／低＝短冷版（一兩句就轉身）；說完接**情境圖 CG 拍**（晚轉身走進雨裡；正式形式，2026-07-03 拍板見前提硬規則）→ CG 收掉後晚 sprite 已移除＝**永久消失**。
3. **公寓收尾**（travel → `apartment`）：進房自動一拍——晚留下的痕跡（文案實作時定稿）＋ set `ending_reclaim_played` → 靜止停（30-A 接手點）。

Protect 序列（28-B ＋ 28-C，三站）：

1. **原地刪除演出**（`datacenter_backup_core`）：短、安靜，一兩頁——與 Reclaim 的多頁轟炸刻意不對稱（「連知道自己為何這麼做的權利都放棄」）。
2. **28-C 中間站（依 Branch B 分岔，兩站擇一）**：
   - 未走 Branch B（`cen_voiceprint_exposed` false）→ travel → `subway_station` 大廳：**上行線復駛背景事件**（車站廣播一拍＋Phase 10 `AmbientSubway` 遠轟；**與主角／結局分支無關、不改聚落安全動線**——「系統照常運轉」的具象化）；小岑 idle 站位票閘旁→**情境圖 CG 拍**（假聲紋過掃描、綠燈打在臉上）＋台詞「大叔，站那邊幹嘛？**車要來了欸。**」（v2.3 §8 舞台化改句）→ CG 收掉後小岑 sprite 已移除＝頭也不回走了。**不動兩張背景圖、月台室不經過。**
   - 走過 Branch B（true）→ travel → `underground_settlement`：空帳篷（複用 24-D `empty_tent`）＋伍姐自動走近一拍**沉默搖頭**（MessageBox，不開對話樹）。
3. **晚拍＋公寓收尾**（travel → `apartment_entrance` → `apartment`）：晚拍板台詞固定——「你又變回來了。」「變回什麼？」晚看著雨，沒有回答；**晚不消失、留在原地**（與 Reclaim 的離去成對比）；進公寓 set `ending_protect_played` → 靜止停。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 28-A | Reclaim 三站序列：灌回演出（trace 兩檔壓垮拍）→ 晚訣別（affinity 兩檔＋CG 拍永久消失）→ 公寓痕跡拍＋`ending_reclaim_played` | headless：route 旗標分派；兩處染色兩檔分派；旗標寫入；序列站禁存 |
| 28-B | Protect 主體：刪除演出（短）→（28-C 中間站）→ 晚固定台詞不消失 → 公寓收尾＋`ending_protect_played` | headless：分派＋旗標寫入；Protect 無染色變體 |
| 28-C | 小岑條件式回收＝中間站分岔：not-B＝地鐵大廳復駛＋小岑過閘不認得你；B＝聚落空帳篷＋伍姐沉默搖頭 | headless：`cen_voiceprint_exposed` 兩態分站正確 |
| 28-D | 回歸 + 存讀檔 + GUI：2 旗標 round-trip；序列站 `can_save_here == false`；選擇前留檔重讀可分走 R / P（含 28-C 兩變體共 3 條）；孤兒檔救援（27 期 route 已設未演出的檔）；Phase 1~27 不退化 | headless PASS + GUI / 純觸控走查 |

依賴：27（`ending_route_reclaim/protect`＋R / P 停原地掛點）、26（五枚碎片旗標 / 26-A 自動對話 / 26-B fade）、24（`travel` op / `cen_voiceprint_exposed` / `empty_tent`）、11（`trace` 只讀）、10（`AmbientSubway`）、5/16（對話系統）、4-E（entry point 演出）、2-G（begin_message 序列）。

canonical 旗標（皆 bool / `story_flags`，**零新存讀檔 schema**）：`ending_reclaim_played` / `ending_protect_played`（公寓終站寫入；30-A 共用收尾觸發掛點）。只讀不寫：`ending_route_reclaim/protect`、`cen_voiceprint_exposed`、`trace`、`affinity_wan`、五枚碎片旗標。**Trace 不寫。**

系統擴充：**無**（travel / entry point 演出 / begin_message / 自動對話 / MessageBox / AmbientSubway / **photo_viewer（9-E，CG 拍顯示；實作時決定直接用或抽薄 `cg_viewer` 變體）** 全複用）。

外部素材：情境圖 CG ×2（`res://assets/generated/sprites/cen/cg_gate_pass/cg_gate_pass.jpeg`＝小岑過閘微綠光、`res://assets/generated/sprites/wan/cg_walk_into_rain/cg_walk_into_rain.jpeg`＝晚走進雨裡；皆已就位）；皆非 user blocker、**不需 walk sprite**（2026-07-03 改情境圖拍板）。晚 / 小岑 / 伍姐 idle 與立繪、全部場景與 BGM 皆既有。

前提硬規則：復駛＝世界背景事件（三結局同一時間點、與主角無關、不改聚落安全；遊戲內僅 Protect not-B 途經可見，其餘由 30-A 收尾文字帶到）；**情境圖 CG＝兩拍（小岑過閘 / 晚走進雨裡）的正式演出形式非暫代（2026-07-03 拍板），升級只走「連續情境圖」加法路線（同系統、原圖續用），動畫化不列入規劃**；Protect 台詞固定不染色；七號不實體登場（和平線「留下」由 30-A 收尾文字帶）；序列全自動＋全程禁存；名字不露出（§4.1）；本 Phase 不做共用收尾（雨＋《雨還沒停》＋一點橙＝30-A）、不碰 Expose（29）、Trace 只讀不寫；台詞補白實作時定稿。

#### Phase 29 — Edited Expose 三判定（A / B / C）

> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8（Edited Expose 三分支表＋演出把關「C 收虛無、不收懲罰」）+ §4.1（名字不對稱）。**核心：兌現 27-C 的判定掛點——最終確認拍後接連續式判定序列（上傳演出 → 判定一次 → A/B/C 分支演出 → 靜止停），全程不離 `broadcast_station`；同時補正 Trace 經濟（echo_db 全殘響 `trace_on_collect: 1`），讓 29-C 臨界與 Phase 24 Branch B 成為真可達。零新場景、零系統擴充。**（規格 2026-07-03 討論定案，六題拍板：經濟補正＋臨界值 / 連續式 / 演出形態 A=CG / 三互斥旗標 / 統一靜止停 / 四子階段切法。）
> **判定規則（27 期拍板順序沿用，臨界值本次定案）**：(1) `trace >= 3`（`EXPOSE_TRACE_THRESHOLD`，**獨立常數**、不與 `TRACE_BETRAYAL_THRESHOLD` 共用，語意各自獨立、數值恰同）→ **C**，不看清洗；(2) 否則未清洗 → **A**；(3) 否則 → **B**。判定於序列開頭跑**一次**、結果傳遞後續演出；結果以三互斥 played 旗標鎖死，**30-A 讀旗標不重算**（避免臨界值日後調整改判舊檔）。

判定序列節拍（29-A 共用框架；27-C 最終確認拍「上傳開始」中性收尾後**不落地自由行動**，直接續接）：

1. **上傳演出（共用）**：鎖點起 `can_save_here = false`、世界移動 / TouchControls 凍結，序列全程與終態不恢復；上傳進度一至兩拍。
2. **判定分派（一次）**：依上列規則分 A/B/C。
3. **A（未清洗＋trace 未臨界）＝聚落焚毀**：警報鎖定拍（企業安全部門瞬間鎖定發射源）→ **情境圖 CG 拍**（聚落火光廢墟；photo_viewer 複用，28 同形式）→「你回到聚落時…」敘述終拍（回聚落路程不玩，27-A 文案跳過慣例）→ set `ending_expose_a_played` → 靜止停。
4. **B（已清洗＋trace 未臨界）＝匿名雜訊**：上傳完成、訊號散進雨夜、被當成匿名垃圾假新聞——「但你保住了所有人」收尾拍（安靜、無 CG、頁數少）→ set `ending_expose_b_played` → 靜止停。
5. **C（trace 臨界）＝當場被抹除**：進度 99% 攔截 → 高危冗餘判定 → 抹除拍，畫面收向黑 / 白噪（**收「你以為在選，系統毫不在意」的虛無，不收「玩家被懲罰」**，v2.3 §8 演出把關；螢幕效果走既有 fade / modulate 手法）→ set `ending_expose_c_played` → 靜止停於收黑畫面。
6. **終態**：三條統一輸入不恢復＝30-A 接手點。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 29-A | Trace 經濟補正 + 判定序列共用框架 + A 分支：echo_db 全殘響補齊 `trace_on_collect: 1`（現況 6 條、補 4 條，trace 理論上限 2→6；Branch B 連帶救活）＋`EXPOSE_TRACE_THRESHOLD := 3`；上傳演出 → 判定一次 → 分派；A＝警報拍＋CG 拍＋敘述終拍＋旗標 | headless：未清洗 + **trace 未臨界** → A（trace 臨界一律先判 C，見 29-C）；確認拍後無自由行動間隙；鎖點後禁存；全殘響採集 +1、重複採集不重加 |
| 29-B | B 分支演出：雜訊收尾拍（無 CG）＋旗標 | headless：已清洗 + trace 未臨界 → B |
| 29-C | C 分支演出：99% 攔截 → 抹除收黑／白噪＋旗標 | headless：trace 臨界（**未清洗 / 已清洗兩組合皆**）→ C（硬鎖優先序雙驗）|
| 29-D | 回歸 + 存讀檔：四組合判定矩陣、三 played 互斥 round-trip、孤兒檔救援、Phase 24 兩分支回歸（Branch B 首次真可達）、Phase 1~27 不退化 | headless PASS + GUI / 純觸控走查 |

依賴：27（`expose_upload_cleaned` / `expose_upload_done` + 判定掛點 + `broadcast_station`）、11（`trace` 只讀）、24（`TRACE_BETRAYAL_THRESHOLD` 分支回歸）、9（echo_db `trace_on_collect` 讀取路徑）、9-E（photo_viewer）。

canonical 旗標（皆 bool / `story_flags`，**零新存讀檔 schema**）：`ending_expose_a_played` / `ending_expose_b_played` / `ending_expose_c_played`（**互斥**，各分支演出終點寫入；30-A 共用收尾掛點）。只讀不寫：`ending_route_expose`、`expose_upload_cleaned`、`expose_upload_done`、`trace`（**Trace 不寫**——經濟補正＝echo_db data 改動，非本序列寫 trace）。

系統擴充：**無**（photo_viewer 9-E、MessageBox / begin_message、fade / modulate 螢幕效果全複用；經濟補正＝echo_db data-only）。

外部素材：情境圖 CG ×1（`assets/generated/maps/cg_settlement_burning/`＝聚落火光廢墟；g2d 可生、29-A 動工前補、**非 user blocker**）；B / C 無新素材。

前提硬規則：連續式——確認拍後無自由行動間隙、無「已上傳未判定」可玩中間態；判定一次、旗標鎖死、30-A 不重算；C 收虛無不收懲罰（無 Game Over 畫面）；三條全程不離廣播站、統一靜止停輸入不恢復；孤兒檔救援＝`expose_upload_done` 已設且三 played 皆 false 的檔讀入 → 強制回 `broadcast_station` 直開判定（trace / cleaned 已凍結，重演必同判）；`EXPOSE_TRACE_THRESHOLD` 獨立於 `TRACE_BETRAYAL_THRESHOLD` 與 28-A 壓垮拍門檻；本 Phase 不做共用收尾（30-A）；名字不露出（§4.1）；文案補白實作時定稿。

#### Phase 30 — 三結局收尾 + 全主線回歸

> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8 收尾段（三條結局共用收尾：雨、那首被清掉的老歌《雨還沒停》、灰色裡的一點橙；終句「雨還沒停，街上的燈已經熄了。」差別只在：這一刻，是誰在記得、誰被保住、誰被你連累）。**核心：兌現 28 / 29 五個靜止停接手點——played 旗標寫入拍完成後自動淡黑，接共用收尾三拍連續情境圖（首尾結構共用、差異集中）＋《雨還沒停》＋文字頁 → credits roll → 回標題；另補全主線脊椎回歸（30-B）與三結局存讀檔矩陣（30-C）。**（規格 2026-07-04 討論定案，逐題拍板：表演形式＝CG＋母題＋文字頁／演出規模＝三拍 11 圖／歌＝《雨還沒停》且賣沒賣照播／文字矩陣 10 變體封頂／credits→標題＋M1 結局記錄＋存檔槽不動／30-B 五終點全跑。**本 Phase 為最終結局，品質優先於成本＝拍板基準**。）
> **接手規則（拍板）**：五接手點（公寓 ×2＝R / P、廣播站 ×3＝Expose A / B / C）終拍完成後**自動**接手（無按鍵——終態輸入已鎖定，不重開輸入路由）；收尾**只讀 5 互斥 played 旗標＋`seven_peace_branch_d`**，不重算 29 判定、不加 trace / affinity 染色（結局收尾要收束不發散）。

三拍演出（30-A；共 11 張情境圖＝1＋5＋5）：

1. **接手淡黑**：雨聲 bed ＋《雨還沒停》（`echo_song_rain_doesnt_stop.mp3`）起。**殘響賣沒賣照播**——收尾播的是「世界裡還在下的雨、還在飄的歌」（21-E 環境母題同語意），非玩家收藏媒體；9-G「賣後媒體失效」只限筆記殘響頁播放鍵，互不干涉。
2. **拍 1 共用遠景**（1 圖，五結局同張）：雨夜街景、灰階、零星燈亮＝「世界照常運轉」；共用開頭文字，**「上行線復駛」固定帶白**（不做條件——沒看過的玩家是補交代，Protect not-B 玩家讀起來是呼應不是重複）。
3. **拍 2 橙特寫**（5 圖，per-結局）：「灰色裡的那一點橙」每結局來源不同——同構圖紀律下差異本身兌現主題：

   | 結局 | 橙的來源 |
   |---|---|
   | Reclaim | 公寓窗口一盞還亮著的燈——有人記得，但屋裡只剩你 |
   | Protect | 傘下兩個剪影旁的攤燈——晚還在，被保住的日常 |
   | Expose A | 地平線遠處未熄的火光——連累的橙 |
   | Expose B | 雨棚下老收音機的指示燈——一聲雜訊的餘溫 |
   | Expose C | 只有街燈，街上無人——沒有人記得你 |

   文字＝per-結局變體段（誰在記得 / 誰被保住 / 誰被你連累）＋**七號和平線帶白**（`seven_peace_branch_d`）插於此段：矩陣＝5 結局 × 和平線有無＝**10 變體**；其中 **Expose A × 和平線那格單獨寫**（七號留下了、然後聚落被燒——是「連累名單多一張臉」，不是「又一張臉保住」，不得套模板）。
4. **拍 3 終景**（5 圖，per-結局）：燈熄之後、拍 2 那點橙的**下場**＝你的結局；**終句共用一行「雨還沒停，街上的燈已經熄了。」五條線一字不改**，壓在五張不同的圖上讀出五種意思：

   | 結局 | 終景（燈熄之後） | 終句讀出來的意思 |
   |---|---|---|
   | Reclaim | 街燈全熄，整條街只剩公寓那扇窗還亮 | 誰在記得——你，一個人亮著 |
   | Protect | 街燈熄、攤燈收，傘下兩剪影走遠將沒入雨幕 | 誰被保住——晚還在、日常還在 |
   | Expose A | 燈熄了，遠處火光弱成餘燼、雨打在煙上 | 誰被你連累——燈熄了，火還沒熄 |
   | Expose B | 收音機指示燈滅，聚落方向完好安靜的黑 | 誰被保住——所有人，只是沒有人知道 |
   | Expose C | 全黑，只有雨，街上沒有人 | 誰在記得——沒有人 |

5. **收尾後流程**：淡黑 → **credits roll**（黑底白字上捲、雨聲墊底、歌收）→ **回標題畫面**。**M1 進度頁新增「結局」分類**（五格、未達成沿用 `????` 佔位慣例；Steam 成就未來地基），記錄寫 **meta 檔、不入存檔槽**（比照語言偏好與 SaveSystem 隔離；寫入時機＝拍 3 完成，中離 credits 仍計）。**存檔槽全數保留**——玩家結局前手動存檔可讀回走他線（30-C 矩陣成立的前提）。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 30-A | 共用收尾三拍（11 圖）＋《雨還沒停》＋10 文字變體＋credits→標題＋M1 結局記錄（meta 檔） | headless：五接手點分派、10 變體選中、meta 寫入 / 重啟保留 / 不入槽；GUI：演出就位 |
| 30-B | 全主線脊椎回歸：參數化 helper 從 `wake_bed` 按真實玩家路徑（不作弊灌旗標；18 戰鬥、24 追逐段以旗標模擬＝已接受捷徑，見方針 30-B）**五終點全跑**（R / P / EA / EB / EC，中段分岔各自湊齊：回執、清洗、trace）；spine 共 6 case（Protect 依 28-C 分岔拆 B / not-B 兩變體） | headless PASS；跨 Phase 接縫（旗標時序 / 轉場鏈 / 任務依賴）無斷點 |
| 30-C | 三結局 × 存讀檔矩陣 + GUI / 純觸控 / i18n 走查 | 全結局可達、存讀檔不退化 |

依賴：28（R / P 接手點＋`ending_reclaim/protect_played`）、29（Expose 接手點＋`ending_expose_a/b/c_played`）、21-E（《雨還沒停》母題語意）、20-B（`seven_peace_branch_d`）、M1（進度頁）、M2（i18n CSV）、10-A（雨聲 bed）、9-E（CG 顯示慣例）、6-D（title_screen）。

canonical：**零新 story_flags**；新 meta 持久化＝結局達成記錄（5 結局 bool，`user://` meta 檔，與 SaveSystem / 存檔槽完全隔離）。只讀不寫：5 互斥 played 旗標、`seven_peace_branch_d`。**Trace 不寫。**

系統擴充：**收尾 UI 元件 ×1**（全螢幕 CG＋文字疊層＋credits 捲動；掛 GameUI、不進 SceneRegistry——結局收尾專用，非通用系統）；其餘（fade / begin_message 推進慣例 / Ambient bus / title 轉場）全複用。

外部素材：情境圖 CG ×11（拍 1 遠景 ×1＋拍 2 橙特寫 ×5＋拍 3 終景 ×5；g2d 生成、**Art Bible 必讀**、構圖照上兩表方向實作時定稿；30-A 動工前補、非 user blocker）；歌與雨聲皆既有；credits 名單文案實作時盤點（引擎 / 字型 / 音樂 / 生成工具授權）。

前提硬規則：讀旗標不重算（29 拍板延續）；染色封頂＝played＋和平線 bool；終句一字不改五線共用；復駛帶白不做條件；credits 後回標題、存檔槽不動；歌照播與殘響賣否無關；名字不露出（§4.1）；文案 / CG 構圖 / credits 名單實作時定稿。

#### Phase 31 — P-00 隱藏遭遇（⬜ 暫緩，可獨立後補）

目的：v2.3 §9 主題型遭遇，非力量挑戰；不必進第一版。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 31-A | 廢屋檔案室候選場景 | headless：載入 |
| 31-B | P-00 機械鏡像三選（Reset / 留[Trace 大升] / 還·公開[可能暴露活人]）| headless：三選 + trace 連動 |
| 31-C | 回歸 | PASS |

依賴：11；其餘獨立。標 ⬜ 待規劃，可延後。

#### Phase 32 — Act 3→4 決意拍（travel_datacenter 敘事補強）

> 敘事事實來源 `subdocs/主線/雨還沒停v2.3` §7 五幕表（3→4 過場）＋ §7.2 三分支後果。**核心：資料中心 travel 不是 gate bug，是缺一個「決意拍子」——玩家（尤其和平線 D）在 travel 選項出現時不知道「為什麼是現在去」。解法全部收在 `travel_datacenter` 對話樹＋story_flags＋i18n CSV 之內：menu 文案線索化＋三線 prelude＋晚的確認 choice＋工作筆記。不動 travel gate、不動場景 flow、不回聚落加路標（七號和平對話一字不動）、零新系統。**（規格 2026-07-11 圓桌討論定案：四席全票共識。）

驗收意圖（what must be true）：

1. **gate 不變**：`passed_nightclub_security AND（seven_peace_branch_d OR seven_stopped_full OR seven_stopped_partial）`；未通 Act 3 仍 locked，locked 文案不動。
2. **玩家點「前往」之前就知道這不是普通傳送點**：menu 文案把「夜總會後場線索＋舊工單＋舊識別證 → 資料中心」的因果接起來；和平線 menu 變體多一句「有些事只能在裡面找到答案」式引導暗示；「前往 AI 資料中心」筆記在 menu 出現那一刻寫入（UI 層先給主線尾聲訊號、取消不回滾也不重複寫）。
3. **決意落在確認那一刻，不是確認之後被追認**：prelude 依七號三線分流（D＝「七號沒背叛、沒人逼你了」的安靜不安；A＝裂痕已開的止損急迫；B＝「你已見過被找到的後果」的代價感）→ 晚的反問 choice（和平線＝「現在沒人逼你了。你還要進去？」）→ 玩家主動選「進去」才 travel；「先離開」回 end、不 travel、不留半套狀態。
4. **決意拍只演一次**：確認 travel 後設 committed 旗標，之後重訪走短版 menu 直達 travel，prelude 與晚的反問不再重複（保住和平線「稀有恩典、安靜、不引導」基調）。未確認前取消再進 = 完整重播（拍子未消費）。
5. **與 26-A 情緒區隔**：出發前晚的反問＝「你確定要去」；`datacenter_entrance` 到場拉扯（26-A）＝「你確定要動手」；兩拍情緒不同、不重複，避免晚連續三拍都在阻止玩家。

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 32-A | `travel_datacenter` 對話樹改造（`menu → branch_selector → prelude_d/a/b → confirm_by_wan → travel`）＋ `add_note` 掛 menu 節點＋committed 短版分流＋三語 CSV | headless：三終端旗標各進對應 prelude；「先離開」不 travel；筆記進 menu 即寫、不重複；「進去」後仍到 `datacenter_entrance:from_nightclub` |
| 32-B | 回歸＋i18n coverage＋GUI / 純觸控走查 | PASS：Phase 25+ 資料中心門禁 / 26-A 拉扯 / 27 路由 / 30 收尾不退化；新 key 三語 lint 過 |

依賴：25-A（`travel_datacenter` 樹與 gate）、20-B（`seven_peace_branch_d`）、24-B（`seven_stopped_full / partial`）、5-A（DialogueRunner effect ops）、M2（i18n CSV）。對照：26-A（到場拉扯＝情緒區隔對象，不動）。

canonical：新 story flag `datacenter_travel_committed`（隨既有 story_flags 序列化，**零新存讀檔 schema**）；新筆記 `note_datacenter_route`（category「工作」）。**Trace / affinity 不寫；三個七號終端旗標與 `passed_nightclub_security` 只讀。**

---

## Phase M1 — 進度頁（Meta / 非敘事 QoL，時序卡在 Phase 19 之前）

>（已完成，全文歸檔於 `subdocs/歸檔/遊戲規格書_歸檔.md`，同標題可 grep。）

## Phase M2 — 多國語系 i18n（Meta / 非敘事 QoL，與 M1 同類）

> **編號說明**：M2 同 M1 為 meta / QoL 系統功能，非故事節拍。out-of-band 編號，**不插 Phase 19、不順延 19~31**（呼應 4-G / M1「寧可不動編號」教訓）。實作契約見 `開發設計方針.md > Phase M2`；操作清單見 `測試指南.md > Phase M2`。
>
> **凍結決策（2026-06-24 user 拍板）**：
> 1. **三語言**：繁中 `zh_TW`（**預設 + fallback**）/ 簡中 `zh_CN` / 英文 `en`。
> 2. **機制**：Godot 原生 `tr()` + CSV 翻譯表（`TranslationServer`），非自訂系統。
> 3. **翻譯來源**：簡中與英文均 **LLM 獨立翻譯**（簡中**非** OpenCC 機械轉）；繁中＝現有文字直接當 `zh_TW` 源欄。
> 4. **相容 iOS / Android**：字型 runtime 切換 + 動態載入（只駐一個 CJK 字型）、OS locale 首啟偵測。
> 5. **i18n 為橫切式改動**（碰到每個有文字的檔），但對玩家行為仍**純加法**：不改任何 Phase 1~19 / M1 的邏輯、旗標語意、存檔資料；只把「顯示用文字」改為查表。

### 目的

系統設定頁新增「語言」選項，玩家可在繁中 / 簡中 / 英文間切換，全遊戲文字（UI、場景提示、筆記、訊息、物品、對話、殘響、商店、任務）依所選語言顯示。

### 驗收意圖（what must be true）

**設定頁與切換**
- 標題畫面與遊戲中暫停選單**皆可進入設定頁**；設定頁有「語言」三選一（繁中 / 簡中 / 英文）。
- 切換語言**即時生效**：當前畫面與其後開啟的所有 UI 都以新語言顯示（不需重啟）。
- 語言選擇**持久化**：關閉遊戲再開，沿用上次選擇；存於獨立設定檔，**不綁存檔槽**（語言是全域偏好，與 New Game / Load 無關）。
- 首次啟動（無設定檔）時依**裝置語言**自動選預設（`zh-*` 繁→`zh_TW`、`zh-Hans/zh-CN/zh-SG`→`zh_CN`、其餘→`en`）；之後以使用者設定為準。

**文字覆蓋**
- 下列全部隨語言切換：UI 按鈕 / 標籤 / 操作提示、場景互動 `prompt_text`、筆記（標題 / 內文 / 分類名）、`STORY_MESSAGES`、物品名 / 描述、9 棵對話樹（speaker / 內文 / 選項）、殘響標題 / 內容、商店品名、任務工作筆記。
- 任一語言下**無漏字 / 無豆腐框（缺字方框）**；繁中用繁體字形、簡中用簡體字形（字型隨語言切換）。
- 含數值的字串（如 credits 數、頁碼 `第 N / M 頁`）切換語言後**佔位正確、語序正確**（英文可能語序不同）。

**Fallback 行為**
- 某 key 在當前語言**缺翻譯**時，退回顯示**繁中**（`zh_TW` 為 fallback locale），不顯示空白或 raw key。
- 某 key **三語全缺**（漏抽）時顯示該 key 字串本身（作為開發期可見的漏譯訊號），不崩潰。

**前提硬規則**
- **純加法（玩家行為層）**：不改既有任何 Phase 1~19 / M1 的旗標語意、UI 流程、存檔既有欄位語意、對話分支 / effect / condition 判定；只把顯示文字改為 `tr()` 查表。
- 對話的 **goto / effect / condition 仍以既有 id / flag 運作**，與翻譯無關（翻譯只動顯示字串，不動邏輯 key）。
- 敘事禁字規則跨三語延續：**任何語言任何文字不得出現「林霏」**（含英文音譯）；其餘各 Phase 既有禁字（七號不明說妹妹等）於三語翻譯一併遵守。
- 英文人名採**一致音譯**（M2-D 落地時鎖定對照表，全遊戲統一）；簡中保留中文名（簡體字形）。

### 範圍邊界 / 已知

- **舊存檔不受影響**：語言偏好不入存檔槽；任何舊存檔讀入後語言以設定檔為準。
- **本 Phase 不做**：日韓 / 其他語言、配音、字型 per-字重切換、文字方向（三語皆 LTR，無 RTL 需求）、CMS / 線上翻譯流程。
- **包體成本（已接受）**：簡中需多帶一個 `NotoSansSC` 字型（約 +10–16MB）；無更省的無損方案（Noto Sans CJK 超集反而 100MB+），列入已接受邊界。
- 字型缺字 fallback chain 不過度工程：Noto 系列涵蓋率高，M2-E 真機掃一遍確認無豆腐即可。
