# After-The-Model 遊戲規格書

> 本文件記錄全遊戲通用系統的規格與驗收條件。場景專屬規格放在 `subdocs/地點/`，由主規格書 link 過去。
>
> 每個系統段落含「驗收」清單：可逐條 check 的行為條件，用於手動驗收與已知問題追蹤。

## 目前狀態

- 引擎：Godot 4.6.3 / GDScript。
- 視角：純 2D 側捲，主角只左右移動。
- 已有可跑原型：`apartment_room.tscn`。
- 已有基礎系統：
  - 玩家左右移動與 walk 動畫。
  - 互動區 `Area2D`。
  - 互動提示框跟隨主角。
  - 共用訊息框，支援逐字顯示。
  - 共用容器 UI，依容器資料切換標題、格數、外觀色。
  - 多個互動區重疊時，選離玩家最近的互動物。
  - `GameState` / `UIMode` autoload。
  - 背包 UI（`I` 開關、5x3、Credits、item icon、鍵盤 focus）。
  - 筆記 UI（`J` 開關、三分類 tab、列表 + 全文）。
  - 容器雙欄 UI（櫥櫃 5x6 / 冰箱 5x2、左背包右容器、跨欄 focus、E 移動 1 個、stack 自動合併）。
  - FloatingToast（容量不足顯示「放不下了。」，1.5s + 0.5s fade 自毀）。
  - `move_up` / `move_down` InputMap action（WASD 上下方向綁定）。
  - UI 開啟時凍結主角移動，並以半透明 overlay 保留世界可見。

## 輸入設計

正式遊戲行為走 Godot InputMap，不在程式中直接監聽實體按鍵。例外：UI focus 元件可在已啟用 UI mode 時補實體 WASD fallback，作為方向鍵導覽鏡像；不得觸發世界互動或資料變更。

| Action | 鍵盤 | 用途 |
|---|---|---|
| `move_left` | A / Left | 世界移動；UI 開啟時改為焦點移動 |
| `move_right` | D / Right | 世界移動；UI 開啟時改為焦點移動 |
| `interact_primary` | E | 世界互動；UI 主要動作 |
| `interact_secondary` | R | UI 查看 / 取消等次要動作 |
| `interact_tertiary` | T | UI 丟棄等第三動作 |
| `open_inventory` | I | 開 / 關背包，已完成 Phase 1-B |
| `open_notebook` | J | 開 / 關筆記，已完成 Phase 1-C |
| `ui_cancel` | Esc | 關閉目前 UI，背包 / 訊息 / 容器已接入 Phase 1-B |

### 驗收

- 所有會改變遊戲狀態的玩家輸入均經 `InputMap`，程式不得直接用 `KEY_E` / `KEY_R` / `KEY_T` 等實體鍵碼觸發互動或資料變更。
- 任一 action 重新綁定後，所有相依行為（移動、互動、UI 焦點）均跟隨更新。

## UI Mode

單一 UI 模式狀態，避免角色移動與 UI 操作互相衝突。

```text
Autoload name: UIMode
File: res://scripts/autoload/ui_mode.gd
Scope: 跨場景共用
```

模式列舉：

```text
NONE
INVENTORY
CONTAINER
NOTEBOOK
MESSAGE   # overlay modal：疊在 caller UI 上，記住並還原 caller mode（見下）
CONFIRM   # Phase 1-E 啟用，由 ConfirmDialog 使用；同為 overlay modal
```

規則：

- `NONE`：玩家可移動，E 與附近物件互動。
- 任何大型 UI 開啟時，玩家不能移動。
- UI 開啟時，WASD / 方向鍵改為移動 UI 焦點。
- `Esc` 關閉目前 UI，狀態回到 `NONE`。
- 同時只能有一個大型 UI（INVENTORY / CONTAINER / NOTEBOOK）開啟；要切到另一個必須先關閉當前 UI。（`MESSAGE` / `CONFIRM` 為 overlay modal，可疊在大型 UI 之上，不受此限——見下「overlay modal 的 caller 還原」。）
- 背包 / 筆記 / 容器 UI 開啟時，**世界仍可見**（半透明 overlay 蓋在世界之上，不全黑遮蓋），方便邊看世界邊操作 UI。

最小 API：

```gdscript
signal mode_changed(new_mode: int)

enum Mode { NONE, INVENTORY, CONTAINER, NOTEBOOK, MESSAGE, CONFIRM }

func get_mode() -> int
func set_mode(mode: int) -> void
func is_world_input_blocked() -> bool   # 當前 mode != NONE 時為 true
```

### 大型 UI 之間互切

`I` / `J` 等大型 UI 鍵在當前 mode != NONE 時的行為：

- 當前 mode 與按鍵對應的 UI **相同** → 關閉，回到 `NONE`。
- 當前 mode 與按鍵對應的 UI **不同** → **先關閉當前 UI、再開新的**（一鍵切換，不要求玩家先 Esc）。

例：
- INVENTORY 中按 `J` → 背包關、筆記開、mode = NOTEBOOK。
- NOTEBOOK 中按 `I` → 筆記關、背包開、mode = INVENTORY。
- CONTAINER 中按 `I` → 容器關、背包開、mode = INVENTORY（雙欄容器同規則）。
- `MESSAGE` / `CONFIRM` overlay 期間，大型 UI 鍵（`I` / `J` 等）不反應（overlay 為暫時 modal，見下「overlay modal 的 caller 還原」）。

實作上 `UIMode` 保持純狀態管理，只更新 mode 並 emit `mode_changed(new_mode)`。場景 controller（目前為 `apartment_room.gd`）負責監聽 signal，關閉舊 UI、開啟新 UI、同步 overlay / prompt / focus。

### overlay modal 的 caller 還原（MESSAGE / CONFIRM）

`MESSAGE`（MessageBox）與 `CONFIRM`（ConfirmDialog）**不是**大型 UI，而是**疊在當前畫面上的 overlay modal**；行為與大型 UI 互切規則不同：

- 進入時 UIMode 記住 caller mode（如 `NONE` / `INVENTORY` / `CONTAINER`），**不關閉 caller UI**（世界 / 背包 / 雙欄維持顯示但停止接鍵）。
- overlay 顯示期間捕捉鍵盤、暫停世界；caller UI 凍結但可見。
- 離開時還原 caller mode，焦點回 caller UI 原位置（如雙欄回原格）。
- overlay 期間 `I` / `J` 等大型 UI 鍵不反應；`MESSAGE` 只接推進 / `Esc`，`CONFIRM` 只接 `E` / `Esc`。
- 這讓「雙欄內放入方塊 → 直接疊 MessageBox 語音 → 關閉後回雙欄」成立（2-E）；**取代** Phase 1-D「雙欄不可有 MessageBox」舊限制——改為「雙欄上可疊 overlay MessageBox，底層雙欄凍結」。FloatingToast 仍用於不需 modal 的即時提示（如「放不下了。」）。

```gdscript
# UIMode 內部（MESSAGE 與 CONFIRM 共用 caller-restore）
var _caller_mode := Mode.NONE

func enter_overlay(overlay_mode: int) -> void:  # overlay_mode ∈ {MESSAGE, CONFIRM}
    _caller_mode = current_mode
    current_mode = overlay_mode
    mode_changed.emit(overlay_mode)

func exit_overlay() -> void:
    current_mode = _caller_mode
    _caller_mode = Mode.NONE
    mode_changed.emit(current_mode)
```

> MVP 只支援**單層 overlay**（不疊 MESSAGE on CONFIRM）；多層 overlay 待實際需要再做。

### 驗收

- 任何 UI 開啟時，主角不能左右移動、不會觸發世界互動。
- 大型 UI 關閉後恢復 `NONE`，主角可移動且 Prompt 重新出現；overlay modal（MESSAGE / CONFIRM）關閉後則**還原 caller mode**（caller=NONE 才回世界、可移動）。
- 同時只能有一個大型 UI 開啟。
- 背包 / 筆記 / 容器 UI 開啟時，背景仍透出世界畫面（透明度由半透明 overlay 控制）。
- `mode_changed` signal 在每次切換時 emit。
- 大型 UI 互切時舊 UI 確實隱藏，無雙重 panel 疊加。
- 進 / 離 `MESSAGE` / `CONFIRM` overlay 時 caller UI 不關閉、焦點正確還原（caller=NONE / INVENTORY / CONTAINER 各情況皆驗）。

## 共用 UI 元件

跨 UI 重複使用的元件，集中規範以避免漂移。實作上應抽成獨立 scene / script。

### BagGrid

玩家背包格子的視覺與資料 component。

- 5x3 = 15 slot，sizing / skin / focus 視覺依「背包系統」段。
- 資料源：`GameState.get_inventory()`，listens `inventory_changed` signal。
- 同一個 `BagGrid` 同時用於：
  - 1-B 背包獨立 UI（INVENTORY mode）
  - 1-D 雙欄左欄（CONTAINER mode）
- 接受外部設定當前 focused index 與「是否捕捉鍵盤輸入」flag（決定誰主導鍵盤）。
- 視覺絕對一致；改背包視覺只能改這一處。

### FloatingToast

短暫提示，不切 UI Mode。

```text
位置        = 視觸發 panel 上方 8px，水平置中於該 panel
字          = 18px 米白 (0.94, 0.92, 0.84, 1.0)
背景        = 半透明黑 (0, 0, 0, 0.7) + 8px padding
顯示時間    = 1.5 秒
淡出時間    = 0.5 秒
重複觸發    = 取代既有 toast、計時器重置；不疊加
```

API：

```gdscript
FloatingToast.show(text: String, anchor_node: Control = null)
```

用途清單：
- 1-D「放不下了。」
- 1-E「無法丟棄這個物品」
- 1-E「這類裝備已經滿了，先卸下身上的再裝備新的。」
- Phase 2+ 解謎條件未滿足等臨時提示

### PanelFooterHint

panel 底部一行操作鍵提示。

```text
字          = 14px 暗灰 (0.55, 0.55, 0.55, 1.0)
位置        = panel 底部 padding 內，左對齊
格式        = "<KEY>: <action>    <KEY>: <action>    ..."
```

API：

```gdscript
PanelFooterHint.set_hints(panel: Control, hints: Array)  # [["E", "移動"], ["Esc", "關閉"]]
```

各 UI 對應 hint（依 Phase 進度）：

| UI | Phase 1-B | Phase 1-C | Phase 1-D | Phase 1-E |
|---|---|---|---|---|
| 背包（單獨） | `Esc / I: 關閉` | — | — | `E: 依物品（裝備/卸下·使用·無）    R: 查看    T: 丟棄    Esc / I: 關閉` |
| 筆記 | — | `Esc / J: 關閉` | — | — |
| 雙欄（兩欄各一，同字串） | — | — | `E: 移動    Esc: 關閉` | `E: 移動    R: 查看    T: 丟棄    Esc: 關閉` |
| ItemDetailModal | — | — | — | `R / Esc: 關閉` |
| ConfirmDialog | — | — | — | `E: 確定    Esc: 取消` |

> 背包 footer 的 **E 段依選中物品變動**（2-D 連帶修正 Phase 1-E，**取代**「consumable 按 E 沉默」舊規）：
> - 可裝備物 → `E: 裝備`（未裝備）/ `E: 卸下`（已裝備）。
> - `category == consumable` 或可反覆使用物 → `E: 使用`。
> - 無對應動作物（如 `old_work_badge`）→ **不顯示 E 段**。
> - `R: 查看` / `T: 丟棄` / `Esc·I: 關閉` 不受影響。`E: 使用` 僅在單獨背包（`I`）出現；雙欄左欄 E 仍為「移動」。
>
> `E: 使用` dispatch：帶 `consume_grants_note` 的物 → MessageBox + `add_knowledge` + 移除該 instance（消耗）；無定義效果的 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」、不消耗；可反覆使用物執行效果、不消耗（預見類型，暫無實例）。Phase 3+ 加血量系統後再補飢餓類效果。
>
> **筆記 footer（2-F，待實作）**：上表 Phase 1-C 欄的 `Esc / J: 關閉` 為早期占位；2-F 起筆記 footer 主打 WASD 導航——`A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`。方向鍵仍可用但不列於 footer。

### ItemDetailModal

R 鍵查看物品 description 用的 modal。**不切 UIMode**（INVENTORY / CONTAINER 不變），開啟時 capture 鍵盤，關閉後焦點回原 grid 格。

**與 MessageBox 疊層**：ItemDetailModal 開啟中觸發 MESSAGE overlay（如 R 查看 `consume_grants_note` 物 → 跳訊息）時，MessageBox 疊在 detail modal 上方；此時 detail modal 凍結、不吃鍵盤輸入（capture 暫交給 MessageBox）。MessageBox 關閉後 detail modal 刷新顯示（消耗物會連 modal 一起關並回 grid；變身物如 `decoder_cube` 則刷新當前 instance），並還原鍵盤 capture。

```text
panel 寬     = 320
panel 高     = 400
位置         = 蓋在背包 panel 或雙欄左欄上方中央
skin         = inventory
```

內部排版（垂直）：

```text
[ icon 128x128，置中 padding 16 ]
[ name 22px 米白，置中 ]
[ description 16px，換行可讀，左右 padding 16 ]
[ flex spacer ]
[ category tag 14px 暗灰，置中於 panel 底 padding 內 ]
```

`category tag` 規則：

| `category` | tag 文字 |
|---|---|
| `key_item` | `劇情物品（不可丟棄）` |
| `equipment` | `裝備（{equipment_slot 中文}）`，例 `裝備（手持）` |
| `consumable` | `消耗品` |
| `misc` | `物品` |

`equipment_slot` 中文映射：`clothing → 衣服`、`hand → 手持`、`accessory → 其他`。

API：

```gdscript
ItemDetailModal.show(instance_id: String, anchor_node: Control = null) -> void
ItemDetailModal.close() -> void
```

操作：

- `R` 或 `Esc` → 關閉並回到 grid focus。
- 其他鍵：modal 開啟期間全部 block（不讓背包 / 雙欄處理）。

### ConfirmDialog

通用確認 dialog。1-E 用於丟棄確認。切 `UIMode.CONFIRM`，開啟期間 capture 所有鍵盤。

```text
panel 寬     = 360
panel 高     = 160
位置         = viewport 置中
skin         = inventory
```

內部排版：

```text
[ message 18px 米白，置中 padding 16 ]
[ flex spacer ]
[ footer hint: "E: 確定    Esc: 取消" ]
```

API：

```gdscript
ConfirmDialog.show(message: String, on_confirm: Callable, on_cancel: Callable = Callable()) -> void
```

操作：

- `E` → 呼叫 `on_confirm` → 關閉 dialog → UIMode 回先前 mode（INVENTORY / CONTAINER）。
- `Esc` → 呼叫 `on_cancel`（如有）→ 關閉 dialog → UIMode 回先前 mode。
- 其他鍵：block。

> ConfirmDialog 切 `CONFIRM` mode；關閉時必須回到 dialog 開啟**之前**的 mode（INVENTORY 或 CONTAINER），而非預設 NONE。UIMode 內部需記住 caller mode。

## 互動系統

每個可互動物件使用 `Area2D`，掛 `interactable_area.gd`。

基本欄位：

```gdscript
interaction_id: String
prompt_text: String
message_id: String
```

目前做法：

- `Area2D` 只負責偵測玩家進出。
- 場景 controller 負責：
  - 選目前互動物。
  - 顯示 prompt。
  - 按 E 後執行對應行為。
- 多個互動區重疊時，選離玩家最近的 `CollisionShape2D`。

### 驗收

- 玩家站在互動區內時，Prompt 顯示對應 `prompt_text`。
- 玩家離開互動區後 Prompt 立刻消失。
- 同時站在多個互動區內時，只顯示最近一個的 Prompt，按 E 也只觸發該物件。

## Prompt

互動提示使用共用 `PromptPanel`。

目前已完成：

- 有 UI 底圖。
- 文字由互動物或目前 UI 狀態決定。
- 位置跟隨主角，水平大致置中在主角上方。

### 驗收

- 主角移動時 Prompt 跟著移動，不會落後或抖動。
- Prompt 不擋住主角頭頂的關鍵互動範圍。
- 任何 UI 開啟時 Prompt 隱藏，UI 關閉後恢復。

## MessageBox

共用訊息框用於短文本敘事與無法執行某動作時的回饋。

目前已完成：

- 訊息框依完整文字自動調整大小。
- 文字逐字出現，速度為每秒 3 字。
- 含標點符號一起逐步顯示。

後續規則：

- `MESSAGE` mode 中，`E` 或 `Esc` 關閉訊息。

### 驗收

- 訊息框尺寸依完整文字長度自動調整，不出現截字或大量留白。
- 文字以每秒 3 字逐字顯示，含標點。
- `MESSAGE` mode 中主角不能移動，按 E 或 Esc 收掉訊息後**還原 caller mode**（overlay 機制：caller=NONE → 回世界、caller=CONTAINER → 回雙欄原格、caller=INVENTORY → 回背包原格）。詳見「overlay modal 的 caller 還原」。

## 容器系統

容器使用資料驅動。

目前 prototype 使用單一 `ContainerPanel`：

```gdscript
CONTAINERS = {
  "cabinet_storage": {
    "title": "櫥櫃",
    "cols": 5,
    "rows": 3,
    "skin": "cabinet",
    "panel_position": Vector2(...)
  },
  "fridge_storage": {
    "title": "冰箱",
    "cols": 5,
    "rows": 2,
    "skin": "fridge",
    "panel_position": Vector2(...)
  }
}
```

已完成：

- 不同容器可設定 title、cols、rows、skin、panel_position。
- 容器 panel 會依格數自動調整大小。
- `cabinet` skin：暗木 / 暗褐。
- `fridge` skin：米白 / 淡藍 / 冷灰。

規格目標：

- 背包：5x3。
- 櫥櫃：5x6，作為玩家倉庫。
- 冰箱：5x2。
- 目前程式中的櫥櫃仍是 5x3，待調整到 5x6。

### 資料層接 GameState（Phase 1-D）

雙欄打開時容器 panel **必須**讀 `GameState.get_container(container_id)`，不再用空 Button grid。

- 場景在 `_ready()` 呼叫 `GameState.configure_container("cabinet_storage", 30)` / `configure_container("fridge_storage", 10)` 初始化。
- 預載資料寫法見 `subdocs/地點/主角公寓.md > 預載資料`。
- panel 顯示時：foreach slot → 若有對應 inventory entry 顯示 icon + 數量；空格顯示 slot skin 底。
- listen `GameState.container_changed(container_id)` signal 即時刷新。
- 移動物品請呼叫 `GameState.move_one_item_to(...)`，禁止 panel 自行操作 GameState 內部資料。

### 驗收

- 不同容器顯示的 title、格數、skin 完全由 `CONTAINERS` dict 驅動，程式不得 hardcode。
- 容器 panel 寬高隨 cols/rows 自動調整。
- 櫥櫃顯示 5x6，冰箱顯示 5x2，背包顯示 5x3。
- 容器 UI 開啟 / 關閉時遵守 UI Mode 規則（主角不能移動、Prompt 隱藏）。
- 容器 panel 內容由 `GameState.get_container()` 提供；空格顯示 skin 底色，有物品顯示 icon + 數量。
- 容器內容變動時透過 `container_changed` signal 即時刷新，無需重開 panel。

## 背包系統

已完成 Phase 1-B 基礎 UI 與人工驗收。物品操作（E 使用 / 裝備、R 查看、T 丟棄）已於 Phase 1-E 完成。

背包是玩家自身容器：

- `I` 開 / 關。
- 單獨打開時只顯示背包，位置置中於 viewport。
- 格數：5x3。
- 顯示 `Credits`，不佔格。
- 全鍵盤 / 手把友善，不依賴滑鼠。
- 開啟時走半透明 overlay，世界仍可見（依 UI Mode 規則）。

背包操作：

```text
方向鍵 / WASD = 選格
E = 使用 / 裝備 / 卸下（Phase 1-E）
R = 查看（Phase 1-E）
T = 丟棄（Phase 1-E）
Esc / I = 關閉
```

Phase 1-B 期間，背包內按 E / R / T 必須被 UI 吃掉但不產生任何效果、log 或提示。

### Panel 尺寸與排版

沿用容器系統常數：

```text
SLOT_SIZE       = 64 x 64
SLOT_GAP        = 4
PANEL_PADDING   = (16, 16)
TITLE_HEIGHT    = 24
```

5x3 計算：

```text
內容寬 = 5 * 64 + 4 * 4 = 336
內容高 = 3 * 64 + 2 * 4 = 200
panel 寬 = 336 + 32 = 368
panel 高 = 200 + 32 + 24 = 256
```

於 1280×800 viewport 置中：左上座標 ≈ `(456, 272)`。

### 標題列（含 Credits）

```text
[ 背包                                    Credits: 0 ]
```

- 左對齊「背包」字樣（18px，inventory skin 標題字色）。
- 右對齊「Credits: <值>」（18px，同色），值來自 `GameState.get_credits()`，聽 `credits_changed` signal 即時更新。
- Credits 內聯於標題列，**不另佔行**。

### Inventory Skin（區別於 cabinet / fridge）

```text
skin id        = "inventory"
panel 底       = (0.12, 0.14, 0.18, 0.88)   # 深藍灰
panel 邊框     = (0.78, 0.42, 0.20, 1.0)    # 主角橙紅
slot 底        = (0.08, 0.10, 0.12, 0.80)
slot 邊框      = (0.30, 0.32, 0.36, 1.0)
標題字色       = (0.94, 0.92, 0.84, 1.0)   # 米白
```

實作建議在 `_apply_container_panel_style` 加 `inventory` 分支（或重構為 dict 對應）。

### 格內顯示

- **Icon**（必有，path 由 `ITEMS_DB[item_id].icon_path` 提供）。
- **數量**：`quantity > 1` 時顯示於右下角，14px，白底深字（或加字陰影確保可讀）。
- **已裝備標記**：右上角 12×12 像素 `E` 字符號，白底深字。1-B 階段預載 item 未裝備，此標記預留位置；1-E 實裝裝備系統後啟用。
- **缺 icon**：顯示紅底白色 `?` placeholder（48×48 居中），方便 dev 期發現未補 icon。

### Focus 視覺（鍵盤導航）

選中格用 2px 亮橙紅外框（同 panel 邊框色 `(0.78, 0.42, 0.20, 1.0)`），蓋在 slot 邊框上，視覺優先順序高於既有 hover。

```text
未選中 / 滑鼠 hover    = 走既有 slot 邊框
鍵盤 focus              = 2px 橙紅外框（dedicated focus_style）
```

Focus 邊界**不繞回**：上 / 下 / 左 / 右抵邊後不移動。

### 半透明 Overlay

開啟背包 / 筆記 / 容器 UI 時，世界上覆蓋一層半透明黑色：

```text
overlay color = (0, 0, 0, 0.3)
```

overlay 在 panel 之下、世界之上。背包 / 筆記 / 容器三者共用同一份 overlay 規格。

### 驗收

- [x] `I` 開啟時 panel 置中於 viewport，尺寸 368×256，左上 ≈ (456, 272)。
- [x] 標題列左「背包」、右「Credits: <值>」；Credits 數字顯示來自 `GameState.get_credits()`。
- [x] 每格顯示對應 icon；可堆疊品於右下角顯示數量；已裝備品於右上角顯示 `E` 標記（1-B 預留位置）。
- [x] 缺 icon 時顯示紅底 `?` placeholder。
- [x] 全鍵盤可完成目前 Phase 1-B 操作，不依賴滑鼠；focus 可見、邊界不繞回。
- [x] 開啟期間主角不可移動，Prompt 隱藏；`Esc` 或 `I` 關閉。
- [x] 開啟期間世界仍透出（黑色 alpha 0.3 overlay）。
- [x] E / R / T 在背包內無反應（無錯誤、無訊息），行為留 Phase 1-E。

## 容器雙欄操作

已完成 Phase 1-D 基礎 UI 與人工驗收。

打開櫥櫃 / 冰箱等外部容器時，UI 改為雙欄：

```text
左：玩家背包
右：外部容器
```

操作：

```text
方向鍵 / WASD = 選格 / 跨欄（抵欄邊自動跳對欄）
E             = 移動 1 個到對方
R             = 查看（Phase 1-E 啟用）
T             = 丟棄（Phase 1-E 啟用）
Esc           = 關閉
```

> ⚠️ 雙欄打開後 **E 鍵不再用於關閉容器**（移除原本單欄 prototype 的「再按 E 關閉」行為）；E 一律是「移動」。`Esc` 為唯一關閉鍵。

### Panel 架構

- 左欄背包必須與 1-B 背包 UI **同一個 `BagGrid` component**（同視覺、同資料源）。
- 右欄為 `ExternalGrid` 顯示當前外部容器（cabinet_storage 或 fridge_storage）。
- 兩欄各自獨立 panel，並排呈現；左右欄 panel 套 inventory skin。

### 雙欄整體 Layout

```text
左欄 panel        = 368 × 256        # 同 1-B 背包
右欄 panel(cabinet)= 368 × ~388      # 5x6 + title
右欄 panel(fridge) = 368 × ~152      # 5x2 + title
panel 間距        = 16
```

**對齊規則**：兩 panel **垂直置中對齊**（panel 中心同高），不對齊頂部。

於 1280×800 viewport 整組置中：

```text
總寬 = 368 + 16 + 368 = 752
整組左上 x ≈ (1280 - 752) / 2 = 264
左欄左上 = (264, viewport 垂直置中對應 y)
右欄左上 = (264 + 368 + 16, viewport 垂直置中對應 y)
```

### 跨欄 input 規則

無 Tab 鍵（手把不便）。靠方向鍵 / WASD 抵欄邊自動跳對欄：

- 左欄最右行按 → → 跳右欄
- 右欄最左行按 ← → 跳左欄
- 跨欄時**保持當前 row index**；對方 row 不足時落對方最後一 row
  - 例：背包 focus 在 row 2（最下），→ 跳 cabinet row 2 或 fridge row 1（fridge 只有 row 0-1）
- 上 / 下抵欄邊不繞回、不跨欄

### 自動整理時機

每次 `add_item` / `remove_item` / `move_one_item_to` 後 GameState **立刻**自動整理：同 id 合併、未滿 stack 補位、空格集中於尾、stack 排序。

> 這修正本規格之前版本「關閉時整理」的舊敘述。整理永遠在 data layer 即時發生，UI 只讀。

### Focus 行為（移動後）

物品移走後，focus **保持原 row/col index**，不跟著物件、不跳到對欄。

理由：玩家連續從容器搬到背包時，focus 不動可以連按 E 快速搬完。

該 index 整理後可能變成空格，玩家以方向鍵繼續導航即可。

### 預設 focus / 重置策略

- 雙欄打開時預設 focus 在**右欄第一格 (row 0, col 0)**。
- 每次重新打開都重置；不記憶上次 focus 位置。

### 「放不下了。」訊息 — FloatingToast

一般失敗 / 操作提示（如放不下）在雙欄不使用 MessageBox，改用 **FloatingToast** 非模態元件（共用，見「共用 UI 元件」段）。敘事性訊息則可用 `MESSAGE` overlay 疊在雙欄上（caller=CONTAINER，關閉後還原雙欄；見「overlay modal 的 caller 還原」）—— overlay 不再與 CONTAINER mode 衝突。

文字：`放不下了。`

### Panel Footer Hint

兩欄 panel 底部各自顯示 footer hint，列出當下可用操作鍵。

```text
Phase 1-D 階段：  "E: 移動    Esc: 關閉"
Phase 1-E 完成：  "E: 移動    R: 查看    T: 丟棄    Esc: 關閉"
```

字 14px，暗灰 `(0.55, 0.55, 0.55, 1.0)`，左對齊距 panel 底邊 padding 內。

詳見「共用 UI 元件 > PanelFooterHint」段。

### 世界 Prompt

雙欄 mode 期間世界 Prompt 完全隱藏（依 UI Mode 規則）。打開容器後 prompt 不顯示「E: 關閉櫥櫃」這類提示，由 Esc + footer 取代。

### 規則彙整

- 可堆疊物品按 E 一次只移動 1 個。
- 目標側若已有同 id 未滿 stack，先補該 stack；補不下時開新 stack；位置由 GameState 整理規則決定。
- 兩側皆無空格、且無未滿 stack 可補時，顯示 FloatingToast「放不下了。」，原物品不消失。
- 開啟時走半透明 overlay alpha 0.3（依 UI Mode 規則）。
- 預設 focus 落在右側容器，移動後 focus 保持 index。
- 雙欄期間 E 永遠是「移動」，關閉只能 Esc。
- T 在雙欄中對任一欄 focused 物品皆可丟棄（背包或容器都生效）；行為走 Phase 1-E ConfirmDialog → discard 流程。
- 劇情重要物品（`discardable=false`）按 T 顯示 FloatingToast「無法丟棄 {item_name}」，不消失。

### 驗收

- 對外部容器互動後 UI 改為雙欄：左背包（同 1-B 視覺）、右容器。
- 兩 panel 垂直置中對齊，間距 16，整組於 viewport 水平置中。
- 雙欄開啟時預設 focus 在右欄第一格 (0, 0)。
- 方向鍵抵欄邊自動跨欄，保持 row index，row 不足時落對方最後一 row。
- E 一次只移動 1 個可堆疊物，目標側有同 id 先補 stack 再開新 stack。
- 目標兩側都放不下時顯示 FloatingToast「放不下了。」，1.5 秒 + 0.5 秒淡出，原物品不消失。
- 移動後 focus 保持原 row/col index。
- 關閉前 / 操作期間背包與容器內物品都已自動整理（合併、排序、空格集中於尾）— 任一時刻檢查皆然。
- 雙欄期間世界 Prompt 隱藏；兩欄 panel 底部 footer 顯示操作鍵提示。
- 雙欄期間 E 不會關閉 panel；`Esc` 為唯一關閉鍵。
- 關閉後再次打開：focus 重置回右欄 (0, 0)，不記憶上次位置。
- 開啟期間世界仍透出（黑色 alpha 0.3 overlay）。
- 櫥櫃顯示 5x6 = 30 slot；冰箱顯示 5x2 = 10 slot。

## GameState

已完成（Phase 1-A）。目前已實作 MVP in-memory 狀態、背包、裝備、知識 / 筆記、外部容器基本 API 與手動驗證腳本。

Phase 1-D 仍需補 `seed_container()`，用於場景預載外部容器內容；正式物品資料系統也仍待後續拆出。

GameState 是 MVP 階段的全域玩家狀態 Autoload。第一版只做記憶體狀態，不做存檔 / 讀檔。

```text
Autoload name: GameState
File: res://scripts/autoload/game_state.gd
Scope: MVP in-memory only
```

### 職責

GameState 管理：

- Credits。
- 玩家背包。
- 裝備。
- 已取得知識。
- 筆記條目。
- 外部容器的 MVP runtime state，例如櫥櫃與冰箱。

GameState 不負責：

- UI 繪製。
- 場景切換。
- 逐字訊息動畫。
- 物件互動 Area 偵測。
- UI 文字、對話字串格式化、提示訊息、log 輸出。

### 初始狀態

新遊戲啟動時：

```gdscript
credits = 0
inventory_slots = 15 # 5x3
inventory = []
equipment = {
  "clothing": [],
  "hand": [],
  "accessory": []
}
knowledge = {}
notes = []
external_containers = {}
```

MVP 中 `notes` 可預載少量起始線索，也可由場景互動逐步加入；若有預載，必須在場景規格中列出。

### 場景預載

新遊戲進入第一個場景時，由該場景的 `_ready()` 呼叫 GameState API 預載測試 / 劇本所需資料：

- 背包預載：`GameState.add_item(item_id, count)`
- 外部容器初始化 + 預載：`GameState.configure_container(id, slots)` 後再以內部 helper 直接寫入該容器內容（或由場景 API 提供，避免重複建立 instance_id 邏輯）。
- 筆記預載：`GameState.add_knowledge(note)`。

預載清單由場景規格負責（例如 `subdocs/地點/主角公寓.md > 預載資料`），GameState 規格只規範 API 與不變性。

### MVP Item Metadata Stub

第一版 `ITEMS_DB` 只作為 MVP temporary stub，用來支撐 stack / equipment / discardable 行為驗證，不是正式物品資料架構。後續正式物品資料應拆成獨立資料系統。

MVP stub 至少包含：

```gdscript
{
  "old_work_badge": {
    "category": "key_item",
    "stackable": false,
    "max_stack": 1,
    "discardable": false,
    "usable": false,
    "equipment_slot": ""
  },
  "fingerless_gloves": {
    "category": "equipment",
    "stackable": false,
    "max_stack": 1,
    "discardable": true,
    "usable": true,
    "equipment_slot": "hand"
  },
  "canned_food": {
    "category": "consumable",
    "stackable": true,
    "max_stack": 5,
    "discardable": true,
    "usable": true,
    "equipment_slot": ""
  },
  "faded_jacket": {
    "category": "equipment",
    "stackable": false,
    "max_stack": 1,
    "discardable": true,
    "usable": true,
    "equipment_slot": "clothing"
  }
}
```

### Knowledge / Notes API

```gdscript
func has_knowledge(id: String) -> bool
func has_note(id: String) -> bool
func add_knowledge(note: Dictionary) -> void
func get_notes(category: String) -> Array[Dictionary]
func get_all_notes() -> Array[Dictionary]
```

`has_knowledge(id)` 與 `has_note(id)` 分工：

- `has_knowledge(id)` 只判斷可作為劇情 gate 的穩定知識；目前由 `category == "身份"` 的 note 寫入，例如大門的 `identity_door_unlock_method`。
- `has_note(id)` 判斷任一分類筆記是否已存在，供 UI / examine / 一次性提示去重使用。
- 不得用 `has_knowledge()` 判斷 `工作` / `線索` 類 note 是否已記過；這類去重一律用 `has_note()` 或 `add_knowledge()` 的 idempotent 行為。

`add_knowledge(note)` 規則：

- `note.id` 必填。
- `note.category` 必填，合法值為 `身份` / `工作` / `線索`。
- `note.title` 必填。
- `note.body` 必填。
- `note.status` 若未提供，預設為 `active`。
- 同一 `id` 重複新增時不新增第二筆，可更新既有 note 內容。
- 若 `note.category == "身份"`，同時設定 `knowledge[note.id] = true`。
- 若其他系統需要將線索轉成穩定知識，應新增一筆 `身份` note，而不是直接改線索文字。
- `get_notes()` 與 `get_all_notes()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。

筆記 note schema：

```gdscript
{
  "id": "identity_door_unlock_method",
  "category": "身份",
  "title": "門鎖的開法",
  "body": "你想起來了。門鎖需要先按住門把下方的舊感應片, 再用右手手套靠近讀取器。",
  "status": "active"
}
```

### Credits API

```gdscript
func get_credits() -> int
func add_credits(amount: int) -> void
func set_credits(value: int) -> void
```

規則：

- Credits 型別為 `int`。
- Credits 不佔背包格。
- `set_credits()` 不允許低於 0；若輸入負值，clamp 到 0。

### Inventory API

```gdscript
func get_inventory() -> Array[Dictionary]
func add_item(item_id: String, count: int = 1) -> bool
func remove_item(item_id: String, count: int = 1) -> bool
```

MVP 背包容量固定 15 格。背包資料以 slot array 表示，空格為 `{}`。

Slot schema：

```gdscript
{
  "instance_id": "item_0001",
  "item_id": "old_work_badge",
  "quantity": 1
}
```

規則：

- 可堆疊物品以 `item_id` 合併。
- 移動堆疊物品時，按一次 E 只移動 1 個。
- 先補目標側未滿 stack，再開新 stack。
- 沒空格回傳 `false`，UI 顯示「放不下了。」。
- 每次新增、移除、移動後自動整理，空格集中在後面。
- `add_item(item_id, count <= 0)` 與 `remove_item(item_id, count <= 0)` 立即回傳 `false`，不改狀態、不 emit signal。
- `get_inventory()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。

### Equipment API

```gdscript
func equip(instance_id: String) -> bool
func unequip(equipment_type: String, slot_index: int) -> bool
func unequip_by_instance(instance_id: String) -> bool     # Phase 1-E helper
func is_equipped(instance_id: String) -> bool             # Phase 1-E helper
func get_equipment() -> Dictionary
```

裝備槽：

```text
clothing: 1
hand: 2
accessory: 2
```

規則：

- 裝備槽滿時 `equip()` 回傳 `false`，UI 顯示：
  ```text
  這類裝備已經滿了，先卸下身上的再裝備新的。
  ```
- 裝備不會離開背包，只是被 equipment 參照。
- 已裝備物品在背包排序中放最前面；卸下後自動整理重排。
- `get_equipment()` 回傳 deep copy，避免 UI 或外部系統直接修改 GameState 內部資料。
- `unequip_by_instance(instance_id)`：當 UI 焦點在背包格時直接用 instance_id 卸下對應裝備，不需先查 slot_index。內部仍走 `unequip` 同樣的整理邏輯。
- `is_equipped(instance_id) -> bool`：給 `BagGrid` 用來判斷是否要畫右上角 `E` 標記。

成功時 emit `equipment_changed`，並 emit `inventory_changed`（因為背包排序變動）。

### External Container API

MVP 中外部容器由 GameState 管理 in-memory runtime state。容器 id 規則：

```text
player_inventory = 保留 id，代表玩家背包
cabinet_storage = 櫥櫃容器，目前 5x6 = 30 slots
fridge_storage = 冰箱容器，目前 5x2 = 10 slots
```

外部容器 id 使用 lowercase underscore，並盡量與 interactable / scene id 一致。空 slot 為 `{}`。

```gdscript
func configure_container(container_id: String, slot_count: int, accepted_item: Array[String] = [], deposit_locked: bool = false) -> void  # accepted_item / deposit_locked 為 Phase 2-C 加入
func get_container(container_id: String) -> Array[Dictionary]
func get_container_config(container_id: String) -> Dictionary
func move_one_item_to(target_container_id: String, instance_id: String) -> bool

# Phase 1-D 加入；用於場景預載外部容器內容
func seed_container(container_id: String, item_id: String, count: int) -> bool
```

`configure_container()` 規則：

- 若 `container_id` 尚未初始化，建立指定數量的空 slot。
- 若容器已存在，不覆蓋既有內容。
- `accepted_item` / `deposit_locked` 旗標於首次 configure 時設定（沿用「已存在不覆蓋」）。既有呼叫（櫥櫃 30 / 冰箱 10）省略兩參數＝空白名單 + 未鎖，行為與現狀完全一致。

`get_container()` 規則：

- 回傳指定容器的 deep copy。
- 未設定的容器回傳空陣列 `[]`。

`get_container_config()` 規則（Phase 2-C 新增）：

- 回傳指定容器的只讀設定 deep copy，例如：
  ```gdscript
  {
    "slot_count": 1,
    "accepted_item": ["decoder_cube"],
    "deposit_locked": true
  }
  ```
- 未設定的容器回傳預設設定：`slot_count = 0`、`accepted_item = []`、`deposit_locked = false`。
- UI 若需要顯示不同失敗訊息，必須讀此 API 或未來的 preflight API，不得直接讀 / 修改 GameState 內部容器結構。

`move_one_item_to()` 規則：

- 動態搜尋 `instance_id` 來源，來源可能是玩家背包或任一外部容器。
- MVP 只支援 `player_inventory -> external container` 與 `external container -> player_inventory`。
- 不支援 `container -> container`；若來源是外部容器且目標不是 `player_inventory`，立即回傳 `false`。
- 堆疊物品一次只移動 1 個。
- 先檢查目標是否可容納：有同 `item_id` 且未滿 stack，或有空 slot。
- 若目標無法容納，立即回傳 `false`，來源與目標都不可變動。
- 成功時先扣來源 1 個，再加入目標；來源數量歸零時清空該 slot。
- 新開 slot 時產生新的 `instance_id`。
- 成功後來源與目標都自動排序。
- 成功後 emit `item_moved(move)`，供需要定位移動結果的系統使用；不要從排序後的背包 / 容器列表猜測新 instance。
- 只回傳 `true` / `false`，不回傳 UI message、不 log UI message。UI 層收到 `false` 後自行決定顯示文字，例如「放不下了。」。

`item_moved(move)` payload：

```gdscript
{
  "source_container_id": "cabinet_storage",
  "target_container_id": "player_inventory",
  "source_instance_id": "inst_12",
  "target_instance_id": "inst_34",  # merge 時為目標既有 stack instance；新 slot 時為新 instance
  "item_id": "worn_rubiks_cube",
  "quantity": 1
}
```

需要後續操作「剛移動的那個物品」（例如 Phase 2-B 拿上手解碼）的 controller 必須使用 `item_moved` payload 的 `target_instance_id` 定位；不得依賴原本傳入的 `source_instance_id`，因為新 slot 會產生新 instance。

`seed_container()` 規則（Phase 1-D 新增）：

- 用途：場景預載，把指定數量的某 item 放入外部容器。
- 必須先 `configure_container()` 否則回傳 `false`。
- 行為等同對該容器多次呼叫 `add_item` 的容器版本：補同 id 未滿 stack → 開新 slot；自動產生 `instance_id`；操作後自動整理。
- 容量不足時：能放多少放多少，未能放完仍回傳 `false`；放完則回傳 `true`。
- 成功時 emit `container_changed(container_id)`。
- 僅限場景 `_ready()` 或 dev tool 呼叫；遊戲流程內物品搬運必須走 `move_one_item_to`。

容器選項擴充（Phase 2-C，規格定稿，**待實作**）：

供「插槽 / 鎖定容器」這類解謎容器使用；櫥櫃 / 冰箱 / 背包省略參數即不受影響。插槽沿用一般雙欄容器 UI（不特化單格，格數可變）。

- `accepted_item`（`Array[String]` 白名單）：空＝接受所有；非空＝僅清單內 item_id 可放入。`move_one_item_to` 目標白名單非空、且來源 item 不在清單 → 回傳 `false`。
- `deposit_locked`（`bool`）：放入該容器的物品不可再移出。`move_one_item_to` 來源若是 `deposit_locked` 容器 → 立即回傳 `false`（不可作為搬運來源）；**放入不受影響**。
- 兩者皆為可組合 config 旗標，不對單一容器寫死 if。
- **false 原因辨識**：`move_one_item_to` 仍只回傳 bool（沿用既有規則）。UI 在移動前自行檢查目標白名單 / 來源 `deposit_locked`，挑對應訊息（`放不下了。` / `放不進去` / 取出被擋字樣）；`move_one_item_to` 為權威 guard，雙重保險。
- 屬「裝備條件互動 / 鑰匙開鎖容器」家族的早期個案；目前只實作 Phase 2 公寓插槽用得到的部分，不提前展開其他變體（如鑰匙被消耗）。

### Discard API（Phase 1-E）

```gdscript
func discard_item(instance_id: String) -> bool
```

通用丟棄入口，由 T 鍵流程呼叫。

規則：

- 動態搜尋 `instance_id` 來源（玩家背包或任一外部容器）。
- 若該 `instance_id` 對應 item 的 `discardable=false` → 立即回傳 `false`，狀態不變。
- 若該 item 已裝備 → 立即回傳 `false`，狀態不變（必須先卸下；UI 端 1-E 把已裝備品的 T 接到「先卸下」流程，或直接擋住）。
- 否則：將 `instance_id` 對應的 1 個（若為 stackable）或整 slot（非 stackable）從來源 slot 移除；數量歸零時清空 slot；操作後來源自動整理。
- 成功時 emit `inventory_changed`（背包來源）或 `container_changed(container_id)`（容器來源）；若該 item 為已裝備品經卸下後再丟棄，額外 emit `equipment_changed`。
- 只回傳 `true` / `false`，不回傳 UI message。UI 層收到 `true` 顯示「已丟棄 {item_name}」toast；收到 `false` 顯示「無法丟棄 {item_name}」toast。

> ⚠️ MVP 階段「stackable 物品按 T 一次丟 1 個」：與 `move_one_item_to` 對稱；不提供整 stack 一次丟。

### Signals

```gdscript
signal inventory_changed
signal container_changed(container_id: String)
signal credits_changed(new_value: int)
signal knowledge_added(id: String)
signal notes_changed
signal equipment_changed
signal item_moved(move: Dictionary)
```

Transfer signal 規則：

- `player_inventory -> external container`：emit `inventory_changed`，並 emit `container_changed(target_container_id)`。
- `external container -> player_inventory`：emit `inventory_changed`，並 emit `container_changed(source_container_id)`。
- 移動成功時額外 emit `item_moved(move)`；emit 順序以資料已更新後為準。
- `count <= 0` 或 atomic 檢查失敗時不 emit。

### 驗收

- `GameState` 以 `GameState.has_knowledge("identity_door_unlock_method")` 可被任何場景直接呼叫。
- `has_note("clue_gloves_decoder")` 可判斷線索筆記是否存在；`has_knowledge("clue_gloves_decoder")` 不應作為線索筆記去重依據。
- 新增同一筆 note 兩次，不會在筆記 UI 中出現重複條目。
- `add_credits(-999)` 或 `set_credits(-1)` 不會讓 credits 小於 0。
- 背包容量超過 15 格時 `add_item()` 回傳 `false`。
- `add_item(item_id, 0)`、`add_item(item_id, -1)`、`remove_item(item_id, 0)`、`remove_item(item_id, -1)` 回傳 `false`，且不改狀態。
- 裝備槽滿時 `equip()` 回傳 `false`，且不替換既有裝備。
- `get_inventory()`、`get_container()`、`get_equipment()` 回傳值被外部修改時，不會改到 GameState 內部資料。
- `configure_container("fridge_storage", 10)` 後可取得 10 格空容器。
- `move_one_item_to("fridge_storage", instance_id)` 可將背包物品移入冰箱；`move_one_item_to("player_inventory", instance_id)` 可將冰箱物品移回背包。
- 嘗試 `container -> container` 回傳 `false`，且不改狀態。
- 目標容器滿格時移動回傳 `false`，來源數量不可減少。
- 背包與容器移動成功時 emit 正確的 `inventory_changed` 與 `container_changed(container_id)`。
- 背包與容器移動成功時 `item_moved(move)` payload 可精準定位 target instance，供 2-B 解碼轉換使用。
- MVP 不寫入磁碟；重開遊戲狀態可重置。

## 物品資料

待實作（GameState 中已有 MVP stub `ITEMS_DB`，正式版稍後拆出獨立資料系統）。

欄位：

```gdscript
{
  "id": "old_work_badge",
  "name": "磨損的工作證",
  "description": "...",
  "category": "key_item",
  "stackable": false,
  "max_stack": 1,
  "discardable": false,
  "usable": false,
  "equipment_slot": "",
  "icon_path": "res://assets/generated/sprites/items/old_work_badge/icon.png"
}
```

欄位規則：

- `icon_path` 必填，指向 64x64 透明 PNG。
- `name` 顯示於查看 R Modal 與 FloatingToast 訊息。
- `description` 顯示於查看 R Modal 描述區。
- `category` 列舉：`key_item` / `equipment` / `consumable` / `misc`。決定 R Modal 底部 tag 文字與 E 鍵行為。
- `equipment_slot` 空字串代表非裝備品；非空時必須為 `clothing` / `hand` / `accessory` 三者之一。
- `stackable` true 時 stack 規則由 `max_stack` 控制；false 時每個 instance 各占一格。
- `discardable` false 時 T 鍵被 `discard_item` API 攔下；UI 端顯示 FloatingToast「無法丟棄 {name}」。
- `consume_grants_note`（選填）代表 E 使用或 R 查看會消耗該 instance 並寫入指定筆記；無此欄位的 consumable 使用時顯示「現在用不上。」且不消耗。
- `usable` / 可反覆使用旗標保留給未來非消耗型使用效果；目前無實例，不提前實作變體。

`Credits` 不放入物品格，而是玩家狀態數字。

### Phase 1 所需 Item Icon 清單

MVP 階段預載至少這 4 個物品（對應 `ITEMS_DB` stub），均需 64x64 透明 PNG，風格遵守 Art Bible（HD 2D + riso 顆粒、限色）：

| item_id | name | category | icon path |
|---|---|---|---|
| `old_work_badge` | 磨損的工作證 | key_item | `res://assets/generated/sprites/items/old_work_badge/icon.png` |
| `fingerless_gloves` | 無指工作手套 | equipment (hand) | `res://assets/generated/sprites/items/fingerless_gloves/icon.png` |
| `canned_food` | 合成罐頭 | consumable | `res://assets/generated/sprites/items/canned_food/icon.png` |
| `faded_jacket` | 隱士防風夾克 | equipment (clothing) | `res://assets/generated/sprites/items/faded_jacket/icon.png` |

生成由 `agent-sprite-forge` 走 `$generate2dsprite`（非場景類），輸出依 AGENTS.md 規則放在 `assets/generated/sprites/items/<item_id>/`。

Phase 2 解謎可能新增更多物品 icon，到時再追加清單。

### 驗收

- 所有物品行為（堆疊、丟棄、裝備）皆由資料欄位決定，程式不得針對特定 `id` 寫死規則。
- `Credits` 不出現在背包格中，但顯示於背包 UI。
- 每個 `ITEMS_DB` 條目都有 `icon_path`，且該檔案存在；缺檔時背包格顯示明顯 placeholder（如紅底問號）方便發現。

## 裝備系統

已完成 Phase 1-E 實作與人工驗收。

裝備槽：

```text
衣服 (clothing)：1
手持 (hand)    ：2
其他 (accessory)：2
```

僅 `category=equipment` 且 `equipment_slot != ""` 的物品可裝備。

E 鍵行為（**只在 INVENTORY mode、focused 在背包格時生效**；CONTAINER mode 中 E 永遠是「移動」）：

| Focused item | E 行為 |
|---|---|
| 未裝備 equipment | 裝備到對應 slot；槽滿時顯示 FloatingToast「這類裝備已經滿了，先卸下身上的再裝備新的。」 |
| 已裝備 equipment | 卸下 |
| `category=consumable` 或可反覆使用物 | 使用 dispatch：帶 `consume_grants_note` 者顯示 MessageBox + 寫入筆記 + 消耗；無效果 consumable 顯示 FloatingToast「現在用不上。」且不消耗；可反覆使用物執行效果且不消耗（預見類型，暫無實例） |
| 無對應動作物（如 `key_item` / `misc`） | 無 E 動作；footer 不顯示 E 段，若收到 E 輸入則不改變狀態 |

排序與視覺：

- 已裝備品在背包排序中固定置頂；同 `item_id` 多裝備時內部順序不強規範。
- 卸下後自動整理重排（依 GameState 自動整理規則），focus 保持 row/col index。
- 已裝備品在背包格右上角顯示 `E` 標記（1-B 已預留位置，1-E 啟用渲染）。

### 驗收

- 衣服槽 1、手持槽 2、其他槽 2，超過時不可裝備。
- 已裝備品在背包排序中固定置頂；卸下後自動重排，focus index 不變。
- 對未裝備品按 E 是「裝備」，對已裝備品按 E 是「卸下」，互不混淆。
- 槽滿時顯示 FloatingToast「這類裝備已經滿了，先卸下身上的再裝備新的。」，原裝備不替換。
- 已裝備品右上角顯示 `E` 標記；卸下後標記消失。
- INVENTORY 中對帶 `consume_grants_note` 的 consumable 按 E 使用時，顯示訊息、寫入筆記並移除該 instance；對無效果 consumable 按 E 顯示 FloatingToast「現在用不上。」且不消耗。
- INVENTORY 中對無 E 動作的 `key_item` / `misc` 不顯示 footer E 段，按 E 不改變狀態。
- CONTAINER 雙欄中 E 永遠是「移動」，不會觸發裝備行為。

## 筆記 / 知識系統

已完成 Phase 1-C 基礎 UI 與人工驗收。

筆記分類：

```text
身份
工作
線索
```

定義：

- `身份`：穩定、已確認的自我資訊，例如這裡是自己的公寓、自己的職業、門鎖開法。
- `工作`：委託、任務、零工、工作紀錄。
- `線索`：暫時、不完整、尚未理解的資訊，可轉化為身份或工作筆記。

操作：

```text
J = 開 / 關筆記
Esc = 關閉
A / D（或 Left / Right）= 切分類
W / S（或 Up / Down）= 選筆記
選到筆記後自動顯示全文
```

> 2-F 修正（**待實作**）：筆記導航比照 `BagGrid` 同時接 WASD 與方向鍵——A / D 切分類、W / S 選筆記，方向鍵保留為鏡像。footer 主打 WASD（見「共用 UI 元件 > PanelFooterHint」2-F 註）。同時筆記內容皆控制在全文欄一頁內，Page Up / Down 捲動移除。

UI 規則：

- 開啟時走半透明 overlay，世界仍可見（依 UI Mode 規則）。
- 三分類顯示為頂部 tab；當前 tab 高亮。
- 同一面板左側顯示該分類筆記列表，右側顯示選中筆記全文（無需另開 Modal）。
- 玩家進入筆記 UI 時預設 focus 在第一個非空分類的第一筆筆記。

### Panel 尺寸與排版

```text
panel 寬 = 880
panel 高 = 560
位置     = viewport 置中（1280x800 viewport 下左上 ≈ (200, 120)）

內部分區：
  TAB_BAR_HEIGHT     = 36         # 頂部三分類 tab
  LIST_PANE_WIDTH    = 240        # 左側列表欄
  PADDING            = 16
  全文欄寬           = panel 寬 - LIST_PANE_WIDTH - PADDING * 3 = ~600
  內容區高           = panel 高 - TAB_BAR_HEIGHT - PADDING * 2 = ~492
```

### Skin

沿用 1-B 已定的 `inventory` skin（深藍灰底 + 主角橙紅邊框 + 米白標題字），與背包同一視覺家族。

### Tab Bar

```text
[ 身份 ]  [ 工作 ]  [ 線索 ]
```

- 每個 tab 固定寬 120px、高 = TAB_BAR_HEIGHT (36px)。
- 文字 18px。
- **未選中** tab：文字暗灰 `(0.55, 0.55, 0.55, 1.0)`，無底線。
- **選中** tab：文字米白 + 底部 2px 橙紅線（同 panel 邊框色 `(0.78, 0.42, 0.20, 1.0)`）。
- 鍵盤 A / D（或 Left / Right）切換 tab；切換後 focus 自動跳到該分類第一筆。

### 列表欄

- 每行高 32px、font 16px。
- 顯示**只有 title**（不顯示 body 預覽）。
- 240px 寬可塞 ~14 筆可見；超出時列表自動垂直捲動跟隨 focus（Godot ScrollContainer + 自動 ensure_control_visible）。
- 行間 hover / focus 視覺：
  - 預設 title 字色依 status 決定（見下）。
  - 鍵盤 focus 行：2px 橙紅外框 + 行底色微亮。
- 空分類：列表顯示「（尚無筆記）」placeholder，右側全文欄空白。

### `status` 視覺

筆記 `status` 欄位 MVP 渲染：

- `active`：title 字色米白 `(0.94, 0.92, 0.84, 1.0)`。
- 其他值（`superseded` / `inactive` / 未來新增）：title 字色灰 `(0.55, 0.55, 0.55, 1.0)`。

1-C 階段所有預載筆記都是 `active`，此規則為 Phase 2 / 3 預留。

### 全文欄

- 用 `RichTextLabel` 顯示 `body`，`scroll_active = true`。
- 字 16px，行高建議 1.5。
- 2-F 起：MVP 筆記內容皆控制在全文欄一頁內，不提供捲動；Page Up / Down 已停用（程式吞掉不作用）。
- 切到不同筆記時全文重新填入。

### 操作 focus 流向

```text
進入筆記 UI    → focus 在「第一個非空分類」的第一筆筆記
A / D          → 切 tab，focus 跳到該分類第一筆（Left / Right 同義）
W / S          → 在列表內上下選筆記；超出列表邊界不繞回（Up / Down 同義）
                 列表捲動跟隨 focus（自動 ensure_control_visible）
切到不同筆記   → 全文欄即時更新
J / Esc        → 關閉筆記
```

全文欄本身**不參與 Tab focus 鏈**。2-F 起筆記皆一頁內，不需捲動全文，Page Up / Down 已停用。

資料範例：

```gdscript
{
  "i#### 2-G 驗收意圖

開場獨白序列（新遊戲進公寓即播；建立「失憶甦醒」基調，銜接 B0 醒來）——

- **進場狀態**：場景載入即進入開場——玩家鎖在 `prone`（趴睡）姿態、世界輸入全擋、互動 prompt 隱藏、MessageBox 接管畫面顯示第 1 頁。
- **共 3 頁**，逐頁逐字播放（沿用既有 MessageBox 6 字/秒打字機）；**文字播放中不可跳過**，整頁顯示完成後才接受換頁。
- **換頁鍵**：WASDE / 方向鍵 / Esc，任一即進下一頁；最後一頁同鍵關閉 MessageBox。
- **換頁提示**：每頁文字跑完後淡入提示——前二頁「▼ 繼續」、第三頁「▼ 開始」；文字未跑完不顯示、不接受換頁。
- **分頁標題**（感官甦醒 / 空白的記憶 / 自囚與尋回）置於該頁正文第一行，隨打字機一起逐字顯示，不另設標題樣式。
- **起身動畫**：第 3 頁一出現即播放 `get_up`（10 frame、約 1 秒、不循環），播完停最後一格切 `idle`；此頁文字同時以 6 字/秒續跑，文字與動畫互不等待（文字與動畫互不影響）。
- **結束**：第 3 頁文字跑完、玩家按鍵關閉 MessageBox → 解除輸入封鎖，玩家以站立 `idle` 開始自由操作。開場結束時角色一律停在站立 `idle`，不論動畫播放進度（被跳過則直接 snap 站姿）。
- **每次進場都播**（MVP 無存檔機制；日後加入存檔再 gate「只播一次」）。
- **隱藏測試捷徑**：開場期間 **1 秒內連按 3 下 T** = 立即補完當頁文字並跳下一頁；於第 3 頁則直接關閉開場、解鎖操作。此捷徑不顯示於提示，純測試用途。
- 沿用既有系統：MessageBox（2-E overlay 視覺 / 打字機）、`UIMode.is_world_input_blocked()` 輸入封鎖路徑、玩家 `AnimatedSprite2D`；無新 GameState API。開場接管邏輯（多頁 / 不可跳過 / 動畫鉤子 / T 捷徑）實作見 `開發設計方針.md > 2-G`。
- 開場文案全文見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`。��垂直捲動，切到其他筆記時捲動位置重置回頂。
- INVENTORY 中按 `J` 直接切到筆記（背包關、筆記開），無雙重 panel 疊加。

## 知識解鎖

門與部分劇情進展使用知識 id 判斷，不直接讀筆記文字。

範例：

```gdscript
GameState.has_knowledge("identity_door_unlock_method")
```

目前規格：

- 大門離開公寓的條件是知識，不是物品。
- 玩家需要從房間探索中取得足夠線索，最後形成穩定知識。

### 驗收

- 任何劇情 gate（如大門）只透過 `GameState.has_knowledge(id)` 判斷，不依賴 inventory item id。
- 未取得對應知識時 gate 行為穩定（顯示鎖定訊息、不放行）。

## 音訊（BGM）

MVP 音訊極簡。解謎短音（`sonar_ping.wav` / `hidden_slot_reveal.wav` / 電磁聲響）由觸發處各自播放，無集中匯流排。背景音樂於 2-F 加入。

### 公寓 BGM（2-F，待實作）

- 進公寓場景自動循環播放單一背景音樂。
- 檔案：`res://assets/bgm/Echoes of a Cozy Night.mp3`（Godot 4.6 原生支援 `AudioStreamMP3`；匯入設定開 `Loop`）。
- 音量寫死 `-12 dB`；設定選單調音量留 Phase 3+。
- 檔案不存在時靜音、不報錯。
- 實作位置：`apartment_room.tscn` 直接掛一個 `AudioStreamPlayer` 節點（`autoplay = on`、stream loop、`volume_db = -12`），不為單場景抽 autoload。Phase 3+ 有第二場景、需跨場景續播時再抽 `MusicManager`。

#### 驗收

- 進公寓場景即聽到 BGM 循環播放，無明顯爆音；循環不中斷停止。
- 音量為背景襯底音量（`-12 dB`），不蓋過解謎短音。
- 移除 BGM 檔案後遊戲仍正常啟動、不噴錯（靜音）。

## Phase 規劃

### Phase 1 — 系統基本功能

目標：建立 GameState、UIMode、背包 / 筆記 / 容器 UI 與物品操作的完整基本框架。Phase 1 結束時所有底層系統可用，但公寓還無法通關（解謎留給 Phase 2）。

| Sub | 內容 | 結束時驗收 |
|---|---|---|
| **1-A** ✅ | GameState autoload + 門 gate | 已完成 |
| **1-B** ✅ | UIMode autoload + 既有訊息 / 容器 UI 接入 UIMode + 背包 UI（`I`）+ 4 個 item icon 前置生成 + 場景預載測試物品 | 已完成並人工驗收：開背包顯示 5x3 + Credits；預載 item 用 icon 顯示；既有 MessageBox / 容器 panel 已改吃 UIMode；UI 開啟時主角不能動；世界半透明 alpha 0.3 可見；E / R / T 在背包內靜默 |
| **1-C** ✅ | 筆記 UI（`J`） | 已完成並人工驗收：開筆記顯示三分類（身份、工作、線索），可鍵盤 Left/Right 切分類、Up/Down 選筆記、PageUp/PageDown 直接捲動全文；右上/左側 focus 視覺符合 Riso Cyberpunk 2px flat orange 橙紅指示線；預載 3 筆 demo 筆記且不包含 identity_door_unlock_method；支援 empty tab placeholder 顯示與 messagebox 逐字 message typewriter 的安全關閉。 |
| **1-D** ✅ | 容器資料化 + 櫥櫃 5x6 + 雙欄 + 物品移動 + Stack + 「放不下了。」 | 已完成並人工驗收：開櫥櫃 / 冰箱顯示真實資料；雙欄左背包右容器，預設 focus 右側；E 移動 1 個；stack 補位規則正確；目標滿時顯示 FloatingToast 不損失物品；雙欄期間 E 僅移動、Esc 為唯一關閉鍵；CONTAINER 中按 I / J 直接切到對應 UI。 |
| **1-E** | 物品操作：查看 R / 丟棄 T / 裝備 E | 可看 description / 丟棄 / `discardable=false` 保護 / 衣服 1 + 手持 2 + 其他 2 槽 / 已裝備品置頂 |

#### Phase 1-B 實作細節

狀態：已完成並人工驗收。保留本段作為已實作內容與後續 Phase 1-D / 1-E 的相依契約。

實作建議順序：

1. **已加入 4 個 item icon**（前置）：`old_work_badge` / `fingerless_gloves` / `canned_food` / `faded_jacket` 四張 64×64 透明 PNG，位置為 `assets/generated/sprites/items/<item_id>/icon.png`。
2. **已建 UIMode autoload**：`scripts/autoload/ui_mode.gd`，註冊 `project.godot`。提供 `Mode` enum、`get_mode` / `set_mode` / `is_world_input_blocked`、`mode_changed` signal。
3. **已重構 `apartment_room.gd` 既有 UI 接 UIMode**：
   - MessageBox 進入時 `UIMode.set_mode(UIMode.Mode.MESSAGE)`、退出 `set_mode(UIMode.Mode.NONE)`。〔歷史快照；2-E overlay 重構後改為 `enter_overlay(MESSAGE)` / 退出 `exit_overlay()` 還原 caller，見「overlay modal 的 caller 還原」。〕
   - 容器 panel 進入時 `set_mode(UIMode.Mode.CONTAINER)`、退出 `set_mode(UIMode.Mode.NONE)`。
   - 大門 / 床訊息顯示時 `set_mode(UIMode.Mode.MESSAGE)`。
   - 依目前 mode 分層處理輸入，UI 開啟時世界互動被 block。
   - Prompt 顯示由 `mode_changed` 同步，UI 開啟時隱藏，回到 NONE 時恢復。
4. **已完成主角移動 gate**：`player.gd` 在讀輸入前檢查 `UIMode.is_world_input_blocked()`，UI 開啟時 `velocity = Vector2.ZERO` 並播放 idle。
5. **已建半透明 overlay**：全 viewport `ColorRect`，color `(0, 0, 0, 0.3)`，panel 之下、世界之上。`mode_changed` != NONE 時顯示。
6. **已抽 `BagGrid` 為共用 component**（前置設計約束，1-D 雙欄要 reuse）：
   - 把「5x3 + slot widget」抽成獨立 scene + script，提供 `set_focused_index` / `set_input_active` API。
   - 同一個 component 之後會在 1-D 雙欄左欄被重用，視覺與資料源完全一致。
   - 詳見「共用 UI 元件 > BagGrid」段。
7. **已建背包 panel**：包含 BagGrid + 標題列（含 Credits）+ Footer hint。依「背包系統」段尺寸 / skin / 排版規格。
8. **已建 slot widget**：64×64 顯示 icon / 數量 / 裝備標記 / focus 外框。
9. **已接 `open_inventory` action**：按 `I`：NONE → INVENTORY、INVENTORY → NONE、MESSAGE / CONTAINER → 關舊 UI 後開 INVENTORY。`Esc` 在 INVENTORY 時關閉。
10. **已接 GameState 資料**：panel 顯示時讀 `get_inventory()` + `get_credits()`。
11. **已建共用 UI 元件 stub**（前置）：
    - `FloatingToast`：1-B 階段為空殼 API，1-D 才會實際用到。
    - `PanelFooterHint`：1-B 用於背包 footer 顯示 `Esc / I: 關閉`。
12. **已做場景預載**：`apartment_room.gd._ready()` 於玩家背包全空時加入 `fingerless_gloves` + `old_work_badge`。容器預載留 1-D。
13. **1-B 範圍內 E / R / T 沉默**：背包格內按下 E / R / T 完全無反應；不顯示 stub 訊息；1-E 再接行為。

驗收檢查清單：

- [x] 4 張 icon 已生成且符合 Art Bible Chapter 5 規格。
- [x] UIMode autoload 註冊成功，可從任何場景呼叫。
- [x] 既有 MessageBox / 容器 panel 開關時 UIMode 狀態正確變更。
- [x] 主角在任何 UI 開啟時不會走動。
- [x] 按 `I` 開啟背包顯示置中 panel 368×256、預載 2 個 item 用 icon 呈現、Credits: 0。
- [x] 半透明 overlay 顯示，世界仍可見。
- [x] 鍵盤導航：方向鍵 / WASD 切換 focus，2px 橙紅外框可見。
- [x] `Esc` / `I` 關閉背包，UIMode 回 NONE，主角恢復可動。
- [x] E / R / T 在背包內無反應（無錯誤、無訊息）。
- [x] `BagGrid` 已抽為獨立 component，1-D 開做時可直接 reuse 不需重構。
- [x] `PanelFooterHint` 已建為共用元件，背包底部顯示 `Esc / I: 關閉`。
- [x] `FloatingToast` 空殼建立，API 可呼叫但不必觸發。

自動驗證：

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless res://tests/manual/test_runner.tscn
```

手動驗收：已完成。

#### Phase 1-C 實作細節

前置：1-B 完成（UIMode autoload、inventory skin、半透明 overlay、`open_inventory` 已接入）。1-C 負責接入 `open_notebook` 與 I/J 互切。

實作建議順序：

1. **筆記 panel 節點**：880×560 容器置中，套 inventory skin。
2. **頂部 tab bar**：3 個 Button 寬 120 高 36，未選中 / 選中視覺依「Tab Bar」段。
3. **列表欄**：左側 240×~492 的 ScrollContainer + VBoxContainer，每行 32px Button 或 Label。
4. **全文欄**：右側 ~600×~492 RichTextLabel，`scroll_active = true`、`fit_content = false`、`bbcode_enabled` 可選。
5. **接 `open_notebook` action**：按 `J` 走 UIMode 規則（與 INVENTORY 互切自動處理）；`Esc` 在 NOTEBOOK mode 下關閉。
6. **接 GameState 資料**：
   - 開啟時呼叫 `GameState.get_notes(<current_tab>)` 填列表。
   - Connect `GameState.notes_changed` signal：列表 / 全文即時刷新。
7. **focus 流向**：
   - 開啟時先掃描三分類找第一個非空，自動切到該 tab，focus 落第一筆。
   - 全部分類皆空時 focus 落在 `身份` tab 的「（尚無筆記）」placeholder（鍵盤無筆記可選）。
8. **status 字色**：title Label 依 `note.status == "active"` 二分色。
9. **列表自動捲動**：focus 變更時 `ensure_control_visible(focused_item)`。
10. **全文捲動重置**：切到不同筆記時 `RichTextLabel.scroll_to_line(0)`。
11. **場景預載 demo 筆記**：`apartment_room.gd._ready()` 末尾呼叫 `GameState.add_knowledge(...)` 三次（詳見 `subdocs/地點/主角公寓.md > 預載資料 > 筆記預載`）。
    - ⚠️ 預載**不得**包含 `identity_door_unlock_method`，否則 Phase 1-C 階段門就自動解了。

驗收檢查清單：

- [x] 按 `J` 開啟筆記，panel 880×560 置中於 viewport，套 inventory skin。
- [x] 預載 3 筆 demo 筆記（身份 / 工作 / 線索 各 1）可見。
- [x] Left / Right 切 tab 後 focus 自動跳該分類第一筆.
- [x] Up / Down 選筆記時右側全文即時更新、捲動位置重置回頂。
- [x] 切到無筆記的分類（若有）顯示「（尚無筆記）」placeholder。
- [x] `J` 或 `Esc` 關閉筆記，UIMode 回 NONE，主角恢復可動。
- [x] INVENTORY 中按 `J` 直接切到筆記（背包關閉、筆記開啟）；NOTEBOOK 中按 `I` 反之。
- [x] 半透明 overlay alpha 0.3，世界仍可見。
- [x] 列表項字色：`active` 米白、其他 status 灰（1-C 階段無實際差異，但 code 路徑可驗）。
- [x] 列表超過 14 筆時可滾動跟隨 focus（1-C 階段預載只有 3 筆，此項以單元驗證 / 動態加 dummy 條測試）。
- [x] 長 body 在全文欄可捲動（Page Up / Page Down 或滑鼠滾輪）。
- [x] panel 底部 footer 顯示 `Esc / J: 關閉`（reuse `PanelFooterHint`）。

自動驗證：

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless res://tests/manual/test_runner.tscn
```

手動驗收：已完成。

#### Phase 1-D 實作細節

狀態：已完成並人工驗收。

前置：1-B 完成（UIMode、半透明 overlay、`BagGrid` component、`FloatingToast` 空殼、`PanelFooterHint`）；1-C 不是必要前置，可平行做。

實作建議順序：

1. **櫥櫃改 5x6**：修 `apartment_room.gd > CONTAINERS["cabinet_storage"].rows = 6`。
2. **GameState 容器初始化**：`apartment_room.gd._ready()` 加：
   - `GameState.configure_container("cabinet_storage", 30)`（5 × 6）
   - `GameState.configure_container("fridge_storage", 10)`（5 × 2）
3. **GameState 補 `seed_container()` API**（規格 External Container API 段已新增）：實作 helper 將指定 item / count 寫入指定外部容器，內部自動產生 instance_id 與整理；emit `container_changed`。
4. **容器預載資料**：依 `subdocs/地點/主角公寓.md > 預載資料 > 容器預載` 呼叫 `seed_container("cabinet_storage", "faded_jacket", 1)` 等。
5. **建 `ExternalGrid` component**：5×N 動態 grid widget，視覺套 inventory skin（同 `BagGrid`）；讀 `GameState.get_container(id)`、listen `container_changed(id)`。
6. **建雙欄 panel**：兩個 panel 並排（左 `BagGrid` + 右 `ExternalGrid`），垂直置中對齊，間距 16，整組水平置中於 viewport。
7. **改寫 `_toggle_container()`**：
   - 進入時 `UIMode.set_mode(CONTAINER)` + 顯示雙欄 panel + 預設 focus 右欄 (0, 0)。
   - 退出走 `Esc`（移除原本「再按 E 關閉」分支）。
8. **跨欄輸入**：方向鍵抵欄邊自動跳對欄；保持 row index，row 不足時落對方最後一 row。
9. **接 E 移動**：focus 在哪欄則「該欄 → 對欄」移動；呼叫 `GameState.move_one_item_to(target_container_id, instance_id)`。
10. **接 FloatingToast**：當 `move_one_item_to` 回 `false`（目標滿）→ 顯示「放不下了。」。
11. **Footer hint**：兩欄各設 `E: 移動    Esc: 關閉`。
12. **世界 Prompt 隱藏**：雙欄 mode 期間 prompt 完全隱藏（沿用 UI Mode 規則）。
13. **Focus 行為**：移動後保持 row/col index 不變、不跨欄。
14. **驗證 stack 補位**：搬罐頭時測 0→1→2→3→4→5 滿一 stack、第 6 個開新 stack 至下個 sorted-first 空格。
15. **驗證自動整理**：在容器與背包間搬幾個物品後，data layer 應同 id 合併、未滿 stack 補位、空格集中尾部。

驗收檢查清單：

- [x] 櫥櫃顯示 5x6 = 30 slot，冰箱顯示 5x2 = 10 slot。
- [x] 按 E 開櫥櫃 / 冰箱，雙欄 panel 出現：左背包視覺同 1-B、右容器顯示真實資料。
- [x] 兩 panel 垂直置中對齊，間距 16，整組水平置中。
- [x] 預設 focus 右欄 (0, 0)；2px 橙紅外框可見。
- [x] 方向鍵抵欄邊自動跨欄；保持 row index；row 不足落對方最後一 row。
- [x] E 移動 1 個：同 id 先補 stack 再開新 slot。
- [x] 兩側無法放下時顯示 FloatingToast「放不下了。」，1.5 秒顯示 + 0.5 秒淡出。
- [x] 移動後 focus index 不變、不跟物件、不跨欄。
- [x] data layer 任一時刻檢查皆已整理（同 id 合併、未滿 stack 補位、空格集中尾部）。
- [x] 雙欄期間 E 不會關閉 panel；`Esc` 為唯一關閉鍵。
- [x] 關閉後再次打開：focus 重置回右欄 (0, 0)。
- [x] 雙欄期間世界 Prompt 隱藏；兩 panel 底部 footer 顯示 `E: 移動    Esc: 關閉`。
- [x] 半透明 overlay alpha 0.3，世界仍可見。
- [x] CONTAINER 中按 `I` 切到背包（容器關、背包開）；按 `J` 切到筆記（容器關、筆記開）。

自動驗證：

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless res://tests/manual/test_runner.tscn
```

手動驗收：已完成。

#### Phase 1-E 實作細節

前置：1-B / 1-D 完成。1-C 不是必要前置但建議完成。

實作建議順序：

1. **UIMode CONFIRM 啟用 + caller mode 還原**：`scripts/autoload/ui_mode.gd` 補 `enter_confirm()` / `exit_confirm()`，內部記住 `_caller_mode`；`set_mode(NONE)` 流程不影響此邏輯。
2. **建 `ItemDetailModal` component**：依「共用 UI 元件 > ItemDetailModal」段，320×400 floating panel；icon 128×128、name 22px、description 16px、底部 category tag。`show(instance_id)` API。R / Esc 關閉。
3. **建 `ConfirmDialog` component**：依「共用 UI 元件 > ConfirmDialog」段，360×160 viewport 置中；message + footer hint；`show(message, on_confirm, on_cancel)` API。E 確定 / Esc 取消。
4. **GameState 補 `discard_item(instance_id)` API**：依「Discard API（Phase 1-E）」段實作；尋找 source（背包或任一容器）、檢查 discardable / 已裝備、執行 remove、自動整理、emit signal。
5. **GameState 確認 `equip` / `unequip` API 行為**：未實作則補；卸下後自動觸發 inventory 整理；`equipment_changed` signal 正確。
6. **背包格 `E` 標記渲染**：1-B 已預留位置，1-E 連動 `GameState.is_equipped(instance_id)` 或等價 helper，動態顯示 / 隱藏標記。
7. **接 R 鍵**（INVENTORY 與 CONTAINER 兩 mode 皆生效）：
   - Focused 為空格 → 沉默。
   - Focused 有物品 → `ItemDetailModal.show(instance_id)`。
8. **接 T 鍵**（INVENTORY + CONTAINER 雙欄兩欄皆生效）：
   - Focused 為空格 → 沉默。
   - Focused 有物品：
     - 若 `discardable=false`（key_item）→ FloatingToast「無法丟棄 {item_name}」，不開 dialog。
     - 若該 item 為已裝備品 → FloatingToast「請先卸下再丟棄」（或直接擋住），不開 dialog。
     - 其他 → `ConfirmDialog.show("確定要丟棄 {item_name}？", on_confirm=do_discard, on_cancel=null)`。
9. **接 E 鍵的裝備 / 使用 dispatch**（**只在 INVENTORY mode、焦點在背包格時**）：
   - `category=equipment` 未裝備 → `equip(instance_id)`；槽滿時 FloatingToast 提示。
   - `category=equipment` 已裝備 → `unequip(slot_index)` 或 `unequip_by_instance(instance_id)`。
   - 帶 `consume_grants_note` 的 consumable → MessageBox + `add_knowledge` + 移除該 instance。
   - 無定義效果的 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」、不消耗。
   - 無對應動作物（如 `key_item` / `misc`）→ 不改變狀態。
10. **footer hint 更新**：
    - 背包單獨 panel footer 的 E 段依選中物品動態顯示：`E: 裝備` / `E: 卸下` / `E: 使用` / 不顯示。
    - 雙欄兩欄 footer 同字串 `E: 移動    R: 查看    T: 丟棄    Esc: 關閉`。
11. **驗證跨 mode 鍵盤鎖**：modal / dialog 開啟時所有非自身鍵被 block；UI Mode 互切（I / J）在 CONFIRM 期間不反應。

驗收檢查清單：

- [ ] R 在 INVENTORY focused 物品 → ItemDetailModal 開啟，顯示 icon / name / description / 對應 category tag；R 或 Esc 關閉，焦點回原格。
- [ ] R 在 CONTAINER 雙欄任一格 focused 物品 → ItemDetailModal 同樣開啟。
- [ ] R 在空格 → 完全沉默。
- [ ] T 在可丟棄物品 → ConfirmDialog 開啟「確定要丟棄 {item_name}？」；E 確定→丟棄並顯示「已丟棄 {item_name}」toast；Esc 取消→無變動。
- [ ] T 在 `discardable=false`（如 `old_work_badge`）→ FloatingToast「無法丟棄 {item_name}」，無 dialog 開啟。
- [ ] T 在已裝備品 → 阻擋訊息「請先卸下再丟棄」，無 dialog 開啟。
- [ ] T 在 CONTAINER 雙欄右欄（容器格）→ 同上行為，可從容器丟棄。
- [ ] T 在空格 → 完全沉默。
- [ ] E 在 INVENTORY 焦點為未裝備 equipment → 裝備成功；右上角顯示 `E` 標記；排序置頂。
- [ ] E 在 INVENTORY 焦點為已裝備 equipment → 卸下成功；標記消失；自動重排（focus index 保持）。
- [ ] E 在 INVENTORY 焦點為裝備槽已滿 → FloatingToast「這類裝備已經滿了，先卸下身上的再裝備新的。」，狀態不變。
- [ ] E 在 INVENTORY 焦點為帶 `consume_grants_note` 的 consumable → MessageBox + 寫入筆記 + 移除該 instance。
- [ ] E 在 INVENTORY 焦點為無效果 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」，不消耗。
- [ ] E 在 INVENTORY 焦點為無動作物（如 `old_work_badge`）→ footer 不顯示 E 段；按 E 不改變狀態。
- [ ] E 在 CONTAINER 雙欄永遠是「移動」，不會誤觸發裝備行為。
- [ ] ConfirmDialog 開啟時 UIMode = CONFIRM；E / Esc 處理完畢後 UIMode 還原到 caller mode（INVENTORY 或 CONTAINER），caller UI 不關閉。
- [ ] ItemDetailModal / ConfirmDialog 開啟期間其他鍵全部 block；I / J / 方向鍵 / E / T 都不會觸發背包行為。
- [ ] Footer 顯示符合「PanelFooterHint > 各 UI 對應 hint」表 1-E 欄。

### Phase 2 — 公寓解謎

目標：玩家透過 1-B ~ 1-E 已建立的系統互動，取得 `identity_door_unlock_method` 知識並通關公寓。

機制鏈與主線敘事已定稿（線性因果鏈：手套 → 解碼 → 聲納找插槽 → 放入 → 開門）。**完整流程 B0–B9、互動物、線索文字寫在 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`**；本段只記跨場景重用的通用機制能力。

本 phase 需新增 / 啟用的通用機制（皆**待實作**）：

1. **裝備條件物品轉換**：身上裝備某物（如解碼手套）時，特定物品 A 在「拿上手」或「裝備態 R 查看」時被轉換成物品 B。屬「裝備條件互動」家族；做成可組合條件，不對單一物寫死。
2. **通用解鎖道具（解碼方塊家族）**：類鑰匙道具，放入對應插槽開啟特定功能；跨關卡會有不同功能版本。
3. **鄰近偵測（聲納）reveal 隱藏互動物**：開關啟動後進入**限時聲納窗**，依玩家 1D 水平距離給音效強度 + 視覺 meter；停留最強區帶寬內（容許微動）累積鎖定環 → 顯露隱藏互動物。限時窗逾時未鎖定則結束、需重新啟動；reveal 後聲納自動關閉且開關移除互動。大型 UI 開啟時聲納與計時凍結，關閉後接續（不歸零）。
4. **插槽 / 鎖定容器**：單格容器 + `accepted_item` 白名單 + `deposit_locked`（放入不可取出）。規格見「External Container API > 容器選項擴充」。
5. **放入觸發**：白名單命中 → 電磁聲響 + MessageBox 提示 + `add_knowledge`；屬既有 `container_changed` signal 流程延伸。
6. **使用 / 查看即消耗的線索道具**：特定 consumable 道具於 E 使用或 R 查看（兩路徑同效）時跳 MessageBox 揭示內容、`add_knowledge` 寫入線索、並移除該 instance（一次性）。屬「可消耗道具」預見家族。

沿用既有系統不改：大門純 `has_knowledge` gate（Phase 1-A）、容器雙欄、筆記系統、物品 R/T/E 操作。

設計原則：留組合空間但不提前實作未用到的變體（如「鑰匙被消耗」「裝備後不同 prompt 分支」等預見機制，到實際關卡需要時才做）。

#### 子階段拆分

| 子階段 | 內容 | 依賴 | 風險 |
|---|---|---|---|
| 2-A | 線索 examine 互動物（桌上電腦 / 左牆錄音機 2 條） | 既有互動 + add_knowledge | 低 |
| 2-B | 解碼手套機制 + 魔術方塊→解碼方塊道具（含手套線索筆記） | 2-A 無硬依賴 | 中 |
| 2-C | 容器擴充：`accepted_item` 白名單 + `deposit_locked`（資料層） | GameState | 中 |
| 2-D | 投影時鐘（偵測終端）+ 營養棒線索麵包屑 + 聲納 reveal 隱藏插槽 | 2-C | 高 |
| 2-E | 插槽放入 → 電磁聲響 + MessageBox + add_knowledge → 開門整合 + 全鏈手動驗收 | 2-B/2-C/2-D | 中 |
| 2-F | 筆記內容/操作修正（測試長筆記改氛圍版、A/D 切分頁 + W/S 選筆記）+ 公寓 BGM | 1-C 筆記 UI | 低 |
| 2-G | 開場獨白序列（prone 甦醒 → 4 頁不可跳過 MessageBox → P4 起身動畫 → 解鎖自由操作） | 既有 MessageBox + 玩家 AnimatedSprite2D | 低 |

實作契約見 `開發設計方針.md`；手動驗收步驟見 `測試指南.md`；場景流程與文字見 `subdocs/地點/主角公寓.md`。

#### 2-A 驗收意圖

examine 線索互動物（兩條：善後員 / 拾遺者）行為意圖——

- 靠近線索互動物顯示其 prompt；按 E → MessageBox 顯示現場敘述句，同時將對應線索筆記寫入知識庫（`add_knowledge`），玩家可於筆記 UI（`J`）查得。
- 兩條線索開局即可 examine，無前置 gating。
- 已記過的線索再 examine：重播現場敘述句，不重覆新增筆記（`add_knowledge` idempotent）。
- 線索互動物沿用既有資料驅動互動物機制，不為單一物寫死分派邏輯。
- 手套線索筆記不在 2-A，改由 2-B 的物品側互動（R 查看 / 裝備手套）產生。
- Phase 2 開工後，`work_ai_cleanup_role` 不再作為新遊戲預載 demo 筆記；必須由桌上電腦 examine 首次寫入，確保 2-A 首次取得可驗。

#### 2-B 驗收意圖

解碼手套（裝備條件物品轉換）+ 魔術方塊→解碼方塊 + 手套線索筆記——

- 裝備解碼手套狀態下，對「可解碼物」執行下列任一動作會將其轉換為對應目標物：
  - 將該物從容器移入背包（「拿上手」）。
  - 在 ItemDetailModal（R 查看）中檢視該物（此為防卡關 fallback；轉換後 modal 刷新顯示新物）。
- 解碼發生當下跳 MessageBox 敘事，同時 `add_knowledge` 寫入「解碼方塊」筆記。
- 轉換以 `item_moved` payload 的 `target_instance_id` 或 ItemDetailModal 當前 instance 定位剛被操作的物品，更新該 instance 的 `item_id`；目標物不再帶可解碼旗標，搬動不重複觸發。
- **未裝備手套**時，對可解碼物拿上手 / R 查看皆為普通物品，不轉換、不產生筆記。
- **不回掃背包**：先把可解碼物拿進背包、後才裝備手套的情況，靠 R 查看補解（不在裝備當下自動掃描）。
- 解碼對應為物品 metadata 的資料欄位（搭配所需裝備條件），不為單一物寫死分派；目前只啟用公寓這顆所需者。
- 手套線索筆記（`clue_gloves_decoder`）於**首次裝備或首次 R 查看手套**任一觸發，一次性寫入。
- 屬「裝備條件互動」與「解碼方塊通用道具」家族的早期個案，不提前實作其他變體。

#### 2-C 驗收意圖

容器擴充（`accepted_item` 白名單 + `deposit_locked`），純資料層，可 headless 驗——

- `configure_container` 省略新參數時，櫥櫃 / 冰箱 / 背包行為與 Phase 1-D 完全一致（空白名單 = 不限、未鎖）。
- 設白名單的容器：清單內 item 可放入；清單外 item `move_one_item_to` 回 `false`，內容不變。
- `deposit_locked` 容器：可放入；其內物品 `move_one_item_to`（作為來源）回 `false`，不可取出。
- 白名單為 `Array[String]`，可放多種 id。
- `move_one_item_to` 維持只回傳 bool；訊息差異由 UI 層依容器旗標自行判定。
- UI 判定訊息差異時透過 `get_container_config(container_id)` 讀取 `accepted_item` / `deposit_locked`，不得讀取 GameState 內部資料結構。
- 細節見「External Container API > 容器選項擴充」。

#### 2-D 驗收意圖

偵測終端（投影時鐘）+ 聲納 reveal 隱藏插槽——

- **啟動前置**：冰箱內 `nutrition_bar_synth_blueberry` 經 **E 使用或 R 查看**（兩路徑同效，仿手套解碼）→ 跳空包裝藏字敘述、`add_knowledge` 投影時鐘線索筆記、該道具 instance 移除（消耗）。此線索取得前投影時鐘不可互動。
- 營養棒 icon 資源：`res://assets/generated/sprites/items/synthetic_blueberry_nutrition_bar/icon.png`。
- 取得線索後投影時鐘變可互動（`required_knowledge` = 該線索）；互動 → 進入聲納模式，單次限時 10 秒。
- 聲納模式：玩家 1D 水平移動，越近隱藏插槽，短音播放頻率 + 視覺強度條越高；最強區帶寬為目標 X 座標 `±36px`，停留其中累積鎖定 `4 秒` → reveal 隱藏插槽。
- 聲納音效只使用短音 `res://assets/sound/sonar_ping.wav`；不做獨立 lock loop。播放頻率隨距離提升即可。
- 大型 UI（背包 / 筆記）開啟時聲納與 10 秒計時凍結，關閉後接續（不歸零）。
- 10 秒內未完成鎖定 → 聲納結束、無 reveal；可重新互動投影時鐘再啟動。
- reveal 後：播放 `res://assets/sound/hidden_slot_reveal.wav`；隱藏插槽常駐並變一般互動容器（沿用雙欄）；聲納自動關閉、不可再啟動，投影時鐘移除互動。
- 投影時鐘＝偽裝的偵測終端，同一物件（世界觀：主角當年自裝、掃自己藏的插槽）。
- 尚待 content / 實作判定：短音播放頻率曲線、視覺強度條與鎖定環樣式、線索與 prompt 文字潤飾。

#### 2-E 驗收意圖

插槽放入整合（電磁聲響 + 語音 MessageBox + add_knowledge → 開門）+ MESSAGE/CONFIRM overlay 重構——

- 把 `decoder_cube` 經雙欄 E 移動放入已 reveal 的 `apartment_slot` → 觸發：電磁聲響 + 語音 MessageBox（**疊在雙欄上**，底層凍結）。
- 語音 MessageBox 關閉後 `add_knowledge("identity_door_unlock_method")`；玩家於筆記（`J`）查得「我鎖上的門」，焦點回雙欄原格。
- **一次性**：語音與 add_knowledge 僅首次放入觸發；`deposit_locked` 使方塊鎖死、不可取出，再開雙欄不重播。
- 放入後插槽常駐、方塊鎖在格內可見、不可取出。
- 取得知識後大門 `has_knowledge` gate 通過；MVP 不轉場，靠近顯示 `door_opened`；未取得前顯示 `door_locked`。
- **MESSAGE / CONFIRM overlay 重構**：MessageBox 可疊在雙欄（CONTAINER）之上；既有 examine(2-A) / 解碼(2-B) MessageBox 行為不變（caller = NONE / INVENTORY）；1-B~1-E 大型 UI 互切與焦點行為回歸不破。
- 電磁聲響素材為 content，定稿前留待填。

#### 2-F 驗收意圖

筆記內容/操作收尾 + 公寓 BGM，皆為既有系統的小幅修正，無新機制——

**筆記內容修正：**

- Phase 1-C 遺留的測試長筆記（id `clue_long_scroll_test`，title「測試長筆記捲動」）改寫為正式氛圍獨白：
  - id 改名 `identity_rainy_night`、分類 `線索`→`身份`、title「雨還沒停」、body 換氛圍文字。
  - body 移除所有測試/捲動字樣與謎題暗示（不得提集齊線索、密碼鎖、手套清理工作、Page Down 等）。
  - 全文皆飄渺情境向、控制在全文欄一頁內。
- 文字定稿見 `subdocs/地點/主角公寓.md > 筆記預載`。

**筆記操作修正：**

- 筆記導航比照 `BagGrid`：A / D 切分頁、W / S 選筆記，方向鍵保留為鏡像（同時接 `move_*` 與 `ui_*`）。
- footer 改 `A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`。
- Page Up / Down 捲動移除（已於程式吞掉）；筆記皆一頁內，不需捲動。

**公寓 BGM：** 見「音訊（BGM）> 公寓 BGM」段。

#### 2-G 驗收意圖

開場獨白序列（新遊戲進公寓即播；建立「失憶甦醒」基調，銜接 B0 醒來）——

- **進場狀態**：場景載入即進入開場——玩家鎖在 `prone`（趴睡）姿態、世界輸入全擋、互動 prompt 隱藏、MessageBox 接管畫面顯示第 1 頁。
- **共 4 頁**，逐頁逐字播放（沿用既有 MessageBox 6 字/秒打字機）；**文字播放中不可跳過**，整頁顯示完成後才接受換頁。
- **換頁鍵**：WASDE / 方向鍵 / Esc，任一即進下一頁；最後一頁同鍵關閉 MessageBox。
- **換頁提示**：每頁文字跑完後淡入提示——前三頁「▼ 繼續」、第四頁「▼ 開始」；文字未跑完不顯示、不接受換頁。
- **分頁標題**（感官甦醒 / 空白的記憶 / 世界與我 / 自囚與尋回）置於該頁正文第一行，隨打字機一起逐字顯示，不另設標題樣式。
- **起身動畫**：第 4 頁一出現即播放 `get_up`（10 frame、約 1 秒、不循環），播完停最後一格切 `idle`；此頁文字同時以 6 字/秒續跑，文字與動畫互不等待（文字約 22 秒、動畫約 1 秒）。
- **結束**：第 4 頁文字跑完、玩家按鍵關閉 MessageBox → 解除輸入封鎖，玩家以站立 `idle` 開始自由操作。開場結束時角色一律停在站立 `idle`，不論動畫播放進度（被跳過則直接 snap 站姿）。
- **每次進場都播**（MVP 無存檔機制；日後加入存檔再 gate「只播一次」）。
- **隱藏測試捷徑**：開場期間 **1 秒內連按 3 下 T** = 立即補完當頁文字並跳下一頁；於第 4 頁則直接關閉開場、解鎖操作。此捷徑不顯示於提示，純測試用途。
- 沿用既有系統：MessageBox（2-E overlay 視覺 / 打字機）、`UIMode.is_world_input_blocked()` 輸入封鎖路徑、玩家 `AnimatedSprite2D`；無新 GameState API。開場接管邏輯（多頁 / 不可跳過 / 動畫鉤子 / T 捷徑）實作見 `開發設計方針.md > 2-G`。
- 開場文案全文見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`。

### Phase 3 — 公寓觸控化（手機可玩）

目的：**把現有公寓 B0–B9 全鏈 + 所有 UI 面板變成 iPhone 純觸控全程可玩**，不新增遊戲內容。觸控規範在此一次訂好，之後 Phase 4+ 新場景直接沿用。

平台策略（與 `技術概念.md > iOS 發佈路線` 一致，但更正一處）：

- 最終目標 **iOS**；MVP **不做 Android**。
- 日常開發 / 驗收在 **Windows 桌面版 + 滑鼠模擬觸控**（`emulate_touch_from_mouse`），零導出、最快迴圈。
- 真機在 **Mac + Xcode 免費簽名（Personal Team）**進開發者自己的 iPhone——**不需付費 Apple Developer Program**。付費（99 USD/年）只在 **TestFlight / App Store** 才需要。`技術概念.md` 把 99 USD 寫成 iOS 前置，僅適用「上架」階段，不適用「自測」。

方法：**螢幕虛擬手把灌既有 InputMap action，面板邏輯零改動**。因全遊戲互動早已只 listen 抽象 action（`move_*` / `interact_primary/secondary/tertiary` / `open_inventory` / `open_notebook` / `ui_cancel`），螢幕按鈕只要送出對應 action 即可，無須改任何面板程式。對應實作契約見 `開發設計方針.md > Phase 3`；操作清單見 `測試指南.md > Phase 3`。

子階段依賴為線性：3-A → 3-B → 3-C → 3-D（皆在 Windows）→ 3-E（需 Mac）。**3-C 完成 = 第一個可玩里程碑（PC 上純螢幕按鈕通關 B0–B9）**；3-E 完成 = iPhone 真機可玩。

#### 3-A 顯示 / 橫向 / 觸控模擬設定 驗收意圖

純 `project.godot` 設定，不碰遊戲邏輯——

- `display/window/handheld/orientation = "landscape"`（強制橫向）。
- `input_devices/pointing/emulate_touch_from_mouse = true`、`input_devices/pointing/emulate_mouse_from_touch = true`。
- 確認 `window/stretch/mode = "canvas_items"`、`window/stretch/aspect = "expand"`（aspect 未顯式設定者補上）。
- **回歸**：鍵盤全程仍可完整玩通 B0–B9，無任何退步；`test_runner` / `verify_game_state` 仍 PASS。
- 桌面版下滑鼠點擊會被當成觸控事件（為 3-B 之後的滑鼠模擬驗收鋪路）。

#### 3-B 世界模式觸控 驗收意圖

建立螢幕虛擬手把元件並接上**世界模式**用到的 action——

- `TouchControls` 元件掛在所有畫面最上層（高於遊戲與 UI），未來新場景自動繼承（不需每場景各加）。
- **左下方向鍵**送 `move_*` / `ui_*`：長按 `move_left` / `move_right` = 持續走路、輕點 = 走一步（`action_press` 按下 / `action_release` 放開）。
- **右下 E** 送 `interact_primary`。
- **右上常駐「背包」「筆記」**送 `open_inventory` / `open_notebook`。
- **顯示規則**：觸控裝置（`DisplayServer.is_touchscreen_available()` 為真）或 debug 強開旗標時顯示；PC 正式版隱藏（讓鍵盤玩家不見多餘按鈕）。
- **驗收**：PC + 模擬觸控、**純螢幕按鈕不碰鍵盤**——能走到大門 / 桌上電腦 / 左牆錄音機按 E examine、能點「背包」開背包、點「筆記」開筆記。

#### 3-C 面板模式觸控 驗收意圖

把同一組按鈕接上**面板內**的行為，並補返回鍵——

- **左下方向鍵**在面板內移焦點（`ui_*` / `move_*`，沿用既有面板導航）。
- **右下 E / R / T** 送 `interact_primary` / `interact_secondary` / `interact_tertiary`（= 移動 / 查看 / 丟棄·裝備，依面板語意）。
- **面板開啟時右上出現「X 返回」**送 `ui_cancel`；面板關閉時隱藏，回到世界模式的「背包 / 筆記」。
- **驗收（純觸控）**：背包內以方向鍵移焦點 → R 查看、T 丟棄、E 裝備 / 卸下；筆記以左右翻頁、上下選筆記；雙欄容器跨欄搬物品；X 關閉面板。
- **里程碑**：本階段完成後，**整個公寓 B0–B9 應能用純螢幕按鈕從醒來玩到開門**（含 2-G 開場「連點 T 三下」跳頁與 2-E 放入解碼方塊開門）。

#### 3-D Safe area + iPhone 比例排版 驗收意圖

排版適配，與「功能對不對」無關——

- 手把按鈕內縮進 `DisplayServer.get_display_safe_area()`，避開瀏海與底部 home indicator。
- 觸控目標 ≥ **44×44 px**（iOS HIG）。
- 在 iPhone 橫向比例（約 19.5:9）下，按鈕不重疊、不遮擋關鍵 UI（互動 prompt、MessageBox、背包格）。
- **驗收**：桌面視窗拉成 iPhone 橫向比例，逐項目視確認排版；真實瀏海遮擋待 3-E 真機才準。

#### 3-E 真機導出 + iPhone 校正 驗收意圖

需要 Mac，獨立於前四段——

- Mac + Xcode **免費簽名（Personal Team）**導出進開發者自己的 iPhone；**不需付費 Apple Developer Program**。
- **驗收（真機）**：iPhone 上純觸控完成 B0–B9；按鈕大小順手、效能可接受、safe area 在真實瀏海 / home indicator 上正確。
- **免費簽名已知限制**（記錄供排程預期，非缺陷）：憑證 7 天到期須重簽、同時最多 3 個免費簽名 app、裝置數有限、不能用 push / iCloud 等進階 entitlement（本單機遊戲皆不需）。
- 付費 99 USD/年僅在要走 TestFlight（發給他人測）或上架 App Store 時才需。

### Phase 4 — 跨場景架構化（Main Host / GameUI / SceneRouter）

目的：把目前綁在 `apartment_room.tscn` 的共用 UI 與場景切換責任抽到常駐宿主，讓「公寓 → 街區 / 地鐵站 → 公寓」成為穩定 runtime 能力。Phase 4 的主目標是架構，不是大量新增內容。

Phase 4 採用宿主架構方案 A：

```text
main.tscn
├── WorldRoot (Node2D)       # 動態載入目前關卡 scene
└── GameUI (CanvasLayer)     # 常駐共用 UI，不隨關卡釋放
```

`GameUI` 負責所有跨關卡共用 UI：

- 互動 Prompt。
- Inventory / Notebook。
- DualPane container UI。
- MessageBox / ConfirmDialog / ItemDetailModal。
- UIOverlay / WorldHUDLabel 等共用 HUD / overlay。

關卡 scene（如 `apartment_room.tscn`、未來 `street_stub.tscn` / `metro_station.tscn`）只負責：

- 背景、玩家、碰撞、互動物、spawn points。
- 場景專屬解謎狀態與內容事件（例如公寓 sonar / hidden slot / 開場獨白觸發）。
- 偵測目前最近互動物，並以資料 / signal 對 `Main` 或 `GameUI` 提出 UI / 轉場請求。

#### Phase 4 不做的完整系統

下列系統必須先考慮架構插槽，但 Phase 4 不實作完整功能，避免 scope 膨脹：

- 完整 NPC 對話系統。
- 完整任務系統。
- SaveSystem 真存檔 / 讀檔。
- MusicManager 完整 crossfade / 音量設定。
- 地鐵完整移動網路。
- 街區正式美術量產。

Phase 4 對這些未來系統只做 contract / metadata / thin stub，確保後續 Phase 5+ 不需要再重拆宿主架構。

#### 核心架構原則

- `GameState` / `UIMode` 繼續作為 Autoload；`GameUI` 不做 Autoload，放在 `main.tscn` 常駐。
- SceneRouter 第一版可掛在 `main.gd` 或獨立節點；不要求立刻做成 Autoload。若後續 SaveSystem / debug teleport / 地鐵網需要全域呼叫，再升級。
- 場景切換使用穩定 `scene_id + entry_point_id`，不得只依賴 file path。
- `entry_point_id` 必須承載「從哪裡進場」語意；不得用同一個入口同時代表新遊戲開場與回程。
- Level 不直接存取 `GameUI` 內部 node path；只透過 signal / Main mediator / GameUI 公開 API 發出請求。
- TouchControls 不再查目前 level scene 的 `UI/...` 固定路徑；改查 `GameUI` 公開 API 或 `UIMode`。
- 任何大型 UI 開啟時，互動 Prompt 由 `GameUI` 統一隱藏；回到 `UIMode.NONE` 後依目前互動物資料恢復。
- 轉場前後 `UIMode` 必須回到 `NONE`，避免把上一個場景的 UI 模式帶進新關卡。
- 公寓開場獨白只在 `apartment:wake_bed` 入口播放；從街道返回公寓等回程入口必須跳過開場，玩家直接站立 `idle` 出現在對應 spawn point。
- 轉場後的環境音 / BGM 屬於新場景；公寓大門真轉場後，不再由公寓門事件播放「門後 / 街道」音樂。Phase 4 可讓 `street_stub` 靜音或由 stub 自己播放簡單 BGM，完整 MusicManager 留後續。

#### 未來系統 contract（Phase 4 先定，完整功能後做）

互動資料必須能涵蓋 message / container / dialogue / transition / quest event，避免未來 NPC、任務、地鐵加入時重拆互動系統。

最小互動資料語意：

```text
type              = "message" | "container" | "dialogue" | "transition"
id                = 穩定互動 id
prompt_text       = GameUI prompt 顯示文字
message_id/text   = MessageBox 內容來源
container_id      = 外部容器 id
dialogue_id       = 未來 DialogueSystem 使用，Phase 4 可 placeholder message
target_scene_id   = SceneRouter 目標場景 id
entry_point_id    = 目標場景 spawn point id
quest_event_id    = 未來 QuestManager 使用，Phase 4 可 noop / log
```

Scene metadata 也必須先有最小 registry 概念：

```text
scene_id           # 可由 registry key 表示
scene_path
default_entry_point_id
music_id            # Phase 4 可不切音樂，但先固定欄位
```

此 registry 後續服務 SceneRouter、SaveSystem、地鐵移動、debug teleport、MusicManager。

Phase 4 先固定下列 entry point 語意：

```text
apartment:wake_bed       # 新遊戲 / 從主選單開始；播放開場，玩家在床上 prone
apartment:from_street    # 從街道回公寓；跳過開場，玩家 idle 站在門邊
street_stub:from_apartment # 從公寓大門出去；玩家出現在街道入口
```

`apartment.default_entry_point_id` 應為 `wake_bed`。`street_stub` 回公寓的互動目標應為 `target_scene_id="apartment"`、`entry_point_id="from_street"`。

#### 子階段

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 4-A0 | root 散落的 `.gd` / `.tscn` 搬進對應資料夾，更新引用 | 檔案就位、引用無誤、headless 回歸 PASS；純路徑變更，無行為改動 |
| 4-A | 建立 `main.tscn` / `WorldRoot` / 最小 SceneRouter / SceneRegistry | 遊戲從 `main.tscn` 啟動並載入公寓；行為與 Phase 3 末狀態一致 |
| 4-B | 抽出 `GameUI.tscn`，把共用 UI 從 `apartment_room.tscn` 移出 | Inventory / Notebook / DualPane / MessageBox / Prompt 均由 `GameUI` 管理 |
| 4-C | 定義 Level ↔ Main ↔ GameUI interaction contract | Level 只送資料 / signal，不直接碰 `GameUI` node path |
| 4-D | TouchControls 改接 GameUI contract | 觸控按鈕顯示規則不再依賴 `ApartmentRoom/UI/...` |
| 4-E | 公寓完整遷移 + B0-B9 回歸 | 公寓全鏈、overlay caller restore、純鍵盤與純觸控路徑不退化 |
| 4-F | 新增第二場景 stub + 大門真轉場 | 公寓大門可轉到 `street_stub`，並可回公寓；GameUI 不閃爍 / 不重建 |
| ~~4-G~~（已取消） | ~~未來系統 contract pass~~ | ❌ 已取消，改由 Phase 5 做真系統；架構契約已由 4-A~4-F 滿足。詳見下方「4-G 驗收意圖（已取消）」與 Phase 5。 |

#### 4-A0 驗收意圖

目錄整理，與「功能對不對」無關——把 root 散落的關卡 / 角色 / 元件腳本搬進語意化資料夾，為後續 main.tscn / GameUI / 第二場景的引用先收斂路徑。

- root 不再有遊戲用 `.gd` / `.tscn`（測試與設定檔除外）；`apartment_room` 系列、`player.gd`、`interactable_area.gd` 各就定位。
- 搬移為**純路徑變更**：不改任何遊戲邏輯、節點結構、資源 uid；唯一允許的內容變更是 `ext_resource` / `load()` 路徑字串。
- 每個 `.gd` 的 `.gd.uid` sidecar 跟著同檔搬移，uid 值不變。
- 場景自身 uid 不變，因此 `project.godot` 的 `run/main_scene`（uid 引用）在本子階段**不需修改**。
- 搬移後 Godot editor 開啟所有場景無 missing resource；headless 回歸（`test_runner.tscn` / `verify_game_state.gd`）PASS。
- 本子階段獨立成一個 commit，diff 只含檔案移動與路徑字串更新，不與 4-A 之後的功能改動混在同一 commit。

#### 4-A 驗收意圖

- `project.godot` 主場景改為 `main.tscn`。
- `main.tscn` 啟動後將 `apartment_room.tscn` instance 到 `WorldRoot`。
- 公寓開場、玩家移動、互動、BGM、現有 UI 行為與 Phase 3 末狀態一致。
- SceneRegistry 至少包含 `apartment` 與預留的 `street_stub` 條目；`apartment.default_entry_point_id == "wake_bed"`，並另有 `from_street` 回程入口。
- SceneRouter API 使用 `scene_id + entry_point_id`，不把門目標寫死為 file path。
- SceneRouter 必須在關卡 `_ready()` 啟動開場邏輯前交付 entry point（例如 add child 前呼叫 `prepare_entry_point()`），避免回公寓時先重播開場。

#### 4-B 驗收意圖

- `InventoryPanel` / `NotebookPanel` / `DualPaneContainer` / `MessageBox` / `ConfirmDialog` / `ItemDetailModal` / `PromptPanel` / `UIOverlay` / `WorldHUDLabel` / `FloatingToast` 由 `GameUI` 持有。
- `apartment_room.tscn` 不再持有上述共用 UI 節點。
- `UIMode` 的所有模式與 overlay caller restore 行為不變。
- `GameUI` 常駐於 `main.tscn`，切換關卡時不銷毀、不重建。
- FloatingToast 不再掛到 `current_scene/UI`；所有 toast 均由 `GameUI` 生成 / 掛載，以維持層級與 anchor 位置一致。

#### 4-C 驗收意圖

- Level 可通報目前最近互動物，`GameUI` 顯示 prompt。
- Level 可請求 message、container、transition；請求由 Main / GameUI / SceneRouter 分派，不由 Level 直接操作 UI internals。
- 多個互動物重疊時仍由 Level 選最近者，`GameUI` 只負責顯示。
- Prompt 在 `UIMode.NONE` 且有目前互動物時顯示；大型 UI / overlay 開啟時隱藏。

#### 4-D 驗收意圖

- TouchControls 在世界模式判斷 E 是否顯示時，不讀 `ApartmentRoom.current_interactable` 或 `UI/...` 固定路徑；改讀 `GameUI` / Main contract。
- TouchControls 在 Inventory / Container mode 判斷 E/R/T 顯示時，不讀 level scene 下的 UI node；改由 `GameUI` 提供 focused item / action availability。
- TouchControls 不讀 Level 私有開場欄位（如 `_opening_monologue_active`）；開場 / 轉場 / 世界輸入封鎖狀態由 `GameUI` 或 Main 的公開 API 提供，確保 PC 觸控 toggle 不會在開場期間誤顯示。
- Phase 3 的 PC 觸控開關、safe area、純觸控 B0-B9 操作不退化。

#### 4-E 驗收意圖

- 公寓 B0-B9 全鏈仍可完成。
- 插槽放入 `decoder_cube` 後，MessageBox 仍能疊在 DualPane 上方，關閉後焦點回原雙欄。
- 開場 MessageBox、一般 examine MessageBox、物品 R/E 觸發 MessageBox、ConfirmDialog 均不破壞 caller restore。
- 公寓場景專屬 sonar / hidden slot / BGM 動態仍留在公寓邏輯，不被誤搬進 `GameUI`。
- 開場多頁流程仍由公寓場景控制頁數、T 三連跳、prone/get_up/idle 動畫；`GameUI` 只提供 MessageBox 顯示通道與頁尾提示等低階能力，不要求一般 `show_message(text, on_closed)` 承載開場全部狀態。
- 回到 `apartment:from_street` 時不得重播開場；玩家必須直接站立於門邊 entry point，且 `UIMode == NONE`。
- `apartment:wake_bed` 入口由開場流程控制床上 prone 起始位置；entry point 定位不得覆蓋該姿態 / 位置。

#### 4-F 驗收意圖

- 新增第二場景 stub（暫定 `street_stub`），只需 placeholder 背景、spawn point、回公寓互動物、1-2 個普通 examine 互動物。
- 公寓大門取得 `identity_door_unlock_method` 後不再只顯示通關文字；可請求 SceneRouter 轉到 `street_stub:from_apartment`。
- `street_stub:from_apartment` 必須定位到街道入口 SpawnPoint，不得落在場景原點。
- `street_stub` 可返回 `apartment:from_street`，且不重播公寓開場。
- 背包、筆記、GameState 資料跨場景保留。
- 轉場後 `UIMode == NONE`，舊 prompt / message / container UI 不殘留。

#### 4-G 驗收意圖（已取消）

> ❌ **本子階段已取消，以下驗收項作廢、不再執行。** 其架構契約已由 4-A~4-F 滿足；`dialogue_id` / `quest_event_id` 不做 placeholder，直接由 Phase 5 做真系統。保留本段僅為歷史脈絡。

- NPC-like interactable 可帶 `dialogue_id`，Phase 4 可暫時轉成 placeholder MessageBox，確認資料路徑存在。
- 互動資料可帶 `quest_event_id`，Phase 4 可 noop / log，不新增完整 QuestManager。
- Scene metadata 可帶 `music_id`，Phase 4 可由各場景自己處理 BGM 或靜音，不新增完整 MusicManager；但不得讓公寓門事件在轉場後繼續播放屬於 street 的音樂。
- SceneRouter 與 SceneRegistry 的 `scene_id + entry_point_id` 可被未來 SaveSystem / 地鐵移動 / debug teleport 重用。
- 沒有任何 Phase 4 實作需要寫死「只有公寓一個場景」。

---

### Phase 5 — NPC + 對話系統（真系統）

目的：在街道場景 `apartment_entrance` 放入第一個可對話 NPC「晚」，並建立可重用的**分支對話系統**（玩家選項 + 條件 / 效果 + 故事旗標）。Phase 5 把 Phase 4 已取消的 4-G placeholder 收掉，改為真正實作對話與故事旗標；任務（Quest）與存檔（Save）仍只預留資料路徑，不在 Phase 5 完整實作。

> 4-G（原 future-system contracts pass）已取消：其架構契約（`scene_id + entry_point_id` 通用轉場、各場景自管 BGM、SceneRouter 持穩定 scene id）已由 4-A~4-F 滿足；`dialogue_id` / `quest_event_id` 的 placeholder 屬丟棄工，故不單獨實作，直接由 Phase 5 做真系統。
>
> 第一個 NPC「晚」的角色定位、語氣與第一段對話定稿見 `subdocs/人/晚.md`。

#### Phase 5 範圍

做：

- 分支對話系統（節點 / 選項 / 條件 / 效果 / 條件路由）。
- 故事旗標 store（bool / int，含好感度 `affinity_wan`）。
- 對話 UI（立繪槽 + 名字 + 內文 + 選項列）。
- `UIMode.DIALOGUE` 模式與觸控對話路由。
- NPC「晚」落地 `apartment_entrance`，第一段對話可玩、重講走短版、好感度 gate 情報可達。

不做（僅預留資料路徑，不得寫死封閉）：

- 完整任務系統 / QuestManager：對話 effect `add_gleaner_lead` 先 log / noop，不得把任務狀態塞進 `GameState.knowledge`。
- 真存檔 / SaveSystem：故事旗標設計成可序列化即可，本 Phase 不做存讀檔。
- 立繪 / 角色圖正式美術：由使用者後續提供；5-A~5-D 用 placeholder 槽，缺圖不得崩潰。
- 戀愛線實際內容：僅以同一 `affinity_wan` 數值與額外旗標預留門檻，不實作劇情。

#### 對話系統資料語意（驗收層）

對話樹節點最小語意：

```text
speaker     # 顯示名字 / 對應立繪；未知名可顯示 "？？"
text        # 該節點台詞
effect[]    # 進入節點時套用：set_flag / add_int / add_gleaner_lead
choices[]   # 玩家選項；每項含 label / goto，可帶 condition / effect
goto        # 無 choices 時自動前進；可為條件路由（依序取第一個成立的 target）
condition   # 用於選項顯隱或 goto 路由；缺省 = 恆真
```

- `condition` 比較對象為故事旗標：`{flag, op, value}`，`op ∈ {==, !=, >, <, >=, <=}`；旗標不存在時視為 `0 / false`。
- 條件路由：同一個玩家選項可導向「依好感度分流」的 gate 節點，由 `goto` 條件清單決定走 `intel`（`affinity_wan >= 2`）或 `intel_locked`。
- `effect` 種類：`set_flag`（bool）、`add_int`（如 `affinity_wan += 1`）、`add_gleaner_lead`（Phase 5 為 log / noop）。

#### 故事旗標 store（驗收層）

- `GameState` 持有故事旗標集合，可存 bool 與 int 兩種值。
- `affinity_wan`（int）即好感度，對話 `condition` / `effect` 共用同一值；戀愛線未來以更高門檻 + 額外旗標推進，不需改 schema。
- 故事旗標與 `knowledge` / `notes` 完全分離：對話 / 任務狀態不得塞進 `knowledge`。
- 故事旗標可被未來 SaveSystem 序列化還原。

#### 子階段

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 5-A | 故事旗標 store + 對話資料 schema + `DialogueRunner`（純邏輯，無 UI） | headless 餵「晚」的對話樹模擬選擇，旗標 / `affinity_wan` / 分支路由正確 |
| 5-B | 對話 UI + `UIMode.DIALOGUE` + interactable `dialogue_id` + 接線 | 純鍵盤可玩完整第一段對話，分支正確，結束後 `UIMode == NONE` |
| 5-C | TouchControls 對話路由 | 純觸控可移選項 / 確認 / 推進，走完一段對話含分支 |
| 5-D | 「晚」落地 `apartment_entrance` + placeholder 美術 + 手動驗收 | 出公寓遇晚、三分支、重講短版、`affinity_wan >= 2` 情報開鎖 |

依賴順序：5-A → 5-B → 5-C → 5-D。先把 Runner 純邏輯 headless 打穩，再上 UI 與觸控，最後內容落地。

#### 5-A 驗收意圖

- `GameState` 新增故事旗標 API（`set_flag` / `get_flag` / `has_flag` / `add_int` + `flag_changed` signal）；故事旗標與 `knowledge` 分離。
- 「晚」的第一段對話（含重講短版）完整轉成 GDScript const dict 對話樹資料。
- `DialogueRunner` 能載入樹、回報當前節點（speaker / text / choices）、依玩家選擇前進、評估 `condition`、套 `effect`、到 terminal 後發出 `finished`。
- 條件路由正確：`affinity_wan >= 2` 走 `intel`、不足走 `intel_locked`。
- effect 正確：`set_flag met_wan` / `knows_wan_name`、`add_int affinity_wan`、`add_gleaner_lead` 為 log / noop 且不寫入 `knowledge`。
- 5-A 不渲染任何 UI；全程 headless 可驗證（納入 `test_runner`）。

#### 5-B 驗收意圖

- `UIMode` 新增 `DIALOGUE` 模式；對話期間主角凍結；進入 / 結束模式切換正確，結束回 `NONE`。
- `interactable_area` 新增 `dialogue_id` 欄位。
- 走近 NPC 顯示 Prompt；按 `E` 觸發對話：Level emit `interaction_requested({type:"dialogue", dialogue_id})`，Main 轉 `GameUI.start_dialogue()`。
- DialoguePanel 顯示立繪槽 / 名字 / 內文 / 選項列；鍵盤 上 / 下 移選項焦點、`E` 確認 / 推進。
- 對話實際由 `DialogueRunner` 驅動；選項分支與 effect 在 UI 流程中正確反映。
- 對話結束後 panel 關閉、`UIMode == NONE`、Prompt 依目前互動物恢復、無殘留。
- Level 不直接存取 `GameUI` 內部 node path（沿用 4-C contract）。

#### 5-C 驗收意圖

- 觸控對話：D-pad 上 / 下移選項焦點、右下確認鍵＝選取 / 推進，沿用 GameUI 公開查詢（不讀固定 node path）。
- 對話期間，世界移動鍵與其他 UI 觸控鍵隱藏 / 不誤觸底層。
- 純觸控可從第一句走到對話結束，含選分支。

#### 5-D 驗收意圖

- 「晚」以 NPC interactable（`interaction_id` + `dialogue_id="wan"`）+ sprite 子節點（placeholder）放在 `apartment_entrance` 暗巷口；立繪 / 角色圖待使用者提供，缺圖時 panel / 場景以 placeholder 呈現不崩。
- 對話入口恆為 `start` 節點（條件路由）：初次 `met_wan` 未設 → 走 `first_meet` 第一段；設 `met_wan` 後 → 走 `retalk` 短版。入口選擇由對話樹資料決定，不在 GameUI 寫 NPC 專屬入口邏輯。
- `affinity_wan >= 2` 後 `intel` 可達並觸發 `add_gleaner_lead`（log）；不足走 `intel_locked`。
- 跨場景：背包 / 筆記 / 故事旗標保留；回公寓再回街道，「晚」的旗標狀態不重置、不重播任何開場。
- 場景 dispatch 以互動物的 `dialogue_id` 欄位泛化判斷，不為單一 NPC 寫死 `interaction_id` 字串分支。

---

### Phase 6 — SaveSystem（手動多槽存檔）

目的：把 Phase 1~5 一直「設計成可序列化、但不做存讀檔」的全域狀態，真正落成可手動存 / 讀的 SaveSystem。新增最小標題畫面（New Game / Load Game）與遊戲中暫停選單，支援多存檔槽。Phase 6 只做存檔；任務（Quest / QuestManager）仍只維持資料路徑（對話 effect `add_gleaner_lead` 維持 log / noop），留待 Phase 7+。

> 既有伏筆收口：`規格書` 開場獨白「每次進場都播（MVP 無存檔機制；日後加入存檔再 gate 只播一次）」由 Phase 6 解決——開場獨白只在 New Game 播，讀檔還原直接回到存檔站位、不重播。

#### Phase 6 範圍

做：

- `SaveSystem` Autoload：擷取 / 還原全域狀態的純邏輯（`capture()` / `apply()`）+ 讀寫存檔檔案。
- 序列化全域可變狀態 + 目前場景 + 玩家精確座標。
- 最小標題畫面：New Game / Load Game。
- 遊戲中暫停選單：Resume / Save / Load / 回標題。
- 多存檔槽（先給 10 槽），Save / Load 共用同一槽列表 UI。
- 讀檔回場：在還原場景內直接 set 玩家座標，跳過 entry point 副作用。

不做（僅預留，不得寫死封閉）：

- 完整任務系統 / QuestManager：`add_gleaner_lead` 維持 log / noop，任務狀態不得塞進 `knowledge`。
- autosave：本 Phase 純手動存讀；預留 `can_save_here` 旗標（SaveSystem 屬性、預設 `true`，**非 API 參數**），由「開 Save 入口」檢查，未來做「危險區不能存」再開。
- 對話 / 開場獨白暫態序列化：存檔只在 `UIMode == NONE` 觸發，存點永遠落在乾淨狀態。
- 設定畫面 / 多語系 UI / 存檔縮圖：標題畫面只含 New Game / Load Game，不擴張。

#### 存檔資料語意（驗收層）

存檔 payload = `GameState` 全可變狀態，**含 `_last_instance_id`**（不存它讀檔後 `generate_instance_id` 會與既有實例撞 id）：

```text
credits, inventory, equipment, knowledge, notes,
external_containers, external_container_configs,
apartment_initialized / apartment_sonar_revealed /
apartment_slot_unlocked / apartment_beyond_door_bgm_triggered,
story_flags（含 affinity_wan / met_wan…）, _last_instance_id,
current_scene_id, player 座標（純側捲只需 x，可帶 facing）
```

- **不序列化** code-owned 常數（`STORY_NOTES` / `STORY_MESSAGES` / `ITEMS_DB` / `EQUIPMENT_LIMITS`）；這些由 code 重建，存了會在改版時蓋掉新文案 / 新物品。
- 序列化用 Godot 原生 `var_to_str` / `str_to_var`（非 JSON、非 ResourceSaver），型別精確 round-trip、零 Resource class boilerplate。詳見 `技術概念.md > 存檔策略`。
- 存檔檔案結構 `{ meta: {...}, data: {...} }`：`meta` 含 `version` / `timestamp` / `scene_name` / `credits`，供槽列表只讀 meta 顯示、不套用 data。
- 多槽：檔名 `user://save_01.sav … save_10.sav`，各槽獨立一份。

#### 邊界與失敗處理（驗收層）

- `meta.version` 不符或檔案損毀 / parse 失敗 → 跳訊息、不 crash、不套用。
- 載入採 **validate 前置（兩段，皆在套用 GameState 之前）**：`SaveSystem.validate` 驗 payload shape / `meta.version`；Main / SceneRouter 驗 `current_scene_id` 存在於其 scene registry（`SCENES` 由 Main 持有，不下放給 SaveSystem 查）。任一不過 → 拒絕載入並提示，**GameState 不被半套用**（維持載入前狀態、不黑屏）。
- 還原的玩家 x 超出場景範圍 → clamp 回場景邊界。
- **New Game 重置**：開新局前必重置全域可變狀態（credits / inventory / equipment / knowledge / notes / containers / story_flags / apartment_* 旗標 / 實例 id 計數）；「Load → 回標題 → New Game」不得殘留上一局狀態。
- 「回標題」時若當前進度未存檔 → 以既有 `ConfirmDialog` 二次確認。
- 存檔只在 `UIMode == NONE` 可觸發（暫停選單僅世界模式可開）；Save 入口檢查預留 gate `can_save_here`（Phase 6 恆 true）。
- **標題畫面不得出現觸控控制**：`TouchControls` 為常駐 autoload，標題頁（非 gameplay 場景）須隱藏其 D-pad / 功能鍵 / PC 觸控 toggle。

#### 子階段

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 6-A | `SaveSystem` Autoload：`capture()` / `apply()` 純邏輯 + `var_to_str` 讀寫（無 UI） | headless round-trip：存 → 改狀態 → 讀 → assert 全域狀態（含 `_last_instance_id`）還原 |
| 6-B | 讀檔回場路徑：載入時進場景、直接 set 玩家座標、壓掉 entry point 副作用；New Game vs Load 開機分流 | headless 驗還原後 `current_scene_id` 與玩家座標正確；新遊戲播開場、讀檔不播 |
| 6-C | 多槽模型：檔名規則 + `meta` header + 列舉 10 槽 API（只讀 meta 不套用） | headless 驗 list / 存指定槽 / 覆蓋槽 / 損毀槽優雅失敗 |
| 6-D | 最小標題畫面 + 開機流程（New Game / Load Game） | 啟動進標題；New Game 進 `wake_bed`、Load Game 列槽 → 選槽還原 |
| 6-E | 暫停選單 + Save / Load 槽列表 UI + 覆蓋確認 + `UIMode==NONE` gating + TouchControls 接線 | 自由探索開暫停選單存讀；覆蓋走 `ConfirmDialog`；面板 / 對話 / 獨白中無法開選單 |
| 6-F | 邊界處理（版本 / 損毀 / 場景缺失 / 座標出界）+ 回標題確認 + GUI 走查驗收 | 各失敗情境不 crash；GUI 純走查存→讀回到同一站位與世界狀態 |

依賴順序：6-A → 6-B → 6-C → 6-D → 6-E → 6-F。先把 capture/apply 純邏輯與回場路徑 headless 打穩，再上槽位、標題、暫停選單 UI，最後補邊界與 GUI 驗收。

#### implementer 契約（已於 `開發設計方針.md > Phase 6` 釘定）

下列契約已在設計方針給出簽名 / 接線，含「開工時定案」的建議預設；本段僅列索引，細節以設計方針為準：

- player 座標存取點（`get_save_x` / `set_save_x` + facing）—— 設計方針 6-B。
- SceneRouter `transition_to` 的 restore payload + 「跳過 entry point 副作用」旗標 —— 設計方針 6-B。
- `UIMode`：設計方針 6-E 建議**新增 `PAUSE`**、槽列表作其 overlay（或複用既有 overlay caller-還原機制）；最終於開工時定案。
- 標題畫面落點：設計方針 6-D 建議**作為開機初始場景**（與 main.tscn 世界互不污染）；最終於開工時定案。

---

### Phase 7 — QuestManager + 第一個任務 vertical slice

目的：把 Phase 5/6 預留的任務資料路徑落成最小真系統，並用第一條可玩任務 `alley_backrooms_3f` 驗證任務啟動、進度推進、工作筆記更新、條件式場景入口、攀爬移動、可錯過隱藏物品與回報完成流程。Phase 7 不做大型任務 UI；任務顯示暫時投影到既有筆記 UI 的「工作」分類。

#### Phase 7 範圍

做：

- `QuestManager` 最小真系統：`start` / `advance` / `complete` / `fail` 與任務狀態查詢。
- `GameState.quest_states` 序列化；任務狀態存讀檔 round-trip。
- 任務狀態與「工作」筆記同步：工作筆記是 quest state 的顯示投影，不是任務狀態來源。
- 對話 effect 接入：晚的對話在正確路徑下啟動 `alley_backrooms_3f`，並新增 / 更新工作筆記。
- `apartment_entrance` 後巷偵查事件：未接任務維持原互動文字；接任務後互動改為危險暗巷提示並推進任務。
- 公寓窗戶條件式入口：必須接任務且偵查後巷、筆記更新後，窗戶才可互動。
- 新增 `apartment_fire_escape` 外牆 / 簡易陽台場景：兩棟樓之間有中央空隙，主角從右棟四樓窗戶出來，沿右棟平台到梯子，下爬一層到右棟三樓，再走 3F 天橋跨過 gap 到左棟三樓平台，抵達目標箱子。
- 梯子攀爬模式：玩家可在梯子上上下移動；不是自由 platforming，不新增跳躍。
- 外牆場景禁存：`apartment_fire_escape` 內 `SaveSystem.can_save_here == false`，回公寓 / 街道後恢復。
- 取得 A 物品「早期 AI 助理啟用盒」；R 查看 A 才取得 B 物品「舊式 AI 授權模組」。
- 回報晚時交出 A 完成任務；若未先查看 A，B 物品錯過（可留待未來以 credits 高價取得）。

不做（僅預留，不得寫死封閉）：

- 大型 Quest UI / 任務列表 / HUD 追蹤 / 地圖 marker。
- 敵人、潛行、警戒值、幫派戰鬥或被打劫系統；後巷危險只用文字與任務路由表現。
- 真實多樓層 platforming、跳躍、跌落傷害或梯子通用化到所有場景。
- B 物品的實際用途；Phase 7 只取得並保留，未來用於舊 AI 終端 / 維修門 / 工程模式等。
- 第二 NPC、回收商、地鐵站正式任務；Phase 7 第一條任務只回報晚。
- 通用 Quest reward API：不做。`alley_backrooms_3f` 的 credits 報酬與好感獎勵改於 **7-K** 直接以對話 effect 發放（見下方 7-K），不另建 reward 系統。（7-A~7-J 階段任務完成不發 credits / item reward；7-K 起調整。）

#### Quest state 與工作筆記語意

任務狀態由 `QuestManager` / `GameState.quest_states` 持有，最小資料語意：

```text
quest_states = {
  quest_id: {
    status: "inactive" | "active" | "completed" | "failed",
    step: String,
    flags: Dictionary
  }
}
```

- `knowledge` / `notes` 不得作為任務狀態來源；任務狀態不得塞進 `knowledge` 假裝完成。
- 任務內部旗標一律放在 `quest_states[quest_id].flags`；不得用 `story_flags` 存同一條任務的子狀態。
- 每次 quest state 變更必須同步刷新對應「工作」筆記（同 `note.id` 覆寫），例如 `quest_alley_backrooms_3f`。
- 工作筆記內容會隨進度改變：接任務、偵查後巷、發現窗路、取得 A、完成任務皆可更新同一筆工作筆記。
- `status` 可映射到筆記 `status`：任務 active → note active；completed / failed → note status 同步。
- 工作筆記查表規則：`status == completed/failed` 時優先使用 status 對應筆記；`status == active` 時使用目前 `step` 對應筆記。不得把 completed 混成 step，也不得保留不會被設定的死 step。
- Phase 7 對話條件需可讀取 quest state 與背包持有狀態；晚的交任務分支必須同時檢查 `alley_backrooms_3f` active、step 為 `found_activation_box`、玩家持有 A。

#### `alley_backrooms_3f` 任務流程

```text
晚的對話建立信任
-> 得知後巷三樓 lead
-> QuestManager.start("alley_backrooms_3f")
-> 工作筆記出現：「暗巷三樓的舊物」
-> 玩家到 apartment_entrance 後巷互動
-> 後巷顯示危險暗巷描述，不開放從街道進入
-> QuestManager.advance(..., "checked_alley")
-> 工作筆記更新：後巷不能硬走，主角想起自己住在四樓，窗外連通平台可能可繞到三樓
-> 回公寓，窗戶變為可互動
-> 從公寓右側窗戶進入 apartment_fire_escape:from_window
-> 在右棟四樓平台走到梯子，爬下一層到右棟三樓
-> 走過 3F 天橋跨越兩棟樓之間的空隙
-> 抵達左棟三樓平台，往左到亮燈住戶窗旁的箱子
-> 搜索時提示盡量不發出聲音
-> 取得 A「早期 AI 助理啟用盒」
-> 可選：R 查看 A，取得 B「舊式 AI 授權模組」
-> 原路爬回四樓窗戶，按 E 回公寓
-> 回報晚並交出 A，任務完成
```

後巷危險文字意圖：暗示該區陰暗巷內可能有幫派 / 歹徒 / 打劫風險，但不明說成戰鬥系統。例如：

```text
巷子深處沒有燈，連雨聲都像被吞掉。這種監視器照不到的地方，通常不留給迷路的人走。牆邊幾個影子沒有動，卻都像在等你先動。
```

箱子搜索文字意圖：箱子旁窗戶亮著燈，表示裡面有人住；主角搜索時必須盡量安靜。例如：

```text
窗裡還亮著燈，薄窗簾後偶爾有人影晃過。你放慢動作，把箱蓋一點一點掀開，盡量不讓潮濕的鉸鏈叫出聲。
```

#### A/B 物品語意

A：`early_ai_assistant_activation_box` / 早期 AI 助理啟用盒

- 保存良好的早期 AI 助理啟用套件，具有收藏價值，因此晚想拿到。
- 拾取時立刻跳訊息，提示玩家它似乎有異常重量 / 夾層，但不直接教學。
- 任務回報需要交出 A。

B：`old_ai_authorization_module` / 舊式 AI 授權模組

- 藏在 A 的夾層內，只有 R 查看 A 才取得。
- 看起來不像收藏品，而是可在某些舊 AI 終端、維修門、工程模式或清運系統中派上用場的硬體授權鑰。
- 查看 A 後 A 不消失；A 仍留在背包並可交任務，B 保留在主角背包。
- 若玩家未查看 A 就交給晚，B 不補發；未來可用 credits 高價取得，Phase 7 不實作該補救路徑。

#### 晚的回報分支條件

交任務選項只在下列條件全部成立時顯示：

```text
quest_status("alley_backrooms_3f") == "active"
quest_step("alley_backrooms_3f") == "found_activation_box"
has_item("early_ai_assistant_activation_box") == true
```

- 若任務未接、尚未取得 A、A 不在背包、或任務已 completed，交任務分支不得顯示。
- 完成時先移除 A；若移除失敗，不得呼叫 `QuestManager.complete()`。
- 任務 completed 後，晚只顯示完成後短句或一般 retalk，不再顯示交任務分支。

#### `apartment_fire_escape` 場景語意

- `scene_id = "apartment_fire_escape"`，唯一 entry point：`from_window`。
- 從公寓右側窗戶進入，玩家出生在右棟四樓平台。
- 場景包含右棟四樓平台、右棟梯子、右棟三樓平台、中央空隙 gap、3F 天橋 / 簡易連通橋、左棟三樓平台、左側目標箱子、亮燈住戶窗。
- 3F walk-line 在兩棟樓之間不連續，必須由天橋 / 連通橋 collision 承接；玩家不是直接走過空地。
- 玩家必須原路返回：從左棟三樓走回天橋，回到右棟三樓，爬回右棟四樓，走回右側窗戶，按 E 轉場回公寓。
- 公寓需新增回程 entry point `from_fire_escape`，讓玩家回來時落在窗邊，不落在門邊。
- 外牆場景是目前唯一垂直多層場景；相機不得沿用只看單一 walk-line 的固定 y。必須讓相機在 4F / 梯子 / 3F 之間垂直跟隨並 clamp 到場景 bounds，或採用足以同時容納兩層動線的固定構圖。
- 梯子攀爬期間，`E` 的優先序為「先離開梯子 / snap 到平台」；離開梯子後下一次 `E` 才能觸發窗戶或箱子互動。climb mode 中不得同時觸發一般互動物。
- 外牆場景期間不可手動存檔；此限制避免 Phase 7 擴大 SaveSystem 到完整 `Vector2` / climb state 存讀。

#### 子階段

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 7-A | `QuestManager` 最小真系統 + `GameState.quest_states` + 存檔整合 | headless 驗 `start` / `advance` / `complete` / `fail`；任務狀態存讀檔 round-trip；任務 flags 不使用 `story_flags` / `knowledge` |
| 7-B | 晚的對話接任務 | 沿用既有 `affinity_wan >= 2` 的 `intel` 節點啟動 `alley_backrooms_3f`；DialogueRunner 可評估 quest / inventory 條件；工作筆記「暗巷三樓的舊物」出現；重複對話不重開任務 |
| 7-C | 街道後巷偵查事件 | 未接任務時後巷沿用原文字；接任務後互動顯示危險暗巷描述、推進 `checked_alley`、更新工作筆記 |
| 7-D | 公寓窗戶條件式入口 | 僅 `checked_alley` 後窗戶可互動；互動轉場 `apartment_fire_escape:from_window`；回程落在 `apartment:from_fire_escape` |
| 7-E | `apartment_fire_escape` 場景骨架 | 右棟四樓平台、梯子、右棟三樓平台、中央 gap、3F 天橋、左棟三樓平台、左側箱子、亮燈窗戶配置完成；唯一 entry point 為 `from_window` |
| 7-F | 梯子攀爬 + 天橋步行 + 相機 + 外牆禁存 | 玩家可在右棟梯子上下爬、在 3F 天橋跨 gap 到左棟平台並原路返回；相機可看見 4F/3F 攀爬動線；外牆場景 Save 入口不可用 / 提示此處不能存；離開後恢復可存 |
| 7-G | 三樓箱子搜索 + A 物品取得 | 搜索文字包含安靜動作與住戶亮燈壓力；背包有空間時才取得 A、推進任務與更新筆記；背包滿時狀態不變且可重試 |
| 7-H | R 查看 A 得 B | 背包有空間時 R 查看 A 取得 B 且 A 保留；背包滿時不設旗標且可重試；直接交 A 則 B 錯過；B 不在 Phase 7 實作實際用途 |
| 7-I | 回報晚 + 任務完成 | 只有任務 active、step 為 `found_activation_box` 且持有 A 時顯示交任務分支；成功移除 A 後才 complete；工作筆記更新 completed；B 若已取得則保留 |
| 7-J | 回歸驗證 | headless / GUI 驗證任務鏈、筆記更新、物品、禁存、回程與存讀檔不退化 |
| 7-K | 任務結局分支（隱藏物交還 + 好感 + 親近對話） | 回報時依是否持有 B 分流；持有 B 時出現「只交 / 連模組一起交」二選一；A/B 得 500、C 得 1000 + `affinity_wan += 2` + `gave_wan_old_module`；三種結局訊息與三種完成工作筆記各異；交出 B 後晚的招呼改親近版；headless / GUI + 存讀檔回歸不退化 |

依賴順序：7-A → 7-B → 7-C → 7-D → 7-E → 7-F → 7-G → 7-H → 7-I → 7-J。先讓任務狀態與筆記投影可驗證，再接內容與場景；梯子攀爬在場景骨架後做，A/B 物品在箱子搜索後做。7-K 為 7-A~7-J vertical slice 完成後追加的結局擴充，依賴 7-I（回報）與既有結局訊息 hook。

#### 7-K 任務結局分支（隱藏物交還 + 好感獎勵 + 親近對話）

> 7-K 在 Phase 7 vertical slice（7-A~7-J）完成後追加，把 `alley_backrooms_3f` 的回報由單一結局擴成三結局，並首次對該任務發放 credits 與好感獎勵。通用 reward API 仍不做；報酬改由對話 effect 直接發放。

目的：玩家回報晚時，依「是否發現隱藏物 B」與「是否願意連 B 一起交出」分出三種結局，各有不同報酬、結局訊息與工作筆記；交出 B 者額外獲得好感並解鎖較親近的日常對話。

**設計前提：晚不知道盒子裡有夾層 / 模組。** 她只看到啟用盒；是否連 B 一起交出，是玩家單方面的選擇。留下 B 自然、不尷尬；交出 B 是主角主動的信任與慷慨，因此才換得晚的另眼相看與好感。

三種結局：

| 結局 | 觸發條件（回報當下） | credits | 好感 `affinity_wan` | 旗標 | 交出物品 | 結局訊息 |
|---|---|---|---|---|---|---|
| A 盲目的清理者 | 未持有 B（從未 R 查看 A 拆出夾層） | +500 | — | — | A | 結局 1 |
| B 拾遺者的直覺 | 持有 B，選「只交啟用盒」 | +500 | — | — | A（B 保留） | 結局 2 |
| C 托付 | 持有 B，選「連模組一起交」 | +1000 | +2 | set `gave_wan_old_module` | A + B | 結局 3（新增） |

- 「是否發現 B」以持有 `old_ai_authorization_module` 判定（B `discardable=false`，持有即等於已拆出且未交出）。
- A 與 B 的報酬與晚的口頭台詞相同（500、收下盒子），差異只在工作筆記與結局訊息：A 不知道自己錯過了夾層；B 是「知道、但刻意把模組留下」。
- 既有結局訊息（commit 621c111）目前只有結局 1 / 結局 2，且 B 是自動發生（沒有玩家選擇）。7-K 補上「發現 B 時的二選一」並新增結局 3，使結局判斷改為三向分流（依 `gave_wan_old_module` → `found_old_ai_authorization_module` → 皆無）。

回報流程：

- 回報時未持有 B → 直接平淡交還（移除 A、+500、完成）= 結局 A。
- 回報時持有 B → 晚先收下盒子並道謝（不提夾層），再由玩家二選一：
  - 只交啟用盒 → 移除 A、+500、完成 = 結局 B（B 留在背包）。
  - 連模組一起交 → 移除 A 與 B、+1000、`affinity_wan += 2`、set `gave_wan_old_module`、完成 = 結局 C。
- 報酬以對話 effect 發放；移除 A 必須排在 effect 最前，移除失敗即中止後續 effect（沿用 7-I「不誤完成」保證）。

工作筆記：完成筆記需依結局分三份文字（A/B/C），不可共用同一份，且要寫得有畫面、不只差幾個字——A 暗示「掂在手裡好像比外殼該有的重，但大概是多心」；B 寫「把那塊像鑰匙的模組留在自己身上」；C 寫「把可能很重要的鑰匙交給同樣對抗遺忘的人」。

親近對話：交出 B（結局 C）後，與晚對話的開場白換成一眼可辨的親近版本（不沿用原 retalk 開場），氣氛是熟朋友、彼此有點不言而喻的小默契；不得把這份親近寫成又一次劇情說明或啟用盒回顧。A / B 結局完成後仍走原本的 retalk。

### Phase 8 — 便利商店 vertical slice + 買賣系統

目的：把街道既有的 flavor 物件「便利商店」升級成可進入的獨立場景，並落成第一個經濟系統（買賣 / ShopPanel）。同時首次展示主角正職「AI 善後員」玩法——修一台「讀了被辭退店員的日記、連他『拒絕工作』一起繼承、因此以為自己是那名店員而失去販賣功能」的店控機器人。修好機器人＝解鎖店內買賣，並連線恢復街道販賣機成迷你飲料商店。

場景敘事 / 互動物 / layout 細節見 `subdocs/地點/便利商店.md`。

#### Phase 8 範圍

做：

- 新增 `convenience_store` 室內場景；街道 `store_front` 由 examine 物件改成門 → `transition_to("convenience_store", "from_street")`；街道新增回程 entry point `from_store`；店內 `SaveSystem.can_save_here == true`。
- 買賣系統：`ITEMS_DB` 新增 `value`（基礎價）；全域賣出折率常數；店家 catalog 資料化（每店 `{item_id: {price, stock}}`）；店內當前庫存進 `GameState` 並存讀檔；commerce API（買 / 賣 / 價格 / 可賣判斷）。
- 新 `UIMode.SHOP` + `ShopPanel`：左店內貨架（價格＋庫存）/ 右背包（售出價）/ 頂部 credits；鍵盤＋觸控操作。
- 街道外面販賣機：修好前為前導線索（一般訊息互動物，取代既有 `vending_machine` examine）；店內機器人修好後，因連線恢復成第 2 個 shop（飲料 catalog），驗證 data-driven 多商店。
- 店內機器人對話狀態機（沿用條件式對話路由）：前導 babble → 接案後深度診斷對話樹（含誤導選項）→ 修好後成 ShopPanel 入口（口吻依結局分流）。
- 公寓電腦兩段 gate：第一次互動仍是舊的 `work_ai_cleanup_role`；第二次起且已 `discovered_vendor_error` → 工作 App 派工 → `QuestManager` 開 `repair_vendor_bot`。
- 蒐證系統：店內 5 個線索物件（多數接案後才可 examine，少數基礎物件接案前可看但只給無關 flavor）；examine 寫線索筆記。
- 修復裝置「店籍主機」三段互動 + 兩種重置結局（直接重置 / 先錄殘響再重置）+ 任務完成。
- 拾遺抉擇：唯有蒐證齊全 + 診斷對話全對才解鎖「先錄殘響再重置」；殘響 → 取得物品 + 筆記（不可賣、目前無用途）。

不做（僅預留，不得寫死封閉）：

- 拾遺副業經濟（殘響 / 收藏物的賣 / 留 / 還 / 發表系統）；殘響本階段只取得並保留。
- 動態 / 浮動定價、時間系統、每日補貨循環；Phase 8 庫存只在「進場景時」或「通關上新事件」重設，不綁時間。
- 通用商店折扣 / 議價 / 好感影響價格；價格固定。
- 後門可進入的後場場景；後門本階段鎖死，僅留未來鉤子。
- 戰鬥 / 潛行 / 故障 AI 物理拆解的通用化；本關修復走對話 + 店籍主機互動，不做機械 minigame。
- 大型商店 / 經濟 HUD；買賣只在 ShopPanel 內進行。

#### 買賣系統語意

物品價值：

- `ITEMS_DB` 每筆可選 `value: int`（基礎價，未填視為 0）。
- 可賣判斷 `is_sellable(item_id)`：`value > 0` 且 `category != "key_item"`，且未顯式標記 `sellable == false`。`key_item`（工作證 / B 模組等）與殘響不可賣。
- 賣出價 = `floor(value * SELL_RATIO)`，`SELL_RATIO` 為全域常數（暫定 0.5，開工可調）。賣出**不回補**店家庫存。

店家資料與庫存：

- 每店一份 catalog 資料：`{item_id: {price, stock}}`（`price` 為買入價，與 `value` 脫鉤，允許店家加價；`stock` 為初始庫存）。
- 店內**當前庫存** `GameState.shop_states[shop_id]` 為可變狀態，必納入 `to_save_dict()` / `load_save_dict()` / `reset_for_new_game()`。
- 庫存重設只發生在：(a) 首次開啟該店時 lazy-init、(b) 通關上新事件呼叫 `refresh_shop_stock(shop_id)`。不綁時間、不每日刷新。
- 買入 `buy_item(shop_id, item_id)` 為原子操作：需同時滿足 `stock > 0`、`credits >= price`、背包有空間（沿用 `add_item` 回傳 bool）。任一不成立則整筆失敗、狀態不變、回 false 並給對應失敗提示。成功則扣庫存、扣 credits、`add_item`。
- 賣出 `sell_item(item_id)`：需 `is_sellable` 且背包持有；成功則 `remove_item` + `add_credits(sell_value)`。

買賣 gate：

- 店內買賣只在 `vendor_bot_repaired == true` 後開放；修好前與機器人互動只走對話，不開 ShopPanel。
- 街道迷你飲料商店只在 `vendor_bot_repaired == true` 後開放；修好前外面販賣機只給前導訊息。

#### 場景與轉場語意

- `scene_id = "convenience_store"`，唯一 entry point：`from_street`。
- 街道 `apartment_entrance` 新增回程 entry point `from_store`，玩家回街道時落在店門口，不落在原預設點；不重播任何開場。
- 店內 `can_save_here == true`（與外牆禁存場景相反）。
- 街道 `store_front` 互動由「examine 訊息」改為「門 → 轉場」；轉場走既有 `scene_transition_requested` 契約，不用 `change_scene_to_file`。

#### `repair_vendor_bot` 任務流程

```text
街上互動外面販賣機（前導 babble）-> set talked_outside_vendor
進店與機器人對話（前導 babble）-> set talked_store_robot
兩 flag 皆真 -> set discovered_vendor_error
回公寓用電腦
  第一次互動 -> 舊內容 work_ai_cleanup_role，set used_room_computer_once
  第二次起 + discovered_vendor_error 且任務未接 -> 派工 -> QuestManager.start("repair_vendor_bot")
  -> 工作筆記出現：去便利商店修販賣機器人
回店 examine 員工區 5 個線索（接案後開放）
與機器人進行深度診斷對話樹（含誤導選項）
  揭示店籍主機存在 -> set mainframe_revealed（quest flag）
  蒐證齊全 + 全程選對 -> set understood_robot_truth（quest flag）
  否則對話結尾引導去主機直接重置
互動店籍主機（mainframe_revealed 後可互動）
  段1：資訊不足 -> 只說明這是主機，不知如何維修
  段2：對話到一定程度（未全對）-> 僅「直接重置」
  段3：understood_robot_truth -> 「直接重置 / 先錄殘響再重置」二選一
重置（任一）-> set vendor_bot_repaired + store_robot_resolution + complete_quest
  直接重置   : store_robot_resolution = "reset"
  先錄殘響   : store_robot_resolution = "gleaned"，取得殘響物品 + 筆記
-> 店內 ShopPanel 開啟；街道外面販賣機連線恢復成迷你飲料商店
-> 修復訊息暗示外面那台因連線也修好了
```

#### 機器人狀態機

機器人 `store_robot` 對話內容隨狀態分流（沿用 `wan` 的 `start` 路由節點條件分支）：

| 狀態 | 條件 | 內容 |
|---|---|---|
| 前導 babble | 任務未接 | 淺層胡言亂語；自稱不是販賣機；讓玩家察覺異常並設 `talked_store_robot` |
| 深度診斷 | `repair_vendor_bot` active 且未修好 | 深度對話樹，揭露店員故事；誤導選項；全對判定；揭示主機 |
| 修好（reset） | `vendor_bot_repaired` 且 resolution == reset | 純 AI 無機感招呼；互動開 ShopPanel |
| 修好（gleaned） | `vendor_bot_repaired` 且 resolution == gleaned | 殘留一點人味的招呼；互動開 ShopPanel |

#### 蒐證 / 診斷語意

- 線索物件 5 個：店員置物櫃、店員日記殘頁、辭退通知 / 排班表、機器人銘牌 / 序號、櫃台照片。examine 各寫一筆 `category == "線索"` 筆記（`clue_*`）。
- 線索開放時機：多數線索需 `repair_vendor_bot` active 才可 examine（接案前不可互動或只給模糊提示）；少數基礎物件（貨架 / 熟食機 / 冷藏櫃）接案前即可看，但只給與維修無關的淺顯 flavor。
- 診斷對話樹的「正確選項」以 `has_note(clue_*)` 為門檻——只有 examine 過對應線索，該正確選項才成立 / 命中真相分支；誤導選項恆在。
- `understood_robot_truth` 只在「5 條線索筆記齊全 + 對話全程走正確分支抵達真相 leaf」時設立；任一線索缺漏或中途選到誤導，落到 partial leaf（只 reveal 主機、不設 understood）。
- 對話可反覆重來；玩家可補查線索、再次對話修正到全對。理解程度取「目前最佳」（旗標一旦設立不回退）。只有在店籍主機真正執行重置那刻才不可逆。

#### 店籍主機三段與兩種結局

「店籍主機」`store_registry_host` 位於員工區，`mainframe_revealed` 前不可互動（不出 prompt）：

| 段 | 條件 | 行為 |
|---|---|---|
| 1 | `mainframe_revealed` 但對話深度 / 蒐證不足 | 只說明「這是店籍主機」，主角不知如何維修，不提供重置 |
| 2 | 診斷對話完成（未全對）| 提供「直接重置」 |
| 3 | `understood_robot_truth` | 提供「直接重置 / 先錄殘響再重置」二選一 |

- 直接重置：set `store_robot_resolution = "reset"`、set `vendor_bot_repaired`、`complete_quest`。
- 先錄殘響再重置：先取得殘響物品 `clerk_echo_recording` + 筆記；若背包滿則整筆中止、不設旗標、不重置、可重試（沿用 7-G/7-H 背包滿防呆）；成功後 set `store_robot_resolution = "gleaned"`、set `vendor_bot_repaired`、`complete_quest`。
- 重置後店籍主機不再提供重置（只給已修復的中性訊息）。
- 修復完成訊息需暗示：外面那台販賣機因連線也一併恢復。

#### 殘響物品語意

- `clerk_echo_recording` / 店員殘響（錄音或筆記形式，文字待填）。
- `value` 未填 / 0，`sellable == false`，`discardable == false`：**不可賣、不可丟**。
- Phase 8 不實作用途；只取得並保留，作為未來拾遺副業鉤子。

#### 子階段

| 子階段 | 內容 | 結束時驗收 |
|---|---|---|
| 8-A | `convenience_store` 場景骨架 + 街道→店內轉場 + can_save | SceneRegistry 含 `convenience_store`（entry `from_street`）；街道 `store_front` 改門可進店、店內可回街道落 `from_store`；店內可手動存檔；headless 載入 + 轉場 + 存讀檔 round-trip PASS |
| 8-B | 販賣機 / 機器人對話化（前導）+ `discovered_vendor_error` | 外面販賣機前導訊息（取代 `vending_machine`）；店內機器人前導 babble 對話；分別設 `talked_outside_vendor` / `talked_store_robot`；兩者皆真才設 `discovered_vendor_error`；單談一台不設 |
| 8-C | 公寓電腦兩段 gate + QuestManager 接案 | 第一次互動電腦 = 舊 `work_ai_cleanup_role` 並設 `used_room_computer_once`；第二次起且 `discovered_vendor_error` 且任務未接 → `QuestManager.start("repair_vendor_bot")`；工作筆記出現；條件不足時不派工 |
| 8-D | 蒐證系統 + 診斷對話樹 + 揭示主機 | 5 線索接案後可 examine 並寫 `clue_*` 筆記；診斷對話含誤導選項；蒐證齊全 + 全對才設 `understood_robot_truth`，否則只設 `mainframe_revealed`；對話可反覆、可修正 |
| 8-E | 店籍主機三段 + 兩種重置結局 + 完成 | 主機三段 gating 正確；直接重置 / 先錄殘響再重置皆 set `vendor_bot_repaired` + `store_robot_resolution` + complete quest；殘響取得（背包滿防呆）；完成訊息暗示外面恢復；工作筆記 completed 依結局分流 |
| 8-F | 買賣系統核心 + ShopPanel + UIMode.SHOP | `ITEMS_DB` 加 `value`；commerce API 與店庫存狀態 + 存讀檔；`UIMode.SHOP` + `ShopPanel`；店內修好後與機器人互動開店；買賣數學、credits / 庫存 / 背包滿 / 不可賣邊界；鍵盤 + 觸控操作 |
| 8-G | 迷你飲料商店 + 多商店資料化 | 修好後外面販賣機成第 2 個 shop（飲料 catalog）；data-driven 多商店：同一 ShopPanel / API 服務兩店、庫存各自獨立、各自存讀檔 |
| 8-H | 回歸 + 存讀檔驗證 + GUI 走查 | 全鏈回歸；接案前 / 接案後 / 蒐證中 / 修好後各節點存讀檔不退化；headless `test_runner` / `verify_game_state` PASS；GUI 純鍵盤 + 純觸控走查清單 |

依賴順序：8-A → 8-B → 8-C → 8-D → 8-E → 8-F → 8-G → 8-H。先讓場景與轉場可驗證，再接前導對話與發現錯誤旗標，再接電腦派工，再做蒐證與診斷，再做修復結局；買賣系統核心可先獨立 headless 測 commerce 邏輯，再於 8-E（修好旗標）後接 gate；多商店在單商店穩定後做。

平行美術軌（不在程式依賴線上）：機器人立繪 / 螢幕臉、便利商店室內 map、ShopPanel item icon、飲料 icon。

#### 旗標 / 狀態一覽（暫定命名，開工可調）

| 名稱 | 所在 | 意義 |
|---|---|---|
| `talked_outside_vendor` | story_flags | 與外面販賣機互動過 |
| `talked_store_robot` | story_flags | 與店內機器人對話過 |
| `discovered_vendor_error` | story_flags | 兩台都互動過（電腦派工前置） |
| `used_room_computer_once` | story_flags | 公寓電腦至少互動過一次 |
| `mainframe_revealed` | quest flags | 對話揭示店籍主機可互動 |
| `understood_robot_truth` | quest flags | 蒐證齊全 + 對話全對 |
| `vendor_bot_repaired` | story_flags | 修好（解鎖店內 + 街道飲料商業） |
| `store_robot_resolution` | story_flags | `reset` / `gleaned`，驅動機器人日後口吻與完成筆記 |
| `clue_*`（5 筆） | notes（線索） | 各線索 examine 紀錄 |
