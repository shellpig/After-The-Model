# 開發設計方針

> 實作層文件：檔案結構、Autoload 簽名、signal、資料契約、實作決策。
> **角色歸屬**：implementer-owned。對應 CLAUDE.md「Role Separation」——本檔與程式碼由實作者維護；測試流程 / 已知問題由 verifier 維護。

## 本檔範圍與邊界

| 寫什麼 | 去哪 |
|---|---|
| 系統該有什麼行為、驗收意圖、跨場景通用規格 | `遊戲規格書.md` |
| 公寓專屬劇情 / 互動物 / 線索文字 | `subdocs/地點/主角公寓.md` |
| **檔案清單、autoload 簽名、signal、資料契約、實作決策** | **本檔** |
| 測試流程、手動驗收步驟清單 | `測試指南.md` |
| 已知問題 / 已接受的邊界決定 | `驗證後已知問題.md`（2-E 收尾才建） |

## 起始範圍

- 本檔**自 Phase 2 起新寫**，不 backfill Phase 1 舊細節。
- Phase 1（GameState / UIMode / 背包 / 筆記 / 容器 / 物品操作）的實作細節仍以 `遊戲規格書.md` 為歷史快照來源；需要時 link 回去，不複製。
- 規格書中現存的 GDScript 簽名 / signal / item metadata stub 屬 Phase 1 既成內容，不搬移。

## Phase 2 — 公寓解謎（實作契約）

> 機制鏈與驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 2`；完整流程與文字見 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`。
> 以下各項簽名 / 檔案結構於對應子階段開工時填，未定者不預先杜撰。

### 2-A 線索 examine 互動物

範圍：兩個世界 examine 互動物——桌上電腦（`work_ai_cleanup_role`）、左牆錄音機（`identity_gleaner`）。手套線索（`clue_gloves_decoder`）不在此，見 2-B。

互動物形態（沿用 `interactable_area.gd`，不新增節點類型）：

- `interactable_area.gd` 新增 export：`note_id`（examine 分支用；非 examine 物留空）。
- `apartment_room.gd` 新增 `NOTES` dict（類比現有 `MESSAGES`），key = `note_id`，value = `add_knowledge` 所需的筆記 payload（id / category / title / body / status）。
- `apartment_room.gd` 的派發 `match` 新增 examine 分支：
  - `show_message(message_id)` 顯示現場敘述句（沿用 `MESSAGES`）。
  - `GameState.add_knowledge(NOTES[note_id])` 寫入筆記。
- 現場敘述句（`MESSAGES`）與筆記正文（`NOTES`）分離維護。

行為契約：

- 兩物開局即可 examine，無 `required_knowledge` 前置。
- 重複 examine：每次重播 `show_message`；`add_knowledge` 依其 idempotent 特性不重覆加。若 examine 分支需要控制「已記入筆記」提示是否出現，先用 `GameState.has_note(note_id)` 判斷，不用 `has_knowledge()`。
- 線索筆記文字見 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`。
- Phase 2 開工時，`work_ai_cleanup_role` 不再作為新遊戲預載 demo 筆記；改由桌上電腦 examine 首次寫入，避免 2-A 首次取得驗收被預載污染。

待 content 補：兩物 prompt 文字（`E: 查看螢幕` / `E: 按下播放`）、「已記入筆記」提示字樣。

### 2-B 解碼手套 + 魔術方塊道具

物品與 metadata：

- `worn_rubiks_cube`（魔術方塊，普通玩具）`max_stack=1`；新增 metadata 欄位 `decodable_to = "decoder_cube"` + 所需裝備條件（裝備某具解碼能力的物，公寓即 `fingerless_gloves`）。
- `decoder_cube`（解碼方塊）`max_stack=1`；無 `decodable_to`，搬動不再觸發。
- 解碼能力標在裝備物上（如 `fingerless_gloves` 加一個「具解碼能力」旗標），讓「裝備條件」可組合判斷，不寫死特定手套 id。
- 兩物 id 命名 / icon / description 為 content，定稿前以上列暫名。

轉換觸發接線（兩處，行為一致）：

- **拿上手**：`move_one_item_to("player_inventory", instance_id)` 成功後，controller 讀取該次 `item_moved` payload 的 `target_instance_id`；若目標物有 `decodable_to`、且玩家已裝備滿足條件的裝備 → 更新該 target instance 的 `item_id` 為 `decodable_to`、跳 MessageBox 敘事、`add_knowledge` 解碼方塊筆記。
- **R 查看（fallback）**：ItemDetailModal 開啟中、查看物有 `decodable_to`、裝備條件滿足 → 更新 modal 當前 instance 的 `item_id`；MessageBox 關閉後 modal 刷新顯示 `decoder_cube`。MessageBox 疊在 detail modal 上方期間 detail modal 凍結、不吃鍵盤（capture 暫交 MessageBox），關閉後還原 capture 並刷新（消耗物則連 modal 一起關回 grid）。
- 轉換定位：不得靠背包排序後的格位猜測剛移入物；拿上手路徑一律使用 `item_moved` payload 的 `target_instance_id`。轉換後 UI 上必須表現為「剛操作的那個物品變了」，不得另新增第二個方塊。
- 因 item_id 變更後不再帶 `decodable_to`，天然防重複觸發；無需額外 flag。

手套線索筆記：

- 首次 `equip` 或首次 R 查看 `fingerless_gloves` → `add_knowledge(clue_gloves_decoder)` +「已記入筆記」提示。
- 一次性：以 `has_note("clue_gloves_decoder")` 判斷避免重覆；`has_knowledge()` 只用於身份 gate，不用於線索筆記去重。

不回掃背包：裝備手套**不**觸發背包既有可解碼物的批次轉換；先拿後戴情況由 R 查看補解。

待 content 補：兩物 id 命名定案、icon、解碼前/後 description、解碼 MessageBox 敘事全文、手套訊息字樣。

待確認實作細節：解碼能力旗標掛在裝備物 metadata 的具體欄位名與型別。

### 2-C 容器擴充（accepted_item / deposit_locked）

對應規格：`遊戲規格書.md > External Container API > 容器選項擴充` + `Phase 2 > 2-C 驗收意圖`。

GameState 改動（純資料層）：

- `configure_container(container_id, slot_count, accepted_item: Array[String] = [], deposit_locked: bool = false)`：新增兩個選用參數，預設值使既有呼叫不受影響。旗標隨容器狀態存放（與 slot 內容同一份 runtime state）。
- `get_container_config(container_id) -> Dictionary`：回傳 `slot_count` / `accepted_item` / `deposit_locked` 的 deep copy；UI 判斷失敗訊息時使用此 API。
- `move_one_item_to(target, instance_id)` 守衛新增兩條（在既有「目標可容納」檢查之外）：
  - 目標容器 `accepted_item` 非空且來源 item_id 不在清單 → `return false`（不變動來源/目標）。
  - 來源容器 `deposit_locked == true` → `return false`（不可作為搬運來源）。
- 回傳型別不變（bool）。不在 GameState 層回傳原因碼。移動成功時 emit `item_moved(move)`，供 2-B 等後續機制定位 target instance。

UI 層（接線於 2-D/2-E，2-C 僅資料層）：

- 雙欄移動前，UI 透過 `get_container_config()` 讀取目標容器 `accepted_item` / 來源容器 `deposit_locked` 預判，挑訊息：`放不下了。`（滿）/ `放不進去`（白名單拒）/ 取出被擋字樣（deposit_locked）。
- `move_one_item_to` 為權威 guard，UI 預判失誤也不會破壞資料。

不動：`get_container` / `seed_container` / `discard_item` 行為不變。`seed_container` 為 dev/預載入口，不受白名單約束（插槽不經 seed）。

待 content 補：deposit_locked「取出被擋」訊息字樣（2-E 定）。

待確認實作細節：旗標存放結構（與 container slots 同 dict 或分開 metadata dict）。

### 2-D 偵測終端（投影時鐘）+ 聲納 reveal

對應規格：`遊戲規格書.md > Phase 2 > 2-D 驗收意圖`；流程與文字見 `subdocs/地點/主角公寓.md`。

線索麵包屑道具（查看即消耗）：

- 新增 item `nutrition_bar_synth_blueberry`（合成藍莓口味營養棒，`category=consumable`，`max_stack` 待定），預載於 `fridge_storage`。
- icon 使用 `res://assets/generated/sprites/items/synthetic_blueberry_nutrition_bar/icon.png`。
- item metadata 新增欄位 `consume_grants_note`（String，note_id）。帶此欄位的物經 **E 使用 或 R 查看** 任一觸發：跳 MessageBox 顯示空包裝藏字敘述 → `add_knowledge`(該 note payload) → 移除該 instance（消耗）；R 路徑於 message 關閉後一併關 modal。兩路徑同效，仿手套解碼雙觸發。
- 一次性天然成立：instance 移除後不再存在，無需 flag。
- 移除 instance 沿用既有 discard/remove 容器路徑（在背包或外部容器皆可消耗）。

背包 E 使用動作（Phase 1-E footer 修正，2-D 連帶；對應規格書「物品操作」段更新）：

- 背包單獨（`I`）footer 的 E 段改為**依選中物品動態決定**（取代 Phase 1-E 固定字串）：
  - 可裝備物 → `E: 裝備`（未裝備）/ `E: 卸下`（已裝備）。
  - `category == "consumable"` 或標記可反覆使用 → `E: 使用`。
  - 其餘（無對應動作，如 `old_work_badge`）→ E 段不顯示。
- 按 E dispatch（單獨背包內）：
  - 可裝備 → 既有 equip / unequip。
  - 帶 `consume_grants_note` 的 consumable → 上述消耗 + 給筆記流程。
  - 無效果 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」，不消耗。
  - 可反覆使用物 → 執行效果、不消耗（預見類型，暫無實例，不提前實作）。
- 雙欄左欄（背包欄）E 仍為「移動」，不變；`使用` 只在單獨背包出現。R 查看路徑於雙欄 / 單獨皆可，故營養棒在冰箱雙欄按 R 亦可消耗得線索。

待確認實作細節：「可裝備」「可反覆使用」如何由 item metadata 判定（category vs. 專屬 flag）；footer 動態字串組裝點；instance 消耗走 `discard_item` 或新 remove API。

投影時鐘互動物（= 偵測終端）：

- 沿用 `interactable_area.gd`；`required_knowledge` = 投影時鐘線索 note_id（取得線索前沿用既有 gate，不顯示 prompt / 不可互動）。
- 互動 E → 呼叫聲納啟動（見下）。

聲納狀態機（掛 `apartment_room.gd` 或抽獨立 helper node，二擇一待定）：

- 啟動 = 進入聲納 active 狀態 + 開 10 秒倒數計時器。
- 每 tick 依玩家 x 與隱藏插槽 x 的 1D 距離算強度 → 驅動短音播放頻率 + 視覺強度條；聲納短音資源為 `res://assets/sound/sonar_ping.wav`。
- 玩家在最強區帶寬內（`abs(dx) <= 36`）累積鎖定進度 4 秒（鎖定環）；離開帶寬則鎖定進度重置（容許帶寬內微動，不要求像素級不動）。prompt 文字「定位中…」。
- 鎖定環滿 → reveal：播放 `res://assets/sound/hidden_slot_reveal.wav`，顯示隱藏插槽為一般互動容器（`configure_container("apartment_slot", 1, ["decoder_cube"], true)`，見 2-C）；聲納狀態關閉、移除投影時鐘互動。
- 10 秒歸零且未鎖定 → 聲納關閉、不 reveal；投影時鐘仍可再互動重啟（狀態機可重入）。
- UIMode 進大型 UI（INVENTORY/NOTEBOOK 等）時暫停聲納 tick + 倒數 + 鎖定進度（不歸零）；回 NONE 恢復。
- reveal 後聲納不可再啟動（投影時鐘互動已移除，狀態機對重複啟動 idempotent）。

待 content 補：強度→短音播放頻率映射、10 秒是否可調、強度條視覺、鎖定環視覺、營養棒/線索/時鐘 prompt 與敘述文字、`nutrition_bar_synth_blueberry` max_stack。

待確認實作細節：聲納狀態機掛 `apartment_room.gd` 內或抽獨立 node；視覺強度條 UI 元件歸屬；鎖定環滿秒數與 10 秒窗的關係（dwell 須 ≤ window）；隱藏插槽 container_id 命名。

### 2-E 插槽放入整合 + MESSAGE/CONFIRM overlay 重構

對應規格：`遊戲規格書.md > Phase 2 > 2-E 驗收意圖` + `UIMode > overlay modal 的 caller 還原`。

UIMode overlay 重構（先做，放入鏈依賴它）：

- `MESSAGE` 改為與 `CONFIRM` 同類的 **overlay modal**：進入記住 caller mode、不關 caller UI、退出還原焦點。
- 既有 `enter_confirm` / `exit_confirm` 一般化為 `enter_overlay(mode)` / `exit_overlay()`，`MESSAGE` / `CONFIRM` 共用 `_caller_mode`。
- caller UI 在 overlay 期間凍結接鍵（不吃方向 / E / R / T）；overlay 優先吃鍵、推進文字、`Esc` 關閉。
- 遷移既有呼叫：examine(2-A) / 解碼(2-B) 開 MessageBox 改走 `enter_overlay(MESSAGE)`（caller 多為 NONE / INVENTORY），視覺行為不變。
- MVP 單層 overlay（不疊 MESSAGE on CONFIRM）。
- 回歸：1-B~1-E 大型 UI 互切、CONFIRM 丟棄確認、focus 還原全測。

放入觸發鏈（`apartment_room.gd`）：

- listen `container_changed("apartment_slot")` → 若 `decoder_cube` 在槽內 且 未 `has_knowledge("identity_door_unlock_method")` → 跑鏈。
- 鏈：播電磁聲響（音效待定）→ `enter_overlay(MESSAGE)` 顯示語音 MessageBox（疊在雙欄上）→ 關閉（`exit_overlay`）後 `add_knowledge(identity_door_unlock_method payload)`。
- 一次性守衛：因 `add_knowledge` 延後到 MessageBox 關閉才寫，逐字顯示窗內 `has_knowledge` 仍為 false，需另設 transient flag。`apartment_room.gd` 持 `_slot_unlock_sequence_started: bool`；觸發條件為 `!_slot_unlock_sequence_started and !has_knowledge(...)`，進鏈第一步即 `_slot_unlock_sequence_started = true`，防同窗重入。`deposit_locked` 另使該槽不再變動，`container_changed` 長期亦不再為此槽觸發。
- `add_knowledge` 時機：MessageBox 關閉（`exit_overlay`）之後寫入。
- 完成後插槽常駐、方塊鎖在格內（deposit_locked），雙欄可開看、不可取出。
- 語音 / 筆記全文見 `subdocs/地點/主角公寓.md > 互動物：隱藏插槽`。

大門（B9，不改現有程式）：

- 沿用 `DoorExitArea` 的 `has_knowledge("identity_door_unlock_method")` gate；MVP 顯示 `door_opened`，不轉場。

待 content 補：電磁聲響音效素材。

待確認實作細節：聲響與 MessageBox 的先後 / 同步時序；overlay 是否需 `get_base_mode()` 之類查詢（MVP 用 `_caller_mode` 即足）；遷移 enter_confirm 既有 caller 的相容性。

### 2-F 筆記內容/操作修正 + 公寓 BGM

對應規格：`遊戲規格書.md > Phase 2 > 2-F 驗收意圖` + `筆記 / 知識系統`（操作段 2-F 修正）+ `音訊（BGM）> 公寓 BGM`。皆為既有系統小幅修正，無新機制、無新 GameState API。

筆記內容（`apartment_room.gd._ready()` 預載）：

- 將第三筆預載 `clue_long_scroll_test` 整筆改寫為 `identity_rainy_night`：id / category(`身份`) / title / body 全換，文字以 `subdocs/地點/主角公寓.md > 筆記預載` 為準。
- 改 category 為 `身份` 後 `add_knowledge` 會連帶設 `knowledge["identity_rainy_night"]=true`；無程式碼讀此 key，無副作用。舊 id `clue_long_scroll_test` 亦無任何引用。

筆記操作（`scenes/ui/notebook_panel.gd`）：

- `_unhandled_input` 切 tab 由 `ui_left`/`ui_right` 擴為 **同時接 `move_left`/`move_right`**（比照 `bag_grid.gd` 的 `ui_* or move_*` 寫法）。
- 選筆記上下：補 `move_up`/`move_down`（W/S）等同既有 `ui_up`/`ui_down`（方向鍵）的列表 focus 移動；維持邊界不繞回。
- footer 字串改 `A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`（含 `is_input_active` 兩處字串）。
- Page Up / Down 既有的吞掉分支保留（已無功能）；筆記皆一頁內，不再實作捲動。

公寓 BGM（`apartment_room.tscn`）：

- 新增子節點 `AudioStreamPlayer`：`stream` = `res://assets/bgm/Echoes of a Cozy Night.mp3`（匯入設定開 Loop）、`autoplay = true`、`volume_db = -12.0`。
- 純場景節點即可，不寫 autoload、不加場景腳本邏輯（缺檔時 `stream` 為 null → 不播放、不報錯）。

待確認實作細節：`notebook_panel.gd` 列表上下選筆記目前是否靠 Godot 內建 focus 鏈（`ui_up`/`ui_down`）；若是，W/S 需在 `_unhandled_input` 內手動推進 focus 或重映射，避免只有方向鍵能動。實作時以實機確認 W/S 真能選筆記。

### 2-G 開場獨白序列（實作契約）

對應規格：`遊戲規格書.md > Phase 2 > 2-G 驗收意圖`；文案全文見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`。

新遊戲進公寓即播的線性開場：prone 甦醒 → 4 頁不可跳過 MessageBox → 第 4 頁起身動畫 → 解鎖自由操作。沿用既有 MessageBox 視覺 / 打字機與玩家 `AnimatedSprite2D`，無新 GameState API。

動畫資產（`apartment_room.tscn` 內玩家 `AnimatedSprite2D` 的 SpriteFrames）：

- 新增 `prone`：單格靜態 = `res://assets/generated/sprites/main_character/get_up/main-character-get-up-10f-fixed-scale-20260530-124840/main-character-get-up-aligned-01.png`（get_up 序列第 1 格）。
- 新增 `get_up`：`main-character-get-up-aligned-01`→`-10` 共 10 frame，`speed = 10 fps`、`loop = false`。
- `idle` / `walk` 不動；`sleep_prone/` 資料夾 2-G 不使用。

開場流程（建議掛 `apartment_room.gd`，做成獨立開場 state，不污染泛用 `MESSAGE` overlay）：

- `_ready` 啟動開場（MVP 無條件啟動，等同「每次進場都播」）：`player.anim.play("prone")`、開場接管輸入（player 因 `is_world_input_blocked()` 不動）、`prompt_panel.visible = false`、開 MessageBox 顯示第 1 頁。
- 開場專用分頁陣列（4 筆），每筆 = 標題（正文第一行）+ 內文；逐頁餵給既有 `_start_message_typewriter`。
- **不可跳過 guard**：本頁打字機未完成（`message_label.text.length() < message_full_text.length()`，或自記 `_opening_page_done` flag）時，換頁鍵全部吞掉不作用。
- 本頁跑完 → 設 `_opening_page_done = true`、淡入換頁提示 Label（前三頁「▼ 繼續」、末頁「▼ 開始」）。
- 換頁鍵 = `move_left/right/up/down`（WASDE）/ `ui_*`（方向鍵）/ `ui_cancel`（Esc）任一 `just_pressed`，且 `_opening_page_done` 為真才前進。
- 進第 4 頁時 `player.anim.play("get_up")`；接 `animation_finished` → `player.anim.play("idle")`（或固定 1 秒計時切換）。文字續跑、不等動畫。
- 末頁 done + 換頁鍵 → 關 MessageBox、退出開場狀態、解除輸入封鎖；強制 `player.anim.play("idle")`（防 get_up 未播完即被跳過）。

隱藏測試捷徑（dev skip）：

- 開場狀態內維護 T 連按計數 + 末次時間戳；`interact_drop`（T）於 `1.0` 秒窗內第 3 次 `just_pressed` → 立即補完當頁文字（`message_label.text = message_full_text` + 設 done）並前進一頁；末頁則關閉開場、解鎖。
- 逾時或非連續 → 計數歸零。
- 不在換頁提示中標示。

輸入接管邊界：

- 開場期間靠開場 state 自身吞掉所有世界互動與大型 UI 開啟鍵（I / J / E）；只放行換頁鍵與隱藏 T。
- 二擇一（實作時定，並回填本段）：(a) 獨立開場 state（UIMode 維持 `NONE`，開場 state 在 `_unhandled_input` / `_process` 攔鍵）；(b) 沿用 `enter_overlay(MESSAGE)` 並擴充其「多頁 + 不可跳過 + 換頁提示 + 動畫鉤子」。MVP 傾向 (a)，較不破壞既有 MESSAGE 回歸。

待 content 補：換頁提示精確字串樣式（顏色 / 淡入時間）；4 頁文案以 subdoc 為準。

待確認實作細節：開場 state 掛 `apartment_room.gd` 內或抽獨立節點；get_up→idle 切換用 `animation_finished` 或固定 1 秒計時；換頁提示 Label 歸屬節點；獨立 state vs. `MESSAGE` overlay 擴充最終擇一。

## Phase 3 — 公寓觸控化（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 3`；操作清單見 `測試指南.md > Phase 3`。
> 核心前提：全遊戲互動已只 listen 抽象 action（`apartment_room.gd` / `scenes/ui/*.gd` 全用 `Input.is_action_*` 或 `event.is_action_pressed`），故觸控層只送 action、**不改任何面板邏輯**。各項簽名 / 節點歸屬於對應子階段開工時填，未定者不預先杜撰。

### 3-A 顯示 / 橫向 / 觸控模擬設定（`project.godot`）

純設定變更，無新檔。預計新增 / 確認下列鍵：

```ini
[display]
window/handheld/orientation="landscape"
window/stretch/mode="canvas_items"   ; 已有
window/stretch/aspect="expand"       ; 未顯式設定則補

[input_devices]
pointing/emulate_touch_from_mouse=true
pointing/emulate_mouse_from_touch=true
```

- `renderer/rendering_method.mobile` 已是 `gl_compatibility`，不動。
- 不改 `[input]` 既有 action 綁定（實體鍵保留供 PC）；觸控按鈕在 3-B 以程式送 action，不靠 InputMap 加觸控事件。
- 回歸保證：鍵盤路徑與 `test_runner` / `verify_game_state` 不受影響。

### 3-B / 3-C TouchControls 元件

新增檔案（暫定）：

```text
scenes/ui/touch_controls.tscn   # CanvasLayer 根，含方向鍵 / E·R·T / 背包·筆記·返回 按鈕
scenes/ui/touch_controls.gd     # 按鈕 → action 派送、顯示規則、面板狀態切換
```

掛載方式（二擇一，實作時定並回填）：(a) 新增 autoload 把 `touch_controls.tscn` instance 到一個高 layer 的 CanvasLayer，全場景自動繼承；(b) 各場景手動放一份。**傾向 (a)**，讓 Phase 4+ 新場景免重工。`CanvasLayer.layer` 須高於遊戲與既有 UI overlay。

按鈕 → action 對應表：

| 按鈕（位置） | action | 階段 |
|---|---|---|
| ← / →（左下） | `move_left` / `move_right`（世界走路）、面板內 `ui_left` / `ui_right` 鏡像 | 3-B / 3-C |
| ↑ / ↓（左下） | `move_up` / `move_down` + `ui_up` / `ui_down`（面板焦點） | 3-C |
| E（右下） | `interact_primary` | 3-B |
| R / T（右下） | `interact_secondary` / `interact_tertiary` | 3-C |
| 背包（右上常駐） | `open_inventory` | 3-B |
| 筆記（右上常駐） | `open_notebook` | 3-B |
| X 返回（右上，面板開時） | `ui_cancel` | 3-C |

派送機制（3-B 實作回填）：統一在按鈕 `button_down` → 送 `pressed=true` 的 `InputEventAction`、`button_up` → 送 `pressed=false` 的 `InputEventAction`，經 `Input.parse_input_event()` 灌入。如此方向鍵長按 = 持續 `is_action_pressed`（走路）、輕點 = 單次 `is_action_just_pressed`（面板移一格 / 互動），一套機制覆蓋兩種需求，無須分流。

> 註：與 `Input.action_press()` / `Input.action_release()` 行為等價，最終採 `InputEventAction` + `Input.parse_input_event()`（見 `scenes/ui/touch_controls.gd > _simulate_action`）。另每顆按鈕設 `focus_mode = FOCUS_NONE`，防止觸控點擊搶走鍵盤焦點導致方向鍵失靈。

顯示規則（最終實作，3-D 收尾改寫）：原 `DisplayServer.is_touchscreen_available()` 方案因**部分 Windows PC（含觸控螢幕筆電）誤判**而棄用。現行：

- CanvasLayer `self.visible = true` **恆真**（PC 端需常駐左上角開關按鈕）。
- `_ready` 以 `OS.get_name()` 判平台：`Windows / macOS / Linux / *BSD / UWP` 視為 PC（`is_pc_platform = true`）。
- `touch_buttons_enabled` 預設：手機平台 `true`（直接啟用），PC 平台 `false`（需手動開）。
- **PC 專屬 `BtnToggle`（左上「[ 觸控:開/關 ]」）**：僅在 PC + 世界 `NONE` 模式 + 開場獨白播畢時顯示，點擊翻轉 `touch_buttons_enabled`。PC 且未啟用時，D-pad / Actions / Menus 全隱藏（讓鍵盤玩家畫面乾淨）。
- `@export var force_show_in_debug` 仍保留宣告但**目前未被引用**（孤兒旗標，留待清理或重新接線）。

返回鍵 / 面板態切換（最終實作）：右上在「世界模式顯示 背包·筆記」與「面板開啟顯示 X 返回」間切換。`BtnClose` 綁 `ui_cancel`，可退出各面板層（含 Modal / Confirm）。

切換時機採**雙軌**（統一在 `_update_dynamic_button_visibility()` 內計算）：

- `UIMode.mode_changed(new_mode)` signal 觸發時立即更新一次（最即時響應）；
- **外加 `_process` 每幀輪詢**——因 E / R / T 的顯示已升級為「情境感知」（見下），需逐幀反應 `current_interactable` / 聚焦格狀態等不會發出 `mode_changed` 的變化。（原 3-C 設想的「純 signal、不輪詢」未採用。）

背包·筆記 vs X 返回：`mode == NONE` 顯示背包·筆記、隱藏返回；否則反之。

E / R / T 情境感知顯示（超出原 3-C「v1 固定顯示」，3-D 期一併做的 polish）：

- **世界 NONE**：R / T 永遠隱藏；E 僅在 `scene.current_interactable != null` 時浮現。
- **INVENTORY / CONTAINER**：依聚焦格內容判定——空格全隱；佔用格 R = 查看恆顯，E 視 category（equipment / consumable / `consume_grants_note`）有無動作，T 視 `discardable` 且非裝備中。
- **NOTEBOOK**：E / R / T 全隱（只翻頁）。
- **MESSAGE**：E（繼續）+ T（連點三下 skip 獨白）顯示，R 隱。
- **CONFIRM**：E（確定）顯示，R / T 隱（取消走 X 返回 = `ui_cancel`）。

（見 `scenes/ui/touch_controls.gd > _update_dynamic_button_visibility`。）

### 3-D Safe area + 比例排版（實作回填）

- **Safe area**：`_ready` 及 `get_viewport().size_changed` 時呼叫 `_update_safe_area()`，讀 `DisplayServer.get_display_safe_area()`，算四邊 margin（`margin_left = safe.position.x`、`margin_right = viewport.x - safe.end.x`，上下同理），套到 full-rect（anchors_preset 15）的根 `$Control` 之 `offset_left/top/right/bottom`，整組按鈕隨之內縮。PC 上 safe area = 全螢幕 → margin 全 0（no-op）。
- **按鈕尺寸**（≥ 44×44 px）：D-pad 54×54、功能鍵 E/R/T/背包/筆記/X 統一 60×60；皆用 `custom_minimum_size` + Container 自適應，不寫死座標。
- **例外**：PC 專屬 `BtnToggle` 為 110×32（高 32 < 44），但僅 PC 滑鼠用、非 iPhone 觸控目標，不受 HIG 約束。
- iPhone 橫向（~19.5:9）下目視排版不重疊、不遮 prompt / MessageBox / 背包格。

> ⚠️ 待 3-E 真機校驗：`get_display_safe_area()` 回傳**物理螢幕像素**，`get_visible_rect().size` 為**邏輯 viewport 單位**；在 `stretch=canvas_items` + `aspect=expand` 縮放下兩者於真機不等比，margin 換算可能需修正。PC（margin=0）與目前桌面比例驗收不受影響。

> 自動測試：`tests/manual/test_runner.gd` 9.1 段驗證 `$Control` 四邊 offset 等於 safe area margin。

### 3-E iOS 真機導出（需 Mac）

- 新增 `export_presets.cfg`（iOS preset：bundle id、orientation = landscape、最低 iOS 版本、icon）；**不入 git 之 secrets / 簽名憑證另議**。
- 導出在 Mac + Xcode 完成；Xcode 開 Personal Team 免費簽名進自己 iPhone（憑證 7 天到期須重簽）。
- 不需付費 Apple Developer Program；付費僅 TestFlight / 上架才需。

待確認實作細節：iOS export preset 欄位於 3-E 開工時回填。（已定：TouchControls 採 (a) autoload；debug 強開旗標來源見上方「顯示規則」段；返回鍵採 `UIMode.mode_changed` signal；R / T 採隨面板態隱藏 / 顯示，未採世界變灰。）

## Phase 4 — 跨場景架構化（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 4`；操作清單見 `測試指南.md > Phase 4`。
> 核心決策：採用 `main.tscn` 常駐宿主 + `WorldRoot` 動態關卡 + `GameUI` 常駐 CanvasLayer。`GameUI` 不做 Autoload；`GameState` / `UIMode` / `TouchControls` 維持 Autoload。

Phase 4 目標是架構遷移與 future-system contract，不做完整 NPC 對話、完整任務、真存檔、完整音樂管理、完整地鐵網、街區正式美術量產。

### 4-A0 檔案結構整理

把目前散在 root 的關卡 / 角色 / 元件腳本搬進語意化資料夾，先收斂路徑再做 main.tscn / GameUI / 第二場景，避免之後反覆改引用。

待搬檔案（root 現況）：

```text
apartment_room.tscn
apartment_room.gd        (+ apartment_room.gd.uid)
player.gd                (+ player.gd.uid)
interactable_area.gd     (+ interactable_area.gd.uid)
```

目標目錄結構（Phase 4 完成後的全貌；4-A0 只負責搬既有檔，標 *新建* 者由後續子階段建立）：

```text
scenes/
├── main/
│   ├── main.tscn            # 4-A 新建
│   └── main.gd              # 4-A 新建
├── levels/
│   ├── apartment/
│   │   ├── apartment_room.tscn   # 4-A0 搬入
│   │   └── apartment_room.gd     # 4-A0 搬入
│   └── street_stub/
│       ├── street_stub.tscn      # 4-F 新建
│       └── street_stub.gd        # 4-F 新建
├── actors/
│   └── player/
│       ├── player.gd             # 4-A0 搬入
│       └── player.tscn           # 目前不存在；Phase 4 不新建 / 不抽出
└── ui/
    ├── game_ui.tscn         # 4-B 新建
    ├── game_ui.gd           # 4-B 新建
    └── （既有 UI 元件不動）
scripts/
├── autoload/
│   ├── game_state.gd        # 不動
│   └── ui_mode.gd           # 不動
└── components/
    └── interactable_area.gd # 4-A0 搬入
```

搬移契約（4-A0 只做這些，不碰邏輯）：

- 用 `git mv` 同時搬 `.gd` 與其 `.gd.uid` sidecar；uid 值不變。
- `player.tscn` 目前不存在；4-A0 只搬 `player.gd` + `player.gd.uid`。Phase 4 期間各 level 可暫時內嵌 `Player` node 並共用 `player.gd`；若 Phase 5+ 需要共用完整 player prefab，再另行規劃。
- `.tscn` 自身 uid 寫在檔案第一行、隨檔移動即可；本專案 `.tscn` 無獨立 `.uid` sidecar。
- `.godot/` 已被 git 忽略，`uid_cache.bin` 為本機快取、不進版控；下次開 editor 自動重建，不需手動處理。
- 唯一允許的內容修改是路徑字串：
  - `apartment_room.tscn` 內三個 `ext_resource` 的 `path`（指向 `apartment_room.gd` / `player.gd` / `interactable_area.gd`）改為新路徑；`uid` 欄位不動。
  - `tests/manual/test_runner.gd` 內 `load("res://apartment_room.tscn")` 改為新路徑（Godot editor 不會自動改 GDScript 字串字面量，必須手動）。
- 全專案僅上述兩檔引用待搬檔案；搬移前以 grep 再確認一次無遺漏引用點。
- `project.godot` 的 `run/main_scene` 是 uid 引用（`uid://f6ts2mjiaoqf` = apartment_room.tscn 自身 uid），場景搬移後 uid 不變，故 4-A0 不需改 `project.godot`；主場景改 `main.tscn` 留待 4-A。

搬移方式（AI 或人工皆可）：

- AI 用 `git mv` + Edit 改上述路徑字串即可；diff 全程明文、可 review、可回滾。
- 完成後人工開一次 Godot editor 目視確認所有場景無 missing resource 紅字（headless 測試只覆蓋公寓，player / interactable 的視覺掛載以 editor 目視最快）。

驗收：headless `test_runner.tscn` / `verify_game_state.gd` PASS；獨立 commit，diff 只含搬檔與路徑字串。

### 4-A Main Host / SceneRouter / SceneRegistry

新增 / 調整檔案（暫定）：

```text
main.tscn
main.gd
scripts/scene_registry.gd        # 可為純 const helper；是否獨立檔依實作簡潔度決定
```

目標場景樹：

```text
Main (Node)
├── WorldRoot (Node2D)
└── GameUI (CanvasLayer)
```

SceneRouter 第一版可先掛在 `main.gd`，不用急著新增 Autoload。需要公開的最小 API：

```gdscript
func transition_to(scene_id: String, entry_point_id: String = "", payload: Dictionary = {}) -> void
func reload_current_scene(entry_point_id: String = "") -> void
func get_current_scene_id() -> String
func get_current_entry_point_id() -> String
```

SceneRegistry 最小資料：

```gdscript
const SCENES := {
	"apartment": {
		"path": "res://scenes/levels/apartment/apartment_room.tscn",
		"default_entry_point_id": "wake_bed",
		"entry_points": ["wake_bed", "from_street"],
		"music_id": "apartment_night"
	},
	"street_stub": {
		"path": "res://scenes/levels/street_stub.tscn",
		"default_entry_point_id": "from_apartment",
		"entry_points": ["from_apartment"],
		"music_id": "street_rain"
	}
}
```

規則：

- `project.godot run/main_scene` 改為 `main.tscn`。
- 任何轉場請求都使用 `scene_id + entry_point_id`；不得在互動物或關卡邏輯中直接寫 `change_scene_to_file()`。
- `transition_to()` 進入新關卡前先關閉所有共用 UI，並確保 `UIMode.set_mode(UIMode.Mode.NONE)`。
- 載入新關卡時，SceneRouter 必須在 `add_child(level)` 前交付 entry point，避免關卡 `_ready()` 先跑錯入口邏輯。建議流程：

```gdscript
var level = packed.instantiate()
if level.has_method("prepare_entry_point"):
	level.prepare_entry_point(entry_point_id, payload)
world_root.add_child(level)
if level.has_method("set_entry_point"):
	level.set_entry_point(entry_point_id, payload)
```

- `prepare_entry_point(entry_point_id, payload)` 用於 `_ready()` 前必須知道的行為（例如公寓是否播放開場）；`set_entry_point(entry_point_id, payload)` 用於節點 ready 後定位玩家到 spawn point。若關卡沒有這些方法，使用 registry 的 `default_entry_point_id`。
- `payload` 只給短期轉場資訊；玩家長期狀態仍歸 `GameState` / 未來 SaveSystem。
- Phase 4 固定入口語意：`apartment:wake_bed` 播開場；`apartment:from_street` 跳過開場並站門邊；`street_stub:from_apartment` 從公寓出去後的街道入口。

### 4-B GameUI 抽離

新增檔案（暫定）：

```text
scenes/ui/game_ui.tscn
scenes/ui/game_ui.gd
```

從 `apartment_room.tscn` 移入 `GameUI` 的共用節點：

```text
PromptPanel
UIOverlay
InventoryPanel
NotebookPanel
DualPaneContainer
ItemDetailModal
ConfirmDialog
MessageBox
PageHintLabel
WorldHUDLabel
FloatingToast
```

保留在關卡 scene 的內容：

```text
Background / parallax / collision
Player
Interactables
SpawnPoints
場景專屬 AudioStreamPlayer / one-shot SFX
公寓專屬 sonar / hidden slot / opening monologue 觸發狀態
```

`GameUI` 最小公開 API：

```gdscript
func set_world_context(level: Node) -> void
func clear_world_context(level: Node = null) -> void

func show_prompt(data: Dictionary) -> void
func hide_prompt() -> void
func has_current_interactable() -> bool
func is_touch_toggle_blocked() -> bool

func show_message(text: String, on_closed: Callable = Callable()) -> void
func begin_message(text: String, options: Dictionary = {}) -> void
func force_finish_message() -> void
func is_message_finished() -> bool
func set_message_page_hint(text: String, visible: bool) -> void
func close_message() -> void
func show_toast(text: String, anchor_node: Control = null) -> void
func open_inventory() -> void
func open_notebook() -> void
func open_container(container_id: String, title: String, slot_count: int) -> void
func close_all_ui(reset_mode: bool = true) -> void

func get_focused_item_context() -> Dictionary
func can_primary_action() -> bool
func can_secondary_action() -> bool
func can_tertiary_action() -> bool
```

`get_focused_item_context()` 回傳空 Dictionary 表示目前沒有 focused item；非空時至少含：

```text
mode
instance_id
item_id
source_pane
available_actions   # ["use" / "move" / "inspect" / "discard"] 等
```

規則：

- `GameUI` 監聽 `UIMode.mode_changed`，統一控制 panel 顯示 / 隱藏與 input active。
- `GameUI` 擁有 Inventory / Notebook / DualPane / MessageBox 的 node path；Level 不得查這些 path。
- `GameUI.show_prompt()` 只負責畫面顯示，不負責選最近互動物。
- `GameUI` 不持有關卡劇情狀態；劇情狀態仍在 Level 或 GameState。
- `show_message(text, on_closed)` 只服務一般單頁訊息。公寓開場多頁流程不得硬塞進此簡化 API。
- `begin_message()` / `force_finish_message()` / `is_message_finished()` / `set_message_page_hint()` / `close_message()` 是 MessageBox 低階 channel，供公寓開場流程控制頁數、T 三連跳、頁尾提示與動畫同步。
- 公寓開場多頁流程仍由 `apartment_room.gd` 控制；`GameUI` 只提供 MessageBox 視覺、打字機與頁尾提示能力，避免 GameUI 持有公寓劇情狀態。
- `show_toast()` 統一生成 / 掛載 FloatingToast；舊 `FloatingToast.show()` 不得再依賴 `get_tree().current_scene.get_node_or_null("UI")`。

### 4-C Level interaction contract

關卡 scene 對 Main / GameUI 的最小 signal（名稱可依實作微調，但語意不可漂移）：

```gdscript
signal current_interactable_changed(data: Dictionary)
signal interaction_requested(data: Dictionary)
signal scene_transition_requested(scene_id: String, entry_point_id: String, payload: Dictionary)
```

互動資料 Dictionary 欄位：

```text
type              # "message" | "container" | "dialogue" | "transition"
id
prompt_text
world_position
message_id
message_text
container_id
container_title
container_slot_count
dialogue_id
target_scene_id
entry_point_id
quest_event_id
payload
```

規則：

- Level 負責 `nearby_interactables`、最近互動物選擇、互動條件判斷、場景專屬 gate。
- `current_interactable_changed({})` 或 `null` 等價語意為沒有互動物；Main / GameUI 必須 hide prompt。
- Main 作為 mediator：接 Level signal，轉呼叫 `GameUI` 或 `transition_to()`。
- `dialogue_id` / `quest_event_id` 在 Phase 4 只需保留資料路徑；沒有 DialogueSystem / QuestManager 時可 placeholder MessageBox 或 noop。
- 互動物不得直接呼叫 `GameUI` 或 `SceneRouter`，避免之後 NPC / 任務 / 地鐵資料化時再拆。

### 4-D TouchControls 改接 GameUI contract

現行 `TouchControls` 已是 Autoload，但目前有讀目前 scene 與 `UI/...` 固定路徑的耦合。Phase 4 必須改成：

```gdscript
var game_ui := get_tree().root.find_child("GameUI", true, false)
```

或由 Main 在 ready 時註冊：

```gdscript
TouchControls.set_game_ui(game_ui)
```

規則：

- 世界模式 E 顯示：使用 `GameUI.has_current_interactable()`。
- Inventory / Container 的 E/R/T 顯示：使用 `GameUI.can_primary_action()` / `can_secondary_action()` / `can_tertiary_action()`。
- PC toggle 顯示：使用 `GameUI.is_touch_toggle_blocked()` 或 Main 提供的等價公開 API。
- TouchControls 不得再查 `scene.get("current_interactable")`、`scene.get("_opening_monologue_active")` 或 `scene.get_node_or_null("UI/InventoryPanel/...")`。
- PC toggle / safe area / 觸控按鈕 layout 不因 GameUI 抽離而重寫，只改資料來源。

### 4-E Apartment migration

`apartment_room.gd` 遷移目標：

- 新增 entry point 欄位與方法：

```gdscript
var _entry_point_id: String = "wake_bed"
var _entry_payload: Dictionary = {}

func prepare_entry_point(entry_point_id: String, payload: Dictionary = {}) -> void:
	_entry_point_id = entry_point_id if not entry_point_id.is_empty() else "wake_bed"
	_entry_payload = payload.duplicate(true)

func set_entry_point(entry_point_id: String, payload: Dictionary = {}) -> void:
	# 節點 ready 後依 entry point 定位 player 到對應 SpawnPoint。
```

`set_entry_point()` 行為：

- `wake_bed`：noop，不覆蓋床上 prone 起始位置；位置與姿態交給既有開場流程控制，避免和 `prone` 擺位打架。
- `from_street`：定位 player 到門邊 SpawnPoint，強制站立 `idle`。

- 刪除對 `$UI/...` 共用 UI 節點的直接依賴。
- 保留公寓專屬 `CONTAINERS` / `MESSAGES` / `NOTES` / sonar / slot unlock / opening monologue 狀態。
- `current_interactable` 可仍為公寓內部狀態，但變更時要 emit `current_interactable_changed(data)` 給 Main / GameUI。
- 開容器時 emit `interaction_requested({"type":"container", ...})`，不直接呼叫 `dual_pane_container`。
- 顯示訊息時 emit `interaction_requested({"type":"message", ...})` 或呼叫 Main 提供的橋接，不直接操作 `MessageBox` node。
- 大門通過 gate 後 emit `scene_transition_requested("street_stub", "from_apartment", {})`，不直接 change scene。
- `_ready()` 中開場啟動必須依 `_entry_point_id` 分支：
  - `wake_bed`：播放既有 prone → MessageBox 多頁 → get_up → idle 開場流程。
  - `from_street`：不播放開場，`_opening_monologue_active = false`，玩家直接 `idle`，由 `set_entry_point()` 放到門邊 spawn。

注意：

- 插槽 `container_changed("apartment_slot")` 觸發的「電磁聲響 → MessageBox → add_knowledge」仍是公寓專屬流程；只把 MessageBox 顯示委託給 `GameUI`。
- `add_knowledge` 時機保持：MessageBox 關閉後寫入。
- `deposit_locked` / accepted item / `GameState` 行為不改。
- 真轉場後，公寓大門不再播放門後 / 街道 BGM。公寓自己的 BGM 可留在公寓 scene；`street_stub` 要嘛靜音，要嘛自己處理簡單場景 BGM。完整 MusicManager 後續再接 `music_id`。

### 4-F 第二場景 stub

新增檔案（暫定）：

```text
scenes/levels/street_stub.tscn
scenes/levels/street_stub.gd
```

最小內容：

```text
Background placeholder
Player spawn point: from_apartment
Interactable: back_to_apartment -> target_scene_id="apartment", entry_point_id="from_street"
Interactable: 1-2 個 message-only examine
```

`street_stub.gd` 必須使用同一套 Level interaction contract，不得另寫一套 UI 控制。
`street_stub.gd` 也必須實作 `set_entry_point("from_apartment", payload)`，將 player 放到 `from_apartment` SpawnPoint；不得依賴場景原點作為入口。

### 4-G Future-system contracts pass

Phase 4 最後檢查以下插槽存在，但不實作完整系統：

- NPC：message-only 互動資料可帶 `dialogue_id`，Main / GameUI 可顯示 placeholder「對話系統未接入」或對應短訊息。
- Quest：互動資料可帶 `quest_event_id`，Phase 4 可 noop；不得塞進 `GameState.knowledge` 假裝任務系統。
- Save：SceneRouter 持有穩定 `current_scene_id` / `entry_point_id`；GameState 長期資料可被未來 SaveSystem 序列化。
- Music：SceneRegistry 有 `music_id`；Phase 4 可不新增 MusicManager，但不再把未來跨場景音樂設計鎖死在公寓節點。轉場後的街道音樂不得由公寓門事件持續播放。
- Metro：轉場 API 已支援任意 `scene_id + entry_point_id`，可被地鐵網呼叫。
- Art：第二場景用 stub，不生成正式街區美術；場景結構先穩定。
