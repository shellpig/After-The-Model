# 開發設計方針

> 實作層文件：檔案結構、Autoload 簽名、signal、資料契約、實作決策。
> **角色歸屬**：implementer-owned。對應 CLAUDE.md「Role Separation」——本檔與程式碼由實作者維護；測試流程 / 已知問題由 verifier 維護。

## 本檔範圍與邊界

| 寫什麼 | 去哪 |
|---|---|
| 系統該有什麼行為、驗收意圖、跨場景通用規格 | `遊戲規格書.md` |
| 公寓專屬劇情 / 互動物 / 線索文字 | `subdocs/地點/主角公寓.md` |
| **檔案清單、autoload 簽名、signal、資料契約、實作決策** | **本檔** |
| 測試流程、手動驗收步驟清單 | `測試指南.md` |
| 已知問題 / 已接受的邊界決定 | `驗證後已知問題.md`（2-E 收尾才建） |

## 起始範圍

- 本檔**自 Phase 2 起新寫**，不 backfill Phase 1 舊細節。
- Phase 1（GameState / UIMode / 背包 / 筆記 / 容器 / 物品操作）的實作細節仍以 `遊戲規格書.md` 為歷史快照來源；需要時 link 回去，不複製。
- 規格書中現存的 GDScript 簽名 / signal / item metadata stub 屬 Phase 1 既成內容，不搬移。

## Phase 2 — 公寓解謎（實作契約）

> 機制鏈與驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 2`；完整流程與文字見 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`。
> 以下各項簽名 / 檔案結構於對應子階段開工時填，未定者不預先杜撰。

### 2-A 線索 examine 互動物

範圍：兩個世界 examine 互動物——桌上電腦（`work_ai_cleanup_role`）、左牆錄音機（`identity_gleaner`）。手套線索（`clue_gloves_decoder`）不在此，見 2-B。

互動物形態（沿用 `interactable_area.gd`，不新增節點類型）：

- `interactable_area.gd` 新增 export：`note_id`（examine 分支用；非 examine 物留空）。
- `apartment_room.gd` 新增 `NOTES` dict（類比現有 `MESSAGES`），key = `note_id`，value = `add_knowledge` 所需的筆記 payload（id / category / title / body / status）。
- `apartment_room.gd` 的派發 `match` 新增 examine 分支：
  - `show_message(message_id)` 顯示現場敘述句（沿用 `MESSAGES`）。
  - `GameState.add_knowledge(NOTES[note_id])` 寫入筆記。
- 現場敘述句（`MESSAGES`）與筆記正文（`NOTES`）分離維護。

行為契約：

- 兩物開局即可 examine，無 `required_knowledge` 前置。
- 重複 examine：每次重播 `show_message`；`add_knowledge` 依其 idempotent 特性不重覆加。若 examine 分支需要控制「已記入筆記」提示是否出現，先用 `GameState.has_note(note_id)` 判斷，不用 `has_knowledge()`。
- 線索筆記文字見 `subdocs/地點/主角公寓.md > Phase 2 解謎設計`。
- Phase 2 開工時，`work_ai_cleanup_role` 不再作為新遊戲預載 demo 筆記；改由桌上電腦 examine 首次寫入，避免 2-A 首次取得驗收被預載污染。

待 content 補：兩物 prompt 文字（`E: 查看螢幕` / `E: 按下播放`）、「已記入筆記」提示字樣。

### 2-B 解碼手套 + 魔術方塊道具

物品與 metadata：

- `worn_rubiks_cube`（魔術方塊，普通玩具）`max_stack=1`；新增 metadata 欄位 `decodable_to = "decoder_cube"` + 所需裝備條件（裝備某具解碼能力的物，公寓即 `fingerless_gloves`）。
- `decoder_cube`（解碼方塊）`max_stack=1`；無 `decodable_to`，搬動不再觸發。
- 解碼能力標在裝備物上（如 `fingerless_gloves` 加一個「具解碼能力」旗標），讓「裝備條件」可組合判斷，不寫死特定手套 id。
- 兩物 id 命名 / icon / description 為 content，定稿前以上列暫名。

轉換觸發接線（兩處，行為一致）：

- **拿上手**：`move_one_item_to("player_inventory", instance_id)` 成功後，controller 讀取該次 `item_moved` payload 的 `target_instance_id`；若目標物有 `decodable_to`、且玩家已裝備滿足條件的裝備 → 更新該 target instance 的 `item_id` 為 `decodable_to`、跳 MessageBox 敘事、`add_knowledge` 解碼方塊筆記。
- **R 查看（fallback）**：ItemDetailModal 開啟中、查看物有 `decodable_to`、裝備條件滿足 → 更新 modal 當前 instance 的 `item_id`；MessageBox 關閉後 modal 刷新顯示 `decoder_cube`。MessageBox 疊在 detail modal 上方期間 detail modal 凍結、不吃鍵盤（capture 暫交 MessageBox），關閉後還原 capture 並刷新（消耗物則連 modal 一起關回 grid）。
- 轉換定位：不得靠背包排序後的格位猜測剛移入物；拿上手路徑一律使用 `item_moved` payload 的 `target_instance_id`。轉換後 UI 上必須表現為「剛操作的那個物品變了」，不得另新增第二個方塊。
- 因 item_id 變更後不再帶 `decodable_to`，天然防重複觸發；無需額外 flag。

手套線索筆記：

- 首次 `equip` 或首次 R 查看 `fingerless_gloves` → `add_knowledge(clue_gloves_decoder)` +「已記入筆記」提示。
- 一次性：以 `has_note("clue_gloves_decoder")` 判斷避免重覆；`has_knowledge()` 只用於身份 gate，不用於線索筆記去重。

不回掃背包：裝備手套**不**觸發背包既有可解碼物的批次轉換；先拿後戴情況由 R 查看補解。

待 content 補：兩物 id 命名定案、icon、解碼前/後 description、解碼 MessageBox 敘事全文、手套訊息字樣。

待確認實作細節：解碼能力旗標掛在裝備物 metadata 的具體欄位名與型別。

### 2-C 容器擴充（accepted_item / deposit_locked）

對應規格：`遊戲規格書.md > External Container API > 容器選項擴充` + `Phase 2 > 2-C 驗收意圖`。

GameState 改動（純資料層）：

- `configure_container(container_id, slot_count, accepted_item: Array[String] = [], deposit_locked: bool = false)`：新增兩個選用參數，預設值使既有呼叫不受影響。旗標隨容器狀態存放（與 slot 內容同一份 runtime state）。
- `get_container_config(container_id) -> Dictionary`：回傳 `slot_count` / `accepted_item` / `deposit_locked` 的 deep copy；UI 判斷失敗訊息時使用此 API。
- `move_one_item_to(target, instance_id)` 守衛新增兩條（在既有「目標可容納」檢查之外）：
  - 目標容器 `accepted_item` 非空且來源 item_id 不在清單 → `return false`（不變動來源/目標）。
  - 來源容器 `deposit_locked == true` → `return false`（不可作為搬運來源）。
- 回傳型別不變（bool）。不在 GameState 層回傳原因碼。移動成功時 emit `item_moved(move)`，供 2-B 等後續機制定位 target instance。

UI 層（接線於 2-D/2-E，2-C 僅資料層）：

- 雙欄移動前，UI 透過 `get_container_config()` 讀取目標容器 `accepted_item` / 來源容器 `deposit_locked` 預判，挑訊息：`放不下了。`（滿）/ `放不進去`（白名單拒）/ 取出被擋字樣（deposit_locked）。
- `move_one_item_to` 為權威 guard，UI 預判失誤也不會破壞資料。

不動：`get_container` / `seed_container` / `discard_item` 行為不變。`seed_container` 為 dev/預載入口，不受白名單約束（插槽不經 seed）。

待 content 補：deposit_locked「取出被擋」訊息字樣（2-E 定）。

待確認實作細節：旗標存放結構（與 container slots 同 dict 或分開 metadata dict）。

### 2-D 偵測終端（投影時鐘）+ 聲納 reveal

對應規格：`遊戲規格書.md > Phase 2 > 2-D 驗收意圖`；流程與文字見 `subdocs/地點/主角公寓.md`。

線索麵包屑道具（查看即消耗）：

- 新增 item `nutrition_bar_synth_blueberry`（合成藍莓口味營養棒，`category=consumable`，`max_stack` 待定），預載於 `fridge_storage`。
- icon 使用 `res://assets/generated/sprites/items/synthetic_blueberry_nutrition_bar/icon.png`。
- item metadata 新增欄位 `consume_grants_note`（String，note_id）。帶此欄位的物經 **E 使用 或 R 查看** 任一觸發：跳 MessageBox 顯示空包裝藏字敘述 → `add_knowledge`(該 note payload) → 移除該 instance（消耗）；R 路徑於 message 關閉後一併關 modal。兩路徑同效，仿手套解碼雙觸發。
- 一次性天然成立：instance 移除後不再存在，無需 flag。
- 移除 instance 沿用既有 discard/remove 容器路徑（在背包或外部容器皆可消耗）。

背包 E 使用動作（Phase 1-E footer 修正，2-D 連帶；對應規格書「物品操作」段更新）：

- 背包單獨（`I`）footer 的 E 段改為**依選中物品動態決定**（取代 Phase 1-E 固定字串）：
  - 可裝備物 → `E: 裝備`（未裝備）/ `E: 卸下`（已裝備）。
  - `category == "consumable"` 或標記可反覆使用 → `E: 使用`。
  - 其餘（無對應動作，如 `old_work_badge`）→ E 段不顯示。
- 按 E dispatch（單獨背包內）：
  - 可裝備 → 既有 equip / unequip。
  - 帶 `consume_grants_note` 的 consumable → 上述消耗 + 給筆記流程。
  - 無效果 consumable（如 `canned_food`）→ FloatingToast「現在用不上。」，不消耗。
  - 可反覆使用物 → 執行效果、不消耗（預見類型，暫無實例，不提前實作）。
- 雙欄左欄（背包欄）E 仍為「移動」，不變；`使用` 只在單獨背包出現。R 查看路徑於雙欄 / 單獨皆可，故營養棒在冰箱雙欄按 R 亦可消耗得線索。

待確認實作細節：「可裝備」「可反覆使用」如何由 item metadata 判定（category vs. 專屬 flag）；footer 動態字串組裝點；instance 消耗走 `discard_item` 或新 remove API。

投影時鐘互動物（= 偵測終端）：

- 沿用 `interactable_area.gd`；`required_knowledge` = 投影時鐘線索 note_id（取得線索前沿用既有 gate，不顯示 prompt / 不可互動）。
- 互動 E → 呼叫聲納啟動（見下）。

聲納狀態機（掛 `apartment_room.gd` 或抽獨立 helper node，二擇一待定）：

- 啟動 = 進入聲納 active 狀態 + 開 10 秒倒數計時器。
- 每 tick 依玩家 x 與隱藏插槽 x 的 1D 距離算強度 → 驅動短音播放頻率 + 視覺強度條；聲納短音資源為 `res://assets/sound/sonar_ping.wav`。
- 玩家在最強區帶寬內（`abs(dx) <= 36`）累積鎖定進度 4 秒（鎖定環）；離開帶寬則鎖定進度重置（容許帶寬內微動，不要求像素級不動）。prompt 文字「定位中…」。
- 鎖定環滿 → reveal：播放 `res://assets/sound/hidden_slot_reveal.wav`，顯示隱藏插槽為一般互動容器（`configure_container("apartment_slot", 1, ["decoder_cube"], true)`，見 2-C）；聲納狀態關閉、移除投影時鐘互動。
- 10 秒歸零且未鎖定 → 聲納關閉、不 reveal；投影時鐘仍可再互動重啟（狀態機可重入）。
- UIMode 進大型 UI（INVENTORY/NOTEBOOK 等）時暫停聲納 tick + 倒數 + 鎖定進度（不歸零）；回 NONE 恢復。
- reveal 後聲納不可再啟動（投影時鐘互動已移除，狀態機對重複啟動 idempotent）。

待 content 補：強度→短音播放頻率映射、10 秒是否可調、強度條視覺、鎖定環視覺、營養棒/線索/時鐘 prompt 與敘述文字、`nutrition_bar_synth_blueberry` max_stack。

待確認實作細節：聲納狀態機掛 `apartment_room.gd` 內或抽獨立 node；視覺強度條 UI 元件歸屬；鎖定環滿秒數與 10 秒窗的關係（dwell 須 ≤ window）；隱藏插槽 container_id 命名。

### 2-E 插槽放入整合 + MESSAGE/CONFIRM overlay 重構

對應規格：`遊戲規格書.md > Phase 2 > 2-E 驗收意圖` + `UIMode > overlay modal 的 caller 還原`。

UIMode overlay 重構（先做，放入鏈依賴它）：

- `MESSAGE` 改為與 `CONFIRM` 同類的 **overlay modal**：進入記住 caller mode、不關 caller UI、退出還原焦點。
- 既有 `enter_confirm` / `exit_confirm` 一般化為 `enter_overlay(mode)` / `exit_overlay()`，`MESSAGE` / `CONFIRM` 共用 `_caller_mode`。
- caller UI 在 overlay 期間凍結接鍵（不吃方向 / E / R / T）；overlay 優先吃鍵、推進文字、`Esc` 關閉。
- 遷移既有呼叫：examine(2-A) / 解碼(2-B) 開 MessageBox 改走 `enter_overlay(MESSAGE)`（caller 多為 NONE / INVENTORY），視覺行為不變。
- MVP 單層 overlay（不疊 MESSAGE on CONFIRM）。
- 回歸：1-B~1-E 大型 UI 互切、CONFIRM 丟棄確認、focus 還原全測。

放入觸發鏈（`apartment_room.gd`）：

- listen `container_changed("apartment_slot")` → 若 `decoder_cube` 在槽內 且 未 `has_knowledge("identity_door_unlock_method")` → 跑鏈。
- 鏈：播電磁聲響（音效待定）→ `enter_overlay(MESSAGE)` 顯示語音 MessageBox（疊在雙欄上）→ 關閉（`exit_overlay`）後 `add_knowledge(identity_door_unlock_method payload)`。
- 一次性守衛：因 `add_knowledge` 延後到 MessageBox 關閉才寫，逐字顯示窗內 `has_knowledge` 仍為 false，需另設 transient flag。`apartment_room.gd` 持 `_slot_unlock_sequence_started: bool`；觸發條件為 `!_slot_unlock_sequence_started and !has_knowledge(...)`，進鏈第一步即 `_slot_unlock_sequence_started = true`，防同窗重入。`deposit_locked` 另使該槽不再變動，`container_changed` 長期亦不再為此槽觸發。
- `add_knowledge` 時機：MessageBox 關閉（`exit_overlay`）之後寫入。
- 完成後插槽常駐、方塊鎖在格內（deposit_locked），雙欄可開看、不可取出。
- 語音 / 筆記全文見 `subdocs/地點/主角公寓.md > 互動物：隱藏插槽`。

大門（B9，不改現有程式）：

- 沿用 `DoorExitArea` 的 `has_knowledge("identity_door_unlock_method")` gate；MVP 顯示 `door_opened`，不轉場。

待 content 補：電磁聲響音效素材。

待確認實作細節：聲響與 MessageBox 的先後 / 同步時序；overlay 是否需 `get_base_mode()` 之類查詢（MVP 用 `_caller_mode` 即足）；遷移 enter_confirm 既有 caller 的相容性。

### 2-F 筆記內容/操作修正 + 公寓 BGM

對應規格：`遊戲規格書.md > Phase 2 > 2-F 驗收意圖` + `筆記 / 知識系統`（操作段 2-F 修正）+ `音訊（BGM）> 公寓 BGM`。皆為既有系統小幅修正，無新機制、無新 GameState API。

筆記內容（`apartment_room.gd._ready()` 預載）：

- 將第三筆預載 `clue_long_scroll_test` 整筆改寫為 `identity_rainy_night`：id / category(`身份`) / title / body 全換，文字以 `subdocs/地點/主角公寓.md > 筆記預載` 為準。
- 改 category 為 `身份` 後 `add_knowledge` 會連帶設 `knowledge["identity_rainy_night"]=true`；無程式碼讀此 key，無副作用。舊 id `clue_long_scroll_test` 亦無任何引用。

筆記操作（`scenes/ui/notebook_panel.gd`）：

- `_unhandled_input` 切 tab 由 `ui_left`/`ui_right` 擴為 **同時接 `move_left`/`move_right`**（比照 `bag_grid.gd` 的 `ui_* or move_*` 寫法）。
- 選筆記上下：補 `move_up`/`move_down`（W/S）等同既有 `ui_up`/`ui_down`（方向鍵）的列表 focus 移動；維持邊界不繞回。
- footer 字串改 `A/D: 切分頁    W/S: 選筆記    I: 背包    Esc/J: 關閉`（含 `is_input_active` 兩處字串）。
- Page Up / Down 既有的吞掉分支保留（已無功能）；筆記皆一頁內，不再實作捲動。

公寓 BGM（`apartment_room.tscn`）：

- 新增子節點 `AudioStreamPlayer`：`stream` = `res://assets/bgm/Echoes of a Cozy Night.mp3`（匯入設定開 Loop）、`autoplay = true`、`volume_db = -12.0`。
- 純場景節點即可，不寫 autoload、不加場景腳本邏輯（缺檔時 `stream` 為 null → 不播放、不報錯）。

待確認實作細節：`notebook_panel.gd` 列表上下選筆記目前是否靠 Godot 內建 focus 鏈（`ui_up`/`ui_down`）；若是，W/S 需在 `_unhandled_input` 內手動推進 focus 或重映射，避免只有方向鍵能動。實作時以實機確認 W/S 真能選筆記。

### 2-G 開場獨白序列（實作契約）

對應規格：`遊戲規格書.md > Phase 2 > 2-G 驗收意圖`；文案全文見 `subdocs/地點/主角公寓.md > 開場序列（B0 開場獨白，2-G）`。

新遊戲進公寓即播的線性開場：prone 甦醒 → 4 頁不可跳過 MessageBox → 第 4 頁起身動畫 → 解鎖自由操作。沿用既有 MessageBox 視覺 / 打字機與玩家 `AnimatedSprite2D`，無新 GameState API。

動畫資產（`apartment_room.tscn` 內玩家 `AnimatedSprite2D` 的 SpriteFrames）：

- 新增 `prone`：單格靜態 = `res://assets/generated/sprites/main_character/get_up/main-character-get-up-10f-fixed-scale-20260530-124840/main-character-get-up-aligned-01.png`（get_up 序列第 1 格）。
- 新增 `get_up`：`main-character-get-up-aligned-01`→`-10` 共 10 frame，`speed = 10 fps`、`loop = false`。
- `idle` / `walk` 不動；`sleep_prone/` 資料夾 2-G 不使用。

開場流程（建議掛 `apartment_room.gd`，做成獨立開場 state，不污染泛用 `MESSAGE` overlay）：

- `_ready` 啟動開場（MVP 無條件啟動，等同「每次進場都播」）：`player.anim.play("prone")`、開場接管輸入（player 因 `is_world_input_blocked()` 不動）、`prompt_panel.visible = false`、開 MessageBox 顯示第 1 頁。
- 開場專用分頁陣列（4 筆），每筆 = 標題（正文第一行）+ 內文；逐頁餵給既有 `_start_message_typewriter`。
- **不可跳過 guard**：本頁打字機未完成（`message_label.text.length() < message_full_text.length()`，或自記 `_opening_page_done` flag）時，換頁鍵全部吞掉不作用。
- 本頁跑完 → 設 `_opening_page_done = true`、淡入換頁提示 Label（前三頁「▼ 繼續」、末頁「▼ 開始」）。
- 換頁鍵 = `move_left/right/up/down`（WASDE）/ `ui_*`（方向鍵）/ `ui_cancel`（Esc）任一 `just_pressed`，且 `_opening_page_done` 為真才前進。
- 進第 4 頁時 `player.anim.play("get_up")`；接 `animation_finished` → `player.anim.play("idle")`（或固定 1 秒計時切換）。文字續跑、不等動畫。
- 末頁 done + 換頁鍵 → 關 MessageBox、退出開場狀態、解除輸入封鎖；強制 `player.anim.play("idle")`（防 get_up 未播完即被跳過）。

隱藏測試捷徑（dev skip）：

- 開場狀態內維護 T 連按計數 + 末次時間戳；`interact_drop`（T）於 `1.0` 秒窗內第 3 次 `just_pressed` → 立即補完當頁文字（`message_label.text = message_full_text` + 設 done）並前進一頁；末頁則關閉開場、解鎖。
- 逾時或非連續 → 計數歸零。
- 不在換頁提示中標示。

輸入接管邊界：

- 開場期間靠開場 state 自身吞掉所有世界互動與大型 UI 開啟鍵（I / J / E）；只放行換頁鍵與隱藏 T。
- 二擇一（實作時定，並回填本段）：(a) 獨立開場 state（UIMode 維持 `NONE`，開場 state 在 `_unhandled_input` / `_process` 攔鍵）；(b) 沿用 `enter_overlay(MESSAGE)` 並擴充其「多頁 + 不可跳過 + 換頁提示 + 動畫鉤子」。MVP 傾向 (a)，較不破壞既有 MESSAGE 回歸。

待 content 補：換頁提示精確字串樣式（顏色 / 淡入時間）；4 頁文案以 subdoc 為準。

待確認實作細節：開場 state 掛 `apartment_room.gd` 內或抽獨立節點；get_up→idle 切換用 `animation_finished` 或固定 1 秒計時；換頁提示 Label 歸屬節點；獨立 state vs. `MESSAGE` overlay 擴充最終擇一。

## Phase 3 — 公寓觸控化（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 3`；操作清單見 `測試指南.md > Phase 3`。
> 核心前提：全遊戲互動已只 listen 抽象 action（`apartment_room.gd` / `scenes/ui/*.gd` 全用 `Input.is_action_*` 或 `event.is_action_pressed`），故觸控層只送 action、**不改任何面板邏輯**。各項簽名 / 節點歸屬於對應子階段開工時填，未定者不預先杜撰。

### 3-A 顯示 / 橫向 / 觸控模擬設定（`project.godot`）

純設定變更，無新檔。預計新增 / 確認下列鍵：

```ini
[display]
window/handheld/orientation="landscape"
window/stretch/mode="canvas_items"   ; 已有
window/stretch/aspect="expand"       ; 未顯式設定則補

[input_devices]
pointing/emulate_touch_from_mouse=true
pointing/emulate_mouse_from_touch=true
```

- `renderer/rendering_method.mobile` 已是 `gl_compatibility`，不動。
- 不改 `[input]` 既有 action 綁定（實體鍵保留供 PC）；觸控按鈕在 3-B 以程式送 action，不靠 InputMap 加觸控事件。
- 回歸保證：鍵盤路徑與 `test_runner` / `verify_game_state` 不受影響。

### 3-B / 3-C TouchControls 元件

新增檔案（暫定）：

```text
scenes/ui/touch_controls.tscn   # CanvasLayer 根，含方向鍵 / E·R·T / 背包·筆記·返回 按鈕
scenes/ui/touch_controls.gd     # 按鈕 → action 派送、顯示規則、面板狀態切換
```

掛載方式（二擇一，實作時定並回填）：(a) 新增 autoload 把 `touch_controls.tscn` instance 到一個高 layer 的 CanvasLayer，全場景自動繼承；(b) 各場景手動放一份。**傾向 (a)**，讓 Phase 4+ 新場景免重工。`CanvasLayer.layer` 須高於遊戲與既有 UI overlay。

按鈕 → action 對應表：

| 按鈕（位置） | action | 階段 |
|---|---|---|
| ← / →（左下） | `move_left` / `move_right`（世界走路）、面板內 `ui_left` / `ui_right` 鏡像 | 3-B / 3-C |
| ↑ / ↓（左下） | `move_up` / `move_down` + `ui_up` / `ui_down`（面板焦點） | 3-C |
| E（右下） | `interact_primary` | 3-B |
| R / T（右下） | `interact_secondary` / `interact_tertiary` | 3-C |
| 背包（右上常駐） | `open_inventory` | 3-B |
| 筆記（右上常駐） | `open_notebook` | 3-B |
| X 返回（右上，面板開時） | `ui_cancel` | 3-C |

派送機制（3-B 實作回填）：統一在按鈕 `button_down` → 送 `pressed=true` 的 `InputEventAction`、`button_up` → 送 `pressed=false` 的 `InputEventAction`，經 `Input.parse_input_event()` 灌入。如此方向鍵長按 = 持續 `is_action_pressed`（走路）、輕點 = 單次 `is_action_just_pressed`（面板移一格 / 互動），一套機制覆蓋兩種需求，無須分流。

> 註：與 `Input.action_press()` / `Input.action_release()` 行為等價，最終採 `InputEventAction` + `Input.parse_input_event()`（見 `scenes/ui/touch_controls.gd > _simulate_action`）。另每顆按鈕設 `focus_mode = FOCUS_NONE`，防止觸控點擊搶走鍵盤焦點導致方向鍵失靈。

顯示規則（最終實作，3-D 收尾改寫）：原 `DisplayServer.is_touchscreen_available()` 方案因**部分 Windows PC（含觸控螢幕筆電）誤判**而棄用。現行：

- CanvasLayer `self.visible = true` **恆真**（PC 端需常駐左上角開關按鈕）。
- `_ready` 以 `OS.get_name()` 判平台：`Windows / macOS / Linux / *BSD / UWP` 視為 PC（`is_pc_platform = true`）。
- `touch_buttons_enabled` 預設：手機平台 `true`（直接啟用），PC 平台 `false`（需手動開）。
- **PC 專屬 `BtnToggle`（左上「[ 觸控:開/關 ]」）**：僅在 PC + 世界 `NONE` 模式 + 開場獨白播畢時顯示，點擊翻轉 `touch_buttons_enabled`。PC 且未啟用時，D-pad / Actions / Menus 全隱藏（讓鍵盤玩家畫面乾淨）。
- `@export var force_show_in_debug` 仍保留宣告但**目前未被引用**（孤兒旗標，留待清理或重新接線）。

返回鍵 / 面板態切換（最終實作）：右上在「世界模式顯示 背包·筆記」與「面板開啟顯示 X 返回」間切換。`BtnClose` 綁 `ui_cancel`，可退出各面板層（含 Modal / Confirm）。

切換時機採**雙軌**（統一在 `_update_dynamic_button_visibility()` 內計算）：

- `UIMode.mode_changed(new_mode)` signal 觸發時立即更新一次（最即時響應）；
- **外加 `_process` 每幀輪詢**——因 E / R / T 的顯示已升級為「情境感知」（見下），需逐幀反應 `current_interactable` / 聚焦格狀態等不會發出 `mode_changed` 的變化。（原 3-C 設想的「純 signal、不輪詢」未採用。）

背包·筆記 vs X 返回：`mode == NONE` 顯示背包·筆記、隱藏返回；否則反之。

E / R / T 情境感知顯示（超出原 3-C「v1 固定顯示」，3-D 期一併做的 polish）：

- **世界 NONE**：R / T 永遠隱藏；E 僅在 `scene.current_interactable != null` 時浮現。
- **INVENTORY / CONTAINER**：依聚焦格內容判定——空格全隱；佔用格 R = 查看恆顯，E 視 category（equipment / consumable / `consume_grants_note`）有無動作，T 視 `discardable` 且非裝備中。
- **NOTEBOOK**：E / R / T 全隱（只翻頁）。
- **MESSAGE**：E（繼續）+ T（連點三下 skip 獨白）顯示，R 隱。
- **CONFIRM**：E（確定）顯示，R / T 隱（取消走 X 返回 = `ui_cancel`）。

（見 `scenes/ui/touch_controls.gd > _update_dynamic_button_visibility`。）

### 3-D Safe area + 比例排版（實作回填）

- **Safe area**：`_ready` 及 `get_viewport().size_changed` 時呼叫 `_update_safe_area()`，讀 `DisplayServer.get_display_safe_area()`，算四邊 margin（`margin_left = safe.position.x`、`margin_right = viewport.x - safe.end.x`，上下同理），套到 full-rect（anchors_preset 15）的根 `$Control` 之 `offset_left/top/right/bottom`，整組按鈕隨之內縮。PC 上 safe area = 全螢幕 → margin 全 0（no-op）。
- **按鈕尺寸**（≥ 44×44 px）：D-pad 54×54、功能鍵 E/R/T/背包/筆記/X 統一 60×60；皆用 `custom_minimum_size` + Container 自適應，不寫死座標。
- **例外**：PC 專屬 `BtnToggle` 為 110×32（高 32 < 44），但僅 PC 滑鼠用、非 iPhone 觸控目標，不受 HIG 約束。
- iPhone 橫向（~19.5:9）下目視排版不重疊、不遮 prompt / MessageBox / 背包格。

> ⚠️ 待 3-E 真機校驗：`get_display_safe_area()` 回傳**物理螢幕像素**，`get_visible_rect().size` 為**邏輯 viewport 單位**；在 `stretch=canvas_items` + `aspect=expand` 縮放下兩者於真機不等比，margin 換算可能需修正。PC（margin=0）與目前桌面比例驗收不受影響。

> 自動測試：`tests/manual/test_runner.gd` 9.1 段驗證 `$Control` 四邊 offset 等於 safe area margin。

### 3-E iOS 真機導出（需 Mac）

- 新增 `export_presets.cfg`（iOS preset：bundle id、orientation = landscape、最低 iOS 版本、icon）；**不入 git 之 secrets / 簽名憑證另議**。
- 導出在 Mac + Xcode 完成；Xcode 開 Personal Team 免費簽名進自己 iPhone（憑證 7 天到期須重簽）。
- 不需付費 Apple Developer Program；付費僅 TestFlight / 上架才需。

待確認實作細節：iOS export preset 欄位於 3-E 開工時回填。（已定：TouchControls 採 (a) autoload；debug 強開旗標來源見上方「顯示規則」段；返回鍵採 `UIMode.mode_changed` signal；R / T 採隨面板態隱藏 / 顯示，未採世界變灰。）

## Phase 4 — 跨場景架構化（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 4`；操作清單見 `測試指南.md > Phase 4`。
> 核心決策：採用 `main.tscn` 常駐宿主 + `WorldRoot` 動態關卡 + `GameUI` 常駐 CanvasLayer。`GameUI` 不做 Autoload；`GameState` / `UIMode` / `TouchControls` 維持 Autoload。

Phase 4 目標是架構遷移與 future-system contract，不做完整 NPC 對話、完整任務、真存檔、完整音樂管理、完整地鐵網、街區正式美術量產。

### 4-A0 檔案結構整理

把目前散在 root 的關卡 / 角色 / 元件腳本搬進語意化資料夾，先收斂路徑再做 main.tscn / GameUI / 第二場景，避免之後反覆改引用。

待搬檔案（root 現況）：

```text
apartment_room.tscn
apartment_room.gd        (+ apartment_room.gd.uid)
player.gd                (+ player.gd.uid)
interactable_area.gd     (+ interactable_area.gd.uid)
```

目標目錄結構（Phase 4 完成後的全貌；4-A0 只負責搬既有檔，標 *新建* 者由後續子階段建立）：

```text
scenes/
├── main/
│   ├── main.tscn            # 4-A 新建
│   └── main.gd              # 4-A 新建
├── levels/
│   ├── apartment/
│   │   ├── apartment_room.tscn   # 4-A0 搬入
│   │   └── apartment_room.gd     # 4-A0 搬入
│   └── street_stub/
│       ├── street_stub.tscn      # 4-F 新建
│       └── street_stub.gd        # 4-F 新建
├── actors/
│   └── player/
│       ├── player.gd             # 4-A0 搬入
│       └── player.tscn           # 目前不存在；Phase 4 不新建 / 不抽出
└── ui/
    ├── game_ui.tscn         # 4-B 新建
    ├── game_ui.gd           # 4-B 新建
    └── （既有 UI 元件不動）
scripts/
├── autoload/
│   ├── game_state.gd        # 不動
│   └── ui_mode.gd           # 不動
└── components/
    └── interactable_area.gd # 4-A0 搬入
```

搬移契約（4-A0 只做這些，不碰邏輯）：

- 用 `git mv` 同時搬 `.gd` 與其 `.gd.uid` sidecar；uid 值不變。
- `player.tscn` 目前不存在；4-A0 只搬 `player.gd` + `player.gd.uid`。Phase 4 期間各 level 可暫時內嵌 `Player` node 並共用 `player.gd`；若 Phase 5+ 需要共用完整 player prefab，再另行規劃。
- `.tscn` 自身 uid 寫在檔案第一行、隨檔移動即可；本專案 `.tscn` 無獨立 `.uid` sidecar。
- `.godot/` 已被 git 忽略，`uid_cache.bin` 為本機快取、不進版控；下次開 editor 自動重建，不需手動處理。
- 唯一允許的內容修改是路徑字串：
  - `apartment_room.tscn` 內三個 `ext_resource` 的 `path`（指向 `apartment_room.gd` / `player.gd` / `interactable_area.gd`）改為新路徑；`uid` 欄位不動。
  - `tests/manual/test_runner.gd` 內 `load("res://apartment_room.tscn")` 改為新路徑（Godot editor 不會自動改 GDScript 字串字面量，必須手動）。
- 全專案僅上述兩檔引用待搬檔案；搬移前以 grep 再確認一次無遺漏引用點。
- `project.godot` 的 `run/main_scene` 是 uid 引用（`uid://f6ts2mjiaoqf` = apartment_room.tscn 自身 uid），場景搬移後 uid 不變，故 4-A0 不需改 `project.godot`；主場景改 `main.tscn` 留待 4-A。

搬移方式（AI 或人工皆可）：

- AI 用 `git mv` + Edit 改上述路徑字串即可；diff 全程明文、可 review、可回滾。
- 完成後人工開一次 Godot editor 目視確認所有場景無 missing resource 紅字（headless 測試只覆蓋公寓，player / interactable 的視覺掛載以 editor 目視最快）。

驗收：headless `test_runner.tscn` / `verify_game_state.gd` PASS；獨立 commit，diff 只含搬檔與路徑字串。

### 4-A Main Host / SceneRouter / SceneRegistry

新增 / 調整檔案（暫定）：

```text
main.tscn
main.gd
scripts/scene_registry.gd        # 可為純 const helper；是否獨立檔依實作簡潔度決定
```

目標場景樹：

```text
Main (Node)
├── WorldRoot (Node2D)
└── GameUI (CanvasLayer)
```

SceneRouter 第一版可先掛在 `main.gd`，不用急著新增 Autoload。需要公開的最小 API：

```gdscript
func transition_to(scene_id: String, entry_point_id: String = "", payload: Dictionary = {}) -> void
func reload_current_scene(entry_point_id: String = "") -> void
func get_current_scene_id() -> String
func get_current_entry_point_id() -> String
```

SceneRegistry 最小資料：

```gdscript
const SCENES := {
	"apartment": {
		"path": "res://scenes/levels/apartment/apartment_room.tscn",
		"default_entry_point_id": "wake_bed",
		"entry_points": ["wake_bed", "from_street"],
		"music_id": "apartment_night"
	},
	"street_stub": {
		"path": "res://scenes/levels/street_stub.tscn",
		"default_entry_point_id": "from_apartment",
		"entry_points": ["from_apartment"],
		"music_id": "street_rain"
	}
}
```

規則：

- `project.godot run/main_scene` 改為 `main.tscn`。
- 任何轉場請求都使用 `scene_id + entry_point_id`；不得在互動物或關卡邏輯中直接寫 `change_scene_to_file()`。
- `transition_to()` 進入新關卡前先關閉所有共用 UI，並確保 `UIMode.set_mode(UIMode.Mode.NONE)`。
- 載入新關卡時，SceneRouter 必須在 `add_child(level)` 前交付 entry point，避免關卡 `_ready()` 先跑錯入口邏輯。建議流程：

```gdscript
var level = packed.instantiate()
if level.has_method("prepare_entry_point"):
	level.prepare_entry_point(entry_point_id, payload)
world_root.add_child(level)
if level.has_method("set_entry_point"):
	level.set_entry_point(entry_point_id, payload)
```

- `prepare_entry_point(entry_point_id, payload)` 用於 `_ready()` 前必須知道的行為（例如公寓是否播放開場）；`set_entry_point(entry_point_id, payload)` 用於節點 ready 後定位玩家到 spawn point。若關卡沒有這些方法，使用 registry 的 `default_entry_point_id`。
- `payload` 只給短期轉場資訊；玩家長期狀態仍歸 `GameState` / 未來 SaveSystem。
- Phase 4 固定入口語意：`apartment:wake_bed` 播開場；`apartment:from_street` 跳過開場並站門邊；`street_stub:from_apartment` 從公寓出去後的街道入口。

### 4-B GameUI 抽離

新增檔案（暫定）：

```text
scenes/ui/game_ui.tscn
scenes/ui/game_ui.gd
```

從 `apartment_room.tscn` 移入 `GameUI` 的共用節點：

```text
PromptPanel
UIOverlay
InventoryPanel
NotebookPanel
DualPaneContainer
ItemDetailModal
ConfirmDialog
MessageBox
PageHintLabel
WorldHUDLabel
FloatingToast
```

保留在關卡 scene 的內容：

```text
Background / parallax / collision
Player
Interactables
SpawnPoints
場景專屬 AudioStreamPlayer / one-shot SFX
公寓專屬 sonar / hidden slot / opening monologue 觸發狀態
```

`GameUI` 最小公開 API：

```gdscript
func set_world_context(level: Node) -> void
func clear_world_context(level: Node = null) -> void

func show_prompt(data: Dictionary) -> void
func hide_prompt() -> void
func has_current_interactable() -> bool
func is_touch_toggle_blocked() -> bool

func show_message(text: String, on_closed: Callable = Callable()) -> void
func begin_message(text: String, options: Dictionary = {}) -> void
func force_finish_message() -> void
func is_message_finished() -> bool
func set_message_page_hint(text: String, visible: bool) -> void
func close_message() -> void
func show_toast(text: String, anchor_node: Control = null) -> void
func open_inventory() -> void
func open_notebook() -> void
func open_container(container_id: String, title: String, slot_count: int) -> void
func close_all_ui(reset_mode: bool = true) -> void

func get_focused_item_context() -> Dictionary
func can_primary_action() -> bool
func can_secondary_action() -> bool
func can_tertiary_action() -> bool
```

`get_focused_item_context()` 回傳空 Dictionary 表示目前沒有 focused item；非空時至少含：

```text
mode
instance_id
item_id
source_pane
available_actions   # ["use" / "move" / "inspect" / "discard"] 等
```

規則：

- `GameUI` 監聽 `UIMode.mode_changed`，統一控制 panel 顯示 / 隱藏與 input active。
- `GameUI` 擁有 Inventory / Notebook / DualPane / MessageBox 的 node path；Level 不得查這些 path。
- `GameUI.show_prompt()` 只負責畫面顯示，不負責選最近互動物。
- `GameUI` 不持有關卡劇情狀態；劇情狀態仍在 Level 或 GameState。
- `show_message(text, on_closed)` 只服務一般單頁訊息。公寓開場多頁流程不得硬塞進此簡化 API。
- `begin_message()` / `force_finish_message()` / `is_message_finished()` / `set_message_page_hint()` / `close_message()` 是 MessageBox 低階 channel，供公寓開場流程控制頁數、T 三連跳、頁尾提示與動畫同步。
- 公寓開場多頁流程仍由 `apartment_room.gd` 控制；`GameUI` 只提供 MessageBox 視覺、打字機與頁尾提示能力，避免 GameUI 持有公寓劇情狀態。
- `show_toast()` 統一生成 / 掛載 FloatingToast；舊 `FloatingToast.show()` 不得再依賴 `get_tree().current_scene.get_node_or_null("UI")`。

### 4-C Level interaction contract

關卡 scene 對 Main / GameUI 的最小 signal（名稱可依實作微調，但語意不可漂移）：

```gdscript
signal current_interactable_changed(data: Dictionary)
signal interaction_requested(data: Dictionary)
signal scene_transition_requested(scene_id: String, entry_point_id: String, payload: Dictionary)
```

互動資料 Dictionary 欄位：

```text
type              # "message" | "container" | "dialogue" | "transition"
id
prompt_text
world_position
message_id
message_text
container_id
container_title
container_slot_count
dialogue_id
target_scene_id
entry_point_id
quest_event_id
payload
```

規則：

- Level 負責 `nearby_interactables`、最近互動物選擇、互動條件判斷、場景專屬 gate。
- `current_interactable_changed({})` 或 `null` 等價語意為沒有互動物；Main / GameUI 必須 hide prompt。
- Main 作為 mediator：接 Level signal，轉呼叫 `GameUI` 或 `transition_to()`。
- `dialogue_id` / `quest_event_id` 在 Phase 4 只需保留資料路徑；沒有 DialogueSystem / QuestManager 時可 placeholder MessageBox 或 noop。
- 互動物不得直接呼叫 `GameUI` 或 `SceneRouter`，避免之後 NPC / 任務 / 地鐵資料化時再拆。

### 4-D TouchControls 改接 GameUI contract

現行 `TouchControls` 已是 Autoload，但目前有讀目前 scene 與 `UI/...` 固定路徑的耦合。Phase 4 必須改成：

```gdscript
var game_ui := get_tree().root.find_child("GameUI", true, false)
```

或由 Main 在 ready 時註冊：

```gdscript
TouchControls.set_game_ui(game_ui)
```

規則：

- 世界模式 E 顯示：使用 `GameUI.has_current_interactable()`。
- Inventory / Container 的 E/R/T 顯示：使用 `GameUI.can_primary_action()` / `can_secondary_action()` / `can_tertiary_action()`。
- PC toggle 顯示：使用 `GameUI.is_touch_toggle_blocked()` 或 Main 提供的等價公開 API。
- TouchControls 不得再查 `scene.get("current_interactable")`、`scene.get("_opening_monologue_active")` 或 `scene.get_node_or_null("UI/InventoryPanel/...")`。
- PC toggle / safe area / 觸控按鈕 layout 不因 GameUI 抽離而重寫，只改資料來源。

### 4-E Apartment migration

`apartment_room.gd` 遷移目標：

- 新增 entry point 欄位與方法：

```gdscript
var _entry_point_id: String = "wake_bed"
var _entry_payload: Dictionary = {}

func prepare_entry_point(entry_point_id: String, payload: Dictionary = {}) -> void:
	_entry_point_id = entry_point_id if not entry_point_id.is_empty() else "wake_bed"
	_entry_payload = payload.duplicate(true)

func set_entry_point(entry_point_id: String, payload: Dictionary = {}) -> void:
	# 節點 ready 後依 entry point 定位 player 到對應 SpawnPoint。
```

`set_entry_point()` 行為：

- `wake_bed`：noop，不覆蓋床上 prone 起始位置；位置與姿態交給既有開場流程控制，避免和 `prone` 擺位打架。
- `from_street`：定位 player 到門邊 SpawnPoint，強制站立 `idle`。

- 刪除對 `$UI/...` 共用 UI 節點的直接依賴。
- 保留公寓專屬 `CONTAINERS` / `MESSAGES` / `NOTES` / sonar / slot unlock / opening monologue 狀態。
- `current_interactable` 可仍為公寓內部狀態，但變更時要 emit `current_interactable_changed(data)` 給 Main / GameUI。
- 開容器時 emit `interaction_requested({"type":"container", ...})`，不直接呼叫 `dual_pane_container`。
- 顯示訊息時 emit `interaction_requested({"type":"message", ...})` 或呼叫 Main 提供的橋接，不直接操作 `MessageBox` node。
- 大門通過 gate 後 emit `scene_transition_requested("street_stub", "from_apartment", {})`，不直接 change scene。
- `_ready()` 中開場啟動必須依 `_entry_point_id` 分支：
  - `wake_bed`：播放既有 prone → MessageBox 多頁 → get_up → idle 開場流程。
  - `from_street`：不播放開場，`_opening_monologue_active = false`，玩家直接 `idle`，由 `set_entry_point()` 放到門邊 spawn。

注意：

- 插槽 `container_changed("apartment_slot")` 觸發的「電磁聲響 → MessageBox → add_knowledge」仍是公寓專屬流程；只把 MessageBox 顯示委託給 `GameUI`。
- `add_knowledge` 時機保持：MessageBox 關閉後寫入。
- `deposit_locked` / accepted item / `GameState` 行為不改。
- 真轉場後，公寓大門不再播放門後 / 街道 BGM。公寓自己的 BGM 可留在公寓 scene；`street_stub` 要嘛靜音，要嘛自己處理簡單場景 BGM。完整 MusicManager 後續再接 `music_id`。

### 4-F 第二場景 stub

新增檔案（暫定）：

```text
scenes/levels/street_stub.tscn
scenes/levels/street_stub.gd
```

最小內容：

```text
Background placeholder
Player spawn point: from_apartment
Interactable: back_to_apartment -> target_scene_id="apartment", entry_point_id="from_street"
Interactable: 1-2 個 message-only examine
```

`street_stub.gd` 必須使用同一套 Level interaction contract，不得另寫一套 UI 控制。
`street_stub.gd` 也必須實作 `set_entry_point("from_apartment", payload)`，將 player 放到 `from_apartment` SpawnPoint；不得依賴場景原點作為入口。

### 4-G Future-system contracts pass

Phase 4 最後檢查以下插槽存在，但不實作完整系統：

- NPC：message-only 互動資料可帶 `dialogue_id`，Main / GameUI 可顯示 placeholder「對話系統未接入」或對應短訊息。
- Quest：互動資料可帶 `quest_event_id`，Phase 4 可 noop；不得塞進 `GameState.knowledge` 假裝任務系統。
- Save：SceneRouter 持有穩定 `current_scene_id` / `entry_point_id`；GameState 長期資料可被未來 SaveSystem 序列化。
- Music：SceneRegistry 有 `music_id`；Phase 4 可不新增 MusicManager，但不再把未來跨場景音樂設計鎖死在公寓節點。轉場後的街道音樂不得由公寓門事件持續播放。
- Metro：轉場 API 已支援任意 `scene_id + entry_point_id`，可被地鐵網呼叫。
- Art：第二場景用 stub，不生成正式街區美術；場景結構先穩定。

## Phase 5 — NPC + 對話系統（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 5`；操作清單見 `測試指南.md > Phase 5`。NPC「晚」內容定稿見 `subdocs/人/晚.md`。
> 4-G 已取消（見規格書 Phase 5 開頭）；`dialogue_id` 等不做 placeholder，直接於 Phase 5 接真系統。
> 沿用 Phase 4 契約：**Level emit signal → Main mediator → GameUI 顯示**；`UIMode` 管模式凍結主角；`TouchControls` 路由按鍵；敘事文字走 GDScript const。

### 新增 / 異動檔總表

```text
新增：
  scripts/dialogue/dialogue_runner.gd     # 5-A：DialogueRunner（純邏輯）
  data/dialogue/wan.gd                     # 5-A：晚的對話樹 const
  data/dialogue/dialogue_db.gd             # 5-A：dialogue_id → tree 對照
  scenes/ui/dialogue_panel.tscn / .gd      # 5-B：對話 UI
異動：
  scripts/autoload/game_state.gd           # 5-A：故事旗標 store
  scripts/autoload/ui_mode.gd              # 5-B：新增 DIALOGUE
  scripts/components/interactable_area.gd  # 5-B：新增 dialogue_id
  scenes/ui/game_ui.gd (.tscn)             # 5-B：start_dialogue + DialoguePanel 掛載
  scenes/main/main.gd                      # 5-B：mediator 處理 type=="dialogue"
  scripts/autoload/touch_controls.gd       # 5-C：對話路由
  scenes/levels/apartment_entrance.gd(.tscn) # 5-D：NPC 節點 + dispatch 泛化
  tests/manual/test_runner.gd              # 5-A：對話邏輯 headless 測試
```

### 5-A 故事旗標 store + 對話資料 + DialogueRunner

#### GameState 故事旗標 store

```gdscript
# scripts/autoload/game_state.gd
var story_flags: Dictionary = {}            # String -> bool | int

signal flag_changed(key: String, value)

func set_flag(key: String, value) -> void           # 設 bool / int，emit flag_changed
func get_flag(key: String, default_value = 0):       # 不存在回 default
func has_flag(key: String) -> bool                   # 存在且非 false / 非 0
func add_int(key: String, delta: int) -> void        # 不存在視為 0 後 += delta，emit flag_changed
```

規則：

- `story_flags` 與 `knowledge` / `notes` 完全分離；對話 / 任務狀態不得寫進 `knowledge`。
- `affinity_wan` 一律以 `add_int` 累加；好感度與戀愛線共用此值。
- `flag_changed` 供未來 UI / debug / SaveSystem；Phase 5 不接 UI 也可先 emit。
- `story_flags` 內容須為可 JSON 序列化型別（bool / int），供未來 SaveSystem。

#### 對話樹 schema（GDScript const dict）

```gdscript
# data/dialogue/wan.gd
const TREE := {
    "start": {                                       # 入口恆為 "start"；條件路由分流首見 / 重講
        "goto": [
            {"condition": {"flag": "met_wan", "op": "==", "value": true}, "target": "retalk"},
            {"target": "first_meet"},
        ],
    },
    "first_meet": {
        "speaker": "晚",
        "text": "...",
        "choices": [
            {"label": "妳在看什麼？", "goto": "watch"},
            {"label": "妳是誰？", "goto": "who"},
            {"label": "（別開眼）", "goto": "flee"},
        ],
    },
    "who": {
        "speaker": "晚",
        "text": "...",
        "effect": [{"op": "set_flag", "key": "knows_wan_name", "value": true}],
        "goto": "watch",
    },
    "kin": {
        "speaker": "晚",
        "text": "...",
        "effect": [
            {"op": "add_int", "key": "affinity_wan", "value": 1},
            {"op": "set_flag", "key": "met_wan", "value": true},
        ],
        "goto": "end_warm",
    },
    "intel_gate": {
        "goto": [
            {"condition": {"flag": "affinity_wan", "op": ">=", "value": 2}, "target": "intel"},
            {"target": "intel_locked"},
        ],
    },
    "intel": {
        "speaker": "晚",
        "text": "...",
        "effect": [{"op": "add_gleaner_lead", "value": "alley_backrooms_3f"}],
        "goto": "end_warm",
    },
    "end_warm": {"speaker": "晚", "text": "..."},   # 無 choices 且無 goto = terminal
    # ... watch / intel_locked / end_cold / flee / retalk 同理（retalk = 重講短版，對應 subdocs/人/晚.md start_again）
}
```

- 節點四種欄位皆可選：`speaker` / `text` / `effect` / （`choices` 或 `goto` 擇一；皆無 = terminal）。
- `goto` 可為 `String` 或條件清單 `[{condition?, target}]`（依序取第一個 condition 成立者；無 condition 視為恆真 fallback）。
- `choices[].condition` 不成立則該選項隱藏；`choices[].effect` 於選取時套用，再進入 `goto` 目標節點。
- 「有新消息嗎？」選項 `goto: "intel_gate"`，由 gate 條件路由分流 `intel` / `intel_locked`，避免在選項層寫死好感度。

#### DialogueDB

```gdscript
# data/dialogue/dialogue_db.gd
const WanDialogue = preload("res://data/dialogue/wan.gd")
const TREES := {
    "wan": WanDialogue.TREE,
}
static func get_tree_for(dialogue_id: String) -> Dictionary:
    return TREES.get(dialogue_id, {})
```

#### DialogueRunner（純邏輯，不渲染）

```gdscript
# scripts/dialogue/dialogue_runner.gd
class_name DialogueRunner
extends RefCounted

signal finished

func start(tree: Dictionary, start_node := "start") -> void
func current() -> Dictionary
# 回傳 { "speaker": String, "text": String,
#        "choices": Array,        # [{ "label": String, "index": int }]，已過濾 condition 不成立者
#                                 #   index = 該選項在「原始 choices 陣列」的序號（穩定值，非可見清單位置）
#        "is_terminal": bool }
func choose(choice_index: int) -> void   # choice_index 必為 current() 回傳項的 index（原始序號）；
                                         # 套選項 effect → 解析 goto → 進入新節點（並套新節點 effect）
func advance() -> void                    # 無 choices 的節點，玩家按 E 推進到 goto；terminal 時 emit finished
```

內部約定：

- 進入節點時即套該節點 `effect`（呼叫 GameState）；條件路由 `goto` 在「進入前」解析、再進入解析結果節點。
- `current().choices` 已過濾掉 `condition` 不成立的選項；每項仍攜帶其在**原始 `choices` 陣列**的 `index`。`choose()` 必須收原始 `index`，不可用可見清單位置，否則隱藏選項後會選錯分支。
- terminal 節點（無 `choices`、無 `goto`）：`current().is_terminal == true`；玩家再按 `E`（`advance()`）→ `emit finished`。
- condition 評估與 effect 套用集中在 helper：`_eval_condition(cond) -> bool`、`_apply_effect(eff)`；`add_gleaner_lead` 於 Phase 5 僅 `print` / log，不寫 GameState 任何任務狀態。

5-A 不依賴任何 UI；`test_runner.gd` 新增 section 直接構造 `DialogueRunner`、餵 `WanDialogue.TREE`、模擬 `choose()` 序列、斷言 `GameState.story_flags`。

### 5-B 對話 UI + UIMode.DIALOGUE + 接線

#### UIMode

```gdscript
enum Mode { NONE, INVENTORY, CONTAINER, NOTEBOOK, MESSAGE, CONFIRM, DIALOGUE }
```

- `DIALOGUE` 比照 INVENTORY 為**獨立模式**（非 overlay）：對話為全屏互動，進入前其他面板應已關閉；`mode != NONE` 既有規則使主角凍結，沿用即可。
- 進入：`GameUI.start_dialogue()` 內 `UIMode.set_mode(DIALOGUE)`；結束 `set_mode(NONE)`。

#### interactable_area.gd

```gdscript
@export var dialogue_id := ""    # 非空 = 此互動物觸發對話
```

#### Level → Main → GameUI 接線

- Level（5-D 的 `apartment_entrance`）按 `interact_primary` 時，若 `current_interactable.dialogue_id` 非空：

```gdscript
interaction_requested.emit({"type": "dialogue", "dialogue_id": current_interactable.dialogue_id})
```

- `main.gd` mediator 既有 `interaction_requested` handler 增加分支：

```gdscript
match data.get("type"):
    "dialogue": game_ui.start_dialogue(data["dialogue_id"])
    # message / container / ... 既有分支不動
```

#### GameUI

```gdscript
# scenes/ui/game_ui.gd
@onready var dialogue_panel := $DialoguePanel
var _dialogue_runner: DialogueRunner

func start_dialogue(dialogue_id: String) -> void
# 1. tree = DialogueDB.get_tree_for(dialogue_id)；空則略過
# 2. UIMode.set_mode(UIMode.Mode.DIALOGUE)
# 3. _dialogue_runner = DialogueRunner.new(); 連 finished → _on_dialogue_finished
# 4. _dialogue_runner.start(tree)（入口恆為 "start"；重講由 start 節點條件路由，不在此挑入口）; _render_dialogue()
func _render_dialogue() -> void          # 取 current()，更新 Portrait/Name/Text/ChoiceList，設預設焦點
func _on_dialogue_finished() -> void     # 隱藏 panel、UIMode.NONE、Prompt 依互動物恢復
```

輸入（鍵盤）：對話 panel 開啟時，GameUI 攔 `ui_up`/`ui_down` 移選項焦點、`interact_primary`(E) 確認 / 推進：

- 有選項：E → `_dialogue_runner.choose(focused_choice.index)`（傳 current() 該項的原始序號，非可見位置）→ `_render_dialogue()`。
- 無選項（含 terminal）：E → `_dialogue_runner.advance()`（terminal 時觸發 finished）。

#### DialoguePanel.tscn 結構

```text
DialoguePanel (Control)            # 預設 hidden
├── Portrait (TextureRect)         # 立繪槽；texture 為 null 時隱藏 / 顯示 placeholder
├── NameLabel (Label)              # speaker；空字串時隱藏
├── TextLabel (Label/RichTextLabel)# 內文；Phase 5 可即時顯示，打字機沿用 MessageBox 與否由實作定
└── ChoiceList (VBoxContainer)     # 選項；每項一個可 focus 的 Button/Label，焦點高亮
```

立繪 / 角色圖待使用者提供；Phase 5 用 placeholder，`Portrait.texture == null` 時不得報錯。

### 5-C TouchControls 對話路由

- `touch_controls.gd` 在 `UIMode == DIALOGUE` 時：D-pad 上 / 下 → 移選項焦點；右下確認鍵 → 選取 / 推進。
- 透過 GameUI 公開查詢驅動，不讀固定 node path，沿用 4-D 風格：

```gdscript
func dialogue_move_focus(dir: int) -> void   # GameUI 提供，+1/-1
func dialogue_confirm() -> void              # GameUI 提供 = 鍵盤 E 等價
func has_active_dialogue() -> bool           # GameUI 提供，決定觸控顯示哪組鍵
```

- 對話期間：世界方向 / E 等鍵與其他面板觸控鍵隱藏；只顯示對話所需 D-pad + 確認。

### 5-D 晚落地 apartment_entrance + dispatch 泛化

#### NPC 節點（apartment_entrance.tscn）

於 `$Interactables` 下新增：

```text
NpcWan (Area2D, interactable_area.gd)
├── CollisionShape2D
└── Sprite (Sprite2D/AnimatedSprite2D)   # placeholder；角色圖待替換
  interaction_id = "talk_wan"
  prompt_text    = "交談"
  dialogue_id    = "wan"
```

立於暗巷口附近（對應地圖 DEAD SHO 死店 / 暗巷）；座標於實作時對齊背景。

#### dispatch 泛化（apartment_entrance.gd）

目前 `_process` 以 `match current_interactable.interaction_id` 寫死分支。5-D 改為**先看 `dialogue_id`**，泛化對話路徑、不為單一 NPC 加字串 case：

```gdscript
if Input.is_action_just_pressed("interact_primary"):
    if current_interactable.dialogue_id != "":
        interaction_requested.emit({"type": "dialogue", "dialogue_id": current_interactable.dialogue_id})
    else:
        match current_interactable.interaction_id:
            "back_to_apartment": ...
            "mailboxes": ...
            "alley_view": ...
```

跨場景：`story_flags` 在 `GameState`（autoload），回公寓再回街道不重置；「晚」重講由對話樹 `start` 節點的條件路由決定（`met_wan == true` → `retalk` 短版，否則 → `first_meet`）。`start_dialogue` 一律以 `"start"` 為入口，不在 GameUI 寫 NPC 專屬入口邏輯。

## Phase 6 — SaveSystem（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 6`；操作清單見 `測試指南.md > Phase 6`；存檔架構決策見 `技術概念.md > 存檔策略`。
> 沿用既有契約：全域可變狀態集中在 `GameState`；轉場走 SceneRouter `scene_id + entry_point_id`；UI overlay / 凍結走 `UIMode`；觸控走 `TouchControls`。
> Phase 6 只做存檔；任務狀態仍不得寫進 `knowledge`，`add_gleaner_lead` 維持 log / noop。

### 新增 / 異動檔總表

```text
新增：
  scripts/autoload/save_system.gd          # 6-A：SaveSystem Autoload（capture/apply + 檔案 IO）
  scenes/ui/title_screen.tscn / .gd        # 6-D：最小標題畫面（New Game / Load Game）
  scenes/ui/pause_menu.tscn / .gd          # 6-E：暫停選單（Resume/Save/Load/回標題）
  scenes/ui/save_slot_list.tscn / .gd      # 6-E：10 槽列表元件（Save / Load 共用）
異動：
  project.godot                            # 6-A：註冊 SaveSystem autoload；6-D：開機初始場景改標題畫面（見 6-D 決策）
  scripts/autoload/game_state.gd           # 6-A：新增 to_save_dict() / load_save_dict()（含 _last_instance_id）
  scenes/main/main.gd                      # 6-B：restore 轉場路徑 + New Game / Load 分流；6-E：開暫停選單
  scenes/actors/player/player.gd           # 6-B：get_save_x()/set_save_x()（或等價座標存取點）
  scripts/autoload/ui_mode.gd              # 6-E：新增 PAUSE / SAVE_LOAD（或複用 overlay，見決策）
  scenes/ui/touch_controls.gd              # 6-E：暫停鈕 + 選單 / 槽列表路由（autoload 經 touch_controls.tscn）
  tests/manual/test_runner.gd              # 6-A/6-C：存讀 round-trip + 槽列舉 headless 測試
```

### 6-A SaveSystem Autoload + GameState 序列化

#### GameState 序列化 API

```gdscript
# scripts/autoload/game_state.gd
func to_save_dict() -> Dictionary
# 收集全可變狀態，深拷貝回傳：
#   credits, inventory, equipment, knowledge, notes,
#   external_containers, external_container_configs,
#   apartment_initialized, apartment_sonar_revealed,
#   apartment_slot_unlocked, apartment_beyond_door_bgm_triggered,
#   story_flags, _last_instance_id
# 不含 code-owned 常數（STORY_NOTES / STORY_MESSAGES / ITEMS_DB / EQUIPMENT_LIMITS）。

func load_save_dict(data: Dictionary) -> void
# 以 data 覆寫上述欄位（含還原 _last_instance_id）；缺欄位用現值 / 安全預設，不 crash。
# 還原後 emit 既有 signals（inventory_changed / notes_changed / credits_changed / equipment_changed…）
# 讓 UI 重繪。

func reset_for_new_game() -> void
# 將所有可變狀態還原為新局初值：credits = 300、inventory 重建空槽（同 _ready）、
# equipment 三槽清空、knowledge / notes / external_containers / external_container_configs 清空、
# story_flags 清空、apartment_* 四旗標 = false、_last_instance_id = 0；emit 既有 signals 讓 UI 重繪。
# New Game 進入前由 Main 呼叫，避免「Load → 回標題 → New Game」殘留舊 credits / inventory / story_flags。
```

- `inventory` / `notes` 等 `Array[Dictionary]` 還原時須保持型別；以 `var_to_str` 序列化天然保型，`load_save_dict` 內重建為 typed array。
- `_last_instance_id` 必還原，否則 `generate_instance_id()` 由 0 重數會與既有 `instance_id` 撞號。

#### SaveSystem

```gdscript
# scripts/autoload/save_system.gd  （Autoload，註冊於 project.godot）
extends Node

const SLOT_COUNT := 10
const SAVE_VERSION := 1

func capture(scene_id: String, player_x: float, facing := 1) -> Dictionary
# 回傳 { "meta": {...}, "data": {...} }
#   meta = { "version": SAVE_VERSION, "timestamp": <unix>, "scene_name": <顯示名>, "credits": <int> }
#   data = GameState.to_save_dict() + { "current_scene_id": scene_id,
#                                       "player_x": player_x, "player_facing": facing }

func write_slot(slot: int, payload: Dictionary) -> bool
# var_to_str(payload) → FileAccess.WRITE 到 _slot_path(slot)；成功回 true。

func read_slot(slot: int) -> Dictionary
# 讀檔 → str_to_var；解析失敗 / 版本不符回 {}（呼叫端視為損毀，不套用）。

func validate(payload: Dictionary) -> bool
# 載入前檢查，不改任何狀態：payload 非空、有 meta / data、meta.version == SAVE_VERSION、
# data 含必要欄位（current_scene_id 為非空 String、player_x 為數值…）。任一不成立回 false。
# 【只驗 payload shape / version】，不查 scene_id 是否存在 —— SCENES 由 Main 持有（main.gd const），
#   SaveSystem 為 autoload 不下放查詢；scene_id 存在性由 Main / SceneRouter 在 apply 前另驗（見 6-B）。
# 【務必在 apply 之前呼叫】。

func apply(payload: Dictionary) -> void
# 前置條件：呼叫端已 validate(payload) == true。
# GameState.load_save_dict(payload.data)；scene / 座標交回 Main 做 restore 轉場（見 6-B）。

var can_save_here := true
# 預留 gate：未來「危險區不能存」時由場景 / 流程切此旗標。Phase 6 恆 true，
# 由「開 Save 入口」處檢查（見 6-E），非 capture() 參數。

func _slot_path(slot: int) -> String   # "user://save_%02d.sav" % slot   （slot 1..10）
```

- 序列化一律 `var_to_str` / `str_to_var`（非 JSON、非 ResourceSaver）。
- `scene_name` 顯示名由 SceneRegistry / 一張 `scene_id → 顯示名` 對照提供（6-C 落地）。

### 6-B 讀檔回場路徑（精確座標）

- SceneRouter `transition_to` 擴充可選 restore：

```gdscript
func transition_to(scene_id: String, entry_point_id := "",
        restore := {}) -> void
# restore 非空（{ "player_x": float, "player_facing": int }）時：
#   add_child 場景後，直接 set 玩家座標，且【不呼叫 entry point 副作用】
#   （不播開場獨白、不跑 wake_bed prone→monologue→get_up）。
# restore 為空 → 沿用既有 entry_point 行為。
```

- player 座標存取點（player.gd 目前各 level 內嵌共用）：

```gdscript
func get_save_x() -> float
func set_save_x(x: float) -> void
func get_facing() -> int          # +1 / -1，無則回 1
func set_facing(f: int) -> void
```

- 開機 / 流程分流（main.gd）：
  - **New Game** → `GameState.reset_for_new_game()` → `transition_to("apartment", "wake_bed")`（播開場）。reset 必先行，否則 Load → 回標題 → New Game 會殘留舊狀態。
  - **Load**（驗證皆在 apply 前，分兩段）：
    1. `payload = SaveSystem.read_slot(slot)`。
    2. `if not SaveSystem.validate(payload):` 跳訊息、中止（shape / version 不過）。
    3. `if not SCENES.has(payload.data.current_scene_id):` 跳訊息、中止（scene 不存在；此檢查在 Main，因 `SCENES` 為 main.gd const，不下放給 SaveSystem）。
    4. 兩段皆過才 `SaveSystem.apply(payload)` → `transition_to(payload.data.current_scene_id, "", {player_x=…, player_facing=…})`（不播開場）。
  - 任一步中止時，`apply` 未被呼叫，GameState 維持載入前狀態、不被半套用。

### 6-C 多槽模型 + meta 列舉

- 檔名：`user://save_01.sav … save_10.sav`（slot 編號 1..10）。
- 列舉供 UI（只讀 meta、不套用 data）：

```gdscript
func list_slots() -> Array
# 回傳長度 SLOT_COUNT 的陣列；每項：
#   空槽   → { "slot": int, "empty": true }
#   有效   → { "slot": int, "empty": false, "meta": {version,timestamp,scene_name,credits} }
#   損毀   → { "slot": int, "empty": false, "corrupt": true }
```

- 損毀 / 版本不符槽在列表標示「（損毀）」，可被覆蓋、不可載入。

### 6-D 最小標題畫面

- 內容只有 New Game / Load Game 兩個入口（不含設定 / 多語系 / 縮圖）。
- **決策（開工時定）**：標題畫面作為「開機初始場景」（`project.godot` main scene 改 `title_screen.tscn`，由它再進 main.tscn / WorldRoot），或作為 main.tscn 內的一個 UI 狀態先於 WorldRoot 載入。建議前者較乾淨（遊戲世界與標題互不污染）。
- New Game → `GameState.reset_for_new_game()` → 載入 main、`transition_to("apartment", "wake_bed")`。
- Load Game → 開 SaveSlotList（read-only 模式）→ 選有效槽 → `validate` → `apply` + restore 轉場。
- **TouchControls 隔離**：`TouchControls` 為 autoload，且 PC 端 `UIMode==NONE` 時會顯示左上 toggle、`NONE` 時顯示 D-pad（[touch_controls.gd](scenes/ui/touch_controls.gd) `_update_dynamic_button_visibility`）。標題畫面非 gameplay Level，必須隱藏整個 TouchControls overlay。實作擇一：Main 進 / 出標題時 `TouchControls.set_visible_overlay(false/true)`；或 `_update_dynamic_button_visibility` 判斷 `current_scene` 非 Level（無 active GameUI / 非世界場景）時整體隱藏。標題頁不得出現 D-pad / 功能鍵 / 觸控 toggle。

### 6-E 暫停選單 + 槽列表 UI + gating

- **開選單條件**：僅 `UIMode == NONE`（自由探索）可開；面板 / 對話 / 開場獨白期間不可開。
- **UIMode 決策（開工時定）**：新增 `PAUSE` / `SAVE_LOAD` 兩模式，或複用既有 overlay caller-還原機制（MESSAGE / CONFIRM 那套）。暫停選單為全屏 modal，建議新增 `PAUSE`；槽列表可作其 overlay。
- 暫停選單項：Resume（關選單回 NONE）/ Save（開 SaveSlotList 寫入模式）/ Load（開 SaveSlotList 讀取模式）/ 回標題。
- **Save 入口 gate**：開「Save」前先查 `SaveSystem.can_save_here`；為 false 時 Save 項禁用 / 提示（Phase 6 恆 true，此為預留 contract，不可省略檢查點）。
- `SaveSlotList`（Save / Load 共用，差在動作）：
  - Save 模式選空槽 → 直接 `capture` + `write_slot`；選已佔用槽 → 既有 `ConfirmDialog` 覆蓋確認後寫入。
  - Load 模式選有效槽 → `validate` 通過才 `apply` + restore 轉場；損毀 / 空槽 / validate 失敗不可選 / 擋下。
- 回標題：若當前進度未存（與最近一次存檔不一致，或可簡化為「一律確認」）→ `ConfirmDialog` 二次確認。
- TouchControls：暫停鈕（建議右上）；選單 / 槽列表沿用 4-D 風格經 GameUI / 選單公開查詢路由，不讀固定 node path。

### 6-F 邊界與失敗處理

- `read_slot` 版本不符 / parse 失敗 → 回 `{}`；UI 顯示「（損毀）」並擋下載入，不 crash。
- 載入驗證分兩段且皆在 `apply` **之前**：`SaveSystem.validate` 驗 shape / version；Main 驗 `current_scene_id ∈ SCENES`。任一不過 → 跳訊息（沿用 MessageBox）、**完全不 apply**，GameState 維持載入前狀態（不留半套用 / 不黑屏）。
- restore 的 `player_x` 超出場景範圍 → `set_save_x` 端 clamp 回場景邊界（或 Main restore 前 clamp）。
- 「回標題」未存確認走既有 `ConfirmDialog`，不另造 modal。

## Phase 7 — QuestManager + `alley_backrooms_3f` vertical slice（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 7`；操作清單見 `測試指南.md > Phase 7`。
> 沿用既有契約：全域長期狀態集中在 `GameState`；轉場走 SceneRouter `scene_id + entry_point_id`；UI 凍結走 `UIMode`；任務顯示暫時投影到既有筆記 UI 的「工作」分類。
> Phase 7 不做大型 Quest UI；不把任務狀態寫入 `knowledge`；`apartment_fire_escape` 外牆場景不可存檔。

### 新增 / 異動檔總表

```text
新增：
  scripts/autoload/quest_manager.gd             # 7-A：QuestManager Autoload
  data/quests/quest_db.gd                       # 7-A：quest_id -> quest data 對照
  data/quests/alley_backrooms_3f.gd             # 7-A/7-B：第一條任務資料與工作筆記模板
  scenes/levels/apartment_fire_escape/apartment_fire_escape.tscn/.gd # 7-E~7-G：兩棟樓 + gap + 天橋 + 梯子 + 箱子
異動：
  project.godot                                 # 7-A：註冊 QuestManager Autoload
  scripts/autoload/game_state.gd                # 7-A：quest_states + save/load/reset
  scripts/autoload/save_system.gd               # 7-F：沿用 can_save_here gate，外牆場景切 false
  scripts/dialogue/dialogue_runner.gd           # 7-B：新增 start_quest effect（或轉呼 QuestManager）
  data/dialogue/wan.gd                          # 7-B：調整晚的對話分支與任務啟動節點
  scenes/main/main.gd                           # 7-D/7-E：SceneRegistry 新增 apartment_fire_escape
  scenes/levels/apartment_entrance.gd(.tscn)    # 7-C：後巷 quest event
  scenes/levels/apartment/apartment_room.gd(.tscn) # 7-D：窗戶條件互動 + from_fire_escape entry point
  scenes/actors/player/player.gd                # 7-F：場景限定 climb mode API
  scripts/autoload/game_state.gd ITEMS_DB       # 7-G/7-H：A/B 物品 metadata
  scenes/ui/game_ui.gd                          # 7-H：R 查看 A 產生 B（非消耗，A 保留）
  tests/manual/test_runner.gd                   # 7-A/7-B/7-H：headless 邏輯測試
```

### 7-A QuestManager + GameState quest state

#### GameState

```gdscript
# scripts/autoload/game_state.gd
var quest_states: Dictionary = {}

signal quest_changed(quest_id: String, state: Dictionary)

func get_quest_state(quest_id: String) -> Dictionary
func set_quest_state(quest_id: String, state: Dictionary) -> void
func has_active_quest(quest_id: String) -> bool
func get_quest_step(quest_id: String) -> String
```

- `quest_states` 必納入 `to_save_dict()` / `load_save_dict()` / `reset_for_new_game()`。
- `quest_states` 不得寫入 `knowledge`；`knowledge` 仍只做身份 gate。
- `load_save_dict()` 還原任務後 emit `quest_changed` 或提供 refresh hook，讓工作筆記 / UI 可重繪。

#### QuestManager

```gdscript
# scripts/autoload/quest_manager.gd
extends Node

signal quest_started(quest_id: String)
signal quest_advanced(quest_id: String, step: String)
signal quest_completed(quest_id: String)
signal quest_failed(quest_id: String)

func start(quest_id: String) -> bool
func advance(quest_id: String, step: String, flags := {}) -> bool
func complete(quest_id: String) -> bool
func fail(quest_id: String) -> bool
func get_status(quest_id: String) -> String
func get_step(quest_id: String) -> String
func get_flag(quest_id: String, key: String, default_value = false)
func set_flag(quest_id: String, key: String, value) -> bool
func sync_work_note(quest_id: String) -> void
```

- `start()` 對已 active / completed 的任務為 idempotent，不重置進度。
- `advance()` 只允許合法 step 轉移；非法轉移回 false，不改狀態。
- `complete()` 可在必要條件成立時呼叫；`alley_backrooms_3f` 不發 credits / item reward，不呼叫 reward 發放。
- 每次 `start` / `advance` / `complete` / `fail` state 變更後必呼 `sync_work_note()`。
- `sync_work_note()` 用 `GameState.add_knowledge(note_payload)` 更新「工作」筆記；同 id 覆寫既有 note body/status。此處 `add_knowledge()` 對 `category == "工作"` 只更新 notes 顯示投影，不寫入 `knowledge` gate；若之後新增 `add_note()` 別名，可由 `sync_work_note()` 改用該別名。

#### Quest data

```gdscript
# data/quests/alley_backrooms_3f.gd
const QUEST_ID := "alley_backrooms_3f"
const WORK_NOTE_ID := "quest_alley_backrooms_3f"

const STEPS := {
    "started": {},
    "checked_alley": {},
    "found_activation_box": {},
}

const WORK_NOTES_BY_STEP := {
    "started": {
        "id": WORK_NOTE_ID,
        "category": "工作",
        "title": "暗巷三樓的舊物",
        "body": "...",
        "status": "active",
    },
    "checked_alley": { ... },
    "found_activation_box": { ... },
}

const WORK_NOTES_BY_STATUS := {
    "completed": { ... },
    "failed": { ... },
}
```

- 工作筆記文字可在開工時微調，但必須覆蓋四個節點：接任務、後巷偵查後、取得 A 後、任務完成。
- `checked_alley` 後的筆記必須寫出：後巷不能硬走；主角想起自家在四樓；窗外連通平台可能可繞到三樓。
- `sync_work_note()` 查表順序固定：若 `status in ["completed", "failed"]`，使用 `WORK_NOTES_BY_STATUS[status]`；否則使用 `WORK_NOTES_BY_STEP[step]`。`completed` / `failed` 不得作為 step，未被設定的死 step 不得保留。

### 7-B 晚的對話接任務

#### Dialogue effect

`DialogueRunner._apply_effect()` 新增 effect：

```gdscript
{"op": "start_quest", "value": "alley_backrooms_3f"}
```

行為：

```gdscript
"start_quest":
    QuestManager.start(eff.get("value", ""))
```

- Phase 5 的 `add_gleaner_lead` 可保留 log / 相容，但 Phase 7 任務啟動應使用 `start_quest`，不要繼續以 `add_gleaner_lead` 假裝任務。
- 對話資料中，任務啟動直接接在既有 `intel` 節點：沿用 Phase 5 已定的 `affinity_wan >= 2` / `intel_gate` 門檻，不新增第二套信任旗標。
- 重新對話時，若任務已 active / completed，不重複 start；可顯示已給過 lead 的短句。

#### Dialogue condition extension

Phase 5 的 `condition` 原本只讀 `story_flags`。Phase 7 需讓晚的回報分支能判斷 quest state 與背包，因此 `DialogueRunner._eval_condition()` 擴充 condition type：

```gdscript
# 舊格式維持相容；未帶 type 時視為 story_flag
{"flag": "affinity_wan", "op": ">=", "value": 2}

{"type": "quest_status", "quest_id": "alley_backrooms_3f", "op": "==", "value": "active"}
{"type": "quest_step", "quest_id": "alley_backrooms_3f", "op": "==", "value": "found_activation_box"}
{"type": "has_item", "item_id": "early_ai_assistant_activation_box", "op": "==", "value": true}
```

- `condition` 可為單一 `Dictionary` 或 `Array[Dictionary]`；Array 代表 AND，全部成立才顯示選項 / 命中 goto。
- `quest_status` 讀 `QuestManager.get_status(quest_id)`；`quest_step` 讀 `QuestManager.get_step(quest_id)`。
- `has_item` 讀現有背包查詢；若尚無 helper，新增 `GameState.has_item(item_id: String, count: int = 1) -> bool`，不得用 UI 文字或 note 反推。
- 舊 `{flag, op, value}` 條件行為不變，繼續用於 `affinity_wan` / `met_wan` 等故事旗標。

### 7-C `apartment_entrance` 後巷偵查事件

現有 `alley_view` 互動分支改為依 quest state 分流：

```gdscript
match current_interactable.interaction_id:
    "alley_view":
        if QuestManager.get_status("alley_backrooms_3f") == "active" \
                and QuestManager.get_step("alley_backrooms_3f") == "started":
            QuestManager.advance("alley_backrooms_3f", "checked_alley")
            interaction_requested.emit({
                "type": "message",
                "message_text": MESSAGES["alley_view_danger"],
                "note_title": "已更新筆記：暗巷三樓的舊物"
            })
        else:
            interaction_requested.emit({
                "type": "message",
                "message_text": MESSAGES["alley_view"]
            })
```

- 未接任務時，後巷文字與 Phase 5/6 現狀一致。
- 接任務後第一次互動顯示危險暗巷描述，推進任務與工作筆記；不轉場、不進後巷。
- 已推進後再次互動可顯示同一危險文字或更短版本，但不得重複改狀態。

### 7-D 公寓窗戶條件式入口

公寓場景新增窗戶互動物（位置依場景美術配置，代表右側窗戶 / 可爬出處）：

```text
ApartmentWindow (Area2D, interactable_area.gd)
  interaction_id = "fire_escape_window"
  prompt_text = "爬出窗外"
```

顯示與互動條件：

```gdscript
func _is_fire_escape_window_available() -> bool:
    return QuestManager.get_status("alley_backrooms_3f") == "active" \
        and QuestManager.get_step("alley_backrooms_3f") == "checked_alley"
```

- 未達條件時，窗戶不顯示 prompt / 不可互動；或顯示普通窗戶描述但不可轉場，開工時擇一，驗收以「不可進外牆」為準。
- 達條件後按 E：

```gdscript
scene_transition_requested.emit("apartment_fire_escape", "from_window", {})
```

公寓新增 entry point：

```text
apartment:from_fire_escape
```

- 從外牆回公寓時，玩家落在窗邊，不落在 `from_street` 門邊。
- `from_fire_escape` 不播開場獨白。

### 7-E `apartment_fire_escape` 場景骨架

SceneRegistry：

```gdscript
"apartment_fire_escape": {
    "path": "res://scenes/levels/apartment_fire_escape/apartment_fire_escape.tscn",
    "default_entry_point_id": "from_window",
    "entry_points": ["from_window"],
}
```

場景需求：

```text
ApartmentFireEscape (Node2D)
├── Player
├── SpawnPoints
│   └── from_window
├── WalkSegments
│   ├── RightBuilding4FPlatform
│   ├── RightBuilding3FPlatform
│   ├── Bridge3F
│   └── LeftBuilding3FPlatform
├── Interactables
│   ├── WindowReturn
│   └── QuestBox
├── Ladders
│   └── MainLadder
├── Camera2D
└── AudioStreamPlayer / BGM（可沿用或暫無）
```

- 唯一 entry point 是 `from_window`；梯子上下不作 entry point。
- 場景是兩棟樓結構：右棟 4F 窗戶 / 右棟 4F 平台 / 右棟梯子 / 右棟 3F 平台 / 中央空隙 gap / 3F 天橋 / 左棟 3F 平台 / 左側箱子。
- 3F 平台在 gap 處不得直接連通；必須以 `Bridge3F` 的 collision / walk segment 連接右棟與左棟。
- 相機必須支援垂直多層動線。建議在 `apartment_fire_escape` 場景內放 `Camera2D`，跟隨玩家 `global_position` 的 x/y，並設定 level bounds clamp；若採固定構圖，必須能同時看見 4F 平台、梯子、3F 天橋與箱子互動區。
- `WindowReturn` 在右側四樓窗戶位置，按 E：

```gdscript
scene_transition_requested.emit("apartment", "from_fire_escape", {})
```

- `QuestBox` 位於三樓左側箱子，只有任務 active 且已 `checked_alley` 時可取得 A。
- 場景 `_ready()` / `prepare_entry_point()` 設：

```gdscript
SaveSystem.can_save_here = false
```

- `apartment_fire_escape.gd._exit_tree()` 必須恢復 `SaveSystem.can_save_here = true`；不再分散到 Main 做場景白名單判斷，避免離開後暫停選單仍禁存。

### 7-F 梯子攀爬模式 + 天橋步行

Phase 7 梯子只需支援 `apartment_fire_escape` 場景，不把全遊戲改成 platformer。

Player 建議 API：

```gdscript
# scenes/actors/player/player.gd
var climb_mode := false
var climb_speed := 180.0
var climb_min_y := 0.0
var climb_max_y := 0.0
var climb_x := 0.0

func enter_climb_mode(ladder_x: float, min_y: float, max_y: float) -> void
func exit_climb_mode(target_y: float) -> void
func is_climbing() -> bool
```

行為：

- climb mode 時停止一般 `walk_line_y` 鎖定，`position.x` snap 到梯子中心。
- `move_up` / `move_down` 控制 `position.y`，clamp 在梯子上下端。
- 到達上端 / 下端後按 E 或方向離開梯子，snap 到對應平台 walk line。
- climb mode 期間左右移動可先禁用；不做跳躍、不做斜向移動、不做跌落。
- 動畫可先用 placeholder climb animation；若尚無美術，可用 idle/walk 暫代但狀態需可驗證。

場景 ladder zone：

```text
MainLadder (Area2D)
  ladder_x
  upper_y
  lower_y
  upper_walk_line_y
  lower_walk_line_y
```

- 玩家靠近梯子顯示 prompt；按 E 進入 climb mode。
- `apartment_fire_escape.gd` 根據玩家在梯子端點決定 `exit_climb_mode(upper_walk_line_y / lower_walk_line_y)`。
- 離開梯子到右棟 3F 後，玩家沿 `Bridge3F` 走過 gap 到左棟 3F；天橋本身只是 walk segment / collision，不另做攀爬或跳躍。
- 輸入優先序：`player.is_climbing() == true` 時，`interact_primary` 只處理「離開梯子 / snap 到平台」，並 `set_input_as_handled()`；同一個 E 不得同時觸發 `WindowReturn`、`QuestBox` 或其它一般互動物。玩家離開梯子後，需要下一次 E 才能觸發平台上的互動物。

### 7-G 箱子搜索 + A 物品取得

箱子互動條件：

```gdscript
QuestManager.get_status("alley_backrooms_3f") == "active"
and QuestManager.get_step("alley_backrooms_3f") == "checked_alley"
and not QuestManager.get_flag("alley_backrooms_3f", "found_activation_box", false)
```

互動行為：

```gdscript
interaction_requested.emit({
    "type": "message",
    "message_text": MESSAGES["quest_box_search_quiet"],
    "on_closed": func():
        var added := GameState.add_item("early_ai_assistant_activation_box", 1)
        if not added:
            interaction_requested.emit({
                "type": "message",
                "message_text": MESSAGES["inventory_full_for_activation_box"]
            })
            return

        QuestManager.advance("alley_backrooms_3f", "found_activation_box", {"found_activation_box": true})
})
```

- 搜索文字必須提到旁邊窗戶亮著燈 / 有人住，主角盡量不發出聲音。
- 成功取得 A 後跳異常提示：A 保存良好，但底部有不該有的卡扣聲 / 第二層重量。
- 若背包滿，先提示背包放不下，且狀態完全不變：不呼 `QuestManager.advance()`、不設 quest flag、不同步工作筆記。箱子仍維持可互動，公寓窗戶仍可再進外牆；玩家整理背包後可重試。
- 7-H 取得 B 使用同一防呆原則：`GameState.add_item(B, 1)` 回 true 後才設 `inspect_grants_flag`；false 時只提示並保持 A 可再次 R 查看。

### 7-H A/B 物品 metadata 與 R 查看

ITEMS_DB 新增：

```gdscript
"early_ai_assistant_activation_box": {
    "id": "early_ai_assistant_activation_box",
    "name": "早期 AI 助理啟用盒",
    "description": "一個保存良好的早期 AI 助理啟用盒。那時候，人們還會把 AI 當成新家電一樣帶回家，拆封、註冊、期待它讓生活變好。",
    "category": "misc",
    "stackable": false,
    "discardable": false,
    "usable": false,
    "icon_path": "TODO: Phase 7 asset",
    "inspect_grants_item": "old_ai_authorization_module",
    "inspect_grants_flag": "found_old_ai_authorization_module"
},
"old_ai_authorization_module": {
    "id": "old_ai_authorization_module",
    "name": "舊式 AI 授權模組",
    "description": "薄片狀的舊式 AI 授權模組。標籤已經褪色，但接點仍然完整。它看起來不像收藏品，比較像某種能讓舊系統暫時認人的鑰匙。",
    "category": "key_item",
    "stackable": false,
    "discardable": false,
    "usable": false,
    "icon_path": "TODO: Phase 7 asset"
}
```

GameUI R 查看處新增通用分支：

```gdscript
if item_meta.has("inspect_grants_item"):
    _handle_inspect_grants_item(instance_id, item_meta)
else:
    item_detail_modal.show_modal(...)
```

處理規則：

- 若 `inspect_grants_flag` 已存在 / 已有 B，僅正常顯示 A 詳細資訊，不重複給 B。
- 第一次 R 查看 A：跳 MessageBox 描述打開夾層 → 嘗試 `GameState.add_item(B, 1)`；成功後才 `QuestManager.set_flag("alley_backrooms_3f", inspect_grants_flag, true)`，失敗則提示背包放不下且不設 flag。
- A 不消耗、不改 item_id；MessageBox 關閉後可回到 A 的 detail modal。
- 若背包滿，先提示無法取出 B，不設 flag；玩家整理背包後可再次 R 查看取得。

### 7-I 回報晚 + 任務完成

晚的 retalk / report branch 增加交任務條件：

```gdscript
{
    "label": "我找到那個啟用盒了。",
    "condition": [
        {"type": "quest_status", "quest_id": "alley_backrooms_3f", "op": "==", "value": "active"},
        {"type": "quest_step", "quest_id": "alley_backrooms_3f", "op": "==", "value": "found_activation_box"},
        {"type": "has_item", "item_id": "early_ai_assistant_activation_box", "op": "==", "value": true},
    ],
    "goto": "alley_backrooms_turn_in",
}
```

完成行為：

```gdscript
var removed := GameState.remove_item("early_ai_assistant_activation_box", 1)
if not removed:
    # 防止對話條件與背包狀態不同步時誤完成；可導回缺物短句。
    return
QuestManager.complete("alley_backrooms_3f")
```

- 回報只需要 A；B 不檢查、不交出。
- 若玩家未 R 查看 A，交出 A 後 B 錯過；不在 Phase 7 補發。
- 若玩家已取得 B，B 保留在背包。
- 若 A 不在背包，交任務分支不得顯示；即使資料異常進到 turn-in 節點，`remove_item()` 回 false 時也不得 complete。
- 7-A~7-I 階段 `alley_backrooms_3f` 不發 credits / item reward；完成後工作筆記更新為 completed；晚的對話改為完成後短句，不重複收 A。**（7-K 起調整：交還改為發 credits 並依分支發好感，見下方 7-K。）**

### 7-K 任務結局分支（隱藏物交還 + 好感獎勵 + 親近對話）

> 7-K 在 7-A~7-J 完成後追加。驗收意圖見 `遊戲規格書.md > Phase 7 > 7-K`；操作清單見 `測試指南.md > 7-K`。
> 設計前提：**晚不知道盒子有夾層 / 模組**；是否交出 B 是玩家單方面選擇。
> 既有結局訊息（commit 621c111）目前只有結局 1 / 結局 2 且無玩家選擇；7-K 補上二選一、新增結局 3、首次發放報酬與好感。

#### 7-K 異動檔

```text
異動：
  data/dialogue/wan.gd                  # 回報節點改路由 + 三結局葉節點；新增 retalk_close 與 start 路由
  scripts/dialogue/dialogue_runner.gd   # 新增 add_credits effect op
  data/quests/alley_backrooms_3f.gd     # 三種完成工作筆記 + resolve_completed_note()
  scripts/autoload/quest_manager.gd     # sync_work_note() completed 改用 resolve_completed_note()（若有）
  scenes/ui/game_ui.gd                  # _check_and_trigger_endings() 由兩向改三向（gave → found → 皆無）
  tests/manual/test_runner.gd           # 三結局 / 報酬 / 筆記 headless 測試
```

#### 旗標與報酬數值

| 項目 | 存放 | 設定處 | 語意 |
|---|---|---|---|
| `found_old_ai_authorization_module` | quest flag（`quest_states[...].flags`） | 7-H：`QuestManager.set_flag("alley_backrooms_3f", ...)` | 是否已從 A 拆出 B |
| `gave_wan_old_module` | story flag（`GameState.story_flags`） | 7-K：對話 effect `set_flag` | 是否把 B 交給晚（結局 C；解親近對話 / 結局訊息分流） |
| `affinity_wan` | story flag（int） | 7-K：對話 effect `add_int` +2 | 好感；結局 C 額外 +2 |
| `alley_backrooms_ended` | story flag | 既有：`_check_and_trigger_endings()` | 結局訊息一次性守衛 |

報酬：A = +500、B = +500、C = +1000（`GameState.add_credits`）。

#### Dialogue effect：新增 `add_credits`

`DialogueRunner._apply_effect()` 新增 case：

```gdscript
"add_credits":
    var amount = eff.get("value", 0)
    GameState.add_credits(amount)
    return true
```

- effect 失敗（回 false）時 runner 會 `break` 中止後續 effect（既有行為）；因此葉節點 effect 順序必須把 `remove_item` 排在 `add_credits` / `complete_quest` 之前，移除失敗就不會誤發錢、誤完成。

#### 對話結構（`data/dialogue/wan.gd`）

`retalk` 既有選項「我找到那個啟用盒了。」仍 `goto "alley_backrooms_turn_in"`（入口名不變，向後相容）。`alley_backrooms_turn_in` 由原本的「收 A + 完成」葉節點改為**路由節點**：

```gdscript
"alley_backrooms_turn_in": {   # 路由：無 speaker/text，只有 goto
    "goto": [
        {"condition": {"type": "has_item", "item_id": "old_ai_authorization_module", "op": "==", "value": true}, "target": "turn_in_found_module"},
        {"target": "turn_in_plain"}
    ]
},
"turn_in_found_module": {       # 晚收下盒子、道謝（不提夾層）→ 玩家二選一；本節點不做 effect
    "speaker": "晚",
    "text": "（收盒子、道謝的台詞草案見下）",
    "choices": [
        {"label": "（只把啟用盒交給她，那塊模組留在自己口袋裡）", "goto": "turn_in_plain"},
        {"label": "（連盒子夾層裡那塊舊模組，也一起遞過去）", "goto": "turn_in_full"}
    ]
},
"turn_in_plain": {             # 結局 A / B 共用
    "speaker": "晚",
    "text": "...",
    "effect": [
        {"op": "remove_item", "item_id": "early_ai_assistant_activation_box", "value": 1},
        {"op": "add_credits", "value": 500},
        {"op": "complete_quest", "value": "alley_backrooms_3f"}
    ],
    "goto": "end_warm"
},
"turn_in_full": {             # 結局 C
    "speaker": "晚",
    "text": "...",
    "effect": [
        {"op": "remove_item", "item_id": "early_ai_assistant_activation_box", "value": 1},
        {"op": "remove_item", "item_id": "old_ai_authorization_module", "value": 1},
        {"op": "add_credits", "value": 1000},
        {"op": "add_int", "key": "affinity_wan", "value": 2},
        {"op": "set_flag", "key": "gave_wan_old_module", "value": true},
        {"op": "complete_quest", "value": "alley_backrooms_3f"}
    ],
    "goto": "end_warm"
}
```

`start` 路由節點新增最高優先分支（交出 B 後走親近版開場），其餘不變：

```gdscript
"start": {"goto": [
    {"condition": {"flag": "gave_wan_old_module", "op": "==", "value": true}, "target": "retalk_close"},
    {"condition": {"flag": "met_wan", "op": "==", "value": true}, "target": "retalk"},
    {"target": "first_meet"}
]}
```

- `retalk_close`：一眼可辨的親近開場（不沿用原 retalk 開場），choices 維持「（站下，effect `affinity_wan += 1`）/ 有新消息嗎（→ `intel_gate`）/（離開）」量級；可加 `end_close` 暖收尾與 `flee_close` 暖道別。氣氛＝熟朋友 + 小默契，不回顧啟用盒。
- 只有結局 C（`gave_wan_old_module`）走 `retalk_close`；A / B 完成後 `gave_wan_old_module` 仍為假，落回既有 `retalk`，行為不變。
- `turn_in_found_module` 不能做 remove/complete（否則 `turn_in_plain` 的 `remove_item` 會失敗）；移除一律在葉節點。

#### 三種完成工作筆記（`data/quests/alley_backrooms_3f.gd`）

把單一 completed 筆記改為三選一，由旗標決定：

```gdscript
const WORK_NOTES_COMPLETED := {
    "plain":       { "id": WORK_NOTE_ID, "category": "工作", "title": "暗巷三樓的舊物", "body": "<A 文字>", "status": "completed" },
    "kept_module": { "id": WORK_NOTE_ID, "category": "工作", "title": "暗巷三樓的舊物", "body": "<B 文字>", "status": "completed" },
    "gave_module": { "id": WORK_NOTE_ID, "category": "工作", "title": "暗巷三樓的舊物", "body": "<C 文字>", "status": "completed" },
}

static func resolve_completed_note() -> Dictionary:
    if GameState.get_flag("gave_wan_old_module", false):
        return WORK_NOTES_COMPLETED["gave_module"]
    elif QuestManager.get_flag(QUEST_ID, "found_old_ai_authorization_module", false):
        return WORK_NOTES_COMPLETED["kept_module"]
    else:
        return WORK_NOTES_COMPLETED["plain"]
```

`QuestManager.sync_work_note()` completed 分支改為：

```gdscript
if status == "completed" and quest_data.has_method("resolve_completed_note"):
    note_payload = quest_data.resolve_completed_note()
elif (status == "completed" or status == "failed") and quest_data.WORK_NOTES_BY_STATUS.has(status):
    note_payload = quest_data.WORK_NOTES_BY_STATUS[status]
else:
    # 既有 step 查表
```

- 沒有 `resolve_completed_note` 的任務沿用 `WORK_NOTES_BY_STATUS`，行為不變（保持泛用）。
- 實作時須確認 GDScript 能以 `has_method()` 偵測靜態函式；若不行，改用常數守衛（如 `const HAS_COMPLETED_NOTE_RESOLVER := true` + `quest_data.get("HAS_COMPLETED_NOTE_RESOLVER")`）。
- `resolve_completed_note()` 於靜態函式中存取 autoload（`GameState` / `QuestManager`）為合法（autoload 為全域單例）。

#### 結局訊息（`game_ui.gd._check_and_trigger_endings()`）

由兩向改三向，新增結局 3；守衛 `alley_backrooms_ended` 不變：

```gdscript
if QuestManager.get_status("alley_backrooms_3f") == "completed" and not GameState.get_flag("alley_backrooms_ended", false):
    GameState.set_flag("alley_backrooms_ended", true)
    if GameState.get_flag("gave_wan_old_module", false):
        # 結局 3：托付（兩頁）
    elif QuestManager.get_flag("alley_backrooms_3f", "found_old_ai_authorization_module", false):
        # 結局 2：拾遺者的直覺（既有，B 留著）
    else:
        # 結局 1：盲目的清理者（既有）
```

- 既有結局 1 / 2 的提示文字（互相指引）需更新，把「交出模組 → 結局 3」一併寫入，避免提示與實際分支不符。

## Phase 8 — 便利商店 vertical slice + 買賣系統（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 8`；操作清單見 `測試指南.md > Phase 8`；場景敘事 / layout / 互動物見 `subdocs/地點/便利商店.md`。
> 沿用既有契約：全域長期狀態集中在 `GameState`；轉場走 SceneRouter `scene_id + entry_point_id`；UI 凍結走 `UIMode`；對話走 `DialogueRunner` + 條件路由；任務走 `QuestManager` + 工作筆記投影；Level 不直接碰 UI node path（走 `current_interactable_changed` / `interaction_requested` / `scene_transition_requested` 契約）。
> Phase 8 不做動態定價 / 時間系統 / 拾遺副業經濟；後門鎖死。

### 新增 / 異動檔總表

```text
新增：
  scenes/levels/convenience_store/convenience_store.tscn/.gd  # 8-A：室內場景（貨架/門/熟食機/櫃台機器人/冷藏櫃/員工區+店籍主機/鎖住後門）
  data/dialogue/store_robot.gd                                # 8-B/8-D：機器人對話樹（前導 babble + 深度診斷 + 修好後）
  data/quests/repair_vendor_bot.gd                            # 8-C：任務資料 + 工作筆記模板
  data/shops/shop_db.gd                                       # 8-F：shop_id -> shop data 對照
  data/shops/convenience_store.gd                             # 8-F：店內 catalog
  data/shops/street_vending.gd                                # 8-G：街道飲料 catalog
  scenes/ui/shop_panel.tscn/.gd                               # 8-F：ShopPanel 雙欄買賣 UI
異動：
  scenes/main/main.gd                                         # 8-A：SceneRegistry 新增 convenience_store
  scenes/levels/apartment_entrance.gd(.tscn)                  # 8-A：store_front 改門轉場 + from_store entry；8-B：外面販賣機前導；8-G：修好後飲料商店
  scenes/levels/apartment/apartment_room.gd(.tscn)            # 8-C：電腦兩段 gate
  scripts/autoload/game_state.gd                              # 8-C：computer/vendor flags；8-E：殘響物品；8-F：ITEMS_DB value、shop_states、commerce API、SELL_RATIO
  scripts/autoload/ui_mode.gd                                 # 8-F：新增 SHOP mode
  scripts/dialogue/dialogue_runner.gd                         # 8-D：診斷對話 condition（has_note）；effect set_quest_flag；8-E：reveal/repair effect；8-F：open_shop effect
  data/dialogue/dialogue_db.gd                                # 8-B：註冊 store_robot
  data/quests/quest_db.gd                                     # 8-C：註冊 repair_vendor_bot
  scenes/ui/game_ui.gd(.tscn)                                 # 8-E：店籍主機重置流程 + 殘響取得；8-F：持有 ShopPanel、open_shop、買賣 routing、toast
  scripts/autoload/touch_controls.gd                          # 8-F：ShopPanel 觸控 routing
  tests/manual/test_runner.gd                                 # 8-A~8-H：headless 邏輯測試 + 回歸
```

### 8-A 場景骨架 + 轉場

#### SceneRegistry（`main.gd`）

```gdscript
"convenience_store": {
    "path": "res://scenes/levels/convenience_store/convenience_store.tscn",
    "entry_points": ["from_street"],
    "music_id": "store_interior"   # 暫可沿用 street_rain，待 BGM 決定
},
```

- 街道 `apartment_entrance` 新增回程 entry point `from_store`（玩家落在店門口附近）。
- `convenience_store.gd` 提供 `prepare_entry_point` / `set_entry_point`（沿用 4-E 簽名）；`from_street` 落在自動門內側 idle，不播任何開場。
- 進場時 `SaveSystem.can_save_here = true`；`_exit_tree()` 不需特別還原（既有預設為可存；僅外牆場景才切 false）。

#### 街道 `store_front` 改門

- `store_front` 互動由「examine 顯示 MESSAGES["store_front"]」改為 emit `scene_transition_requested("convenience_store", "from_street")`（沿用 4-E 大門模式）。
- 不用 `change_scene_to_file`；不直接碰 GameUI。

### 8-B 前導對話 + 發現錯誤旗標

#### 機器人前導對話（`data/dialogue/store_robot.gd`）

- 註冊進 `dialogue_db.gd`；機器人 `store_robot` interactable 帶 `dialogue_id = "store_robot"`（沿用 5-D dialogue dispatch 泛化）。
- `start` 路由節點依狀態分流（沿用 `wan.gd` 模式）：

```gdscript
"start": {"goto": [
    {"condition": {"flag": "vendor_bot_repaired", "op": "==", "value": true}, "target": "repaired_router"},
    {"condition": {"type": "quest_status", "quest_id": "repair_vendor_bot", "op": "==", "value": "active"}, "target": "diagnose_intro"},
    {"target": "babble_intro"}
]}
```

- `babble_intro`：前導胡言亂語 leaf；其 effect 設 `talked_store_robot`。
- `repaired_router`：依 `store_robot_resolution` 分 reset / gleaned 兩種招呼（見 8-E）。

#### 外面販賣機前導（`apartment_entrance`）

- 既有 `vending_machine` examine 文字改為前導 babble 訊息（與機器人錯亂呼應）。
- 互動 effect 設 `talked_outside_vendor`（透過 level → Main → GameState；或 interactable 帶一次性 flag 設定 hook）。

#### 發現錯誤聚合（`GameState`）

```gdscript
func _maybe_set_discovered_vendor_error() -> void:
    if get_flag("talked_outside_vendor", false) and get_flag("talked_store_robot", false):
        set_flag("discovered_vendor_error", true)
        add_knowledge(STORY_NOTES["clue_vendor_error_lead"])
```

- 在 `set_flag("talked_outside_vendor"/"talked_store_robot", true)` 後呼叫；idempotent。
- `clue_vendor_error_lead` 為 `category == "線索"` 的一次性前置筆記，只在兩台都互動後與 `discovered_vendor_error` 同步成立；單談其中一台不可新增。筆記文案只提示「這不像單一設備故障，可能會是主角可接的工作；可回房間電腦確認」，不提前透露 `repair_vendor_bot` 任務細節。

### 8-C 公寓電腦兩段 gate + 接案

#### 電腦互動邏輯（`apartment_room.gd` 桌上電腦）

```text
on interact 電腦：
  if not GameState.get_flag("used_room_computer_once", false):
      顯示舊內容（work_ai_cleanup_role examine note）
      GameState.set_flag("used_room_computer_once", true)
  elif GameState.get_flag("discovered_vendor_error", false)
       and QuestManager.get_status("repair_vendor_bot") == "":
       # 未接任務時 get_status 回 ""（quest_states 無此 id）；codebase 沒有 "inactive" 狀態值
      顯示派工訊息
      QuestManager.start("repair_vendor_bot")
  else:
      顯示舊內容 / 無新訊息
```

- 第一次互動恆給舊內容（建立「你有份工作」鋪陳），即使 `discovered_vendor_error` 已成立。
- `work_ai_cleanup_role` 既有 examine 行為不破壞（沿用 2-A note）；只是改由電腦 controller 依 gate 決定顯示哪段。

#### 任務資料（`data/quests/repair_vendor_bot.gd`）

```gdscript
const QUEST_ID := "repair_vendor_bot"
const WORK_NOTE_ID := "quest_repair_vendor_bot"

const STEPS := { "started": {} }        # QuestManager.start() 寫死 step = "started"；蒐證 / 診斷期間不換 step，完成由店籍主機 complete

const WORK_NOTES_BY_STEP := {
    "started": { "id": WORK_NOTE_ID, "category": "工作", "title": "便利商店的故障機器人", "body": "...", "status": "active" },
}

const WORK_NOTES_BY_STATUS := {         # sync_work_note() completed/failed fallback 會讀此常數，必須存在
    "failed": { "id": WORK_NOTE_ID, "category": "工作", "title": "便利商店的故障機器人", "body": "<保留：目前無失敗路徑>", "status": "failed" },
}

const HAS_COMPLETED_NOTE_RESOLVER := true   # sync_work_note() 以 "HAS_COMPLETED_NOTE_RESOLVER" in quest_data 常數守衛啟用 resolver

const WORK_NOTES_COMPLETED := {
    "reset":    { "id": WORK_NOTE_ID, "category": "工作", "title": "便利商店的故障機器人", "body": "<直接重置版>", "status": "completed" },
    "gleaned":  { "id": WORK_NOTE_ID, "category": "工作", "title": "便利商店的故障機器人", "body": "<拾殘響版>", "status": "completed" },
}

static func resolve_completed_note() -> Dictionary:
    if GameState.get_flag("store_robot_resolution", "") == "gleaned":
        return WORK_NOTES_COMPLETED["gleaned"]
    return WORK_NOTES_COMPLETED["reset"]
```

- 沿用 7-K 的 `resolve_completed_note()` 機制；注意 `sync_work_note()` completed 分支的守衛是 `"HAS_COMPLETED_NOTE_RESOLVER" in quest_data` **常數檢查**（不是 `has_method`），漏掉該常數 resolver 不會被呼叫且 fallback 讀 `WORK_NOTES_BY_STATUS` 會因常數缺失出錯。
- 註冊進 `quest_db.gd`。

### 8-D 蒐證 + 診斷對話樹

#### 線索物件（`convenience_store`）

- 5 個線索 interactable（員工置物櫃 / 日記殘頁 / 辭退通知 / 機器人銘牌 / 櫃台照片），各帶 `note_id = clue_*`、`category == "線索"`（沿用 2-A examine + `has_note` 去重）。
- 線索開放 gate：interactable 在 `repair_vendor_bot` 非 active 時不可互動或只給模糊 prompt；接案後開放 examine。少數基礎 flavor 物件（貨架 / 熟食機 / 冷藏櫃）不 gate，給無關淺說。

#### 診斷對話樹（`store_robot.gd` / `diagnose_*`）

- 正確選項以 `has_note(clue_*)` 為條件（沿用 5-B/7-B condition：`{"has_note": "clue_clerk_diary"}` 或既有 condition 機制；若無 `has_note` condition op，於 `DialogueRunner` 新增，讀 `GameState.has_note()`）。誤導選項無條件恆在。
- 對話結構：數個 choice 節點，每節點一個「正確」（gated by 對應 clue note）+ 若干誤導；走完全部正確分支抵達 `diagnose_truth` leaf，否則任一誤導 / 缺 note 落到 `diagnose_partial` leaf。
- effect：
  - 揭示主機（兩 leaf 共用，或對話進入診斷即設）：`{"op": "set_quest_flag", "quest_id": "repair_vendor_bot", "key": "mainframe_revealed", "value": true}`。
  - `diagnose_truth` leaf 額外：`{"op": "set_quest_flag", "quest_id": "repair_vendor_bot", "key": "understood_robot_truth", "value": true}`。
- `DialogueRunner._apply_effect()` 新增 `set_quest_flag` op → `QuestManager.set_flag(quest_id, key, value)`。
- 對話可反覆：旗標只設不清；玩家補查 clue 後重談可從 partial 升到 truth。

### 8-E 店籍主機三段 + 兩種結局

#### 互動物 `store_registry_host`（員工區）

- `mainframe_revealed`（quest flag）為假時：interactable 不可互動 / 不出 prompt（level controller 檢查 quest flag）。
- 可互動後，依狀態分三段（level emit `interaction_requested`，Main → GameUI 顯示對應訊息 / 選項；或主機本身帶一個小型選單 message）：

```text
段1：not understood 且 診斷未完成（無 partial/truth leaf 走過）-> 顯示「這是店籍主機，你還看不懂怎麼修」
段2：診斷完成（partial）但 not understood          -> 提供「直接重置」
段3：understood_robot_truth                        -> 提供「直接重置 / 先錄殘響再重置」
```

- 「診斷完成」判定：可用一個 quest flag `diagnosed`（partial/truth leaf 皆設）區分段1與段2；段3 由 `understood_robot_truth` 蓋過。

#### 重置流程（`game_ui.gd` 或主機 message 選項 effect）

- 直接重置：

```text
set_flag store_robot_resolution = "reset"
set_flag vendor_bot_repaired = true
QuestManager.complete("repair_vendor_bot")
顯示修復訊息（暗示外面販賣機因連線也恢復）
```

- 先錄殘響再重置（背包滿防呆，沿用 7-G/7-H）：

```text
if not GameState.add_item("clerk_echo_recording", 1):
    顯示背包放不下，整筆中止（不設旗標、不重置、可重試）
    return
GameState.add_knowledge(殘響筆記 payload)   # category 線索（合法分類僅 身份/工作/線索；不可用「拾遺」否則 Notebook UI 不顯示）
set_flag store_robot_resolution = "gleaned"
set_flag vendor_bot_repaired = true
QuestManager.complete("repair_vendor_bot")
顯示修復訊息
```

- complete 後機器人 `repaired_router` 依 `store_robot_resolution` 給 reset / gleaned 招呼，招呼結尾以 `open_shop` effect 開 ShopPanel（見 8-F 開店入口）。
- 重置後 `store_registry_host` 只給已修復中性訊息，不再提供重置。

#### 殘響物品（`ITEMS_DB`）

```gdscript
"clerk_echo_recording": {
    "id": "clerk_echo_recording",
    "name": "店員的殘響",
    "description": "...",
    "category": "misc",
    "stackable": false, "max_stack": 1,
    "discardable": false, "usable": false,
    "sellable": false,          # 不可賣
    "icon_path": "res://assets/generated/sprites/items/clerk_echo_recording/icon.png"
}
```

### 8-F 買賣系統核心 + ShopPanel

#### GameState — 物品價值與賣出

```gdscript
const SELL_RATIO := 0.5

func get_item_value(item_id: String) -> int            # ITEMS_DB[item_id].get("value", 0)
func get_sell_value(item_id: String) -> int            # floor(value * SELL_RATIO)
func is_sellable(item_id: String) -> bool              # value > 0 and get_sell_value > 0 and category != "key_item" and sellable != false（get_sell_value > 0 防 floor 後 0 元賣出）
func sell_item(instance_id: String, count := 1) -> bool # 以焦點背包格 instance_id 定位（仿 discard_item）；is_sellable(該格 item_id) + 該格持有 + 未裝備（_is_equipped 則回 false，沿用 discard 守衛，不自動卸下）→ 移除該格指定數量 + add_credits(get_sell_value * count)；不回補店庫存。不可用 remove_item(item_id)（多 stack/instance 會賣錯格 / 誤觸 force-unequip 副作用）
```

- `ITEMS_DB` 既有條目按需補 `value`（消耗品 / misc 可賣；key_item 不填或標 `sellable=false`）。

#### GameState — 店庫存與買入

```gdscript
var shop_states: Dictionary = {}   # { shop_id: { item_id: {price, stock} } }
signal shop_changed(shop_id: String)

func get_shop_stock(shop_id: String) -> Dictionary     # lazy-init from ShopDB on first access
func refresh_shop_stock(shop_id: String) -> void       # 依 catalog 重設庫存（通關上新 / 顯式呼叫）
func get_buy_price(shop_id: String, item_id: String) -> int
func can_buy(shop_id: String, item_id: String) -> Dictionary   # {ok: bool, reason: String}
func buy_item(shop_id: String, item_id: String) -> bool        # 原子：stock>0 + credits>=price + 背包有空間
```

- `buy_item` 原子性與順序：先純讀檢查 `stock > 0`、`credits >= price`（不過則回 false + 對應 reason）；通過後呼叫 `add_item`（**唯一可能失敗的變動步驟**，背包滿回 false 時狀態未動）；`add_item` 成功後才扣庫存、扣 credits。GameState 沒有背包空間預判 API，空間判定以 `add_item` 成敗為準，不另寫預測邏輯；`can_buy` 只檢 stock / credits 供 UI 提示，背包滿由 `buy_item` 實際執行時回報。
- `shop_states` 納入 `to_save_dict()` / `load_save_dict()` / `reset_for_new_game()`。
- catalog 來源 `data/shops/shop_db.gd`：`{ shop_id: { "name": String, "stock": {item_id: {price, stock}} } }`。

#### UIMode

- 新增 `SHOP`；`mode != NONE` 凍結玩家（沿用既有規則）。`SHOP` 與 INVENTORY/NOTEBOOK 互斥（開店時不可同時開背包）。

#### ShopPanel（`scenes/ui/shop_panel.tscn/.gd`）

- 雙欄：左 `BuyGrid`（店內貨架，顯示名稱 / 買價 / 庫存）；右 `SellGrid`（背包，顯示名稱 / 可賣則顯示售出價、不可賣灰掉）；頂部 credits；footer hint。
- 由 GameUI 持有（沿用 4-B：GameUI 持所有共用 UI 節點），公開：

```gdscript
func open_shop(shop_id: String) -> void   # 設 UIMode.SHOP，填雙欄
func is_shop_open() -> bool
func shop_move_focus(dir) / shop_confirm() # 供 TouchControls（沿用 5-C dialogue routing 模式）
```

- 操作：方向鍵 / focus 在兩欄間切換；左欄 E = `buy_item(shop_id, item_id)`，右欄 E = `sell_item(焦點格 instance_id)`；成功 / 失敗走 FloatingToast（credits 不足 / 背包滿 / 缺貨 / 不可賣 / 已裝備需先卸下）。右欄已裝備格與不可賣物同樣灰掉。`ui_cancel` 關閉回 NONE。
- `shop_changed` / `credits_changed` / `inventory_changed` 重繪雙欄。

#### 開店入口

- 店內機器人 `store_robot`：`vendor_bot_repaired` 後互動仍走對話（`repaired_router` 依 resolution 給 reset / gleaned 招呼），招呼 leaf 帶 effect `{"op": "open_shop", "value": "convenience_store"}`；`DialogueRunner._apply_effect()` 新增 `open_shop` op → 對話收尾後請 GameUI `open_shop`（DIALOGUE 正常結束 → 切 `UIMode.SHOP`，不疊 overlay）。修好前互動只走對話、無此 effect。
- 街道外面販賣機：`vendor_bot_repaired` 後互動**直接開店**（level emit `interaction_requested` 帶 shop 意圖 → Main → `GameUI.open_shop("street_vending")`，無對話）；修好前只給前導訊息（見 8-G）。

### 8-G 迷你飲料商店 + 多商店資料化

- `data/shops/street_vending.gd`：飲料 catalog（待填品項 / 價格）。
- 街道外面販賣機 interactable：`vendor_bot_repaired` 後互動 → `GameUI.open_shop("street_vending")`（直接開店、無對話，與店內機器人「招呼後開店」不同）；修好前只給前導訊息。
- 同一 `ShopPanel` / commerce API 服務兩店，僅 `shop_id` 不同；`shop_states["convenience_store"]` 與 `["street_vending"]` 庫存獨立、各自存讀檔。

### 8-H 回歸 / 存讀檔

- headless 擴充 `test_runner.gd`：commerce 數學（買 / 賣 / 邊界）、shop_states 存讀檔 round-trip、電腦 gate 真值表、發現錯誤聚合、診斷對話 truth/partial 分流、店籍主機三段、殘響背包滿防呆。
- 全鏈回歸（8-H）：8-A~8-G 不破壞既有 Phase 1~7 headless PASS；新增 commerce / 對話 / 任務測試一併 PASS。

## Phase 9 — 拾遺系統（殘響蒐集 + 收藏家）（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 9`；操作清單見 `測試指南.md > Phase 9`；人物見 `subdocs/人/鹿其琛.md`；場景見 `subdocs/地點/收藏家的店.md`。
> 沿用既有契約：全域長期狀態集中在 `GameState`；轉場走 SceneRouter `scene_id + entry_point_id`；對話走 `DialogueRunner` + 條件路由；Level 不直接碰 UI node path。
> Phase 9 不走 QuestManager（鑑定鏈非派工性質）；不做「還」流程、TravelPanel、時間系統。

### 新增 / 異動檔總表

```text
新增：
  data/echoes/echo_db.gd                                  # 9-A：殘響資料庫（segments / image / audio / sell_price / 評語）
  scripts/components/echo_point.gd                        # 9-D：殘響採集點（Area2D；微光 + 距離雜訊 + 靜止計時）
  scenes/levels/collector_shop/collector_shop.tscn/.gd    # 9-C：收藏家的店
  data/dialogue/lu_qichen.gd                              # 9-C/9-G：鹿其琛對話樹（首見 / 鑑定 / 收購 / 提及還）
  data/dialogue/travel_street_east.gd                     # 9-C：暫時通道目的地選單（對話樹實作）
  scenes/ui/photo_viewer.tscn/.gd                         # 9-E：照片檢視 overlay
異動：
  scripts/autoload/game_state.gd        # 9-A：echo store + API + signal + 存讀檔 + 店員殘響 backfill；9-B：old_probe_module / gleaner_gloves ITEMS_DB、fingerless_gloves discardable=false；9-C：install_probe_module()
  scenes/main/main.gd                   # 9-C：SceneRegistry 新增 collector_shop
  scenes/levels/apartment/apartment_room.gd(.tscn)        # 9-B：時鐘取模組分支；9-F：echo point ×1
  scenes/levels/apartment_entrance.gd(.tscn)              # 9-C：右端 leave_block + from_collector_shop entry；9-F：echo point ×5
  scenes/levels/apartment_fire_escape.gd(.tscn)           # 9-F：echo point ×2
  scenes/levels/convenience_store/convenience_store.gd(.tscn)  # 9-F：echo point ×2
  scenes/ui/notebook_panel.gd(.tscn)    # 9-A：殘響分頁（投影）；9-E：媒體 footer + E/R
  scenes/ui/game_ui.gd(.tscn)           # 9-D：採集 toast；9-E：photo_viewer 持有 + 殘響音訊 AudioStreamPlayer
  scripts/dialogue/dialogue_runner.gd   # 9-C：effect travel / install_module；9-G：condition echo_complete / echo_unsold + effect sell_echo
  data/dialogue/dialogue_db.gd          # 9-C：註冊 lu_qichen / travel_street_east
  scripts/autoload/touch_controls.gd    # 9-E：筆記殘響頁 E/R 路由（沿用 3-C 情境查詢，若 GameUI 查詢已覆蓋則僅驗證）
  tests/manual/test_runner.gd           # 9-A~9-H：headless 測試 + 回歸
```

### 9-A 殘響資料模型 + 筆記投影

#### EchoDB（`data/echoes/echo_db.gd`）

```gdscript
const ECHOES := {
    "echo_clerk": {
        "title": "店員的殘響",
        "segments": [ {"id": "s1", "text": "<8-E 錄音轉錄，待填>"} ],
        "audio_path": "res://assets/audio/echoes/echo_clerk.ogg",   # 可選；image_path 同理
        "sell_price": 300,                                           # 數值開工可調
        "comment": "<鹿其琛收購評語，待填>",
    },
    # echo_room401_tenant（3 段、image_path）/ echo_song_rain_doesnt_stop（3 段、audio_path）
    # echo_lu_family（1 段、不可收購）
}
```

- 純 const 資料 + static 查詢 helper（`get_echo(id)` / `get_segment_count(id)`），不持狀態（仿 `quest_db` / `shop_db`）。

#### GameState — echo store

```gdscript
var echo_progress: Dictionary = {}   # { echo_id: {"collected": Array[String], "sold": bool} }
signal echo_changed(echo_id: String)

func collect_echo_segment(echo_id: String, segment_id: String) -> bool
    # 不存在則建檔；重複片回 false 無副作用；成功 append + emit echo_changed
func record_full_echo(echo_id: String) -> void          # 一次寫滿全部段（8-E 回寫用）；idempotent
func has_echo_segment(echo_id, segment_id) -> bool
func is_echo_known(echo_id) -> bool                     # collected 至少 1 片
func is_echo_complete(echo_id) -> bool                  # collected 數 == EchoDB 段數
func is_echo_sold(echo_id) -> bool
func sell_echo(echo_id: String) -> bool                 # complete 且未 sold → add_credits(sell_price) + sold=true + emit；否則 false
```

- `echo_progress` 納入 `to_save_dict()` / `load_save_dict()` / `reset_for_new_game()`（沿用 6-A shape 驗證寬鬆原則：缺欄位給空 Dictionary 預設）。
- 店員殘響 backfill：`_maybe_backfill_clerk_echo()`——`get_flag("store_robot_resolution", "") == "gleaned"` 且 `not is_echo_known("echo_clerk")` 時 `record_full_echo("echo_clerk")`。呼叫點：(a) `set_flag("store_robot_resolution", ...)` hook（新遊戲流程當下寫入）；(b) `load_save_dict()` 尾端（舊存檔補寫）。

#### NotebookPanel — 殘響分頁（投影）

- 新增分頁「殘響」（A/D 切分頁、W/S 選條目沿用既有 notebook 操作）；**不寫入一般 notes store**，列表與內文即時讀 `GameState` echo API + `EchoDB`。
- 列表項：`title +（n/N）`；已售出加註「已售出」。
- 詳情：標題 + segments 依 canonical 順序；未採集段顯示 `????`（佔位行式樣開工定，建議「段落 N：????」）。
- 接 `echo_changed` 重繪（面板開著時採集也即時更新）。

### 9-B 時鐘取模組

#### 時鐘互動分支（`apartment_room.gd`）

```text
on interact 時鐘（偵測終端）：
  if GameState.has_knowledge("identity_door_unlock_method")
     and not GameState.get_flag("probe_module_taken", false):
      if not GameState.add_item("old_probe_module", 1):
          顯示背包放不下訊息；整筆中止（不設 flag、不寫筆記、可重試）
          return
      GameState.set_flag("probe_module_taken", true)
      GameState.add_knowledge(STORY_NOTES["clue_probe_module_lead"])   # category 線索
      顯示取出模組訊息
  else:
      原有時鐘行為不變
```

- `clue_probe_module_lead` 文案方向：模組老舊、用途不明；「街區外有個收舊貨的鹿三爺，什麼都認得」；不透露殘響概念。
- ITEMS_DB 新增 `old_probe_module`：`discardable=false`、`sellable=false`、不可裝備、`usable=false`。
- `fingerless_gloves` 既有條目補 `discardable=false`（防鑑定前丟失 softlock）；`sellable` 維持不可賣。

### 9-C 暫時通道 + 收藏家的店 + 鑑定

#### SceneRegistry（`main.gd`）

```gdscript
"collector_shop": {
    "path": "res://scenes/levels/collector_shop/collector_shop.tscn",
    "entry_points": ["from_street"],
    "music_id": "collector_shop",
},
```

- 街道 `apartment_entrance` 新增 entry point `from_collector_shop`（落在街道最右端）。
- `collector_shop.gd` 提供 `prepare_entry_point` / `set_entry_point`（4-E 簽名）；`from_street` 落門口 idle，不播開場；`can_save_here` 維持預設 true。

#### 暫時通道（街道右端）

- 新互動物 `leave_block`（街道最右端）帶 `dialogue_id = "travel_street_east"`，走既有 dialogue dispatch。
- `travel_street_east` 對話樹 = 目的地選單：「要去哪裡？」選項「收藏家的店」（effect `travel`）/「取消」（結束）。未來目的地加選項即可；待目的地多時再換正式 TravelPanel。
- `DialogueRunner._apply_effect()` 新增 `travel` op：`{"op": "travel", "scene_id": "collector_shop", "entry_point_id": "from_street"}` → 比照 `open_shop` 的 pending 交棒模式，具體路徑：
  1. `DialogueRunner` 新增 `pending_travel: Dictionary`（預設 `{}`）；`travel` effect 寫入 `{"scene_id": ..., "entry_point_id": ...}`，不在對話中途轉場。
  2. `DialoguePanel.close_dialogue()` 收尾時檢查 `_runner.pending_travel`；非空則呼叫 `GameUI.travel_requested(scene_id, entry_point_id)`（新 signal）。
  3. `Main._ready()` 連接 `game_ui.travel_requested`，handler 直接呼叫既有 `transition_to(scene_id, entry_point_id, {})`。

#### 鹿其琛對話樹（`data/dialogue/lu_qichen.gd`）

- `start` 路由（沿用 `wan.gd` / `store_robot.gd` 模式）：

```gdscript
"start": {"goto": [
    {"condition": {"flag": "met_lu", "op": "==", "value": false}, "target": "first_meet"},
    {"target": "hub"}
]}
```

- `first_meet`：effect 設 `met_lu`。
- `hub`（平時入口）選項（條件式顯隱，沿用 7-I 選項 condition）：
  - 「給他看那個模組」：condition = inventory 持有 `old_probe_module` 且持有或裝備 `fingerless_gloves` 且 `gleaner_gloves_installed == false` → `appraise` 分支。
  - 「給他看收藏」：任一殘響符合收購條件時顯示 → `sell_menu`（見 9-G）。
  - 閒聊 / 離開。
- `appraise` 分支：鑑定獨白（滔滔不絕段）→ effect `{"op": "install_module"}` → 後續對話段教使用方式 + **口頭提及「還」**（「有些東西……賣給我之外，還有別的去處。以後再說。」方向，文字見 subdoc）。
- `DialogueRunner._apply_effect()` 新增 `install_module` op → `GameState.install_probe_module()`。

#### GameState.install_probe_module()

```gdscript
func install_probe_module() -> bool:
    # 1. 驗證前置條件（任一失敗 → return false，不動任何東西）
    #    - old_probe_module 在背包
    #    - fingerless_gloves 在背包或裝備中（記錄其 instance 與位置）
    # 2. 全通過後一次性變更：
    #    remove old_probe_module
    #    fingerless_gloves -> gleaner_gloves 原地置換：
    #      裝備中 -> 置換 equipment 槽該 instance 的 item_id；在背包 -> 置換該格 item_id
    #      （同 instance_id、同位置，只換 id；emit inventory_changed / equipment_changed）
    # 3. set_flag("gleaner_gloves_installed", true)
```

- ITEMS_DB 新增 `gleaner_gloves`：同 `fingerless_gloves` 裝備位 / 類別，名稱「拾遺手套」、說明更新；`discardable=false`、`sellable=false`。

### 9-D 感知採集機制

#### EchoPoint（`scripts/components/echo_point.gd`，Area2D）

```gdscript
@export var echo_id: String
@export var segment_id: String
@export var prompt_text: String = "採集殘響"
```

- active 條件：玩家裝備 `gleaner_gloves` 且 `not GameState.has_echo_segment(echo_id, segment_id)`；接 `equipment_changed` / `echo_changed` 重新評估。
- 視覺：active 時顯示微光節點（placeholder 可用閃爍 sprite / 粒子；視覺語言沿用聲納 reveal 電磁感），非 active 時整體隱藏。
- 雜訊：`AudioStreamPlayer2D` loop（雜訊 SFX variant 之一），active 時播放，靠距離衰減（`max_distance` / attenuation 用引擎內建，不自寫音量曲線）。
- 靜止判定：玩家在 Area 內且速度 ≈ 0 連續 ≥ 1.0 秒 → 通知 level controller 將本點設為 current interactable（走既有 `current_interactable_changed` 契約出 prompt）；玩家移動或離開 → 重置計時、撤 prompt。
- E 採集：`collect_echo_segment()` 成功 → 採集 SFX + GameUI toast「殘響已記錄：<title>（n/N）」→ 本點 deactivate（消失）。
- 各 level 場上以 `EchoPoint` instance 配置（`.tscn` 內擺點），level controller 只負責 prompt 轉發，不持殘響邏輯。

### 9-E 媒體層

#### NotebookPanel footer（沿用 PanelFooterHint + 3-C 情境鍵）

- 殘響詳情頁、該條 `is_echo_complete` 且未 `sold` 時：
  - 僅 `image_path` → `E: 看照片`；僅 `audio_path` → `E: 播放錄音`；皆有 → `E: 看照片 / R: 播放錄音`；皆無 → 無媒體控制。
- 觸控：沿用面板模式情境按鈕（E / R 顯隱跟上述一致）；若 TouchControls 既有 `can_*_action` 查詢不含 notebook 情境，於 GameUI 補對應查詢。

#### 照片檢視（`scenes/ui/photo_viewer.tscn/.gd`）

- GameUI 持有的 overlay（仿 ItemDetailModal：記住 caller mode = NOTEBOOK，關閉還原，不關 caller UI）；顯示 `image_path` texture；`E` / `ui_cancel` 關閉。

#### 錄音播放

- GameUI 持有一個非定位 `AudioStreamPlayer`（殘響專用，不與 BGM / SFX player 共用）；播放中再按 → 停止；關閉筆記、切分頁、切條目 → 停止。
- 不做進度條 / seek；播完自動停。

### 9-F 內容鋪設

- 4 條殘響文字寫入 `echo_db.gd`（內容方向見規格書內容清單；場景擺位見各 level `.tscn`）。
- 採集點分佈：公寓 ×1、街道 ×3、逃生梯 ×1、便利商店 ×1、收藏家的店 ×1（共 7；`echo_clerk` 無場上採集點）。
- 素材落點：照片 → `assets/generated/sprites/echoes/...`（Art Bible 管線）；音訊 → `assets/audio/echoes/`；雜訊 SFX variant ×3~4 → `assets/audio/sfx/`。
- 逃生梯（外牆禁存場景）採集不受 `can_save_here=false` 影響（採集只寫 GameState，不觸存檔）。

### 9-G 收購（賣 vs 留）

#### `sell_menu`（`lu_qichen.gd`）

- 每條可收購殘響一個靜態選項，condition = `{"echo_complete": "<echo_id>"}` + `{"echo_unsold": "<echo_id>"}`（Array=AND 沿用 7-B）；**排除 `echo_lu_family`**（資料中不建選項）。
- 選定 → 該條專屬評語節點（`comment` 文字）→ 二選一「賣他 / 還是留著」→ 賣：effect `{"op": "sell_echo", "value": "<echo_id>"}`；留：返回 `sell_menu`。
- 無任何符合條件殘響時：「給他看收藏」選項不顯示（hub 端同樣用 condition 控制；若既有 condition 不支援「任一 echo 可賣」聚合，hub 選項改為恆顯示、進入後由鹿其琛說「殘缺的故事不值錢」打發，二擇一開工時定，傾向後者——少一個聚合 condition op）。

#### DialogueRunner 擴充

- condition op：`echo_complete` / `echo_unsold`（讀 `GameState.is_echo_complete` / `not is_echo_sold`）。
- effect op：`sell_echo` → `GameState.sell_echo(echo_id)`；失敗（防御性）不加錢不標記。
- 賣出後筆記投影自動反映（`echo_changed` → NotebookPanel 重繪；媒體 footer 條件含 `not sold` 自然失效）。

### 9-H 回歸 / 存讀檔

- headless 擴充 `test_runner.gd`：echo API（採集 / 重複片 / 集滿判定 / record_full_echo idempotent）、店員殘響 backfill（新流程 + 舊存檔兩路）、時鐘 gate 真值表 + 背包滿防呆、install_probe_module（裝備中 / 背包中兩路置換）、travel effect 轉場、echo_progress 存讀檔 round-trip、sell_echo（未集滿 / 已售出 / 正常）、收購 condition 分流。
- 全鏈回歸：9-A~9-G 不破壞 Phase 1~8 headless PASS。

## Phase 10 — 氛圍與演出（Atmosphere & Presentation pass）（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 規劃 > Phase 10`；操作清單見 `測試指南.md > Phase 10`。
> 本階段純演出，不改互動 / 解謎 / 對話 / 任務 / 存讀檔邏輯。只在街道 `apartment_entrance` 做垂直切片。
> 既有可複用基礎：`main.gd` BGM ducking（`pause_bgm` / `resume_bgm`，由 `game_ui.gd` 依 echo 音訊播放觸發）；`Camera2D` 已有 `position_smoothing`；EchoPoint 微光（`_draw()`）與漸強雜訊（positional `AudioStreamPlayer2D`）。

### 新增 / 異動檔總表

```text
新增：
  assets/audio/ambient/street_rain_loop.mp3      # 10-A：雨環境 bed（無縫 loop）
  assets/audio/ambient/subway_rumble*.mp3        # 10-A：地鐵遠轟 one-shot（單檔或 _a/_b/_c 變體，.gd 隨機挑）
  assets/vfx/rain_streak.png                     # 10-B：雨絲粒子貼圖（8x64，RGB 全白、形狀在 alpha、頭虛尾實）
  assets/vfx/rain_splash.png                     # 10-B：落地水花粒子貼圖（24x24，壓扁漣漪環）
  assets/vfx/gen_rain_vfx.py                     # 10-B：上兩張貼圖的生成腳本（Pillow+numpy，確定可重現、改參數可重生）
  assets/shaders/vignette.gdshader               # 10-B：暗角（亦可用一張預乘 vignette 貼圖取代，二擇一）
  assets/shaders/billboard_screen.gdshader       # 10-C-1：看板螢幕質感（掃描線 + flicker + 邊緣輝光；只作用於看板那塊 sprite）
  assets/generated/sprites/street_billboard_ad/… # 10-C-1：看板廣告靜圖（不去背、opaque；長寬比對齊看板矩形；先 1 張，OK 再加）
  assets/generated/sprites/wan/idle_glance/wan-idle-glance-20260616-225800/hires_1024/final_normalized/  # 10-D：晚撇一眼暗巷 6 幀素材（黑邊版，已對齊頭頂 / 鞋底 baseline）
  scripts/components/idle_break.gd               # 10-D：可掛載的 idle-break 驅動組件（每隔隨機秒插播指定動畫；刪節點即還原）
  （10-C-2 條件式）街道分層素材 + 發光層 + 濕地面反射素材  # 走 agent-sprite-forge，受 Art Bible 規範
異動：
  scenes/levels/apartment_entrance.tscn          # 10-A：環境音節點；10-B：雨粒子 / 水花 / vignette；10-C-1：看板廣告螢幕 + 路燈柔光池；10-C-2：分層 / 發光層 / Parallax / Light2D / 反射；10-D：晚改 AnimatedSprite2D + IdleBreak 子節點
  scenes/levels/apartment_entrance.gd            # 10-A：地鐵 Timer 隨機間隔 + 環境音控制；10-B：相機微擺；10-C-1：看板輪播切換（多張時）；10-C-2：發光層動畫驅動
  scenes/main/main.gd                            # 10-A：echo duck 擴及環境音（壓 Ambient bus）；釐清街道 BGM 單一來源
  scenes/ui/game_ui.gd                           # 10-A：既有 echo duck 觸發點同步 duck Ambient bus（若改走 bus）
  project.godot / default_bus_layout.tres        # 10-A：新增 Audio Bus「Ambient」
  tests/manual/test_runner.gd                    # 10-A~10-C：節點存在 / 載入無錯 / 回歸護欄
```

### 10-A 街道音景

#### Audio Bus「Ambient」

- `default_bus_layout.tres` 新增 bus `Ambient`（route 到 Master）。環境音 player 全部輸出到 `Ambient`。
- **理由**：1~2 軌環境音用獨立 bus，ducking 只需壓一個 bus，比逐 player tween 乾淨；與既有「逐 player 壓 BGM」不衝突（BGM 仍走 `main.gd` 既有路徑）。

#### 環境音節點（`apartment_entrance`）

```text
AmbientRain   : AudioStreamPlayer（bus="Ambient", autoplay, loop, volume_db≈-22）
AmbientSubway : AudioStreamPlayer（bus="Ambient", one-shot, volume_db≈-26, 可掛 reverb）
SubwayTimer   : Timer（one_shot, 由 .gd 每次以隨機 40~90 秒重設並 start；timeout → AmbientSubway.play()）
```

- 雨 bed 非定位（全域 `AudioStreamPlayer`，非 `AudioStreamPlayer2D`）。素材為 **mp3**（與既有 BGM 同格式）；loop 沿用 `main.gd` 既有 pattern——`AudioStreamMP3` 有 `loop` 屬性，`if "loop" in stream: stream.loop = true` 即可。
- 地鐵間隔在 `.gd`：`_arm_subway()` → `SubwayTimer.start(randf_range(40.0, 90.0))`；`timeout` 播放後再 `_arm_subway()`。

#### echo ducking 擴及環境音

- 既有：`game_ui.gd` 偵測 echo 媒體播放 → `main.pause_bgm()` / `resume_bgm()`。
- 擴充：同一觸發點同步壓 / 放 `Ambient` bus：`AudioServer.set_bus_volume_db(idx, ducked)` ↔ 還原（以 tween 或既有 0.3s 淡入淡出對齊）。封裝成 `main.duck_ambient(on: bool)` 由 `game_ui.gd` 在現有 duck 規則處一併呼叫，保持「單一規則 keyed on `_audio_echo.playing`」。
- EchoPoint 漸強雜訊（positional）**不** route 到 Ambient bus、**不**在此 duck（它是偵測線索，非媒體播放）。

#### 街道 BGM 單一來源釐清

- 現況：`apartment_entrance.tscn` 內有 `BGMPlayer` 節點（-12dB），而 `main.gd` 另有中央 BGM 系統 + SceneRegistry `music_id`。確認街道實際由誰播：
  - 若由 `main.gd` 中央系統播 → 移除 / 停用場景內 `BGMPlayer`（避免雙頭疊音、ducking 只壓中央那軌時場景軌仍響）。
  - 若由場景內節點播 → 將 ducking 對齊到該節點，或統一收編進中央系統。
- 二擇一於開工時定，原則：**單一 BGM 來源**，ducking 才不會漏壓。

### 10-B 不分層視覺基底（`apartment_entrance`）

#### 節點

```text
CanvasModulate           : color≈冷藍夜色；可由 .gd 極慢呼吸（modulate value 微幅 tween，週期數十秒）
RainFar  : GPUParticles2D（texture=res://assets/vfx/rain_streak.png；稀 / 細 / 慢，低 amount，低 alpha，modulate 染霓虹色，輕傾角）
RainNear : GPUParticles2D（texture=res://assets/vfx/rain_streak.png；粗 / 快，amount 仍受手機上限約束）
RainSplash : GPUParticles2D（texture=res://assets/vfx/rain_splash.png；畫面底部水花，一次性 burst 樣式 / 短生命）
Vignette : CanvasLayer + ColorRect(material=vignette.gdshader)  # 或一張全螢幕預乘 vignette 貼圖
```

- 雨粒子放在隨相機移動的層（或設為相機子節點 / 固定螢幕空間），確保 4080 寬場景任何位置都有雨；**amount / lifetime 設保守值，手機優先**。
- `CanvasModulate` 作用於整個 `CanvasItem` 樹——確認不會把 UI（`GameUI` 在獨立 CanvasLayer）一起染；若染到則 UI 層排除。
- vignette 與雨層的繪製順序：雨在世界之上、vignette 最上、但都在 `GameUI` 之下（不可遮提示 / 背包 / MessageBox）。

#### 相機微擺（`apartment_entrance.gd`）

- 在既有 `Camera2D.position_smoothing` 之上，加極輕微 idle offset（如低頻 sin drift，振幅數 px）；**不得**改變 `limit_*` 與既有跟隨手感；玩家移動時可淡出微擺，避免暈。

### 10-C-1 看板廣告螢幕（免分層，可先行，無 gate）

中央後方遠景看板（青色面板，烤在 `Background` 死圖裡）疊一塊會「播放」的廣告螢幕。**固定在世界座標**（看板在背景上不隨鏡頭走），故掛在場景樹下、非 `Camera2D` 子節點。

#### 節點（`apartment_entrance`）

看板是**透視斜面**（左高右低、右側往內縮），仿射的 `AnimatedSprite2D` + skew 貼不準；改用 **`Polygon2D`** 以 4 頂點直接貼合斜面。

```text
BillboardScreen : Polygon2D（定位於世界座標；不必旋轉，斜由頂點吃）
  - polygon = 看板顯示面 4 角（實測，世界 px；節點 position=0 時直接用）：
        TL(1198, 58)  TR(1322, 76)  BR(1320, 128)  BL(1198, 118)
  - texture = opaque 廣告圖（不去背，平面正圖）
  - uv = (0,0)(texW,0)(texW,texH)(0,texH) 依序對應上面 polygon 四角
  - material = ShaderMaterial(billboard_screen.gdshader)   # 螢幕質感
```

- **顯示面實測**：包圍盒約 x1198–1322 / y58–128（124×70 px）；左邊高 ~61 / 右邊高 ~54（透視）；face 比例 ≈ **2.1:1**。
- **廣告圖是「平面正圖」**：AI 只生**正面** 2:1 廣告（約 1024×512），**透視由 Polygon2D 的 UV→頂點映射自動完成**；切勿叫 AI 畫已斜掉的圖（會雙重透視）。
- **「之後加幀不用重做」**：擴輪播時換 `Polygon2D.texture`（多張 + `.gd` 切換 + glitch 轉場、每張停幾秒）；節點與 shader 不動。
- **screen shader 對 Polygon2D**：shader 作用在 `UV`（廣告圖空間），掃描線 / flicker / 邊緣輝光照常；UV 為規則矩形，掃描線會跟著斜面走（正好對）。
- **screen shader（`billboard_screen.gdshader`）**：在 sprite 自身 UV 上做掃描線（密度可調）+ 偶發 flicker（亮度 / alpha 抖動）+ 邊緣輝光 / 輕微面板暗角；可選極輕 RGB 錯位；參數 export 方便調。**只作用於這塊 sprite，非全螢幕**——與「不做全螢幕 CRT」不衝突。
- **發光感**：螢幕在死圖裡本就泛青光；shader 提亮 + 邊緣輝光即可，毋須 `Light2D`。要溢光可另疊一張極淡 additive 副本（後置、非必要）。
- **繪製順序**：在 `Background` 之上、雨層之下（雨應蓋過看板）；確認不被 vignette / 雨糊掉。
- **素材**：`assets/generated/sprites/street_billboard_ad/`，長寬比對齊看板矩形（實際 px 進編輯器量），opaque，riso + Art Bible 限色，內容走 AI 世界觀在地廣告。**先 1 張**。

#### 路燈柔光池（選配，同屬 10-C-1）

- 1~3 盞 `PointLight2D`（軟大弱、暖橘或冷藍）擺在死圖既有燈源位置；軟光 texture（放射狀漸層）。手機 ≤ 3。與看板獨立，可單獨做或略過。

### 10-C-2 分層進階（條件式，gate 在 10-B 目視通過後）

> 僅在決定投入美術分層時開工；規格不保證執行。前置：街道背景分層素材（背景 / 中景 / 前景 + 獨立發光層），受 `Art Bible.md` 規範。

- **發光層**：招牌 / 窗戶獨立透明 sprite，Additive（`CanvasItemMaterial.blend_mode = BLEND_MODE_ADD`）；由 `.gd` / `AnimationPlayer` 動 `modulate.a`（壞霓虹 stutter / 緩慢呼吸）。優先於 `Light2D` 打平圖。
- **`Light2D`（`PointLight2D`）**：僅有來源、軟（大 texture、低 energy）、數量 ≤ 3；手機效能優先。
- **視差**：`ParallaxBackground` + `ParallaxLayer`（或 `Parallax2D`），遠層 `motion_scale` < 近層；與 `Camera2D` limit 對齊避免露邊。
- **濕地面反射**：地面反射 strip（鏡像發光層 + 垂直翻轉 + 低 alpha + 輕微 UV 擾動 shader），或 `Sprite2D` 反射貼圖。
- **kill-switch**：若 10-B 已足夠，本段整體可不開；已開但效果不達標時，退回保留 10-A/10-B。

### 10-D 晚 idle break（撇一眼暗巷）

走真動畫幀；採**可獨立移除**的組件設計，刪節點即還原靜態 idle。

#### 節點（`apartment_entrance` 之 `Interactables/NpcWan`）

- 現況：`NpcWan/Sprite2D` 為單張 texture（`wan-idle-transparent`）。改為 `AnimatedSprite2D`：
  - SpriteFrames：`idle`（1 幀，現有圖）+ `idle_glance`（6 幀，**非 loop**）。
- `NpcWan` 下掛 `IdleBreak`（Node，`scripts/components/idle_break.gd`）：
  - export：`target_path`（指向 AnimatedSprite2D）、`idle_anim` / `break_anim`、`interval_min` / `interval_max`。
  - 行為：`Timer` 每隔 `randf_range(min, max)` 秒 → 播 `break_anim` 一次 → `animation_finished` 回 `idle_anim` → 重新計時。
  - **可獨立移除**：刪 `IdleBreak` 節點即停（`AnimatedSprite2D` 留在 `idle`）；不碰 `apartment_entrance.gd` 與 `talk_wan` 邏輯。

#### 素材

- `assets/generated/sprites/wan/idle_glance/wan-idle-glance-20260616-225800/hires_1024/final_normalized/`：以現有 `wan-idle` 為 reference，forge 生 6 幀並後處理為黑邊版；主用檔為 `sheet-transparent.png` / `animation-preview.gif`，單幀為 `01_idle.png`、`02_begin_turn.png`、`03_full_glance_hold_a.png`、`04_full_glance_hold_b.png`、`05_turn_back.png`、`06_idle_settle.png`。動作節奏：中性 → 開始轉向右側暗巷 → 完整右瞥停 2 幀 → 轉回 → 回 idle。素材已對齊頭頂 / 鞋底 baseline，鞋子完整；`fix_magenta_edge.py` 已把透明邊緣洋紅 fringe 轉黑。**驗收第一關是「接得上現有 idle」**（臉 / 線條 / 配色 / 比例不漂移）；接不上挑圖或重生。
- 暗巷在晚的右側（街道偏東，`AlleyViewArea` ≈ x2618、晚 ≈ x2207），故「撇一眼」是朝畫面右轉頭。

### Phase 10 測試 / 回歸

- `test_runner.gd` 擴充（headless 能驗的有限）：街道場景載入無 runtime error；10-A 環境音節點 + `Ambient` bus 存在、`main.duck_ambient` 介面存在且 idempotent；10-B 雨粒子 / vignette 節點存在；10-C-1 `BillboardScreen`（Polygon2D）存在、`billboard_screen.gdshader` 載入無錯；10-D `NpcWan` 之 `AnimatedSprite2D` 有 `idle` / `idle_glance` 動畫、`IdleBreak` 節點存在。
- 回歸護欄：Phase 1~9 headless PASS 不退化；EchoPoint 採集 / ducking 行為不退化；`talk_wan` 對話仍可觸發。
- **氛圍品質、手機流暢度、看板 / 晚的「看起來對不對」為人工 GUI / 真機走查項，headless 不負責。**

---

## Phase 11+ — 主線《雨還沒停》v2.3（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 11+`；操作清單見 `測試指南.md > Phase 11+`；敘事 / 台詞 / 因果事實來源見 `subdocs/主線/雨還沒停v2.3.md`。
> **順敘執行、不跳號**：Phase 編號＝遊戲時序。本段為規劃骨架，逐 Phase 開工時再把該 Phase 細項展開到完整契約（沿用既有「開工才補細節」慣例，不一次寫滿 90 子階段）。
> 複用優先：殘響（EchoDB / `echo_progress` / EchoPoint / 媒體層）、DialogueRunner / DialoguePanel（condition / effect）、QuestManager、SaveSystem 多槽、跨場景架構（main.tscn + WorldRoot + GameUI contract + SceneRegistry）。新場景一律照 SceneRegistry `scene_id + entry_point_id` 註冊；新狀態一律納入 `to_save_dict` / `load_save_dict` / `reset_for_new_game`。

### 跨 Phase 新地基契約

#### Trust / Trace / 清洗（Phase 11）

> **狀態：11-A/B/C/E 已實作（commit `9f25c55`），headless 待本機驗證；11-D 刻意不做。** 以下為 as-built 契約。

```text
GameState（已實作）：
  signal trace_changed(new_value: int)   # 隱性軸變動；目前無 UI 綁定，留給未來 debug overlay
  var trace: int = 0
  const TRACE_DELTA_SELL := -1           # 賣一條殘響的 trace 降幅（方向性占位，開工可校）
  add_trace(delta: int) -> void          # 正負皆可；delta==0 為 no-op、不 emit；不顯數值
  get_trace() -> int
  get_trust(target: String) -> int       # adapter：回傳 get_flag("affinity_" + target, 0)（如 "wan" → affinity_wan）
DialogueRunner（已實作）：
  effect op "add_trace" { value:int }    # 通用 Trace 變動，供未來「留」↑ /「還」對話使用
存讀檔（已實作）：to_save_dict 寫 "trace"；load_save_dict 缺鍵時 →0（向後相容）；reset_for_new_game 歸零
```

- **賣 / 留 / 還 → trace（11-C，as-built）**：
  - **賣**：hook **集中在 `GameState.sell_echo()`**（成交時 `add_trace(TRACE_DELTA_SELL)`），**不**走對話 effect——因「賣＝再次商品化」是無條件的，集中才保證任何賣出點都生效、不依賴對話作者記得加。
  - **留 / 還**：對應的對話 effect（保存殘響↑ / 給回執·替鹿還↓或不增）尚未有觸發場景（Phase 18/20/鹿線才出現）；本階段先提供通用 `add_trace` effect op 當 hook，數值與接點待那些 Phase 落地時定。
  - 增量數值開工時校（v2.3 §6 表為方向，非數字）。
- **清洗落地時點**：Phase 11 **不**做清洗 UI / 機制（避免提前蓋 placeholder，呼應 4-G 取消教訓）。清洗第一次硬兌現在 **Phase 27**（Expose 上傳前清洗閘）：對「可保存記憶資料」加 `cleaned: bool` + 清洗動作（降 trace、標記、不可逆移除指向活人的細節欄位）。若決定在「替鹿還母親殘響」先讓玩家碰清洗，於鹿線該 Phase 加輕量 hook，仍以 27 為硬兌現點。
- **Trust 第一版不獨立軸**：所有「需要信任」的查詢走 `get_trust()`，內部接 `affinity`；日後抽獨立軸只改 adapter 內部，呼叫端不動。
- **Trust target id 清單（canonical，先定以防各自發明 key）**：`get_trust("<id>")` 讀 `affinity_<id>`；加分一律 `add_int("affinity_<id>", n)`（對話 effect 用既有 `add_int` op，**不要**另發明 effect）。

  | 角色 / 對象 | target id | flag key | 首次需要 |
  |---|---|---|---|
  | 晚 | `wan` | `affinity_wan` | 已存在（Phase 5+）|
  | 七號 | `seven` | `affinity_seven` | Phase 20 / 24 |
  | 伍姐 | `wu` | `affinity_wu` | Phase 16 / 24 |
  | 小岑 | `cen` | `affinity_cen` | Phase 16 / 24 |
  | 聚落（整體信任）| `settlement` | `affinity_settlement` | Phase 24 |
  | 鹿其琛 | `lu` | `affinity_lu` | 鹿線 |

  > 新增 target 時只在本表登錄一筆 + 用 `add_int` 對該 flag 加減；`get_trust` 無需改 code（已是泛型）。Phase 16（三張臉落地）為最晚需在開工前確認此清單的點。

#### 跳躍 / 平台移動（Phase 12，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 12`（含架構決策）；操作清單見 `測試指南.md > Phase 12`。
> **核心決策：walk-line 之上的腳本化拋物弧 + 抓邊緣，不引入全域重力。** 不動 `climb_mode`。

##### 現況基準（開工前必讀）

- `scenes/actors/player/player.gd`（`CharacterBody2D`）：**無重力**。`_physics_process` 水平 `position.x += dir*speed`，`position.y = _walk_y_at(x)` 吸附 walk line；`walk_height_points` 可給高度剖面。`climb_mode` 為獨立垂直模式。動畫：`idle`/`walk`/`climb`/`prone`/`get_up`。座標 API：`get_save_x`/`set_save_x`/`snap_to_walk_line`/`get_facing`/`set_facing`。
- `project.godot [input]`：已有 `move_left/right/up/down`、`interact_primary/secondary/tertiary`、`ui_cancel`。**無 `jump`。**
- `scenes/ui/touch_controls.gd`：DPad（`BtnUp/Down/Left/Right`）+ `Actions/BtnE`；`_bind_button(btn, action)` 綁 input action；`set_game_ui()` 注入 GameUI；顯隱依 `UIMode`（世界模式才顯 DPad / 動作鈕）。

##### 新增 / 異動檔

```text
異動：
  project.godot                          # 12-A：新增 input action「jump」（鍵盤 Space + 觸控 emit）
  scenes/actors/player/player.gd         # 12-A：跳躍狀態機（拋物弧 + 抓邊緣）；不動 climb / walk-line
  scenes/actors/player/…（SpriteFrames） # 12-A：新增 jump（起跳）動畫；可選 fall
  scenes/ui/touch_controls.gd            # 12-C：新增 Jump 鈕（_bind_button 綁 "jump"）+ 顯隱規則
  scenes/ui/touch_controls.tscn          # 12-C：Actions 下新增 BtnJump 節點
  tests/manual/test_runner.gd            # 12-A~12-C：action / 節點 / API 存在 + 回歸護欄
新增：
  scripts/components/ledge_area.gd       # 12-B：可抓取的較高邊緣（Area2D：x 範圍 + 目標 walk y）
  scripts/components/jump_gap.gd         # 12-B：gap 標記（x 區間無走道；落入非跳躍狀態時的處理）
  scenes/levels/jump_proto/…             # 12-B：最小平台原型測試床（gap + ledge + 採集點）
```

##### 12-A player.gd 跳躍狀態（拋物弧 + 抓邊緣）

```text
新增（建議簽名）：
  @export var jump_height := 120.0       # 拋物弧頂點離 walk line 的高度 px
  @export var jump_duration := 0.55      # 滯空總時長 s
  @export var air_speed_scale := 1.0     # 空中水平速度相對地面比例（探索取向，預設等速）
  var _jump_t := 0.0                     # 弧進度（0..jump_duration）；>0 表示空中
  var _jump_base_y := 0.0                # 起跳當下的 walk line y（弧的基準）
  func is_jumping() -> bool              # _jump_t > 0.0
```

- **起跳條件**：`not climb_mode and not UIMode.is_world_input_blocked() and not is_jumping() and Input.is_action_just_pressed("jump")`。
- **弧計算**：以正規化進度 `u = _jump_t / jump_duration ∈[0,1]`，垂直偏移 `offset = jump_height * 4*u*(1-u)`（拋物線，u=0.5 時頂點）；每幀 `position.y = _walk_y_at(position.x) - offset`（基準持續取目前 x 的 walk line，使空中走過斜面也合理），**除非已抓到 ledge**（見下）。水平照常 `position.x += dir*speed*air_speed_scale*delta`，仍受該段 min/max 與 gap 邊界約束。
- **落地**：`_jump_t >= jump_duration` → `_jump_t = 0`，`position.y = _walk_y_at(position.x)`，回 walk / idle。
- **抓邊緣**：弧程中（建議只在 `u >= 0.35` 的上升末段~頂點窗）若 player x 落在某 `LedgeArea2D` 的 x 範圍內，且當前 sprite 高度已接近該 ledge 目標 y（含容錯邊距）→ 結束跳躍、`walk_line_y`（或場景 walk 基準）切到 ledge y、`snap_to_walk_line()`。**這是「跳上更高平台」唯一路徑**。
- **無二段跳 / 蹬牆**：`is_jumping()` 為真時忽略再次 `jump`；不偵測牆面。
- **動畫**：採**單一連貫 clip `jump`**（jump→fall 一鏡到底），落地 / 抓邊緣由狀態機接回 `walk`/`idle`；詳見下方「12-A 跳躍動畫使用契約」。`_apply_sprite_transform()` 既有分支不動，新增 jump 分支（z_index 比照一般 0）。
- **不動**：`climb_mode` 全段、`walk_height_points`、`get_save_x`/`set_save_x`/`snap_to_walk_line`、入口落點。

##### 12-A 跳躍動畫使用契約（單一連貫 clip：jump→fall，落地由狀態接回 idle）

**素材形態**：一條不循環（`loop = false`）的 SpriteFrames 動畫 `jump`，內容＝完整拋物弧一鏡到底：蓄力蹲 → 蹬地起跳 → 上升 → 頂點 → 下落 → 落地緩衝。**不另做獨立 `fall` 動畫**——方案 A 滯空時間腳本固定，單 clip 足夠；下落段就是同一條 clip 的後半。素材規格與輸出見下方「生圖交付」。

**接進 SpriteFrames**：比照既有 `walk`/`get_up`，在各 level 的內嵌 SpriteFrames sub-resource 新增 `jump` 動畫；`loop = false`；`speed` 設成「總幀數 ÷ `jump_duration`」（6 幀 / 0.55s ≈ 11fps，可微調手感）。每幀 512×512 透明 PNG，**與 walk 幀同框、同腳底錨點、同縮放**（見生圖交付），切換 idle↔jump 不可有尺寸 / 落點跳動。

**播放與接回（由 `player.gd` 狀態驅動，不是動畫驅動）**：
- 起跳當下 `anim.play("jump")`。
- 空中（`is_jumping()` 為真）：**jump 分支優先**，跳過既有 `walk`/`idle` 的 `anim.play(...)`；仍依水平輸入更新 `flip_h`（朝向），但不切 `walk`。
- 落地（`_jump_t >= jump_duration`）或抓到 ledge 成功 → 狀態機呼叫 `anim.play("walk" if dir != 0 else "idle")` 接回。
- **嚴禁用 `animation_finished` 訊號接回 idle**：① 抓邊緣會在頂點窗提前結束跳躍（clip 還沒播完）；② 若 clip 比滯空短先播完，`loop = false` 會停在最後一幀（落地緩衝姿勢）等狀態接回，不可因 finished 提前轉 idle。**轉場時機 = 跳躍狀態結束，不是 clip 播完。**

**垂直位移全由腳本弧負責，動畫不可自帶整體上下平移**：每幀角色錨點（腳底基準）固定在與 idle/walk 相同的 canvas y；肢體可蹲、可收腿、手可張，但「整個人往上飄」是 `position.y`（弧 offset）的事，不可畫進幀裡，否則雙重位移。

**生圖交付（給美術 / Codex）**：
- 路徑 `assets/generated/sprites/main_character/jump/<timestamp>/`；幀 `main-character-jump-1..N.png` + `sheet-transparent.png` + prompt / raw / metadata 留同資料夾（沿用 walk/get_up 慣例）。
- 規格：純側視、**只畫朝右一版**（程式 `flip_h` 翻左）、512×512 透明 PNG、與 walk 幀同框 / 同腳底錨點 / 同人物比例（7–8 頭身）；flat cel-shade indie comic + 手繪粗黑墨線 + halftone 中間調 + Mignola 黑塊；服裝 / 橙色標記鎖定同 `Art Bible.md Ch3` 主角規格（deep teal 高領斜拉鍊外套、olive cargo、戰術靴、斜背包、額上護目鏡、右肩橙 patch + 雙手橙手套背板）。
- 禁：動態模糊 / 速度線 / anime 效果 / 漸層 / 整體垂直平移入幀。

##### 12-B 平台 / ledge / gap 組件 + 原型床

- **`ledge_area.gd`（Area2D）**：export `target_walk_y:float`（抓上後的 walk 基準）、`grab_x_min/max` 或用碰撞形狀 x 範圍；提供 `contains_x(x)->bool` 與 `get_target_y()->float` 給 player 抓取判定查詢。
- **`jump_gap.gd`**：標記一段「無走道」x 區間。無重力世界，gap 的語意＝**該段沒有 walk line**；玩家不可用走的進入（由場景以該段為 walk 邊界，或落入時掉到較低 walk line）。第一版最簡：gap 兩側為兩段 walk 區間，玩家必須跳過 gap 落到對側區間 x 範圍。
- **採集點抵達**：沿用既有 `EchoPoint`（`scripts/components/echo_point.gd`），擺在只有跳上 ledge 才到得了的位置——**不需改 EchoPoint**，純擺位。
- **追逐翻越**：以同一跳躍弧跨過低障礙；第一版不需獨立 vault 組件（障礙就是一段窄 gap 或矮 ledge）。
- **原型床 `jump_proto`**：一段最小序列＝平地 → gap（跳過）→ 較高 ledge（抓上）→ 採集點。獨立場景，便於 GUI 反覆驗手感；**不綁正式場景**，正式平台內容在 Phase 15/18 作者化。

##### 12-C 觸控

- `touch_controls.tscn`：`Control/Actions` 下新增 `BtnJump`。
- `touch_controls.gd`：`_ready` 內 `_bind_button(btn_jump, "jump")`（沿用既有 `_bind_button` 模式，emit input action，不直接呼叫 player）。
- **顯隱**：Jump 鈕併入既有「世界模式才顯動作鈕」規則（`UIMode.NONE` 顯示；面板 / 對話 / 暫停隱藏），與 `BtnE` 同群處理。
- **不讀固定節點路徑**：跳躍是 input action 驅動，TouchControls 只 emit action，player 自行 `Input.is_action_just_pressed("jump")`，無需經 GameUI 查詢（與 E / R / T 的情境查詢不同；跳躍無情境 gating）。

##### Phase 12 測試 / 回歸

- `test_runner.gd`：`InputMap.has_action("jump")`；player 有 `is_jumping()` 與跳躍參數；起跳→落回後 `get_save_x` 不變、`snap_to_walk_line` 仍回 walk line；`climb_mode` 行為不退化；`jump_proto` 載入無錯、`LedgeArea2D` 節點與 API 存在；TouchControls `BtnJump` 存在並綁 `jump`。
- 回歸護欄：Phase 1~11 headless 不退化；既有走動 / 攀爬 / 存讀檔座標不變。
- **手感（短跳 / 抓邊緣容錯 / 原型床能否通過 / 觸控好按）＝ GUI / 真機走查，headless 不負責。**

#### 戰鬥（Phase 13，實作契約）

> 驗收意圖 + 架構決策見 `遊戲規格書.md > Phase 13`；操作清單見 `測試指南.md > Phase 13`。
> **核心：棍子只 stun、kill＝繞後格式化（限機器）、人類不格式化、輸=岔線無 Game Over。** 複用 `player.gd`（含 12 跳躍）、`interact_primary` 長按、DialogueRunner、QuestManager。

##### 現況基準（開工前必讀）

- `player.gd`：walk-line 模型 + Phase 12 跳躍弧；互動走 `interact_primary`(E) / `secondary`(R) / `tertiary`(T)，long-press 機制需確認既有有無（若無，格式化長按在 player / 敵人組件內自行計時 `Input.is_action_pressed("interact_primary")`）。
- `UIMode`：`is_world_input_blocked()` 在對話 / 面板 / 暫停為真——戰鬥邏輯一律 gate 在世界模式。
- DialogueRunner / DialogueDB：人類嚇退 / 交易走既有對話樹 dispatch。
- QuestManager / GameState flags / `trace`：失敗岔線 set 旗標 + 可改 trace（Phase 11）。
- `project.godot [input]`：`attack` 已有雛形；格式化沿用 `interact_primary`。
- `jump_proto` 現況已有 Phase 13 雛形（player attack 動畫 + `Walker01` 巡邏 / 被擊倒 / 自修復）。正式實作時要把戰鬥內容收斂到獨立 `combat_proto`，`jump_proto` 回到跳躍 / 平台原型用途，不再承載 Phase 13 長期內容。

##### 新增 / 異動檔

```text
異動：
  project.godot                          # 13-A：確認 / 維護 input action「attack」（鍵盤 + 觸控 emit）
  scenes/actors/player/player.gd         # 13-A/B：揮擊觸發 + 格式化長按計時 hook（或獨立組件掛 player）
  scenes/actors/player/…（SpriteFrames） # 13-A：attack 揮擊動畫（可選 format 長按姿勢）
  scenes/ui/touch_controls.tscn/.gd      # 13-F：Actions 新增 BtnAttack（_bind_button "attack"）+ 顯隱規則
  tests/manual/test_runner.gd            # 13-A~13-E：action / 節點 / 狀態機 / API / 旗標邏輯 + 回歸護欄
新增：
  scripts/components/melee_stick.gd      # 13-A：棍子揮擊判定 → 命中給 stun（倒地自修復，不壞、無升級、無傷害）
  scripts/components/enemy_base.gd       # 13-A：敵人 AI 基底（本階段巡邏/受擊 stun + is_stunned；察覺/追擊後置）
  scripts/components/machine_enemy.gd    # 13-B：繼承基底，stun 中背後可 can_format
  scripts/components/human_enemy.gd      # 13-C：繼承基底，can_format 恆 false；解法走對話 / 出口
  scripts/components/format_reset.gd     # 13-B：can_format 區判定 + 長按計時 + defeated（或併入 machine_enemy）
  scripts/components/combat_loss.gd      # 13-D：通用「輸=岔線」hook（set 場景定義旗標 + 轉替代流程，無 Game Over）
  scenes/levels/combat_proto/…           # 13-E：隧道清潔機原型測試房（拋棄式，不接正式動線）
```

##### 13-A 棍子 stun + 敵人 AI 骨架

- **`melee_stick.gd`**：`attack` 觸發 → 前方判定區（Area2D / 形狀 query）命中 `enemy_base` → 呼叫 `enemy.apply_stun(duration)`。**棍無耐久、無等級、無傷害數值。**
  - **揮擊命中距離 ≠ 格式化距離**：命中判定用 melee reach（walker_01 原型現為 `attack_reach≈200px`），13-B 的 `can_format` 距離（原型 `160px`）是另一個獨立常數；兩者不共用、各自校正，勿混為單一全域值。
- **`enemy_base.gd`** 狀態機（本階段最小版）：`patrol`（右側區域巡邏 / 來回）→ 受擊 `apply_stun(t)` 進 `stunned`（表現為倒地自修復，`is_stunned()->bool`）→ 起身回 `patrol`。export：巡邏範圍、stun / 自修復時長、巡邏速度。**無 HP 欄位**——敵人不靠血量結束。
  - **`is_stunned()` 只在自修復窗為 true**：對齊 walker_01 現況分段 `FALL → PRONE（自修復）→ GETUP → RECOVER_IDLE`，`is_stunned()` 僅在 `PRONE`（自修復）回 true；倒地過場（FALL）/ 起身（GETUP）/ 起身後緩衝（RECOVER_IDLE）一律 false，`can_format` 也不在這些子狀態開啟。
- 察覺 / 追擊（`alert` / `chase`）本階段不做；正式遭遇需要時再加，不阻塞 Phase 13-A/B 原型。
- 命中只給 stun、不結束敵人；倒地自修復就是 stun 表現。結束機制在 13-B（機器格式化）/ 13-C（人類非 kill 解法）。

##### 13-B 繞後格式化（限機器）

- **`can_format` 判定**：敵人 `is_stunned()` 為 true + 玩家位於敵人**背向側**（玩家 x 在敵人面朝反方向）+ 原型 X 距離 `160px` 內。`machine_enemy.can_format()->bool`。正式場景的距離要依主角 / 敵人 scale 校正，不盲套 160。
- **長按計時**：在 `can_format` 為真時按住 `interact_primary` → `format_reset.gd` 累計 `format_t`，達 `format_duration = 2.0` → `enemy.defeated()` + set defeated 旗標（場景 / quest 定義的 key）。
  - **時序不變式**：`format_duration` 必須 **< 機器自修復時長**（walker_01 現 `prone_repair_time≈5.0s`），否則 stun 窗在按滿前就結束、永遠格式化不了。日後調自修復時長時須一併確認此不等式仍成立。
- **中止**：鬆手 / 離開 `can_format` 區 / 敵人起身 / 進 UI 或對話 → `format_t` 歸零、不 defeated、可重試（不消耗、不懲罰）。
- **defeated 表現**：機器留在場上，播放停機 / prone frame，不爆炸、不消失。
- 繞後靠 Phase 12 輕跳越過或繞過敵人；正面 `can_format` 永遠 false。

##### 13-C 人類敵人分支

- **`human_enemy.gd`**：繼承 `enemy_base`，`can_format()` 恆 `false`（永遠打不開格式化）。
- **解法**：① 對話嚇退 / 交易 → DialogueRunner 對話樹（`dialogue_id` dispatch，沿用 5-D 泛化）；② 環境阻隔 / 潛行撐過 → 抵達出口 Area2D set 旗標；③ 逃離。
- 本階段只做骨架與 headless 護欄，**不需要正式人類敵人圖**；正式人類保全留到 Phase 23 / 25 作者化。
- 無「kill」——人類不會死在玩家手上（敘事鐵則）。

##### 13-D 輸 = 岔故事線（無 Game Over）

- **`combat_loss.gd`**：通用失敗 hook。失敗條件（受擊次數上限 / 被逮 / 計時）達成 → 不死亡畫面、不重來；`set_flag(<場景定義失敗旗標>)` + 觸發替代流程（轉場 / 對話 / quest step），可改 `trace` / 染色。
- 失敗旗標與替代流程**由各遭遇場景定義**（export 失敗旗標 key + 替代流程 callback / signal）；本階段給機制 + 原型示範一條岔線。
- **原型最小版**：`combat_proto` 中隧道清潔機起身後碰到 / 抓到玩家一次 → `GameState.set_flag("combat_proto_failed", true)`，玩家送回同場景左側安全點（約 `x = 250`），顯示替代訊息；walker 只在右側區域巡邏，避免失敗後連續觸發。
- **不碰存讀檔懲罰**：失敗不刪檔、不強制讀檔；玩家從岔線續玩。

##### 13-E 原型測試床 `combat_proto`

- 隧道清潔機（machine_enemy）一台 + 一段可繞後的空間 + 出口 + 左側安全點。獨立場景，反覆驗手感用，**不接正式動線**（同 `jump_proto` / `test_runner` 的拋棄式定位）。正式 Act 2B 隧道遭遇在 Phase 18 用此組件作者化。
- 實作時從 `jump_proto` 現況收斂：保留 / 搬出 player attack、walker、倒地自修復、format / loss 原型到 `combat_proto`；避免 `jump_proto` 繼續混入戰鬥驗收責任。

##### 13-F 觸控

- `touch_controls.tscn`：`Actions` 下新增 `BtnAttack`；`touch_controls.gd` `_bind_button(btn_attack, "attack")`。格式化長按沿用 `BtnE`（`interact_primary`）。
- 顯隱併入「世界模式才顯動作鈕」規則；面板 / 對話 / 暫停隱藏。
- 沿用既有 `_bind_button` emit action 模式，不直接呼叫戰鬥邏輯、不讀固定節點路徑。

##### Phase 13 測試 / 回歸

- `test_runner.gd`：`InputMap.has_action("attack")`；`enemy_base` 狀態機 + `is_stunned()` + 倒地自修復 stun 計時；`machine_enemy.can_format()` 只在 stun 中背後距離內 true、正面 / 非 stun false；長按 2 秒填滿 → defeated 旗標且機器留場停機；中斷條件歸零；`human_enemy.can_format()` 恆 false；`combat_loss` 失敗 set `combat_proto_failed`、不重來、玩家送安全點；`combat_proto` 載入無錯、敵人 / 組件節點存在；TouchControls `BtnAttack` 存在綁 `attack`。
- 回歸護欄：Phase 1~12 headless 不退化；無敵人場景按 `attack` 無副作用；既有走動 / 跳躍 / 互動 / 對話 / 存讀檔不退化。
- **手感（stun 窗 / 繞後好不好繞 / 格式化長按時長 / 失敗岔線體感 / 觸控好按）＝ GUI / 真機走查，headless 不負責。**

#### 戰鬥場景互動契約（Phase 18，前瞻契約）

> 由 Phase 13 review（13-A/B/C 完成後）追記。解決 13-A 發現的 **`attack` 與 `interact_primary` 同綁 E（physical_keycode 69）** 衝突。Phase 13 原型（`combat_proto` / 13-D/E）刻意把敵人與互動物分開，靠現有「只擋格式化」即可；**本契約在 Phase 18 正式作者化 Act 2B 隧道遭遇前拍板生效**，屆時敵人與互動物會共存於同場景。

##### 現況基準（已存在，不重造）

- **戰鬥姿勢已是 per-scene**：`player.gd` 的 `walk_anim` / `idle_anim` 為場景 export override，戰鬥場景指向 `combat_walk` / `combat_idle`，`combat_walk_scale_mult` 處理 960px↔512px 尺寸差（見 `scenes/actors/player/player.gd:19-25`）。
- **`has_current_interactable()` 已存在**（`scenes/ui/game_ui.gd:243`），且 TouchControls 世界 E 已在用（`scenes/ui/touch_controls.gd:337`）——查詢端不需新造。
- **格式化抑制已存在**：`player._in_format_zone()` → `FormatReset.has_target()`（`scenes/actors/player/player.gd:289`）。
- **觸控無此衝突**：BtnE 與 BtnAttack（13-F）是兩顆獨立按鈕；E-vs-E 撞鍵**只發生在鍵盤**（dev / 驗收環境）。本契約讓鍵盤與觸控行為一致；若日後想徹底免風險，亦可把鍵盤 `attack` 改綁獨立鍵，**不影響本契約**。

##### 1. `combat_mode` gate（attack 限戰鬥場景）

- `player.gd` 新增 `@export var combat_mode := false`；**只有**戰鬥場景的 `Player` 設 `true`。
- 攻擊起手判斷加上 gate：`combat_mode and Input.is_action_just_pressed("attack") and not _e_reserved()`（取代現行無條件讀 `attack`，見 `player.gd:141`）。
- 非戰鬥場景（公寓 / 街道 / 店 / 地鐵站 / 聚落 hub）`combat_mode == false`：按 E **永不**揮棍，E 全歸互動——90% 的遊戲維持乾淨，零衝突。

##### 2. E 優先鏈（單一仲裁，集中於 player）

E 在戰鬥場景的優先順序，**集中由 player 仲裁**（消除「attack 在 player、interact 在 level/Main」雙 owner 的結構味道）：

```text
格式化長按（is_action_pressed，held）   ← FormatReset.has_target() 為真時最優先
  > 互動（is_action_just_pressed）        ← has_current_interactable() 為真時吃掉這次 E
  > 揮擊（is_action_just_pressed）        ← 前兩者皆否才揮棍
```

- 新增 `_e_reserved() -> bool`＝`_in_format_zone() or _has_current_interactable()`；攻擊起手 guard 用它。
- player 取得 GameUI 參照的方式比照 TouchControls 的 `set_game_ui()` 注入（不讀固定節點路徑、不違反 4-C/4-D contract）；`_has_current_interactable()` 轉呼 `game_ui.has_current_interactable()`。
- 互動本身的執行維持既有 level→Main→GameUI 路徑不變；本鏈只決定「這次 E 要不要被 attack 吃掉」。

##### 3. 條件式互動準則（作者規範，與 2 互補非冗餘）

- **2 防雙觸發**（按一下不會同時互動＋揮棍）；**3 防 2 的副作用**（避免「想揮棍時 E 卻被旁邊互動物吃掉」）。兩者互補。
- 戰鬥場景的互動物**優先擺在無敵人處**；或 gate 在「敵人擊敗 / 狀態解除後才可互動」——沿用既有條件式互動 pattern（公寓窗戶 `checked_alley` / 店主機 `mainframe_revealed`，即 `required_knowledge` / flag gating）。
- 落地後在戰鬥點周邊**不應**同時存在「想揮棍的距離」與「可互動的距離」重疊；重疊區即為作者疏漏。

##### Phase 18 測試 / 回歸（屆時補）

- `combat_mode == false` 場景按 `attack` 無揮棍、無副作用（沿用 13 既有「無敵人場景按 attack 無副作用」護欄擴成「非戰鬥場景」）。
- `combat_mode == true` 場景：E 優先鏈三段（格式化 > 互動 > 揮擊）各自命中；`_e_reserved()` 為真時 `is_attacking()` 不啟動。
- 既有走動 / 跳躍 / 互動 / 對話 / 存讀檔 / 格式化不退化；觸控 BtnE / BtnAttack 維持各自綁定。
- **手感（戰鬥場景互動會不會誤吃 / 揮棍會不會誤觸互動）＝ GUI / 真機走查。**

### Act 1 主線鉤子（Phase 14，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 14`；操作清單見 `測試指南.md > Phase 14`；敘事 / 因果事實來源見 `subdocs/主線/雨還沒停v2.3.md` §3（罪責回扣四段式）/ §4.1（林霏）/ §4.4（鹿）/ §4.3（晚）。
> **核心：在既有平民區（公寓 / 街道 / 便利商店）埋三條主線引信，不新建場景、不新增美術 / 立繪 / 音圖。** 全部產出＝對話樹資料 + `story_flags` 旗標 + 一個被動碰撞觸發組件。依賴 Phase 11（記憶碎片旗標骨架 + Trust/Trace；已本機 headless PASS 2026-06-19）。

#### 拍板決策（2026-06-20，原「開工定點」全部定案）

1. **觸發點擺位**：`memory_fragment_area` 擺在街道 `apartment_entrance`，**x≈1500**（出公寓落點 `from_apartment` x=730 與便利商店 `store_front` x≈2618 之間的空走道）。玩家出公寓固定落 x=730、進店必往右走，**必經 x≈1500 → 必撞**。
2. **統一 gate**：14-B 兩條鉤的條件 gate 一律 `get_flag("mem_frag_linfei_1")`（撞過林霏才鬆動）；14-C gate＝`get_flag("mem_frag_linfei_1") AND not ada_misrecognized`，不再要求 `talked_store_robot`。
3. **14-C 可達性（原 babble 路由死角）解法＝首談前置鉤 + 必撞擺位**：因 (1) 保證 `mem_frag_linfei_1` 在任何店控互動前已 true，玩家第一次和未修好店控對話時，先播阿達誤認鉤並設 `ada_misrecognized` + `talked_store_robot`，再落回既有 babble。修店任務完成（`vendor_bot_repaired`）後 start 改路由 `repaired_router` 不再進 babble，但誤認鉤此前已觸發過——**死角消失，不需在 repaired 招呼補鉤**。
4. **台詞 final**：四段定稿見下方各子階段「台詞（final）」區塊，不再改；事實來源 v2.3 §3/§4.1/§4.3/§4.4。守冷度、點到為止、**不**解釋林霏 / 阿達是誰（揭露留 Phase 19/22）。

#### 現況基準（開工前必讀）

- `GameState`：`story_flags` store（bool/int）已存在；`set_flag(key, value)` / `get_flag(key, default)`；已納入 `to_save_dict` / `load_save_dict`（缺鍵向後相容）/ `reset_for_new_game`。新旗標只加 key、不改 store 結構。
- DialogueRunner / DialogueDB：`dialogue_id` dispatch（5-D 泛化）+ 條件路由（`get_flag` / quest / inventory / `quest_flag` / `echo_*`）；effect op：`set_flag` / `add_int` / `start_quest` / `add_credits` / `add_trace` / `add_item` / `add_note` / `travel` / `install_module` / `sell_echo`。對話檔：`data/dialogue/wan.gd`（晚）、`data/dialogue/lu_qichen.gd`（鹿）、`data/dialogue/store_robot.gd`（店控機器人，Phase 8 既有 start 依狀態路由）。
- EchoDB / EchoPoint：殘響採集需裝備 `gleaner_gloves` + 靜止 ≥1s + E；媒體層（照片 / 錄音 / 歌）集滿才出。**14-A 的「一震」不是採集**——是被動碰撞、無媒體、無需手套。林霏整條殘響的採集 + 媒體在 Act 3（Phase 21 核心房間，原 Phase 22 已併入），女聲歌（Phase 21，外部委製 blocker）才掛得上。
- 平民區場景：`apartment_room` / `apartment_entrance`（街道）/ `convenience_store` 皆已存在；本 Phase 只擺新觸發 / 改對話資料，不新建場景。

#### 新增 / 異動檔

```text
異動：
  scripts/autoload/game_state.gd     # 14-A~C：新旗標納入 to_save_dict / load_save_dict（缺鍵→預設）/ reset_for_new_game；STORY_MESSAGES 加 key "mem_frag_linfei_1"（一震短訊）
  data/dialogue/lu_qichen.gd         # 14-B：鹿「從線上面下來的」對話鉤（條件 gate）
  data/dialogue/wan.gd               # 14-B：晚瞥見主角失神的對話鉤（條件 gate）
  data/dialogue/store_robot.gd       # 14-C：店控機器人誤認①分支（接 Phase 8 既有樹）
  scenes/levels/apartment_entrance.tscn  # 14-A：擺一個記憶碎片觸發 Area2D（定案：x≈1500，door↔store 之間）
  tests/manual/test_runner.gd        # 14-A~D：觸發 / 旗標 / 對話分支條件 + 回歸護欄
新增：
  scripts/components/memory_fragment_area.gd  # 14-A：被動一次性碰撞觸發（Area2D）
```

#### 旗標（canonical，先定以防各自發明 key）

| key | 型別 | 語意 | 設於 | 後續用途 |
|---|---|---|---|---|
| `mem_frag_linfei_1` | bool | 林霏殘響碎片①「一震」已觸發（記憶碎片漸進骨架第 1 顆，沿用「記憶碎片 / 旗標」段）| 14-A 碰撞 | gate 14-B / 14-C 鉤子；→ Phase 21 林霏核心殘響 |
| `lu_hinted_topside` | bool | 鹿說過「你是從線上面下來的」對話鉤已出現 | 14-B 鹿對話 effect | 鹿線 / 主角來歷鋪墊 |
| `wan_noticed_daze` | bool | 晚瞥見主角失神的鉤已出現 | 14-B 晚對話 effect | 晚線（§4.3 鏡子）鋪墊 |
| `ada_misrecognized` | bool | 店控機器人誤認①（阿達四段式揭露第①段，v2.3 §3）| 14-C 店控對話 effect | → Phase 19 阿達②③ |

> 記憶碎片漸進骨架見下「### 記憶碎片 / 旗標」：撞林霏①(14) → 往上通勤(17) → 抹過阿達(19) → 黑戶藏身(22) → 真相(26)。本 Phase 落地第 1 顆。

#### 14-A 林霏殘響碎片①「莫名一震」（碰撞觸發）

- **`memory_fragment_area.gd`（Area2D）**：export `fragment_flag: String`（如 `mem_frag_linfei_1`）、`message_id: String`（key into `GameState.STORY_MESSAGES`，組件以 `show_message(GameState.STORY_MESSAGES[message_id])` 走既有 GameUI / MessageBox）。`body_entered` → 若 `not GameState.get_flag(fragment_flag, false)`：`show_message(...)` + `GameState.set_flag(fragment_flag, true)`；**一次性**（旗標即去重，已觸發不再播）。被動、無需 E、無需手套；UIMode 非 NONE 時不重複觸發（旗標已擋）。
- **擺位（定案）**：街道 `apartment_entrance` x≈1500（door x=730 ↔ store x≈2618 之間，必撞，見上「拍板決策 1」）。「一震」＝主角無意識帶著林霏殘響、Act 1 撞到她（v2.3 §4.1）；文字守冷度，只給生理感受 + 一句不明所以，**不**解釋她是誰。
- **台詞（final）** — `STORY_MESSAGES["mem_frag_linfei_1"]`：
  > （你經過時，胸口無端一緊，像有人在很遠的地方喊了你一聲——又像那一聲其實是衝著別人去的。）
  > 雨水順著脖子灌進來。你停了半秒，說不出自己在等什麼。
  > 那陣震過去了。你想不起來，剛才差點想起的是什麼。
- **不做**：不採集、不加 EchoPoint、不加媒體、不掛女聲歌（全在 Phase 21，原 Phase 22 已併入）。可選 forward：在 EchoDB 預登一條 `echo_linfei` id + `????` 投影（沿用 9-A pattern），媒體 / 採集留空；**非 14-A 必需**，視 Phase 21 開工再補。
- **存讀檔**：`fragment_flag` 走 `story_flags` 既有序列化；round-trip 保留（14-D 驗）。

#### 14-B 鹿「從線上面下來的」+ 晚瞥見失神（對話鉤）

- **鹿（`lu_qichen.gd`）**：在 `hub` 進入前插一條一次性鉤——鹿不經意點出「你是從線上面下來的」（§4.4 平行對照 / 主角來歷）。**條件 gate（定案）**：`get_flag("mem_frag_linfei_1")` AND `not lu_hinted_topside`；命中後播鉤、effect `set_flag("lu_hinted_topside", true)`，再落 `hub`。一次性、重講不重設。
  - **台詞（final）**：
    > 鹿其琛：（他端詳你的時間比平常久了些，指節在杖頭上敲了兩下。）
    > 你身上有股味道。不是雨，也不是這條街的——是上面的。
    > 你是從線上面下來的吧？（他笑了笑，沒追問。）
    > 沒事。下得來的人，各有各的理由。我只是隨口一說。
- **晚（`wan.gd`）**：加一條一次性鉤——晚瞥見主角忽然失神 / 走神（§4.3 晚比她說的更知道你是誰）。**條件 gate（定案）**：`get_flag("mem_frag_linfei_1")` AND `not wan_noticed_daze`；effect `set_flag("wan_noticed_daze", true)`。沿用既有 `affinity_wan` 路由，不破壞 5-D / 7-B 既有分支（met_wan / retalk / retalk_close / intel）。
  - **台詞（final）**：
    > 晚：（她話說到一半停住，盯著你看。）
    > 喂。你剛剛跑哪去了？人在這，眼神不在。
    > ……我見過那種眼神。在那些快被刪乾淨的人臉上。
    > （她別過頭，聲音輕下來。）算了。你要是想不起來，我也不替你想。
- **純資料**：兩條皆對話樹資料 + flag，沿用既有立繪 / DialogueRunner condition / effect，**無新美術、無新 effect op**。

#### 14-C 阿達①店控機器人誤認（接 Phase 8 店控樹）

- **`store_robot.gd`**：在 babble 路由前（未修好、quest 未 active 狀態）插一條首談前置誤認鉤——店控機器人把主角誤認成「阿達」（v2.3 §3 四段式①）。**條件 gate（定案）**：`get_flag("mem_frag_linfei_1")` AND `not ada_misrecognized`；命中 effect `set_flag("ada_misrecognized", true)` + `set_flag("talked_store_robot", true)`，播完後 goto 既有 `babble_intro` 或等價 babble 續段。這讓首談即可觸發，不依賴「已談過店控」這個尚未成立的旗標。**可達性**：必撞擺位保證撞林霏先於任何店控互動，首談前置鉤必能命中（見上「拍板決策 3」）。
- **克制**：阿達本人**不登場**（只是機器人認錯名字 + 一句「我見過你」埋罪責回扣）；**不**提雨衣 / 維修棍（那是 Phase 19 ②的料）、不解釋阿達是誰。不動 Phase 8 既有 repaired / quest active / `discovered_vendor_error` 路由與買賣交棒。
  - **台詞（final）**：
    > 店控機器人：（你靠近時，它的鏡頭猛地對焦在你臉上，散熱風扇拔高了一階。）
    > ……阿達？是你嗎，阿達。你怎麼會在這——
    > （機械臂抬到一半，僵住，又慢慢放下。）
    > 不對。你不是阿達。可是我見過你。我很確定我見過你。
    > 在哪……我想不起來在哪。
    > （螢幕臉暗了一瞬，那行小字又跳出來：「本日公休」。）
- **純資料**：對話樹 + flag，無新節點美術。

#### 14-D 回歸 + 存讀檔 + GUI 走查

- `test_runner.gd`：`memory_fragment_area.gd` 載入無錯、`body_entered` 觸發路徑 set 旗標且去重；四旗標 round-trip（save → load 保留、缺鍵→預設、reset 歸零）；對話鉤 condition 正確（未撞林霏前不出、撞後出、重講不重設旗標）；店控誤認分支條件正確且不破壞 Phase 8 既有路由。
- 回歸護欄：**Phase 1~13 headless PASS 不退化**；既有 `talk_wan` / 鹿鑑定 / 店控 repaired / 買賣 / 存讀檔不退化。
- GUI 走查（人工）：走過觸發點看「一震」短訊只播一次；與鹿 / 晚 / 店控機器人對話看三條鉤在條件成立後出現、旗標就位；存讀檔後不重播 / 不重觸發。

### Act 2 場景骨架（Phase 15，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 15`；操作清單見 `測試指南.md > Phase 15`；場景設計（layout / flavor final 文案 / 掛點）見 `subdocs/地點/地鐵站.md` / `subdocs/地點/地下道聚落.md`；敘事 / 因果事實來源見 `subdocs/主線/雨還沒停v2.3.md` §4.2（三張臉 / 黑戶網）/ §7.1（Act 2 拆段）。
> **核心：新建 Act 2 兩個真場景（地鐵站 + 地下道聚落），交付真美術 / 真 BGM / final flavor。NPC（16）/ 殘響（17）/ 戰鬥（18）內容後置，但以文件登記的真掛點預留，本 Phase 不放暫代資源。** 依賴 Phase 14（Act 1 動線 + travel 解鎖 gate）、Phase 11（Trust/Trace + `can_save_here`）。

#### 場景切分：每個地點切兩室＝4 個 scene_id（2026-06-21 user 拍板）

> 原規劃「每場景 1 個 scene_id、2 圖橫向拼接成單一大場景」改為**每個地點切成兩個獨立 scene_id 房間**，由場景內 `InteractableArea` 互轉。理由：單室相機橫移過長、拼接圖過寬（地鐵 5760 / 聚落 6400）不利相機 clamp 與後續 16~20 往兩側補內容。切分後共 **4 個 scene_id**：

| 地點 | 室 / scene_id | 對應美術 | `MAP_WIDTH` | 室間互轉 |
|---|---|---|---|---|
| 地鐵站 | `subway_station`（大廳/閘口 concourse）| `subway-station-concourse.jpeg` | 1376 | `gate_to_platform` → 月台 |
| 地鐵站 | `subway_station_platform`（月台）| `subway-station-platform.png` | 4800 | `exit_to_street` → 大廳；`stairs_to_settlement` → 聚落 |
| 地下道聚落 | `underground_settlement`（左 panel 帳篷群）| `underground-settlement-left.png` | 4352 | `exit_to_subway` → 月台；`go_right` → 右 panel |
| 地下道聚落 | `underground_settlement_right`（右 panel 淨水/隧道口）| `underground-settlement-right.png` | 4352 | `go_left` → 左 panel |

> 全鏈動線：街道 → `subway_station`（大廳）→ `subway_station_platform`（月台）→ `underground_settlement`（左）→ `underground_settlement_right`（右）；反向同。4 個 scene_id 皆 `can_save_here = true`；地鐵兩室共用 `music_id="subway_station"`、聚落兩室共用 `music_id="underground_settlement"`。下方各子階段沿用此切分。

#### 不暫代原則（本 Phase 硬規則，呼應 4-G / 11-D 教訓）

- **真 BGM**：地鐵站、聚落各一軌**專屬**（委製 / 生成），**不**借 `assets/bgm/` 既有 6 軌頂著。BGM 未就位前該子階段不算完成（場景可先載入測試，但驗收 gate 卡 BGM final）。
- **真美術**：兩場景各一套 Art Bible 合規背景 + prop（g2d / agent-sprite-forge 生），**不**用色塊 / placeholder 幾何頂著當「之後再換」。
- **final flavor**：互動物 examine 是定稿環境敘事文字（事實來源 v2.3 §4.2），**不**寫「（待填）」/「佔位訊息」。
- **後置 ≠ 暫代**：NPC / 殘響 / 戰鬥是「尚未抵達的真內容」，在場景中表現為**真實的空**（空帳篷、有人剛離開的痕跡、鎖著的維修口），掛點登記在 subdocs 開放時機表，**不放**假 NPC / 假物件 stand-in。

#### 現況基準（開工前必讀）

- SceneRegistry：`scenes/main/main.gd` 的 `const SCENES` dict（`scene_id → {path, default_entry_point_id, entry_points, music_id}`）；`transition_to(scene_id, entry_point_id)` 雙向轉場；存檔時 `main.gd` 以 `SCENES.has(scene_id)` 驗證，新場景只要進 `SCENES` 即納入存讀檔，無另一份白名單。
- BGM：場景自管，於該場景 `.gd` 的 `_ready` 呼叫 `main.play_bgm("res://assets/bgm/<檔>")`（pattern 見 `collector_shop.gd`）；`music_id` 為登記用標籤。
- Travel 選單：街道 `apartment_entrance` 最右端 `leave_block` 互動 → 目的地選單（對話樹，pattern 見 `data/dialogue/travel_street_east.gd`，Phase 9 已加 `collector_shop`）；effect `travel` 走 `transition_to`。地鐵站作為新目的地加入同一選單。
- 存檔護欄：`SaveSystem.can_save_here`；聚落 / 地鐵站皆為安全點（`true`）；對照外牆 / 戰鬥類禁存（7-F pattern）。
- 可複用元件：`InteractableArea`（門 / flavor examine）、`EchoPoint`（17 採集點，本 Phase 只在 subdocs 登記掛點）、`glow_layer.gd` / 路燈 / 反射（10-C 氛圍，視需要掛）。

#### 新增 / 異動檔

```text
新增（每地點切兩室＝各 2 scene_id，見「場景切分」）：
  scenes/levels/subway_station/subway_station.tscn + .gd                        # 15-A：地鐵站大廳/閘口（concourse）
  scenes/levels/subway_station/subway_station_platform.tscn + .gd               # 15-A：地鐵站月台（platform）
  scenes/levels/underground_settlement/underground_settlement.tscn + .gd        # 15-B：地下道聚落左 panel（帳篷群）
  scenes/levels/underground_settlement/underground_settlement_right.tscn + .gd  # 15-B：地下道聚落右 panel（淨水/隧道口）
  subdocs/地點/地鐵站.md                                            # 場景設計檔（layout / final flavor / 掛點）
  subdocs/地點/地下道聚落.md                                        # 場景設計檔
異動：
  scenes/main/main.gd                  # 15-A/B：SCENES 註冊 4 個 scene_id + entry points + music_id；apartment_entrance entry_points 加 from_subway（回街道落點）
  scripts/autoload/save_system.gd      # 15-A/B：4 個新 scene_id 的存檔顯示名（get_scene_display_name）
  data/dialogue/travel_street_east.gd  # 15-A：街道 travel 選單加「地鐵站」目的地（解鎖 gate＝lu_hinted_topside）
  tests/manual/test_runner.gd          # 15-A~D：載入 / split 全鏈雙向轉場 / round-trip / 旗標 / flavor / 回歸護欄
  # 註：reached_settlement 走既有 set_flag → story_flags store，已隨 to_save_dict 持久化（缺鍵→false / reset 歸零），故 game_state.gd 不需改。
已交付資源（真，非暫代；2026-06-21 落地，prompt 事實來源在各 asset 資料夾 .txt，不複製進規格）：
  # 地鐵站背景：2 圖橫向拼接，總寬 5760 =（大廳 1920）+（月台 3840）
  assets/generated/maps/subway_station/subway-station-concourse.jpeg  # 大廳/閘口層 1920×720：左=手扶梯往上回街道、右=票閘（複用 anchor-03 風格）
  assets/generated/maps/subway_station/subway-station-platform.png    # 月台 3840×720：停運班次螢幕(=P17 錨點)居中 + 長椅 + 遺忘行李箱 + 右下角下行口
  # 聚落背景：2 圖橫向拼接，總寬 6400 =（左 3200）+（右 3200）
  assets/generated/maps/underground_settlement/underground-settlement-left.png   # 左 panel 3200×720：左緣上行電扶梯底端 + 帳篷群（空帳篷有生活痕跡，無 NPC）
  assets/generated/maps/underground_settlement/underground-settlement-right.png  # 右 panel 3200×720：淨水站發電 + 中央空地(留空) + 收音機 + 右緣封閉深隧道口(=P18 戰鬥入口)
  # 專屬 BGM（不借既有 6 軌）
  assets/bgm/The Last Platform.mp3            # 地鐵站 BGM（music_id="subway_station"，空曠迴響 / 遠列車 / 滴水）
  assets/bgm/The Deleted Still Breathe.mp3    # 聚落 BGM（music_id="underground_settlement"，底層嗡鳴 / 舊機低頻 / 微弱人聲底噪）
  # 註：地鐵 map 資料夾已統一為 subway_station（與 scene_id / scene 資料夾一致）。
```

#### 旗標（canonical，先定以防各自發明 key）

| key | 型別 | 語意 | 設於 | 後續用途 |
|---|---|---|---|---|
| `reached_settlement` | bool | 首次抵達地下道聚落（Act 2 hub 抵達）| 15-B 進入 `underground_settlement` | Phase 16 NPC 首見 / 重講路由；Act 2 進度 gate |

> 地鐵 travel 目的地的**解鎖 gate** 用既有 Act 1 旗標（建議 `lu_hinted_topside`＝鹿「你是從線上面下來的」，正合下沉母題；開工定點，可改 Phase 14 完成訊號）。本 Phase **不**新增 NPC / 殘響 / 戰鬥旗標——那些在 16~18 各自落地。

#### 15-A 地鐵站 `subway_station`（真美術 + 真 BGM + 街道接入）

- **場景**：純 2D 側捲；廢棄 / 半廢地鐵站，由 2 圖橫向拼接（總寬 5760）＝**大廳/閘口層**（concourse 1920，左手扶梯往上回街道、右票閘）+ **月台**（platform 3840，停運班次螢幕／長椅／遺忘行李箱／右下角下行口）。停運通勤螢幕（可複用 `billboard_screen` CRT 母題，置於月台圖）、半廢未斷電（殘留緊急燈閃爍）、通向更深的下行口（停運手扶梯，月台右下角）。母題＝**下沉門檻**：上方通勤世界的遺跡，主角自此下到 Act 2。
- **SceneRegistry（切兩室）**：
  - `SCENES["subway_station"] = {path, default_entry_point_id="from_street", entry_points=["from_street","from_platform"], music_id="subway_station"}`（大廳/閘口）。
  - `SCENES["subway_station_platform"] = {path, default_entry_point_id="from_concourse", entry_points=["from_concourse","from_settlement"], music_id="subway_station"}`（月台）。
  - 兩室 `_ready` 皆 `main.play_bgm("res://assets/bgm/The Last Platform.mp3")`（共用 `music_id`，切室不換曲）。
- **街道接入**：`travel_street_east.gd` 目的地選單加「地鐵站」項，gate＝`lu_hinted_topside`（Phase 14-B 鹿鉤）；effect `travel` → `subway_station:from_street`（**落大廳 concourse 左側手扶梯旁，非月台**——主角從街道下手扶梯進大廳，再過閘往右到月台）。回街道：大廳 `exit_to_street` → `apartment_entrance:from_subway`（已在 `apartment_entrance` entry_points 補 `from_subway` 落點，不播開場獨白）。
- **大廳↔月台**：大廳 `gate_to_platform` → `subway_station_platform:from_concourse`；月台 `exit_to_street` → `subway_station:from_platform`（回大廳）。
- **下行口**（月台最右 `stairs_to_settlement`）：→ `underground_settlement:from_subway`。此下行手扶梯＝聚落左緣上行手扶梯的**同一座**（月台看上端往下、聚落看底端往上）。月台 `from_settlement` 回程落點＝月台最右下行口旁。
- **存檔**：`can_save_here = true`。

#### 15-B 地下道聚落 `underground_settlement`（真美術 + 真 BGM + 抵達旗標）

- **場景**：純 2D 側捲；排水 / 維修隧道改造的街友聚落，由 2 圖橫向拼接（總寬 6400）＝**左 panel**（3200，左緣上行電扶梯底端 + 帳篷群／暖）+ **右 panel**（3200，淨水站發電 / 中央空地 / 收音機 / 右緣封閉深隧道口／冷）。舊機改裝的淨水站 / 發電（**會運轉**，聚落有自己的拼裝電源）、暖色燈泡 vs 冷管線對比、林霏黑戶網的物理痕跡（假身份工作站位置留白給 16）。母題＝**暖人味 vs 冷基建**，被系統刪除者的家。是 Act 2 的 hub（16~20 都在此延展）。
- **SceneRegistry（切兩室）**：
  - `SCENES["underground_settlement"] = {path, default_entry_point_id="from_subway", entry_points=["from_subway","from_right"], music_id="underground_settlement"}`（左 panel）。
  - `SCENES["underground_settlement_right"] = {path, default_entry_point_id="from_left", entry_points=["from_left"], music_id="underground_settlement"}`（右 panel）。
  - 兩室 `_ready` 皆 `main.play_bgm("res://assets/bgm/The Deleted Still Breathe.mp3")`。
- **轉場**：上行口（左 panel 左緣電扶梯底端，`from_subway` 落點）`exit_to_subway` → `subway_station_platform:from_settlement`（落地鐵月台最右下行口旁）。此上行手扶梯＝地鐵月台右下行口的**同一座**。左↔右：左 panel `go_right` → `underground_settlement_right:from_left`；右 panel `go_left` → `underground_settlement:from_right`。深隧道口（右 panel 右緣 `deep_tunnel`）Phase 15 封閉 flavor，Phase 18 才開戰鬥。
- **抵達旗標**：左 panel `_ready` `GameState.set_flag("reached_settlement", true)`（idempotent、跨存檔保留；首抵即設）。
- **存檔**：`can_save_here = true`。

#### 15-C 兩場景 flavor 互動物（final 文案）+ 真掛點預留

- **flavor 互動物**：以 `InteractableArea` examine 給**定稿**環境敘事（非佔位）。地鐵站：停運通勤螢幕（「往上的線停了」呼應 Phase 17 碎片）、滲漏排水、遺留行李箱。聚落：空帳篷（有人剛離開的痕跡）、伍姐維修的淨水站、舊機發電、收音機底噪源。文案事實來源 v2.3 §4.2，定稿寫入 subdocs。
- **真掛點預留（登記不放假物件）**：在 subdocs 開放時機表登記——NPC 站位（小岑 / 伍姐 / 七號 → Phase 16）、聚落殘響 `EchoPoint` 採集點座標（→ Phase 17）、隧道清潔機戰鬥入口（→ Phase 18 接 `combat_proto`）。**本 Phase 不擺 NPC / 不放 EchoPoint / 不放敵人**；以真實的「空」呈現，後續 phase 往登記點補真內容。

#### 15-D 回歸 + 存讀檔 + GUI 走查

- `test_runner.gd`：兩場景載入無 error；全鏈雙向轉場（街道→地鐵→聚落→地鐵→街道）落點正確；`reached_settlement` round-trip（save→load 保留、缺鍵→false、reset 歸零）；travel 目的地 gate 正確（Act 1 旗標未成立不出 / 成立後出）；`can_save_here` 在兩場景皆 true；flavor examine 就位。
- 回歸護欄：**Phase 1~14 headless PASS 不退化**；既有 travel（collector_shop）/ 街道轉場 / 存讀檔不退化。
- GUI 走查（人工）：地鐵站 / 聚落 BGM 為專屬曲（非借軌）；美術 final（非 placeholder）；flavor 文案定稿；全鏈轉場順、無孤兒落點；存讀檔後位置 / 旗標保持。

### 三張臉 NPC 落地（Phase 16，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 16`；操作清單見 `測試指南.md > Phase 16`；**人設 / 外觀 / 對話定稿草案 / 旗標事實來源見 `subdocs/人/三張臉.md`**；NPC 站位（真掛點）見 `subdocs/地點/地下道聚落.md`；敘事 / 因果事實來源見 `subdocs/主線/雨還沒停v2.3.md` §4.2（三張臉 / 黑戶網）/ §4.1.1（林霏↔七號交易，本 Phase 不揭露）。
> **核心：把黑戶網前景三人（小岑 / 伍姐 / 七號）落地在 Phase 15 已建的地下道聚落兩室，沿用 Phase 5 對話系統（無新系統）。產出＝3 個對話樹 + DialogueDB 註冊 + 3 個 NPC Area2D + 新旗標納入既有存讀檔。** 依賴 Phase 15（聚落兩室 + `reached_settlement`）、Phase 5（對話樹 schema / DialogueRunner / DialoguePanel / dispatch 泛化）、Phase 11-B（`affinity_*` trust adapter）。

#### 三前提（先讀，避免敘事破功；硬規則）

1. **主角此時失憶**，到 Phase 16 為止只撞過林霏殘響碎片①（`mem_frag_linfei_1`，純「莫名一震」），**完全不知道「林霏」這個名字**。揭露留 Phase 19 / 22。
2. **任何一條對話都不准出現「林霏」三字**。伍姐只讓玩家「知道聚落有過一個建立者、女性、已不在」（`knows_settlement_had_maker`），名字與真相全部後置。
3. **七號只鋪墊「我有一個名字還掛在上面」，不明說妹妹**（Act 2A 鉤子，payoff 在 Phase 20 / 24 七號線）。違反任一前提＝敘事破功，視為驗收不通過。

#### 現況基準（開工前必讀）

- 對話系統（Phase 5，可直接複用，**不新增系統**）：對話樹 schema 見 `data/dialogue/wan.gd`（入口恆 `start`，`goto` 條件陣列做首見 / 重講路由；node 有 `speaker` / `text` / `choices` / `effect` / `goto`；terminal＝無 `choices` 且無 `goto`）；effect op `set_flag` / `add_int` / `add_credits` 等已存在於 `DialogueRunner`；DialogueDB 在 `data/dialogue/dialogue_db.gd` 以 `preload` + `TREES` dict 註冊 `dialogue_id → TREE`。
- NPC 落地 pattern（Phase 5-D，照搬）：場景 `$Interactables` 下放 `Area2D + interactable_area.gd`，設 `interaction_id` / `prompt_text="交談"` / `dialogue_id`；場景 `.gd` 的 dispatch **已泛化為先看 `dialogue_id`**（見 5-D），新增 NPC **不需改 dispatch 邏輯**，只要掛 Area2D + 設 `dialogue_id`。
- 旗標存讀檔：`set_flag` / `add_int` 寫入 `GameState.story_flags`，已隨 `to_save_dict` / `load_save_dict` 持久化（缺鍵→預設）/ `reset_for_new_game` 歸零。**本 Phase 不需改 `game_state.gd`**。
- Trust adapter（11-B）：`get_trust("cen"/"wu"/"seven")` ＝ `get_flag("affinity_"+target, 0)`；三個 `affinity_cen` / `affinity_wu` / `affinity_seven` 已登錄於本檔 trust-target 清單（target id `cen` / `wu` / `seven`），暖向選項用 `add_int` 加。
- NPC 站位（聚落兩室）：小岑＝左室 `underground_settlement` 帳篷群；伍姐＋七號＝右室 `underground_settlement_right`（伍姐蹲在淨水站 / 發電舊機前、七號靠右緣深隧道口牆）。對齊 `subdocs/地點/地下道聚落.md` 真掛點表。

#### 新增 / 異動檔

```text
新增：
  data/dialogue/cen.gd      # 16-A：小岑對話樹（dialogue_id "cen"）
  data/dialogue/wu.gd       # 16-B：伍姐對話樹（dialogue_id "wu"）
  data/dialogue/seven.gd    # 16-C：七號對話樹（dialogue_id "seven"）
異動：
  data/dialogue/dialogue_db.gd                                   # 16-A~C：preload 三檔 + TREES 加 "cen"/"wu"/"seven"
  scenes/ui/dialogue_panel.gd                                    # 16-D：start_dialogue 立繪掛接 if/elif 加 cen/wu/seven 三支（指向各自 portrait.png）
  scenes/levels/underground_settlement/underground_settlement.tscn        # 16-A：$Interactables 下加 NpcCen（帳篷群）
  scenes/levels/underground_settlement/underground_settlement_right.tscn  # 16-B/C：加 NpcWu（淨水站前）+ NpcSeven（深隧道口）
  tests/manual/test_runner.gd                                    # 16-D：對話樹載入 / 路由 / 旗標 / round-trip 護欄
  # 美術立繪 / 角色圖（6 張，§5 交付清單）已交付且已在 repo（assets/generated/sprites/cen|wu|seven，各含 idle 角色圖 + dialogue_portrait 立繪）。
  # 註：story_flags 走既有 set_flag store，game_state.gd 不需改；affinity_* target 已登錄，trust adapter 不需改。
```

#### 旗標（canonical，先定以防各自發明 key；事實來源 `subdocs/人/三張臉.md` §4）

| key | 型別 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `met_cen` | bool | 小岑首見→重講路由 | 16-A 小岑自我介紹 / 被抓包冷收尾 | — |
| `met_wu` | bool | 伍姐首見→重講路由 | 16-B 伍姐維修人 / 建立者節點 | — |
| `met_seven` | bool | 七號首見→重講路由 | 16-C 七號鋪墊鉤 | — |
| `affinity_cen` / `affinity_wu` / `affinity_seven` | int | 好感（trust adapter，已登錄 target `cen`/`wu`/`seven`）| 暖向選項 `add_int` | Phase 24 等 |
| `knows_settlement_had_maker` | bool | 知道聚落有過一個女性建立者 / 已不在，**不知名字、不知是林霏** | 16-B 伍姐建立者節點 | Phase 19 / 22 揭露時連回林霏 |
| `seven_hinted_name_topside` | bool | 七號鋪墊「有名字掛在上面」（不明說妹妹）| 16-C 七號鋪墊鉤 | Act 2A → Phase 20 / 24 七號線 gate |

> 全部 bool / int，納入既有 story_flags 存讀檔（缺鍵→預設）/ reset。沿用既有機制，本 Phase 不新增系統。

#### 16-A 小岑 `cen`（左室帳篷群）

- **NPC 節點**（`underground_settlement.tscn` `$Interactables` 下）：`NpcCen (Area2D, interactable_area.gd)` + `CollisionShape2D` + `Sprite`（idle 靠帳篷、placeholder 至立繪到位）；`interaction_id="talk_cen"` / `prompt_text="交談"` / `dialogue_id="cen"`。立於左室帳篷群（暖燈泡帳篷邊），座標實作時對齊背景。
- **對話樹**（`data/dialogue/cen.gd`，schema 照 `wan.gd`；**台詞定稿草案見 `三張臉.md` §3.1，實作可微調標點 / 斷行，語氣 / 分支 / 旗標以該檔為準**）：
  - `start`：`goto` 條件陣列＝`met_cen==true → retalk`，否則 `→ first_meet`。
  - `first_meet`（斜睨打量）→ choices：「我不是來搶地盤的。」/「你一個人住這？」（短回後）皆 `→ intro`（自我介紹節點）；「（你感覺口袋被碰了一下）手放規矩點。」`→ pickpocket_caught`。
  - `pickpocket_caught`（**被抓包冷收尾**）：手一縮、不洗白（「手快一點才不會餓死」）；effect `set_flag met_cen=true`；terminal（無 choices 無 goto，冷收）。**不引入偷竊 / 失竊系統——純對話分支**。
  - `intro`（自我介紹）：「叫我小岑……我這條嗓子是有人幫我弄成『聽起來像活人』」（⚠️ **不具名**那人）；effect `set_flag met_cen=true` + `add_int affinity_cen +1`；`goto end_warm`。
  - `end_warm`（暖收尾：分一口吃的不介意）terminal。
  - `retalk`：「又是你……沒偷你東西，真的。」+ choices「來看看你。」(`add_int affinity_cen +1` → `end_warm`) /「（離開）」(`→ leave`)。
  - 守則：**不寫成可憐小白兔**；防備 / 小偷小摸是生存本能，不洗白。

#### 16-B 伍姐 `wu`（右室淨水站 / 發電前）— C 版（玩家問現象，不問名字）

- **NPC 節點**（`underground_settlement_right.tscn`）：`NpcWu (Area2D, interactable_area.gd)`；`interaction_id="talk_wu"` / `prompt_text="交談"` / `dialogue_id="wu"`；立於淨水站 / 發電舊機前（半蹲工作姿態，對齊右室淨水站 prop）。
- **對話樹**（`data/dialogue/wu.gd`；台詞定稿見 `三張臉.md` §3.2）：
  - `start`：`met_wu==true → retalk`，否則 `→ first_meet`。
  - `first_meet`（蹲著沒抬頭）→ choices：「這些機器都是妳修的？」`→ repairer`（維修人節點）；「這套淨水、發電……是誰弄起來的？」`→ maker`（**建立者節點，C 版核心**）；「（離開）」`→ leave`。
  - `repairer`：前城市維修員、城不要她下來修這座；effect `set_flag met_wu=true` + `add_int affinity_wu +1`；`goto end_cold`（「多搬兩趟水再說」）。
  - `maker`（**C 版核心，不具名**）：「有過一個人……這套東西一半是她搭起來的。**她不是好人，只是比我們晚一點壞掉。名字？她自己把自己關掉了。**」；effect `set_flag met_wu=true` + `set_flag knows_settlement_had_maker=true` + `add_int affinity_wu +1`；`goto end_cold`。✅ 玩家得到：**女性建立者 / 能力強 / 道德灰 / 已不在**——「知道有這麼一個人物」；**沒有名字、沒有林霏、沒有與主角的連結**。
  - `retalk`：「來了。手別閒著……」+ choices「（搭把手）」(`add_int affinity_wu +1` → `end_cold`) /「（離開）」+ **可選追問 `ask_maker_more`，condition gate＝`knows_settlement_had_maker==true`**：「她最後不是被抓走的，是自己把自己關掉的……去問還活著的人——我不替死人講話。」（**仍不具名**）terminal / 回 retalk。
  - 守則：**不神化她、不洗白成聖女**；伍姐刀子嘴、務實、冷。

#### 16-C 七號 `seven`（右室深隧道口牆）

- **NPC 節點**（`underground_settlement_right.tscn`）：`NpcSeven (Area2D, interactable_area.gd)`；`interaction_id="talk_seven"` / `prompt_text="交談"` / `dialogue_id="seven"`；立於右緣深隧道口的牆邊（抱胸、半身沒入陰影），**緊鄰 Phase 18 戰鬥入口掛點但不重疊互動區**。
- **對話樹**（`data/dialogue/seven.gd`；台詞定稿見 `三張臉.md` §3.3）：
  - `start`：`met_seven==true → retalk`，否則 `→ first_meet`。
  - `first_meet`（目光掃過你）→ choices：「你是誰？」/「你不像會躲的人。」皆 `→ hook`（鋪墊鉤）；「（保持距離）」`→ leave`。
  - `hook`（**核心鋪墊鉤**）：「躲？你們可以一直躲。我不行。**我有一個名字……還掛在上面。**別問了。」；effect `set_flag met_seven=true` + `set_flag seven_hinted_name_topside=true`；`goto end_cold`（目光沉進更深隧道口的黑）。⚠️ **全程不提妹妹**——「一個名字掛在上面」是唯一鉤子。
  - `retalk`：「還是你……我不交朋友。但你要真往更深的地方去，小心。」+ **可選追問 `ask_name`，gate＝`seven_hinted_name_topside==true`**：「我說了，別問。……等你哪天也丟了個非找回不可的人，你就懂了。現在，滾。」terminal /「（離開）」。
  - 守則：沉默、警戒、壓抑攻擊性；同情化但醜陋留在他本人（後續他要賣小岑位置 / 伍姐舊身份，**本 Phase 只鋪墊不爆**）。

#### 16-D DialogueDB 註冊 + 條件路由 + 跨場景旗標 + 回歸護欄

- `dialogue_db.gd`：`preload` `cen.gd` / `wu.gd` / `seven.gd`；`TREES` 加 `"cen"` / `"wu"` / `"seven"`。`start_dialogue` 一律以 `"start"` 入口，重講由各樹 `start` 條件路由決定（`met_* == true → retalk`），**不在 GameUI / 場景寫 NPC 專屬入口邏輯**。
- **立繪掛接**（`scenes/ui/dialogue_panel.gd` `start_dialogue`）：現況以 `dialogue_id` 的 if/elif 寫死載入 `portrait.png`（既有 `wan` / `store_robot` / `lu_qichen`，見 ~L45–64）。16-D 照同 pattern 加三支 `cen` / `wu` / `seven`，各指向已交付立繪：
  - `cen` → `res://assets/generated/sprites/cen/dialogue_portrait/cen-dialogue-portrait-20260621-141759/portrait.png`
  - `wu` → `res://assets/generated/sprites/wu/dialogue_portrait/wu-dialogue-portrait-20260621-143046/portrait.png`
  - `seven` → `res://assets/generated/sprites/seven/dialogue_portrait/seven-dialogue-portrait-idle-match-right-20260621-214200/portrait.png`
  - 沿用既有 `ResourceLoader.exists` 守衛（缺檔 → `texture = null` 安全降級，不崩）。三檔已在 repo，正常情況可載入。
- 跨場景：三人旗標在 `GameState.story_flags`（autoload），離開聚落再回來不重置；首見→重講路由正確。
- `test_runner.gd` Phase 16 區：三樹 `get_tree_for` 非空且載入無 error；模擬 `first_meet → 選項 → 旗標就位 → 重講走 retalk` 三人各一路徑；`knows_settlement_had_maker` / `seven_hinted_name_topside` 由對應節點正確設、可選追問 gate 條件正確；六旗標（`met_*` ×3 + 兩 bool + `affinity_*` int）round-trip（save→load 保留、缺鍵→預設、reset 歸零）；**不准出現「林霏」字串**（可加斷言掃三樹 text 不含「林霏」）。
- 回歸：**Phase 1~15 headless PASS 不退化**（既有 `wan` / `store_robot` / `lu_qichen` / travel 對話、聚落兩室轉場 / `reached_settlement` / 存讀檔不退化）。

#### 16-E GUI / 觸控走查（人工）

- 進聚落（街道→地鐵→聚落）→ 左室與小岑、右室與伍姐 / 七號對話可玩；首見走 `first_meet`、再談走 `retalk`。
- 小岑被抓包冷收尾分支可達；伍姐建立者節點 / 追問、七號鋪墊鉤 / 追問條件正確。
- 純觸控：D-pad 選項 + 確認推進對話（沿用 Phase 5-C 路由，不需新接線）。
- 立繪 / 角色圖到位後目視風格合規（Art Bible Ch3，三人不可大面積飽和橙）；未到位前以 placeholder 驗對話邏輯，不卡邏輯驗收。

### Act 2A 碎片 + 殘響鋪設（Phase 17，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 17`；操作清單見 `測試指南.md > Phase 17`；敘事事實來源 `subdocs/主線/雨還沒停v2.3.md` §7.1 / §10 / §4.2；場景真掛點見 `subdocs/地點/地鐵站.md`（通勤螢幕 / 月台碎片錨）/ `subdocs/地點/地下道聚落.md`（右室殘響採集點）。
> **核心：用既有組件（無新系統）把 Act 2A 兩個母題鋪進 Phase 15/16 已建兩室——記憶碎片複用 `MemoryFragmentArea`（Phase 14 同款），聚落殘響複用 `EchoPoint` + `EchoDB`（Phase 9）。產出＝1 個 MemoryFragmentArea 實例 + 1 條 STORY_MESSAGES + 1 條 EchoDB 殘響 + 1 個 EchoPoint 實例 + test_runner 護欄。** 依賴 Phase 14（碎片組件 / pattern）、Phase 9（EchoPoint / EchoDB / echo_progress / 拾遺手套感知）、Phase 15（兩室 + 通勤螢幕 / 殘響真掛點）、Phase 16（兩室 NPC 已落，避免站位重疊）。

#### 前提（先讀，避免敘事破功；硬規則）

1. 主角**失憶**：17-A「你以前往上通勤」只給身體記得、腦子想不起的失神（沿用 `mem_frag_linfei_1` 基調），**不揭身份、不點名、不解釋過去職務**。揭露留 Phase 19 / 22 / 26。
2. 17-B 殘響＝**一個被刪的普通住戶**，**不得是林霏 / 不得是阿達**：林霏核心殘響 payoff 在 Phase 21（原 Phase 22 已併入）、阿達殘響②在 Phase 19，本 Phase 不得預支。**segment 文字不准出現「林霏」**，且不得與 16-B 伍姐建立者節點連名。
3. 殘響感知沿用 Phase 9 語意：**裝備 `gleaner_gloves` 才有存在感** + 區域內**靜止 ≥1.0s** 才出提示 + E 採集一次性。不新增 UI / 小地圖 / 指標。

#### 現況基準（開工前必讀，皆可直接複用，不新增系統）

- 記憶碎片組件 `scripts/components/memory_fragment_area.gd`：`@export fragment_flag` / `@export message_id`；進區一次性 `set_flag(fragment_flag, true)` 並向上找有 `interaction_requested` 的祖節點 emit `{type:"message", message_text:...}`；文字取 `GameState.STORY_MESSAGES[message_id]`；`get_flag` 已 true 或 `UIMode != NONE` 時早退不觸發。已用於 14-A（`mem_frag_linfei_1`，見 `apartment_entrance.tscn`）。
- 殘響採集組件 `scripts/components/echo_point.gd`：`@export echo_id` / `@export segment_id`；`_update_active_state` 以 `GameState.is_item_equipped("gleaner_gloves")` + `has_echo_segment` 控存在感（微光 + 漸強雜訊）；區內靜止 ≥1.0s emit `player_entered`，`collect()` 呼 `GameState.collect_echo_segment` + SFX + toast，採集後以 `echo_progress` 判定永久消失。
- 殘響資料庫 `data/echoes/echo_db.gd`：`ECHOES` dict 每條 `title` / `segments[]`（`id`/`text`）/ 可選 `image_path` / 可選 `audio_path` / `sell_price` / `comment`（收藏家評語）。現有 4 條（`echo_clerk` / `echo_room401_tenant` / `echo_song_rain_doesnt_stop` / `echo_lu_family`）。
- 存讀檔：`mem_frag_*` 走 `GameState.story_flags`、`echo_progress` 走 `GameState.echo_progress`，二者皆已隨 `to_save_dict` / `load_save_dict`（缺鍵→預設）/ `reset_for_new_game` 持久化。**17 不需改 `game_state.gd` 邏輯**（僅 17-A 加一條 STORY_MESSAGES 文字常數）。
- 真掛點（Phase 15 已登記，不放假物件）：地鐵 `subway_station_platform` 停運通勤螢幕（`CommuterScreenArea` 一帶）＝碎片錨；聚落右室 `underground_settlement_right` 殘響採集點＝右室「NPC/殘響」區。

#### 新增 / 異動檔

```text
異動：
  data/echoes/echo_db.gd                                                  # 17-B：ECHOES 加 echo_settlement_erased（1–2 segment + 可選媒體 + 收藏家評語）
  scripts/autoload/game_state.gd                                          # 17-A：STORY_MESSAGES 加 "mem_frag_commute_topside" 文字（**僅資料常數，無邏輯改動**）
  scenes/levels/subway_station/subway_station_platform.tscn               # 17-A：$Interactables 下加 MemoryFragmentArea（fragment_flag/message_id="mem_frag_commute_topside"），落通勤螢幕 / 月台錨
  scenes/levels/underground_settlement/underground_settlement_right.tscn  # 17-B：加 EchoPoint（echo_id="echo_settlement_erased", segment_id="s1"），落底噪源 / 發電一帶，避開七號掛點 / 伍姐互動 / P18 入口
  tests/manual/test_runner.gd                                             # 17-C：碎片旗標 / 採集 / round-trip / 回歸護欄
新增：無（複用既有組件 / 系統）。
可選（不卡 headless，後置委製）：assets/images/echoes/<…>.png 或 assets/audio/echoes/<…>.ogg ＝ echo_settlement_erased 媒體層。
```

#### 旗標 / 資料（canonical，先定以防各自發明 key）

| key | 型別 / 位置 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `mem_frag_commute_topside` | bool / `story_flags` | 記憶碎片「你以前往上通勤」（失神、不揭身份）| 17-A 進區一次性 | Phase 26 真相揭露時回連「往上＝去服務富人」|
| `echo_settlement_erased` | `echo_progress[echo_id]` | 聚落殘響＝一個被刪住戶（1–2 segment）| 17-B 採集 | Act 2 拾遺賣 / 留 / 還；強化「被刪的人仍在殘響」|

#### 17-A 記憶碎片「你以前往上通勤」

- **節點**（`subway_station_platform.tscn` `$Interactables` 下）：`MemoryFragmentArea`（`memory_fragment_area.gd`）+ `CollisionShape2D`；`fragment_flag="mem_frag_commute_topside"`、`message_id="mem_frag_commute_topside"`。落點覆蓋停運通勤螢幕 / 月台錨前的必經帶（玩家從聚落上行回月台、或進月台時會經過），照 14-A「擺在必經路徑保證觸發」pattern。
- **文字**（`game_state.gd` `STORY_MESSAGES["mem_frag_commute_topside"]`，**定稿由實作者寫，守失神基調**）：第一人稱、站在停運通勤螢幕前，身體想起「每天清晨往**上**走」的通勤節奏（上行去服務富人區），肌肉記憶閃一下就斷；比 `mem_frag_linfei_1` 深半格（這次是主角自己的過去），但**只給差點想起又抓不住、不給答案、不點名**。
- 一次性：`get_flag` 已 true 不重觸、`UIMode != NONE` 不觸發（組件內建，不需改組件）。

#### 17-B 聚落殘響採集點（複用 EchoPoint）

- **殘響資料**（`echo_db.gd` `ECHOES["echo_settlement_erased"]`）：`title`（聚落被刪住戶，**不具名 / 不是林霏 / 不是阿達**）、`segments`＝1–2 條（`{id:"s1",text:...}`…，小體量）、`sell_price`（比照既有殘響可賣、集滿才收）、`comment`（鹿其琛評語，定稿補）。`image_path` / `audio_path` 可選、未到位先不填。
- **節點**（`underground_settlement_right.tscn` `$Interactables` 下，照 Phase 9 EchoPoint pattern）：`EchoPoint`（`echo_point.gd`）+ `CollisionShape2D`；`echo_id="echo_settlement_erased"`、`segment_id="s1"`（多段則各掛一點，`segment_id` 對應）。落於右室**收音機底噪源 / 發電舊機**一帶；**避開**七號 16-C 站位（深隧道口牆）、伍姐 16-B 互動區（淨水站前）、與 Phase 18 戰鬥入口（深隧道口）——座標對齊 `subdocs/地點/地下道聚落.md` 右室殘響掛點。
- 採集行為（既有，無需改組件）：未裝 `gleaner_gloves` 完全無感；裝後微光 + 漸強雜訊；靜止 ≥1.0s 出提示；E 採集→ `collect_echo_segment` + SFX + toast「殘響已記錄：<title> (n/total)」；採集後該點永久消失（`echo_progress` 判定，跨存讀檔保持）。

#### 17-C 回歸 + 存讀檔護欄

- `test_runner.gd` Phase 17 區：
  - 碎片：模擬進區設 `mem_frag_commute_topside`；round-trip（save→load 保留、缺鍵→false、reset 歸零）；二次進區不重觸（已 true 早退）。
  - 殘響：`EchoDB.has_echo("echo_settlement_erased")` 真、`get_segment_count` 與 `segments` 一致；`collect_echo_segment` 寫 `echo_progress`、重複採集無副作用；`echo_progress` round-trip（collected / sold 保留、reset 清空）。
  - 媒體層可選：echo 缺 `image_path`/`audio_path` 不報錯（採集 / 筆記不依賴媒體）。
  - **斷言**：`echo_settlement_erased` 任一 segment text 不含「林霏」字串。
- 回歸：**Phase 1~16 headless PASS 不退化**（既有碎片①、Phase 9 既有 4 條殘響採集 / 賣留、聚落兩室轉場 / 三 NPC 對話 / `reached_settlement` / 存讀檔不退化）。

### Act 2B 戰鬥遭遇 + 回執取得（Phase 18，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 18`；操作清單見 `測試指南.md > Phase 18`；戰鬥 E 鍵仲裁見上方「戰鬥場景互動契約（Phase 18，前瞻契約）」（`combat_mode` gate + E 優先鏈，本 Phase 兌現）；敘事事實來源 `subdocs/主線/雨還沒停v2.3.md` §4.2.1（回執本質 / 表面 / 把關）/ §4.2.2（賣給晚）/ §7.1（2B 拆段）；場景真掛點見 `subdocs/地點/地下道聚落.md`（右室 `deep_tunnel`＝戰鬥入口）。
> **核心：把 Phase 13 戰鬥組件（已完成）作者化進真動線——`deep_tunnel` 由封閉 examine 改為轉場進新戰鬥場景，敵人沿用 walker_01（13-E 原型同款），戰後以失物物流箱發回執，回執可經晚對話分支賣出並鎖死和平線。** 本 Phase 需新造的僅：① 1 個戰鬥場景 `tunnel_combat`；② 1 條 ITEMS_DB key item；③ 兌現 `combat_mode` gate；④ 晚的賣回執對話分支（盡量複用既有 effect ops）。戰鬥機制本身不重造。

#### 前提（先讀，避免敘事破功；硬規則）

1. 主角**失憶**：18-B 撿到回執時它**只是一張沒人認領的行政物流單**（模糊代碼 / 無完整姓名），玩家看不出與七號有關；認出代碼留 Phase 20。回執描述守 §4.2.1「表面」，**不得提七號 / 妹妹 / 林霏**。
2. **無 Game Over**：戰敗沿用 13-D 岔線（送安全點 + 替代訊息、改 trace 可選），不刪檔、不重來；清潔機格式化＝停機留場（13-B），**不會被打死**。
3. 回執雜物外觀：可賣給晚，但賣＝**鎖死和平線**（§4.2.2）。妹妹永不登場、「她還在」不得寫成溫馨節拍（§4.2.1 把關）——本 Phase 不寫七號認出代碼的文案（那是 Phase 20）。

#### 現況基準（開工前必讀，皆可直接複用）

- 戰鬥組件（Phase 13，已 headless PASS）：`player.gd` attack / `melee_stick.gd` 命中；`enemy_base`→`machine_enemy`→`walker_01` 巡邏 / `apply_stun` / 倒地自修復（`FALL→PRONE→GETUP→RECOVER_IDLE`，`is_stunned()` 僅 PRONE）；`format_reset.gd` 繞後按住 E `FORMAT_DURATION=3.0` → `defeated()` 停機；`combat_loss.gd` 戰敗岔線。原型床 `scenes/levels/combat_proto/combat_proto.tscn`＝作者化範本。
- 自修復排程（最新）：`walker_01.repair_schedule: Array[float] = [2.5, 4.0, 8.0]`（依擊倒次數放寬窗口）；per-instance 計數、場景重載歸零。戰鬥場景可在 `tscn` 覆寫此 export 調手感。
- 戰鬥姿勢 per-scene：`player.gd` `walk_anim`/`idle_anim` export 指向 `combat_walk`/`combat_idle`、`combat_walk_scale_mult` 處理尺寸差（見 combat_proto 範本）。
- `combat_mode` gate / E 優先鏈：前瞻契約已定義 `player.gd` `combat_mode` + `_e_reserved()`；本 Phase **戰鬥場景 Player 設 `combat_mode=true` 兌現**。
- 道具系統：`ITEMS_DB` + `add_item`（atomic、背包滿回 false 不掉件，見 `game_state.gd:551`）/ `has_item` / `remove_item`；隨 inventory 存讀檔。
- 對話 effect ops（DialogueRunner，已存在）：`set_flag` / `remove_item` / `add_item` / `add_trace`（11-C）；18-C 賣回執複用這些。**入帳 / 降 affinity 若無對應 op，補最小 effect op（add_credits / adjust_affinity），複用既有 `match op` 結構、不改既有 op 語意。**
- 轉場 / 存檔：`transition_to(scene_id, entry_point_id)` + SceneRegistry；戰鬥場景 `can_save_here=false`（沿用 7-F 禁存 pattern）。

#### 新增 / 異動檔

```text
新增：
  scenes/levels/tunnel_combat/tunnel_combat.tscn / .gd   # 18-A：廢棄通道戰鬥區（自 combat_proto 收斂；Player combat_mode=true；walker_01 一台；失物物流箱 InteractableArea；出口回右室）
異動：
  scripts/autoload/game_state.gd                          # 18-B：ITEMS_DB 加 childcare_supply_receipt（key item）；（如需）add_credits / affinity effect 支援
  scripts/dialogue/dialogue_runner.gd                     # 18-C：如缺 add_credits / adjust_affinity effect op 則補最小版（複用既有 match op 結構）
  scenes/levels/underground_settlement/underground_settlement_right.gd / .tscn  # 18-A：deep_tunnel 互動由 message 改為 transition_to("tunnel_combat","from_settlement")；加回程 entry point from_deep_tunnel
  scenes/actors/player/player.gd                          # combat_mode gate + E 優先鏈（兌現前瞻契約；若 13 review 已落則僅戰鬥場景設旗）
  data/dialogue/<wan 對話檔>                                # 18-C：晚加「賣回執」分支（gate has_item；effect remove_item + add_credits + add_trace + set_flag(peace_line_locked) + affinity_wan↓）
  tests/manual/test_runner.gd                             # 18-D：遭遇 / 格式化旗標 / 回執取得 / 背包滿 / 賣→鎖線 / round-trip / 回歸
SceneRegistry：新增 tunnel_combat（music_id 戰鬥曲或沿用聚落曲；can_save_here=false）。
可選（不卡 headless，後置委製）：回執 icon、隧道清潔機專屬圖、戰鬥區背景＝placeholder 不卡邏輯。
```

#### 旗標 / 資料（canonical，先定以防各自發明 key）

| key | 型別 / 位置 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `tunnel_machine_defeated` | bool / `story_flags` | 隧道清潔機已格式化停機 | 18-A defeated 觸發 | gate 18-B 搜索 / 出口；存讀檔保留 |
| `childcare_supply_receipt`（item） | ITEMS_DB key item / inventory | 林霏未送出的遺物（表面雜物） | 18-B `add_item` | Phase 20 交七號＝「還」；18-C 賣給晚 |
| `peace_line_locked` | bool / `story_flags` | 已賣回執給晚 → 和平線永久鎖死 | 18-C `set_flag` | Phase 20 和平線 gate「未設」 |

#### 18-A 隧道清潔機遭遇（作者化 13-E 原型）

- 新場景 `tunnel_combat`：自 `combat_proto` 收斂——廢棄通道背景 + 一台 `walker_01`（＝隧道清潔機）+ 可繞後空間 + 左側安全點（戰敗 combat_loss 落點）+ 出口。`Player` 設 `combat_mode=true`、`walk_anim`/`idle_anim`＝`combat_walk`/`combat_idle`。
- `deep_tunnel` 入口：`underground_settlement_right.gd` 將 `deep_tunnel` case 由 emit message 改為 `scene_transition_requested.emit("tunnel_combat","from_settlement",{})`；examine flavor（`MESSAGES["deep_tunnel"]`）可保留為「進入前」提示或移除。是否 gate 於 Act 2A 完成由實作者定（如需用既有旗標，不新增 gate 旗標）。
- 格式化結束：場景偵測 walker `is_defeated()`（連 signal 或 poll）→ `GameState.set_flag("tunnel_machine_defeated", true)`，解鎖 18-B 搜索點 / 出口。defeated 走 13-B（停機留場、不爆炸）。
- 戰敗：沿用 13-D，combat_loss 失敗旗標 + 送安全點 + 替代訊息；不死亡、不重來。

#### 18-B 戰後探索 + 回執取得

- ITEMS_DB `childcare_supply_receipt`：`category="key_item"`、`stackable=false`、`discardable=false`、`sellable=true`、`usable=false`、`icon_path`（後置，placeholder 不卡）。`name="兒少照護補給回執"`、`description` 守 §4.2.1「表面」（物流單 / 模糊代碼 / 無完整姓名，看不出與七號有關）。
- 取得：戰鬥區內**失物物流箱** `InteractableArea`，gate `tunnel_machine_defeated`（清潔機未停機不可搜，呼應 §4.2.1「漂在失物物流箱裡」）→ E 互動 `add_item("childcare_supply_receipt")`，一次性（取得後互動物關閉 / 改 examine）。
- **背包滿防呆**：`add_item` 回 false（背包滿）時，**不消耗、不掉件**，跳提示「背包已滿」，玩家清空間後可重試（沿用 `add_item` atomic 行為，`game_state.gd:551`）。
- 媒體 / 美術後置：回執 icon 未到位以 placeholder，不卡取得 / 背包邏輯。

#### 18-C 回執賣給晚（鎖死和平線）

- 觸點：晚（街道 NPC）對話新增「賣回執」分支，**gate `has_item("childcare_supply_receipt")`**；晚台詞守 §4.2.2「這種回執沒什麼價。除非你知道上面的代碼是誰的。」（輕提醒、不阻止）。
- 賣出 effect（DialogueRunner choice effect 陣列）：`remove_item("childcare_supply_receipt")` + 入帳（add_credits，缺 op 則補）+ `add_trace`（Trace↓）+ affinity_wan↓（Trust↓，缺 op 則補）+ `set_flag("peace_line_locked", true)`。
- 不可逆：賣出後 `has_item` 為 false，Phase 20 和平線（需持回執）永久不可達——**這是刻意代價，不給挽回路標**。
- 不賣（留著）：不設 `peace_line_locked`，回執留作 Phase 20「還」給七號的前提。

#### 18-D 回歸 + 存讀檔護欄

- `test_runner.gd` Phase 18 區：
  - 遭遇：`tunnel_combat` 載入無錯、walker / Player / 失物物流箱 / 出口節點存在；Player `combat_mode==true`。
  - 格式化：模擬 walker `defeated()` → `tunnel_machine_defeated` 為 true。
  - 回執：搜索點 gate（未 defeated 不給）；`add_item` 取得、`has_item` 真；背包滿時 `add_item` 回 false 且回執未掉、未重複加。
  - 賣給晚：模擬賣出 effect → `has_item` 假、`trace` 降、`peace_line_locked` 真；未賣則 `peace_line_locked` 未設、`has_item` 保持。
  - round-trip：`tunnel_machine_defeated` / 回執持有 / `peace_line_locked` save→load 保留、缺鍵→預設、reset 歸零。
- 回歸：**Phase 1~17 headless PASS 不退化**（含 Phase 13 戰鬥組件、Phase 9 殘響賣留、聚落轉場 / NPC、存讀檔；非戰鬥場景按 attack 無副作用＝`combat_mode` gate 護欄）。
- **手感（戰鬥窗口 / 繞後 / 失物箱會不會誤吃 E / 戰敗體感）＝ GUI / 真機走查，headless 不負責。**

### Act 2C 阿達罪責（Phase 19，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 19`；操作清單見 `測試指南.md > Phase 19`；敘事事實來源 `subdocs/主線/雨還沒停v2.3.md` §3（罪責回扣四段式，②阿達殘響 / ③舊善後工單鎖 2C）/ §7.1（Act 2 拆段）。
> **核心：全程複用既有組件（無新系統），落在 Phase 15 已建的聚落左室 `underground_settlement`。** 三拍：①`EchoPoint`+`EchoDB` 一條阿達殘響（Phase 9 同款）；②一個 examine 互動物（舊工單）→ 揭露 + 取得既有 `old_work_badge`；③`MemoryFragmentArea` 收束碎片（Phase 14/17 同款）。唯三處小擴充：(a) `EchoDB` 殘響加 `trace_on_collect` 欄位 + `collect_echo_segment` 兌現「留→Trace↑」；(b) `MemoryFragmentArea` 加 `require_flag` gate；(c) 聚落左室 controller 加一條 examine 分派。

#### 前提（先讀，避免敘事破功；硬規則）

1. **②③鎖 Act 2C、不漏到 Act 4**：阿達④本人登場是 Phase 26；本 Phase 阿達**不得現身**（只給殘響 / 工單 / 碎片）。
2. **罪責中檔**：揭露主角＝底層執行者（「只是照流程」），不洗白、不加碼、不長篇自白；守 v2.3 §3 冷度。
3. **不提林霏**：三拍任一文字**不得含「林霏」**；不解釋主角為何被標記（留 Phase 21）。
4. **順序鎖死**：採殘響（19-A）→ 工單揭露（19-B）→ 收束碎片（19-C）。工單在採殘響前只給中性提示；碎片 gate＝`read_old_work_order`。

#### 現況基準（沿用，不重造）

- `EchoPoint`（`scripts/components/echo_point.gd`）：`@export echo_id/segment_id/prompt_text`；裝 `gleaner_gloves` 才 active、靜止 ≥1.0s 出提示、E → `GameState.collect_echo_segment` + SFX + toast；採集後依 `echo_progress` 永久消失。掛在 level `$Interactables` 下、`player_entered/exited` 由 level 連接（同 Phase 17 右室）。
- `EchoDB`（`data/echoes/echo_db.gd`）：`ECHOES[id] = {title, segments:[{id,text}], sell_price, comment, image_path?, audio_path?}`。
- `GameState.collect_echo_segment(echo_id, segment_id)`→ append 至 `echo_progress[echo_id].collected`、emit `echo_changed`；`is_echo_complete` / `sell_echo`（內含 `add_trace(TRACE_DELTA_SELL=-1)`）既有。
- `MemoryFragmentArea`（`scripts/components/memory_fragment_area.gd`）：`@export fragment_flag/message_id`；`body_entered` 一次性、`UIMode==NONE` 才觸發、向上找 `interaction_requested` 發 message。
- 聚落左室 controller（`scenes/levels/underground_settlement/underground_settlement.gd`）：`_trigger_interaction()` 依 `interaction_id` match 分派 message / transition；`MESSAGES` dict 存 examine 文字；小岑 `NpcCen` 已在帳篷群（16-A）。
- `old_work_badge`（既有 ITEMS_DB key item：`磨損的工作證`、`category=key_item`、`discardable=false`）——目前**無取得點**，本 Phase 接上。

#### 19-A 阿達殘響②（複用 EchoPoint）

- **殘響資料**（`echo_db.gd` `ECHOES["echo_ada_reset"]`）：`title`（如「被格式化的阿達」，**不具名為林霏**）、`segments`＝1–2 段（`{id:"s1",text:...}`，第三人稱「他」描述執行人：雨衣 / 維修棍 /「只是照流程」，守冷度）、`sell_price` > 0（比照既有殘響可賣、集滿才收）、`comment`（鹿其琛評語，定稿補）、**新增 `trace_on_collect`: 1**（int）。`image_path` / `audio_path` 可選、未到位先不填。
- **`trace_on_collect` 接線**（`game_state.gd` `collect_echo_segment` 最小擴充）：append 成功後，若 `is_echo_complete(echo_id)` 由本次轉為 true 且 `echo_data.has("trace_on_collect")`，呼叫 `add_trace(echo_data["trace_on_collect"])`**一次**。因 `collect_echo_segment` 對已採 segment 回 false（不重入），完成只觸發一次；trace 為採集當下事件、非由狀態重算，故存讀檔載入不會重複加。其餘殘響無此欄位＝行為不變（向後相容）。
- **節點**（`underground_settlement.tscn` `$Interactables` 下，照 Phase 9/17 EchoPoint pattern）：`EchoPoint` + `CollisionShape2D`；`echo_id="echo_ada_reset"`、`segment_id="s1"`（多段則各掛一點）。落點**避開小岑 16-A 帳篷站位**與左室既有 examine 物（空帳篷 / 聚落中央）；座標對齊 `subdocs/地點/地下道聚落.md` 左室殘響掛點。

#### 19-B 舊善後工單③（examine → 揭露：設旗標 + 線索筆記 + badge 描述換版）

- **節點**（`underground_settlement.tscn` `$Interactables` 下）：一個 `InteractableArea`（`interactable_area.gd`）+ `CollisionShape2D`；`interaction_id="examine_old_work_order"`、`prompt_text`（如「E: 翻看一張舊派工單」）。
- **controller 分派**（`underground_settlement.gd` `_trigger_interaction()` 加一條 match 分支 `"examine_old_work_order"`）：
  - **gate＝先採殘響**：`if not GameState.is_echo_complete("echo_ada_reset")` → emit message（中性提示，如「一張看不懂的舊派工單，編號被磨花了」），**不設旗標 / 不換描述**，return。
  - **完整揭露**（已採殘響）：`if not GameState.get_flag("read_old_work_order", false)`：
    1. `GameState.set_flag("read_old_work_order", true)` + `GameState.add_knowledge(GameState.STORY_NOTES["clue_old_work_order"])`。
    2. emit 揭露 message（工單編號＝舊工作證號碼 → 確認兇手＝失憶前主角；守罪責中檔冷度）。
    3. **badge 不在此取得**——`old_work_badge` 早於公寓開場即在背包（Phase 1 預載 key item，不可丟 / 不可賣），19-B 只讓玩家「認出」它：`read_old_work_order` 觸發 `get_item_description` 換成揭露版描述（見 `game_state.gd`）；**不 `add_item`、無背包滿防呆**。
  - **已揭露**（`read_old_work_order` 已 true）：emit 簡短重看 message，不重複設旗標。
- **資料**：`game_state.gd` `STORY_NOTES` 加 `clue_old_work_order`（`category="身份"`、`title`、`body`＝把「派工單編號＝你的舊工作證」「你就是那個善後員」點明，定稿守冷度）。`old_work_badge` 沿用既有 ITEMS_DB 定義（不改）。

#### 19-C 收束碎片「你親手抹過阿達」（複用 MemoryFragmentArea + gate）

- **組件最小擴充**（`memory_fragment_area.gd`）：加 `@export var require_flag: String = ""`；`_on_body_entered` 在既有「flag 已設 / UIMode 非 NONE」早退之外，補一條 `if require_flag != "" and not GameState.get_flag(require_flag, false): return`。**預設 "" ＝現行行為**，Phase 14 / 17 碎片不受影響（向後相容）。
- **節點**（`underground_settlement.tscn` `$Interactables` 下）：`MemoryFragmentArea` + `CollisionShape2D`；`fragment_flag="mem_frag_erased_ada"`、`message_id="mem_frag_erased_ada"`、`require_flag="read_old_work_order"`。落點擺在工單 / 左室出口前的必經帶（玩家揭露工單後離開時會經過），照 14-A「必經路徑保證觸發」pattern。
- **文字**（`game_state.gd` `STORY_MESSAGES["mem_frag_erased_ada"]`，定稿守失神基調）：第一人稱、手記得維修棍重量與那句「只是照流程」、腦中只翻起噁心與空白；比 Phase 17「往上通勤」深一格（主動加害的回聲），**不給完整自白、不點名林霏**。

#### 19-D 回歸 + 存讀檔護欄

- `test_runner.gd` Phase 19 區：
  - 殘響：`EchoDB.has_echo("echo_ada_reset")`、`get_segment_count` 與 `segments` 一致；`collect_echo_segment` 寫 `echo_progress`；採集完成觸發 `trace_on_collect` 一次（trace +1）、重複採集 / 載入不重加；可賣（`sell_echo` → trace −1、`sold` 真）；其餘殘響無 `trace_on_collect` 行為不變。
  - 工單：未採殘響 examine 不設 `read_old_work_order`；採殘響後 examine → `read_old_work_order` 真 + `clue_old_work_order` 入筆記 + badge 描述換揭露版；`old_work_badge` 公寓開場即持有（19-B **不 `add_item`**），故 `has_item("old_work_badge")` 恆真；重看不重複設旗標。
  - 碎片：`require_flag` 未達不觸發；達 `read_old_work_order` 後一次性設 `mem_frag_erased_ada`；二次進區不重觸。
  - round-trip：`echo_progress`（collected/sold）/ `read_old_work_order` / `old_work_badge` 持有 / `mem_frag_erased_ada` / `trace` save→load 保留、缺鍵→預設、reset 歸零。
  - **斷言**：`echo_ada_reset` 任一 segment、`clue_old_work_order.body`、`STORY_MESSAGES["mem_frag_erased_ada"]` 三者文字不含「林霏」。
- 回歸：**Phase 1~18 headless PASS 不退化**（含 Phase 9 既有殘響採集 / 賣留、Phase 14/17 既有碎片觸發、聚落左室轉場 / 小岑對話 / `reached_settlement` / 存讀檔；`MemoryFragmentArea` 既有碎片因 `require_flag` 預設 "" 不退化）。
- **手感（殘響採集點 / 工單會不會誤吃 E / 碎片觸發時機 / 揭露情緒重量）＝ GUI / 真機走查，headless 不負責。**

### Act 2D 七號可選 + 和平線 Branch D（Phase 20，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 20`；操作清單見 `測試指南.md > Phase 20`；敘事事實來源 `subdocs/主線/雨還沒停v2.3.md` §4.2.1（回執真正用途）/ §4.2.3（七號和平成功線）/ §7.1（Act 2 拆段）。
> **核心：不新增場景、不新增系統，沿用 Phase 16 七號對話樹 + Phase 18 回執 + Phase 11 Trust/Trace。** Phase 20 只在 `data/dialogue/seven.gd` 補一組窄漏斗對話，成功時移除 `childcare_supply_receipt`、設 `seven_peace_branch_d`、調整 `affinity_seven` / trace；失敗或錯過時不製造災難，讓 Phase 24 讀 `seven_peace_branch_d` 是否缺席。

#### 前提（先讀，避免敘事破功；硬規則）

1. **稀有恩典，不加路標**：成功條件＝持有回執 + 事件爆發前主動找七號 + 不威脅 / 不嘲諷 / 不逼問 + 選擇交還。不得新增筆記、任務 step、toast 或 UI 文案提示玩家「拿回執找七號」。
2. **妹妹永不登場**：她只是一筆仍在系統內的紀錄；回執只證明近期仍收照護補給、未被刷白 / 死亡登記。不寫成溫馨重逢，不保證她幸福。
3. **七號不洗白**：成功後七號自願放棄交網，但不立刻成朋友；仍保持警戒與灰度。沒走 D 時，Phase 24 仍承接七號背叛。
4. **賣掉回執永久鎖死 D**：`peace_line_locked == true` 時，本 Phase 成功入口不可達；不補救、不反悔。

#### 現況基準（沿用，不重造）

- `data/dialogue/seven.gd`：入口 `start` 依 `met_seven` 路由首見 / `retalk`；`retalk` 已有 `ask_name` 追問（gate＝`seven_hinted_name_topside`）。Phase 20 應在 `retalk` 或其後續節點加「拿回執給他看」入口，不改首見鋪墊語意。
- DialogueRunner condition 已支援 `has_item`；effect 已支援 `remove_item`、`set_flag`、`add_int`、`add_trace`。若實作時發現缺 op，補最小 op，不改既有 op 語意。
- `childcare_supply_receipt`：Phase 18-B 已加入 ITEMS_DB 並可取得；Phase 18-C 晚可買走並設 `peace_line_locked`。
- `affinity_seven`：Phase 16 已登錄的 trust adapter target；`GameState.get_trust("seven")` 讀 `affinity_seven`。
- `underground_settlement_right`：七號已站在右室深隧道口牆邊；本 Phase 不需要新增場景節點。

#### 旗標 / 資料（canonical）

| key | 型別 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `seven_peace_branch_d` | bool / `story_flags` | Branch D 和平成功；七號已因回執自願放棄交網 | 20-B 成功交還回執 | Phase 24 跳過背叛爆點或走低壓版本；Act 4 / Protect 染色 |
| `seven_betrayal_pending` | bool / `story_flags` | 七號事件待爆；由 Phase 24 初始化 A/B 判定使用 | Phase 24 若 `seven_peace_branch_d != true` 時設 | Phase 24 / 26 / 28 |
| `childcare_supply_receipt` | inventory item | 七號能認出真正用途的回執；可賣、可留、可交還 | Phase 18-B 取得；20-B 成功交還時移除 | Phase 20 D；M1 進度曾取得不退勾 |
| `peace_line_locked` | bool / `story_flags` | 回執已賣給晚，和平線永久鎖死 | Phase 18-C | Phase 20 gate |

> `seven_betrayal_pending` **不**在 Phase 20 錯誤對話時設，避免玩家仍在事件前、仍持回執時被過早鎖死。Phase 20 成功與否的事實來源是 `seven_peace_branch_d`；Phase 24 初始化再把「未 D」轉成 pending。

#### 20-A 七號對話加和平入口

- **修改檔**：`data/dialogue/seven.gd`。
- **入口位置**：`retalk` choices 加一個隱藏式選項，例如「把那張回執拿給他看」。不要寫成「和平線」或「妹妹」。
- **gate**：
  ```gdscript
  {
      "condition": [
          {"type": "has_item", "item_id": "childcare_supply_receipt", "op": "==", "value": true},
          {"flag": "peace_line_locked", "op": "!=", "value": true},
          {"flag": "seven_peace_branch_d", "op": "!=", "value": true}
      ],
      "target": "receipt_probe"
  }
  ```
  若 DialogueRunner 的 Array condition 走 AND（Phase 7 已有 pattern），沿用；若 `op !=` 對缺鍵行為需確認，缺鍵應視為 false。
- **錯誤語氣路線**：`receipt_probe` 可給 2–3 個選項，其中威脅 / 嘲諷 / 逼問都進 `receipt_fail_cold`：不移除回執、不設旗標、不改 trust/trace。成功選項才進 `receipt_return`。
- **已 D 後重講**：`seven_peace_branch_d == true` 時，`retalk` 走一段短版冷招呼，不再顯示交還選項。

#### 20-B 交還回執 + Branch D

- **成功節點**（建議 id `receipt_return` → `receipt_recognized` → `peace_branch_d_done`）：七號先不信，接著認出代碼 / 舊符號 / 錯字，台詞包含 v2.3 拍板節拍：「她還在。」「那我不能回去。」「我一回去，他們就會順著我找到她。」「……幹。」收束為「我欠你一次。不是欠你們。是欠你。」
- **effect 順序**：
  ```gdscript
  [
      {"op": "remove_item", "item_id": "childcare_supply_receipt", "value": 1},
      {"op": "set_flag", "key": "seven_peace_branch_d", "value": true},
      {"op": "add_int", "key": "affinity_seven", "value": 2},
      {"op": "add_trace", "value": -1}
  ]
  ```
  `remove_item` 失敗時 DialogueRunner 既有語意會中止後續 effect；測試需覆蓋。若最後決定 Trace 只不增不降，可把 `add_trace` 改 0 或省略，但不得上升。
- **不可同時設** `peace_line_locked`；交還與賣出互斥。
- **文字禁線**：不讓妹妹出場、不提供姓名、不保證幸福；不把七號寫成被感化的朋友。

#### 20-C 未處理延燒到 Phase 24

- Phase 20 不需要一個「結束 Phase」互動。未交還的狀態以 `seven_peace_branch_d != true` 表示。
- Phase 24 初始化七號事件時：若 `seven_peace_branch_d == true`，走 D/低壓路徑；否則設 `seven_betrayal_pending=true`，再依 Trace / Trust / 是否持回執等判 A/B。
- `peace_line_locked == true` 時，Phase 20 成功入口不可達；即使玩家後續沒有回執，這仍是刻意代價。

#### 20-D 回歸 + GUI 走查護欄

- `test_runner.gd` Phase 20 區：
  - `seven.gd` 對話樹存在 Phase 20 節點；成功入口 gate 對 `has_item("childcare_supply_receipt")` / `peace_line_locked` / `seven_peace_branch_d` 生效。
  - 成功交還：回執移除、`seven_peace_branch_d` true、`affinity_seven` 上升、trace 不增或微降；save→load 保留、缺鍵→false、reset 歸零。
  - 錯誤語氣 / 缺回執 / 已賣 / 已 D：不移除回執、不設 D、不改 trust/trace（已 D 的短版招呼除外）。
  - Phase 1~19 不退化，尤其 Phase 18 賣回執仍能鎖死 D、M1 特殊道具進度不因移除回執退勾。
- GUI / 純觸控：七號右室對話可玩；交還選項隱而可達；錯誤路線冷收尾；成功路線節拍成立；BtnE / 對話選項可推進。

### Act 3 夜總會 + 林霏核心殘響 + 主題歌（Phase 21，實作契約；原 Phase 22 已併入）

> 驗收意圖見 `遊戲規格書.md > Phase 21`；操作清單見 `測試指南.md > Phase 21`；敘事事實來源 `subdocs/主線/雨還沒停v2.3.md` §4.1（林霏核心殘響台詞）。
> **合併說明**：原 Phase 21（主題歌）+ 原 Phase 22（夜總會 + 林霏核心殘響）併為單一 Phase 21；Phase 22 編號保留不再使用，23~31 不變。
> **核心**：新增 1 場景 `nightclub` + 1 跨幕殘響 `echo_linfei`，沿用 Phase 9 拾遺 + Phase 10 ducking/Ambient + Phase 14 碎片 pattern；**唯一系統擴充＝EchoDB/EchoPoint 媒體分段門檻解鎖**（半→歌、全→照片）。
> **結構先行、素材後掛**：21-A~C 無外部素材依賴；21-D 等素材到貨補掛；21-E 複用既有《雨還沒停》歌檔（非新素材）；一律不暫代（新素材由 user 提供）。

#### 前提（硬規則）

1. **不暫代**：夜總會背景美術 + 場景 BGM、林霏女聲歌、林霏殘響照片（皆 user 提供新素材）全部到貨後才掛；不放 placeholder 美術 / 不借 BGM / 不寫待覆寫佔位文字（沿用 Phase 15 規則）。**《雨還沒停》不是新素材**——既有 `echo_song_rain_doesnt_stop` 殘響的歌檔（`assets/audio/echoes/echo_song_rain_doesnt_stop.mp3`）已在，21-E 直接複用。
2. **林霏殘響＝劇情功能性記憶鑰匙**：歌＝林霏的聲音。媒體「集滿才出」沿用既有規則，但林霏特別＝**分兩段門檻**：收集達半→解鎖歌、全滿→解鎖照片。
3. **可賣，但結局只台詞微調**：林霏殘響走標準收藏家流程（集滿才收，`sell_echo`→Trace↓ + credits，與其他殘響一視同仁）；唯一特殊＝賣出時 set `sold_linfei_echo`，供 Phase 26~28 結局讀取改少數台詞（本 Phase 不寫結局台詞，僅留 forward-契約 + 存檔）。**不改結局路由、不硬鎖。**
4. **不揭主角身份**：碎片「黑戶藏身方式」只揭林霏在保護黑戶藏身網，不解釋主角為何被標記（留 Phase 26 真相碎片）。

#### 現況基準（沿用，不重造）

- `EchoPoint` / `EchoDB`（Phase 9）：採集需 `gleaner_gloves` + 靜止 ≥1s + E；`echo_progress` 既有存讀檔（含 sold，Phase 19 已落地）。
- 媒體播放走 Phase 9 echo 媒體路徑 + Phase 10 ducking；環境母題走 Phase 10 Ambient bus。
- `MemoryFragmentArea`（Phase 14）+ `story_flags`：碎片旗標漸進解鎖；`require_flag` gate 已於 Phase 19 加入。
- SceneRegistry / `transition_to(scene_id, entry_point_id)`：新場景走既有雙向轉場。
- 收藏家賣出 `sell_echo`（Phase 9-G / 19-A）：集滿才收、Trace↓。

#### 旗標 / 資料（canonical）

| key | 型別 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `echo_linfei` | EchoDB 條目 / `echo_progress` | 林霏核心殘響；N 段（預設 6）；半→歌、全→照片媒體；可賣 | 21-B 登記 / 採集 | M1 殘響進度；Phase 26~28 染色 |
| `mem_frag_hideout` | bool / `story_flags` | 收束碎片「你看到的是黑戶藏身方式」 | 21-C（gate＝採過林霏殘響）| Phase 26 真相碎片串接 |
| `sold_linfei_echo` | bool / `story_flags` | 林霏殘響已賣給收藏家 | 21-F 賣出 | **forward-契約**：Phase 26~28 結局讀此改少數台詞 |

#### 21-A 夜總會場景骨架 + 轉場

- 新增 `nightclub` 場景：SceneRegistry 註冊 `scene_id` + `music_id` + entry points `from_settlement` / 回程；雙向 `transition_to`；`can_save_here` 依場景定（非戰鬥，預設可存）。
- 入口從聚落接：聚落側加一個往夜總會的 `InteractableArea` / 轉場點（落點與 Phase 15 聚落兩室慣例一致；具體掛點待場景設計）。
- 美術 + BGM 由 user 提供後落地（不暫代）。
- 驗收：headless 載入 `nightclub`、雙向 round-trip。

#### 21-B 林霏核心殘響房間 + 分段媒體

- EchoDB 新增 `echo_linfei`：N 段文字（**預設 N=6，可調**）；段落含「別讓他們被找到」拍板台詞（v2.3 §4.1），其餘 segment 寫方向、定稿待實作者補。
- EchoPoint 落 `nightclub`；採集沿用 Phase 9（手套 + 靜止 ≥1s + E）；`trace_on_collect`＝採集 Trace↑（與其他殘響一致）。
- **系統擴充（媒體分段門檻）**：EchoDB 媒體條目支援 unlock 門檻＝已採段數；`收集數 >= ceil(N/2)` 解鎖「歌」media slot（21-D 掛資產，先空）、`收集數 == N` 解鎖「照片」media（user 提供）。既有「集滿才出」＝門檻 == N 的特例，需向後相容（其他殘響不受影響）。
- 驗收：headless `echo_linfei` 登記、採集流程、半 / 全門檻各自觸發正確 media 狀態、`echo_progress` round-trip（歌資產暫空不影響門檻邏輯）。

#### 21-C 收束碎片「黑戶藏身方式」

- `MemoryFragmentArea` 落 `nightclub`（或林霏殘響房動線必經）；旗標 `mem_frag_hideout` + STORY_MESSAGES 文字（方向：林霏在維護黑戶藏身網；不揭主角身份）。
- **gate＝採過林霏殘響**（`require_flag`；建議綁採滿，與照片 media 同步較自然，順序鎖）。未達 gate 前只給中性提示。
- 驗收：headless 碎片旗標於 gate 後可設、round-trip。

#### 21-D 女聲主題歌掛載

- 資產 `assets/audio/main_theme/<林霏女聲歌>`（user 提供）進 21-B「歌」media slot；播放走 Phase 9 echo 媒體 + Phase 10 ducking。
- 驗收：headless 音訊資源載入 + 媒體接線；GUI：採集達半時播歌。

#### 21-E 《雨還沒停》環境母題

- **複用既有歌檔**：`assets/audio/echoes/echo_song_rain_doesnt_stop.mp3`（Phase 9 既有 `echo_song_rain_doesnt_stop` 殘響的歌檔，已在 repo）；**非新素材、非新 blocker**。可選做一段 loop / 環境剪輯版，但不阻塞。
- 落地鐵卡帶 / 街友哼唱播放點；走 Phase 10 Ambient bus（`assets/audio/ambient/` 既有 street_rain_loop / subway_rumble 床）；**不綁身世**（純環境母題，不解釋來源；與 Act 1 既有殘響採集點各自獨立）。
- **與林霏女聲歌（21-D）刻意分離**：v2.3 §2.7 拍板——《雨還沒停》維持環境母題不升格，林霏記憶鑰匙＝另一首全新女聲歌；兩者不得互換。
- 驗收：headless 播放點存在。

#### 21-F 可賣 + 回歸 + 存讀檔

- 林霏殘響可賣：收藏家集滿才收，`sell_echo`→Trace↓ + credits（標準流程）；賣出 effect 加 `set_flag sold_linfei_echo`。
- **forward-契約**：Phase 26~28 結局（晚 / 旁白）讀 `sold_linfei_echo` 改少數台詞；本 Phase 不寫結局台詞。
- `test_runner.gd` Phase 21 區：`nightclub` 載入 + round-trip；`echo_linfei` 採集 / 半全媒體門檻 / 可賣 / `echo_progress`（含 sold）；`mem_frag_hideout` gate + round-trip；`sold_linfei_echo` round-trip；Phase 1~20 不退化。
- 驗收：PASS。

### 新場景與 SceneRegistry

| Phase | scene_id（暫定） | entry points（暫定） |
|---|---|---|
| 15 | `subway_station`（地鐵站大廳＝下沉門檻）| `from_street` / `from_platform`（+ 回街道 `apartment_entrance:from_subway`）|
| 15 | `subway_station_platform`（地鐵站月台）| `from_concourse` / `from_settlement`（+ 下行 `underground_settlement:from_subway`）|
| 15 | `underground_settlement`（地下道聚落左＝Act 2 hub）| `from_subway` / `from_right`（+ 回地鐵 `subway_station_platform:from_settlement`）|
| 15 | `underground_settlement_right`（地下道聚落右＝淨水/隧道口）| `from_left` / `from_deep_tunnel`（深隧道口 Phase 18 轉場戰鬥區 + 回程落點）|
| 18 | `tunnel_combat`（廢棄通道戰鬥區＝Act 2B 遭遇，戰鬥場景）| `from_settlement`（← 右室 `deep_tunnel`）；出口回 `underground_settlement_right:from_deep_tunnel`；`can_save_here=false` |
| 21 | `nightclub`（夜總會＝Act 3 林霏 / 七號交易舊場；原 Phase 22）| `from_settlement` / 回程 |
| 24 | `tunnel_chase`（深隧道追逐 房間1＝七號小爆點限時選門迷宮）| `from_settlement`（← 右室深隧道口 `deep_tunnel_opened` 選單）；正解高門 → `tunnel_chase_right:from_left`；左緣鎖住（不可退回聚落）；`can_save_here=false` |
| 24 | `tunnel_chase_right`（深隧道追逐 房間2＝三出口隨機真出口）| `from_left`（← 房間1）；左緣 → `tunnel_chase:from_right`（backtrack，計時續跑）；真出口 or 倒數歸零 → 攤牌情境圖 → `underground_settlement_right:from_deep_tunnel`；`can_save_here=false` |
| 25 | `datacenter_entrance`（設施入口大門＝anchor-05；善後員門禁 gate）| `from_nightclub`（← `nightclub_entrance` travel「AI 資料中心」）；左緣 → `nightclub_entrance:from_datacenter`（回程）|
| 25 | `datacenter_backup`（伺服器陣列廊道＝戰鬥③）| `from_entrance`（← 大門，門禁 gate）/ `from_core`（backtrack）；右端門按 E（`exit_to_core` 互動）→ `datacenter_backup_core:from_backup`；戰鬥中 `can_save_here=false` |
| 25 | `datacenter_backup_core`（核心備份區＝結局觸發掛點）| `from_backup`（← 廊道）；本 Phase 只放中性 examine 佔位，26-D 接結局路由；`can_save_here=true` |
| 27 | `underground_broadcast`（blocker 1 拍板才建）| Act 4 後進入 |
| 31 | `abandoned_archive`（P-00，暫緩）| — |

- 每個新場景：SceneRegistry 註冊 `scene_id` + `music_id` + entry points；雙向轉場走既有 `transition_to(scene_id, entry_point_id)`；`can_save_here` 依場景定（戰鬥 / 外牆類禁存沿用 7-F pattern）。

### 關鍵道具 / 資料

- **兒少照護補給回執**（Phase 18 取得）：ITEMS_DB 新增 key item（canonical id `childcare_supply_receipt`；不可裝備；可賣給晚＝ trace↓ + `peace_line_locked` 鎖死和平線；可交給七號＝「還」）。持有以 `has_item` 為事實來源；取得 gate `tunnel_machine_defeated`。表面模糊代碼 / 無姓名；真正用途由七號對話 condition 認出代碼（v2.3 §4.2.1）。Phase 20 成功交還時移除 item、設 `seven_peace_branch_d`、調整 `affinity_seven` / trace；契約見 `> Act 2B 戰鬥遭遇 + 回執取得（Phase 18，實作契約）` 與 `> Act 2D 七號可選 + 和平線 Branch D（Phase 20，實作契約）`。
- **林霏殘響**（Phase 14 碎片① → Phase 21 核心房間，原 Phase 22 已併入）：EchoDB 一條跨幕殘響 `echo_linfei`；分段媒體（半收集→女聲歌、全收集→照片）；可賣（set `sold_linfei_echo`）。契約見 `> Act 3 夜總會 + 林霏核心殘響 + 主題歌（Phase 21，實作契約）`。
- **每幕一條殘響**＝一個被刪的人（v2.3 §10）；阿達殘響（Phase 19）、聚落殘響（Phase 17）。

### 記憶碎片 / 旗標

- 記憶碎片＝ `story_flags` 漸進解鎖（沿用既有 story_flags store）；逐 Phase 加碎片旗標：撞林霏①(14)、往上通勤(17)、抹過阿達(19)、黑戶藏身方式(22)、真相「自己選擇被刪」(26)。
- 七號三分支（Phase 24）＝ QuestManager quest（`seven_betrayal`）+ 語意結果旗標：`seven_peace_branch_d`（D，Phase 20 事實來源）/ `seven_stopped_full`（A）/ `seven_stopped_partial`（B）；未 D 時設 `seven_betrayal_pending` 進 A/B 判定（公式＝Trace 臨界 × 伍姐 / 小岑 affinity≥2 警告，見 `> Act 3→4 七號小爆點（Phase 24，實作契約）`）；Branch B → `cen_voiceprint_exposed` → 28-C 小岑缺席、`seven_stopped_partial` → 26-D 檔案重標記呈現。
- 和平線 Branch D（Phase 20）＝多條件 AND（持回執 + 事件前主動 + 不威脅 / 不嘲諷 / 不逼問 + 交還）；成功設 `seven_peace_branch_d`，賣回執（18-C）以 `peace_line_locked` 鎖死 D。

### 三結局路由（Phase 27–29）

- 觸發點：Act 4 備份前（26-D）→ `main` / 結局 router 依玩家選擇 + Trace + 清洗狀態分派。
- **硬鎖規則**：Reclaim / Protect 只讀染色（Trust/Trace 影響台詞 / 小岑回收，不擋結局）；**Expose 硬分 A/B/C**：
  - **判定順序（先判 trace 臨界，再判清洗）**：
    1. `trace >= 臨界` → **C**（99% 被攔、當場被抹除）。**C 優先於 A/B**——trace 過高直接 C，不看清洗狀態。
    2. 否則 `cleaned == false` → **A**（聚落焚毀）。
    3. 否則（`cleaned == true` 且 `trace < 臨界`）→ **B**（匿名雜訊、保住所有人）。
  - 臨界值開工校。
- 小岑條件式回收（28-C）：讀 24-B Branch B 旗標決定「安全不認得你」或「永遠缺席 + 伍姐搖頭」。

### 主題歌 / 音訊 / 美術素材（Phase 21）

```text
新增（user 提供新素材，不暫代；blocker）：
  assets/audio/main_theme/<林霏女聲歌>.mp3   # 記憶鑰匙；掛林霏殘響「半收集」媒體 slot
  <林霏殘響照片>                                # 林霏殘響「全收集」媒體
  <夜總會背景美術 + 場景 BGM>                   # nightclub 場景

複用既有（非 blocker）：
  assets/audio/echoes/echo_song_rain_doesnt_stop.mp3   # 《雨還沒停》既有歌檔；21-E 鋪環境母題
```

- 女聲歌走既有殘響媒體層播放路徑（沿用 Phase 9 echo 媒體 + Phase 10 ducking）；照片＝全收集 media。**《雨還沒停》母題＝複用既有歌檔**，走 Phase 10 Ambient bus pattern；與林霏女聲歌刻意分離（v2.3 §2.7）。詳細契約見 `> Act 3 夜總會 + 林霏核心殘響 + 主題歌（Phase 21，實作契約）`。

### Act 3 夜總會保全四解小解謎（Phase 23，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 23`；操作清單見 `測試指南.md > Phase 23`；場景設計事實來源 `subdocs/地點/夜總會.md > 保全解謎`。
> **核心**：把 Phase 21 落地的 `nightclub`（門面廳）`back_door` 無條件轉場，改造成**四解小解謎**（賄賂 / 假裝身份 / 引開 / 潛行），任一成功 set `passed_nightclub_security`。**不新增場景**（沿用 Phase 21 三場景），新增 1 個保全 NPC + 1 個 `bar_bot` 互動物 + 1 個工牌 examine + 1 個 ITEMS_DB key item；唯一系統擴充＝DialogueRunner 補 `credits` condition type。
> **範圍更正**：規格書原 23 子階段表「人類保全潛行 / 撐過（13-C）」是舊薄殼，已改寫為四解小解謎；本 Phase **不做 13-C 式戰鬥遭遇**。

#### 前提（硬規則）

1. **保全是人類，只可智取**：玩家不能打死 / 格式化保全（沿用 Phase 13-C 人類分支精神）；不給 attack 對保全的傷害路徑。四解殊途同歸。
2. **大門口閘門無代價**：`biometric_gate` 刷閘被拒只是劇情拍子，**無 Trace、無 add_credits、不卡關**，純導向側門。
3. **工牌不點名**：`nightclub_staff_pass` 描述只撐「夜總會舊員工」職務感，**不得出現「七號」/「林霏」/「妹妹」**。
4. **引開 / 潛行 transient 不存檔**：騷動 / 保全離崗只在當前進場有效；只有 `passed_nightclub_security` 持久。
5. **四解皆不動 Trace**：賄賂 / 假裝身份 / 引開 / 潛行成功都不呼叫 `add_trace`；本關不是道德軸節點。

#### 現況基準（沿用，不重造）

- `scenes/levels/nightclub/nightclub.gd`：門面廳 controller。目前 `_trigger_interaction()` 只 match `exit_to_entrance` / `back_door`（`back_door` 無條件 `scene_transition_requested("nightclub_back", "from_lobby", {})`）。場景現有互動物：`ExitToEntranceArea` / `BackDoorArea` / `EchoPoint`（s4）。
- `scenes/levels/nightclub/nightclub.tscn`：`Interactables` 節點下加 `Bodyguard` / `BarBot` / `StaffPass` 三個 Area2D；保全世界 sprite 用 `assets/generated/sprites/nightclub_bodyguard/map_npc/sprite.png`、`bar_bot` 用 `assets/generated/sprites/nightclub_bar_bot/map_npc/sprite.png`。
- `scenes/levels/nightclub/nightclub_entrance.gd`：大門口 controller。`biometric_gate` examine 走既有 message 派發（純文字、不設旗標、不動 Trace），文末提示走 `service_door`（已存在的進門面廳通道）。
- DialogueRunner（`scripts/dialogue/dialogue_runner.gd`）：condition 已支援 `story_flag` / `has_item` / `has_note` / `quest_status` / `quest_flag` / `echo_complete` / `echo_unsold`；effect 已支援 `set_flag` / `add_int` / `add_credits` / `add_item` / `remove_item` / `add_note` / `add_trace` / `start_quest` / `complete_quest` / `open_shop` / `travel` / `sell_echo`。**無 `credits` condition**——本 Phase 補。
- `GameState.get_credits()` / `add_credits(int)`（負值扣款，`set_credits` clamp 至 ≥0）/ `add_item` 回 bool（背包滿 false）/ `set_flag` / `get_flag` 皆既有。
- DialogueDB（`data/dialogue/dialogue_db.gd`）：對話樹註冊表；新樹 `nightclub_bodyguard` 比照既有樹登記。

#### 旗標 / 資料（canonical）

| key | 型別 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `passed_nightclub_security` | bool / `story_flags` | 四解任一成功通過門面廳保全 | 23-B 賄賂 / 假裝身份成功；23-C 潛行成功 | `back_door` gate；後場門反覆進出 |
| `found_staff_pass` | bool / `story_flags` | 已取得舊員工工牌＝假裝身份佐證 | 23-A 工牌 examine（add_item 成功後同步設）| 23-B 假裝身份對話 gate |
| `nightclub_staff_pass` | ITEMS_DB key item / inventory | 夜總會門禁工牌（假裝身份道具）| 23-A examine 取得 | M1 特殊道具進度曾取得不退勾；不可賣 / 不可丟 |

> **與 `old_work_badge` 區隔**：`old_work_badge`（Phase 19-B）＝主角自己的舊善後員工作證（罪責 key item，顯示名「磨損的工作證」）；`nightclub_staff_pass`＝夜總會撿到的別人的門禁工牌（過保全道具）。兩者敘事功能、id、顯示名、描述刻意拉開（顯示名建議「門禁工牌」/「夜總會員工通行證」，描述強調霓虹 logo / VIP 後場權限 / champagne 金的場所感），避免玩家在背包混淆。icon 由實作者自製。

#### 23-A 場景接線骨架

- **大門口 `biometric_gate`**：examine → message「不在賓客名單 / 富區 AI 把關」+ 提示走側邊員工通道；**不設旗標、不動 Trace、不扣 credits**。
- **保全 NPC**：`nightclub.tscn` 加 `Bodyguard` Area2D（`dialogue_id="nightclub_bodyguard"` + 世界 sprite），站位擋在 `back_door` 前。門面廳 `_trigger_interaction()` 加 `dialogue` 派發分支（沿用既有 `dialogue_id` dispatch pattern，emit `interaction_requested` → Main → `start_dialogue`）。
- **`back_door` gate retrofit**：`_trigger_interaction()` 的 `back_door` 分支改為——
  ```gdscript
  "back_door":
      if GameState.get_flag("passed_nightclub_security", false):
          scene_transition_requested.emit("nightclub_back", "from_lobby", {})
      elif _bodyguard_off_post:   # 23-C 潛行：離崗空窗
          GameState.set_flag("passed_nightclub_security", true)
          scene_transition_requested.emit("nightclub_back", "from_lobby", {})
      else:
          interaction_requested.emit({"message_id": "NIGHTCLUB_BACK_DOOR_BLOCKED"})
  ```
  通過後 `back_door` 可反覆轉場（gate 為 true 直接過）。
- **工牌 examine**：`StaffPass` Area2D examine →
  ```gdscript
  if GameState.add_item("nightclub_staff_pass"):
      GameState.set_flag("found_staff_pass", true)
      # 顯示工牌取得 message + 寫線索筆記（可選）
  else:
      # 背包滿：不設 flag、不吞、可重試，顯示背包滿提示
  ```
  沿用 Phase 19-B / 18-B 的「add_item 回 false 即中止、可重試」防呆。
- **ITEMS_DB**：`game_state.gd` 加 `nightclub_staff_pass`（`category=key_item`、`stackable=false`、`discardable=false`、`usable=false`、name/description 用 i18n key、icon_path 指實作者自製圖）；`items.csv` 補三語 name/desc。

#### 23-B 保全對話樹（賄賂 + 假裝身份）

- **新檔** `data/dialogue/nightclub_bodyguard.gd`，註冊進 `dialogue_db.gd`。
- **`start` 路由**：`passed_nightclub_security == true` → 已放行的中性招呼（不再給解法選項）；否則進保全攔阻入口，列出可走的解法選項。
- **賄賂分支**：
  - gate（成交分支只在 credits 足夠時出現）：
    ```gdscript
    {"condition": {"type": "credits", "op": ">=", "value": 500}, "target": "bribe_accept"}
    ```
  - `bribe_accept` effect：
    ```gdscript
    [
        {"op": "add_credits", "value": -500},
        {"op": "set_flag", "key": "passed_nightclub_security", "value": true}
    ]
    ```
  - credits < 500：成交選項不出 / 走 `bribe_reject` 冷回絕，不扣款、不設旗標、可重試。
- **假裝身份分支**：
  - gate：
    ```gdscript
    {"condition": {"flag": "found_staff_pass", "op": "==", "value": true}, "target": "bluff_pass"}
    ```
  - `bluff_pass` effect：`set_flag passed_nightclub_security = true`。
  - 無 `found_staff_pass`：走 `bluff_busted` 被識破回絕，不設旗標、可重試。
- **引開提示**：保全對話可給一句暗示「去碰那台酒水機器人」的 flavor，但**引開 / 潛行邏輯不在對話樹**，在 level（23-C）。
- **DialogueRunner 補 `credits` condition**（最小 op，`_eval_condition_dict` match 加一支）：
  ```gdscript
  "credits":
      current_val = GameState.get_credits()
  ```
  （`target_val` 走 `cond.value`，沿用既有 `>=` 數值比較分支。）
- **i18n**：保全 speaker / 各節點 text / choices key 化進 `dialogue.csv` 三語；文案由實作者自行發揮，守前提硬規則（不點名七號 / 林霏 / 妹妹）。

#### 23-C 引開→潛行 transient（兩步組合）

- **`bar_bot` 互動**：門面廳 `_trigger_interaction()` 加 `bar_bot_distract` 分支 → 觸發騷動：保全 sprite **modulate alpha tween 淡出**（離崗演出，非位移瞬移）+ 設場景變數 `_bodyguard_off_post = true`。可顯示騷動短訊息。
- **離崗語意**：`_bodyguard_off_post` 為**場景內 transient 變數**（非 `story_flags`、不存檔）。保全離崗後，**同一次進場內**不歸位（玩家還在本圖，保全都不回來）；只有玩家**離開門面廳再從別圖回來**（`_ready` / `set_entry_point` 重建場景）時，若 `passed_nightclub_security` 仍為 false 才以「在崗 + 不透明」初始化（歸位）。
- **潛行成功**：離崗空窗期 E `back_door` → 走 23-A gate retrofit 的 `_bodyguard_off_post` 分支 → set `passed_nightclub_security` + 轉場。**潛行不可單獨**——未引開（`_bodyguard_off_post == false`）時 `back_door` 仍被保全擋下（走 blocked message）。
- **不存檔**：`_bodyguard_off_post` 不進 `to_save_dict`；存檔只記 `passed_nightclub_security`（已成功）/ `found_staff_pass` / 物品。

#### 23-D 回歸 + 存讀檔 + GUI 走查護欄

- `test_runner.gd` Phase 23 區（變數加 `_phase23` 後綴避免衝突）：
  - 場景接線：未通過時 `back_door` 不轉場（gate false）；set `passed_nightclub_security` 後可反覆轉場。
  - 工牌：`add_item("nightclub_staff_pass")` 成功 → `found_staff_pass` true；背包滿模擬 → add_item false、不設 flag、不掉件。
  - 賄賂：credits=499 無成交分支 / credits=500 成交後 `get_credits()` 減 500 + 旗標 true；回絕路線不扣款不設旗標。
  - 假裝身份：持 `found_staff_pass` 可過、無則被識破；過後設旗標。
  - 潛行：`_bodyguard_off_post=true` 時 `back_door` 設旗標 + 轉場；false 時被擋；transient 不入存檔。
  - `credits` condition：`>=` 比較對 `get_credits()` 生效。
  - round-trip：`passed_nightclub_security` / `found_staff_pass` / `nightclub_staff_pass` 持有 save→load 保留、缺鍵→false、reset 歸零；M1 特殊道具進度因取得工牌打勾、不退勾。
  - Phase 1~21 不退化，尤其 Phase 21 `echo_linfei` 採集 / 後場殘響房可達。
- GUI / 純觸控：大門口刷閘導向側門；四解各自可玩（賄賂足 / 不足、假裝身份有 / 無工牌、引開→潛行兩步、淡出演出）；保全淡出 / 歸位視覺成立；BtnE / 對話選項 / examine 純觸控可推進。

### Act 3→4 七號小爆點（Phase 24，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 24`；操作清單見 `測試指南.md > Phase 24`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §7.2（七號事件三分支）。
> **核心**：v2.3 §7.2 三分支（D 和平 / A 攔下 / B 部分攔下，**無 C**）quest 化。沿用既有 QuestManager + 七號 NPC（聚落右圖）+ Trust/Trace + Phase 12 跳躍 + Phase 7 攀爬；**新增 1 個追逐場景 `tunnel_chase` + 1 個目的地選單對話 `travel_deep_tunnel` + 1 個 quest 檔**；聚落右圖深隧道口從「直通 `tunnel_combat`」改「看 `deep_tunnel_opened` 路由」。Branch B 後果（小岑暴露）餵 Phase 26-D / 28-C。
> **拍板（2026-06-27）**：分支判定公式、旗標語意命名、深隧道兩去處選單、追逐關地形全部定死於本契約（取代規格書原薄殼）。

#### 前提（硬規則）

1. **無 C 分支 / 不做大災難線**（§7.2）：七號背叛最壞只到 Branch B（小岑被犧牲），**不導致主線斷裂**；「聚落焚毀」屬 Expose 結局層（§8），不在本 Phase。
2. **D 窗口只在 Phase 20**：和平線（`seven_peace_branch_d`）只能在 Phase 20 事件爆發前達成；本 Phase 爆發後不再給 D（沿用 20-A「事件尚未爆發」gate）。持回執卻沒在 Phase 20 走 D ＝ 錯過窗口，本 Phase 不補救。
3. **七號是人類，只可智取**：追逐＝對話 / 威嚇 / 交易 + 翻越移動，**不引入殺死 / 格式化七號的路徑**（沿用 13-C 精神）。
4. **小岑代價是純敘事**：Branch B 的「聲紋暴露」＝旗標 + 場景演出（帳篷空了），**無 minigame / 無可玩潛行防守關**。
5. **分支由旗標確定性計算**：A/B/D 全可 headless 重現（不依賴手感 / 戰鬥輸贏），供 24-E round-trip 驗證。

#### 現況基準（沿用，不重造）

- `scenes/levels/underground_settlement/underground_settlement_right.gd`：`_trigger_interaction()` 的 `deep_tunnel` 分支**現為無條件** `scene_transition_requested("tunnel_combat", "from_settlement", {})`。`NpcSeven`（`dialogue_id="seven"`）+ 伍姐（`dialogue_id="wu"`）已在場（`underground_settlement_right.tscn`）；`SpawnPoints/from_deep_tunnel` marker 已存在（追逐回程落點）。`MESSAGES["deep_tunnel"]`「封住·維修中」文案目前為 dead（match 未列入）——本 Phase **不啟用該文案**（深隧道平時仍直通 tunnel_combat）。
- `scenes/levels/underground_settlement/underground_settlement.gd`：`MESSAGES["empty_tent"]`「空帳篷…被踩扁的濾水包…離開得很急」**已存在**（Branch B 帳篷空了直接複用）；小岑 NPC 在此圖（左聚落）。
- `tunnel_combat`（Phase 18 Act 2B 戰鬥區，取回執）：entry `from_settlement` ← 深隧道口；出口回 `underground_settlement_right:from_deep_tunnel`；`can_save_here=false`。**本 Phase 不動其接線**，只在深隧道口外加一層選單。
- `QuestManager`：`start` / `advance` / `complete` / `fail`；quest 資料檔比照 `data/quests/repair_vendor_bot.gd`（step / status / 工作筆記模板 + resolver）。
- `GameState`：`get_trace()` / `add_trace(int)`；`get_trust(target)`＝`affinity_<target>`（int）；`add_int(key, delta)`（暖向選項既用）；`get_flag` / `set_flag`；`add_item` 回 bool。
- `DialogueRunner`：`travel` effect op 已支援（範例 `data/dialogue/travel_street_east.gd`）；condition / effect 清單見 Phase 23 契約「現況基準」。
- `passed_nightclub_security`（Phase 23）/ `seven_peace_branch_d` / `seven_betrayal_pending`（Phase 20）/ `affinity_seven`（trust adapter target `seven`）皆既有。

#### 旗標 / 資料（canonical）

| key | 型別 | 語意 | 設於 | 後續回收 |
|---|---|---|---|---|
| `seven_betrayal_triggered` | bool / `story_flags` | 七號事件已爆發 | 24-A 爆發 | 去重 + 關閉 D 窗口 |
| `seven_betrayal_pending` | bool / `story_flags` | 非 D，待 A/B 判定（Phase 20 已定）| 24-A（非 D 時）| 24-B 判定 |
| `seven_stopped_full` | bool / `story_flags` | **＝§7.2 Branch A（成功攔下）** | 24-C 攔截結算 | 結局染色 |
| `seven_stopped_partial` | bool / `story_flags` | **＝§7.2 Branch B（部分攔下）** | 24-C 攔截結算 | **Phase 26-D 檔案重標記呈現** |
| `cen_voiceprint_exposed` | bool / `story_flags` | 小岑聲紋暴露（Branch B 代價）| 24-C 結算（與 partial 同設）| **Phase 28-C 小岑永遠缺席** |
| `deep_tunnel_opened` | bool / `story_flags` | 深隧道已開放（七號逃過後）| 24-C 七號逃走 | 深隧道口路由（兩去處選單）|
| `tunnel_chase_true_exit` | int / 獨立存讀檔欄位（**新**）| 房間2三出口的真出口（0/1/2）| **`reset_for_new_game()` 隨機骰一次** | 房間2出口判定；讀檔 / 重進 / 折返不重骰 |

> **追逐關 transient 狀態**（**不**納存讀檔，追逐中 `can_save_here=false`，只需跨房間1↔2 轉場存活、追逐開始重置）：全域倒數剩餘秒數、已試錯誤口集合（per-run，供「已知死路」短訊息）、通話接通 %。放 GameState transient 變數（`_tunnel_chase_*` 前綴）或追逐 controller，**不寫 `story_flags` / save dict**。

> **不另開 `act4_file_remarked`**：Phase 26-D 直接讀 `seven_stopped_partial`（避免 key 增生）。`TRACE_BETRAYAL_THRESHOLD`（第一版 = 3，可調）建議定為 `data/quests/seven_betrayal.gd` 的 const。

#### 24-A 七號事件 quest 化 + 爆發 gate

- **新檔** `data/quests/seven_betrayal.gd`（比照 `repair_vendor_bot.gd`）+ 註冊 `data/quests/quest_db.gd`；含三分支工作筆記模板 + resolver。
- **爆發 gate**：進聚落右圖時（`set_entry_point` / `_ready` 末尾）檢查——
  ```gdscript
  func _maybe_trigger_betrayal() -> void:
      if not GameState.get_flag("passed_nightclub_security", false):
          return                                   # 夜總會未通＝Act 3 未完，不爆
      if GameState.get_flag("seven_betrayal_triggered", false):
          return                                   # 已爆，去重
      if GameState.get_flag("seven_peace_branch_d", false):
          return                                   # D：和平已成，不爆發（可選低壓招呼）
      GameState.set_flag("seven_betrayal_triggered", true)
      GameState.set_flag("seven_betrayal_pending", true)
      QuestManager.start("seven_betrayal")
      # → 觸發七號攔截對話 / 逃走演出
  ```
  flag-based ＝ 與探索順序無關（爆點前在聚落 / 深隧道亂逛 N 次都不武裝）。

#### 24-B 分支判定 + 伍姐 / 小岑提前警告

- **判定公式**（攔截結算時，24-C 內計算）：
  ```gdscript
  var got_advance_warning := GameState.get_trust("wu") >= 2 or GameState.get_trust("cen") >= 2
  var trace_too_high := GameState.get_trace() >= SevenBetrayal.TRACE_BETRAYAL_THRESHOLD
  # seven_stopped_partial = trace_too_high and not got_advance_warning   # §7.2 B
  # seven_stopped_full    = 其餘                                          # §7.2 A
  ```
  語意：留太多殘響（Trace 高）→ 七號更易拼出網；但與伍姐 / 小岑「還得夠」（affinity ≥ 2）→ 提前警告 → 趕得上 ＝ A。`>= 2` 沿用既有 `affinity_*` 慣例，不新建 Trust 軸。
- **提前警告分支**：`data/dialogue/wu.gd` / `data/dialogue/cen.gd` 各加一條 `affinity ≥ 2` gate 的警告分支（condition `story_flag affinity_wu / affinity_cen >= 2`），台詞暗示「七號最近在打聽藏身網 / 賣什麼」；**僅敘事鋪陳**——`got_advance_warning` 在公式裡直接讀 affinity，不依賴玩家是否看過該分支（保持確定性）。

#### 24-C 深隧道追逐 + 兩去處選單接線

- **深隧道口路由 retrofit**（`underground_settlement_right.gd` 的 `deep_tunnel` 分支改為）：
  ```gdscript
  "deep_tunnel":
      if GameState.get_flag("deep_tunnel_opened", false):
          interaction_requested.emit({"type": "dialogue", "dialogue_id": "travel_deep_tunnel"})
      else:
          scene_transition_requested.emit("tunnel_combat", "from_settlement", {})
  ```
- **目的地選單** `data/dialogue/travel_deep_tunnel.gd`（比照 `travel_street_east.gd`）：兩個 `travel` effect choice → ① `{"op":"travel","scene_id":"tunnel_combat","entry_point_id":"from_settlement"}` ② `{"op":"travel","scene_id":"tunnel_chase","entry_point_id":"from_settlement"}`；i18n key 入 `dialogue.csv` 三語。
- **追逐關＝限時選門迷宮，切兩室**（2026-06-28 重設計，取代原單室跳躍翻障礙版；新 `.tscn` + `.gd` 各兩支）。SceneRegistry 註冊 `tunnel_chase`（房間1，entry `from_settlement`）+ `tunnel_chase_right`（房間2，entry `from_left`）；兩室 `can_save_here=false`（沿用 7-F 禁存）。背景：`assets/generated/maps/tunnel_chase/tunnel-chase-left.png`（房間1）/ `tunnel-chase-right.png`（房間2）。
  - **房間1（`tunnel_chase`）出口**：3 個互動口——①高處需爬樓梯/梯子的**門＝固定正解** → `scene_transition_requested("tunnel_chase_right","from_left",{})`；②③地面相鄰兩洞＝錯誤死路（見下方死路處理）。**左緣鎖住**：玩家走到最左 → MessageBox `MSG_TUNNEL_CHASE_MUST_PURSUE`「現在必須追上七號」並擋回（不轉場回聚落）。
  - **房間2（`tunnel_chase_right`）出口**：3 個互動口（index 0=中央梯子頂光洞 / 1=右上 FAN ROOM 風扇 / 2=右下 EMERGENCY EXIT 門）；`tunnel_chase_true_exit`（存讀檔 int，`reset_for_new_game()` 骰 `randi() % 3`）指定真出口 → 攤牌結算；另兩個＝錯誤死路。左緣 → `tunnel_chase:from_right`（backtrack，計時續跑，**非**錯誤口、不扣時間）。
  - **梯子攀爬＝完全沿用 Phase 7-F 防火梯那套 climb 機制**（不發明新組件）：每根可爬的梯子＝一個 `interactable_area.gd` 互動口（`interaction_id` 如 `chase_ladder_*`），場景 controller 比照 `apartment_fire_escape.gd` 的 `_try_enter_ladder()` → `player.enter_climb_mode(ladder_x, min_y, max_y)` → `_update_climb_state()`（W/S 上下、`player.is_climbing()`、到頂/底 auto-exit）。房間1正解高門需先爬梯上去、房間2「中央梯子頂光洞」與抵達上層風扇/門的梯子皆走此機制；climb mode 僅追逐兩室使用（同 7-F「climb mode 僅本場景」精神）。平台間若有窄 gap 才用 Phase 12 跳躍。
  - **全域倒數（transient，跨兩室）＝七號交易通話進度**：總長 `TUNNEL_CHASE_TOTAL_SEC = 180`，房間1→2 轉場沿用同一剩餘時間（存 GameState transient `_tunnel_chase_time_left`，追逐開始於房間1 `_ready` 首次進入時 init、backtrack/轉場不重置）。UI 複用 23-C 紅字倒數 pattern，呈現「通話接通 %」= `int((1 - time_left/180) * 100)` + 里程碑狀態文字（`UI_TUNNEL_CALL_DIALING` / `_VERIFYING` / `_CONFIRMING` / `_CLOSING`）。
  - **錯誤死路處理**（房間1兩洞 + 房間2兩錯口，共 4 個）：互動 → 若該口未試過 → MessageBox 長版「死路」+ `_tunnel_chase_time_left -= 20`（**各口只扣一次**，加入 transient `_tunnel_chase_tried`）；已試過 → 短版 MessageBox「已知死路」、**不再扣時間**。折返原地，可重試。死路扣時不可扣成負（`max(0, ...)`）。
  - **觸發攤牌的兩條路**：① 走到房間2真出口；② `_tunnel_chase_time_left` 歸零（任一時刻檢查）。兩者皆 → 播七號攤牌情境圖 overlay（複用 23-C `set_monologue_active` + 鎖操作 + 訊息框；圖 `assets/generated/sprites/seven_tunnel_showdown/situation/`）→ 結算 → 轉回 `underground_settlement_right:from_deep_tunnel`。**倒數歸零＝純張力**，攤牌文字框架成「接通瞬間你撲上去打斷」，**分支結果不變**（恆由 24-B 公式定）。攤牌文字需 A 版 / B 版兩段（D 玩家不進此追逐），讀已結算的 `seven_stopped_full` / `seven_stopped_partial` 分流。
  - **不新增戰鬥機制**；核心是限時選門，移動到出口才用既有 12/7。
- **七號逃走觸發**（爆發對話收尾 / 七號往深隧道跑的演出末）：`GameState.set_flag("deep_tunnel_opened", true)` 並把玩家帶進 `tunnel_chase`（首次追擊）；之後深隧道口常駐兩去處選單。
- **攔截結算**（攤牌觸發時呼叫＝房間2真出口 or 倒數歸零，可 headless；公式即 24-B，與追逐表現/剩餘時間無關）：
  ```gdscript
  func _resolve_seven_branch() -> void:
      var got_warning := GameState.get_trust("wu") >= 2 or GameState.get_trust("cen") >= 2
      var trace_high := GameState.get_trace() >= SevenBetrayal.TRACE_BETRAYAL_THRESHOLD
      if trace_high and not got_warning:
          GameState.set_flag("seven_stopped_partial", true)
          GameState.set_flag("cen_voiceprint_exposed", true)   # B 代價
          GameState.add_int("affinity_seven", -1)
          GameState.add_int("affinity_wu", -1)
      else:
          GameState.set_flag("seven_stopped_full", true)
          GameState.add_int("affinity_seven", -2)
      GameState.set_flag("seven_betrayal_pending", false)
      QuestManager.complete("seven_betrayal")
  ```
  Trace 三分支皆不動（不呼叫 `add_trace`）。

#### 24-D Branch B 代價（小岑暴露演出）

- `cen_voiceprint_exposed`（24-C 結算已隨 partial 設）→ 聚落左圖 `underground_settlement` 進場時**條件式移除小岑 NPC**（`queue_free` / `visible=false` + 互動關閉），改為玩家可 examine 既有 `empty_tent`「帳篷空了 / 踩扁零件」。
- A / D 時小岑照常在場、`empty_tent` 不可 examine（維持原 NPC 對話）。
- **不新增美術 / 不新增旗標**：複用既有 `empty_tent` message 與小岑 NPC 節點；Phase 28-C 小岑條件式回收讀 `cen_voiceprint_exposed`。

#### 24-E 回歸 + 存讀檔

- `test_runner.gd` Phase 24 區（變數加 `_phase24` 後綴避免衝突）：
  - 爆發 gate：未通夜總會（`passed_nightclub_security` false）不爆；D 玩家（`seven_peace_branch_d`）不爆；非 D 首進聚落右圖設 `seven_betrayal_triggered` + `seven_betrayal_pending`；重進不重複爆發。
  - 分支判定：`trace < 3` / `trace >= 3 且有警告` → `seven_stopped_full`；`trace >= 3 且無警告` → `seven_stopped_partial`（+ `cen_voiceprint_exposed`）；兩旗標互斥；邊界值（trace = 2 / 3）正確分流。
  - affinity：A → `affinity_seven` −2；B → `affinity_seven` −1 + `affinity_wu` −1；D 不動。
  - 深隧道口：`deep_tunnel_opened` false → 直通 `tunnel_combat`；true → `travel_deep_tunnel` 選單；選單兩去處皆可達；`tunnel_chase` 載入 + 雙向 round-trip。
  - Branch B 演出：`cen_voiceprint_exposed` true → 聚落小岑不在場 + `empty_tent` 可 examine；A/D → 小岑在場。
  - round-trip：`seven_betrayal_triggered` / `seven_stopped_full` / `seven_stopped_partial` / `cen_voiceprint_exposed` / `deep_tunnel_opened` / `affinity_seven` save→load 保留、缺鍵→false / 0、reset 歸零。
  - Phase 1~23 不退化（尤其 Phase 21 `echo_linfei` 採集 / 後場殘響房、Phase 23 四解、`tunnel_combat` 入口仍可達）。
- **外部素材：已全數到貨（2026-06-28）**——`tunnel-chase-left.png` / `tunnel-chase-right.png`（`assets/generated/maps/tunnel_chase/`）+ 七號攤牌情境圖（`assets/generated/sprites/seven_tunnel_showdown/situation/`）。NPC 立繪 / 對話皆已到貨。
- GUI / 純觸控：三分支各自可玩；追逐關限時選門可玩（倒數/通話接通 % 顯示、走錯扣時、已知死路短訊息、房間1左緣鎖住、房間2出口需攀爬抵達）；真出口 / 倒數歸零皆觸發攤牌情境圖；深隧道口兩去處選單；Branch B 帳篷空了演出成立；BtnE / 對話選項純觸控可推進。

### Act 4 備份區 + 戰鬥③（Phase 25，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 25`；操作清單見 `測試指南.md > Phase 25`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §7 五幕表（Act 4）+ §6（阿達線鎖定）+ §8（三結局共用觸發點）。
> **核心**：建 Act 4 climax 三場景骨架（`datacenter_entrance`＝anchor-05 + `datacenter_backup` + `datacenter_backup_core`）+ 戰鬥③「跑到門」。**純複用既有系統**：travel 目的地選單（9-C）、interactable / scene router、Phase 13 戰鬥組件（機器格式化 + 人類不可殺）。**唯一新做＝人類保全最小追擊 AI（朝玩家 x 移動）**。所有演出（晚拉扯、阿達④、真相碎片、結局觸發點接線）＝Phase 26；三結局＝27–29。**本 Phase 不寫結局分支、不碰 Trace、無新存讀檔欄位。**

#### 場景（25-A）

- 三場景 SceneRegistry 註冊（scene_id + music_id + entry points），見上方 `Act 2/3/4 場景一覽表`。
- `datacenter_entrance`：用 anchor-05；大門互動 = 善後員門禁 gate（見下）；其餘 examine 為 flavor。
- `datacenter_backup`：伺服器陣列廊道；戰鬥③ 掛點；右端門按 E（`exit_to_core` 互動）→ 轉場 `datacenter_backup_core`。
- `datacenter_backup_core`：核心；放「你自己的備份」Area2D 互動掛點，本 Phase 只給中性 `interaction_requested` message（佔位 key 如 `MSG_DATACENTER_OWN_BACKUP_PLACEHOLDER`），**不接結局**（26-D 才接）；`can_save_here=true`。

#### 入口動線 + travel gate（A2）

- `nightclub_entrance` 加 travel 目的地「AI 資料中心」（沿用 `data/dialogue/travel_*.gd` 目的地選單 pattern）→ `{"op":"travel","scene_id":"datacenter_entrance","entry_point_id":"from_nightclub"}`；i18n key 入 `dialogue.csv` 三語。
- **travel gate（純讀既有終端旗標、零新旗標、不改 Phase 24）**：
  ```gdscript
  func _act4_unlocked() -> bool:
      if not GameState.get_flag("passed_nightclub_security", false):
          return false
      return GameState.get_flag("seven_peace_branch_d", false) \
          or GameState.get_flag("seven_stopped_full", false) \
          or GameState.get_flag("seven_stopped_partial", false)
  ```
  D/A/B 三線皆可達且強制 Act 3 已通；未滿足時 travel 選單不出「AI 資料中心」項（或出灰項給提示）。
- `nightclub_entrance` 補一個 `from_datacenter` 回程落點。

#### 善後員合法門禁 gate（A3，反諷）

- `datacenter_entrance` 大門互動分派：
  ```gdscript
  if GameState.has_item("old_work_badge") and GameState.get_flag("read_old_work_order", false):
      # 放行：scene_transition_requested("datacenter_backup", "from_entrance", {})
  else:
      # 中性「權限不足」訊息（key MSG_DATACENTER_ACCESS_DENIED），不轉場
  ```
- 兩條件皆既有：`old_work_badge`＝公寓開場 key item（`discardable:false` / key_item 不可賣，**永久持有、無 soft-lock**）；`read_old_work_order`＝19-B 揭露旗標。**19-B 因此自動成 Act 4 硬前置。**
- **晚 flavor 演出（不參與 gate）**：travel 到資料中心時插一段晚台詞，提醒玩家「再去看一眼那張舊卡」（引導 R 查看 badge 揭露版描述）；新 dialogue 片段或 STORY_MESSAGES + 三語；晚台詞晚出 / 沒出 / 玩家略過皆不影響開門。鋪 Phase 26 晚拉扯。

#### 戰鬥③（25-B，低階保全，跑到門）

- 場景 `datacenter_backup` 混合敵人：
  - **機器哨兵**＝`machine_enemy`（沿用 Phase 13：`can_format` stun 中背後 true、`format_reset` 按住 E → `defeated()`），擋路、可清。
  - **人類保全**＝`human_enemy`（`can_format` 恆 false、`apply_stun` no-op，**不可殺**），**新增最小追擊**：`_physics_process` 朝 `player.global_position.x` 走（速度常數、夾在場景邊界、走 walk line），兌現 Phase 13 後置的「察覺 / 追擊」；碰到玩家＝knockback / 短 stagger，不抓不死。
- **勝利條件＝抵達 `datacenter_backup` 右端門按 E 進門**（`exit_to_core` interactable，與 tunnel_combat / 24-C 出口同型的 E 互動出口 → `scene_transition_requested`）→ 轉場 `datacenter_backup_core:from_backup`。
- **無失敗態**（B3）：不設 combat_loss / 不送回安全點岔線 / 無 Game Over；唯一進度＝跑到門。`player.combat_mode` 沿用 Phase 18 契約（attack gate + `_e_reserved()` E 優先鏈）。
- 戰鬥廊道 `can_save_here=false`（沿用戰鬥 / 7-F 禁存慣例）；離開戰鬥進核心恢復 `true`。

#### 存讀檔 / test_runner（25-C）

- **無新存讀檔欄位**：travel gate / 門禁 gate 全讀既有旗標（`passed_nightclub_security` / `seven_*` / `read_old_work_order`）與既有 item（`old_work_badge`）；場景到訪走既有 `visited_scenes`（M1）。
- `test_runner.gd` Phase 25 區（變數加 `_phase25` 後綴避免衝突）：
  - 三場景載入 + entry point + 全鏈雙向 round-trip（entrance↔backup↔core + entrance↔nightclub）。
  - travel gate：未通 Act 3（`passed_nightclub_security` false）不可達；D / A / B 三終端旗標各自可達。
  - 門禁 gate：缺 `read_old_work_order` 擋；`has_item("old_work_badge")` + `read_old_work_order` 放行。
  - 戰鬥③：機器哨兵可格式化、人類保全 `can_format` false / `apply_stun` no-op；右端門 E 互動 → 轉場核心；非戰鬥場景 attack 無副作用。
  - 核心「自己的備份」掛點 examine 給中性佔位（不觸發結局）。
  - Phase 1~24 不退化。

#### 素材（**已全數到貨，2026-07-02**）

- map 兩張（AI 區視覺紀律＝halftone 極少 / 無、橙＝唯一暖色，見 Art Bible）：`assets/generated/maps/datacenter_backup/datacenter_backup-20260629-224400.png` + `assets/generated/maps/datacenter_backup_core/datacenter_backup_core-20260630-073049.png`。
- 機器哨兵 sprite：`assets/generated/sprites/phase25_rack_sentinel/`（`hidden_idle/idle.png` 機架潛伏 + `horizontal_move/horizontal_move.png` 水平移動）；人類保全 sprite：`assets/generated/sprites/phase25_human_security_guard/idle/idle.png` + `walk/walk.png`（concept 圖在 `sprites/phase25_enemy_concepts/`）。
- Act 4 專屬 BGM 兩軌（`assets/bgm/`，2026-07-02 到貨）：**`Heartbeat of the Machine.mp3`＝戰鬥③ `datacenter_backup`**；`The Cold Mirror (Loop).mp3`＝`datacenter_entrance` / `datacenter_backup_core`。
- `datacenter_entrance` 用既有 anchor-05，無需新圖。

### Act 4 演出 + 真相碎片 + 結局觸發點（Phase 26，實作契約）

> **狀態：✅ 已完成**（headless PASS，commits `2bc4da7` / `003db3b` / `d31f4a0`）。以下為實作契約存檔，供回查。
> 驗收意圖見 `遊戲規格書.md > Phase 26`；操作清單見 `測試指南.md > Phase 26`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §4.3（晚拉扯拍板台詞）+ §3（四段式④拍板台詞）+ §7（Act 4 真相碎片）+ §8（三結局共用觸發點）+ §7.2（Branch B）。
> **核心**：在 Phase 25 三場景上掛全部 Act 4 演出。**零新系統**：對話（Phase 5/16）、`MemoryFragmentArea`（Phase 14 同款）、examine 條件變體（Phase 19 同款）、走近自動觸發 Area2D（body_entered 一次性，與 MemoryFragmentArea 同模式）。**本 Phase 不寫結局分支、不開選單、不碰 Trace、零新存讀檔 schema（4 旗標全走 story_flags）。**（2026-07-02 討論定案。）

#### 場景佈點（`datacenter_entrance`，可走 x = 50–1320）

entrance 既有參照：落點 `from_nightclub` x=120 / ExitToNightclub x≈90 / DeliveryBot x≈500 / MainGate x≈688。

| 節拍 | 建議佈點 | 節點 |
|---|---|---|
| 26-B 阿達④ | x≈300（落點與 DeliveryBot 之間） | `AdaNPC`（Area2D + Sprite2D，Phase 16 NPC 同款）＋ `AdaTriggerArea`（Area2D body_entered） |
| 26-A 晚拉扯 | x≈620（MainGate 前必經） | `WanDatacenterNPC`（同上，複用既有晚 idle sprite / 立繪）＋ `WanPullTriggerArea` |

兩觸發區不重疊。帶工單來的玩家常同一趟連吃兩拍（阿達→晚），中間隔 ~300px + DeliveryBot flavor，屬設計內節奏。

#### 26-A 晚拉扯

- 新對話樹 `data/dialogue/wan_datacenter.gd`（id `wan_datacenter`）註冊 `dialogue_db.gd`；start 條件路由：
  - `wan_act4_pull_seen` false → `pull` 主線：§4.3 兩句拍板台詞（活人安全版＋私心版）**一次給足、不設 affinity 門檻**；結尾 effect `{"op":"set_flag","flag":"wan_act4_pull_seen"}`。
  - true → `retalk` 短版（中性一兩句，5-D 慣例）。
- 觸發：`WanPullTriggerArea` body_entered（玩家）且 `UIMode.mode == NONE` 且未看過 → 走 Level→Main(`start_dialogue`) 標準接線；看過後觸發區停用，晚本體保留標準 `dialogue_id` E 互動。
- 拉扯**不參與門禁 gate**（25-A 契約不動）；看完即可過門。

#### 26-B 阿達④

- 新對話樹 `data/dialogue/ada.gd`（id `ada`）：一拍、無選項——拍板句「你忘了啊？也好。至少有人可以忘」＋少量補白；結尾 effect `{"op":"set_flag","flag":"ada_final_words_seen"}`。
- 觸發 gate：`AdaTriggerArea` body_entered 且 `get_flag("read_old_work_order")` 且 `ada_final_words_seen` false（**順序護欄：④不得早於③**；缺工單本趟不登場、下趟補，門禁本來就要工單故必然補到）。
- 離場：對話結束（UIMode 回 NONE 且旗標已設）→ Tween modulate 淡出 → 停用 `AdaNPC` + 觸發區。**永久消失**：`ada_final_words_seen` true 時 `_ready` 直接不生成 / 隱藏。
- 素材（g2d 生成，26-B 動工前補）：世界 sprite idle（`assets/generated/sprites/ada/idle/`）＋對話立繪（`assets/generated/sprites/ada/dialogue_portrait/`）；離場用 fade **不需 walk**。

#### 26-C 真相碎片

- `datacenter_backup_core` 備份主機**前必經路徑**掛 `MemoryFragmentArea`（Phase 14 同款）：id `mem_frag_chose_deletion`，走近自動收取 → messagebox（key `MSG_MEM_FRAG_CHOSE_DELETION`）＋ set 同名旗標；已收取重入不觸發。不用 `trace_on_collect` / `require_flag`（core 已是最深處）。
- 文案方向：真相＝「你自己選擇被刪、是為了保護那個網」；回收 `mem_frag_commute_topside`（往上＝去服務富人）與 `mem_frag_hideout`（你看到的是黑戶藏身方式）兩碎片意象（純文字回連、不作 gate）。
- 空間順序（碎片先於觸發點）＋ 26-D 的旗標 gate＝雙保險。

#### 26-D 結局觸發點武裝 + Branch B 檔案重標記

- **自己的備份**（25-A 既有互動物，改分派）：
  ```gdscript
  if GameState.get_flag("mem_frag_chose_deletion", false):
      # 重量級 examine（key MSG_DATACENTER_OWN_BACKUP_TRUTH；名字露出一次，§4.1 名字不對稱）
      # + GameState.set_flag("stood_before_own_backup", true)  # 冪等、可重看
  else:
      # 維持 25-A 中性佔位 MSG_DATACENTER_OWN_BACKUP_PLACEHOLDER，不 set 旗標
  ```
  **不開任何選單**（4-G 教訓：選單形式由 27-B 一次定死）。`stood_before_own_backup`＝**forward 契約：27-B 三結局路由掛同一互動物 + 此旗標**。
- **檔案索引終端**（core 新增 examine 互動物）：`seven_stopped_partial` false → 中性 flavor（key `MSG_DATACENTER_FILE_INDEX_NEUTRAL`，備份區檔案流水）；true → 變體（key `MSG_DATACENTER_FILE_INDEX_REMARKED`）＝**小岑聲紋檔具體標紅「待清理」＋一批未具名黑戶檔案被重標記**（v2.3 §12 開放問題 5，2026-07-02 拍板；背叛要具體 §4.2.4；不點名伍姐／七號／林霏）。可重看、不 set 旗標。
- Branch B 變體**不寫進**自己備份的 examine：「小岑被連累」與「你自己的真相」分屬兩互動物，避免互鈍化。

#### 旗標（全走 `story_flags`，零新 schema）

| key | 型別 | 語意 | 寫入點 | 下游 |
|---|---|---|---|---|
| `wan_act4_pull_seen` | bool | 晚拉扯已看 | 26-A 對話 effect | 26-A retalk 路由 |
| `ada_final_words_seen` | bool | 阿達④已登場 | 26-B 對話 effect | 26-B 一次性 / 重進不生成 |
| `mem_frag_chose_deletion` | bool | 真相碎片已收 | 26-C 收取 | **26-D 武裝條件** |
| `stood_before_own_backup` | bool | 已站上結局觸發點 | 26-D examine | **forward 契約：27-B 三結局路由** |

只讀不寫：`read_old_work_order`（26-B gate）、`seven_stopped_partial`（26-D 終端變體）。

#### i18n / 訊息 key

- 新 MSG key（`data.csv` 三語，M2 慣例）：`MSG_MEM_FRAG_CHOSE_DELETION` / `MSG_DATACENTER_OWN_BACKUP_TRUTH` / `MSG_DATACENTER_FILE_INDEX_NEUTRAL` / `MSG_DATACENTER_FILE_INDEX_REMARKED`。
- 新對話樹 `wan_datacenter` / `ada` 全文入 `dialogue.csv` 三語。
- §4.3 / §3 拍板句直接用；其餘補白文案實作時定稿。

#### test_runner（26-E，變數加 `_phase26` 後綴避免衝突）

- 26-A：觸發（模擬 body_entered 或直呼 level 觸發函式）→ 旗標寫入；重入 / 旗標已設不重播；retalk 路由正確；不擋門禁。
- 26-B：缺 `read_old_work_order` 不觸發；齊全觸發＋旗標；`ada_final_words_seen` true 重進場景不生成。
- 26-C：必經自動收取＋旗標；重入不重複。
- 26-D：碎片前備份 examine＝中性、不 set 旗標；碎片後＝TRUTH＋set `stood_before_own_backup`；終端依 `seven_stopped_partial` 兩版正確分派。
- 26-E：四旗標存讀檔 round-trip；26-A/B 一次性跨存讀不重播；Phase 1~25 不退化。

### 地下廣播站 + 結局路由 + 清洗閘（Phase 27，實作契約）

> **狀態：✅ 27-A~D 全部已完成**（27-A commit `7941519`；27-B/27-C/27-D 本次實作——`data/dialogue/own_backup.gd` + `broadcast_upload.gd` + `datacenter_backup_core.gd` 四層分派 + `broadcast_station.gd` 上傳終端 gate + `test_runner.gd` Phase 27-B/27-C/27-D 區塊，headless 本機待跑）。
> 驗收意圖見 `遊戲規格書.md > Phase 27`；操作清單見 `測試指南.md > Phase 27`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8（三結局 + Expose 清洗）+ §6（清洗定義）+ §4.1（名字不對稱）；跨階段判定語意見本檔 `> 三結局路由（Phase 27–29）`（判定順序 C→A/B 不變，**臨界值與判定實作＝Phase 29**）。
> **核心**：27-B 兌現 26-D forward 契約（三選對話掛「自己的備份」同一互動物）；27-A 新建 `broadcast_station`；27-C 清洗第一次硬兌現（11-D 移入）。**本 Phase 不碰 Trace、不做 A/B/C 判定、不做結局演出。**（2026-07-02 討論定案；v2.3 §12 開放問題 1「升正式場景」/ 3「不硬鎖」同日拍板。）

#### 27-A `broadcast_station` 場景

- `scenes/levels/broadcast/broadcast_station.tscn` + `broadcast_station.gd`（level controller 慣例：`current_interactable_changed` / `interaction_requested` / `scene_transition_requested` signal 接 Main，4-C contract）；`main.gd` SceneRegistry 註冊 `broadcast_station`，唯一 entry `from_backup_core`。
- 單室；**無回資料中心出口互動物**（單向，唯一前進＝上傳終端）；`SaveSystem.can_save_here = true`（2026-07-02 user 拍板可存檔）；map 圖已到貨（`assets/generated/maps/broadcast_station/broadcast_station-20260702-234241.png`，2026-07-02），開工時定相機 clamp（單室慣例）。
- 進場演出：entry `from_backup_core` 進場自動播一次性 MessageBox（key `MSG_BROADCAST_ARRIVAL`，交代空間跳躍——「帶著拷貝離開設施」方向，實作時定稿）。**一次性由動線天然保證**：單向且無二次入口＝只會進場一次；讀檔回場走 6-B 直接落座標、不觸發 entry 演出，**不需去重旗標**。
- 互動物：`UploadTerminal`（`dialogue_id = "broadcast_upload"`，27-C）＋ 2–3 個 flavor examine（key `MSG_BROADCAST_FLAVOR_*`，發射設備 / 舊錄音載體 / 前人痕跡方向——暗示有人維護但不在場，**不放 NPC**）。
- BGM：新軌已到貨（`assets/bgm/The Yellow Light Tape.mp3`，user 委製，2026-07-02），場景自管 BGM 慣例掛載。

#### 27-B 三選對話樹 `own_backup`

- 新 `data/dialogue/own_backup.gd`（id `own_backup`）註冊 `dialogue_db.gd`；`dialogue.csv` 三語。
- `datacenter_backup_core` controller「自己的備份」分派升級（疊在 26-D dispatch 之上，26-D 程式不動）：

  ```gdscript
  if 任一 ending_route_* 旗標已設:
      # 「已決定」中性 examine（key MSG_DATACENTER_OWN_BACKUP_DECIDED），不可重選
  elif GameState.get_flag("stood_before_own_backup", false):
      # start_dialogue("own_backup")  # 27-B 三選——第一次讀懂自己（26-D TRUTH），再伸手才是決定
  elif GameState.get_flag("mem_frag_chose_deletion", false):
      # 26-D：TRUTH examine + set stood_before_own_backup（不動）
  else:
      # 25-A 中性佔位（不動）
  ```

- 樹結構（節拍拍板，台詞補白實作時定稿；存檔提示拍 2026-07-03 P28 規格討論拍板回補）：
  1. 存檔提示 pre-node（一次性）：start 條件路由——`ending_save_hint_seen` 未設 → 提示 node（**文案已拍板**：「你站在自己的備份前面。也許，你也該為現在的自己留一份。（接下來的選擇無法撤回——建議先存檔。）」，三語翻譯實作時補）＋推進 effect `set_flag ending_save_hint_seen` → goto 重錨定；已設 → 直接重錨定。玩家看完可「退開」當場存檔（`datacenter_backup_core` `can_save_here == true`）再重開。
  2. 重錨定 node（短，一兩行；名字不露出）。
  3. choices：「灌回去」/「刪掉它」/「拷貝走」/「退開」（退開＝結束回 NONE、無 effect、可重開）。
  4. 各動詞 → 確認前小節 node（重量拍，不劇透結局）→ choices「就這麼做」/「回去再想」（退回第 3 拍）。
  5. 「就這麼做」effect（**鎖點，互斥旗標唯一寫入點**）：
     - 灌回去 → `set_flag ending_route_reclaim` → 短收尾 node 停原地（28-A 接演出掛點）。
     - 刪掉它 → `set_flag ending_route_protect` → 短收尾 node 停原地（28-B 接演出掛點）。
     - 拷貝走 → `set_flag ending_route_expose` → `travel` effect（既有 op）單向轉場 `broadcast_station:from_backup_core`。
- 互斥保證：入口 gate（任一 route 旗標 → DECIDED examine 不再開樹）＋ 單寫入點（各確認拍 effect）；test_runner 驗三旗標僅一真。

#### 27-C 清洗閘對話樹 `broadcast_upload`

- 新 `data/dialogue/broadcast_upload.gd`（id `broadcast_upload`）註冊 `dialogue_db.gd`；上傳終端 examine 分派：`expose_upload_done` 已設 → 中性已上傳 examine（key `MSG_BROADCAST_UPLOAD_DONE_IDLE`）不重開樹；否則開樹。
- 五拍 node 對映：
  1. 掃描 node：condition `echo_count`（新 condition type，見下）分兩檔文字變體（蒐得多 / 少；**靜態兩版文案、不做 runtime 字串插值**，M2 顯示與邏輯分離慣例；門檻 N 開工校）。
  2. 警告 node：基本版＋`seven_stopped_partial` condition 變體句（Branch B：你已見過一次「被沿線找到」的下場；不點名）。
  3. 反諷＋首選 node：清洗＝善後員的手藝（做過一萬次的動作）；choices「動手清洗」/「原樣保留」/「退開」（退開＝結束、無 effect）。
  4. 代價 node（兩版）：清洗→刪除演出（不告知刪了哪些）；原樣→靜默一拍。
  5. 最終確認 node：「開始上傳？」choices「是」/「退開」。**「是」＝鎖點**，effect 依路徑：`set_flag expose_upload_cleaned`（清洗路 true / 原樣路 false）＋`set_flag expose_upload_done`→ 中性收尾 node（上傳開始；29 判定演出接手）。退開＝清洗選擇不保留、下次重走整段——**旗標只在最終確認寫入，天然無「已清洗未上傳」中間態**。
- DialogueRunner 擴充（唯一系統擴充，Phase 23 補 `credits` condition 同例）：condition type `echo_count`＝`{"echo_count": N}`（已蒐殘響段數 ≥ N）；讀 `GameState.get_collected_echo_segment_count()`（**新只讀 API**，計 `echo_progress` 目前持有（未賣）已收 segment 總數；邊界語意開工時若有疑再校）。

#### 旗標（全走 `story_flags`，零新 schema）

| key | 型別 | 語意 | 寫入點 | 下游 |
|---|---|---|---|---|
| `ending_route_reclaim` | bool | 結局路由＝拿回自己 | 27-B 確認拍 effect | 28-A 演出掛點 |
| `ending_route_protect` | bool | 結局路由＝保住別人 | 27-B 確認拍 effect | 28-B 演出掛點 |
| `ending_route_expose` | bool | 結局路由＝刪改後公開 | 27-B 確認拍 effect | 27-A 轉場 / 29 前提 |
| `expose_upload_cleaned` | bool | 上傳檔已清洗與否 | 27-C 最終確認 effect | 29-A/B 分界 |
| `expose_upload_done` | bool | 上傳已完成 | 27-C 最終確認 effect | 29 判定觸發掛點 |
| `ending_save_hint_seen` | bool | 三選前存檔提示已看過（一次性去重） | 27-B 提示拍推進 effect | 無（僅去重；2026-07-03 拍板回補） |

三 route 旗標**互斥**（單寫入點 + DECIDED gate 保證）。只讀不寫：`stood_before_own_backup`（26-D）、`seven_stopped_partial`（警告變體）、`echo_progress`（計數染色）。**Trace 不讀不寫。**

#### i18n / 訊息 key

- 新 MSG key（`data.csv` 三語，M2 慣例）：`MSG_BROADCAST_ARRIVAL` / `MSG_BROADCAST_FLAVOR_1..3` / `MSG_DATACENTER_OWN_BACKUP_DECIDED` / `MSG_BROADCAST_UPLOAD_DONE_IDLE`。
- 新對話樹 `own_backup` / `broadcast_upload` 全文入 `dialogue.csv` 三語；三語禁字（無「林霏」）；名字不露出（§4.1，26-D 已露一次）；節拍固定、台詞補白實作時定稿。

#### test_runner（27-D，變數加 `_phase27` 後綴避免衝突）

- 27-A：SceneRegistry 載入 `broadcast_station`；`can_save_here == true`；場景內存讀 round-trip；flavor examine 派發。
- 27-B：分派鏈四層正確（佔位 → TRUTH+旗標（26-D 不變）→ 重看開三選 → DECIDED 鎖定）；首開樹先出存檔提示拍＋set `ending_save_hint_seen`、旗標已設重開直入重錨定；退開不寫 route 旗標；三路確認拍各自寫入且互斥；Expose → transition 至 `broadcast_station`。
- 27-C：退開不留狀態；清洗 / 原樣兩路 → `expose_upload_cleaned` 二態正確＋`expose_upload_done`；`echo_count` 兩檔變體分派；`seven_stopped_partial` 警告變體；上傳後終端 idle 變體不重開樹。
- 27-D：6 旗標存讀檔 round-trip（含 `ending_save_hint_seen`，讀檔後提示不重播）；三選前存檔 → 讀檔可分別走三路；Phase 1~26 不退化（尤其 26 四旗標、25 travel / 門禁、戰鬥③）。

### Reclaim + Protect 結局演出（Phase 28，實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase 28`；操作清單見 `測試指南.md > Phase 28`；敘事事實來源 `subdocs/主線/雨還沒停v2.3` §8（＋2026-07-03 加註：小岑舞台化改句、上行線復駛背景事件）。
> **核心**：零新場景、零系統擴充；結局序列＝27-B 收尾掛點觸發 → `travel` 串既有場景 + epilogue entry point 演出（4-E 慣例）→ 終站公寓寫旗標靜止（30-A 接手）。

#### 序列控制（28-A / 28-B 共用）

- 觸發：`own_backup` R / P 確認拍短收尾 node 結束 → route 旗標已設且對應 `ending_*_played` 未設 → 序列自動開始（站 1 原地演出）。27-B 的「停原地」自此改為「接序列」，27 程式不動、28 疊掛。
- **孤兒檔救援**（27 期存檔跨版本護欄）：Main 讀檔完成後檢查——R / P route 旗標已設且 `ending_*_played` 未設 → 無視存檔場景 / 座標，直接 `transition_to("datacenter_backup_core", ...)` 從站 1 重演（結局不可懸空；27 期玩家確認後可自由走動存檔，該狀態在 28 落地後必須收斂）。
- 序列期間：世界移動 / TouchControls 凍結（演出拍慣例）；各 epilogue entry point 進場即 `SaveSystem.can_save_here = false`，序列全程不恢復。
- 站間推進：每站演出結束 → `travel`（24 既有 op / Main API）至下一站 epilogue entry point。
- 新增 epilogue entry points（4-E 慣例）：
  - `apartment_entrance:epilogue_wan`（R / P 共用落點；演出依 route 旗標分流）
  - `subway_station:epilogue_cen`（Protect not-B 中間站）
  - `underground_settlement:epilogue_settlement`（Protect Branch B 中間站）
  - `apartment:epilogue_home`（終站；演出拍後寫 `ending_reclaim_played` / `ending_protect_played` → 玩家凍結靜止＝30-A 接手點）

#### 28-A Reclaim

- 站 1 `datacenter_backup_core`：begin_message 多頁不可跳過（2-G 慣例；key `MSG_EPILOGUE_RECLAIM_P1..Pn`，內容回收五枚碎片旗標對應意象），末頁壓垮拍兩檔 key（`MSG_EPILOGUE_RECLAIM_CRUSH_HIGH` / `_LOW`）由 level code 依 `GameState.get_trace() >= RECLAIM_CRUSH_TRACE_THRESHOLD`（新常數，**獨立於 29 判定臨界**，開工校）選用；**零系統擴充**（message key 選擇在 code 側）。
- 站 2 `apartment_entrance:epilogue_wan`：晚自動觸發對話（26-A `npc_auto_dialogue_area` 複用）開樹 `wan_epilogue`（新 `data/dialogue/wan_epilogue.gd` 註冊 `dialogue_db.gd`；start 依 route 旗標分流 R / P 兩支）；Reclaim 支依 `affinity_wan >= 2` 條件路由長訣別 / 短冷版（5-A int 旗標 condition 既有）；結束 → **CG 拍**（GameUI 開 photo_viewer 路徑顯示 `wan/cg_walk_into_rain`，關閉即推進；直接用或抽薄 `cg_viewer` 變體實作定）→ CG 收掉後移除晚節點＝**永久消失** → travel 站 3。
- 站 3 `apartment:epilogue_home`：進場自動一拍 MessageBox（key `MSG_EPILOGUE_RECLAIM_TRACE`，晚留下的痕跡，文案實作時定稿）→ set `ending_reclaim_played` → 靜止。

#### 28-B Protect 主體

- 站 1 `datacenter_backup_core`：begin_message 一至兩頁（key `MSG_EPILOGUE_PROTECT_P1..P2`）；**短、安靜——與 Reclaim 多頁不對稱＝表達，不得加長**。
- 站 2 ＝ 28-C 分岔（見下）。
- 站 3 `apartment_entrance:epilogue_wan` Protect 支：拍板台詞固定三句（「你又變回來了。」／「變回什麼？」／晚看著雨，沒有回答）；**晚不 fade、不消失** → travel `apartment:epilogue_home` → set `ending_protect_played` → 靜止。

#### 28-C 中間站分岔

- 分岔判定：`cen_voiceprint_exposed`（24-D）——false → `subway_station:epilogue_cen`；true → `underground_settlement:epilogue_settlement`。
- 地鐵站（not-B）：**復駛套件**＝進場廣播一拍（key `MSG_EPILOGUE_SUBWAY_RESUME`）＋ `AmbientSubway` 遠轟層拉起（Phase 10 既有 bus）；小岑演出＝idle sprite 站位票閘旁 → **CG 拍**（`cen/cg_gate_pass`：假聲紋過掃描、綠燈打臉）＋拍板台詞「大叔，站那邊幹嘛？車要來了欸。」（key `MSG_EPILOGUE_CEN_PASS`；CG 顯示中或收掉後出，實作定）→ CG 收掉後移除小岑節點＝頭也不回走了 → travel 站 3。**不動兩張背景圖；月台室不經過；停運螢幕 examine 不做變體（全自動序列玩家無自由 examine）。**
- 聚落（B）：`empty_tent` 空帳篷位一拍 ＋ 伍姐 sprite 自動走近 → **沉默搖頭拍**（MessageBox key `MSG_EPILOGUE_WU_SILENT`；不開對話樹）→ travel 站 3。
- 復駛語意護欄：世界背景事件，與 route / 主角無關、不改聚落動線與安全狀態；僅此站可見。

#### 旗標（全走 `story_flags`，零新 schema）

| key | 型別 | 語意 | 寫入點 | 下游 |
|---|---|---|---|---|
| `ending_reclaim_played` | bool | Reclaim 演出已完成 | 站 3 `epilogue_home` 進場拍 | 30-A 共用收尾掛點 |
| `ending_protect_played` | bool | Protect 演出已完成 | 站 3 `epilogue_home` 進場拍 | 30-A 共用收尾掛點 |

只讀不寫：`ending_route_reclaim/protect`、`cen_voiceprint_exposed`、`trace`、`affinity_wan`、五枚碎片旗標。**Trace 不寫。**

#### i18n / 素材

- 新 MSG key（`data.csv` 三語）：`MSG_EPILOGUE_RECLAIM_P*` / `_CRUSH_HIGH` / `_CRUSH_LOW` / `_TRACE`、`MSG_EPILOGUE_PROTECT_P*`、`MSG_EPILOGUE_SUBWAY_RESUME`、`MSG_EPILOGUE_CEN_PASS`、`MSG_EPILOGUE_WU_SILENT`。
- 新對話樹 `wan_epilogue` 全文入 `dialogue.csv` 三語；小岑舞台化改句、晚 Protect 三句拍板句直接用；補白實作時定稿；名字不露出。
- 素材：情境圖 CG ×2——`assets/generated/sprites/cen/cg_gate_pass/`（小岑過閘綠燈）＋ `assets/generated/sprites/wan/cg_walk_into_rain/`（晚走進雨裡）；g2d 可生、**非 user blocker**。**正式形式非暫代**（2026-07-03 拍板）：升級只走「連續情境圖」加法（同顯示路徑、原圖續用），動畫化不列入規劃；**不需 walk sprite**。

#### test_runner（28-D，變數加 `_phase28` 後綴避免衝突）

- R / P 分派：route 旗標 → 對應序列站鏈；灌回 / 刪除演出 key 派發正確（頁數不對稱）。
- 染色：`trace` 高低兩檔 → CRUSH key 二選正確；`affinity_wan` 兩檔 → `wan_epilogue` 訣別分支路由正確。
- 28-C：`cen_voiceprint_exposed` 兩態 → 分站正確（subway / settlement）。
- 旗標：`ending_*_played` 寫入＋存讀 round-trip；序列各站 `can_save_here == false`；孤兒檔救援（route 已設未 played 的檔讀入 → 強制回 `datacenter_backup_core` 開序列）。
- 選擇前留檔 → 讀檔可分走 R / P（含 28-C 兩變體，共 3 條路）。
- Phase 1~27 不退化（尤其 27 三選 / 26 四旗標 / 24 分支旗標）。

### 存讀檔 / 回歸總則

- 每個 Phase 新增的旗標 / quest 狀態 / trace / 物品 / 場景座標**一律**納入 `to_save_dict` / `load_save_dict` / `reset_for_new_game`，並在該 Phase 末子階段做存讀檔 round-trip。
- `tests/manual/test_runner.gd` 逐 Phase 擴充：新節點 / 新 API 存在、新旗標跨存讀檔、戰鬥 / 分支 / 結局判定正確；**Phase 1~10 headless PASS 不得退化**。
- 戰鬥手感、跳躍手感、場景氛圍、結局演出「對不對」＝人工 GUI / 真機走查，headless 僅護欄（同 Phase 10 性質）。

---

## Phase M1 — 進度頁（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase M1`；操作清單見 `測試指南.md > Phase M1`。**Meta / 非敘事 QoL**，不佔故事編號（不動 Phase 19~31）。**核心：純加法**——新增 5 個收集分類的進度查詢，沿用筆記既有「左列表＋右詳情」UI 與觸控路由，不重造 UI 框架、不改既有 Phase 行為。

### 前提（硬規則）

1. **純加法**：不得改既有 Phase 1~18 的任何旗標語意 / UI 流程 / 存檔既有欄位語意；只新增欄位與唯讀查詢。
2. **判定＝曾達成，非當前持有**：場景 / NPC / 任務 / 殘響 / 特殊道具一旦達成永久打勾。特殊道具尤其——回執可賣、解碼方塊會放進插槽、手套會升級（`change_item_id`），都會離開背包，故必須在「獲得當下」寫入持久集合。
3. **防劇透**：未解鎖顯示 `???`，沿用殘響分頁慣例；遮罩在 UI 端做（資料層仍給真名，便於測試斷言）。

### 新增 GameState 狀態 + API

```gdscript
# game_state.gd 新增（三集合皆納入 to_save_dict / load_save_dict / reset_for_new_game）
var visited_scenes: Dictionary = {}          # set 語義：{ scene_id: true }
var talked_npcs: Dictionary = {}             # set 語義：{ dialogue_id: true }
var collected_special_items: Dictionary = {} # set 語義：{ item_id: true }
# left_apartment_once 走既有 story_flags（set_flag/get_flag），不另開欄位

const PROGRESS_SCENES := ["apartment","apartment_entrance","convenience_store","apartment_fire_escape","collector_shop","subway_station","subway_station_platform","underground_settlement","underground_settlement_right","tunnel_combat"]
const PROGRESS_NPCS := ["wan","store_robot","lu_qichen","cen","wu","seven"]
const PROGRESS_QUESTS := ["alley_backrooms_3f","repair_vendor_bot"]   # 加上 left_apartment_once 里程碑＝任務總數 3
const PROGRESS_ECHOES := ["echo_clerk","echo_room401_tenant","echo_song_rain_doesnt_stop","echo_lu_family","echo_settlement_erased"]
const PROGRESS_SPECIAL_ITEMS := ["early_ai_assistant_activation_box","old_ai_authorization_module","childcare_supply_receipt","old_probe_module","gleaner_gloves","clerk_echo_recording","decoder_cube"]

func mark_scene_visited(scene_id: String) -> void        # 冪等；白名單外仍記錄無妨，但 summary 只數白名單
func mark_npc_talked(dialogue_id: String) -> void        # 白名單外忽略
func _maybe_mark_special_item(item_id: String) -> void   # add_item / change_item_id 成功後呼叫；白名單命中才寫
func get_progress_summary() -> Dictionary                # 純讀，見下方
```

`get_progress_summary()` 回傳（純讀，不改狀態）：
```gdscript
{
  "scenes":  { "done": int, "total": 10, "items": [ {"id":String,"name":String,"done":bool}, ... ] },
  "npcs":    { "done": int, "total": 6,  "items": [...] },
  "quests":  { "done": int, "total": 3,  "items": [...] },
  "echoes":  { "done": int, "total": 5,  "items": [...] },
  "special": { "done": int, "total": 7,  "items": [...] },
  "overall_done": int, "overall_total": 31,
  "overall_pct": int   # round(overall_done * 100 / overall_total)
}
```
- 場景完成 = `visited_scenes.has(id)`；NPC = `talked_npcs.has(id)`；特殊道具 = `collected_special_items.has(id)`。
- 殘響 = `is_echo_complete(id)`（已採段數 == `EchoDB.get_segment_count(id)`；鹿家記事 `unknown_total` 取「採到現有段數即完成」）。
- 任務 done = `left_apartment_once`（`get_flag("left_apartment_once",false)`）＋ 各 `quest_states[id].status=="completed"`，total 固定 3。
- `items[].name`：已解鎖回真名（ITEMS_DB / EchoDB title / dialogue 顯示名 / SaveSystem 場景顯示名 / quest 模板標題 / 「離開公寓家」字面）；未解鎖由 UI 端改 `???`。

### 接線點（寫入時機）

| 集合 / 旗標 | 寫入點 | 說明 |
|---|---|---|
| `visited_scenes` | `main.gd transition_to()` 進場成功後 `GameState.mark_scene_visited(scene_id)` | 含戰鬥場 tunnel_combat；冪等 |
| `talked_npcs` | 對話 start 時（`GameUI.start_dialogue` 或 `DialogueRunner.start`）對白名單 dialogue_id 呼叫 `mark_npc_talked` | 排除 travel_street_* / store_registry_host |
| `left_apartment_once` | 第一次自公寓開門轉場 `apartment_entrance` 時 `set_flag("left_apartment_once",true)`（在 `main.transition_to` 偵測 apartment→apartment_entrance，或公寓大門 on_closed callback）| 走既有 story_flags |
| `collected_special_items` | `game_state.gd add_item()` 與 `change_item_id()` 成功後呼叫 `_maybe_mark_special_item(item_id)` | 白名單命中才寫；涵蓋解碼方塊 / 拾遺手套的 `change_item_id` 升級路徑 |

### UI（notebook_panel）

- `notebook_panel.gd`：分類列尾端加「進度」分頁；進入時左列固定 6 entry（總進度 / 場景 / NPC / 任務 / 殘響 / 特殊道具），沿用既有 W/S 選項 + A/D 翻頁 + 觸控路由。
- 選「總進度」→ 右詳情顯示五分類 `done/total` 五行 + 整體 `N%`。
- 選某分類 → 右詳情顯示 `分類 done/total` 標頭 + 逐項清單（已解鎖：真名 + ✓；未解鎖：`???`）。
- 進度頁為**唯讀展示**，不接 E/R/T 物品操作、不開 signal；開頁即時呼叫 `get_progress_summary()`。

### 新增 / 異動檔

```text
異動：
  scripts/autoload/game_state.gd          # 新增 3 集合 + 5 白名單常數 + mark_*/_maybe_mark_special_item/is_echo_complete/get_progress_summary + add_item/change_item_id hook + to_save_dict/load_save_dict/reset_for_new_game 納入
  scenes/main/main.gd                     # transition_to 進場 mark_scene_visited；偵測離開公寓 set left_apartment_once
  scenes/ui/notebook_panel.gd (+ .tscn)   # 「進度」分頁 + 總進度 / 分類詳情渲染（含 ??? 遮罩）
  scenes/ui/game_ui.gd / 對話接線          # 對話 start mark_npc_talked（白名單）
  tests/manual/test_runner.gd             # Phase M1 區（見測試指南）
（SaveSystem 不需改：payload 由 GameState.to_save_dict 自帶。）
```

### 存讀檔 / 邊界

- 三集合 + `left_apartment_once` 納入 `to_save_dict`/`load_save_dict`，缺鍵 → 空 Dictionary / false（沿用 Phase 6 缺鍵相容）；`reset_for_new_game` 清空。
- 舊存檔不 backfill：Phase 18 前的存檔進度偏低＝預期；列入已知邊界。
- `old_work_badge` 不在白名單（遊戲無取得點），即使 add 也不計入特殊道具。

---

## Phase M2 — 多國語系 i18n（實作契約）

> 驗收意圖見 `遊戲規格書.md > Phase M2`；操作清單見 `測試指南.md > Phase M2`。**Meta / 非敘事 QoL**，out-of-band 編號，不動 Phase 19~31。**機制＝Godot 原生 `tr()` + CSV 翻譯表**；三語言 `zh_TW`（預設+fallback）/ `zh_CN` / `en`；簡中與英文 LLM 獨立翻譯。**對玩家行為純加法**——只把顯示文字改為查表，不動邏輯 key / 旗標 / 存檔資料。

### 前提（硬規則）

1. **顯示與邏輯分離**：對話 / 互動的 **goto / effect / condition 永遠用既有 id / flag**；翻譯只動「顯示用字串」（speaker / text / label / prompt / 筆記 title·body / 物品 name·desc / message）。資料層的邏輯 key 不進 CSV、不被 `tr()`。
2. **fallback = zh_TW**：`TranslationServer` fallback locale 設 `zh_TW`；某語言缺某 key → 顯示繁中；三語全缺 → 顯示 key 本身（漏譯訊號）。
3. **語言偏好不入存檔槽**：存 `user://settings.cfg`，與 SaveSystem 完全隔離。
4. **行動相容**：字型 runtime 動態載入、只駐當前語言一個 CJK 字型；OS locale 首啟偵測。

### 新增 Autoload：`LocaleManager`（`scripts/autoload/locale_manager.gd`）

```gdscript
extends Node
# Autoload 名稱：LocaleManager（加入 project.godot autoload，排在 GameState 之後即可）

signal locale_changed(locale: String)

const SUPPORTED := ["zh_TW", "zh_CN", "en"]
const DEFAULT_LOCALE := "zh_TW"          # 同時為 TranslationServer fallback
const SETTINGS_PATH := "user://settings.cfg"

# 字型：en 沿用 TC 字型的 Latin 字形，不另帶第三個字型
const FONT_FOR := {
    "zh_TW": "res://assets/font/Noto_Sans_TC/static/NotoSansTC-Regular.ttf",
    "zh_CN": "res://assets/font/Noto_Sans_SC/static/NotoSansSC-Regular.ttf",  # M2-A 新增
    "en":    "res://assets/font/Noto_Sans_TC/static/NotoSansTC-Regular.ttf",
}

var current_locale: String = DEFAULT_LOCALE

func _ready() -> void:
    TranslationServer.set_fallback_locale(DEFAULT_LOCALE)   # 缺翻譯退繁中
    var saved := _load_saved_locale()                       # settings.cfg 有則用
    var initial := saved if saved != "" else detect_default_locale()
    set_locale(initial)

func set_locale(locale: String) -> void:
    if not SUPPORTED.has(locale):
        locale = DEFAULT_LOCALE
    current_locale = locale
    TranslationServer.set_locale(locale)
    _apply_font(locale)        # 動態載入該 locale 的字型、設為預設 theme font、釋放前一個
    _save_locale(locale)       # 寫 settings.cfg
    locale_changed.emit(locale)

func detect_default_locale() -> String:
    # OS.get_locale() 例：zh_TW / zh_CN / zh_Hans_CN / en_US…
    var sys := OS.get_locale()
    if sys.begins_with("zh"):
        if "Hans" in sys or "CN" in sys or "SG" in sys:
            return "zh_CN"
        return "zh_TW"        # zh_TW / zh_HK / zh_Hant 等歸繁中
    return "en"

func _apply_font(locale: String) -> void: ...   # 見「字型切換」
func _load_saved_locale() -> String: ...        # ConfigFile 讀 [i18n] locale；無檔/無鍵回 ""
func _save_locale(locale: String) -> void: ...  # ConfigFile 寫
```

- `GameState` / `UIMode` / `TouchControls` 維持 Autoload；`LocaleManager` 新增為第 4 個 Autoload。
- **不**把語言塞進 `GameState`（語言非遊戲狀態，是應用偏好）。

### project.godot 異動

```ini
[internationalization]
locale/translations=PackedStringArray(
    "res://locale/ui.zh_TW.translation", "res://locale/ui.zh_CN.translation", "res://locale/ui.en.translation",
    "res://locale/story.*", "res://locale/items.*", "res://locale/dialogue.*", "res://locale/data.*"   # 各域 CSV 匯入產物，分子階段陸續加入
)
locale/fallback="zh_TW"

[autoload]
LocaleManager="*res://scripts/autoload/locale_manager.gd"

# 移除/改寫：gui/theme/custom_font 不再 hardcode 單一 TTF；改由 LocaleManager runtime 設定（見下）
```

### 字型切換（行動記憶體關鍵）

- 現況：`project.godot` 寫死 `theme/custom_font = NotoSansTC`。改為 **runtime 設定預設 theme 字型**。
- `_apply_font(locale)`：
  1. `var f := load(FONT_FOR[locale]) as FontFile`（或 `FontFile.new(); f.load_dynamic_font(path)`）。
  2. 設為全域預設字型：`ThemeDB.fallback_font = f`（或專案根 Theme 的 `default_font`）。
  3. 釋放前一個 locale 的 CJK 字型參照，讓記憶體只駐當前一個 CJK 字型。
- en 與 zh_TW 共用 TC 字型 → 兩者間切換不重載字型。
- 字型檔放 `assets/font/Noto_Sans_SC/static/NotoSansSC-Regular.ttf`（M2-A 新增）。

### CSV 翻譯表（分域，每子階段加一檔）

- 位置：`locale/<domain>.csv`，Godot 匯入為 `.translation`（每 locale 一個產物）。
- 表頭固定：`keys,zh_TW,zh_CN,en`（第一欄 key，其餘為 locale 欄）。
- **一 key 一列**；三語同列，利 diff / 校稿 / 漏譯偵測。
- 分域檔（對應子階段）：`ui.csv` / `story.csv`（notes+messages）/ `items.csv` / `dialogue.csv` / `data.csv`（echoes+shops+quests）。

#### key 命名規則（機械可生）

| 域 | key 規則 | 例 |
|---|---|---|
| UI | `UI_<PANEL>_<ELEM>` | `UI_TITLE_NEW_GAME` / `UI_PAUSE_RESUME` / `UI_SETTINGS_LANGUAGE` |
| UI 格式字串 | 同上，值含 `%d`/`%s` 佔位 | `UI_NOTEBOOK_PAGE_FMT` = `第 %d / %d 頁`（en `Page %d of %d`）|
| 場景提示 | `PROMPT_<scene>_<interaction_id>` | `PROMPT_APARTMENT_DOOR_EXIT` |
| 筆記分類 | `CAT_<name>` | `CAT_WORK` / `CAT_IDENTITY` / `CAT_CLUE` / `CAT_ECHO` |
| 筆記 | `NOTE_<id>_TITLE` / `NOTE_<id>_BODY` | `NOTE_IDENTITY_GLEANER_TITLE` |
| 訊息 | `MSG_<id>` | `MSG_DOOR_LOCKED` |
| 物品 | `ITEM_<id>_NAME` / `ITEM_<id>_DESC` | `ITEM_FINGERLESS_GLOVES_NAME` |
| 對話內文 | `DLG_<tree>_<node>_TEXT` | `DLG_CEN_FIRST_MEET_TEXT` |
| 對話選項 | `DLG_<tree>_<node>_CHOICE<n>` | `DLG_CEN_FIRST_MEET_CHOICE0` |
| 說話者 | `SPEAKER_<name>`（共用，跨 node 不重列）| `SPEAKER_CEN` = `小岑` |
| 殘響 | `ECHO_<id>_TITLE` / `ECHO_<id>_BODY` | `ECHO_CLERK_TITLE` |
| 商店 / 任務 | `SHOP_*` / `QUEST_<id>_STEP<n>` 等 | `QUEST_REPAIR_VENDOR_BOT_TITLE` |

#### 抽取工具（一次性、機械化）

- 寫一支腳本（Python venv 或 Godot tool script）走訪 `game_state.gd` 的 const dict、`data/**/*.gd` 的 TREE、`scenes/**/*.tscn` 的 `prompt_text`、UI 腳本字面量：
  - 產出 CSV `zh_TW` 欄（現有中文），`zh_CN`/`en` 留空待譯。
  - **改寫源碼**：把資料 dict 的 inline 顯示文字換成對應 key（邏輯 key 不動）；UI 字面量包成 `tr("KEY")`。
  - 標記含 `%` 佔位的字串供人工確認語序。
- 此工具屬建置輔助，不進遊戲 runtime。

### 顯示端接 `tr()`（view 層，runner / data 保持純）

- **DialogueRunner 不變**（純邏輯，operate on id/flag）。資料 TREE 的 `text`/`label`/`speaker` 改存 key。
- **DialoguePanel**：渲染時 `tr(node_text_key)`、`tr(choice_label_key)`、`tr(speaker_key)`。
- **notebook_panel**：標題 `tr(NOTE_*_TITLE)`、內文 `tr(NOTE_*_BODY)`、分類 `tr(CAT_*)`；含進度頁分類名（M1）一併 key 化。
- **MessageBox / FloatingToast / ItemDetailModal / ShopPanel / PhotoViewer / 各 footer hint**：show 時對 key `tr()`；格式字串 `tr(key) % [args]`（先 `tr` 再 `%`，因英文語序可能不同）。
- **互動分派**：`interactable_area.gd` 的 `prompt_text` 改存 key；場景 controller 顯示時 `tr(prompt_key)`。`message_id` 既走 STORY_MESSAGES → 改 `tr("MSG_"+id)`。
- **Control 自動翻譯**：純靜態 Label/Button 文字（標題、暫停選單固定字）可直接在 `.tscn` 填 key，靠 Godot auto-translate；動態組裝字串才手動 `tr()`。

### 即時切換 / live refresh

- `LocaleManager.locale_changed` → 已開啟的常駐 UI（GameUI HUD、開著的 panel、設定頁自身）連此 signal 重新渲染當前內容。
- 多數 panel 開啟時才 render → 天然吃到新 locale；只有「切語言當下正開著的頁」需 refresh hook。保持輕量，不做全域重建。

### 設定頁（新增）

- `scenes/ui/settings_panel.tscn` / `.gd`：最小內容＝語言三選一（繁中 / 簡中 / 英文）+ 返回；鍵盤（上下選、E 確認）+ 觸控可操作。
- **入口**：
  - 標題畫面：`title_screen` 加「設定」按鈕，沿用既有 `save_slot_list` 子面板顯隱模式（overlay，不換場景）。
  - 暫停選單：`pause_menu` 加「設定」項；遊戲中開啟 gate `UIMode==NONE`（沿用暫停選單既有 gating）。
- 選語言即呼叫 `LocaleManager.set_locale(locale)`；設定頁自身連 `locale_changed` 即時換字。
- TouchControls：設定頁沿用既有面板觸控路由（方向移焦 + E 確認 + 返回）。

### 子階段拆分（依賴鏈 M2-A → B → C → D → E）

| 子階段 | 範圍 | 產出 / 驗收 |
|---|---|---|
| **M2-A 基礎建設** | `LocaleManager` autoload、`TranslationServer` 接線、fallback=zh_TW、`NotoSansSC` 字型納入 + runtime 動態切換、`settings.cfg` 讀寫、OS locale 首啟偵測、**最小設定頁**（標題+暫停入口）。先建一張 `ui.csv` 含**少量** key（設定頁自身 + 標題 + 暫停）即可驗證三語切換 + 字型切換閉環。 | headless：set_locale 改 TranslationServer locale、settings.cfg round-trip、detect_default 映射、字型 resource 載入、fallback 行為 |
| **M2-B UI chrome** | 13 UI 腳本 178 處 + 12 `.tscn` `prompt_text` 全抽 key→`ui.csv`，三語填；Control auto-translate / 手動 `tr()` 接上；格式字串佔位處理 | UI 三語顯示、無漏字、英文不破版 |
| **M2-C 敘事資料** | `STORY_NOTES`（title/body）+ `STORY_MESSAGES` + `ITEMS_DB`（name/desc）+ 分類名 → `story.csv` / `items.csv`，三語 | 筆記 / 訊息 / 物品三語 |
| **M2-D 對話樹** | 9 棵樹造 key（text/choice/speaker）→ `dialogue.csv`，三語；DialoguePanel 走 `tr()`；**鎖定英文人名音譯對照表**（全遊戲統一）；三語禁字檢查（無「林霏」）| 9 樹三語、分支 / effect 不退化、人名一致 |
| **M2-E 收尾** | `data/echoes`·`shops`·`quests` → `data.csv` 三語；全鏈 headless 回歸；三語 GUI / 純觸控走查；**真機（iOS/Android）字型 + 切換 + OS locale 偵測驗收** | 全鏈 headless PASS + 真機 |

### i18n 驗證網 / 覆蓋把關（機械化，分階段累加）

> **動機**：i18n 是橫切改動，錯誤對人眼隱形——繁簡一眼難辨、漏字只在非預設語言才現形、漏抽只在切語言才露。M2-A/B 純靠 GUI 走查已證實會漏網（zh_CN 欄殘留繁體字、`game_ui` toast / `shop_panel` footer 裸字串未抽）。故**每子階段交付前必跑以下 headless 機械閘**（寫進 `tests/manual/test_runner.gd`），GUI / 真機走查只作補充。

三組檢查，守不同方向；缺一不可（品質檢查只驗「已存在的列乾不乾淨」，覆蓋檢查才驗「該有的列在不在」）：

| 組 | 真相來源 | 抓什麼 | 落地時機 |
|---|---|---|---|
| **品質 (a)(b)(c)** | CSV 自己 | (a) 三語欄皆非空；(b) CSV ↔ `.translation` 同步（改了 CSV 忘 reimport）；(c) zh_CN 欄無繁體字洩漏 | M2-A/B 建好；之後所有新列自動套用 |
| **覆蓋① (i) 靜態字面量** | 程式裡的字面量 | `tr("literal")` / `.tscn prompt_text` 用到、CSV 卻缺該列 | **M2-B 收尾一次建好**（UI 字面量已凍結，範圍最小） |
| **覆蓋② (ii) 資料驅動** | 資料字典的 key | 走訪 dict／TREE 枚舉每個顯示欄位的 key，斷言 CSV 有對應列；可抓動態 `tr(note.title)` 與「整欄忘了 key 化」 | **逐 domain 增量**：C 加 story/items、D 加 dialogue、E 加 data；永久累加跑 |

- **(ii) 的前提＝顯示欄位契約**：要抓「忘了 key 化的整欄」，lint 需一份**獨立於實作者選擇**的「每資料結構顯示欄位清單」（哪些欄是顯示文字＝必須 key+CSV、哪些是邏輯＝不准動）。各 domain 開工時在本文件對應契約段 pin，**不提前寫**——資料未 key 化前無對象可查。key 形狀沿用上方「key 命名規則」表。
- **已知缺口（裸 CJK 未包 `tr()`）**：(i) 掃 `tr("…")`，看不到**從未包進 `tr()`** 的裸中文（`game_ui` toast 即此型）；(a)(b)(c) 與 (ii) 也看不到。目前靠 GUI 走查補。是否加第四道輕量掃描——「view 層 `.gd`/`.tscn` 出現 CJK 字元，但不在註解 / `tr()` / CSV 綁定內 → FAIL」——**待 user 拍板**（會有註解、debug `print` 誤報，需維護排除清單）。

#### 後續 phase 常規則（i18n 上線後的長期義務）

i18n 一旦上線即**非「做完的 phase」，而是對所有後續含文字 phase 的一筆稅**：

- **DoD 增列**：任何後續 phase（含 Phase 19+ 故事節拍）新增顯示文字時，「完成」定義增列「補 key + 三語 CSV 列（zh_TW 寫源、zh_CN/en 翻譯）」。
- **(ii) 自動接住，不需重寫**：往既有資料結構加新內容（新對話 node、新筆記、新物品…），既有覆蓋斷言自動枚舉到 → 缺 CSV 列即 FAIL；(a)(b)(c) 品質閘亦自動套用新列。
- **lint 保證範圍**：只保證「有填、非空、非繁體洩漏」，**不保證翻譯品質**——新內容 zh_CN/en 仍需 LLM 翻譯 + 人工校稿。
- **唯一需擴充 (ii) 的情況**：引入**全新資料結構**（M2 時不存在的新 dict／新顯示欄位類型）時，一次性把其顯示欄位契約 + 枚舉加入 (ii)（等同新增一 domain）；沿用既有結構加內容則不需。

### 新增 / 異動檔（總覽）

```text
新增：
  scripts/autoload/locale_manager.gd          # Autoload：locale 狀態 / 字型切換 / settings.cfg / OS 偵測
  scenes/ui/settings_panel.tscn + .gd         # 語言設定頁
  assets/font/Noto_Sans_SC/...NotoSansSC-Regular.ttf
  locale/ui.csv / story.csv / items.csv / dialogue.csv / data.csv（+ 匯入產物 .translation）
  tools/i18n_extract.py（或 .gd tool）        # 一次性抽取 / 改寫輔助（不進 runtime）
異動：
  project.godot                               # [internationalization] translations + fallback；[autoload] LocaleManager；移除 hardcode custom_font
  scenes/ui/title_screen.* / pause_menu.*     # 設定入口
  scenes/ui/notebook_panel / dialogue_panel / shop_panel / item_detail_modal / floating_toast / message box / footer hints  # 顯示端 tr()
  scripts/components/interactable_area.gd     # prompt_text 改存 key
  scenes/levels/**/*.tscn（12）               # prompt_text 改 key
  data/dialogue/*.gd（9）/ data/echoes·shops·quests  # inline 顯示文字改 key
  scripts/autoload/game_state.gd              # STORY_NOTES/STORY_MESSAGES/ITEMS_DB 顯示文字改 key（dict 結構不變、邏輯 id 不動）
  scenes/ui/touch_controls.gd                 # 設定頁觸控路由（沿用面板模式）
  tests/manual/test_runner.gd                 # Phase M2 區（見測試指南）
（SaveSystem 不改：語言不入存檔。）
```

### 存讀檔 / 邊界

- 語言偏好存 `user://settings.cfg`（`[i18n] locale=`），與存檔槽隔離；無檔 → OS 偵測 → 預設。
- 缺翻譯 → fallback zh_TW；三語全缺 → 顯示 key（開發期漏譯訊號）。
- 對玩家邏輯純加法：Phase 1~19 / M1 的旗標 / 任務 / 對話分支 / 存檔資料不變，headless 不得退化。
- 已接受邊界：簡中字型 +~10–16MB 包體；不做 RTL / 配音 / 其他語言。
