# 驗證後已知問題

本文件追蹤驗收/實測發現的待修問題、已修復問題，以及已接受的邊界決定。

- **待修**：已定位 root cause、已決定處理方向，但尚未動手修的問題。
- **已修復**：曾列為待修，後續已完成修正並保留 root cause / 驗收方向供回查的問題。
- **已接受邊界**：明知如此、但本階段刻意不處理的取捨。

格式：每個問題一節，標題以 phase prefix 命名（例如 `8-H：...`），含「現象 / Root cause / 影響範圍 / 處理方式 / 狀態」。phase prefix 使用最新正在進行或已完成的 phase；不使用 `KI-###` 流水號。

建立日期：2026-06-06（Phase 4-F 期間，第一筆為跨場景回公寓狀態重置）。

---

## 待修

（暫無）

---

## 已修復

### 25-C：24-C tunnel_chase 兩室 Player 缺少 AnimatedSprite2D / CollisionShape2D 子節點（隨 25-C 順手修復、commit message 未記錄，特此補記）

- **發現時間**：2026-07-02，Phase 25 驗證 code review 時發現該修復被夾帶在 25-C commit `4dcb28b` 內出貨。
- **狀態**：✅ 已修復（commit `4dcb28b`），並由 25-C 回歸測試（`tunnel_chase` / `tunnel_chase_right` 載入）覆蓋。
- **嚴重度**：中。屬 Phase 24-C 遺留缺陷。

#### 現象

1. `tunnel_chase.tscn` / `tunnel_chase_right.tscn` 的 Player 節點（掛 `player.gd`）原本沒有 `AnimatedSprite2D` 與 `CollisionShape2D` 子節點。
2. `player.gd` 的 `@onready var anim: AnimatedSprite2D = $AnimatedSprite2D` 取得 null，`_ready()` / `_physics_process()` 內 `anim.play(...)` 每 frame 觸發 script error；GUI 下主角在兩個追逐房間無圖可見。

#### Root cause

24-C 建場景時 tscn 已定義 `SpriteFrames_player` / `RectangleShape2D_player` sub_resource，但漏掉把兩個子節點掛到 Player 下；當時 headless 測試只驗追逐邏輯（倒數 / 選門 / 攤牌），未斷言 Player sprite 節點存在，故未攔下。

#### 影響範圍

- 直接：Phase 24-C 兩個追逐房間的視覺與 runtime error 噪音。
- 不受影響：追逐倒數、錯誤口扣秒、真出口骰選、24-B 攤牌分支判定（皆不依賴 Player sprite）。

#### 處理方式

25-C（commit `4dcb28b`）已在兩個 tscn 的 Player 下補回 `AnimatedSprite2D`（引用既有 `SpriteFrames_player`）與 `CollisionShape2D`（引用既有 `RectangleShape2D_player`）。此修復屬 Phase 24 範圍、卻隨 Phase 25 commit 出貨且 commit message 未記錄，本節為事後補記。

#### 驗收方向

- headless：`tunnel_chase` / `tunnel_chase_right` 載入 + Phase 24-C 測試不退化（2026-07-02 已 PASS）。
- GUI：Phase 25 走查時順帶目視兩個追逐房間主角 sprite 正常顯示。

---

### 8-H：發現販賣機錯誤後缺少「回電腦接案」前置線索筆記

- **發現時間**：2026-06-11，Phase 8 規格補強。
- **狀態**：✅ 已修復並驗收完成（在 GameState 聚合兩台互動時，同步寫入一次性線索筆記 `clue_vendor_error_lead`）。
- **嚴重度**：中低。主線不一定阻斷，但玩家在外面販賣機與店內機器人都談過後，狀態已可回房間電腦接任務，卻缺少筆記層提示，容易不知道下一步該回電腦確認工作。

#### 現象

1. 玩家先後與便利商店外販賣機、商店內機器人互動。
2. `talked_outside_vendor` 與 `talked_store_robot` 皆成立，`discovered_vendor_error` 也成立。
3. 玩家已符合回公寓電腦觸發 `repair_vendor_bot` 派工的前置條件，但筆記 UI「線索」分類沒有新增任何提示。

#### Root cause

Phase 8-B 目前只把兩台互動結果聚合成 `discovered_vendor_error`，缺少同一時刻寫入前置線索筆記的規格與實作。這讓「狀態已開門」與「玩家理解下一步」之間少了一個敘事提示。

#### 影響範圍

- 直接：玩家可能不知道要回房間電腦查看工作 App。
- 不受影響：`discovered_vendor_error` gate、`used_room_computer_once` gate、`QuestManager.start("repair_vendor_bot")` 本身的派工條件。

#### 處理方式

在 `discovered_vendor_error` 首次成立時，同步新增一筆 `category == "線索"` 的一次性筆記，暫定 id `clue_vendor_error_lead`。筆記內容只暗示「這不像單一設備故障，可能會是主角可接的 AI 善後工作；可回房間電腦確認」，不提前透露任務細節。單談其中一台不可新增；重複互動不可重複新增。

#### 驗收方向

- 只談外販賣機或只談店內機器人：不設 `discovered_vendor_error`，不新增 `clue_vendor_error_lead`。
- 兩台都談過：設 `discovered_vendor_error`，筆記 UI「線索」分類新增 `clue_vendor_error_lead`。
- 重複互動：不重複新增筆記。
- 存檔後讀檔：該線索筆記保留。

---


### 4-F：從街道回公寓時，公寓狀態被重置（門重新上鎖、櫥櫃重複塞物）

- **發現時間**：2026-06-06，Phase 4-F 雙向轉場實測。
- **狀態**：✅ 已修復（採用方法 B：門改問持久狀態 `GameState`、世界鋪設用 `apartment_initialized` 守衛）。
- **嚴重度**：高。破壞跨場景遊玩的基本一致性，玩家解完公寓謎題出門後回來會發現門又鎖上，無法理解。

#### 現象

1. 玩家完成公寓解謎鏈（B0–B9），開大門轉場到 `street_stub`。
2. 在街道按 E（`back_to_apartment`）轉回 `apartment:from_street`。
3. 回到公寓後，再走到大門按 E，門顯示「鎖著」(`door_locked`)，彷彿謎題沒解過。
   - 預期：門應維持已解鎖狀態，公寓整體狀態與離開前一致。

#### Root cause

Phase 4 架構為「離開公寓 → 公寓節點 `queue_free`；回來時重新 `instantiate` 全新公寓實例」。問題出在**門的開鎖判斷讀的是 per-instance 區域旗標，而非持久狀態**：

- 門 gate 條件在 [`scenes/levels/apartment/apartment_room.gd:315`](scenes/levels/apartment/apartment_room.gd)：
  ```gdscript
  "door_exit":
      if _slot_unlock_sequence_started:   # ← 讀的是區域旗標
          ...開門 + 轉場...
      else:
          ...顯示「門鎖著」...
  ```
- `_slot_unlock_sequence_started` 宣告於 [`:86`](scenes/levels/apartment/apartment_room.gd)（預設 `false`），只在本實例把方塊插入插槽（B8, [`:461-462`](scenes/levels/apartment/apartment_room.gd)）時被設為 `true`。
- 新實例的此旗標必然 `false`，於是判定「門鎖著」。
- 真正的持久解鎖狀態 `GameState.has_knowledge("identity_door_unlock_method")` **有正確保留**（GameState 為 Autoload，跨場景持久），但門沒去問它。

**一句話：門問錯了人——該問持久的 `GameState`，卻問了每次重生都歸零的區域旗標。**

#### 同源的第二顆雷（連帶處理）

`apartment_room.gd._ready()`（[`:91-133`](scenes/levels/apartment/apartment_room.gd)）把「新遊戲鋪世界」的初始化放在**每次場景載入**都跑，但 Phase 4 後 level `_ready()` 會在每次重進場執行：

- `configure_container(...)` — **冪等**，有 `if not has(container_id)` 守衛（[`game_state.gd:454`](scripts/autoload/game_state.gd)），重進不會重建。✅
- `seed_container(...)` — **無守衛**（[`game_state.gd:699`](scripts/autoload/game_state.gd)），無條件往櫥櫃塞物。⇒ **每次回公寓，櫥櫃會被多塞一顆魔術方塊 + 夾克 + 罐頭**，連已用掉/拿走的都復活。❌
- `add_knowledge(...)` — 冪等（依 id 更新，[`game_state.gd:235-244`](scripts/autoload/game_state.gd)）。✅
- inventory 補發 — 有「背包全空才補」守衛（[`:103-108`](scenes/levels/apartment/apartment_room.gd)），大致無害但粗糙。⚠️

根本問題同源：**Phase 4 之後，「一局只該做一次」的初始化不該綁在 level `_ready()`。**

#### 影響範圍

- 直接：公寓門在二次進場後誤判上鎖（玩家可見、阻斷體驗）。
- 連帶：櫥櫃容器每次回公寓重複堆疊種子物品（解謎物/食物復活，可能破壞背包經濟與謎題唯一性）。
- 不受影響：筆記/知識（冪等）、容器設定（冪等）。

#### 處理方式（方法 B：修門 + 世界鋪設改為「一局一次」）

1. **門改讀持久狀態**：將 [`apartment_room.gd:315`](scenes/levels/apartment/apartment_room.gd) 的 gate 條件由 `_slot_unlock_sequence_started` 改為 `GameState.has_knowledge("identity_door_unlock_method")`。
2. **世界鋪設一局一次**：在 `GameState` 新增一個持久旗標（暫定 `apartment_initialized: bool`）；把 `_ready()` 中的 `seed_container(...)` 與 inventory 補發包進 `if not GameState.apartment_initialized:`，跑完設為 `true`。
   - `configure_container` 已冪等，可留在外面或一併納入守衛皆可。
   - 此設計順著未來 SaveSystem 方向：「新遊戲種子」本就不該綁 level 載入。
3. **不採用**的替代：A（只修門）治標不治本，留下櫥櫃重塞；C（把 seeding 整包移到 main.gd 新遊戲初始化）最乾淨但動面廣，超出 4-F stub 範圍，留待正式做存檔/新遊戲流程時再做。

#### 待確認的附帶決策（修動前向使用者敲定）

- 改讀 `has_knowledge` 後，二次走出公寓會**重播大門轉場演出與「Beyond the Door」BGM stinger**（`_beyond_door_bgm_triggered` 亦為 per-instance，重生歸零）。通常合理；若希望「第二次之後安靜直接出門」，需另加持久旗標抑制。

#### 驗收方向（修完後）

- 解謎開門 → 出街道 → 回公寓 → 再按大門：應直接開門/轉場，不再顯示鎖著。
- 來回多次後，櫥櫃內種子物品數量不增長。
- 背包、筆記、裝備、credits 跨來回保持一致。
- headless 回歸：`test_runner.tscn` / `verify_game_state.gd` 仍 PASS。

---

## 已接受邊界

（暫無）
